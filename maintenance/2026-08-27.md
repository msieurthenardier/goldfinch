# Maintenance Report: 2026-08-27

**Date**: 2026-08-27
**Triggered by**: [Mission: Search and Startup Choice](../missions/16-search-and-startup-choice/mission.md)
**Assessment**: Maintenance Required

*Third maintenance cycle, and the first full-category sweep since the
2026-06-05 cold baseline — the 2026-07-11 cycle was operator-scoped to the
Mission 06 debt ledger and explicitly left categories 1–10 unrefreshed. Nine
missions (08 history → 16 search/welcome) landed in between. Tree inspected:
v0.14.0, HEAD `0cd2e3b` on `mission/16-debrief-methodology-disposition`
(1 ahead / 2 behind `origin/main`, clean).*

## Inspection Scope and Delegation

- **Scope**: full codebase (~186 source files, 54k lines; 221 test files),
  categories 1–7, 9, 10, Accessibility, plus — at the operator's request —
  a triage of all 20 open GitHub issues against the current tree. Category 8
  (CI/CD) was left out of this cycle by operator choice.
- **Delegation** (two waves, seven agents, then Architect + roundtable):
  - Wave 1: Inspector tool pass (Sonnet — suite ×2, lint, typecheck, audit,
    outdated, licenses, git/remote, env vars); Security Reviewer (Opus —
    `src/main/`, `automation/`, `vault/`, preloads, `goldfinch://` gating);
    Issue Triage Reviewer (each open issue read via REST, verified against
    missions/squawks/BACKLOG/tree); Accessibility Reviewer (Sonnet — static
    WCAG 2.1 AA over chrome + internal pages, with #174 validated first).
  - Wave 2: two manual Inspectors (Sonnet) partitioned backend
    (`src/main`, `src/shared`, `src/preload`, `scripts/`, docs) and renderer
    (`src/renderer/`, `test/unit/`), each seeded with the tool-pass leads.
  - Architect (Opus) assessed 57 consolidated findings, ruled directly on
    three reviewer disagreements, and raised four challenges; one roundtable
    cycle (three rebuttals) resolved all of them — two in the reviewers'
    favour on new evidence, one as a clarification. No unresolved
    disagreements.
- **Effectiveness notes for future cycles**:
  - The backend/renderer partition worked; the two agents' overlap surfaced
    two genuine disagreements (both resolved by the Architect reading the
    code), which a single Inspector would have silently picked one side of.
  - The issue-triage agent was the highest-value add-on of the cycle: it
    found three delivered-but-open issues, one issue (#81) whose blocking
    decision had already been made by a later mission, and reconciled four
    trackers. Worth making a standing member of the crew.
  - The roundtable changed the headline: Accessibility's rebuttal moved
    #174 from Action Required to Critical on evidence the Architect verified
    (and corrected in one detail — see F48); the renderer Inspector's
    sheet-coverage table turned a "merge into advisory" proposal into a new
    flight. Challenges that ask for *specific* evidence pay for themselves.
  - Sonnet Inspectors mis-called two known-debt items as done by reading a
    doc cite instead of the code (`tearoff-overlay-manager.js`; the M16
    cache-sync item). Future prompts should require a `file:line` in the
    code, not a doc, for any "done" claim.
  - The tool-pass agent's "no same-named test file" heuristic produced
    three false positives out of seven — fine as a lead list, never as a
    finding.

## Categories Inspected

1. Security (automated + manual review)
2. Test Systems (+ test-quality review)
3. Dependency Health
4. Code Quality
5. Type & API Safety
6. Documentation
7. Git & Branch Hygiene
9. Infrastructure & Config
10. Performance & Resource Cleanup
— Accessibility (WCAG 2.1 AA, static)
— Open GitHub issues (triage against the tree)

*Not inspected this cycle: 8. CI/CD Pipeline (operator choice).*

## Executive Summary

Structurally the codebase is in good health: 3792/3792 tests in ~3.4 s with
no flakes (the M16 "+18 % suite timing" watch item is **disproven** — today
is faster than M16 F2), lint/typecheck/audit clean, zero TODOs, no deprecated
Electron APIs, `main.js` down 2724 → 2014 lines since July, resource
discipline verified, and the security core (IPC sender gating, `goldfinch://`
serving, MCP transport auth, vault crypto, permission allowlist) verified
sound across twenty-plus invariants. What accumulated over nine missions is
four things: **a WCAG Level-A keyboard exclusion from page content (#174,
Critical)**; guard drift on the automation surface that lets the admin tier
read and drive internal pages including the vault manager, contradicting a
documented invariant; a 2666-line sheet layer where 17 of 19 sheet lifecycles
have no executable verification of any kind; and a documentation-and-ledger
deficit (four owed CLAUDE.md subsections; ≥9 debrief action items done but
unticked) that is now the input to every future sweep. A four-flight
maintenance mission is scaffolded for the items needing design; 24
squawk-sized items are logged as squawks 0016–0039; the GitHub issue review
closes three delivered issues and sequences the rest.

## Findings by Category

Finding numbers (F1–F57) are the Architect's; the digest and roundtable
record are in the session transcript. Squawk ids are noted where logged.

### Category 1: Security

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F1 | Admin MCP tier can drive and read internal `goldfinch://` pages, incl. the vault manager | Action Required | New | Mission 17 Flight 2 |
| F9 | Vault tools rebuild target resolution and drop the sheet/popup resolver guards | Action Required | New | Mission 17 Flight 2 |
| F2 | `manager.json` KDF params trusted unvalidated on read | Action Required | New | Mission 17 Flight 4 |
| F8 | Fresh-profile vault import adopts a foreign recovery key + admin seal | Action Required | New | Mission 17 Flight 4 |
| F4 | No rate limit on MCP tool calls — `vaultUnlock` is a loopback oracle | Action Required | New | Deferred to next cycle (policy-shaped) |
| F3 | Jar color interpolated raw into a `style` attr via `innerHTML` (chrome) | Advisory | New | Squawk 0020 |
| F5 | Vault clipboard copy never cleared | Advisory | New | Squawk 0021 |
| F6 | Rotation is re-wrap-only; MRK never re-keyed — threat-model doc omits it | Advisory | New | Squawk 0022 (doc); MRK re-key = backlog |
| F7 | No anti-rollback / vaultId binding in vault AAD | Advisory | New | Backlog (AAD change = migration) |
| F10a | `internal-settings-set` has no key allowlist (can write automation key hashes) | Advisory | New | Fold into Flight 2 if cheap; else next cycle |
| F10b | `tab-create` does not validate `url`; `trusted` renderer-supplied | Advisory | New | Next cycle |
| F10c | `download-media` trusts renderer-supplied `webContentsId` (reopens DD6 leak) | Advisory | New | Mission 17 Flight 2 (trust-boundary change) |
| F10d | Downloads `retry` rides the default session, not the jar | Advisory | New | Next cycle |
| F10e | `shell.openPath` on downloads with no dangerous-type warning | Advisory | New | Backlog |
| F10f | Farbling removable via `delete navigator.hardwareConcurrency` (`configurable:true`) | Advisory | New → #147 | Evidence for the #147 mission |
| F10g | Vault fill-icon is a page-readable oracle (jar kind, live lock state) | Advisory | New | Backlog |
| F10h | Audit log records full URLs incl. query strings | Advisory | New | Squawk 0023 |
| F10i | `storage-access` / `top-level-storage-access` / `window-management` auto-granted | Advisory | New | Next cycle / #147 |
| F10j | `npm run dev:automation` passes `--no-sandbox` while binding the server | Advisory | New | Dev-only; document |
| F10k | `docs/mcp-automation.md` over-claims (`:42`, `:344-373`, `:686`) | Advisory | New | Mission 17 Flight 2 (closing step) |
| F10l | `escapeHtml` (`context.js:50-53`) doesn't escape `'` — safe at every call site | Advisory | New | Document or fix opportunistically |
| F10m | Electron declared with a caret range | Advisory | New | Squawk 0037 |
| F11 | Security verified-OK inventory | Pass | — | — |

**Details:**

- **F1** *(fix shape superseded by the Flight 2 pivot 2026-08-28: the admin tier is now ALLOWED to reach internal guests — a high-bar, key-gated, loopback-bound tier, NOT dev-only; the non-admin wall this worried about already holds at the resolver — see the flight Design Decisions, DD2 correction)* — `src/main/automation/engine.js:167` sets `allowInternal:true`
  for the admin tier, so the resolver's internal-session refusal at
  `resolve.js:215` never fires. The compensating control is an op-local
  `isInternalContents` refusal — present on `evaluate`/`injectScript`/
  `openDevTools`/`closeDevTools` in `observe.js`, on all of `nav.js`,
  `zoom.js`, `print.js`, `find.js` — but **absent** from `input.js`
  (`click:284`, `typeText:294`, `scroll:364`, `pressKey:396`) and from
  `observe.js` `readDom:286`, `readAxTree:388`, `captureScreenshot:194`.
  `observe.js:452` states the intended invariant ("internal-session
  exclusion EVEN FOR ADMIN"); `docs/mcp-automation.md:42` asserts it.
  Attack: an admin-key holder targets the `goldfinch://vault` tab, clicks a
  reveal control, `readDom`s plaintext — bypassing the vault tools'
  access-key model. Severity note: the admin key is already highly
  privileged (loopback + `GOLDFINCH_AUTOMATION_ADMIN`), so this is Action
  Required rather than Critical — but it contradicts a documented invariant
  and reaches vault plaintext.
- **F9** — `src/main/vault/vault-context.js:398` builds resolver deps
  without `isSheetContents`/`sheetMenuFor`/`isTabViewWcId`/`isPopupWcId`;
  `main.js:1024-1032` never supplies them, so the `typeof`-gated checks at
  `resolve.js:197` and `:231` no-op. An admin session can name the
  master-password sheet or the find overlay as a `vaultFill` target; only
  the origin match (`vault-context.js:456`) still refuses. Same root cause
  as F1: guard drift between the resolver and its callers.
- **F2** — `vault-store.js:378-380` checks only `typeof doc.kdf ===
  'object'`; the params feed scrypt at `:666,:755,:786,:825,:871,:1577`.
  `validateImportedKdf` (`:216`) is only called on the import path
  (`:975`). With vault-file write access an attacker downgrades the KDF
  (the recovery path re-wraps under the weak params — a silent, permanent
  downgrade) or DoSes the main process with an absurd `N`. At-rest
  encryption exists precisely to survive file-write, so this is in the
  threat model. Fix shape: call `validateImportedKdf` from `_readManager`;
  the flight owes a ruling on legacy-params compatibility.
- **F8** — `vault-store.js:1023-1033` writes `bundle.mrk.recovery` /
  `bundle.mrk.admin` verbatim as a fresh profile's manager: a user talked
  into importing an attacker-built bundle has a recovery key and admin
  seal the attacker holds, and (F6) rotation never re-keys the MRK. Fix
  shape: force `rotateRecovery` + `rotateAdminKey` after a fresh adopt
  before the profile is usable.
- **F4** — `mcp-server.js:519-543` CallTool has no counter or delay (only
  `MAX_BODY_BYTES:217`, `MAX_SESSIONS:222`); `vault-context.js:257`.
  Deferred: bucket sizing, backoff curve and lockout UX are policy
  decisions, and the loopback + Host/Origin/peer guard + hashed-key model
  bounds exposure meanwhile.
- **F6** (roundtable-resolved) — `docs/vault.md:342-344,127` is honest that
  rotations rewrite one `manager.json` slot and never re-key the MRK, and
  admin/recovery rotation *is* real revocation for the online path
  (`vault-store.js:1691` `openAdminSeal`; `:826-827`). The gap is the
  `:426-433` "does NOT protect against" list omitting "an already-extracted
  MRK survives every rotation", and `:351` "the prior admin key is
  invalidated" over-claiming. One doc bullet + one wording change (squawk
  0022); compromise-mode rotation is a backlog feature because it inverts
  the single-slot rotation invariant the vault store is built on.
- **F11 verified OK** — every `ipcMain` channel reachable only through a
  named `contextBridge` method; guests cannot reach IPC; sender identity
  from `event.sender`, never the payload; `goldfinch://` serving is
  traversal-proof by construction (exact host+path map,
  `internal-page-map.js`, `internal-assets.js`); internal-IPC gate requires
  an allowlisted origin AND `session.__goldfinchInternal === true`
  (`internal-ipc.js:41-46`); MCP endpoint binds 127.0.0.1 only
  (`mcp-server.js:945`), Host/Origin/peer guard first, CSPRNG 32-byte keys
  stored as SHA-256, constant-time compare, per-request re-validation;
  jar-tier automation fenced by distinct server-side guards (`scope.js:198`,
  `resolve.js:215,:299`, `tabs.js:47`); no raw CDP passthrough; vault
  crypto core sound (scrypt N=2¹⁷/r=8/p=2, fresh salt/IV per envelope,
  AES-256-GCM, AAD-bound, idle-lock zeroizes, no logging); permissions a
  positive allowlist, default-deny (`session-runtime.js:236-244`);
  certificate errors fail closed, client cert never auto-selected
  (`app-lifecycle.js:99`); popups inherit the opener's jar with safe prefs
  (`guest-wiring.js:37-44,153-180`); burner/internal excluded from
  persistence by positive allowlist; SQLite parameterized throughout; no
  `child_process`; renderer XSS sweep clean (`textContent`/`escapeHtml`
  everywhere); `.env` gitignored and untracked.

### Category 2: Test Systems

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F12 | Suite healthy; M16 timing watch item disproven | Pass | Known → closed | Tick the M16 A/B item (squawk 0034) |
| F14 | `menu-overlay.js`: 17 of 19 sheet lifecycles have no executable verification | Action Required | New | Mission 17 Flight 3 |
| F13 | `welcome-controller.js` contract is grep-shape only; factory already injectable | Action Required | Known (M15 F2 → M16) | First item of the next cycle (Flight 5 if capacity) |
| F16 | No direct unit coverage: `main.js`, `internal-preload.js` (764), `webview-preload.js`, `chrome-preload.js` | Advisory | New | `internal-preload.js` pin with the harness flight |
| F15 | `pages/settings.js` / `pages/vault.js` route-wiring tests only | Advisory | Known (test headers) | Same harness |
| F17 | `bookmarks-bar.test.js` 2.1 s from 35+ repeated dynamic `import()` | Advisory | New | Squawk 0024 |
| F18 | Five test files hand-roll FakeElement/FakeDocument/fakeIpc | Advisory | New | Extract `helpers/fake-dom.js` with the harness flight |
| F19 | Track the grep-shape test count per sweep (17 files today) | Advisory | New | Standing metric |

**Details:**

- **F12** — 3792/3792, 0 skip/todo, two runs 3381 ms and 3327 ms (wall
  ~3.4 s), no flakes. Trend: M15 F3 3091 ms/3558 → M16 F2 3658 ms/3763 →
  today 3327–3381 ms/3792; per-test ≈ 0.87 ms, flat. The +18 % watch item
  from the M16 F2 debrief is disproven, not merely unreproduced. Per-file
  floor is ~130–150 ms process startup; `node --test` already runs the whole
  glob in one process, so `--test-concurrency` would not help further.
- **F14** (roundtable-resolved, Architect-verified) — `menu-overlay.js`
  registers 19 sheets: `menu`, `info-popup`, `input-dialog`, `suggestions`,
  `vault-unlock`, `auth-basic`, `vault-picker`, `cert-picker`,
  `vault-capture`, `vault-set`, `vault-recovery-show`, `vault-stepup`,
  `vault-accesskey-show`, `vault-adminkey-show`, `vault-import`,
  `vault-change-master`, `vault-recover`, `downloads`, `bookmark-edit`.
  `AUTOMATABLE_MENU_TYPES` (`resolve.js:53`) is exactly
  `{bookmarks-overflow, bookmark-edit}`, gating sheet `readDom`/
  `readAxTree`/`captureScreenshot` at `:200`. Cross-checked against all 76
  behavior specs and their run logs: **8 sheets have no spec at all** (the
  `vault-set` … `vault-recover` family), **4 have specs never executed**
  (`vault-capture`, `cert-picker`, two `vault-unlock`-interior specs),
  **2 have only stale pre-gate runs** (`downloads` 2026-07-08, `auth-basic`
  2026-07-28 — both before the 2026-08-06 allowlist), 1 is ruled
  inconclusive at runtime (page-context `menu`). Only `bookmark-edit` and
  the `bookmarks-overflow` case are spec'd, run, and drivable today. The
  file is a bare `(() => { … })()` IIFE with no exports, loaded as a module
  script depending on globals — it cannot be unit-imported at all, so this
  is a *verification* gap, not the same harness gap as F13.
- **F13** — `test/unit/search-engines.test.js:225-544` holds 18
  `readFileSync(welcome-controller.js)` blocks (~30 assertion lines).
  Risk example: a `classList.toggle('hidden', cond)` with `cond` inverted
  still passes the id-presence pins. Scoping (renderer Inspector):
  `createWelcomeController(deps)` already takes `document, els, attachView,
  welcomeSetPreference, onSettingsChanged, currentSearchEngine,
  currentHomePage` — the same factory-deps shape `tab-controller.test.js`
  exercises; a harness needs a FakeElement/FakeDocument (reusable from
  that file), stubs for those deps, and fixture tabs
  `{welcome:{reasons,pendingQuery}}`. "Same-day extraction, not a
  redesign." `welcomeReasons` is already unit-pinned via
  `tab-controller.test.js:332`.
- **F16** — no `require()` of `src/main/main.js` (2014), `src/preload/
  internal-preload.js` (764 — the trusted-embedder bridge), `webview-
  preload.js` source (only the bundle is structurally checked), or
  `chrome-preload.js` (path literal only) anywhere under `test/unit/`.
  Tool-pass leads `mcp-tools.js`/`observe.js`/`input.js` were noise — they
  have dedicated tests.

**Test-metric trend (from the M15/M16 flight debriefs + this sweep)**

| Point | Tests | Duration | Skips/flakes |
|---|---|---|---|
| M15 F3 (corrected) | 3558 | 3091 ms | 0 |
| M16 F1 | 3714 | ~3.0 s | 0 |
| M16 F2 | 3763 | 3658 ms | 0 |
| M16 F3 | 3790 | ~3.3–4.0 s | 0 |
| This sweep (×2) | 3792 | 3381 / 3327 ms | 0 |

Slowest files: bookmarks-bar 2095 ms (F17 — fixable), vault-context 1341,
vault-editor-model 1231, vault-crypto 931 (real scrypt, expected),
automation-mcp-server 898, settings-store 875. No optimization beyond F17
is warranted; the suite is at the process-overhead floor.

### Category 3: Dependency Health

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F20 | Dependencies healthy | Pass | — | — |
| F21 | Electron 44.0.0 available (major) | Pass | — | Existing behavior-test-gated policy applies |

**Details:** `npm audit` 0 across all severities; `npm ls` clean; no unused
declared dependencies; `brace-expansion` override still load-bearing
(deduped to 5.0.9 across five transitive chains); licenses MIT/ISC/
Apache/BSD/BlueOak only, no copyleft; Dependabot weekly with a
`dev-minor-patch` group and standalone majors, PR #164 merged same-day
2026-08-27; the closed-unmerged Dependabot PRs are superseded group PRs,
not stuck work. Dependabot alerts, code scanning, and secret scanning are
all disabled on the repo (informational; enabling secret scanning is
cheap). Outdated: 7 packages, one major (electron 43.2.0 → 44.0.0).

### Category 4: Code Quality

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F23 | `menu-overlay.js`: 19 `register` + 12 `attachModalCard` near-identical blocks | Action Required | New | Mission 17 Flight 3 |
| F22 | `main.js` 2724 → 2014 lines since 2026-07-11 | Pass | Known → favorable | Optional budget pin |
| F25 | Renderer module sizes; only `renderer.js` budget-guarded | Advisory | Known (M15) | Squawk 0025 (bookmarks-bar pin) |
| F26 | Three modal implementations, three cycles | Advisory | Known (M12) | Schedule or formally accept |
| F27 | `dropHandled` dead flag; overflow→page drag dead gesture | Advisory | Known (M15) | Squawk 0026 (flag); gesture → #150 |
| F24 | `pages/settings.js`: 39 hand-wired bindings | Advisory | New | Extract on the 4th control type |
| F28 | `PICK_PREFIX` dead export | Advisory | New | Squawk 0027 |
| F29 | `tearoff-overlay-manager.js` "consumer-less" (M09) | Pass | Known → closed | Ruled live; tick (squawk 0034) |
| F30 | Quality passes | Pass | — | — |

**Details:**

- **F23** — sheet registrations at `menu-overlay.js:159, 457, 792, 1008,
  1098, 1188, 1294, 1391, 1461, 1590, 1651, 1709, 1812, 1921, 2036, 2272`
  (and three more), each hand-repeating trigger/menu/dismissible/onOpen/
  onClose/focusReturn wiring, 20–80 lines apiece. A lifecycle fix (e.g.
  secret-scrub-on-close) must be re-applied by hand across ~12 vault/auth/
  cert/bookmark-edit sheets — and per F14, with no net under most of them.
  Extraction target: `createSheetEntry({node, cycle, dismissible, onOpen,
  onClose})` in an importable module, testable against mock nodes without
  a full DOM harness.
- **F29** (Architect ruling) — `main.js:65,1249` passes the factory into
  `window-factory.js:246`, which constructs it and stores it at
  `record.tearoffOverlay:253`; `show`/`setPosition`/`hide` are driven from
  `register-overlay-ipc.js:707-716`, `isVisible`/`show` from
  `register-tab-ipc.js:952`, `teardown` from `window-factory.js:269`; the
  renderer reaches it via `chrome-preload.js:297-299`. Live. The M09
  "consumer-less" claim came from grepping for direct invocation instead
  of following the factory-injection idiom this codebase uses pervasively.
- **F30** — TODO/FIXME/HACK/XXX: 0 in all of `src/`; dual-export ESM shims
  in `src/shared/`: 0 (M07 promise kept; 7 files are deliberately CJS-only
  and pinned by `preload-graph-esm-free.test.js`); `{ok:false, error}`
  consistent across all six `register-*-ipc.js` (07-11 advisory resolved);
  `innerHTML` confined to 3 renderer files with escaping; CSS orphan `#id`
  selectors 0; stale-comment scan clean (no `BrowserView`, no live
  `<webview>`, no hardcoded Google outside `search-engines.js`; the
  auto-attach comments correctly say "retired").

### Category 5: Type & API Safety

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F32 | "Electron 42 cookie shape" assumption in `jar-data-ipc.js` never re-checked after the 43 bump | Action Required | New | Squawk 0019 (escalate if drift found) |
| F31 | Typecheck clean; 180 `@ts-ignore`/`any` concentrated in vault/db modules | Pass | — | — |

**Details:** `jar-data-ipc.js:324-326` says the wipe logic is "contingent on
Electron 42's Cookie shape … re-check this assumption on any Electron version
bump"; 43.2.0 merged 2026-07-24 (PR #125) with no recorded re-check. Cookie
shape drives jar-data wiping, so an unnoticed CHIPS/partitioned field would
leave data behind. Cheap to verify → squawk vehicle at Action Required
severity. No deprecated Electron API usage anywhere (`protocol.handle` in
use; `setPermissionRequestHandler` shape current).

### Category 6: Documentation

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F33 | Operator username/home path in a committed leg doc | Action Required | New | Squawk 0016 (grounding) |
| F34 | CLAUDE.md: four owed subsections still absent | Action Required | Known (M11/M14/M15/M16) | Ride Mission 17's flights (see below) |
| F37 | Debrief ledger stale: ~70 unticked, ≥9 confirmed done | Action Required | New | Squawk 0034 (ticks); process fix in `/flight-debrief` |
| F35 | `docs/renderer-menu.md` Consumers list names 3 of 16 template families | Advisory | New | Squawk 0028 |
| F36 | BACKLOG.md carries 3 superseded entries | Advisory | Known-done | Squawk 0029 |
| F38 | `GOLDFINCH_VAULT_TRACE` undocumented; no `.env.example` | Advisory | New | Squawk 0030 |
| F39 | Docs passes (Commands accurate; no CHANGELOG by design; `docs/vault.md` current) | Pass | — | — |

**Details:**

- **F34** — absent as dedicated sections: the popup subsystem
  (`popup-registry`, opener parity — zero mentions; M14), the drag
  machinery (MIME set and marker-only semantics, `bookmark-drag.js`,
  `guest-bookmark-drop.js`, spring-loading, the four-timer ordering
  invariant — zero mentions; M15), the menu-overlay / chrome-indicator
  pattern (M11), and the viewless tab record as a *general* primitive
  (documented per-instance for welcome at `CLAUDE.md:242`; M16). Also
  still owed: `persist-jar-gate.js` named as the boundary-predicate
  template (idiom described at `:249`, file never named; M09) and the
  "logic behind a review" DOM-contract rule (M16). Recommendation: Flight
  3 owns the sheet/chrome-indicator subsection as its closing step; the
  popup and drag subsections ride the next flight that touches those
  files, with a squawk id assigned at that flight's debrief rather than a
  bullet.
- **F37** — confirmed done in the tree but still `- [ ]`: JSON→SQLite
  migration (M08 — `app-db.js` document/bookmarks stores; only the vault
  file stays JSON by design), tool-count invariant consolidation (M08),
  `__goldfinchNavGuarded` ordering test (M13 → `latch-ordering-invariant.
  test.js`), `docs/vault.md` refresh (M12), jars 4-module reference (M08 →
  `CLAUDE.md:91`), dual-export shim removal (M07), classifier parity test
  (BACKLOG → `shortcut-classifier-parity.test.js`), the 07-11
  result-contract advisory, omnibox honest close reasons (M08). Plus the
  two closed by this sweep's evidence (F12 timing A/B; F29
  tearoff-overlay-manager). **Not** done despite a doc cite: the M16
  cache-sync item — the `{search}/{home}` override is still at
  `welcome-controller.js:193-203, :240, :329` with the "cache may not have
  landed yet" comments at `:301`/`:350`. Process recommendation: make
  "re-verify every open action item against the tree" a standing
  close-out step in `/flight-debrief`, since M16 already recorded this as
  a recurrence.

### Category 7: Git & Branch Hygiene

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F40 | Git hygiene clean | Pass | — | — |
| F41 | Prettier drift on 315 files; `prettier --check` not enforced in CI | Advisory | New | Operator-ruled 2026-08-27: enforce — squawk 0039 (one formatting commit + CI check) |

**Details:** 0 open PRs; one stale remote Dependabot branch; the only >1 MB
blob is a real asset (`src/renderer/assets/goldfinch_color.png`, 1.42 MB);
`test-out.txt`/`test-time.txt` gitignored; tag `v0.14.0` = package.json;
commit conventions consistent (39/40 across `flight/`, `squawk/`,
`mission/`, `release-prep:`, Dependabot). The one secret-pattern hit in the
last 60 commits' diffs is a truncated placeholder certificate
(`'-----BEGIN CERTIFICATE-----\nMIID...'`) in `test/unit/auth-challenges.
test.js` (`b52e59e`) — not a secret. Local `main` is 2 behind origin
(informational).

### Category 9: Infrastructure & Config

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F42 | Dev-profile `setPath('userData')` runs after `app.whenReady` | Action Required | Known (#121) | Squawk 0017 |
| F43 | `asar:false` undocumented; `files: src/**/*` ships preload sources + everything under `src/` | Advisory | New | Document the rationale; tighten globs if size matters |
| F44 | `npm run a11y` exit-code collision | Advisory | Known (M09) | Squawk 0031 |

**Details:** `app-lifecycle.js:160-161` → `init-profile.js:50`; Chromium
child processes carry the real profile's `--user-data-dir`. Fix shape: move
the `!app.isPackaged` `setPath` to `main.js` module scope beside
`setAppUserModelId` (squawk 0002's placement precedent) with a source-order
pin. `scripts/`: all eight scripts have live purposes and valid references.
mac `identity: null` (unsigned) and no updater wiring are part of issue #101.

### Category 10: Performance & Resource Cleanup

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F45 | Resource discipline clean | Pass | — | — |
| F46 | Orphan guest view when a tab closes before `tab-create` resolves (renderer side) | Advisory | Known (#134.3) | Squawk 0018 |
| F47 | `pruneExpired` coupling unchanged; `rerollSeed` skip now a documented policy | Advisory | Known (M08) | Informational |

**Details:** timers paired (main: `find.js:154/130`; app-lifetime retention
sweep `.unref()`'d by design; vault idle-lock and capture-drop timers
re-armed correctly); history writes are per-row prepared INSERTs under WAL;
renderer listeners are `pagehide`-scoped; tab/jar/toast Maps pruned on
close; omnibox debounced 100 ms with a sequence guard. **F46** (Architect
ruling): `tab-create` is synchronous on the main side, which is exactly
what guarantees the view exists when `tab-controller.js:343`'s
`if (!tabs.has(tab.id)) return;` bails — the leak is real and renderer-side.

### Accessibility (WCAG 2.1 AA)

| # | Finding | Severity | New/Known | Recommendation |
|---|---------|----------|-----------|----------------|
| F48 | Keyboard-only users are excluded from page content (#174) | **Critical** | Known (#174) | Mission 17 Flight 1 |
| F49 | Omnibox suggestion listbox has no combobox / `aria-activedescendant` semantics — and cannot, cross-view | Action Required | New | Mission 17 Flight 1 |
| F50 | Toast auto-dismiss has no pause on hover/focus | Advisory | New | Squawk 0032 |
| F51 | Accessibility verified-OK | Pass | — | — |

**Details:**

- **F48** (roundtable-resolved; Architect-verified) — WCAG 2.1.1 Keyboard
  / 2.4.3 Focus Order, Level A. Two compounding causes: (1) the chrome's Tab
  order is closed-loop — `shared/keydown-action.js:61-134` has no "focus
  page" action (every branch but F12 requires ctrl/meta); Enter in the
  address bar calls `navigate()` then `els.address.blur()`
  (`navigation-controller.js:327-333`) without focusing the guest;
  `tab-create` and `tab-navigate` never `.focus()` the guest; the only
  guest `.focus()` in the tree, `tab-set-active`
  (`register-tab-ipc.js:869,899-905`), is gated on `wasPageFocused` and so
  can only *preserve* focus a guest already had. (2) Once a guest does hold
  focus, `shared/cross-view-nav.js` returns `'tab-handoff'` for **any**
  unmodified forward Tab, and `guest-wiring.js:97-113` `preventDefault()`s
  it and ejects to the address bar; only Shift+Tab falls through to
  Chromium. The Architect found one incidental keyboard path into a guest —
  Ctrl+F → Escape (`register-overlay-ipc.js:698` `refocusGuest:true` →
  `find-overlay-manager.js:291` `wc.focus()`) — after which only reverse
  traversal survives until the first forward Tab. That is not a workaround,
  but it *is* proof that `wc.focus()` from main is a working focus-entry
  primitive, which narrows the flight's open design question to "which
  gesture triggers it, and how is tab-exhaustion at the guest's last
  focusable detected" (Electron has no native signal). Both behaviors are
  pinned as correct by `cross-view-nav.test.js` and behavior spec
  `chrome-guest-keyboard-nav.md` steps 5/6 (whose own setup establishes
  guest focus with a mouse click). Absorbs BACKLOG "Internal-page keyboard
  focus" and M08 debrief H8. Critical changes ordering and headline, not
  vehicle — a designed flight.
- **F49** — `index.html:78-85` `#address` has `aria-autocomplete="list"`
  only; the `role="listbox"` rows render in the menu-overlay sheet's
  separate `WebContentsView` (`menu-overlay.js:15`), a different document,
  so `aria-activedescendant`/`aria-controls` cannot link; selection lives
  in JS only (`navigation-controller.js:306-314`). Same cross-view design
  problem as F48 — solve once.
- **F51 verified OK** — contrast `--fg/--bg` 13.3:1, `--fg-dim` 6.0:1,
  welcome surface 16.4:1 and its focus ring ≥ 3.49:1 (deliberate fix at
  `styles.css:858-864`); all 11 `outline:none` sites paired with a
  `:focus-visible` replacement; `prefers-reduced-motion` in 10 blocks across
  6 files; welcome auto-run only on the user's own click (3.2.2); sheets/
  popovers have Escape-to-close, focus-restore-to-invoker and Tab
  containment (`menu-overlay.js:11-22,445-482,606-899`); 0 unlabeled
  buttons; 13/13 Settings inputs labeled; landmarks on every page;
  `aria-pressed`/`aria-selected`/`aria-expanded` state semantics. 72
  `[a11y]` rows across 23/76 behavior specs. `npm run a11y` needs a running
  app — not run this cycle. M14's "a11y sheet-sweep ruling" for the
  vault-on-auth-sheet flight is still open (two cycles).

### Open GitHub Issues (triage against the tree @ `0cd2e3b`)

| Issue | Kind | Classification | Disposition |
|---|---|---|---|
| #37 configurable search engine | enh | **delivered** (M16) | Close — squawk 0033 |
| #39 pinnable DevTools | enh | **delivered** (`329df6d`; detached, allowed) | Close — squawk 0033 |
| #122 bookmarks (4 pieces) | enh | **delivered** (M15; piece 2 dropped by ruling → #113) | Close with note — squawk 0033 |
| #81 tracker domain not PSL-backed | defect | **squawk-sized** — M12 already vendored `psl.js`; fail-closed ruled acceptable 2026-08-27 | Squawk 0035 |
| #121 dev-profile redirect after ready | defect | **squawk-sized** | Squawk 0017 |
| #134 tab lifecycle | defect/enh | item 3 **squawk-sized**; items 1–2 flight-sized | Squawk 0018; rest backlog |
| #104 WebRTC IP leak | defect | burner-only carve-out **squawk-sized**; normal-tab policy → #147 | Squawk 0036 |
| #174 keyboard Tab never reaches page | defect | **flight-sized**, Critical | Mission 17 Flight 1 |
| #163 nav failures render blank | defect | **flight-sized** — no `did-fail-load` handling anywhere; highest user-visible | Next cycle, before #143 |
| #150 overflow→bar drag first-attempt | defect | **flight-sized** (small) — needs scoped review + captured failing trace; enabling leg = drag/drop instrumentation in admin MCP (M15 item) | Next cycle |
| #133 crash resilience | defect | **flight-sized**; leg 3 (`crashReporter`) is a cheap carve-out but needs a retention/location call | Next cycle; make #133 canonical over the BACKLOG seed |
| #143 TLS interstitial / indicators / viewer | defect | **flight-sized** (1–2 flights) | After #163, inside the #144 mission |
| #65 Windows taskbar pin (= squawk 0002, escalated) | defect | **flight-sized** — fix shipped v0.13.4, verification FAILED ×2; next hypothesis: NSIS uninstall phase deletes the shortcut target | Plan with #101 |
| #156 vault password generator on sign-up | enh | **flight-sized** — generator substrate exists (`shared/password-generator.js`); fill-icon path needs several DDs | Backlog |
| #118 downloads popup Clear | enh | borderline squawk (three surfaces); adds behavior → not a squawk | Backlog / ride a downloads touch |
| #168 silent AudioContext fingerprinting | defect | `autoplayPolicy` line spike-verified but user-visible behavior change; rest → #147 | #147 mission |
| #147 fingerprinting audit | enh | **mission-sized** (absorbs #104, #168; + F10f evidence) | Backlog mission |
| #144 popup abuse + per-origin permissions | enh | **mission-sized** (+#143; M14 "site trust & permissions" seed canonical) | Backlog mission |
| #101 signed installers / auto-update / version UI | enh | **mission-sized**; version-in-About is a small carve-out but adds UI | Backlog mission (+#65) |
| #113 vault indicator "Lock now" + pinnable | enh | **operator-ruled 2026-08-27**: indicator stays where it is, not pinnable (`CLAUDE.md:116` rule stands); "Lock now" context menu is squawk-sized | Squawk 0038; close #113 when it lands |

Tracker reconciliation (squawk 0033/0029): #133 ↔ BACKLOG crash seed; #174
↔ BACKLOG focus seed + M08 H8; #143/#144 ↔ M14 seed; #104/#168 ⊂ #147; #150
tracked in three places (issue canonical). Labels: 8/20 issues labeled,
none of the eight defect-class ones. The 15 most recent closed issues all
closed `completed`; nothing closed as not-planned.

## Severity Summary

| Severity | Count |
|----------|-------|
| Critical | 1 |
| Action Required | 14 |
| Advisory | 31 |
| Pass | 12 |

Action Required: F1, F2, F4, F8, F9, F13, F14, F23, F32, F33, F34, F37,
F42, F49. Critical: F48.

## Squawks Logged This Cycle

| Squawk | Finding | Item |
|---|---|---|
| [0016](../squawks/0016-operator-path-leak-in-leg-doc.md) | F33 | Operator path in a committed leg doc (**grounding**) |
| [0017](../squawks/0017-dev-profile-redirect-after-ready.md) | F42 / #121 | Dev-profile `setPath` before `ready` |
| [0018](../squawks/0018-orphan-guest-view-on-view-created-early-return.md) | F46 / #134.3 | Orphan guest view on `onViewCreated` early return |
| [0019](../squawks/0019-electron-42-cookie-shape-assumption-unverified.md) | F32 | Re-verify Electron 43 cookie shape |
| [0020](../squawks/0020-jar-color-unescaped-in-chrome-innerhtml.md) | F3 | `isSafeColor` on jar color |
| [0021](../squawks/0021-vault-clipboard-copy-never-cleared.md) | F5 | Vault clipboard clear timeout |
| [0022](../squawks/0022-vault-docs-threat-model-omits-extracted-mrk.md) | F6 | Vault threat-model doc bullet |
| [0023](../squawks/0023-audit-log-records-full-urls.md) | F10h | Strip query/fragment from audit log |
| [0024](../squawks/0024-bookmarks-bar-test-repeated-dynamic-import.md) | F17 | Hoist dynamic import in `bookmarks-bar.test.js` |
| [0025](../squawks/0025-bookmarks-bar-line-budget-pin.md) | F25 | `bookmarks-bar.js` budget pin |
| [0026](../squawks/0026-bookmarks-bar-dead-drophandled-flag.md) | F27 | Delete dead `dropHandled` |
| [0027](../squawks/0027-pick-prefix-dead-export.md) | F28 | `PICK_PREFIX` dead export |
| [0028](../squawks/0028-renderer-menu-doc-consumers-stale.md) | F35 | `docs/renderer-menu.md` Consumers list |
| [0029](../squawks/0029-backlog-entries-superseded.md) | F36 / F57 | BACKLOG.md superseded entries |
| [0030](../squawks/0030-vault-trace-env-var-undocumented.md) | F38 | `GOLDFINCH_VAULT_TRACE` + `.env.example` |
| [0031](../squawks/0031-a11y-audit-exit-code-collision.md) | F44 | `a11y-audit.mjs` exit codes |
| [0032](../squawks/0032-toast-auto-dismiss-no-pause.md) | F50 | Toast pause on hover/focus |
| [0033](../squawks/0033-github-issues-out-of-sync-with-shipped-work.md) | F52 / F57 | Close #37/#39/#122; label defects; cross-link |
| [0034](../squawks/0034-debrief-action-item-ledger-stale.md) | F37 / F12 / F29 | Tick done-but-unticked debrief items |
| [0035](../squawks/0035-tracker-registrable-domain-not-psl-backed.md) | #81 | PSL-backed `registrableDomain` |
| [0036](../squawks/0036-burner-tabs-leak-ip-via-webrtc.md) | #104 | Burner-only WebRTC IP policy |
| [0037](../squawks/0037-electron-43-caret-range.md) | F10m | Exact Electron pin |
| [0038](../squawks/0038-vault-indicator-lock-now-context-menu.md) | F56 / #113 | "Lock now" context menu on the vault indicator (operator-ruled; indicator not pinnable) |
| [0039](../squawks/0039-prettier-drift-not-enforced.md) | F41 | Formatting commit + `prettier --check` in CI (operator-ruled: enforce) |

Not logged (failed the squawk gate — adds behavior or needs a policy call):
F10c (Flight 2), #118, #168 `autoplayPolicy`, #133 leg 3, #101 version-in-
About.

## Known Debt Carried Forward

- **Deferred from this cycle's mission** (Action Required, next cycle):
  F4 MCP rate limiting; F13 + F18 + F16 DOM harness for chrome controllers
  (first item of the next cycle, or Flight 5 if Mission 17 lands early);
  F34 popup/drag CLAUDE.md subsections (ride the next touching flight);
  F37 full ledger sweep (0034 covers the enumerated ticks).
- **Prior-debrief items still open, unchanged**: M16 sync local cache
  write at `chrome-welcome-set`/`internal-settings-set` + retire the
  `{search}/{home}` override (no third consumer yet); M15 sheet
  non-bookmarks consumers unverified after DD1f (now part of F14);
  `bookmarks-bar.js` extraction at the drag-session seam; M14
  stale-bounds diagnosis, `modelShapeOk` extraction, vault-on-auth-sheet
  flight + a11y sheet-sweep ruling; M13 vault fill/capture re-run under
  `sandbox:true`, chrome-view sandbox flip; M12 three modal
  implementations (F26 — schedule or formally accept), import-destination
  HAT parts A/B; M09 `KEY_MAP` PageDown/PageUp, classifier hand-mirror
  unification (parity test now exists), persist-jar-gate docs; M08
  `did-navigate` → recorder smoke assertion, `pruneExpired` coupling,
  `getHistory` audit detail.
- **Trigger-gated, none tripped**: robot-glyph SVG duplication (still 2
  consumers); wipe-composition triple (not re-verified this cycle);
  `commitOrRevertName` HAT-only witness (not re-verified); `main.js`
  line-count watch (favorable — 2724 → 2014).
- **Security lows for a future hardening pass**: F7, F10a, F10b, F10d,
  F10e, F10g, F10i, F10l (see Category 1 table).
- **Deferred by design**: MRK re-key / compromise-mode rotation (inverts
  the single-slot rotation invariant); Electron 44 major (policy-gated);
  #147 / #144 / #101 missions.

## Recommendations

1. **Fly Mission 17 as scaffolded, Flight 1 first.** #174 is a Level-A
   keyboard exclusion in a shipping browser; the `find-overlay` `wc.focus()`
   path is proven prior art that shrinks the design question.
2. **Complete squawk 0016 (grounding) on the first turnaround** — a
   two-minute scrub against an explicit house rule on a public repo. Batch
   the rest as one or two turnarounds; 0035 (#81, fails-open tracker
   blocking) and 0036 (#104 burner leak) are the privacy-bearing ones.
3. **Make the issue-triage agent and the "re-verify open action items"
   step standing parts of the crew** — this cycle's tracker reconciliation
   found three delivered-but-open issues and ≥9 done-but-unticked items,
   and M16 had already recorded the recurrence.
4. **Next cycle's flight candidates, in order**: #163 (blank page on nav
   failure — the highest user-visible defect in the backlog), the DOM
   harness (F13/F18/F16), F4 rate limiting, #150.
5. **Operator decisions** — ruled 2026-08-27 at report review: #113 → the
   vault indicator stays where it is (not pinnable) and gains a "Lock now"
   context menu (squawk 0038); F41 Prettier → enforce in CI after one
   formatting commit (squawk 0039); GitHub secret scanning → enabled on the
   repository the same day; F33 → no history rewrite, squawk 0016 cleans up
   the file only. No operator decisions remain open from this cycle.

**Regression-gate recommendations (behavior tests)** — author specs inline
during Mission 17's flight design:
- Flight 1: rewrite `chrome-guest-keyboard-nav.md` so guest entry is
  established by **keyboard** (not a mouse click) and forward Tab traverses
  page content; add a keyboard-only "navigate → activate a link → fill a
  form field" spec as the pin for #174.
- Flight 2: a spec that, under an **admin** key, attempts `click`/`readDom`
  on a `goldfinch://vault` tab and asserts refusal — the invariant
  `observe.js:452` states.
- Flight 3: whichever vault/auth/cert/downloads sheets the flight makes
  drivable get a spec each; the pre-gate `downloads` and `auth-basic` specs
  are re-run, not trusted.
- Flight 4: a spec (or unit truth table) that loads a `manager.json` with
  out-of-bounds KDF params and asserts refusal without a hang.

## Maintenance Mission

[Mission: Codebase Health — 2026-08-27 Maintenance](../missions/17-maintenance/mission.md)
— scaffolded with four flights (keyboard reachability; automation
internal-session invariant; sheet lifecycle verification; vault trust
boundary). Squawks 0016–0039 cover the rest.
