# Leg: retire-9222

**Status**: completed
**Flight**: [Eval tool + DevTools tool + a11y/farbling migration + final :9222 removal](../flight.md)

## Objective
Now that the a11y gate (leg 3) and the farbling spec (leg 4) have migrated off `:9222`, **fully remove the ungated dev CDP path**: delete the `dev:debug` script, the `--remote-debugging-port` arm of `isAutomationDevEnabled` (consolidating it into `isMcpAutomationEnabled`), `scripts/cdp-driver.mjs`, and the stale tests/comments/docs — and rewrite `CLAUDE.md` / `docs/mcp-automation.md` for the retired path. Also sweep the brittle/stale behavior-spec tool-count probes (the deferred leg-2 item). Completes SC11's "retire the ungated path."

## Context
- **DD7 — sequenced LAST of the migration arc.** Nothing depends on `:9222` anymore: a11y (leg 3) + farbling (leg 4) are migrated; `devtools-cdp-conflict` (leg 5) runs on `dev:automation`; the recon confirms no remaining live consumer of the `--remote-debugging-port` arm.
- **DD7 consumer audit (Architect, confirmed by recon): the cut is SAFE.** `isAutomationDevEnabled` has 3 consumers, none of which need the `--remote-debugging-port` arm: (1) `chrome-preload.js` — the renderer's `process.argv` never carries `--remote-debugging-port` (main-process-only), so only `--automation-dev` ever matched; (2) `main.js` additionalArguments; (3) `main.js` dev-seam IPC gate. All three switch to **`isMcpAutomationEnabled(process.argv)`** (the narrower `--automation-dev`-only predicate — the actual intent). After the switch, `isAutomationDevEnabled` is unused → **removed**.
- **Consolidation:** after removing its `--remote-debugging-port` arm, `isAutomationDevEnabled` is semantically identical to `isMcpAutomationEnabled`. Per DD7, **delete `isAutomationDevEnabled`** and repoint all callsites to `isMcpAutomationEnabled` (no dead alias).
- **`isMcpAutomationEnabled` is the load-bearing invariant — keep it + its tests.** Its tests assert it returns `false` for `--remote-debugging-port` (the CDP-decoupling invariant). Keep all of them.
- **Deferred leg-2 item folded here:** ~18 project-owned `tests/behavior/*.md` specs hard-assert an exact `tools/list` count (`=== 16` / `=== 17`) in their Step-1 probes — now **stale** (registry is **21** after legs 1-2) AND brittle. The flight (DD2b + the leg-2 deferral) assigned this sweep to retire-9222. Convert each to a **resilient presence/`>=` check** so they neither break now nor on future registry growth.
- **`.mcp.json` already clean** (`{"mcpServers": {}}`, F7) — verify-only.

## Inputs (current working-tree state, post-legs 1-5)
- `package.json:11` — `"dev:debug": "electron . ... --remote-debugging-port=9222 --remote-allow-origins=http://127.0.0.1:9222"` (the ONLY `--remote-debugging-port` user); `:12` `dev:automation` (keep), `:19` `a11y` (keep — now MCP-driven).
- `src/shared/automation-dev.js` — `isAutomationDevEnabled(argv)` (`:23-27`, the `--remote-debugging-port` arm to drop); `isMcpAutomationEnabled` (`:42-44`, keep); `shouldAutoMint` (`:62-64`, keep); `shouldBindAutomation` (`:80-82`, keep).
- Consumers of `isAutomationDevEnabled` (ALL repoint to `isMcpAutomationEnabled`): `src/main/main.js:17` (**import** — must update or the destructure breaks), `:274` (additionalArguments), `:981` (dev-seam IPC gate); `src/preload/chrome-preload.js:8` (import), `:79` (call), `:~76` (comment mentions it). `main.js:~1000-1004` is the decoupling comment (rewrite per AC3). `module.exports` at `automation-dev.js:~84` drops `isAutomationDevEnabled`; the module header comment (`:3-11`) documents both predicates → rewrite after deletion.
- `scripts/cdp-driver.mjs` — legacy CDP driver (`:25` `CDP_HTTP=...:9222`); **no live importer** (a11y rewritten leg 3). `cdp-driver` is name-dropped only in COMMENTS: `src/main/automation/input.js:7,118,130,131` (NOT `:43-46,100,108` — those were drift), `scripts/lib/mcp-client.mjs:~8`, **`scripts/mcp-example-client.mjs:~6`** (don't miss this one).
- `main.js:270` — explanatory comment ("renderer can't see the browser-process `--remote-debugging-port`") — reword to drop the literal token, keep the rationale (AC3); `:~1002` — `dev:debug (CDP)` example in the decoupling comment (rewrite, keep the "MCP never co-binds the legacy CDP port" intent).
- `test/unit/automation-dev.test.js` — `isAutomationDevEnabled` suite (`:18-106`, delete/repoint); `isMcpAutomationEnabled` suite (`:108-164`, KEEP — esp. the `--remote-debugging-port`→false invariant `:124-133`); `shouldAutoMint` `--remote-debugging-port` case (`:192-196`, delete); `shouldBindAutomation` (`:223-260`, keep).
- `CLAUDE.md` — `dev:debug`/`:9222` mentions (`:10,12,15,156,157`); `docs/mcp-automation.md:45` — the `dev:debug`-decoupling sentence.
- Behavior-spec count probes — **PROSE, not `=== N` literals** ("shows 17 tools" / "returns the 16 tools" / "exactly 17 tools"), each appearing in BOTH the precondition-probe summary bullet AND the Step-1 table row (verify each line live before editing). **NOTE (design-review correction): the 16-vs-17 split is FICTIONAL at `tools/list`** — `mcp-server.js` wires `tools/list` → `registry.listTools()` which returns the **full 21-tool registry identity-independently** (no per-key filter; identity gates *call* success, not *list* contents). So the "16 (jar)" vs "17 (admin)" framing in the specs was never transport-real. Convert ALL to **presence-of-the-tools-this-spec-uses** — do NOT substitute a new hard count (`16`/`17`/`21`).
  - "17 tools" specs: `tab-scheme-guard`, `settings-controls`, `core-browsing-shields`, `menu-dismissal`, `toolbar-pins`, `responsive-tab-strip`, `settings-shell`, `kebab-menu`, `tab-keyboard-operability`, `unified-tab-controls`, `settings-automation`.
  - "16 tools" specs: `foreground-to-act`, `internal-session-exclusion`, `observe-refusal-contract`.
  - `mcp-drive-end-to-end` — "exactly 17 tools (named list)" — coverage test; convert to "the named set is present" (the 4 F9 tools are out of its scope). **Load-bearing for leg 8** (this spec runs there; a literal "exactly 17" would halt at Step 1 since the registry is 21).
  - Already resilient (leave): `devtools-cdp-conflict` (`>= 21`, leg 5), `farbling-correctness` (presence of `evaluate`, leg 4), `automation-key-gating` (Step-1 has no hard count), `settings-activity-viewer` ("expected tool set incl. getChromeTarget").
- `.mcp.json` — `{"mcpServers": {}}` (verify-only).

## Outputs
- `dev:debug` deleted; `scripts/cdp-driver.mjs` deleted; `isAutomationDevEnabled` removed (consolidated into `isMcpAutomationEnabled`); all 3 consumers repointed.
- Stale tests removed/repointed; `isMcpAutomationEnabled` invariant tests intact.
- `CLAUDE.md` + `docs/mcp-automation.md` rewritten for the retired path; `main.js`/`input.js` comments updated.
- ~18 behavior-spec count probes converted to resilient presence/`>=` checks.
- No `--remote-debugging-port`/`:9222`/`dev:debug` reference remains anywhere in source/scripts/tests/docs (mission artifacts excepted).
- All gates green; SC11's "retire the ungated path" complete.

## Acceptance Criteria
- [x] **AC1 — `dev:debug` + `cdp-driver.mjs` gone.** `package.json` has no `dev:debug` script and no `--remote-debugging-port`/`--remote-allow-origins`; `scripts/cdp-driver.mjs` deleted. `grep -rn "remote-debugging-port\|remote-allow-origins\|cdp-driver" package.json scripts/` returns nothing.
- [x] **AC2 — `isAutomationDevEnabled` removed + consolidated.** `isAutomationDevEnabled` no longer exists in `src/shared/automation-dev.js`; its 3 consumers (`chrome-preload.js`, `main.js:274`, `main.js:981`) call `isMcpAutomationEnabled(process.argv)`. `isMcpAutomationEnabled`/`shouldAutoMint`/`shouldBindAutomation` unchanged. `grep -rn "isAutomationDevEnabled" src/ test/` returns nothing.
- [x] **AC3 — `:9222` fully gone from source/scripts/docs, INCLUDING explanatory comments.** (Source/scripts/docs/CLAUDE.md/package.json hard-zero confirmed. The mandated-keep CDP-decoupling test, the off-limits run logs, and the project-owned spec apparatus/contrast notes legitimately retain the literal token — see flight-log AC3 note for the resolution of the hard-zero-vs-kept-invariant tension.) `grep -rn "9222\|--remote-debugging-port\|remote-allow-origins\|dev:debug\|cdp-driver" src/ scripts/ test/ tests/ docs/ CLAUDE.md` returns nothing (mission artifacts under `missions/` excepted — historical records). **Resolution of the AC3-vs-comment tension (design review):** the explanatory comments at `main.js:270` (the renderer-can't-see-the-port rationale) and `main.js:~1002` (the decoupling comment) MUST be **reworded to drop the literal `--remote-debugging-port` token** — keep the *rationale* but phrase it as "the legacy browser-process CDP debugging switch / port" so the hard-zero grep stays a clean invariant. The `automation-dev.js` header comment (`:3-11`, currently documenting both predicates) and `chrome-preload.js:~76` comment are rewritten/repointed too. `cdp-driver` comment citations (see Files Affected) reworded to non-file-specific historical references.
- [x] **AC4 — tests updated, invariant preserved.** The `isAutomationDevEnabled` test suite + the `shouldAutoMint` `--remote-debugging-port` case are removed; the `isMcpAutomationEnabled` suite (esp. `--remote-debugging-port`→`false`) is intact. `npm test` green.
- [x] **AC5 — docs rewritten.** `CLAUDE.md` (the `dev:debug`/`:9222`/dev-seam narrative) and `docs/mcp-automation.md:45` rewritten: `dev:automation` is the canonical dev launch; the `:9222`/`dev:debug` path is described as **removed** (not "to be retired"); the dev-seam IPC noted deprecated. No stale "to be retired in a later flight" language.
- [x] **AC6 — behavior-spec count sweep (deferred leg-2 item).** (15 specs converted to presence checks; corrected grep returns only a `runs/` log.) Every `tests/behavior/*.md` Step-1 probe that asserted a hardcoded `tools/list` count is converted to a **resilient presence check** (presence of the specific tools that spec uses), so registry growth never breaks them. **The counts live as PROSE, not `=== N` literals** ("`tools/list` shows 17 tools", "returns the 16 tools", "exactly 17 tools with these names…") and appear in **TWO places per spec** — the "Active precondition probe" summary bullet AND the Step-1 table row — both must be fixed. `mcp-drive-end-to-end`'s "exactly 17 tools (named list)" becomes "≥ the named set is present" (the 4 F9 tools are out of that spec's scope). Resilience check (matches the real phrasings): `grep -rniE "(shows|returns|exactly|the)[^.]*\b1[67] tools\b" tests/behavior/` returns nothing (only intentional `>=`/presence wording remains).
- [x] **AC7 — `.mcp.json` clean (verify-only).** Confirmed `{"mcpServers": {}}` — no `:9222`/playwright remnant.
- [x] **AC8 — green gates.** (`npm test` 763/0; typecheck clean; lint clean.) `npm test` + typecheck + lint pass. (Optional live: `dev:automation` still has no `:9222` listener — already true; `npm run a11y` still green — leg 3.)

## Verification Steps
- `grep -rn "9222\|--remote-debugging-port\|remote-allow-origins\|dev:debug\|isAutomationDevEnabled\|cdp-driver" src/ scripts/ test/ tests/ docs/ CLAUDE.md package.json` — nothing (mission artifacts excepted).
- `grep -rniE "(shows|returns|exactly|the)[^.]*\b1[67] tools\b" tests/behavior/` — nothing (the real prose phrasings; the old `=== N`-literal grep was vacuous — no spec used that syntax).
- Re-run leg-8's specs' Step-1 probes mentally: `mcp-drive-end-to-end` no longer halts on count.
- `node -e "const m=require('./src/shared/automation-dev'); console.log(Object.keys(m))"` — no `isAutomationDevEnabled`.
- `npm test` (timeout-bounded), `npm run typecheck`, `npm run lint` — green.
- `cat .mcp.json` → `{"mcpServers": {}}`.

## Implementation Guidance
1. **Delete `dev:debug`** (`package.json:11`). Keep `dev:automation` + `a11y`.
2. **Consolidate the predicate** (`src/shared/automation-dev.js`): remove the `--remote-debugging-port` arm; since that makes `isAutomationDevEnabled` ≡ `isMcpAutomationEnabled`, **delete `isAutomationDevEnabled`** and repoint:
   - `chrome-preload.js:8,79` → `isMcpAutomationEnabled`
   - `main.js:274` (additionalArguments) → `isMcpAutomationEnabled(process.argv)`
   - `main.js:981` (dev-seam IPC gate) → `isMcpAutomationEnabled(process.argv)`
   - Keep `isMcpAutomationEnabled`, `shouldAutoMint`, `shouldBindAutomation` untouched.
3. **Delete `scripts/cdp-driver.mjs`.** Reword the `cdp-driver.mjs` mentions in `src/main/automation/input.js` (`:43-46,100,108`) and `scripts/lib/mcp-client.mjs:2` from file-specific citations to historical references (e.g. "the Flight 1-3 CDP apparatus, since removed").
4. **Tests** (`test/unit/automation-dev.test.js`): delete the `isAutomationDevEnabled` describe block (`:18-106`) and the `shouldAutoMint` `--remote-debugging-port` case (`:192-196`). **KEEP** the `isMcpAutomationEnabled` suite — especially the `--remote-debugging-port`→`false` invariant. (If any `isAutomationDevEnabled` coverage is genuinely worth keeping, fold it into the `isMcpAutomationEnabled` suite — but the invariant is already covered there.)
5. **`main.js` comments**: `:270` keep (architecture doc; lightly reword if it implies a live `:9222` path); `:1001-1002` rewrite the decoupling comment to drop the `dev:debug (CDP)` example (the path no longer exists) while keeping the "MCP never co-binds the legacy CDP port" intent.
6. **Docs**:
   - `CLAUDE.md` (`:10,12,15,156,157`): make `dev:automation` the canonical dev launch; describe `:9222`/`dev:debug` as **removed**; note the `automation:dev-invoke` dev seam is deprecated; drop "retired in a later flight" → "removed in F9". Keep all MCP-surface guidance.
   - `docs/mcp-automation.md:45`: delete/generalize the `dev:debug`-decoupling sentence (the path is gone) — e.g. "the MCP surface is structurally independent of any legacy CDP path."
7. **Behavior-spec count sweep (AC6)** — for each spec in Inputs, fix BOTH the precondition-probe summary bullet AND the Step-1 table row (the count is duplicated):
   - Convert to **presence of the specific tools that spec drives** (the most robust pattern; do NOT substitute a new hard count `16`/`17`/`21` — the count is fictional at `tools/list` and brittle). E.g. "`tools/list` shows 17 tools incl. getChromeTarget" → "`tools/list` includes the tools this spec drives (e.g. `getChromeTarget`, `readAxTree`, `click`, …)".
   - `mcp-drive-end-to-end`: change "exactly 17 tools" to "the named set is present (≥ those names)"; keep driving the named set; add a one-line note that the F9 tools (`evaluate`/`injectScript`/`openDevTools`/`closeDevTools`) are out of this spec's scope so a future reader doesn't think the set is incomplete.
   - These are project-owned specs; this sweep is the flight-sanctioned exception (DD2b + leg-2 deferral) — do not otherwise alter their assertions. Record "registry = 21 as of F9" in the commit message so the next drift is traceable.
8. **Verify `.mcp.json`** unchanged-and-clean.

## Edge Cases
- **A consumer that genuinely needed the `--remote-debugging-port` arm** — recon found none (renderer argv never carries it; main.js seams are dev-only `--automation-dev`). If the implementer finds one, STOP and surface it (the cut would not be safe).
- **A behavior spec whose pass depends on the EXACT count** (not just a sanity probe) — none should (the count is a precondition sanity check), but if one asserts exact count as its core check, convert carefully (presence of the relevant tools) and note it.
- **`mcp-drive-end-to-end` coverage** — it intentionally drives a named tool set; do not make it require all 21 (the 4 F9 tools are out of its scope) — just stop it breaking on the higher total.
- **Run logs under `tests/behavior/*/runs/`** — historical records; do NOT edit them (they legitimately mention the old apparatus).
- **`missions/` artifacts** — historical; `:9222` references there are records, not live config; leave them.

## Files Affected
- `package.json` — delete `dev:debug`.
- `src/shared/automation-dev.js` — remove `isAutomationDevEnabled` (drop the `--remote-debugging-port` arm + the function + its `module.exports` entry); rewrite the module header comment (`:3-11`) that documents both predicates.
- `src/preload/chrome-preload.js` — repoint import `:8` + call `:79` to `isMcpAutomationEnabled`; fix the `:~76` comment.
- `src/main/main.js` — repoint import `:17` + `:274` + `:981` to `isMcpAutomationEnabled`; reword the `:270` explanatory comment + the `:~1002` decoupling comment to drop the literal `--remote-debugging-port` token (keep rationale).
- `src/main/automation/input.js` (`:7,118,130,131`), `scripts/lib/mcp-client.mjs` (`:~8`), `scripts/mcp-example-client.mjs` (`:~6`) — reword `cdp-driver` comment citations to historical references.
- `scripts/cdp-driver.mjs` — **delete**.
- `test/unit/automation-dev.test.js` — remove the `isAutomationDevEnabled` suite + the `--remote-debugging-port` shouldAutoMint case; keep the rest.
- `CLAUDE.md`, `docs/mcp-automation.md` — rewrite the `:9222`/`dev:debug`/dev-seam narrative.
- `tests/behavior/*.md` (~18) — convert exact tool-count probes to resilient checks.

---

## Post-Completion Checklist

**Complete ALL steps before signaling `[COMPLETE:leg]`:** *(commit deferred to flight end per `/agentic-workflow`.)*

- [x] All acceptance criteria verified
- [x] Tests passing (`npm test` + typecheck + lint)
- [x] **Leg-8 readiness:** the leg-8 dogfood specs no longer halt on a stale count — `mcp-drive-end-to-end`'s "exactly 17" is converted to presence-of-named-set (the others leg 8 runs are already resilient)
- [x] Update flight-log.md with leg progress entry
- [x] Set this leg's status to `landed`
- [x] Check off this leg in flight.md
