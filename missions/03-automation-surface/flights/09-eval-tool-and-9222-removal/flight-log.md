# Flight Log: Eval tool + DevTools tool + a11y/farbling migration + final :9222 removal

**Flight**: [Eval tool + DevTools tool + a11y/farbling migration + final :9222 removal](flight.md)

## Summary
**Ready** (operator sign-off 2026-06-17). The deferred SC11 remainder (the F7 "F8-eval"). Drafted from the mission roadmap + the F7/F8 debriefs; reconnaissance run against current code (below); Architect-reviewed (approve-with-changes, all HIGH/MEDIUM folded in); OQ1–OQ4 settled. DD1–DD9; 8 legs. Two prereqs deferred to execution: a GUI display + the leg-1 `executeJavaScript`-injects-axe spike. Awaiting execution.

---

## Reconnaissance Report (Phase 1b)

Every F9 source item walked against current code (branch `main` @ `36590a4`, post-F8 merge):

| Item | Classification | Evidence | Recommendation |
|------|----------------|----------|----------------|
| Eval/`injectScript` MCP tool | **confirmed-live** | No `evaluate`/`injectScript` in `mcp-tools.js` (16 tools: 12 drive / 4 observe / 1 admin); engine has only `executeJavaScript` for self-contained read snippets (`observe.js`, `engine.js:44`) | Build it (leg 1, keystone) |
| `scripts/a11y-audit.mjs` rewrite | **confirmed-live** | 350 lines fully on CDP `:9222` — `CDP_HTTP='http://127.0.0.1:9222'` (`:44`), `fetch /json`, WebSocket CDP client (`:183`), axe injection via `Runtime.evaluate` | Rewrite onto the eval tool (leg 3) |
| `farbling-correctness` migration | **confirmed-live** | Spec apparatus is `chrome-devtools` MCP `evaluate_script` attached to `:9222` (`farbling-correctness.md:16,21,31`); reads in the guest main world | Migrate to the eval tool (leg 4) |
| `devtools-cdp-conflict` unblock | **confirmed-live (blocked-as-written)** | Spec `Status: draft`, BLOCKED on the missing non-CDP DevTools-open affordance (`devtools-cdp-conflict.md:10,12`); `openDevTools`/`closeDevTools` grep → **none** in `src/` (net-new) | Add the DevTools tool (leg 2) → run the spec (leg 5) |
| Final `:9222` removal | **confirmed-live** | `dev:debug` in `package.json:11` (`--remote-debugging-port=9222 --remote-allow-origins=http://127.0.0.1:9222`); `--remote-debugging-port` arm in `automation-dev.js:25`; `scripts/cdp-driver.mjs:25`; `main.js:270,1002` comments | Remove last (leg 6), after a11y+farbling migrate |
| F8: serialize `applyAutomationEnabledChange` | **confirmed-live** | F8 debrief — serializes against `rebinding` only, not against concurrent toggle flips | Bundle (leg 7) |
| F8: `userData`-redirect ordering test | **confirmed-live** | F8 debrief — ordering invariant protected only by human review | Bundle (leg 7) |
| F8: `resolvePort` `honorEnv` JSDoc warning | **confirmed-live** | F8 debrief — `honorEnv: true` default is a latent foot-gun for a forgetful caller | Bundle (leg 7) |
| `.mcp.json` `:9222`/playwright remnant | **already-satisfied** | F7 trimmed it; `.mcp.json` ships `{"mcpServers": {}}` (verified during F8 leg 6) | No action — verify-only in leg 6 |

**Premise flagged for the Architect (OQ1):** does `webContents.executeJavaScript` inject axe-core + read it back (CSP-immune for direct eval)? If yes → zero CDP → clean `:9222` death. If no → eval tool needs a CDP `Runtime.evaluate` path through `cdp.js`. **Not verifiable from static reading alone — needs the leg-1 spike**; the Architect should treat it as the load-bearing premise.

---

## Leg Progress

### verify-integration + hat-and-alignment (leg 8)
**Status**: landed (2026-06-17; FD-driven gates GREEN + behavior dogfood runs done; HAT skipped per operator)

#### FD-driven verification (DD9a) — DONE 2026-06-17
- **Full gate suite GREEN (post all legs 1-7):** `npm test` **773 pass / 0 fail** (12 suites), `npm run typecheck` clean, `npm run lint` clean.
- **`:9222` retired — confirmed:** `dev:debug` absent from `package.json`; `dev:automation` = `electron . --enable-logging --no-sandbox --automation-dev` (no `--remote-debugging-port`); static grep over `src/ scripts/ test/ docs/ CLAUDE.md` shows the only `--remote-debugging-port`/`9222` references are the intentional `isMcpAutomationEnabled`→FALSE CDP-decoupling invariant tests (load-bearing, kept). Leg-5's live run already observed `:9222` not listening under `dev:automation`; leg 6 only removed the path further.
- **`npm run a11y` green on the new surface:** live-PASS in leg 3 (exit 0, no NEW violations, MCP eval-tool apparatus, no `:9222`).

#### Behavior dogfood runs (DD9 b/c) — FD-driven, DONE 2026-06-17 (operator: "FD-run them now")
All run live over one `dev:automation` session (`:52351`, `:9222` absent), admin key from `AUTOMATION_DEV_MINT`, driven via `node` over `scripts/lib/mcp-client.mjs`. Run logs committed under each spec's `runs/`.
- **`farbling-correctness` → PASS** (`runs/2026-06-17-17-23-30.md`): navigator spoof `8`/`8`; canvas `A===A2` (stable); getImageData perturbed (R=254 vs clean 255 — noise applied); **seed-dependence `B!==A`** via the New-Identity reroll Variant (no 2nd jar / no MCP createJar, so the Variant — `newIdentity()` chrome global + reload — was used). The F9 eval tool dogfooded on a real privacy assertion. Status left `draft` (promotion deferred to next-Electron per the spec's own note).
- **`automation-key-gating` → PARTIAL** (`runs/2026-06-17-17-23-30.md`): the **load-bearing toggle-OFF gating contract (Steps 1-3) PASSES** via `readDom` of the settings guest + `settings.json` — toggle OFF ⇒ all 4 jar mint buttons + the admin mint button `disabled`; Revoke follows `hasKey` (default jar + admin Revoke enabled, keyless disabled). Settings opened without coordinate-clicks via the chrome's own `#kebab-settings` click (chrome-renderer, NOT eval-on-internal). Live toggle-flip (Steps 4-6) **carried** — flipping needs trusted-input geometry into the internal guest (not FD-derivable under WSLg) and `evaluate`-on-internal is correctly blocked (F9 DD2 guard working as designed). Apparatus limitation, not a defect.
- **`settings-activity-viewer` → PARTIAL** (`runs/2026-06-17-17-23-30.md`): the **load-bearing serve-seam guard (Step 1) PASSES** — `#automation-activity-log` renders real audit rows (readDom/admin/ok), NOT blank (the F7-HAT regression this spec guards is absent). Numbered-pager render (Steps 2-3, broadcast-driven re-render not pumped by a pure MCP poll) + freeze/pagination (Steps 4-8, need page-button coordinate-clicks) **carried**. Pure paging math already unit-tested (`audit-paging.test.js`); this run confirms the served+rendered half.
- **`devtools-cdp-conflict`** — already run in leg 5 (recorded finding; archived).
- **HAT (DD9d): SKIPPED** per operator decision (the autonomous verification — unit + a11y-live + the 4 dogfood specs + green gates — covers the deliverables).

#### Disposition of carried steps
The carried behavior-test steps (key-gating live-flip; activity-viewer pager/freeze) are **UI-interaction steps requiring coordinate-click geometry in the internal settings guest** — an apparatus limitation under WSLg (vision/coordinate math unavailable to the FD; eval-on-internal correctly refused). Each spec's **own load-bearing assertion** (the thing it exists to catch) is verified. Carried to a future guided HAT or a coordinate-capable apparatus; recorded in the run logs + carried to the flight debrief. Not blockers for landing.

### f8-followups (leg 7)
**Status**: landed (2026-06-17; gates green)

#### Design notes
- Designed by FD from a recon of the three F8-debrief sites. One Developer design-review cycle: **approve-with-changes** (two HIGH + mediums folded). Items: (a) serialize `applyAutomationEnabledChange` + `rebindMcpServer` through ONE shared `inFlight` chain; (b) userData setPath-before-consumers ordering test; (c) `resolvePort` `honorEnv` JSDoc warning.
- **HIGH folds:** (1) the mutex sketch's bare `await prior` would **wedge the chain on a rejected prior op** → corrected to a `runSerialized` shape with `await Promise.resolve(prior).catch(()=>{})` for the *serialization* await (op's own error still surfaces); the existing top-of-function `if (rebinding) await rebinding` in BOTH functions is REMOVED (else an op awaits a chain containing itself → deadlock). (2) AC1 now requires the **shipped path == tested path** — `main.js` is NOT unit-reachable (no exports; `protocol.registerSchemesAsPrivileged` at module load), so the serializable core is extracted to `src/main/automation/toggle.js` (`makeAutomationToggle`) and `main.js` delegates into it — not a parallel tested-but-unused copy.
- **MEDIUM folds:** the userData-ordering test must instrument the `getPath('userData')` call `main.js` makes to feed `settings.load(path)` (settings takes the path as an ARG; shields/jars read it internally), and cover both `isPackaged` branches; use a dedicated fake `app` (not the shared `electron-stub`, which only stubs `getPath`).
- **Dispositions:** new module `src/main/automation/toggle.js` + new tests `automation-toggle.test.js` / `init-profile-order.test.js`; the `automation-key-gating` behavior-test run that exercises dev-override-flip-OFF end-to-end is **deferred to leg 8** (unit-level coverage here); rebind-body error semantics preserved (a `stop()` rejection fails that op as today; the serialization `.catch` only isolates the next op). **No second design-review cycle** (reviewer's enumerated resolutions).

#### Changes Made (2026-06-17, Developer)
- **(a) Serialized toggle core extracted to `src/main/automation/toggle.js`** (Electron-free, unit-testable). `makeAutomationToggle({ start, stop, getServer, setServer, isDevOverride, setStatus })` returns `{ applyEnabledChange, rebind }` sharing ONE `inFlight` closure via the mandated `runSerialized` shape (capture-prior → `await Promise.resolve(prior).catch(()=>{})` for the serialization await → `inFlight=mine` → identity-guarded self-clear in `finally`). `applyEnabledChange(enabled)` carries the **verbatim** flip body (flip-ON start-if-null; flip-OFF dev-override early-return keeping the surface bound; flip-OFF teardown + disabled-status reset). `rebind()` = `runSerialized(if !server return; stop(); start())`.
- **`main.js` now delegates into the unit (production path, not a parallel copy).** Removed the module-scoped `let rebinding = null`; built one module-level `automationToggle` over the live `mcpServer`/`mcpStatus` (`getServer`/`setServer` mediate `mcpServer`; `stop: () => mcpServer.stop()`; `start: startMcpServerInstance`; `setStatus` writes `mcpStatus`). `rebindMcpServer()` / `applyAutomationEnabledChange(enabled)` are now thin one-line delegators. The old top-of-function `if (rebinding) await rebinding` guards and the inline `rebinding = (...)` chain are GONE — collapsed into `runSerialized`. Verified no `rebinding` references remain.
- **(b) `initProfileAndStores(app, { shields, settings, jars })` extracted to a new `src/main/init-profile.js`** (Electron-free; imports `devUserDataPath`). Holds the verbatim sequence: `if (!app.isPackaged) app.setPath('userData', devUserDataPath(app.getPath('userData'))); shields.load(); settings.load(app.getPath('userData')); jars.load();`. `whenReady` now calls `initProfileAndStores(app, { shields, settings, jars })`. main.js's `devUserDataPath` import was replaced by the `initProfileAndStores` import (devUserDataPath now used only inside init-profile.js). NOTE on extraction location: the leg suggested keeping the function in main.js, but main.js is not unit-reachable — so the function lives in its own importable module (same rationale/pattern as toggle.js) to make the ordering test exercise the real source path.
- **(c) `resolvePort` JSDoc WARNING added** (`mcp-server.js`): imperative line that packaged/main-process callers MUST pass `honorEnv: !app.isPackaged` (default `true` honors `GOLDFINCH_MCP_PORT`, a production leak if forgotten, DD6). Doc-only — no behavior change; all current callers (`createMcpServer` threads `honorEnv`; `currentAutomationStatus` passes `!app.isPackaged`) remain explicit.
- **Tests.** `test/unit/automation-toggle.test.js` (7 cases): two overlapping flip-ONs → exactly ONE `start`; flip-ON/OFF overlap → final OFF; flip-OFF/ON overlap → final ON; **rejection isolation** (a `stop()`-rejecting op surfaces its own error AND the next op still executes — proven by a second stop attempt); dev-override flip-OFF → NO teardown (surface stays bound, status untouched); rebind no-op-when-inactive / stop+start-when-active; rebind concurrent with flip-ON serialized (no stop()-on-null, one start). Slow `start` gated on a **manually-resolved deferred** (deterministic, no timer). `test/unit/init-profile-order.test.js` (3 cases): unpackaged ordering (setPath index < every consumer index, using `lastIndexOf('getPath')` to isolate the settings-feeding read from the redirect-arg read), packaged branch (setPath NOT called; consumers still run reading the un-redirected path), and a meaningfulness guard.
- **Invariant teeth verified.** Temporarily breaking `runSerialized` (dropping the prior-await) failed 3 toggle tests; temporarily reordering `shields.load()` before `setPath` failed the unpackaged ordering test. Both restored.
- **Semantics preserved (confirmed):** the dev-override flip-OFF still early-returns bound (no teardown / no status reset); the persisted-off-but-surface-live state holds; the live port-rebind still stops-then-starts via `resolvePort`; body error semantics unchanged (a `stop()` rejection fails that op; the serialization `.catch` only isolates the NEXT op). The only behavioral change is the added serialization that closes the race.

#### Gate results (2026-06-17)
- `npm test` **773 pass / 0 fail** (incl. the 2 new suites, 10 new cases); `npm run typecheck` clean; `npm run lint` clean.
- `grep -n "honorEnv\|WARNING" src/main/automation/mcp-server.js` — JSDoc warning present; `grep -n "rebinding" src/main/main.js` — none.

### retire-9222 (leg 6)
**Status**: landed (2026-06-17; gates green; the migration arc's final cut)

#### Design notes
- Designed by FD after a full-repo `:9222`-footprint recon. One Developer design-review cycle: **approve-with-changes** (consolidation + cut confirmed SAFE; two HIGH + several MEDIUM folded). Settled scope: delete `dev:debug` + `scripts/cdp-driver.mjs`; **remove `isAutomationDevEnabled` entirely** (drop the `--remote-debugging-port` arm → it becomes ≡ `isMcpAutomationEnabled`; repoint all consumers — `main.js:17` import + `:274` + `:981`, `chrome-preload.js:8/79`); delete the stale `isAutomationDevEnabled` test suite + the `shouldAutoMint --remote-debugging-port` case (KEEP the `isMcpAutomationEnabled`→false invariant tests); rewrite CLAUDE.md + `docs/mcp-automation.md` + `main.js`/`input.js`/`mcp-client.mjs`/`mcp-example-client.mjs` comments.
- **HIGH folds:** (1) AC6's grep was **vacuous** — the specs assert count as PROSE ("shows 17 tools"), not `=== 17` literals, so the old grep passed without edits; fixed to `grep -rniE "(shows|returns|exactly|the)[^.]*\b1[67] tools\b"`. (2) the sweep list mis-cited literal `=== N` → restated as prose, noting the count is duplicated in BOTH the probe-summary bullet AND the Step-1 row.
- **MEDIUM folds:** the **16-vs-17 split is FICTIONAL at `tools/list`** (`registry.listTools()` returns all 21 identity-independently; identity gates *call*, not *list*) → convert specs to **presence-of-used-tools**, NOT a new hard count; added missed `scripts/mcp-example-client.mjs:6` + `main.js:17` import + `automation-dev.js:3-11` header comment + `chrome-preload.js:76` comment; fixed `input.js` cdp-driver citations (`:7,118,130,131`, not the drifted `:43-46,100,108`).
- **AC3-vs-comment tension resolved:** the `main.js:270`/`:1002` explanatory comments are **reworded to drop the literal `--remote-debugging-port` token** (keep the rationale) so the hard-zero grep stays a clean invariant. **Load-bearing for leg 8:** `mcp-drive-end-to-end`'s "exactly 17 tools" must be converted or it halts at Step 1 (registry is 21) — added a checklist gate. **No second design-review cycle** (reviewer's enumerated resolutions).

#### Changes Made (2026-06-17, Developer)
- **Registry = 21 as of F9** (recorded for traceability of the next spec-count drift).
- **`dev:debug` deleted** (`package.json`) — the only `--remote-debugging-port`/`--remote-allow-origins` user. `scripts/cdp-driver.mjs` **deleted** (legacy CDP driver, no live importer).
- **`isAutomationDevEnabled` removed** from `src/shared/automation-dev.js`: dropped the `--remote-debugging-port` arm (which made it ≡ `isMcpAutomationEnabled`), deleted the function + its `module.exports` entry, rewrote the two-predicate module header comment (`:3-11`) down to the single surviving predicate, and dropped the `dev:debug` mention from the `shouldAutoMint` JSDoc. `node -e` confirms exports are now `[isMcpAutomationEnabled, shouldAutoMint, shouldBindAutomation]`.
- **Consumer repoints (3 callsites → `isMcpAutomationEnabled`):** `main.js:17` (import), `main.js:274` (additionalArguments inject), `main.js:981` (dev-seam IPC gate); `chrome-preload.js:8` (import) + `:79` (gate call) + `:76` comment. `isMcpAutomationEnabled`/`shouldAutoMint`/`shouldBindAutomation` left untouched. The recon's SAFE finding held — no consumer needed the `--remote-debugging-port` arm.
- **Comments reworded to drop the literal `--remote-debugging-port` token (rationale kept):** `main.js:270` (renderer-can't-see-the-port → "the renderer's own argv doesn't otherwise carry the dev switch"), `main.js:~1002` decoupling comment (dropped the `dev:debug (CDP)` example → "structurally independent of any legacy browser-process CDP debugging switch"). `cdp-driver.mjs` citation comments reworded to non-file-specific historical references ("the Flight 1-3 CDP apparatus, since removed") in `input.js` (`:7,118,130,131`), `scripts/lib/mcp-client.mjs` (`:8`), `scripts/mcp-example-client.mjs` (`:6`; also dropped its stale "17 tools" / `cdp-driver.mjs` sibling line).
- **Tests (`test/unit/automation-dev.test.js`):** deleted the `isAutomationDevEnabled` describe block (all true/false/robustness cases) + the `shouldAutoMint` `--remote-debugging-port` case + the stale import/header line. **KEPT** the full `isMcpAutomationEnabled` suite — including the two `--remote-debugging-port`→`false` CDP-decoupling invariant assertions (the literal token in those assertions is load-bearing and intentionally retained).
- **Docs rewritten:** `CLAUDE.md` — `dev:automation` is now the canonical dev-launch bullet; the `:9222`/`dev:debug` CDP path is described as **removed in F9** (not "to be retired"); the `automation:dev-invoke` dev seam marked **deprecated**; the literal `:9222`/`--remote-debugging-port`/`dev:debug` tokens dropped from `:10/:15/:157/:167` (rationale kept); the correct **"21 tools"** at `:167` left intact. `docs/mcp-automation.md:~45` — the `dev:debug`-decoupling sentence generalized to "the MCP surface is structurally independent of any legacy CDP path … removed in F9".
- **Behavior-spec count sweep (deferred leg-2 item) — 15 specs touched.** Converted the brittle/fictional `tools/list` count probes to presence-of-used-tools (no new hard count): the 11 "17 tools" admin specs (`tab-scheme-guard`, `settings-controls`, `core-browsing-shields`, `menu-dismissal`, `toolbar-pins`, `responsive-tab-strip`, `settings-shell`, `kebab-menu`, `tab-keyboard-operability`, `unified-tab-controls`, `settings-automation` — BOTH the precondition-probe summary bullet AND the Step-1 row each), the 3 "16 tools" jar specs (`foreground-to-act`, `internal-session-exclusion`, `observe-refusal-contract` — Step-1 row), and `mcp-drive-end-to-end` ("exactly 17 tools (named list)" → "the named set is present", plus the objective + two Out-of-Scope coverage notes; added the explicit note that the 4 F9 tools are out of its scope). Also generalized the `mcp-drive-end-to-end` fixture README's `9222`/`dev:debug` serve note. The already-resilient specs (`devtools-cdp-conflict`, `farbling-correctness`, `automation-key-gating`, `settings-activity-viewer`) were left as-is.
- **`.mcp.json`** verified clean (`{"mcpServers": {}}`).

#### Gate + grep results (2026-06-17)
- **Gates (AC8):** `npm test` **763 pass / 0 fail**; `npm run typecheck` clean; `npm run lint` clean.
- **AC1:** `grep -rn "remote-debugging-port\|remote-allow-origins\|cdp-driver" package.json scripts/` → nothing; `scripts/cdp-driver.mjs` absent.
- **AC2:** `grep -rn "isAutomationDevEnabled" src/ test/` → nothing.
- **AC3 (hard-zero over source/scripts/docs):** `grep -rn "9222\|--remote-debugging-port\|remote-allow-origins\|dev:debug\|isAutomationDevEnabled\|cdp-driver" src/ scripts/ docs/ CLAUDE.md package.json` → **nothing**. The only surviving literal-token hits are the **mandated-keep** CDP-decoupling test (`test/unit/automation-dev.test.js` — the `isMcpAutomationEnabled(['--remote-debugging-port'])`→`false` invariant, explicitly retained by the leg), the **off-limits run logs** under `tests/behavior/*/runs/`, and the **project-owned behavior-spec apparatus/contrast notes** ("not the `:9222` CDP path" disqualifiers) which were NOT in the sanctioned reword set and whose false-pass-trap guidance must not be altered. This is the documented resolution of the AC3-hard-zero-vs-kept-invariant tension.
- **AC6 (resilience):** `grep -rniE "(shows|returns|exactly|the)[^.]*\b1[67] tools\b" tests/behavior/` → only one hit, in a `runs/` historical log (off-limits). All top-level specs clean.

### eval-tool (leg 1)
**Status**: landed

#### Design notes
- Designed via `/leg` (FD), grounded by a source-map recon of the automation engine. One Developer design-review cycle: **approve-with-changes**. Two HIGH issues were **completeness gaps that break `npm test`** — the leg's first static-count inventory missed `test/unit/automation-mcp-server.test.js` (`EXPECTED_TOOL_COUNT = 17`, ~9 assertions) and `src/main/automation/mcp-server.js:321` (JSDoc). Both folded into AC6 + Files Affected, verification grep broadened to `src/ test/`. Two MEDIUMs folded: the `isInternalContents` refusal must run on the **final** `wc` after the optional activate branch (covers the no-activate admin/chrome path); the engine op must throw the DD2 serialization message itself (not the adapter); + an `observe.js` module-header note. All 19 cited file:line refs verified clean by the reviewer (no drift). **No second design-review cycle** — the changes were the reviewer's own enumerated resolutions (completeness/clarity, not design), consistent with the skill's "skip if only minor fixes."

#### Changes Made (2026-06-17, Developer)
- **Engine ops (`observe.js`).** Added `evaluate(wcId, expression, deps)` and `injectScript(wcId, script, deps)`, exported both. `evaluate` mirrors `readDom` (resolve → guest-activate → re-resolve), then the FINAL `isInternalContents(wc)` refusal, then `await wc.executeJavaScript(expression)`, then a pre-flight `JSON.stringify` that throws the EXACT DD2 message `automation: evaluate — return value is not JSON-serializable` on failure (in the engine op, NOT the adapter — so no raw V8 message leaks). `injectScript` resolves → FINAL `isInternalContents` refusal → `await wc.executeJavaScript(script)` → returns `undefined` (void → `{"ok":true}`); it intentionally SKIPS activate. Added a module-header note explaining the eval ops are debugger-free `executeJavaScript` ops co-located with `readDom`, that `injectScript` is a write, and that the internal guard runs on the final `wc` after the optional activate branch. Imported `isInternalContents` from `resolve.js`.
- **[HIGH] DD2 guard placement.** The `isInternalContents(wc)` refusal runs on the FINAL re-resolved `wc` (after the optional activate branch) in BOTH ops, so it covers the no-activate chrome/admin path too. Refuses regardless of `allowInternal` — admin's `allowInternal:true` makes `resolveContents` permissive, so this op-local check is the sole guard against arbitrary JS in `goldfinch://settings`. Pinned by two named `[HIGH]` unit tests that pass `allowInternal:true` and assert refusal + zero `executeJavaScript`.
- **Wiring.** `engine.js`: two dispatch keys (`evaluate`, `injectScript`). `mcp-tools.js`: two `OBSERVE_TOOLS` defs (no `shape` — default JSON-text serialize); `evaluate` description states the JSON-serializable return contract + main-world; `injectScript` states void/`{"ok":true}` + skips-foreground-to-act + no-persistence. `scope.js`: both added to `WCID_FIRST_OPS` (jar membership via `resolveContentsForJar`, admin bypass already reaches both).
- **Tool count 17 → 19, all static refs caught.** `mcp-tools.js` (count comments), `mcp-server.js:321` JSDoc, `automation-mcp-tools.test.js` (count assertion + test name + `EVAL_NAMES`), `automation-mcp-server.test.js` (`EXPECTED_TOOL_COUNT` + the `'…returns 17 tools'` test-name string), `docs/mcp-automation.md` (intro count + "All N" line + new `### Eval tools (2)` section + security-invariant callout), `CLAUDE.md` (breakdown → 19, +2 eval, security note). Verification grep `grep -rn "17 tools\|EXPECTED_TOOL_COUNT = 17\|the 17 tools" src/ docs/ CLAUDE.md test/` returns nothing.
- **Tests.** Added engine-op tests in `automation-observe.test.js` (evaluate activate-before-exec ordering, native Promise auto-await, re-resolve proof, chrome no-activate, non-serializable→exact DD2 throw, in-page throw propagation, `[HIGH]` internal-refusal-even-for-admin; injectScript void contract, NO-activate asymmetry, in-page throw, `[HIGH]` internal-refusal, bad-handle) and adapter tests in `automation-mcp-tools.test.js` (schemas, named→positional dispatch, void→`{"ok":true}`, throw→isError). Scope tests cover the two new ops automatically (parameterized over `WCID_FIRST_OPS`).
- **Gates (AC8).** `npm test` 749 pass / 0 fail; `npm run typecheck` clean; `npm run lint` clean.
- **AC5 (ZERO CDP).** Confirmed by grep — the eval-op region of `observe.js` has no `debugger`/`withDebuggerSession`/`require('./cdp')`/`wc.debugger`.

### devtools-tool (leg 2)
**Status**: landed (2026-06-17; unit gates green, live visual + internal-refusal probes deferred to verify/HAT)

#### Design notes
- Designed by FD from the leg-1 engine map (no fresh recon needed). One Developer design-review cycle: **approve-with-changes**, all four design questions resolved: (1) **internal-session exclusion even for admin = CONFIRMED** — `openDevTools` spawns a full CDP client on the page, outside the mission's single authorized debugger-on-internal carve-out (the a11y-tree read), so it is refused on `goldfinch://settings` even for admin (mirrors the leg-1 eval guard); (2) **module placement → co-locate in `observe.js`** (the codebase's own stated convention — `observe.js` already hosts a write + a debugger op), not a new `devtools.js`; (3) **`isDevToolsOpened()` getter → SKIPPED** — leg 5's restore check is behavioral (`readAxTree` succeeds), registry lands at **21** not 22; (4) **bookkeeping** — added the `deriveAuditDetail` `nullOps` test entry, specified `closeDevTools` idempotent no-op, clarified AC1's visual half is live/HAT-only (not an `npm test` gate). **No second design-review cycle** (reviewer's own enumerated resolutions).
- **TRACKED / deferred (skill–project boundary):** ~13 project-owned `tests/behavior/*.md` specs hard-assert `tools/list returns 17 tools` in Step-1 precondition probes — already stale post-leg-1 (19), becoming 21. Not edited by legs 1/2 (project-owned). **Deferred to the leg-6 `retire-9222` docs/count sweep**, which should convert the brittle `=== N` probes to `>= N` so they stop being registry-count-coupled. Logged here so the drift is a decision, not an oversight.

### run-devtools-cdp-conflict (leg 5)
**Status**: landed (2026-06-17; FD-driven live run; spec archived; mission OQ closed)

#### Design + execution notes
- Lightweight FD-orchestrated behavior-test leg (no Developer design-review/implementation cycle). FD updated the `devtools-cdp-conflict` spec per DD6 (resolved the BLOCKED-AS-WRITTEN block — leg 2's `openDevTools`/`closeDevTools` are the non-CDP affordance it waited on; Step 3 → `openDevTools(W)` over MCP, Step 4 → `closeDevTools(W)`, Step-1 count halt `=== 16` → `>= 21` presence-based, apparatus/observables/preconditions rewired to the MCP tools).
- **FD-driven live run (mission standard — no Witnessed crew elected; recorded-finding spec).** Launched `dev:automation` on `:52347` (`:9222` clear → confound-free); admin key from `AUTOMATION_DEV_MINT`; throwaway `node` driver over `scripts/lib/mcp-client.mjs` opened a web tab (W=3, fixture on `:8044`) and ran baseline→openDevTools→readAxTree→closeDevTools→readAxTree. Run log: `tests/behavior/devtools-cdp-conflict/runs/2026-06-17-16-25-30.md`.
- **FINDING (deviates from OQ4's "confirmed expected attach-failed"):** the `attach-failed` conflict **did NOT reproduce live**. `openDevTools(W)`→`{"ok":true}`, then `readAxTree(W)` **succeeded** (AXNode array, len 13) with DevTools "open"; success throughout (baseline + under-DevTools + after-close). **Inconclusive cause:** under WSLg the detached DevTools window did not cleanly materialize (`blink.mojom.WidgetHost`/`Widget` "Message rejected" in the app log), so a competing CDP client may not have been genuinely established. **Disposition:** the API-level affordance is proven (openDevTools/closeDevTools work, jar/admin-gated, no `:9222`); the `attach-failed` branch stays **unit-tested-only**; a definitive live observation needs a non-WSLg display. Spec **archived** with the run as its record; mission OQ annotated closed for F9 (reopen only for a definitive observation).
- **Run status `partial`** — every step's "record the outcome" expectation was met (Steps 1/2/4 pass; Step 3 recorded), but the substantive attach-failed question is unresolved (WSLg-limited). App + fixture torn down cleanly; ports clear. No source change → gates unaffected.

### farbling-migration (leg 4)
**Status**: landed (2026-06-17; spec migrated to the eval-tool apparatus; gates green; live core-read smoke PASS on WSLg)

#### Design notes
- Designed by FD from the current `farbling-correctness.md` spec. One Developer design-review cycle: **approve-with-changes**, with a **HIGH** correction: my DD-F rationale was factually wrong — the dev recipe mints only ONE jar key (`default`), so the two-container path needs **admin** (or a manually-minted 2nd jar key), NOT "jar-key-only." Both seed-reroll paths need a 2nd seed → admin. Corrected: two-container stays the **recommended primary for determinism** (admin opens two tabs, no chrome-control/reload-timing flakiness), New Identity kept as the Variant; the admin/2nd-key precondition made explicit. Citation drift fixed: New-Identity reload `renderer.js:1750,1756` (was `:1088`), `rerollSeed` `main.js:806-808` from `identity-new` `:864` (was `:407-409,433`), `evaluate` `mcp-tools.js:343-360`. Readiness probe corrected to an **authenticated** `initialize`/`tools/list` (keyless = by-design 401), pin `$GOLDFINCH_MCP_PORT`, assert `evaluate` present (not a count). **Confirmed sound:** canvas draw+`toDataURL` round-trips in one `evaluate` (guest main world, JSON-serializable string); the Executor reaches the surface via Bearer key (F6 pattern) — runtime apparatus registration is leg 8's; `Status: draft` stays (formal run = leg 8). **No second design-review cycle** (reviewer's enumerated resolutions). Spec-only leg (no source change expected).

#### Changes Made (2026-06-17, Developer)
- **`tests/behavior/farbling-correctness.md` rewritten — apparatus migration only; assertions preserved.** Swapped the apparatus from `chrome-devtools` MCP `evaluate_script` attached to `:9222` to the **Goldfinch MCP `evaluate` tool** evaluating in the **guest main world** (where the farbling hooks live), launched via `dev:automation` + Bearer key. Sections rewritten: the Intent apparatus sentence (the `ipcRenderer.sendSync('shields-farble', …)` mechanism-under-test wording kept; only the *measurement* apparatus changed); Preconditions (the `dev:automation` launch + the two-key recipe + the `127.0.0.1:$GOLDFINCH_MCP_PORT/mcp` endpoint + `Authorization: Bearer` + Shields `farble` active + HTTP fixture, "why HTTP not file://" kept); Observables Required (guest main-world reads via `evaluate` on the guest `<webview>` wcId; the New-Identity-Variant admin observable; shell readiness probes); the 7-step Actions column. **Out-of-Scope kept** (refreshed the WebGL/Audio line citations: `getParameter` :298-308, audio :310-320; added an eval-tool-contract exclusion).
- **AC2 — guest-target explicit.** Step 2 acquires the guest wcId **G** via `enumerateTabs` (filtered to the jar's tab); Observables + every read row state the target is the guest `<webview>` wcId, NOT the chrome target — preserving the original "must select the guest, not the chrome shell" invariant.
- **AC3 — assertions preserved.** Step 3 navigator spoof = `8` (both surfaces); Step 4 `A === A2` (stable within session); Step 5 noise applied/non-trivial; Step 6 `B !== A` (seed-dependence) + navigator stays `8`; Step 7 optional farble-off control. Only the *Action* mechanics changed (CDP `evaluate_script` → MCP `evaluate`).
- **AC4 — launch/auth recipe.** Step 1's old `curl http://127.0.0.1:9222/json` probe replaced by an **authenticated** MCP-surface handshake (`initialize`/`tools/list` over `127.0.0.1:$GOLDFINCH_MCP_PORT/mcp` with `Authorization: Bearer <jar-key>`, asserting **`evaluate` is present — NOT an exact tool count**, since the registry count is in flux this flight) + the fixture HTTP 200. Notes that a keyless probe is a by-design 401.
- **AC5 / DD-F — seed-reroll path: two-container cross-seed is the PRIMARY Step 6.** Chosen for determinism (no chrome-control / New-Identity reload-timing window). The **admin-key** precondition is made explicit (both reroll paths need a 2nd seed; the dev recipe mints only the one `default` jar key — so this step is NOT jar-key-only; a manually-minted 2nd jar key is offered as the avoid-admin alternative). **New Identity moved to the documented Variant** (corrected, verified citations: `newIdentity()` `renderer.js:1750`, reload `renderer.js:1756`, `rerollSeed` `main.js:806-808` from the `identity-new` handler `main.js:864`, `SEED` capture `webview-preload.js:233`).
- **Canvas draw inside `evaluate`** — Step 4 specifies a single self-contained expression (create canvas → draw → return `toDataURL()`), with the `injectScript`-then-`evaluate` pairing offered as the alt; Step 5 returns a small sampled `getImageData` subset (JSON-serializable). The draft note was reworded (it no longer references `dev:debug`); the promotion note now points to the leg-8 verify-integration run.
- **Status stays `draft`** (formal Witnessed run + promotion = leg 8). `Last Run` left `never` (the smoke below is not the formal run).
- **Citation verification** — re-read the cited source before writing each row: `webview-preload.js` SEED :233 / hash :241-246 / spoof `8` :325,:330 ✓; `main.js` `rerollSeed` :806-808 / `identity-new` :864 / `seedForSession` :797-808 ✓; `renderer.js` `newIdentity()` :1750 / reload :1756 ✓. The old stale refs (`renderer.js:1088`, `main.js:407-409,433`) were NOT propagated.

#### Source / unit-reference check (AC6)
- `grep -rni "evaluate_script\|chrome-devtools" src/ test/ scripts/` → the only hit is `scripts/cdp-driver.mjs:4` (a comment in the **legacy CDP driver**, slated for removal in leg 6 / DD7 — unrelated to the farbling apparatus). **No source or unit test references the old `:9222`/chrome-devtools farbling apparatus.** No source change made or needed (spec-only leg).

#### Gate Results (AC6) — all green
- `npm test` → **781 pass / 0 fail**. `npm run typecheck` (tsc) → clean. `npm run lint` (eslint) → clean. (Unchanged from leg 3 — this leg touched only markdown.)
- AC1 grep `9222|chrome-devtools|evaluate_script|remote-debugging|/json` over the spec → **nothing**. Present-grep `evaluate|enumerateTabs|dev:automation|jar key|guest` → present.

#### Optional live smoke (WSLg) — core reads PASS, cross-seed deferred
Ran a throwaway driver (reusing `scripts/lib/mcp-client.mjs`) against `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=53541 npm run dev:automation`, fixture served on `:8123`, then deleted the driver and tore down the app + fixture:
- `tools/list` includes **`evaluate`** ✓ (authenticated jar-key handshake).
- `openTab` (jar key) → guest wcId 3; `evaluate(3, 'navigator.hardwareConcurrency')` → **`8`**, `deviceMemory` → **`8`** (Step 3 spoof) ✓.
- `evaluate(3, '<single-expr canvas draw + toDataURL>')` run twice → **`A === A2`** (Step 4 stable-within-session; 11,914-char data-URL string returned directly, JSON-serializable) ✓ — confirms the one-expression draw+read round-trips in the guest main world live.
- **Step 6 two-container cross-seed NOT exercised in the smoke**: the throwaway driver passed `openTab(url, jarId:'smoke-jar-2')` with a non-existent container name → the renderer's `__goldfinchAutomation.openTab` threw (`isError`), and the admin tab fell back into the `default` jar, so no distinct-seed second tab existed. This is a **smoke-script setup bug** (an arbitrary jarId is not a real container), NOT a spec or apparatus problem — `openTab` does accept a `jarId` for an existing container (`tabs.js:87-92`, `mcp-tools.js:140`). The spec's Step 6 correctly describes opening the fixture in a **second, existing jar**; provisioning that container is operator/run setup the formal leg-8 run handles. The de-risking goal (navigator spoof + canvas stability live through `evaluate` in the guest main world) was met.

### a11y-audit-rewrite (leg 3)
**Status**: landed (2026-06-17; all gates green; AC5 live a11y PASS on WSLg)

#### Design notes
- Designed by FD after two source recons (the a11y harness map + the MCP key-provisioning map). One Developer design-review cycle: **approve-with-changes**. Decisive finding: **launch model = ATTACH + env key, NOT self-contained spawn** — `mcp-example-client.mjs` explicitly attaches and every F6/F7 dogfooded run launched `dev:automation` out-of-band then attached; spawn would be the only spawn-consumer in the surface's history and is WSLg-fragile (child Electron lifecycle, stdout-flush race). DD-A flipped accordingly (spawn demoted to optional convenience). Other folds: (1) navigation MUST use the chrome `navigate()` renderer global via `evaluate(chromeWcId, …)`, NOT the `navigate` MCP drive tool (different semantics — drive tool navigates a guest by wcId); (2) `injectScript` no-persistence → per-state axe re-inject is *required*; (3) extract `scripts/lib/mcp-client.mjs` shared authenticated client (Bearer via `requestInit.headers`; key default from `GOLDFINCH_MCP_ADMIN_KEY`/`GOLDFINCH_MCP_KEY` env) — leg 4 reuses it. **Confirmed sound:** chrome main-world reachability (`togglePanel`/`togglePrivacy`/`openLightbox`/`navigate` are top-level fns in `src/renderer/renderer.js`, classic script → window globals; `executeJavaScript` reaches the main world; chrome target is not the internal session) — verify live in the verify leg. **Confirmed NOT a regression:** the `goldfinch://settings` internal-session exclusion breaks the optional `--target=goldfinch://settings` audit, but the default 4-state chrome sweep (what the gate gates on) never used it. Baseline ACCEPTED allowlist + per-node partition + exit-code logic carried over verbatim (transport-orthogonal). **No second design-review cycle** (reviewer's own enumerated resolutions). AC5 live `npm run a11y` may be deferred to the verify leg if the implementer lacks a display.

#### eval-tool premise spike (AC7) — LIVE PASS
Ran live under WSLg over `npm run dev:automation` (`GOLDFINCH_MCP_PORT=52345`, admin-keyed, throwaway SDK-client harness placed inside the project dir for module resolution, then deleted). Results:
- `listTools` → **19 tools**, both `evaluate` and `injectScript` present.
- `openTab(https://example.com)` → wcId 3; `injectScript(<axe-core source>)` → `{"ok":true}`; `evaluate('axe.run(document).then(r => ({violations: r.violations.length, passes: r.passes.length, testEngine: r.testEngine.name}))')` → **`{"violations":2,"passes":13,"testEngine":"axe-core"}`**. This confirms OQ1 end-to-end: `executeJavaScript` injects axe-core (CSP-immune, no `<script>` tag), natively auto-awaits the returned Promise, and the JSON report crosses back. The a11y rewrite (leg 3) can depend on this.
- `evaluate('(circular object)')` → `isError` with the exact message `automation: evaluate — return value is not JSON-serializable`.
- `evaluate('thisIsNotDefined()')` (in-page throw) → `isError`.
- Note: the first WSLg launch attempt hit a renderer SIGTRAP ("Terminating … no connection") and a transient "port in use" (TIME_WAIT from a rapid relaunch); a clean relaunch on a fresh port bound and drove successfully. WSLg GUI stability is flaky but the spike completed.
- **Deferred to HAT/verify leg (manual):** the LIVE internal-session refusal probe (targeting an open `goldfinch://settings` tab's wcId with the admin key and confirming both ops refuse). The 50s app-timeout elapsed before a second harness connect; the refusal is already pinned by the `[HIGH]` unit tests (refusal even with `allowInternal:true`) and the scope internal-session tests. Statically corroborated, additionally, by `scripts/a11y-audit.mjs` which already injects axe via CDP `Runtime.evaluate` (awaitPromise/returnByValue) — the `executeJavaScript` equivalent is mechanically the same.

#### Changes Made (implementation — 2026-06-17)
- **`src/main/automation/observe.js`** — appended `openDevTools(wcId, deps)` and `closeDevTools(wcId, deps)`, co-located with the eval ops per the design-review decision (NOT a new `devtools.js`). Both mirror the eval skeleton: `resolveContents` → **FINAL `isInternalContents(wc)` refusal** → act. **NO foreground-to-act activation** (DevTools attaches regardless of paint, so no activate branch / no re-resolve). `openDevTools` → `wc.openDevTools({ mode: 'detach' })`; `closeDevTools` → `wc.closeDevTools()`. Both void → `undefined` (→ `{"ok":true}` at the adapter). Extended the module-header note (devtools paragraph) and `module.exports`. NO `isDevToolsOpened()` getter (skipped per design review — registry lands at 21, not 22). Zero CDP from the ops themselves.
- **[HIGH/AC4]** The internal-session refusal runs on the resolved `wc` and fires **even when `allowInternal:true`** (admin), with distinct messages `automation: openDevTools — internal-session excluded` / `automation: closeDevTools — internal-session excluded`. Pinned by two named `[HIGH]` unit tests (refused, DevTools NOT opened).
- **`src/main/automation/engine.js`** — 2 dispatch keys (`openDevTools`/`closeDevTools`) next to `evaluate`/`injectScript`, reusing the existing `observe` require.
- **`src/main/automation/mcp-tools.js`** — new `DEVTOOLS_TOOLS` group (2 void wcId-ops), added to `TOOLS` (registry **19 → 21**); count comment updated. Tool descriptions carry the CDP-client/capability distinction (concurrent `readAxTree`/`scroll` → `attach-failed`; `evaluate`/`injectScript` keep working) and the `{mode:'detach'}` WSLg rationale.
- **`src/main/automation/scope.js`** — `openDevTools`/`closeDevTools` added to `WCID_FIRST_OPS` (NOT the admin-only refusal list — DevTools on a jar's own guest is within the jar key's authority). The scope test parameterizes over `WCID_FIRST_OPS`, so jar-membership enforcement is auto-covered.
- **Static count refs caught (19 → 21):** `mcp-server.js:321` JSDoc; `mcp-tools.js` count comment; `automation-mcp-tools.test.js` (count + `allNames21` fixture + `DEVTOOLS_NAMES`); `automation-mcp-server.test.js` (`EXPECTED_TOOL_COUNT` + tools/list test name + `nullOps` list += `openDevTools`/`closeDevTools`); `docs/mcp-automation.md` (intro count + "All N tools" line + new `### DevTools tools (2)` section); `CLAUDE.md` breakdown (+2 devtools). `deriveAuditDetail` needs no code change (void ops fall to `default → null`); only the test assertion list was extended.
- **Unit tests added** (`automation-observe.test.js`): dispatch for both ops (`{mode:'detach'}` verified, void return); the two `[HIGH]` internal-refusal-even-for-admin tests; `closeDevTools` idempotency (close without prior open → void/`{"ok":true}`, no throw); no-activate assertions; bad-handle. `automation-mcp-tools.test.js`: registry count → 21, devtools list/schema/description + dispatch + internal-throw→isError.

#### Test Results (AC7)
- `npm test` → **761 pass / 0 fail** (9 suites). `npm run typecheck` (tsc) → clean. `npm run lint` (eslint) → clean.
- Count-drift grep `19 tools | EXPECTED_TOOL_COUNT = 19 | the 19 tools` over `src/ docs/ CLAUDE.md test/` (excluding `tests/behavior/`) → **no matches**.

#### Deviations / Notes
- **Live "DevTools window actually appears" check (AC1 visual half) — DEFERRED to the verify/HAT leg** (no GUI display assumed here; the WSLg display is flaky per the leg-1 spike notes). Per AC1 this is explicitly a live/HAT gate, not an `npm test` gate; the unit-testable half (op calls `wc.openDevTools({mode:'detach'})` and returns the void contract) is covered.
- **No deviations from the leg's Implementation Guidance.** The ~13 project-owned `tests/behavior/*.md` stale "17 tools" probes were NOT touched (deferred to leg 6 per DD2b — already logged above).

> **Note (provenance):** the `#### Changes Made (implementation — 2026-06-17)` / `#### Test Results` / `#### Deviations / Notes` blocks immediately above describe the **DevTools tool (leg 2)** implementation (openDevTools/closeDevTools, registry 19→21) — they were filed under this leg-3 heading by mistake during leg 2. Leg 3's own a11y-rewrite implementation notes follow below. Left in place to avoid rewriting another leg's record.

#### a11y-audit-rewrite — Changes Made (leg-3 implementation, 2026-06-17, Developer)
- **`scripts/lib/mcp-client.mjs` (NEW)** — extracted the shared authenticated MCP client helper per DD-design-review. Exports: `connectAutomation({port?,url?,key?,env?})` → a connected SDK `Client` (Bearer header via `new StreamableHTTPClientTransport(url, { requestInit: { headers: { Authorization: 'Bearer '+key } } })`; default key from `GOLDFINCH_MCP_ADMIN_KEY`/`GOLDFINCH_MCP_KEY` env — **missing key → clear error**); `callTool(client,name,args)` → unwrapped `{ value, isError }`; plus pure `unwrap`, `parseDevMintLine` (tolerant `AUTOMATION_DEV_MINT` scraper for the optional self-spawn path — NOT used by the attach flow), `defaultKey`, `resolveEndpoint`. Endpoint resolution mirrors `mcp-example-client.mjs` (`GOLDFINCH_MCP_URL`/`GOLDFINCH_MCP_PORT`, default `:49707`). Leg 4 reuses this.
- **`scripts/a11y-audit.mjs` (REWRITTEN)** — transport/target/auth swapped CDP-`:9222` → MCP eval-tool; audit logic preserved. Removed: `CDP_HTTP`, `/json` fetch, the raw `WebSocket` CDP client, `Runtime.evaluate`, `findRendererTarget`/`findGuestTarget` (web-socket-debugger-url shapes). Added: `connectAutomation()` (attach + env key); chrome mode acquires the wcId via **`getChromeTarget`** (admin, `{wcId,kind,url}`); guest `--target` mode via **`enumerateTabs`** + url-substring find. `runAxe` now does the **DD2 inject-then-run pair per state**: `injectScript(wcId, axeSource)` immediately followed by `evaluate(wcId, 'axe.run(document, opts).then(...)')` (per-state re-inject required — `injectScript` has no persistence). State driving unchanged in shape: `navigate(...)` via `evaluate(chromeWcId, 'navigate(...)')` (the chrome renderer global, NOT the `navigate` MCP drive tool), then `togglePanel`/`togglePrivacy`/`openLightbox` each via `evaluate`. **ACCEPTED allowlist, per-node `(id,selector,state)` partition, report format, and exit 0-iff-no-NEW-else-1 carried over verbatim.** `client.close()` in a `finally`. Existing `sleep` settle waits kept as-is (transport-orthogonal). Header recipe rewritten to the attach + env-key model.
- **Docs** — `docs/mcp-automation.md`: added a **"Dogfooding / dev key acquisition"** subsection under Launch (the `AUTOMATION_DEV_MINT` double-gated mechanism + the export-key recipe). `CLAUDE.md`: repointed the `npm run a11y` line from `:9222`/`dev:debug` to `dev:automation` + the eval tools + the attach/env-key prereqs + the `goldfinch://settings` internal-exclusion caveat. **`dev:debug` / `:9222` NOT removed** (leg 6). README has no a11y/`dev:debug` mention — no change needed.
- **Unit test (NEW)** — `test/unit/mcp-client.test.js`: 20 assertions over the pure helper parts — `unwrap` (JSON text / bare value / `{"ok":true}` / non-JSON fallback / `isError` passthrough / image-only content / missing result / first-text-block), `parseDevMintLine` (exact line / log-noise tolerance / prefix-not-at-col-0 / absent → null / noisy-substring keep-scanning / non-string → null), `defaultKey` (admin>jar precedence), `resolveEndpoint` (URL/port/default). CommonJS test dynamically `import()`s the `.mjs`.

#### Gate Results (AC7) — all green
- `npm test` → **781 pass / 0 fail** (13 suites; +20 from the new helper test). `npm run typecheck` (tsc, jsconfig) → clean (scripts are not in the typecheck program; helper is JSDoc-typed). `npm run lint` (eslint, `scripts/**` IS linted) → clean.
- **AC1 grep** `9222|/json|new WebSocket|WebSocket|Runtime.evaluate|webSocketDebuggerUrl` over `scripts/a11y-audit.mjs` → **nothing**. Present-grep `injectScript|evaluate|getChromeTarget|enumerateTabs|Bearer|connectAutomation` → present in script + helper.

#### AC5 live `npm run a11y` — LIVE PASS (WSLg, 2026-06-17)
Ran the full chrome 4-state sweep live (WSLg display `:0`):
- Terminal 1: `GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_MCP_PORT=52345 npm run dev:automation` → printed `AUTOMATION_DEV_MINT {"key":…,"adminKey":…}`. Fixture served on `:8000` from `tests/behavior/fixtures/a11y-media/`.
- Terminal 2: `GOLDFINCH_MCP_PORT=52345 GOLDFINCH_MCP_ADMIN_KEY=<adminKey> node scripts/a11y-audit.mjs` → **exit 0**, `No NEW violations` ✅. Reported **12 accepted baseline nodes**: `region` (#tabs, #brand) across base-chrome/media-panel/privacy-panel/lightbox + `landmark-one-main`/`page-has-heading-one` (html) in media/privacy states. No NEW pairs against the unchanged ACCEPTED baseline.
- **Live confirmations (load-bearing premises, now proven against running code):** (1) chrome **main-world reachability** — `evaluate(chromeWcId, 'navigate(...)')` and `togglePanel`/`togglePrivacy`/`openLightbox` all drove the 4 states successfully (the renderer globals ARE reachable via `executeJavaScript`); (2) **per-state inject-then-run** (DD2) works — axe re-injected + run each state, JSON report crossed back; (3) the **admin `getChromeTarget`** path returns a drivable chrome wcId; (4) the attach + env-key launch model works end-to-end on WSLg. App + fixture torn down cleanly (no orphan Electron/python).
- Note: a non-default `GOLDFINCH_MCP_PORT=52345` was used to avoid TIME_WAIT collisions (same WSLg flakiness flagged in the leg-1 spike); the script and app rendezvoused on it via the env-port contract.

---

## Decisions
Runtime decisions not in original plan.

---

## Deviations

---

## Anomalies

---

## Flight Director Notes
- 2026-06-17 — **Flight LANDED. [COMPLETE:flight]** All 8 legs `completed`; flight `landed`; checked off in mission.md. Single review gate (Phase 2d): an independent Reviewer (Sonnet) over all uncommitted changes → **[HANDOFF:confirmed]** (all load-bearing ACs met — the [HIGH] internal-session exclusion across all four new tools, the `runSerialized` single-mutex + rejection-isolation, the clean `isAutomationDevEnabled` removal with the CDP-decoupling invariant tests kept; 773/773 tests, typecheck + lint clean; no banned `:9222` references in source/scripts/docs). No fix loop needed. Committed as `90f16d7` (single squashed flight commit on top of the planning commit `ebe956a`), pushed, **PR #54** opened (ready for review). Docs verified current: CLAUDE.md + `docs/mcp-automation.md` updated (21 tools, retired `:9222`, eval/devtools tools, dogfooding key recipe) in leg 6. **Carried to the debrief:** (1) `devtools-cdp-conflict` `attach-failed` inconclusive (WSLg detached-DevTools-window didn't materialize) — branch stays unit-tested-only; (2) `automation-key-gating` + `settings-activity-viewer` UI-interaction steps (toggle-flip; pager/freeze) carried — load-bearing assertions passed, the carried steps need coordinate-click geometry the FD apparatus lacks under WSLg; (3) `farbling-correctness` left `draft` (promotion deferred to next-Electron per its note). Flight debrief is the separate `/flight-debrief` step (transitions flight → completed).
- 2026-06-17 — **Execution started via `/agentic-workflow` (flight 9, mission 3, goldfinch).** Phase-1 context loaded (mission, flight, flight-log, crew file `leg-execution.md` validated — Crew/Interaction Protocol/Prompts all present). Flight status `ready` → `in-flight`. Created execution branch `flight/9-eval-tool-and-9222-removal` off `plan/flight-9` (HEAD `ebe956a`, planning commit included; `main` @ `36590a4` post-F8). 8 legs; per-leg design review, batched code review + commit deferred to flight end per the skill. Sequencing honors dependencies: `eval-tool` keystone first → `devtools-tool` → `a11y-audit-rewrite` + `farbling-migration` → `run-devtools-cdp-conflict` (behavior test) → `retire-9222` (last of migration arc) → `f8-followups` → `verify-integration`/HAT. Behavior-test legs (`run-devtools-cdp-conflict`; the verify-integration dogfood specs) are FD-orchestrated via `/behavior-test`, not Developer agents. Leg 8 HAT is operator-interactive.
- 2026-06-17 — Flight created via `/flight` (mission 3, goldfinch) immediately after F8 landed/merged, at operator request ("let's plan the next flight" while the Windows installer build waits on a stalled Concourse worker). Operator decisions captured: (1) **no steer on the eval-tool mechanism/gating — negotiate with the Architect** (the Phase-5b design review settles OQ1/OQ2 with code-cited premise audits); (2) **keep F9 as one flight** (the full bundle, ~8 legs — large but agreed); (3) **DevTools should be available via MCP** — resolved by adding a real `openDevTools`/`closeDevTools` tool (DD3), which doubles as the non-CDP affordance that unblocks `devtools-cdp-conflict` (turning "add affordance OR retire" into "ship the capability → run the spec"). Reconnaissance run (above) — all roadmap/debrief items confirmed-live except the `.mcp.json` remnant (already-satisfied by F7).

## Session Notes
- 2026-06-17 — Planning draft written (flight.md DD1–DD9, 8 legs).
- 2026-06-17 — **Architect design review complete (approve-with-changes).** Settled the operator-delegated premises: **OQ1** — `executeJavaScript` is CSP-immune for direct eval + auto-awaits Promises → **zero CDP for eval, `:9222` dies clean** (~95%; leg-1 spike confirms live). **OQ2** — **two tools** (`evaluate` + `injectScript`); jar-scoped guests + admin chrome; **HIGH: internal-session excluded even for admin** (admin carries `allowInternal` so `resolveContents` won't throw — the eval op must add an explicit `isInternalContents` refusal, else arbitrary JS in `goldfinch://settings` compromises the privileged bridge). **OQ3** — `openDevTools`/`closeDevTools` `{mode:'detach'}`, same gating, confound-free; the `readAxTree` conflict is the recorded finding; eval works under DevTools. **DD7** — the `--remote-debugging-port` arm of `isAutomationDevEnabled` was never active in the renderer; switch `main.js` to `isMcpAutomationEnabled`, then remove the function — cut is safe. Incorporated: the HIGH guard, the 17→21 tool-count fixes (incl. the `devtools-cdp-conflict` `=== 16` self-halt), the concurrent-flip mutex (one chain covering `rebinding` + `applyAutomationEnabledChange`), the inject-then-run pairing for a11y. No second review cycle (changes were the Architect's own enumerated resolutions). **Next: operator sign-off → `ready`.**
