# Flight Log: Bulk spec migration + ungated-path hardening (scoped)

**Flight**: [Bulk spec migration + ungated-path hardening (scoped)](flight.md)

## Summary
Flight `planning` (drafted 2026-06-16). Scope (operator): migrate the surface-compatible remaining Group-B specs onto the admin/jar MCP surface, add audit-log paging (20/page, in-memory), and **harden** the ungated `:9222` path (not fully remove it). The `evaluate` MCP tool + `farbling-correctness` + the `a11y-audit.mjs` rewrite + the *final* `:9222` removal are **deferred to a follow-on (F8-eval)** because they need in-page JS evaluation the surface lacks. Persistence of the audit log is explicitly a **future mission** (operator). SC11 part 2 (scoped).

Operator decisions (planning, 2026-06-16): scope = migrate-what-fits + harden + paging; audit paging = **20/page, in-memory**, persistence deferred to a future mission; eval-dependent items (`farbling-correctness`, `a11y-audit` rewrite, full `:9222` removal) deferred to F8-eval.

---

## Reconnaissance Report
Sources: mission SC11 (part 2), the F6 debrief action items, the F6 recon's Group-A/Group-B split. Every cited item walked against current code (2026-06-16).

| Item | Classification | Evidence / disposition |
|------|----------------|------------------------|
| **Un-migrated Group-B specs** | **confirmed-live** | 12 Group-B at F6; F6 migrated 3 (`tab-keyboard-operability`, `kebab-menu`, `settings-shell`). Remaining drive via `:9222`/`cdp-driver`: `unified-tab-controls`, `responsive-tab-strip`, `toolbar-pins`, `menu-dismissal`, `tab-scheme-guard`, `settings-controls`, `settings-automation`, `core-browsing-shields`, `farbling-correctness`. Classified by launch apparatus (`dev:debug`/`cdp-driver` count vs `dev:automation`). The 8 Group-A `mcp-*`/`foreground-to-act`/`observe-refusal-contract`/`internal-session-exclusion`/`devtools-cdp-conflict` only *mention* `:9222` to disqualify it — already on the surface. |
| **`settings-automation`** | **confirmed-live (migratable)** | `tests/behavior/settings-automation.md:23,44,55` — reads the chrome `#automation-indicator` + the settings-guest viewer via `cdp-driver`/`:9222`; the MCP session it stages is the system-under-test. Migrate the READS to admin `getChromeTarget`+`readDom`/`readAxTree`. Eval-free (DOM elements). |
| **`core-browsing-shields`** | **needs-human-recheck** | Guest-driving (shields). Migratable to a jar key + guest `wcId` IF its assertions are DOM/a11y/content-readable (request-blocked → element/content absence). If it reads script-runtime values → defer with `farbling`. Premise-audit at `migrate-guest-specs` leg. |
| **`farbling-correctness`** | **confirmed-live; DEFERRED (eval)** | Asserts on script-runtime fingerprint values (`navigator.*`, canvas/WebGL) not in `outerHTML`; the surface has no in-page `evaluate`. Also uses the disqualified `chrome-devtools` MCP. → **F8-eval** (needs the `evaluate` tool). |
| **`a11y-audit.mjs` rewrite** | **confirmed-live; DEFERRED (eval)** | `scripts/a11y-audit.mjs:1-4,44,137` — injects axe-core via CDP `Runtime.evaluate` at `:9222` (CSP-bypass) and runs axe's rule engine in-page. `readAxTree` gives the AX *tree*, NOT axe rule evaluation. Rewrite needs an `evaluate`/`inject` capability. → **F8-eval**. |
| **`dev:debug` `--remote-allow-origins=*`** | **confirmed-live** | `package.json:11`. Wide-open Origin allow-list (the cautionary tale). F7 **hardens** (narrow to loopback-Origin) — can't fully remove while a11y+farbling still use `:9222`. → `harden-ungated-path` leg (DD3). |
| **`.mcp.json` Playwright-`:9222` entry** | **confirmed-live** | `.mcp.json` registers `playwright` `--cdp-endpoint http://127.0.0.1:9222`. With the bulk leaving `:9222`, trim it (the migrated specs use the MCP surface, not Playwright-MCP). Full `.mcp.json` cleanup completes when `:9222` is removed (F8-eval). |
| **`devtools-cdp-conflict` `BLOCKED-AS-WRITTEN`** | **confirmed-live; carry blocked** | Needs a non-CDP DevTools-open affordance (deferred since F3). Stays blocked through F7; re-evaluate at `harden-ungated-path` (default: annotate + keep blocked → F8-eval). |
| **Audit-log paging gap** | **confirmed-live** | `settings.js` `LOG_DISPLAY_CAP = 50` silent slice of the 500-entry ring (`audit-log.js`). F6 debrief flagged it as a pre-bulk-migration must (bulk runs generate hundreds of entries). → `audit-log-paging` leg (DD4): 20/page, in-memory, renderer-only. |

**Premise audit (both axes) for the deferred items** — the surface can *act* + *observe* DOM/a11y/pixels, but has **no in-page JS `evaluate`**: confirmed against `observe.js:READ_DOM_SNIPPET` (outerHTML only) and the 17-tool registry. `farbling` (runtime-value reads) and `a11y-audit` (axe injection) are therefore not expressible on the current surface → F8-eval. This is the load-bearing reason for the scope split.

---

## Design Review Notes
Architect review (2026-06-16): **approve with changes** — all incorporated (single cycle; reviewer-prescribed fixes, no new design risk → operator sign-off is the next gate).
- **[HIGH] `core-browsing-shields` is not pure guest-driving** — its Step-5 tracker-block assertion reads the **chrome** privacy panel (`.tag.blk`/`#privacy-count`), reachable only via `getChromeTarget`+`readDom` (admin); a jar key is refused. Reframed DD2 + the leg to an **admin** spec (guest nav via `openTab` + chrome-panel read via `getChromeTarget`); operator Q1 → admin-only (simpler; the spec asserts chrome-visible state, not jar isolation).
- **[HIGH] Audit paging + live ring is incoherent without a freshness contract** — the ring grows on every tool call (broadcast per `record`), so naive page indices shift under the operator mid-read. DD4 now specifies **freeze-on-page-2+** (page 1 live; page 2+ snapshots + "paused — N newer · back to live"), newest-first, renderer-side windowing over the full snapshot.
- **[MED] DD3 hardening / Node WebSocket** — `a11y-audit.mjs` uses Node 22's global `WebSocket`, which sends **no `Origin`** by default; narrowing `--remote-allow-origins` may be a no-op or may break the attach. Added an **empirical WS-probe before landing the flag** + a **named divert** in Adaptation Criteria.
- **[MED] `settings-automation` dual target** — the settings-guest viewer reads need the `settings-shell` admin-`allowInternal` `enumerateTabs`→internal-`wcId` pattern, not `getChromeTarget` (chrome-only). Technical Approach §3 + the leg updated.
- **[LOW] `.mcp.json` trim ordering** — trim the Playwright-`:9222` entry only after the specs that name it (`responsive-tab-strip`) are rewritten; noted in DD3.
- Suggestions incorporated: paging newest-first; `devtools-cdp-conflict` annotation clarified (block = missing non-CDP DevTools affordance, not the Origin); a confound-free "`:9222` stopped" re-run AC added to Verification; renderer-side-windowing-over-full-snapshot noted.
- Confirmed sound: the scope split (migrate-what-fits / defer-eval / harden-not-remove); the premise audit (no in-page eval → farbling + a11y genuinely can't migrate); the bulk-before-harden sequencing; reuse of the F6 template; prerequisites.

---

## Leg Progress

### 2026-06-16 — Leg 1 `pressKey-modifier-chords` (source) → landed
**Status**: landed (implementation complete; batched — not committed per-leg).

**Changes Made**:
- `src/main/automation/input.js` — `keyEvents(name, modifiers = [])` now: resolves a single printable letter/digit (`/^[a-z0-9]$/i` → uppercase-letter / as-is-digit keyCode) for chord use, after the existing `ShiftTab` composite and `KEY_MAP` branches (so existing names win first); normalizes + validates modifiers against the canonical `{control, shift, alt, meta}` via a `MODIFIER_ALIASES` map (`ctrl`→`control`, `cmd`/`command`→`meta`, `option`→`alt`, case-insensitive), **throwing** on unknown; merges the composite's intrinsic `shift` with caller modifiers and de-dupes in canonical order. No-modifier calls are byte-identical (empty list → `modifiers:[]`; `ShiftTab` → `['shift']`). `pressKey` signature → `(wcId, name, modifiers, deps)`.
- `src/main/automation/engine.js:74` — `pressKey` wrapper forwards `modifiers`.
- `src/main/automation/mcp-tools.js` — `pressKey` tool schema gains optional `modifiers: { type:'array', items:{ enum:['control','shift','alt','meta'] } }` (canonical four only — aliases are normalized defensively at the `keyEvents` layer but not advertised); `wcId` stays required, `name`/`key` `anyOf` unchanged; `call` forwards `args.modifiers`; description + key-name doc mention chords + single-char keys.
- `src/main/automation/mcp-server.js` — `deriveAuditDetail` `case 'pressKey'` appends the modifiers (`key=M+control`, `key=P+control+shift`); bare-key + empty-modifiers calls keep `key=<name>`.
- `docs/mcp-automation.md` — pressKey entry documents `modifiers` (Ctrl+M / Ctrl+Shift+P examples, accepted names, unknown-modifier rejection); audit-detail note mentions the chord suffix.
- Tests: `test/unit/automation-input.test.js` (+chord shape, multi-modifier, alias + case normalization, unknown-modifier throw, single-char/digit resolution, ShiftTab merge/de-dupe, byte-identical backward-compat; the 3 existing `pressKey(…)` calls updated for the new 4-arg signature). `test/unit/automation-mcp-tools.test.js` (+modifiers schema/enum assertion, +chord call-wiring; the 3 existing pressKey call-mapping assertions updated for the forwarded third arg). `test/unit/automation-mcp-server.test.js` (+single/multi/empty-modifier audit-string cases).

**Notes**:
- Gates green: `npm test` **670 pass / 0 fail**, `npm run typecheck` clean, `npm run lint` clean.
- AC4 (backward compat) honored — the only existing-test edits are the mechanical signature/arg updates the new `modifiers` slot requires (an intentional extension); no behavior assertion changed. Per the AC4 note, no single-char-throws inversion was fabricated (the suite has none).
- AC6 (trusted-input invariant) — chords ride `actOn` → `sendInputEvent`; no debugger/CDP/`:9222` touched (the only debugger path, `scroll`, is unchanged).
- Electron-vocabulary correction from design review respected: the canonical normalization is our own contract choice, not an Electron requirement (`^42` `sendInputEvent` accepts both `ctrl` and `control`).
- A typecheck constraint surfaced: `modifiers` cannot be JSDoc-optional (`[modifiers]`) since `deps` follows it — typed as `{string[]|undefined}` instead (matches the `engine.js` wrapper); `keyEvents`' `modifiers = []` default absorbs `undefined`.

### 2026-06-16 — Leg 2 `migrate-chrome-specs-a` (spec-authoring) → landed
**Status**: landed (markdown-only; batched — not committed per-leg).

**Changes Made** (apparatus rewrite of 3 Group-B specs onto the admin MCP surface; step semantics preserved, no checkpoint added/dropped):
- `tests/behavior/unified-tab-controls.md` — Preconditions/Observables/Step-1 probe rewritten from `dev:debug`/`:9222`/Playwright-MCP to the admin MCP surface (`dev:automation` + `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=49707`; admin Bearer; `getChromeTarget`→chrome `wcId`). CDP verbs → MCP tools: Step 1 probe → `tools/list` (17) + `getChromeTarget`; mouse new-tab/container clicks → coordinate `click(wcId,x,y)` located via `captureWindow()`; keyboard activation → focus-anchor `click` then `pressKey(wcId,'Tab'/'ShiftTab'/'Enter'/'Space'/'Escape')`; roving-tabindex/`aria-expanded`/focused-node → `readAxTree(wcId)`; jar-dot → `readDom(wcId)`; focus-ring delta → `captureWindow()`. Focus-anchor + coordinate-click notes added. Out-of-Scope "not CDP-drivable" → "not drivable over the MCP surface" (semantics unchanged). `chrome-devtools`-disqualified warning preserved.
- `tests/behavior/responsive-tab-strip.md` — same apparatus rewrite. **Geometry made screenshot-observed**: Steps 2–4 (tab widths / overflow-scroll fallback / favicon+close visibility) judged from single `captureWindow()` frames; Steps 5–6 (width-invariance + deferred-reflow timing) judged from **before/after `captureWindow()` deltas** (Validator compares two frames); the `mouseleave` re-expand induced by a `click(wcId,x,y)` at a safe coordinate **outside** `#tabstrip` (no move-only primitive). Maximize/restore (Step 7) read from `data-state`/accessible name via `readDom`/`readAxTree`; WSLg caveat preserved. Step 6 carries the Validator-can't-separate-mouseleave-from-click F8-eval-defer flag. Existing Out-of-Scope (no exact floor px / scroll-onset count) preserved verbatim. Added explicit "no numeric `getBoundingClientRect`/`scrollWidth` over the surface — screenshot is source of truth" + no-move-only + focus-anchor notes.
- `tests/behavior/toolbar-pins.md` — **dual-target** rewrite: chrome toolbar (`#toggle-media`/`#toggle-privacy`) via `getChromeTarget`+`readDom`/`readAxTree`/`captureWindow`; settings **Appearance** toggles via admin `enumerateTabs`→guest `wcId` (`click(guestWcId,...)`/`pressKey(guestWcId,...)`, `aria-pressed` via `readAxTree(guestWcId)`); `settings.json` `toolbarPins` via filesystem Read. **Step 6 shortcut migrated to the leg-1 chord capability**: focus-anchor `click(wcId,...)` then `pressKey(wcId,'M',['control'])` (Shields variant `pressKey(wcId,'P',['control','shift'])`). **Step 9 (`npm run a11y`) left verbatim** as the F8-deferred axe harness (shell command, not migrated) with an explanatory note. Status **stays `draft`**; no `Last Run` fabricated. Out-of-Scope "not CDP-drivable" → "not drivable over the MCP surface". Two-target keep-them-straight + focus-anchor + coordinate-click notes added.

**Notes**:
- AC6 grep (`9222|cdp-driver|dev:debug|remote-debugging` over the 3 specs) returns **nothing** (exit 1 / no match).
- AC8 gates green: `npm test` **670 pass / 0 fail**, `npm run typecheck` clean, `npm run lint` clean (these are doc edits; leg-1's source + tests in the tree stayed green).
- Mirrored the F6-migrated `tab-keyboard-operability.md` / `settings-shell.md` framing (port-pin + Bearer-key block + `getChromeTarget` probe + the two apparatus-rule notes) for consistency.
- No `Last Run` lines touched, no Out-of-Scope assertion semantics changed, no other spec's `:9222` references touched. Historical `runs/` logs untouched.
- Live runs are leg 8 (`verify-integration`); this leg authors only.

### 2026-06-16 — Leg 3 `migrate-chrome-specs-b` (spec-authoring) → landed
**Status**: landed (markdown-only; batched — not committed per-leg).

**Changes Made** (apparatus rewrite of 3 more Group-B specs onto the admin MCP surface; step semantics preserved, no checkpoint added/dropped):
- `tests/behavior/menu-dismissal.md` — **full migrate** (chrome-only + guest page-click). Preconditions/Observables/Step-1 probe rewritten from `dev:debug`/`:9222`/`cdp-driver`/Playwright-MCP to the admin MCP surface (`dev:automation` + dev-mint/admin env + `GOLDFINCH_MCP_PORT=49707`; admin Bearer; `getChromeTarget`→chrome `wcId`). **Two-`wcId` bookkeeping** spelled out: menu opens are coordinate `click(chromeWcId,…)` re-located via `captureWindow` each time (pill shifts); **page-click dismissal (Steps 2–3) clicks the active guest webview `wcId`** (`enumerateTabs`) — rides the chrome `window`-blur handler (`renderer.js`) when focus crosses into the webview; **in-chrome outside-click (Step 5) stays on the chrome `wcId`** — rides the `document` pointerdown handler. **All focus assertions migrate WITHOUT eval**: read `readAxTree(chromeWcId)` and scan for the node whose `focused` property is set (Escape-restores-to-trigger Step 6; arrow/Home/End-stay-in-menu Step 7; Space/ArrowUp open-and-focus-first/last Step 9). Keyboard via `pressKey(chromeWcId,…)` (focus-anchor first). **Stale "manual-only / not cleanly CDP-drivable" disclaimer reconciled**: the `click(guestWcId,…)` IS now a real trusted page click (no longer manual); row-conventions footnote + Out-of-Scope updated to leave **only genuine OS/app-switch focus loss** as manual. The DD1 preload-forward Variant dropped (page-click is now driven directly). `chrome-devtools`-disqualified warning kept. Status stays `active`; `Last Run` untouched.
- `tests/behavior/tab-scheme-guard.md` — **full migrate** (multi-target, purely observational). Apparatus header → admin MCP surface. **Observational integrity preserved**: the hostile `window.open`/`window.location`/`<iframe>`/`fetch` vectors are driven **by the trigger page's own buttons** (coordinate `click(guestWcId,…)` on the web-guest `wcId`, located via `captureWindow`), **never by the harness** — explicit precondition + row-conventions note. Outcomes observed via `enumerateTabs()` (no forbidden-scheme tab), `readDom(guestWcId)` (webview stays on the original `http://`), `captureScreenshot`, and the fixture's DOM status elements **`#goldfinch-fetch-result`** (Step 11) / **`#goldfinch-embed-result`** (Step 10) — the fixture performs the fetch/embed and writes the result; harness only reads. Step-1 probe → `tools/list`(17) + `getChromeTarget` + `openTab` the HTTP fixture. Multi-target kept straight: chrome `wcId` (`getChromeTarget`, Steps 12–13 trusted kebab→Settings) vs web-guest `wcId` (`openTab`/`enumerateTabs`) vs internal-guest `wcId` (`enumerateTabs` `allowInternal`). `chrome-devtools`-disqualified warning kept. Status stays `active`; `Last Run` untouched (13/13 history preserved).
- `tests/behavior/settings-controls.md` — **partial migrate**. **Apparatus header migrated WHOLESALE** (Preconditions + Observables + Step-1 probe → admin MCP surface) per AC1, even though Steps 9–12 defer (per-step-body deferral only). Steps 1–8 migrated: settings guest `wcId` via `enumerateTabs({ allowInternal: true })`; toggle/input drive via `pressKey`/`typeText(guestWcId,…)`; toggle/input state via `readDom`/`readAxTree(guestWcId)` (focused-node, no eval); chrome slide-out panel (Step 5) via `getChromeTarget`+`readDom` (+ the leg-1 `pressKey(chromeWcId,'P',['control','shift'])` chord option); `shields.json`/`settings.json` via filesystem; new-tab home-effect (Step 8) via `enumerateTabs`+`readDom`. **Steps 9 & 10 annotated deferred-to-F8-eval** with the old CDP/`:9222`/`Runtime.evaluate` wording **stripped** — re-phrased to name the in-page `evaluate` MCP tool; assertion intent kept inline (Step 9: `window.goldfinchInternal` absent in web tabs; Step 10: page-context `ipcRenderer.invoke('internal-settings-set',…)` rejected + `settings.json` unchanged). **Steps 11–12** kept verbatim as the F8-deferred axe harness (`npm run a11y`) with a one-line deferred-harness note. Status stays `active`; `Last Run` untouched.

**Notes**:
- **AC7 grep** (`9222|cdp-driver|dev:debug|remote-debugging` over the 3 specs) returns **zero matches** (grep exit 1). All deferral annotations name "`evaluate` MCP tool / F8-eval" / `npm run a11y` — none carry the four tokens.
- **AC8 gates green**: `npm test` 0 fail, `npm run typecheck` clean, `npm run lint` clean (doc edits only; leg-1 source + tests in the tree stayed green).
- **Key design-review correction honored**: focus assertions migrated **without** in-page eval — `document.activeElement` → `readAxTree` focused-node; `getBoundingClientRect()` re-query → `captureWindow`-locate; `document.getElementById('webview').focus()` → `click(guestWcId,…)` coordinate page-click. Mirrored the green-live `tab-keyboard-operability.md` framing + leg-2's `unified-tab-controls`.
- **F8-eval defers carried for F8 planning / debrief**: `settings-controls` **Step 9** (`typeof window.goldfinchInternal === undefined` in a web guest — a script-runtime read with no DOM/a11y/pixel manifestation) and **Step 10** (page-context `ipcRenderer.invoke('internal-settings-set', { homePage:'https://evil.test/' })` privilege-escalation probe) both need an in-page `evaluate(wcId, expr)` MCP tool the current surface lacks → F8-eval. Steps 11–12 (`npm run a11y` axe-injection over `goldfinch://settings`) remain the F8-deferred axe harness (in-page rule engine, not migrated). All four are annotated in-spec, not dropped.
- No `Last Run` lines touched, no Out-of-Scope assertion semantics changed (only the menu-dismissal manual-only reconciliation, which the leg directed), no other spec's `:9222` references touched. Historical `runs/` logs untouched.
- Live runs are leg 8 (`verify-integration`); this leg authors only.

### 2026-06-16 — Leg 4 `migrate-settings-automation` (spec-authoring) → landed
**Status**: landed (markdown-only; batched — not committed per-leg).

**Changes Made** (dual-target apparatus rewrite of `tests/behavior/settings-automation.md` onto the admin MCP surface; staged session under test unchanged; step semantics preserved, no checkpoint added/dropped):
- **Apparatus header (AC1)** — Preconditions / Observables / Step-1 probe rewritten from `dev:debug`/`:9222`/`cdp-driver.mjs` to the admin MCP surface (`dev:automation` + `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT={port}`; admin Bearer block mirroring `settings-shell`; `tools/list`(17) + `getChromeTarget` probe). `chrome-devtools`-disqualified warning preserved. Added the focus-anchor + coordinate-click + property-vs-attribute apparatus-rule notes. Single `dev:automation` instance serves both the harness driving session and the staged session(s).
- **Dual-target routing (AC2)** — chrome `#automation-indicator`/badge/`title`/`aria-label` reads go via `getChromeTarget`→chrome `wcId` + `readDom`/`readAxTree`/`captureWindow`; the entire `goldfinch://settings` `#automation` section (toggle, address/port fields, Keys subsection, `#automation-active-sessions`, `#automation-activity-log`) goes via admin `enumerateTabs` (`allowInternal`)→internal-guest `wcId` (NOT `getChromeTarget`). Two-`wcId` keep-them-straight note added; each step names its target.
- **Property-vs-attribute (AC3)** — Steps 3/6/7/8 read post-interaction control state from the **filesystem store** (`automationEnabled`/`automationPort`/key hashes in `userData/settings.json`) as the **authoritative witness**, reusing Step 8's "assert the *stored* value, not the live checkbox" framing across all of them. `readAxTree` `checked`/`value` is framed as a **secondary UI-reflection check degradable to `partial`** (AX `checked` is a string `"true"`/`"false"`; AX-on-guest shape LIVE-UNKNOWN pending leg-8), with re-render + `readDom` documented as the fallback. Text/`hidden`/`disabled`/`title` reads stay on `readDom` (attribute-serialized — Step 9 `#automation-admin.hidden`, Step 8 Revoke `disabled` correctly `readDom`). No step's pass/fail spine depends on `outerHTML` reflecting a mutated property or on an unproven AX shape.
- **Steps 2–10 (AC4)** — every UI-control step's Action/Expected intent preserved; only apparatus framing + mechanism words changed. Drive via coordinate `click(guestWcId,…)` (located via `captureWindow`) / `typeText(guestWcId,…)` (focus-anchor first) / `pressKey`. **Step 5** reads `#automation-message` ("Copied") via `readDom`, not the clipboard (clipboard is not an MCP observable).
- **Steps 11–13 (AC5/AC6)** — staged jar/admin session (loopback `initialize` POST) kept **exactly as written** (system under test). Indicator/viewer count/list assertions expressed relative to the **harness admin-session baseline** (staged + 1, identity `admin` / name `app / chrome`). Staged session disambiguated by snapshotted `sessionId`/jarId/`since` (both rows are `kind:'admin'` once Step 12 stages an admin session). **Action log accounted as polluted**: Step 11 asserts the staged session's entries **by staged jarId, NOT newest-first** (the newest rows are the harness's own `readDom`/`readAxTree`/`getChromeTarget` reads). Step 12 reframed: indicator **already carries `.admin`** from the harness — assert the *staged admin row is added*, not "gains `.admin`". Step 13 asserts the MCP-observable behavior (staged session **removed on DELETE**, list returns to harness-only baseline) + keeps the proper-vs-ungraceful (DD6) distinction.

**Notes**:
- **AC7 grep** (`9222|cdp-driver|dev:debug|remote-debugging` over the spec) returns **zero matches** (grep exit 1). The staging POST names `/mcp` + `{port}`, not `:9222`.
- **AC8 gates green**: `npm test` 0 fail, `npm run typecheck` clean, `npm run lint` clean (doc edit only; the in-tree leg-1 source + tests stayed green).
- **Reflexivity observability limit + leg-9 HAT dependency (record for leg-9 planning / debrief):** the absolute **zero-session / indicator-hidden** state (Step 11 empty-state; Step 13 proper-termination "returns to *No automation sessions*" + "indicator hides") is **NOT observable over the MCP surface** — every read is itself a live admin session, so the harness is always ≥1 session. These sub-assertions are routed to the **leg-9 HAT**, which must witness the indicator hiding when the last *real* session closes **with the MCP harness disconnected** (a human/dev-tools observer, not an MCP client). The dogfooding-reflexivity reason is inline in the spec (Steps 11/13 + the row-conventions caveat + Out-of-Scope). SC10's *present/named/distinguished/removed-on-DELETE* behavior stays covered over the MCP surface against the baseline; only the *hidden-at-true-zero* frame moves to the HAT.
- **AX-checked/value degradability pending leg-8 (record for leg-8 live confirmation):** the `readAxTree` `checked`/`value` UI-reflection check (Steps 3/8) is marked **degradable-to-`partial`** because guest AX-node `checked`/`value` carriage is LIVE-UNKNOWN until leg-8 confirms it against actual `getFullAXTree` output (AX `checked` is the *string* `"true"`/`"false"`). The store read is the authoritative witness regardless; leg-8 should confirm whether the AX sub-read holds or the re-render + `readDom` fallback is needed.
- No `Last Run` line touched (12/13 history preserved); status stays `active`; `## Out of Scope` extended (added the indicator-hidden→HAT bullet + action-log self-noise exclusion), no other spec's `:9222` touched; historical `runs/` logs untouched.
- Live runs are leg 8 (`verify-integration`); the hidden-at-zero sub-checks land on the leg-9 HAT. This leg authors only.

### 2026-06-16 — Leg 5 `migrate-core-browsing-shields` (spec-authoring) → landed
**Status**: landed (markdown-only; batched — not committed per-leg).

**Changes Made** (apparatus rewrite of `tests/behavior/core-browsing-shields.md` from CDP-`:9222` onto the admin MCP surface as an **ADMIN** spec per DD2; step semantics preserved, no checkpoint added/dropped):
- **Apparatus header (AC1)** — Preconditions / Observables / Step-1 probe rewritten from `dev:debug`/`:9222`/CDP to the admin MCP surface (`dev:automation` + `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT={port}`; admin Bearer block mirroring `unified-tab-controls`/`settings-shell`; `tools/list`(17) + `getChromeTarget` + fixture HTTP 200 probe). `chrome-devtools`-disqualified warning kept (and re-worded to "not a CDP/DevTools-port attach path"). Focus-anchor + coordinate-click apparatus-rule notes added.
- **Admin-spec shape (AC2, DD2)** — explicit Precondition stating this is an ADMIN spec: guest nav (Steps 3–4, 6) via `openTab`/`navigate`/`enumerateTabs`; the Step-5 chrome privacy-panel read via `getChromeTarget`+`captureWindow`+`readDom` (both `getChromeTarget` and `captureWindow` admin-gated; a jar key is refused `getChromeTarget`, `scope.js:149`). One identity — correct because the spec asserts chrome-visible state, not jar isolation.
- **Step 5 eval-free, correct witness (AC3)** — the old `Runtime.evaluate document.querySelectorAll('.tag.blk')` / `getElementById('privacy-count').textContent` apparatus dropped. New: `captureWindow(chromeWcId)` to locate the Shield toggle, coordinate `click #toggle-privacy` to **open the panel**, then `readDom(chromeWcId)` and assert a **`class="tag blk"` row naming `google-analytics.com`** in the returned `outerHTML` (PRIMARY blocked-specific witness; static rendered tags, present only when the panel is open). Stated explicitly: this is `readDom`, not eval. The inaccurate "shows `Shield (N)`" wording removed — `#privacy-count` correctly reframed as the **toolbar badge** (bare-integer *total* count, `.hidden` at zero), kept only as an optional ≥1 secondary cross-check.
- **Param-strip via readDom.url (AC4)** — Step 4 navigates the guest to the fixture at `?utm_source=test&q=keep` and asserts `readDom(guestWcId).url` shows `utm_source` STRIPPED, `q=keep` PRESERVED (the `redirectURL` clean URL). The "do NOT assert the privacy aggregate's `stripped` count (0 for mainFrame by design — `recordRequest` early-returns on `resourceType==='mainFrame'`)" note preserved verbatim in intent.
- **Steps 1–3, 6** — Step 1 probe = `tools/list`(17) + `getChromeTarget` + fixture HTTP 200. Step 2 launch smoke = `getChromeTarget` (chrome `wcId`) + `enumerateTabs` (≥1 guest). Step 3 = `openTab('https://example.com/')` + `readDom(guestWcId)` body "Example Domain" + `url`. Step 6 = second `openTab` + poll `enumerateTabs` until two guests + `activateTab`.
- **Served fixture precondition kept** (tracker `<script src=google-analytics>` + `utm_*` params). **CRITICAL rationale prose rewrite for AC6**: the old "use a port other than 9222 (the CDP port)" → "use a port distinct from `{port}` (the MCP port)" so no literal `9222` survives in prose.

**Notes**:
- **AC6 grep** (`9222|cdp-driver|dev:debug|remote-debugging` over the spec) returns **zero matches** (grep exit 1) — the prose rationale rewrite (no literal `9222`) confirmed; fixture port `8080` and `{port}` carry no match.
- **AC7 gates green**: `npm test` **670 pass / 0 fail**, `npm run typecheck` clean, `npm run lint` clean (doc edit only; the in-tree leg-1 source + tests stayed green).
- Mirrored the leg-2 `unified-tab-controls` / F6 `settings-shell` apparatus framing (port-pin + Bearer-key block + `getChromeTarget` probe + coordinate-click / focus-anchor / `chrome-devtools`-disqualified notes) for consistency.
- No `Last Run` line touched (5/5 history preserved); status stays `active`; `## Out of Scope` + `## Variants` unchanged; no other spec's `:9222` references touched; historical `runs/` logs untouched.
- Live confirmation is leg 8 (`verify-integration`). This leg authors only.

### 2026-06-16 — Leg 6 `audit-log-paging` (renderer source) → landed
**Status**: landed (renderer-only source change; batched — not committed per-leg).

**Changes Made** (replace the silent `LOG_DISPLAY_CAP = 50` slice in the Settings activity viewer with 20/page pagination, newest-first, freeze-on-page-2+ per DD4 — renderer-only, no backend/IPC/ring change):
- **New pure module `src/shared/audit-paging.js`** (DD4 state machine), modeled exactly on the `src/shared/url-safety.js` dual-export idiom (UMD tail + browser-global) so it lints clean under the `src/shared/**` node-globals eslint block. `@ts-check`-clean (JSDoc types). Exports `windowPage(activeLog, page, pageSize)` (reverse-then-slice newest-first; `total`/1-based `showingFrom`/`showingTo`/`hasPrev`/`hasNext` from the **passed** log), `countNewer(liveLog, frozenLog)` (count by `ts` strictly > max frozen `ts` — eviction-robust, not a length delta), `activeLog(state)` (`frozenLog ?? liveLog`), and `reduceAudit(state, event)` (the broadcast/next/prev/back-to-live freshness machine; pure, returns same-ref on no-op clamps).
- **New test `test/unit/audit-paging.test.js`** (`node:test` + `node:assert/strict`, `require(...)` the module). 25 cases: `windowPage` boundaries (0/1/19/20/21/41, first/middle/last page, newest-first, a strictly-descending-`ts` fixture, frozen-total fixture); `countNewer` (none/some/eviction-equal-length/empty-frozen); `reduceAudit` (broadcast-page-1-stays-live, next-freezes, broadcast-while-frozen-keeps-rows-updates-live, prev-to-page-1-clears-and-same-tick-broadcast-no-re-freeze, prev-page-3→2-keeps-freeze, next/prev clamp no-ops, back-to-live reset, back-to-live ≡ prev-to-page-1, unknown-event no-op).
- **`src/renderer/pages/settings.js`** activity IIFE rewired: dropped `LOG_DISPLAY_CAP` + the `reverse().slice(0,50)`; module-scope pager `state` driven by `reduceAudit`; init + each `onAutomationActivity` broadcast dispatch `{type:'broadcast',log}` then render `windowPage(activeLogOf(state), page, 20)` via the existing row builder (extracted to `buildLogRow`). Sessions list rendered unchanged (always reflects latest snapshot). Indicator "Showing X–Y of N", prev/next `disabled` state, and the page≥2 "Paused — N newer · " affordance (N=`countNewer`) wired; prev/next/back-to-live click handlers; back-to-live restores focus to the prev button. `pagehide` listener cleanup preserved. Deferred jar-names re-render rewired off the removed `lastSnap` onto `haveSnapshot`/`renderActivity()`.
- **`src/renderer/pages/settings.html`**: `<script src="../shared/audit-paging.js" defer>` added **before** `settings.js` (the url-safety.js loading precedent); **static** pager DOM under `<h4>Recent actions</h4>` — real prev/next `<button type="button">`s with `aria-label`s, an indicator `<span>`, and a `role="status"` paused region (with its back-to-live button), present at parse time so the live region announces reliably.
- **`src/renderer/pages/settings.css`**: `.activity-pager` / `.activity-indicator` / `.activity-paused` styles mirroring the existing `.activity-*` block.
- **`src/renderer/renderer-globals.d.ts`** + **`eslint.config.mjs`**: declared the four injected globals (`windowPage`/`countNewer`/`activeLogOf`/`reduceAudit`) for tsc and added them to the renderer eslint global allowlist — the identical machinery url-safety's globals already use. (`activeLog` is exposed to the renderer as `activeLogOf` to avoid colliding with a generic page global; the CJS export keeps the canonical `activeLog` name for `require`/tests.)

**Notes**:
- **AC6 grep** (`LOG_DISPLAY_CAP|slice(0, 50)` over `settings.js`) returns **zero matches**.
- **AC5 scope**: this leg touches only `src/shared/audit-paging.js` (new) + `src/renderer/pages/*` + `src/renderer/renderer-globals.d.ts` + `eslint.config.mjs` + the new test — **no `src/main/**`**. (The `src/main/automation/*` and behavior-spec diffs present in the working tree are from prior batched legs 1–5, not this leg.) `docs/mcp-automation.md` left unchanged: it documents the broadcast **data contract** (newest-last 500-ring — unchanged here), not the viewer's 50-cap/paging, so updating it was unnecessary and out of AC5's renderer-only scope.
- **AC8 gates green**: `npm test` **692 pass / 0 fail**, `npm run typecheck` clean, `npm run lint` clean.
- **AC9 (a11y) structure asserted here** (real `<button>`s + accessible names + static `role="status"` region present at parse time); **live keyboard/AT behavior is verified in the leg-9 HAT** (focus visibility, screen-reader announcement of the indicator + paused count).

---

### 2026-06-16 — Leg 7 `harden-ungated-path` (config/doc) → DETERMINISTIC PARTS DONE (leg stays `ready`)
**Status**: ready — deterministic parts done; the `--remote-allow-origins` narrowing in `package.json` is **PENDING the operator's live two-arm WS probe** (AC1/AC2/AC3 + the land-vs-divert disposition). Batched — not committed per-leg.

**Deterministic deliverables completed** (no live app required):
- **`.mcp.json` trimmed (AC4)** — removed the stale `playwright` server entry (`npx -y @playwright/mcp@latest --cdp-endpoint http://127.0.0.1:9222`), leaving valid JSON `{"mcpServers": {}}` (file kept, not deleted, per project convention). **Pre-trim verification**: `grep -rn "playwright\|@playwright/mcp\|cdp-endpoint\|:9222" tests/ .mcp.json package.json scripts/` confirmed the **only** Playwright-MCP reference (`playwright`/`@playwright/mcp`/`cdp-endpoint`) was `.mcp.json` itself — all other `:9222` hits are historical run logs, spec apparatus notes (`a11y-audit.mjs`, `cdp-driver.mjs`, `farbling-correctness`), none of which consume Playwright-MCP. `responsive-tab-strip` (the formerly-named Playwright consumer) was rewritten onto the MCP surface in leg 2. **Post-trim**: `grep -rn "9222" .mcp.json` → empty (exit 1); `JSON.parse` confirms valid.
- **`devtools-cdp-conflict.md` annotated (AC5)** — added a Flight-7 annotation at the BLOCKED-AS-WRITTEN block clarifying the block is gated solely on the **missing non-CDP DevTools-open affordance**, explicitly NOT the `:9222` `--remote-allow-origins` Origin (which F7 narrows separately); noted the spec's venue is `dev:automation` (no `:9222`), so the Origin hardening is irrelevant to it. Status unchanged (`draft`), venue unchanged, unblock carried to **F8-eval**. Not unblocked.
- **CLAUDE.md Playwright sentence fixed (AC6, partial)** — the stale `npm test` bullet sentence "The Playwright MCP can also attach via `.mcp.json`." (false once the entry is trimmed) replaced with "The migrated behavior tests now drive the in-process MCP automation surface (`npm run dev:automation`), not Playwright-MCP over `:9222`."

**Gates (AC7)**: `npm test` **692 pass / 0 fail**, `npm run typecheck` clean, `npm run lint` clean (config/doc change — green as expected).

**PENDING (gated on the live probe — NOT touched this pass):**
- `package.json` `dev:debug` `--remote-allow-origins=*` narrowing — gated on the operator's two-arm Node-22 WS probe (arm 1 no-Origin attach; arm 2 disallowed-Origin rejection) + the `a11y`/`farbling` post-narrowing confirmation. Land-vs-divert disposition (AC1/AC2/AC3) decided after the probe.
- `BACKLOG.md` allow-origins item — gated on the probe outcome (land vs F8-eval tracking).
- Any Origin-injecting shim — divert-only, decided after the probe.

---

## Flight Director Notes
_Orchestration decisions recorded here during execution._

### 2026-06-16 — Flight planned + signed off (status `ready`)
Designed via `/flight` (recon → spec → Architect review → operator sign-off). Architect: approve-with-changes, all incorporated (single cycle; no second pass — reviewer-prescribed fixes, no new design risk). **Operator signed off; status → `ready`.** Staged for a future `/agentic-workflow` run (not executed now — operator's call). Sequencing reminder for execution: bulk migration (legs 1–4) BEFORE `harden-ungated-path` (leg 6); the `harden` leg's `--remote-allow-origins` narrowing is a resolve-or-divert (empirical Node-WS Origin probe first).

### 2026-06-16 — Execution started (`/agentic-workflow`, status → `in-flight`)
Flight Director loaded `/agentic-workflow`. Loaded: projects.md (goldfinch path), ARTIFACTS.md (filesystem artifacts), `leg-execution.md` crew (well-formed: Crew + Interaction Protocol + Prompts present; Developer=Sonnet, Reviewer=Sonnet-never-Opus, Accessibility Reviewer disabled). Read mission (active, SC11 part 2), flight spec, flight log. Branch `flight/07-spec-migration-and-hardening` cut from clean `main` (F6 landed, v0.5.1 released). Flight `ready` → `in-flight`. Plan: 7 autonomous legs (design+design-review+implement each, deferring code review/commit to one batched Phase-2d review per the agentic-workflow deferred-review model), then batched review/commit/draft-PR, then the interactive `hat-and-alignment` leg (FD-guided, not autonomous).

---

### 2026-06-16 — Design-review cycles (legs 1 + 2)
- **Leg 2 (`migrate-chrome-specs-a`) design-review cycle 1** (Developer): found [HIGH] the surface can't send `Ctrl+M`/`Ctrl+Shift+P` chords (`toolbar-pins` Step 6), [MED] `responsive-tab-strip` `mouseleave` has no move-only primitive, [LOW] AC4 over-claimed eval-free. The HIGH drove the operator decision to add leg 1 (above). MED/LOW resolved inline (mouseleave via outside-strip click; geometry scoped to visual before/after deltas; `toolbar-pins` Step 9 `npm run a11y` kept as the F8-deferred axe harness, not migrated).
- **Legs 1 + 2 design-review cycle 2** (Developer, coupled review): **leg 2 → approve** (all four revisions confirmed sound; AC6 grep stays clean — `npm run a11y` carries no port literal, `:9222` is internal to `a11y-audit.mjs`). **Leg 1 → approve with changes**: [MED] corrected a factual error (Electron `^42` natively accepts `ctrl`/`cmd`; the canonical `{control,shift,alt,meta}` allow-list is our choice, not an Electron requirement); [LOW] noted the third `pressKey` consumer `mcp-server.js deriveAuditDetail` (args-level, uncoupled — extended to log the chord); [LOW] softened the AC4 test-inversion clause (no single-char-throws test exists in the suite). API pinned (designer Qs): MCP `modifiers` enum advertises only the canonical four (aliases normalized at the `keyEvents` layer); single-char key set is `/^[a-z0-9]$/i` (no punctuation chords in F7). All incorporated; no third cycle (reviewer-prescribed clarifications, no new high-severity risk). Legs 1 + 2 → `ready`.

## Decisions
_Runtime decisions not in the original plan._

### 2026-06-16 — New prerequisite leg: `pressKey` modifier-chord support (source capability)
**Context**: The leg-1 design review (Developer, design-review pass) found that `toolbar-pins` Step 6 + its Shields variant assert that an *unpinned* toolbar item keeps its **keyboard shortcut** (`Ctrl+M` / `Ctrl+Shift+P`) — a **modifier chord**. The MCP surface's `pressKey` cannot send chords: `keyEvents()` (`src/main/automation/input.js:38-48`) accepts only `KEY_MAP` single keys + the `ShiftTab` composite, and the `pressKey` tool schema (`src/main/automation/mcp-tools.js:252-271`, `PRESS_KEY_NAMES`) has no modifier path. This is a **trusted-input gap, NOT an in-page-eval gap**, so the flight's existing divert rule ("eval items → F8-eval") does not cover it — a genuinely new, unplanned discovery.
**Decision**: **Operator (2026-06-16): add modifier-chord support as a prerequisite source leg** (chosen over deferring the checkpoints to F8 or leaving them hybrid on `:9222`). A small, well-scoped trusted-input extension — adjacent to but distinct from the deferred `evaluate` capability (it adds *more of the existing trusted-input mechanism*, not arbitrary in-page JS). New leg `01-pressKey-modifier-chords` is inserted **first**; the planned migration/paging/harden legs shift +1.
**Impact**: F7 leg list grows from 8 → 9. New numbering: 1 `pressKey-modifier-chords` (source), 2 `migrate-chrome-specs-a`, 3 `migrate-chrome-specs-b`, 4 `migrate-settings-automation`, 5 `migrate-core-browsing-shields`, 6 `audit-log-paging`, 7 `harden-ungated-path`, 8 `verify-integration`, 9 `hat-and-alignment`. `toolbar-pins` Step 6 / Shields-variant Step 6 now migrate to `pressKey` **with modifiers** (no longer deferred). Flight remains SC11-part-2-scoped; this is a capability the migration genuinely requires, recorded as an in-flight scope adjustment (not a silent expansion).

---

## Deviations
_Departures from the planned approach._

---

## Anomalies
_Unexpected issues._

---

## Session Notes
_Chronological notes from work sessions._

### 2026-06-16 — Batched intermediate commit (legs 1–6 complete + leg-7 deterministic; review PASSED)
Flight Director: batched implementation of all 7 autonomous legs complete and submitted for the single deferred Phase-2d code review. **Review PASSED — `[HANDOFF:confirmed]`.** Gates green: **692 tests pass**, `typecheck` + `lint` clean.

**Committing now (intermediate):**
- **Legs 1–6 → `completed`**: `presskey-modifier-chords` (source capability — `pressKey(wcId, name, modifiers)`), `migrate-chrome-specs-a`, `migrate-chrome-specs-b`, `migrate-settings-automation`, `migrate-core-browsing-shields`, `audit-log-paging`. All ACs verified, reviewed, checked off in flight.md.
- **Leg 7 (`harden-ungated-path`) → deterministic parts landed** (`.mcp.json` Playwright-`:9222` entry trimmed; `devtools-cdp-conflict` block annotated; docs). **Status stays `ready`** — the `--remote-allow-origins` narrowing is deferred to a live Node-22 WS Origin probe (resolve-or-divert per DD3). Not marked completed.

**Flight stays `in-flight`** — does NOT land on this commit. Remaining = the live pass:
- **Leg 7 narrowing** — live WS probe to choose the `--remote-allow-origins` form (or divert/document the residual).
- **Leg 8 (`verify-integration`)** — live spec runs on the MCP surface + deferred-path (`a11y`/`farbling`) regression check.
- **Leg 9 (`hat-and-alignment`)** — FD-guided interactive HAT.

Branch pushed; **draft PR** opened onto `main` ("Flight 7: spec migration + ungated-path hardening (scoped)"), marked draft pending live verification (legs 7 narrowing / 8 / 9).
