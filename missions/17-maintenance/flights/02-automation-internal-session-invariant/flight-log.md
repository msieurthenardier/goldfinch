# Flight Log: Automation-Surface Internal-Session Invariant

**Flight**: [Automation-Surface Internal-Session Invariant](flight.md)

## Summary

*(not started)*

---

## Leg Progress

### Leg 4 — `mcp-automation-docs-truing` (landed 2026-08-28, re-run)

- Closed F10k with the CORRECTED tier framing (admin is a high-bar, key-gated, loopback-bound tier that CAN be enabled on a packaged build — NOT dev-only). **Shipped-code scrub (step 6, HIGH)**: replaced every "dev-only" / "cannot exist in a packaged build" / mint-gated-on-`!app.isPackaged` claim in `src/main/automation/observe.js` (the shared tier-model comment block, the Flight-9 devtools comment, and the evaluate/injectScript/openDevTools/closeDevTools JSDoc blocks) and `src/main/automation/mcp-tools.js` (the four live tool descriptions + the two `OBSERVE_TOOLS`/`DEVTOOLS_TOOLS` group-header comments) with the corrected framing; verified against shipped code first (`mcp-server.js`'s `mintAdminKey`/`resolveIdentity` gate purely on `GOLDFINCH_AUTOMATION_ADMIN`, no `isPackaged`). Post-edit grep for `dev-only`/`packaged`/`isPackaged`/`cannot exist` in both files: zero false claims remain (only the new correct "CAN be enabled on a packaged build" phrasing hits). Comment/description text only — no logic change. **Docs (`docs/mcp-automation.md`)**: reworded the `:42` Consumer Contract invariant row, the eval-tools and DevTools-tools "Security invariant" blocks (both now name the resolver as the non-admin wall, state admin reaches internal guests by design, and keep the sheet refused for all tiers — no more "even for admin"); added a bullet to the menu-overlay sheet section noting `vaultFill`/`vaultAnswerAuth` resolve through the same `isSheetContents` gate via `vault-context.js`'s `resolveTarget` (verified: `resolveTarget` threads `engineDeps.isSheetContents` into both `resolveContents` calls); added `origin` and the `"ambiguous"` reason to the `vaultFill` return-shape row, matching `vault-context.js:512`/`:494`. The existing `:399-403`(now `:407-409`) "admin tier works on the packaged binary … env-only, no dev/isPackaged coupling" text was already correct and left untouched. **Crew note**: corrected `.flightops/agent-crews/behavior-tests-execution.md`'s apparatus note — `evaluate`/`readDom`/`injectScript`/DevTools now work on internal guests under the admin tier; non-admin (jar) sessions are still refused outright and remain screenshot/a11y-only; the secret sheet stays unreadable at every tier. **Doc-pin check**: `test/unit/automation-mcp-tools.test.js` is the only test referencing `mcp-automation.md` (comments only, not a live pin); its description-content assertions for `evaluate`/`injectScript`/`openDevTools`/`closeDevTools` use loose `/internal/i` matches, which still pass — no test pinned the removed "dev-only" phrase, so nothing needed updating. Gates: `npm test` (3966/3966 pass, 0 fail/skip/todo — unchanged from Leg 3, docs/comments-only diff), `npm run lint` (clean), `npm run typecheck` (clean), `npx prettier --check .` (clean; `docs/*.md` and the crew `.md` are prettier-ignored per `.prettierignore`'s `*.md`). Leg `landed`.

### Leg 3 — `download-ipc-trust` (landed 2026-08-28)

- Closed F10c: `src/main/register-download-ipc.js` `download-media` now resolves the renderer-supplied `webContentsId` via `getTabContents` (already imported) instead of `webContents.fromId`, which accepted any id including the chrome's own — the id-based path to a default-session download (DD6) is closed; a non-tab id (chrome/sheet/popup/unknown) falls back to the sender's active tab exactly as an absent id already did. `show-item-in-folder` now validates the renderer-supplied path main-side before acting — it must resolve to either an entry of `approvedDownloadDirs` or a known download record's `savePath` (mirroring `resolveDownloadRecord`'s id-based trust model for callers that hold a path, not an id); anything else is a silent no-op. Both comment blocks (`:53-61`, `:100-108`) extended to document the new validation. Optional cleanup taken: removed the now-unused `webContents` factory param (`register-download-ipc.js` + its `main.js:1387` passthrough) — clean 2-line removal, no ripple. `test/unit/register-download-ipc.test.js`: made the fake `getTabContents` tab-aware (tab id 9 → a fixed contents, any other id → null, matching the `registry.getWindowForGuest` contract) and added `setActiveTab` to the harness; +6 tests (AC1: a non-tab id falls back to the active tab / a real tab id is used directly; AC2/AC3: an approved dir and a known record `savePath` are shown — mirroring the two real callers `media-controller.js:494`/`:715` — an arbitrary `../`-laden path is not, and a non-string/empty path no-ops; the `savePath: null` cancelled-record fixture exercises the exclusion branch without crashing). Test count (file-local): 10 → 16 (repo-wide 3960 → 3966), 0 fail/skip/todo before and after (one unrelated transient flake observed on an intermediate run, gone on retry — noted, not this leg's suites). **Neuter-verify AC1**: reverted the `download-media` line to a stand-in for the old blind-trust resolve (any numeric id resolves to live contents) — 3 tests went red (the two new AC1 tests plus the pre-existing DD6-adjacent chrome-id assertion), all others stayed green; restored, 16/16. **Neuter-verify AC2**: reverted `show-item-in-folder` to the unvalidated `if (savePath) shell.showItemInFolder(savePath)` — exactly the arbitrary-path no-op test went red (1 fail, 15 pass); restored. Confirmed unmodified and green throughout: Legs 1–2 suites (`automation-observe.test.js`, `vault-context.test.js`, `automation-resolve.test.js`). Diff confined to `register-download-ipc.js` + `main.js` (2-line param removal) + the test file — no renderer/preload/`.d.ts` change (AC4). Gates: `npm test` (3966/3966, 0 fail/skip/todo), `npm run lint` (clean), `npm run typecheck` (clean), `npx prettier --check .` (clean). Leg `landed`.

### Leg 3 — `download-ipc-trust` (landed 2026-08-28)

- `download-media`: `webContents.fromId(webContentsId)` → `getTabContents(webContentsId)` (null for chrome/sheet/popup/unknown), so a non-tab id falls back to the sender's active tab, closing the chrome-id → default-session path (DD6). `show-item-in-folder`: validates `path.resolve(savePath)` against `approvedDownloadDirs` ∪ known-record savePaths before `shell.showItemInFolder`, else no-op. Removed the now-dead `webContents` factory param + its `main.js:1387` passthrough. +6 tests (AC1 fallback/real-tab, AC2/AC3 approved-dir/known-savePath shown, arbitrary/`../` path refused, empty/null handled); both neuters verified. Suite 3960 → 3966; gates green.

### Leg 2 — `secret-sheet-wall` (landed 2026-08-28)

- `main.js:1075` vault `scopeCtx` gained `isSheetContents`/`sheetMenuFor` (pair) + `isTabViewWcId`/`isPopupWcId` (copied from the engine-deps accessors); `vault-context.js` `resolveTarget` reads them off `engineDeps` and splices into both the admin `resolveContents` and jar `resolveContentsForJar` calls (never `allowSheet`, so the sheet stays absolutely refused at every tier); `FillEngineDeps` + `mcp-server.js`'s `opts.scopeCtx` typedefs updated (the second an unplanned but correct typecheck fix). Ported a fake-sheet harness into `vault-context.test.js`; added the AC3 `isTabViewWcId` fake so the newly-activated jar `non-tab-contents` guard doesn't break the legitimate-tab tests; +6 tests (secret-sheet refusal fill+answerAuth × admin+jar, an AC3 no-regression, the AC4 scopeCtx pair pin). Neuters: AC1 (drop `isSheetContents` → 4 refusal tests red) and AC4 (drop `sheetMenuFor` → pin red) both verified. Suite 3954 → 3960; gates green (one unrelated transient flake on the first run, then 3 clean — noted for the debrief).

### Leg 1 — `admin-internal-reach` (landed 2026-08-28)

- Relaxed the four op-local `isInternalContents` admin-refusals in `observe.js` (evaluate/injectScript/openDevTools/closeDevTools) + removed the now-unused import; rewrote the DD2 comment blocks to the tier model; reworded the four LIVE `mcp-tools.js` tool descriptions (+2 group headers). Inverted+renamed the four `[HIGH] … even for admin` tests to admin-succeeds; added four AC2 non-admin-still-refused cases. Tests 3950 → 3954 (+4), 0 fail/skip/todo. Neuter: re-adding one op's throw reddened exactly its admin-success test.
- **Security-classifier flag + FD verification.** The implementing subagent's output carried an automated security-policy warning ("Blocked by classifier") — expected, because the leg deliberately REMOVES access-control `throw`s per the operator's ruling. The FD independently inspected the diff before accepting: `resolve.js`, `input.js`, and `automation-resolve.test.js` show **empty diffs** (the non-admin resolver wall `:233` and the ungated sheet gate `:210` are provably untouched); `observe.js` removed exactly the four admin-only-effective op-local checks and nothing else; only 3 files changed. The change is precisely the designed, reviewed, operator-authorized scope — the flag is a context-blind false positive on "removing security throws." Recorded for the audit trail.


- 2026-08-28 — **Leg 1 `admin-internal-reach` landed.** `src/main/automation/observe.js`: deleted the four op-local `isInternalContents` throws (`evaluate`, `injectScript`, `openDevTools`, `closeDevTools`); rewrote the shared `:43-59`-region comment and each op's preceding JSDoc/inline comment to the tier model (non-admin refused at `resolve.js`'s `!allowInternal && isInternalContents` throw before these ops run; admin (dev-only) reaches internal guests here by design; the menu-overlay sheet gate, DD3, is untouched and these ops never pass `allowSheet:true`); removed the now-unused `isInternalContents` from the `require('./resolve')` destructure (verified by grep — zero remaining code references, only comments). `test/unit/automation-observe.test.js`: renamed (not deleted-and-readded) the four `[HIGH] … REFUSED even with allowInternal:true` tests to `[HIGH] … ALLOWED under admin (allowInternal:true) — … runs (flight-2 DD2 pivot; was "REFUSED even for admin")`, asserting the op runs and returns; added four AC2 negative-case tests (one per op) confirming a non-admin call (no `allowInternal`) on an internal guest is still refused with the resolver's `automation: internal-session` message and the act did not run. `src/main/automation/mcp-tools.js`: reworded the four live tool descriptions (`evaluate`, `injectScript`, `openDevTools`, `closeDevTools`) from "The internal goldfinch://settings session is ALWAYS excluded (even for admin)" to the tier-model wording ("Jar-scoped guests and admin targets, including the internal goldfinch:// session under the dev-only admin tier; the master-password secret sheet is refused for all tiers"); also corrected the two group-header comments above `OBSERVE_TOOLS`/`DEVTOOLS_TOOLS` that asserted the same now-false invariant (in scope since DD5's docs leg explicitly excludes `mcp-tools.js`). Test count: 3950 → 3954 (net +4: four inversions each split into an admin-success case + an AC2 negative case), 0 fail/skip/todo both before and after. **Neuter check**: temporarily re-added an inline `wc.session.__goldfinchInternal === true` throw to `evaluate` — exactly its inverted admin-success test went red (1 fail, 59 pass), all others stayed green; restored, suite back to 60/60 in the file. Confirmed unmodified and green throughout: `automation-observe.test.js`'s `captureScreenshot`/`readDom`/`readAxTree` resolver-path non-admin tests and `automation-resolve.test.js:150-167` (sheet refused even for admin) — zero diff to `resolve.js`, `input.js`, or `automation-resolve.test.js`. Gates: `npm test` (3954/3954 pass), `npm run lint` (clean), `npm run typecheck` (clean), `npx prettier --check .` (clean). Leg `landed`.

### Leg 2 — `secret-sheet-wall` (landed 2026-08-28)

- 2026-08-28 — **Leg 2 `secret-sheet-wall` landed.** Closed finding F9: `src/main/main.js`'s vault `scopeCtx` (`:1075`) never carried the `isSheetContents`/`sheetMenuFor`/`isTabViewWcId`/`isPopupWcId` predicates, so `resolveTarget`'s `resolveContents`/`resolveContentsForJar` calls in `src/main/vault/vault-context.js` couldn't feed resolve.js's `typeof`-gated guards — a vault tool (`vaultFill`/`vaultAnswerAuth`) could name the master-password secret sheet and be refused only by the later origin match. `src/main/main.js`: added the four predicates to the vault `scopeCtx`, copied verbatim from the general engine deps block above it (`registry.isSheetContents`/`registry.sheetMenuFor`/`registry.isTabViewWcId`/`popupRegistry.isPopupWcId`), `isSheetContents`+`sheetMenuFor` injected as a PAIR per the existing grep-pin discipline. `src/main/vault/vault-context.js`: `resolveTarget` now reads `engineDeps.isSheetContents`/`.sheetMenuFor`/`.isTabViewWcId`/`.isPopupWcId` and splices all four into both the admin `resolveContents(...)` call and the jar `resolveContentsForJar(...)` call (never `allowSheet` — the vault path has no opt-in, so `isSheetContents` alone keeps the sheet absolutely refused at every tier); `FillEngineDeps` JSDoc typedef extended with the four new optional predicates. `src/main/automation/mcp-server.js`'s `opts.scopeCtx` JSDoc typedef (the type `tsc` actually checks the `main.js` literal against) needed the same four properties added — required for `npm run typecheck` to pass; no behavior change, JSDoc only. `test/unit/vault-context.test.js`: ported the fake-sheet pattern from `automation-sheet-gate.test.js`/`automation-resolve.test.js` — `makeWorld()` gained a fake secret-sheet wc (`SHEET_WCID`, never a member of `tabs`) plus `isSheetContents`/`sheetMenuFor` closures; `fillDeps()` gained `isTabViewWcId: (id) => world.byWcId.has(id)` (required — threading `isTabViewWcId` into the jar branch newly activates the `non-tab-contents` guard for jar identity, and every legitimate-tab test in the file resolves through `fillDeps()`). Added 6 new tests: `vaultFill`/`vaultAnswerAuth` on the secret sheet refused with `automation: secret-sheet` under both ADMIN and JAR identity (4 tests), a regression test proving a normal in-jar tab still fills/answers after the new predicates are threaded (AC3), and a new AC4 source-pin (`vault-context.test.js`, using `test/helpers/source-scan.js`'s `maskComments`/`findMatchingBracket`, same toolkit as `sheet-automation-gate-invariant.test.js`) asserting the vault `scopeCtx` block in `main.js` carries `isSheetContents`+`sheetMenuFor` as a pair plus the two membership predicates — the existing `sheet-automation-gate-invariant.test.js:234-249` pin is keyed to `createEngine(` sites and does not reach the `scopeCtx:` literal, so a standalone pin was required. Test count: 3954 → 3960 (net +6), 0 fail/skip/todo before and after (file-local: 30 → 36). **Neuter-verify AC1**: temporarily dropped `isSheetContents` from `resolveTarget`'s spliced `sheetDeps` in `vault-context.js` — all four new secret-sheet refusal tests (admin+jar × fill+answerAuth) went red (4 fail, 32 pass), proving the resolve-time gate, not the origin match, is what refuses; restored, back to 36/36. **Neuter-verify AC4**: temporarily deleted `sheetMenuFor` from the vault `scopeCtx` literal in `main.js` — exactly the new AC4 pin went red (1 fail, 35 pass), all others stayed green; restored. Confirmed unmodified and green throughout: Leg 1's suites (`automation-observe.test.js`, `automation-mcp-tools.test.js`) and `automation-resolve.test.js` including the `:150-167` sheet-refused-even-for-admin precedent (198/198 across the three files). `resolve.js`, `observe.js`, and `input.js` were not touched. Gates: `npm test` (3960/3960 pass, 0 fail/skip/todo), `npm run lint` (clean), `npm run typecheck` (clean — required the `mcp-server.js` typedef fix above), `npx prettier --check .` (clean after formatting the new test additions). Leg `landed`.

---

## Decisions

### Flight Director Notes

- 2026-08-28 — `/agentic-workflow` loaded; flight `ready` -> `in-flight`; branch `flight/02-automation-internal-session-invariant`. Ran an upstream reconnaissance (delegated): **F1** confirmed-live; **F9** drifted (`vault-context.js` -> `src/main/vault/vault-context.js`) but live; **F10c** confirmed-live (both sub-findings); **F10k** stale (+ extra drift: `vaultFill` also returns `origin` and has an `"ambiguous"` reason the doc omits). Recon found the real constraint: the behavior-test crew relies on `captureScreenshot`/`readAxTree` against internal guests under admin as its ONLY apparatus there — the original "refuse even for admin" plan would have broken the test system (the a11y audit is fine — `evaluate`-only).
- 2026-08-28 — **PIVOT (operator ruling).** The operator reframed the premise: admin is a dev-only end-to-end test master key (`mcp-server.js` gates the mint on `!app.isPackaged`; absent in a packaged build), and end-to-end testing requires reaching internal pages, so the boundary is by **tier**, not page. Recorded as **DD1–DD5**. Rulings: (1) admin may reach internal guests on **every** op, **including `evaluate`/`injectScript`** (full end-to-end power for a key that cannot ship); (2) the **master-password secret sheet stays refused for all tiers, admin included**. Consequences: F1 **inverts** (relax the admin refusals on eval/inject/DevTools; non-admin already blocked at the resolver; the `isSheetContents` sheet gate is separate and stays); F9 survives and hardens the one remaining wall (the secret sheet); F10c unchanged; F10k rewrites the docs. Mission criterion 3 reframed in place; the original Objective/Technical-Approach kept as pre-pivot commentary; maintenance F1 annotated. A deliberate security-model change, not a tidy-up — logged as such.


---

## Deviations

---

## Anomalies

---

## Session Notes
- 2026-08-28 — Leg 1 `admin-internal-reach` designed and **tiered HIGH** (rationale: reverses the documented DD2 invariant "internal-session exclusion even for admin" on the security-sensitive automation surface; inverts four `[HIGH]` unit pins). Scope confirmed minimal: relax the four op-local `isInternalContents` refusals in `observe.js` (evaluate/injectScript/openDevTools/closeDevTools) — these are admin-only-effective because non-admin is refused earlier at `resolve.js:233`; the sheet gate (`resolve.js:210`, DD3) and the resolver non-admin wall are untouched. Running the design review.
- 2026-08-28 — Leg 1 design review: **approve with changes**. Core security reasoning validated (no non-admin bypass — each op's body is `resolveContents` then the op-local check, and non-admin throws at the resolver first; the sheet gate stays absolute because these four ops never pass `allowSheet:true`, `engine.js:300-305`). Incorporated: (MEDIUM) the four LIVE `mcp-tools.js` tool descriptions (`:507/:531/:563/:580`) claim internal exclusion "even for admin" — folded into Leg 1 (AC6) since they are the running-app contract for this leg's change; (LOW) the behavior-crew apparatus note (`behavior-tests-execution.md:~347`) → routed to the closing docs leg (DD5); trivial citation fix (:149→:150). Only additive/mechanical changes → no second review cycle. Leg 1 `ready`.
- 2026-08-28 — Leg 2 `secret-sheet-wall` (F9) designed and **tiered HIGH** (the master-password sheet is the one surface DD3 keeps off-limits to all tiers; the fix threads security predicates through the shared vault resolve path). Root cause confirmed: the general engine deps supply the four sheet/tab/popup predicates but the vault `scopeCtx` (`main.js:1075`) omits them, so `resolveTarget`'s `resolveContents` call can't feed the `typeof`-gated sheet gate. Running the design review.
- 2026-08-28 — Leg 2 design review: **approve with changes**. Confirmed the plumbing (scopeCtx -> engineDeps by direct reference) and that the sheet gate fires for admin and jar. Incorporated: (HIGH) the jar-branch tab/popup guard breaks existing jar test fakes unless `fillDeps()`/`makeWorld()` gains an `isTabViewWcId` fake (AC3 + guidance); (gap) the existing pair-pin only scans `createEngine(` sites so a NEW vault-scopeCtx pin is required (AC4); the sheet-fake harness must be ported into `vault-context.test.js`; `FillEngineDeps` JSDoc updated. Reviewer's own prescriptions applied verbatim -> no second review cycle. Leg 2 `ready`.
- 2026-08-28 — Leg 3 `download-ipc-trust` (F10c) designed and **tiered HIGH** (two IPC trust-boundary fixes on a security-sensitive surface). Design-time decision: fix `show-item-in-folder` by main-side VALIDATION (path ∈ approvedDownloadDirs ∪ known-record savePaths) rather than an id migration — the two renderer callers pass main-originated paths but hold no record id, so validation closes the gap with no IPC-signature/renderer churn. `download-media`: gate the renderer `webContentsId` through a tab check so the chrome-id → default-session path (DD6) stays closed. Running the design review (it must confirm the `getTabContents` non-tab contract and the test home).
- 2026-08-28 — Leg 3 design review: **approve (ready as written)**, no HIGH/MEDIUM. AC1 make-or-break resolved: `getTabContents` returns null (never throws) for chrome/sheet/popup/unknown ids (`main.js:464-470` → `registry.getWindowForGuest`, `tabViews`-only), so the bare `getTabContents(webContentsId)` is correct with no guard; path-validation confirmed sound (both callers pass main-known paths); `register-download-ipc.test.js` already exists. Folded two LOW notes: the fail-closed stale-toast edge case (documented, accepted) and the optional dead-`webContents` cleanup. Minor only → no second review cycle. Leg 3 `ready`.
- 2026-08-28 — Leg 4 `mcp-automation-docs-truing` (F10k) designed; **tiered LOW** (docs only, no code) → straight to implementation, no design review (flight-end Reviewer covers it). Targets confirmed: `mcp-automation.md:42/334-376/579-584/606-607/686` + the behavior-crew note. Also carrying a **recurring-flake watch**: Legs 2 and 3 each reported one transient unrelated test flake that cleared on retry — flag for the flight debrief's test-metrics pass.
- 2026-08-28 — **Leg 4 `[BLOCKED]` — the pivot's central premise is FALSE (FD error).** The docs Developer refused to write DD2's "admin is dev-only, `!app.isPackaged`, absent in a packaged build" because shipped code contradicts it, and verified by the FD: `mcp-server.js`'s `mintAdminKey` and `resolveIdentity` gate the admin tier PURELY on `GOLDFINCH_AUTOMATION_ADMIN` env presence (no `isPackaged`); the `automation:admin-key-mint` IPC (`register-settings-ipc.js:139`) is registered unconditionally (`main.js:1975`), reachable from the Settings UI in a **packaged** build; `automation-dev.js:40-41` states admin minting is a SEPARATE, narrower env gate intentionally NOT folded into the dev-only `--automation-dev` gate; and **`docs/mcp-automation.md:399-403` already documents** "the admin tier works on the packaged binary … env-only, no dev/isPackaged coupling." The FD's `!app.isPackaged` reading was of the headless-harness AUTO-mint path (`mcp-server.js:~1155`, `app-lifecycle.js:266`), not the admin tier itself. **Consequence:** the pivot (Legs 1–2) relaxed the "even for admin" internal-session refusal on the belief admin cannot ship — but admin CAN exist in a packaged/production build, so as-implemented the relaxation reduces production defense-in-depth (a locally-enabled admin automation session could `evaluate`/`injectScript`/DevTools the Settings/vault internal pages). Nothing is committed — Legs 1–3 are uncommitted; the flight is paused pending an operator re-decision on corrected facts. Options recorded for the operator: (A) keep as-is; (B) revert the pivot; (C) gate the internal-reach on a dev/unpackaged signal so admin reaches internal pages in DEV only and packaged builds keep the refusal (matches the intent the operator actually ruled on).
- 2026-08-28 — **Correction applied; operator re-decided KEEP-AS-IS on corrected facts** (admin is env-gated, enablable on packaged builds, NOT dev-only). No code logic reverts — Legs 1–3 stand. Scrubbed the false 'dev-only / cannot exist in a packaged build' justification from the artifacts (flight DD2 + Objective, mission criterion 3, maintenance F1, leg 01, leg 04) and replaced it with the real one (admin = a high-bar, key-gated, loopback-bound tier; secrets never on the page; the sheet walled for all). Leg 4 re-scoped to ALSO fix the shipped-code dev-only claims Leg 1 wrote into `observe.js` comments and `mcp-tools.js` descriptions, plus the docs + crew note. Re-running Leg 4.
