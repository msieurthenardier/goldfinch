# Flight Log: The Welcome Surface

**Flight**: [The Welcome Surface](flight.md)

## Summary

In flight as of 2026-08-24. Two autonomous legs planned; deferred-commit model (single review + commit after the last leg).

---

## Reconnaissance Report

Source items verified against `main` at `ab349ea` (Flight 1 merged), 2026-08-24. No retirements proposed; the operator accepted the classifications during flight planning.

| Item | Classification | Evidence | Recommendation |
|---|---|---|---|
| Squawk 0005 — `homePageCache` never boot-seeded | confirmed-live | `src/renderer/chrome/window-controller.js:127` seeds only `searchEngine`; `src/renderer/renderer.js:492` `setHomePage` still coalesces `\|\| HOMEPAGE` | Claimed by DD4 |
| Three `\|\| HOMEPAGE` coalescing sites | drifted → confirmed-live | now `renderer.js:49`, `:492`, `:1549` (mission cited 48/478/1534) | Claimed by DD4 |
| `toUrl`'s `\|\| 'google'` site | confirmed-live | `src/renderer/chrome/navigation-controller.js:105` | Claimed by DD3/DD4 |
| Mission KI — `openSiteSettingsTab()` reuses any internal tab | confirmed-live | `src/renderer/chrome/overlay-menus.js:118` `.find(isInternalTab)`; zero unit coverage | Claimed by DD10 |
| Mission KI — `settings.js` truthy home-page guard | confirmed-live, two sites | `src/renderer/pages/settings.js:133` (load) and `:152` (broadcast) | Claimed by DD6 |
| Squawk 0006 — `search-engine-upgrade` step 2 premise | confirmed-live | fails only because of 0005; true as authored once DD4 lands | Claimed by DD5 — re-run closes it, no re-authoring |
| Squawk 0003 — broadcast-invariant detection half by substring | confirmed-live, latent | `test/unit/broadcast-invariant.test.js:60-61`; no helper wraps `settings.set` today | Out of scope — this flight adds no settings key and its one new write handler uses the detected direct shape (DD1); turnaround squawk |
| Squawk 0007 — `search-engine-preference` step 6 apparatus | confirmed-live | spec/doc only | Out of scope; recommended turnaround before this flight's acceptance gate |
| Squawk 0008 — crew-file apparatus facts | confirmed-live | doc only | Out of scope; recommended turnaround before this flight's acceptance gate |
| Census find — `INTERNAL_JAR_NAMES` has no `vault` entry | confirmed-live, new | `src/renderer/chrome/tab-controller.js:46` falls back to `'Settings'` for a Vault tab | Not this flight's surface — logged as squawk 0009 |
| Mission Flight-2 framing — "a new internal page is a three-allowlist change" and the list of specs to claim | superseded by DD1 | no internal origin is added | Preserved in the mission as commentary; specs recorded as not engaged in DD11 |

---

## Leg Progress

### Leg 1 `welcome-surface-and-home-routing` — implementation complete, 2026-08-24

**Status**: leg left `in-flight` per the deferred-commit model — behavior specs are FD-owned; the FD lands the leg (sets `landed`) after both runs below pass.

**Gates (before → after, wall-clock)**:
- `npm test`: **3716 pass / 0 fail → 3744 pass / 0 fail** (both runs ~3.3s wall-clock; `real 0m3.3s` per `time npm test`). +28 tests net (+26 from this leg's new/extended coverage across `tab-controller`, `navigation-controller`, `window-controller`, `overlay-menus`, `tab-context-model`, `session-snapshot`, `closed-tab-capture`, `register-settings-ipc`, `seam-contract`; +2 from the new `test/unit/homepage-literal-scan.test.js`).
- `npm run typecheck`: clean before and after (0 errors); ~0.4s wall-clock both runs. One transient error surfaced mid-implementation (`openWelcomeTab`'s destructured-object param needed an explicit JSDoc `@param` — TS couldn't infer the `reasons` field's type from defaults alone) and was fixed before this report.
- `npm run lint`: clean before and after; ~3.1–3.2s wall-clock both runs.

**Hand-performed broadcast-invariant neuter check** (Hard Rule / AC "chrome-welcome-set"): temporarily deleted the `broadcastSettings();` line from the new `chrome-welcome-set` handler in `src/main/register-settings-ipc.js`, ran `node --test test/unit/broadcast-invariant.test.js` — the static-scan test went **RED**: `handler(s) mutate settings without broadcasting settings-changed: register-settings-ipc.js:chrome-welcome-set` (26 pass / 1 fail). Restored the line (`diff` against the pre-edit backup confirmed byte-identical), re-ran — **27/27 pass**. The net correctly classifies the new handler.

**Fresh-profile fixture commands for the FD** (both specs; run from the goldfinch worktree root):

*`welcome-home-routing`* — a fresh scratch profile, no pre-Flight-1 checkout needed:
```
export XDG_CONFIG_HOME="$(mktemp -d)"
GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 npm run dev:automation
```
Then, as the spec's own fixture-setup step (before step 1): set the home page to `https://example.com` and the search engine to DuckDuckGo via Settings, and create a bookmark to `https://example.org` — see the spec's Preconditions for the exact sequence. Attach an MCP client with the printed admin key and run all 10 steps (step 10 is the FD-driven quit/relaunch).

*`search-engine-upgrade`* (closes squawk 0006 — re-run only, no re-authoring) — needs an AUTHENTIC pre-Flight-1 profile, produced from a separate worktree:
```
git worktree add ../goldfinch-preflight1 c8563f3   # commit immediately before Flight 1 merged
cd ../goldfinch-preflight1 && npm ci
export XDG_CONFIG_HOME="$(mktemp -d)"   # keep this path — the SAME profile is reused below
GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation
```
Set the home page to `https://fixture.example.net` in Settings, quit. Verify the fixture (the settings document row exists, `version: 2`, custom home page) before proceeding. Then, back in this flight's own worktree, relaunch against the SAME `XDG_CONFIG_HOME`:
```
cd <goldfinch-worktree-root>
GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation
```
and run the spec's steps. Expected (per DD5): step 2's premise is now true as authored — the boot window's first Ctrl+T honors the fixture's home page — so the spec should pass 5/5 (its last run partial-failed step 2 only on the not-yet-fixed squawk 0005).

**Deviations from the leg spec**:
1. **`openWelcomeTab` does NOT call `if (dnd) cancelDnd()`**, unlike `createTab`/`closeTab` (design review's stated model — Implementation Guidance #2 says only "mirrors `createTab`'s untrusted branch for jar resolution and strip insertion"). Adding it would have made `test/unit/tab-drag-invariants.test.js`'s pinned "EXACTLY FIVE `cancelDnd()` call sites" (AC7) go to six. Every `openWelcomeTab` call site (Ctrl+T, the `+` pill, a container-menu item, the boot path) runs from outside the tab strip's native-DnD pointer capture, so it cannot race a live drag session in practice — documented in a comment at the call site. Flagging this explicitly since it's a real (if narrow) divergence from "mirrors `createTab`'s branch," not an oversight.
2. **`attachView`'s pending-URL race guard** (Edge Cases: "attachView racing a second navigate") was implemented as specified (`tab.attaching` flag, latest-URL-wins queue, applied via `tabNavigate` once the first attach's `wcId` arrives) even though it isn't a numbered Acceptance Criterion — unit-pinned in `tab-controller.test.js`.
3. **The welcome panel's `onSettingsChanged` auto-attach** (Edge Cases: "`homePage` set to `null` while a welcome tab is active in another window... setting it non-null hides the home block... which then attaches to the new home page") was implemented in `welcome-controller.js` — not unit-pinned (no test harness seam for it beyond wiring the subscription), verifiable live in `welcome-home-routing` step 9.
4. **`register-settings-ipc.test.js`, `shortcut-controller.test.js`, `seam-contract.test.js`** required updates for the new handler/dep/seam-entry respectively — not called out individually in the leg's Outputs but necessary for the gates to stay green (structural fallout, not scope growth).

**Out-of-scope defects found — none.** No new defects surfaced during implementation; squawks 0005, 0006, and the `openSiteSettingsTab` Known Issue are all claimed and closed by this leg's own work (0006 pending the FD's `search-engine-upgrade` re-run above).

**For leg 2 — ground truth** (exported surfaces leg 2 builds on):
- `tab-controller.js` exports (unchanged shape expectation): `openWelcomeTab({ container?, reasons: Iterable<string>, pendingQuery? }): Tab` — builds the viewless record, self-activates, returns it; `attachView(tab, url): void` — validates `isSafeTabUrl`, no-ops on a rejected URL, races-safe (queues the latest URL if a second call arrives while one `tabCreate` is in flight), clears `tab.welcome` and hides the panel synchronously, then runs the shared `onViewCreated(tab, wcId)` continuation once `tabCreate` resolves; `openNewTab(container? = null): Tab` — `currentHomePage() == null ? openWelcomeTab(...) : createTab(...)`.
- `tab.welcome` record shape: `{ reasons: Set<string>, pendingQuery: string | null }` (set by `openWelcomeTab`, cleared to `null` by `attachView`). This leg only ever populates `reasons` with `'home'` and leaves `pendingQuery` `null` — leg 2 is expected to add `'search'` to the reasons set and a real `pendingQuery` string from the `toUrl`-returns-null path and the `sel:search` context-menu path (per flight DD3).
- The engine-block slot: `welcome-controller.js` builds an EMPTY `<div id="welcome-engine-block">` inside `#welcome-surface`, appended after the burner note, and renders nothing into it this leg. `show(tab)` currently only toggles `#welcome-home-block`'s `.hidden` class off `tab.welcome.reasons.has('home')` — leg 2 will need an analogous toggle/render call for the engine block off `reasons.has('search')`, plus the pending-query prompt text `Where should we search for "<query>"?` DD7 describes. No engine data is imported or rendered by this leg (`src/shared/search-engines.js` is untouched by `welcome-controller.js`).
- The chrome-bridge write channel (`welcomeSetPreference` → `chrome-welcome-set`) already accepts `searchEngine` as a key (the main-side handler was written to spec for both keys from the start, even though only `homePage` is exercised this leg) — leg 2's engine block can call `welcomeSetPreference({ key: 'searchEngine', value })` with no main-side change needed.
- `applyToolbarAffordances(tab)` and the `hasView` gate on `tabContextModel` both key off `tab.wcId == null` / are unaffected by `tab.welcome`'s contents — leg 2 does not need to touch either.

### Leg 2 `search-handoff-and-fresh-defaults` — implementation complete, 2026-08-25

**Status**: leg left `in-flight` per the deferred-commit model — behavior specs are FD-owned; the FD lands the leg (sets `landed`) after all four runs below pass.

**Gates (before → after, wall-clock)**:
- `npm test`: **3744 pass / 0 fail → 3761 pass / 0 fail** (`real 0m3.3s`–`0m3.5s` per `time npm test`, several runs). +17 net (+4 `navigation-controller.test.js`, +4 `tab-controller.test.js`, +9 `search-engines.test.js` — 7 pending-query-cap tests plus 2 structural checks: no engine label/description duplicated in `welcome-controller.js`, and `settings.html` carries `#search-engine-clear`). `settings-store.test.js` and `homepage-literal-scan.test.js` net zero new tests (renames/extensions only, counted below).
- `npm run typecheck`: clean before and after (0 errors); ~0.35–0.4s wall-clock every run.
- `npm run lint`: clean before and after; ~2.9–3.3s wall-clock every run.
- **Line-budget note**: `renderer.js` grew to sit exactly at `RENDERER_LINE_BUDGET` (1650, `seam-contract.test.js`) after this leg's additions (boot-barrier `searchEngine` await, the `sel:search` null-engine handoff, cache/dep wiring). Trimmed comment verbosity to land exactly on budget rather than raise it — no FD ruling requested or needed, but flagging that the budget is now fully consumed; the next flight/leg that grows `renderer.js` further will need either more trimming or a budget-raise ruling.
- **One transient single-test flake observed, not reproduced**: one `npm test` run out of ~6 total this session reported 3760/1 fail with no captured failing-test name (the terminal's `grep` for `not ok` on an immediate re-run found nothing, and three further consecutive runs were clean 3761/0). Treated as environmental noise, not a real regression — no code change correlates, and it never recurred. Flagging for the FD's awareness rather than silently discounting it.

**Renamed tests** (Hard Rule — inverted-meaning tests renamed, not deleted):
- `navigation-controller.test.js`: `'toUrl builds the search URL from the current search engine when one is chosen; returns null when none is'` — RENAMED from `'toUrl builds the search URL from the current search engine, non-default and default'` (the null-engine case now asserts `null`, not a Google-coalesced URL). The sibling `'URL conversion and navigation preserve internal-tab refusal and web-tab capture'` test was NOT renamed (name/meaning unchanged) — only narrowed, its now-relocated Google-fallback assertion noted inline.
- `settings-store.test.js`: design review's pre-counted 32/9 split, verified by hand against the leg's line list (87, 88, 89, 91, 97, 98, 203, 229, 1190, 1191, 1323, 1341, 1376, 1419, 1420, 1425, 1426, 1447, 1511, 1549, 1558, 1602, 1611, 1651, 1655, 1771, 1775, 1826, 1827, 1831, 1832, 1855 — all 32 confirmed present and renamed, none of the 9 keep-fixtures at 611, 633, 1022, 1032, 1156, 1165, 1300, 1316, 1373 touched). 3 of the 32 have a changed TEST TITLE (the old title named the removed-fallback value directly): `'defaults on first load — no settings.json present, pinned to disk at v3 as null/null (DD5)'`, `'searchEngine — default on first load is null (unset)'`, `'migration: no row, no legacy file → defaults, pinned to disk as null/null (DD5 row-less pin-on-load)'`. The other 29 keep their existing (value-agnostic) titles and carry an inline `RENAMED (M16 F2 L2 / DD5 — was 'google'` / `'https://www.google.com')` comment at the changed assertion.
- `homepage-literal-scan.test.js`: extended (not renamed) — `welcome-controller.js` added to the scanned-files list; the regex widened from `HOMEPAGE` / `google.com` to also catch the bare quoted `'google'` engine-id literal (leg 2 deletes the app's last such literal, `navigation-controller.js`'s `|| 'google'` and `renderer.js`'s `searchEngineCache = 'google'` seed).

**DD8 red-when-neutered re-verification (hand-performed)**: temporarily replaced `VALIDATORS.searchEngine` in `src/main/settings-store.js` with `(v) => v === null || typeof v === 'string'`, ran `node --test test/unit/settings-store.test.js` — **RED, 77 pass / 6 fail** (`kagi`/URL-shaped/empty-string `set()` rejections, the hostile-stored-value repair test, the removed-id repair test, and the legacy-hostile-searchEngine pin-on-load test all failed, as expected — a permissive validator lets every hostile value through). Restored the exact original line via the Edit tool, `diff` against a pre-edit backup confirmed byte-identical, re-ran — **83/83 pass**. The set still catches a neutered validator after the DEFAULTS flip.

**Pending-query cap check**: `PENDING_QUERY_MAX = 2048` in `src/shared/search-engines.js`; `capPendingQuery` applied at both capture sites — `navigation-controller.js`'s `handoffSearch` (address-bar / omnibox-Enter path) and `renderer.js`'s `sel:search` dispatch (page-context "Search for …" path) — each grep-pinned with an inline `PENDING_QUERY_MAX` comment so `grep -n "PENDING_QUERY_MAX"` across the three files finds the constant and both capture sites per the leg's Verification Steps. `buildSearchUrl` truncates a second time (belt-and-suspenders) before `encodeURIComponent`. Positive control: an over-length input truncates to EXACTLY 2048 chars (`search-engines.test.js`); a `<script>`-shaped query round-trips as inert text/encoded-data at both layers (`capPendingQuery` does no sanitization — it is only ever consumed via `textContent` or `encodeURIComponent`, never `innerHTML`).

**Grep-AC verification (Verification Steps, all run)**:
- `grep -rn "'google'\|google.com" src/renderer/ src/main/settings-store.js` → **no hits**.
- `grep -c "createTab(toUrl(" src/renderer/renderer.js` → **0**.
- `grep -n "PENDING_QUERY_MAX" src/shared/search-engines.js src/renderer/chrome/navigation-controller.js src/renderer/renderer.js` → constant + both capture sites, all three files hit.
- `grep -n "homePage: null\|searchEngine: null" src/main/settings-store.js` → both present in `DEFAULTS`.

**Fixture notes for the FD** (all four specs, run from the goldfinch worktree root):
- `welcome-first-launch` — **NO fixture setup** (a genuinely fresh scratch profile IS the test; first launch on an empty `XDG_CONFIG_HOME` must show both welcome blocks).
- `welcome-search-handoff` — fixture sets **only the home page** (via Settings) before step 1, leaving the engine unset so the search-handoff path is what's under test.
- `new-tab-default-routing` (re-run) — new Preconditions bullet: set the home page to `https://example.com` via Settings before step 1 (a fresh profile now boots to a viewless welcome tab `enumerateTabs` can't see; the boot tab attaches to the home page on the broadcast).
- `search-engine-preference` (re-run) — new Preconditions bullet: set the search engine to Google via Settings before step 1 (a fresh profile now has no engine; the fixture restores the spec's "Google selected explicitly" premise so its own settable/propagation intent stays exercised).
- `search-engine-upgrade` is NOT re-run by this leg (already closed by leg 1 / squawk 0006) — only its step 3 wording was loosened per the Validator recommendation; no re-run needed for that edit alone, though the FD may fold it into a future pass if convenient.

**Deviations from the leg spec**:
1. **`welcomeReasons(home, engine)` keeps the unused `home` parameter** rather than dropping it to a single-arg `welcomeReasons(engine)` — the leg's own Outputs signature is `welcomeReasons(home, engine)`, and every call site (`openNewTab`, the boot path) only ever invokes it once `home` is already known `null` (their own branch), so `home` carries no logic today. Kept per the leg's stated signature for boot-path/openNewTab call-site symmetry and documentation value; ESLint's default `args: 'after-used'` mode does not flag it. Flagging since it's a real (if inert) unused-parameter divergence from strict minimalism, not an oversight.
2. **`homepage-literal-scan.test.js`'s regex widened to `/['"]google['"]/`** (quoted literal, either quote style) rather than the leg's literal phrasing of "no `'google'` literal" — chosen so the scan also catches a double-quoted reintroduction, not just single-quoted; verified against a negative control (the word "Google" alone, unquoted, in prose would NOT trip it — none remains in the four scanned files after this leg's comment rewrites, checked by hand).
3. **Two structural tests not explicitly named as new test-file additions in the leg's Outputs line** (`'no engine label/description is duplicated in welcome-controller.js'` and `'settings.html carries the search-engine Clear affordance'`) were added to `search-engines.test.js` rather than `settings-page-shared-scripts.test.js` — the leg's Outputs bullet calls for "a structural test that welcome-controller.js contains no engine label/description string... and that settings.html has `#search-engine-clear`" without naming a file; `search-engines.test.js` was chosen as the natural home (it already imports `SEARCH_ENGINES` and runs the same file-content-scan idiom the settings-page sibling test uses).
4. **CLAUDE.md's Settings-store section gained a new bullet** (`DEFAULTS.homePage`/`searchEngine` are `null`, no v4) rather than folding the DD5 explanation into the existing v3-bump bullet — kept separate so the "what changed at v3" and "what changed at v3-without-a-bump, later" stories don't blur into one paragraph.

**Out-of-scope defects found — none.** No new defects surfaced during implementation. Squawk 0010 (Settings home-page status-line staleness, logged by leg 1) is known and deferred — left untouched, not folded into this leg's Settings-page edits (the engine Clear/hint code added here is new, not a fix to the pre-existing home-page staleness bug).

---

## Decisions

### Flight design review (planning, 2026-08-24)
**Context**: `/flight` Phase 5b — Architect review of the spec against `main` at `ab349ea`.
**Cycle 1**: approve with changes. DD1 (chrome-owned viewless welcome tab, not an internal page) verified to the line — writable address bar via the empty-url branch, bookmarks bar per jar, `navigate()` never takes the internal early-return, bookmark clicks already funnel through `navigate()`, snapshot/closed-tab exclusion is architectural absence, the chrome-bridge write shape is caught by the broadcast-invariant net, no internal-page invariant touched. Findings incorporated: **[high]** DD8 reversed — every cross-window move path is keyed on a live `wcId`, so a viewless tab is refused today; the flight adopts the refusal and omits the dead move items from the tab-context menu instead of building viewless transfer. **[medium]** `welcome-home-routing` step 5 re-authored to the `openNewTab(makeBurner())` chrome seam (the container menu is a non-automatable sheet). **[medium]** DD3 gains the single `openWelcomeTab` constructor so `sel:search` never special-cases `createTab(null)`. **[medium]** DD3 gains the mission's length cap (`PENDING_QUERY_MAX = 2048`, positive-control test). **[low]** DD7 gains viewless-tab affordance hygiene (nav buttons, toolbar buttons, tab-context move items) and the guest-slot layout note; DD5 notes the test-rename volume; leg design must re-check two test files the review could not reach.
**Cycle 2** (targeted pass on the revised DDs): approve with changes. Confirmed sound: DD8's guards refuse viewless cross-window transfer independently at four layers; the tab-context-model gate belongs at the model as a caller-computed param (mirrors `isInternal`); DD3's constructor placement and `sel:search` rewiring fit the factory-deps pattern; `updateNavButtons` self-corrects on wcId arrival; `makeBurner` is in the closed set. Incorporated: **[high]** the toolbar-button hygiene is extracted into `applyToolbarAffordances(tab)` and re-run in the wcId-arrival continuation (gating it in `activateTab` alone would have left ordinary tabs' Media/Shields/DevTools buttons disabled after their view arrived), with a positive-control test; **[medium]** leg 1 explicitly publishes `openNewTab` in the `evaluate` closed set with an FD-ruling comment; **[low]** DD8 notes the `dragstart` guard also refuses same-window mouse reorder; DD3 notes the cap is pre-encode; DD5 says count the renames. Two cycles reached — design review closed; the three low notes are recorded, not re-reviewed.

*(runtime decisions recorded here during execution)*

---

### Flight Director Notes

**2026-08-24 — Flight open.** Crew file `leg-execution.md` validated (Crew/Interaction Protocol/Prompts present). Flight status `ready` → `in-flight`. Branch `flight/02-welcome-surface` rebased onto `main` at `31a74b4` (squawk turnaround #166 merged — the behavior-test crew file now carries the Project Apparatus Notes this flight's runs rely on). 2 autonomous legs, 0 completed. Squawks 0005 and 0006 claimed by this flight; no other open squawks.

**2026-08-24 — Leg 1 `welcome-surface-and-home-routing` designed.** Risk tier: **HIGH** — a new tab lifecycle state (a strip record with no view, `wcId == null` past the boot round-trip) that several layers could foreclose (`tab-set-active`, snapshot/closed-tab capture, move paths, context menu); cache/freshness change (`homePageCache` boot seed + raw setter, `HOMEPAGE` deleted); a shared-signature change (`createTab` loses its default argument — six call sites and the boot path move to `openNewTab`); a new chrome→main settings write channel (security-adjacent; must be seen by the broadcast-invariant net). Design review required per tier. Cache contract declared in the leg. Citations audited against `2e79d5c`. Behavior-spec execution is FD-owned; the Developer leaves the leg `in-flight`.

**2026-08-24 — Leg 1 design review: 1 cycle, approve with changes (all incorporated; cycle 2 skipped — every fix adopts the reviewer's own recommendation verbatim, no design shifted).** Findings: **[high]** the DD10 `openSiteSettingsTab` predicate as drafted (fragment-free regex) could never match a real Settings tab — every creation/navigation path sets `tab.url` to `goldfinch://settings/#privacy` — so it would have created a duplicate on every call, and the drafted three unit cases would have masked it with a fragment-less fixture; replaced with a host-only match and a fourth "call twice → no second tab" case with a fragment-bearing fixture. **[medium]** panel placement simplified to a CSS child of `#webviews` (already `position: relative` with inset children) — no rect measurement or resize wiring; z-order confirmed safe (guests composite above chrome; a welcome tab has no guest). **[low]** drop-to-navigate noted as structurally unreachable for a viewless tab; `closeTab` skip range corrected to `:661-664`. Confirmed: the `onViewCreated` extraction has no `createTab`-local dependencies; jar resolution deps are already injected; the broadcast-invariant net scans `register-settings-ipc.js` and will classify the new handler; `hasView` defaults keep the audit call site untouched; no existing test pins `createTab()` with no args. Leg status → `ready`. `[HANDOFF:review-needed]`.

**2026-08-25 — Leg 1 behavior acceptance, spec 1: `welcome-home-routing` — PASS, 10/10.** Run log: `tests/behavior/welcome-home-routing/runs/2026-08-25-02-45-35.md`. Live two-agent mode; fresh scratch profile; fixture (home page, engine, bookmark) folded into checkpoint 1 as a setup row. Every claim held on rendered evidence: Clear → welcome tab in every new-tab path (Ctrl+T, second window, burner via the `openNewTab(makeBurner())` seam) with the home block only; same-tab in-place navigation from the address bar and from a bookmark (before/after `selectedIndex` + tab-count probes were the decisive evidence — coincident titles make screenshots alone insufficient); Set from the welcome surface reached Settings, the row, and window 2 live; across a clean quit/relaunch the stored `session` document held exactly the six web tabs (no welcome/burner/Settings record — DD9 observed on disk) and the boot window's first Ctrl+T honored the home page (**squawk 0005 closed by observation**). Apparatus corrections for the crew notes (squawk 0008 follow-up): allowlisted sheets are read-only to automation — no write op ever resolves a sheet wcId; a star click persists a bookmark immediately; re-capture Settings before every click (status-line insertion shifts controls ~24 px); `enumerateTabs` omits viewless tabs and its `windowId` argument is informational. Side finding → **squawk 0010** (Settings' home-page status line stays stale after an external write; the field and row are correct). DD7's cross-window auto-attach was observed live (window 2's welcome tab attached itself when the home page was set) — designed, but it shifts later rows' baselines; noted for spec authors.

**2026-08-25 — Leg 1 behavior acceptance, spec 2: `search-engine-upgrade` re-run — PASS, 5/5 (was 4/5 on 2026-08-24).** Run log: `tests/behavior/search-engine-upgrade/runs/2026-08-25-03-16-36.md`. Fresh authentic fixture from the `c8563f3` worktree (`version 2`, custom home page, no `searchEngine`), then the Flight 2 build on the same profile. Step 2 — the row that failed on squawk 0005 — passed as authored: the boot window's Ctrl+T opened on `https://fixture.example.net`. **Squawk 0006 closed** without re-authoring (DD5's prediction held). Step 3 met Google's `/sorry/` anti-automation interstitial after the day's repeated scripted searches from this IP; the committed `google.com/search?q=…` URL was preserved in the tab title and the `continue=` parameter and the Validator passed the row on the rendered bar (host + encoded query present) — an environment artifact to expect on future Google-routed rows.

**2026-08-25 — FD ruling: leg 1 lands.** Both specs pass outright; no dispositions needed. Leg 1 → `landed`. Squawks 0005 and 0006 → completed at the flight-end commit (sign-off rides the flight Reviewer). Squawk 0010 logged, deferred to a turnaround (not this flight's surface — a Settings-page freshness nit).

**2026-08-25 — Leg 2 `search-handoff-and-fresh-defaults` designed.** Risk tier: **HIGH** — a defaults change on two existing settings keys with repair semantics flipping to unset (DD5; no version bump by design — the leg must confirm the store's per-key repair tolerates a `null` default given its documented `typeof null` hazard); the removal of the app's last engine-coalescing site with a `null` return from a shared function (`toUrl`) consumed by two entry points; new per-record state (the pending query) with a mission-mandated cap; and it reverses four existing behavior specs' premises (this leg re-authors their preconditions after a premise audit against DD5 — Flight 1's debrief lesson applied before the run). Design review required. Citations audited against the leg-1 working tree.

**2026-08-25 — Leg 2 design review: 1 cycle, approve with changes (all incorporated; cycle 2 skipped — every fix adopts the reviewer's recommendation).** **[high]** the boot path must not read the caches: the boot `Promise.all` awaits `homePage` but not `searchEngine`, and `searchEngineCache` pre-seeds as `'google'` — `openNewTab()` at boot could show the home block only (failing `welcome-first-launch` step 1) or, worse under DD5, route a search typed before the seed to Google without a choice. Fix: await `searchEngine` in the boot barrier, compute reasons from awaited values via a pure `welcomeReasons(home, engine)`, and pre-seed `searchEngineCache` as `null`. **[medium]** there is no empty-input guard in `navigate` today (Enter on an empty bar searched for `''`) — with the engine commonly unset it would open spurious welcome tabs; a guard is added with a test. **[medium]** the merged `navigate` body is now written out: the null check runs before leg 1's welcome-attach branch (`attachView(tab, null)` would silently drop the query). Confirmed: the `typeof null` hazard does not apply (both keys have null-accepting validators; the row-less pin writes explicit JSON `null`); `showWelcomePanel` (`renderer.js:199`) is the re-render seam; `currentSearchEngine` must be injected into `tab-controller.js`; the 41 Google literals in `settings-store.test.js` split 32 to rename / 9 to keep (listed in the leg); suggestion commits never reach `toUrl`'s null branch; drop-to-navigate does not exist. Leg status → `ready`. `[HANDOFF:review-needed]`.

**2026-08-25 — Leg 2 acceptance gate, `welcome-first-launch` checkpoint 3: FAIL → fix at the gate.** The Validator failed the row on its "each with its hint" clause: the engine block shows its unset hint, the home-page field shows none when unset (rendered pixels and a11y agree). The gap is in this leg's own surface — DD6 defined the engine hint and the post-Clear status text but no persistent unset hint for the home page. FD ruling: a fix Developer adds `HOME_UNSET_HINT` to the home-page IIFE and makes the status line reflect state on load and on every broadcast (symmetric with the engine block), with a structural test; that also subsumes squawk 0010 (stale status after an external write). The checkpoint is rerun after the fix (a fresh Settings tab picks the file up — internal pages are served from disk per request, so the first-launch profile is not disturbed). Checkpoints 1–2 stand.

**2026-08-25 — Fix at the acceptance gate: home-page unset hint, applied.** `src/renderer/pages/settings.js`'s home-page IIFE gained `HOME_UNSET_HINT` and a `reflect(value)` helper (sets the input and the status hint together) called from the initial `settingsGet('homePage')` load AND from the `onSettingsChanged` broadcast handler (same `!== undefined` guard as before) — symmetric with the search-engine block's `UNSET_HINT`/`applyChecked`. The Save/Clear click handlers' own local messages ('Saved', 'Cleared — …', error text) are unchanged and still run in their `.then()`.
- **Broadcast-ordering finding (verified by reading `src/main/register-settings-ipc.js`'s `internal-settings-set` handler)**: `broadcastSettings()` is called at line 49, synchronously after `settings.set()` and BEFORE the handler's `return config` (line 62) — i.e. the `settings-changed` broadcast is sent to every window (including the one that called `settingsSet`) before that same call's `invoke()` promise resolves back to the renderer. So on the page that clicked Save/Clear, the broadcast-driven `reflect()` call lands first and the local `.then()` message lands after and wins, exactly as intended; every OTHER open window only ever sees the broadcast's `reflect()`. No `queueMicrotask` adjustment was needed — the handler broadcasts before it returns, not after.
- **Squawk 0010 updated**: status → `in-progress` (Corrective Action: "subsumed by the M16 F2 leg 2 home-page unset-hint fix — the `onSettingsChanged` handler now calls `reflect(all.homePage)`, so an external write updates both the field and the status line instead of leaving the previous local message in place"; Verification: the structural test below, plus a `welcome-home-routing` step 8 re-observation deferred to the flight debrief). Sign-off left for the FD.
- **Tests**: `test/unit/settings-page-shared-scripts.test.js` gained one grep-shape structural test asserting `HOME_UNSET_HINT` is defined in the home-page controller block and that its `onSettingsChanged` handler calls `reflect(all.homePage)` (comment notes no DOM harness exists for internal pages, per this file's house convention); also asserts no search-engine-name literal appears in that block (Grep-AC hygiene rule).
- **Gates (before → after, wall-clock)**: `npm test`: **3761 pass / 0 fail → 3762 pass / 0 fail** (+1, the new structural test; `real 0m3.4s`). `npm run typecheck`: clean before and after (0 errors; ~0.4s). `npm run lint`: clean before and after (~3.2s).
- Files touched: `src/renderer/pages/settings.js`, `test/unit/settings-page-shared-scripts.test.js`, this leg's artifact (Outputs), this flight-log entry, `squawks/0010-settings-home-page-status-stale-after-external-write.md`. No welcome-surface or store files touched. Not committed — left for FD review per this leg's deferred-commit model.

**2026-08-25 — Leg 2 acceptance gate, `new-tab-default-routing` fixture: premise error → second fix at the gate.** The re-authored fixture ("set the home page; the boot tab attaches") does not hold on a fresh profile: the boot welcome tab carries BOTH reasons and `settle()` attaches only when nothing is left unset — so after the home page alone it correctly keeps showing the engine block. The leg's premise audit checked the spec against DD5 but missed that the boot record has two reasons. Underneath it is a product gap the same read exposed: `show(tab)` never settles, so a background welcome tab shown after every preference got set elsewhere would render an empty panel and never attach (DD7 says it attaches to the home page). FD ruling: fix Developer makes `show()` settle when nothing is unset, adds structural coverage, and corrects the fixture bullet to set BOTH preferences before step 1. The chrome bundle loads at window creation, so the re-run restarts on a fresh profile after the fix; checkpoint 1 is not judged on the fixture-broken state.

**2026-08-25 — Second fix at the acceptance gate: `welcome-controller.js` settles on `show()`, applied.** `src/renderer/chrome/welcome-controller.js`: split the old `show(tab)` body into a private `render(tab)` (the block-drawing logic — unchanged) and made `show(tab)` set `currentTab` then delegate to `settle(tab)`. `settle(tab)` is now the single decision point reached from three call sites — `show(tab)` (every activation-class event AND the search-handoff in-place re-render), `submitEngine`'s success path, and the `onSettingsChanged` broadcast handler — and its own dead-end fallback (no reason unset, no pending query, home still unset — not expected to be reachable, since every welcome record's reasons always include `'home'`) was changed from a recursive `show(tab)` call to `hide()` + `els.address.focus()`, matching DD7's stated fallback and avoiding a `show()`↔`settle()` loop now that `show()` calls `settle()`.
- **Background-record scope, decided**: `onSettingsChanged` stays scoped to `currentTab` only — it does NOT walk every open tab and settle every background welcome record eagerly. DD7's wording ("the tab stays a welcome tab until it navigates or until nothing is missing") is about a record being looked at, not a promise to eagerly convert every dormant welcome tab in the tab strip the instant Settings changes; eagerly settling all of them would fire background `tabCreate`/navigate calls the operator never asked for. The smaller, DD7-consistent fix is what `show()` now provides: a background record catches up the moment it becomes the active tab (already-verified ordering — `tab-controller.js`'s `activateTab` sets `ctx.activeTabId = id` before calling `showWelcomePanel`, so `attachView`'s own `tab.id === ctx.activeTabId` check fires correctly when `show()` triggers an attach).
- **Tests**: `test/unit/search-engines.test.js` gained one grep-shape structural test (no DOM harness exists for `welcome-controller.js`, same house convention as the other structural checks on that file) asserting `show(tab)` calls `settle(tab)` and that `settle(tab)` never calls back into `show(tab)` (still asserting it calls `render(tab)` for the ordinary unset-reason case). `test/unit/tab-controller.test.js`'s harness stubs `showWelcomePanel`/`hideWelcomePanel` as call-trackers rather than wiring the real `welcome-controller.js` — checked and confirmed it cannot express a live "attaches on show" case without defeating that decoupling, so no unit case was added there.
- **Spec fixture corrected**: `tests/behavior/new-tab-default-routing.md`'s fixture-setup bullet now sets both the home page and the search engine (Google) via Settings, then returns to the boot tab and closes the Settings tab, before step 1 — see the leg artifact's Outputs for the exact wording and why (the fresh boot tab carries both reasons).
- **Gates (before → after, wall-clock)**: `npm test`: **3762 pass / 0 fail → 3763 pass / 0 fail** (+1, the new structural test; `real 0m4.1s` → `0m3.3s`, `duration_ms` 4110 → 3024 — no slowdown, ordinary run-to-run variance). `npm run typecheck`: clean before and after (0 errors; ~0.4s). `npm run lint`: clean after this fix's own regex literal was corrected mid-task (`no-regex-spaces` — two hand-typed test regexes originally hard-coded `\n  }`'s two literal spaces; rewritten as `\n {2}}`); ~3.2–3.3s.
- Files touched: `src/renderer/chrome/welcome-controller.js`, `test/unit/search-engines.test.js`, `tests/behavior/new-tab-default-routing.md`, `CLAUDE.md` (welcome-surface note), this leg's artifact (Outputs, x2), this flight-log entry. Not committed — left for FD review per this leg's deferred-commit model.

**2026-08-25 — Leg 2 behavior acceptance, spec 1: `welcome-first-launch` — PASS, 7/7 (checkpoint 3 on a rerun after the first fix at the gate).** Run log: `tests/behavior/welcome-first-launch/runs/2026-08-25-04-22-08.md`. A brand-new profile boots to a single welcome tab offering both blocks with an empty, writable address bar and no committed URL; the row is pinned `version 3` with both keys explicitly `null` (DD5 observed on disk); Settings shows both fields unset, each with its hint (once `HOME_UNSET_HINT` was added); choosing an engine removes only its block and the next new tab offers only the home block; a search then goes straight to the chosen engine; setting the home page from the surface navigates that tab and ends the welcome surface for new tabs; the final row holds exactly the two choices.

**2026-08-25 — Leg 2 behavior acceptance, spec 2: `welcome-search-handoff` — PARTIAL, 6/6 automated pass; step 7 operator-performed, pending.** Run log: `tests/behavior/welcome-search-handoff/runs/2026-08-25-04-48-18.md`. With a home page set and no engine: a plain new tab is not a welcome tab; a search hands off to a welcome tab beside the untouched page, showing the engine block headed with the pending query and committing no provider URL; choosing DuckDuckGo runs that search in the same slot; the row records the choice; the next search goes straight to DuckDuckGo; the Settings engine Clear unchecks every radio with its hint; a fresh handoff after Clear is escaped by typing an address in the same tab, discarding the query with no provider URL ever committed. Step 7 (page-context "Search for …" with the engine unset) is outside `AUTOMATABLE_MENU_TYPES` and is operator-performed by the suite's convention (squawk 0007 precedent); structural coverage: `sel:search` routes a null `toUrl` through `openWelcomeTab` with the selection as `pendingQuery`. **Disposition: the leg lands with this row recorded as an operator hand check pending** — ten seconds at the flight debrief: engine unset, select text → right-click → "Search for …" → a welcome tab in the source jar with the selection as its pending query; choose an engine → it runs.

**2026-08-25 — Leg 2 behavior acceptance, spec 3: `new-tab-default-routing` re-run — PASS, 8/8 (checkpoint 1 on a redo of the fixture after the second fix at the gate).** Run log: `tests/behavior/new-tab-default-routing/runs/2026-08-25-05-04-03.md`. Jar routing of new tabs is unchanged by the welcome-surface rewrite: boot tab in the personal default; `openTab` follows the live default flag; moving the flag reroutes without a restart; an emptied registry falls through to the Burner identity and yields fresh `burner-<n>` tabs; the first jar added to an empty registry auto-claims the default; a second does not move it. The fixture's first form (home page only) exposed the `show()`-never-settles gap recorded above; fixed, fixture corrected to set both preferences, run restarted on a fresh profile.

**2026-08-25 — Leg 2 behavior acceptance, spec 4: `search-engine-preference` re-run — PARTIAL, 7/7 automated pass; step 6 operator-performed, pending (was 7/8 with step 6 inconclusive on 2026-08-24).** Run log: `tests/behavior/search-engine-preference/runs/2026-08-25-05-35-38.md`. With the re-authored fixture (engine Google, home page unset): Settings shows Google selected and the home page empty with its unset hint; the fixture engine is honored with no user choice (Google `/sorry/` interstitial again — judged on the committed bar plus the rendered title/`continue=` target); selecting DuckDuckGo leaves the home page untouched; searches from a new welcome tab (attaching in place, tab count unchanged), from a second window, and after a full quit/relaunch (PID-attested; the row's key hashes and wcId renumbering corroborated it) all commit to `duckduckgo.com`; setting the home page leaves the engine untouched; both preferences survive restart independently. Step 6 (context-menu search with an engine set) is operator-performed by spec (squawk 0007) and pending the same hand check as `welcome-search-handoff` step 7 — the 2026-08-24 debrief verified it by hand on Flight 1's build, and this leg did not change the `sel:search` → `toUrl` path when an engine is set. The Executor's transcript was lost after its step-8 report (second occurrence on this spec); its closing summary is reconstructed from its final notes in the run log. Apparatus note carried into the crew notes: restart-adjacent rows should carry an explicit Orchestrator PID attestation by default.

**2026-08-25 — FD ruling: leg 2 lands.** All four acceptance specs pass on every automated checkpoint after two fixes at the gate (home-page unset hint; `show()` settles); the two operator-performed rows (`welcome-search-handoff` step 7, `search-engine-preference` step 6) are recorded as hand checks pending for the flight debrief — both exercise the page-context sheet, which no automation tier can drive, and both have structural coverage in this leg's unit tests. FD verification after the last fix: `npm test` **3763 pass / 0 fail** (`duration_ms` 3138), `npm run typecheck` clean, `npm run lint` clean. Leg 2 → `landed`. Squawk 0010 → completed at the flight-end commit alongside 0005 and 0006 (sign-off rides the flight Reviewer). Proceeding to Phase 2d: flight-end Reviewer over all uncommitted changes (both legs, both gate fixes, artifacts, run logs).

**2026-08-25 — Phase 2d: flight-end review, 1 cycle, `[HANDOFF:confirmed]`.** Reviewer (Sonnet, no knowledge of the Developers' reasoning) reviewed all ~51 uncommitted paths — both legs, both gate fixes, squawks 0005/0006/0010, artifacts, and the six run logs. Independently ran `npm test` **3763 pass / 0 fail** (`real 0m3.3s`), typecheck clean (~2.3s), lint clean (~3.1s); leak grep over the whole diff (username, home paths, key material) clean. Traced every leg acceptance criterion to the diff and its test: the `chrome-welcome-set` handler (key-restricted, literal `settings.set(` for the broadcast-invariant net), `PENDING_QUERY_MAX` at both capture sites plus `buildSearchUrl`, `toUrl`'s null path through `navigate`'s ordered guards, the boot barrier, `DEFAULTS` null/null with unchanged validators, `hasView` wiring, the host-only `openSiteSettingsTab`, and every `wcId == null` gate (activation, close/backfill, drag/tear-off, context menu, snapshot/closed-tab capture) each backed by a unit test. Awareness note, not a defect: `submitHome` attaches unconditionally on success even when the engine block is still showing (required by leg 1's AC and DD7's disjunctive exit — setting a home page is itself a navigation); no behavior spec exercises the home-first-then-engine order — a candidate row for the debrief. One non-blocking artifact note (legs unchecked in `flight.md`) — covered by the close-out below.

**2026-08-25 — Flight lands.** Squawks 0005, 0006, 0010 → `completed` with the Reviewer's sign-off. Legs 1 and 2 → `completed`; `flight.md` legs, checkpoints, prerequisites, and contributing criteria checked; mission criteria 3, 5, 6, 7, 8, 9, 10 checked as met (criterion 7's engine-unset context-menu entry point and `search-engine-preference` step 6 remain operator hand checks pending for the debrief); mission Known Issues claimed by this flight (`openSiteSettingsTab`, `homePageCache` seed, `settings.js` truthy guards) and the four open questions this flight resolved checked; Flight 2 checked in the mission. Flight status → `landed`. One commit on `flight/02-welcome-surface`; PR opened draft then marked ready. Apparatus cleanup: fixture worktree removed, scratch profiles deleted, no Electron processes left running. `/flight-debrief` next. `[COMPLETE:flight]`

**2026-08-25 — Flight debrief.** Developer and Architect interviews run (`flight-debrief.md`). Operator hand checks closed at the debrief: `welcome-search-handoff` step 7 and `search-engine-preference` step 6 both functionally passed on the Flight 2 build (recorded in each run log's Operator Notes). Operator feedback — unstyled Settings Clear buttons, underwhelming welcome page — routed to Flight 3. Squawks logged: 0011 (`requestTearOff` guard untested), 0012 (crew-file apparatus facts). Flight status `landed` → `completed`; PR #167 carries the flight commit and this debrief.
