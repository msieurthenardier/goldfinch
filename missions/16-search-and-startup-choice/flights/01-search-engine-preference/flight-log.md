# Flight Log: Search Engine as a Preference

**Flight**: [Search Engine as a Preference](flight.md)

## Summary

In flight as of 2026-08-11. Two autonomous legs planned; deferred-commit model (single review + commit after last leg).

---

### Flight Director Notes

**2026-08-11 — Flight open.** Crew file `leg-execution.md` validated (Crew/Interaction Protocol/Prompts present). Flight status `ready` → `in-flight`. 2 autonomous legs, 0 completed. Squawk 0005 (homePageCache boot seed) logged pre-flight and deferred to M16 F2 — not this flight's scope.

**2026-08-11 — Leg 1 `preference-core` designed.** Risk tier: **HIGH** — schema change (settings `version` 2→3, new key, two validators widened), migration-ladder restructure (single-shot → per-rung guards, DD5 trap), and a novel persistence behavior (pin-on-load for row-less profiles). Design review required per tier. Citation audit performed at authoring (all settings-store.js citations re-verified against working tree this session).

**2026-08-11 — Leg 1 design review: 2 cycles, approved.** Cycle 1 (Developer, Sonnet): approve with changes — 1 high (corrupt document-row repairs never persisted: `parseAndRepair` catch hardcodes `migrated: false`, so pin-on-load case (c) needed code, not just a test), 1 medium (two existing tests pin the old behavior — `settings-store.test.js:71-85` `version === 2`, `:1410-1423` `readRow === null` — and would fail red on a correct implementation; now enumerated for rewrite-and-rename), plus rung-guidance reword and Google/Bing description copy resolved. Cycle 2: deltas confirmed accurate and implementable; one attribution error in the artifact corrected (rename rule is a methodology convention, not a flight-local rule). Leg status → `ready`.

**2026-08-11 — Leg 1 `preference-core` implemented, all acceptance criteria verified. Leg status → `landed`.**

- **Landed**: `src/shared/search-engines.js` (new, real ESM) — the mission's ordered eight-engine table (`{ id, label, template, description }`), `SEARCH_ENGINE_IDS` (frozen `Set`, the validator's membership source), `getSearchEngine`, and `buildSearchUrl` (never throws — `null` on an unknown id, mirroring `jarDataClassById`'s lookup-miss shape). `settings-store.js`: DEFAULTS bumps `version` to 3 and gains `searchEngine: 'google'`; `Settings` typedef widens `homePage` to `string | null` and adds `searchEngine: string | null`; `VALIDATORS.homePage` widened to accept `null`, `VALIDATORS.searchEngine` added as a strict membership check against `SEARCH_ENGINE_IDS`; `migrateStored()` restructured to per-rung guards (`from < 2` re-guards the `restoreSession` discard; the v2→v3 rung is stamp-only); `parseAndRepair()` renamed its return field `migrated` → `needsPersist` and the corrupt-bytes catch now returns `needsPersist: true` (previously hardcoded `migrated: false`, the design-review high finding); `load()`'s row-present branch persists on `needsPersist`, and the no-row branch now does a best-effort pin-on-load save (previously left the profile row-less). `CLAUDE.md`'s Settings-store section gained the `searchEngine`/DD5 documentation and the `homePageCache` boot-seeding correction (squawk 0005 wording).
- **Test counts**: `test/unit/settings-store.test.js` 66 → 83 tests (all passing); new `test/unit/search-engines.test.js` adds 22. Full suite (`npm test`): 3706 passing, 0 failing, before and after (no regressions elsewhere). `npm run typecheck` and `npm run lint` both clean.
- **Neuter check (DD8), performed by hand**: relaxed `VALIDATORS.searchEngine` to `(v) => v === null || typeof v === 'string'`, ran `test/unit/settings-store.test.js` — **6 tests went red**: `searchEngine — non-curated id ("kagi") rejected by set(), prior value kept`, `searchEngine — URL-shaped string rejected by set(), prior value kept (no user-supplied template)`, `searchEngine — empty string rejected by set(), prior value kept (null is the only unset representation)`, `searchEngine — non-curated / hostile stored values repair to the default at load()`, `searchEngine — an id removed from the curated table repairs silently to the default at load()`, `pin-on-load: legacy settings.json containing a hostile searchEngine repairs through the same shared path`. Restored the validator; full suite green again (83/83, then confirmed clean in the full 3706-test run).
- **Test rewrites**: the two spec-enumerated tests (`test/unit/settings-store.test.js:71-85` version===2, `:1410-1423` readRow===null) were renamed (not deleted-and-re-added) to pin pin-on-load instead — see the `RENAMED (M16 F1 / DD5 — was '...')` comments directly above each in the test file for the exact before/after framing. One additional test needed the same rename treatment beyond the two spec-named ones, found while making the suite green: `'v1→v2 migration — v2 row with restoreSession:false is NOT re-migrated (stays false)'` also pinned a claim ("no row rewrite at load") that DD5 makes false for a v2 row (it now migrates to v3 and IS rewritten, while the `restoreSession:false` opt-out itself is still correctly preserved) — renamed to `'v2 row migrates to v3 (searchEngine pinned explicit), restoreSession:false opt-out NOT re-discarded'` with the same rename-not-delete treatment and an inline comment explaining why. Every other test asserting a literal `version === 2` on a scenario that now resolves to the current schema version got a mechanical value update to `3` (automation-keys/spellcheck/restoreSession default-on-first-load tests, the four remaining v1-row migration tests renamed `v1→v2` → `v1→v3` in their titles since the transform now lands in one hop) — these were plain number corrections, not semantic reversals, so left un-flagged by the rename convention.
- **Deviations from spec**: none blocking. One self-correction worth noting for future legs: my first draft of `search-engines.js`'s header comment literally contained the string `require('electron')` in prose (describing what the module does NOT do), which the leg's own verification command (`grep -c require\(.electron.\) src/shared/search-engines.js` → expect `0`) and my own first draft of the "Electron-free" unit test both matched as a false positive. Fixed by rewording the comment to avoid the literal substring and by comment-stripping lines before the regex in the unit test. Grep-AC authors: a literal `grep -c 'require(...)'` verification step is trivially defeated by a comment that discusses the pattern in prose — worth a note in Flight Control's Grep-AC convention guidance if this recurs.
- **For leg 2 (ground truth, not aspiration)**: `settings-store.js`'s `set()`/`load()`/`get()`/`getAll()` surface is unchanged in shape — leg 2 can call `store.set('searchEngine', id)` exactly like any other key, through the existing `internal-settings-set` handler (DD3), no new IPC needed. `src/shared/search-engines.js` exports `SEARCH_ENGINES` (ordered, frozen, for rendering the radio group from a single source), `SEARCH_ENGINE_IDS` (Set, not needed renderer-side but available), `getSearchEngine(id)` (returns `null` on miss, never throws), and `buildSearchUrl(id, query)` (also returns `null` on an unknown id rather than throwing — leg 2's `toUrl` should treat a `null` result the same as it would treat an unset/corrupt engine, i.e. this should be structurally unreachable once `searchEngine` is always a validated stored value, but the null-return contract exists so a defensive caller doesn't need a try/catch). `buildSearchUrl` escapes via `encodeURIComponent` — confirmed it correctly escapes `&`, `#`, `+`, spaces, and non-ASCII, and that a literal `%s` in the query text does not create a second substitution point. No renderer or preload file was touched (verified via `git status --porcelain`). Squawk 0005 (homePageCache boot-seeding gap) is unchanged/untouched by this leg — its CLAUDE.md description is now corrected, but the underlying renderer defect is still live and is leg 2 / DD4's to fix or subsume, not this leg's.
- **Out-of-scope defect found**: none beyond what's already tracked (squawk 0003, squawk 0005 — both pre-existing and out of this leg's scope per the flight spec).

**2026-08-11 — Leg 2 `consumers-and-settings-ui` designed.** Risk tier: **HIGH** — cache/freshness behavior (`searchEngineCache`, the flight's corrected DD4 — the one place this flight can silently recreate squawk 0005's defect class), a shared-surface change (`toUrl`, consumed by both search entry points and pinned by existing tests), and an internal-page asset-route change (`internal-page-map.js`). Design review required per tier. Cache freshness contract declared in the leg per the risk-check rule. Citation audit performed post-leg-1 (renderer files untouched by leg 1, all citations re-verified). Behavior-spec execution is FD-owned; Developer leaves the leg `in-flight` — FD lands it after specs pass (deferred-commit model note updated in the leg's checklist).

**2026-08-11 — FD apparatus probe (behavior-test prerequisite): VERIFIED.** Launched `npm run dev:automation` against a fresh scratch profile (`XDG_CONFIG_HOME` scratchpad dir) with `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1`. App booted (WSLg/Wayland), keys minted, MCP bound to **49709** (free-port fallback off the configured 49707 — the specs' "discover the live port" note proven necessary). Unkeyed probe: HTTP 401 at `/` and `/mcp` (auth gate alive). Admin-keyed `initialize`: HTTP 200, serverInfo goldfinch 0.13.4, tools capability present. Leg 2 Developer's MCP-timeout flag resolved as sandbox noise — FD's probe also required sandbox-disabled networking; no regression. This instance retained as the fresh-profile fixture for `search-engine-preference`.

**2026-08-11 — Leg 2 design review: 1 cycle, approved with changes (all incorporated; no design shifts, cycle 2 skipped per protocol).** Findings: verification grep couldn't see the coalescing site (fixed with a targeted second command); no DOM-driven controller-test harness exists for ANY internal page — settings-page coverage restated as structural test + behavior specs; nav-controller test edit is a single shared harness (`navigation-controller.test.js:34-112`); settings-page GET-vs-broadcast race acknowledged as inherited-and-accepted; `searchEngine: null` via raw IPC renders no radio checked — accepted F1 behavior by FD ruling (honest unset; F2 owns the UX). Leg status → `ready`.

**2026-08-11 — Leg 2 `consumers-and-settings-ui` implemented, all Developer-verifiable acceptance criteria checked off. Leg status stays `in-flight`** (deferred-commit model — the FD runs the two behavior specs and lands the leg after they pass; the one unchecked AC in the leg artifact is the FD-run behavior-acceptance criterion).

- **Landed** (uncommitted, on top of leg 1's uncommitted work — nothing reverted):
  - `src/renderer/renderer.js` — `searchEngineCache` (module-scope, default `'google'`), `setSearchEngine(value)` (raw store, no coalescing — null survives for Flight 2), `currentSearchEngine()`; imports `buildSearchUrl` from `../shared/search-engines.js`; wires `buildSearchUrl`/`currentSearchEngine` into the `navigationController` deps object and `setSearchEngine` into the `windowController` deps object.
  - `src/renderer/chrome/navigation-controller.js` — `toUrl`'s hardcoded `https://www.google.com/search?q=...` line replaced with `buildSearchUrl(currentSearchEngine() || 'google', s)` (the flight's one Google-coalescing read site, commented as Flight-1-only semantics); `buildSearchUrl`/`currentSearchEngine` added to the deps destructure.
  - `src/renderer/chrome/window-controller.js` — boot seed `window.goldfinch.settingsGet('searchEngine').then(setSearchEngine).catch(() => {})` beside the `bookmarksBarEnabled` seed; `settings-changed` handler gains `if (all && all.searchEngine !== undefined) setSearchEngine(all.searchEngine);` beside the `homePage` line (guard is `!== undefined`, never truthiness); `setSearchEngine` added to the deps destructure.
  - `src/main/internal-page-map.js` — `'/search-engines.js': shared('search-engines.js')` added to the `settings` route map.
  - `src/renderer/pages/settings.html` — a `search-engines.js` module script tag (alongside `audit-paging.js`/`safe-color.js`); a `<fieldset class="shields-group search-engine-group"><legend>Search engine</legend><div id="search-engine-options"></div></fieldset>` shell in `#appearance`, directly after `#home-page-status` and before the session-restore fieldset (adjacent to the home-page control, independent group). The fieldset carries no engine-specific text — settings.js renders it.
  - `src/renderer/pages/settings.js` — `@ts-ignore` import of `SEARCH_ENGINES` from `./search-engines.js` (serving-path convention); a new IIFE controller (mirrors the home-page/shields IIFEs' shape) that builds one `<label class="search-engine-option shield-row">` per engine (`createElement`/`textContent` only) with a description `<p>`, wires each radio's `change` → `settingsSet('searchEngine', id)`, checks the matching radio from the initial `settingsGet('searchEngine')` and on every `onSettingsChanged` broadcast (guard `!== undefined`), and detaches its listener on `pagehide` (the established DD5 cleanup pattern).
  - `src/renderer/pages/settings.css` — `.search-engine-group`/`.search-engine-row`/`.search-engine-option`/`.search-engine-description` rules, following the existing `.shield-row` pill-control and `.startup-toggle-group` divider conventions; no new visual language.
  - `CLAUDE.md` — extended the exact `homePageCache` paragraph in `### Settings store` (the one leg 1 touched) with a new `searchEngineCache` bullet: boot-seed + broadcast contract, the raw-store/no-coalescing setter, the one coalescing read site, and the Settings-page radio group's single-source-of-truth wiring.
  - Tests: `test/unit/navigation-controller.test.js` — the shared harness (`buildSearchUrl` from the real `src/shared/search-engines.js`, `currentSearchEngine: () => state.searchEngine`) plus a new test covering `toUrl` under DuckDuckGo, Google, and `null` (all coalesce correctly); the existing default-engine assertion at the old `:118` survives byte-identical (searchEngine defaults `null` in the harness, coalescing to Google). `test/unit/window-controller.test.js` — `setSearchEngine` added to the harness deps/mock `settingsGet`; a new test pins the boot seed AND the broadcast guard (`null` applied, `undefined`/absent ignored). `test/unit/internal-page-map.test.js` — `/search-engines.js` added to the expected settings-route key set + a resolved-path assertion. New file `test/unit/settings-page-shared-scripts.test.js` (no settings-page sibling existed before this leg — modeled byte-for-byte on `jars-page-shared-scripts.test.js`/`vault-page-shared-scripts.test.js`'s script-tag-contract pattern): script-tag presence/resolution/module-pin/defer-pin checks, the `/search-engines.js` route assertion, and a structural no-duplication check (engine id/label/description must appear only in `search-engines.js`, never in `settings.html`'s search-engine fieldset or in `settings.js`) — this is the leg's structural settings-page coverage per the Outputs section (no DOM-driven controller-test harness exists for any internal page; live radio behavior is the behavior specs' job).
- **Test counts**: `npm test` 3706 → 3714 (all passing, 0 failing) — +8 (6 new in `settings-page-shared-scripts.test.js`, 1 in `navigation-controller.test.js`, 1 in `window-controller.test.js`). `npm run typecheck` and `npm run lint` both clean.
- **Verification greps** (leg spec's Verification Steps), all as specified: `grep -n "google.com/search" navigation-controller.js` → no hits; `grep -n "searchEngineCache" renderer.js` → exactly the definition, setter, and accessor (3 lines); `grep -rn "currentSearchEngine() || 'google'" navigation-controller.js` → exactly 1 hit; the bounding `grep -rn "'google'" src/renderer/chrome/ src/renderer/renderer.js` shows exactly that one coalescing site plus the `searchEngineCache` default-value line and two explanatory comments.
- **`npm run dev:automation` smoke** (Developer step, per Implementation Guidance #9): the app boots successfully against the full uncommitted change set — main process starts, the dev profile's settings load through the widened v3 schema with `searchEngine` pinned `'google'` (confirmed by reading `~/.config/goldfinch-dev/app.db`'s settings row directly), the jar + admin automation keys mint and print the `AUTOMATION_DEV_MINT` line without error. No console/parse errors traced to any file this leg touched (`settings.js`'s new `search-engines.js` import, the new `#search-engine-options` markup, `navigation-controller.js`, `window-controller.js`) — a broken serving-path import or a bad `internal-page-map.js` route would have surfaced as a 404/SyntaxError/module-resolution failure at exactly this boot phase, and none did. **Could not confirm, for the FD's attention**: direct HTTP reachability to the MCP port (`curl http://127.0.0.1:49707/mcp`) timed out (connection never established, not refused) across three separate launch attempts in this sandboxed session, despite a same-pattern control test (a plain `python3 -m http.server` on a different port, launched and curled the same way) succeeding immediately — so this reads as an environment/sandbox networking quirk in this session rather than a real bind failure (I made zero changes to `automation/mcp-server.js`, `app-lifecycle.js`, or any bind path this leg; `startMcpServerInstance()` is also fired-and-forgotten pre-existing code, unrelated to this leg's diff). The flight's own Prerequisites already flag automation-surface liveness as a "probe at run time — decay-prone" item — **the FD should re-probe MCP reachability directly in its own execution environment before trusting this smoke result and before running either behavior spec**, since only that context's reachability actually matters for the runs. (Housekeeping note: three earlier smoke attempts in this session left orphaned Electron subprocesses behind — `pkill -f "dev-launch.mjs"` does not reach them, since Electron re-execs past the node wrapper; `pkill -9 -f "node_modules/electron/dist/electron"` does. All confirmed cleaned up — `ps aux` shows no goldfinch/electron processes after this session's work.)
- **Upgrade-fixture worktree commands** (for the FD to execute, per `search-engine-upgrade.md`'s Preconditions — produces the authentic pre-upgrade profile the spec requires; not run this session beyond the dry validation below):
  ```bash
  # 1. Worktree at the pre-implementation commit (leg 1's code is uncommitted on the
  #    working branch, so HEAD == c8563f3 == the authentic pre-Flight-1 build).
  git -C ~/projects/goldfinch worktree add /tmp/goldfinch-fixture-worktree c8563f3

  # 2. Reuse the existing node_modules (OS/arch-specific, not commit-specific;
  #    node_modules is gitignored so the fresh worktree has none) — avoids a second
  #    full `npm install` / electron download.
  ln -s ~/projects/goldfinch/node_modules /tmp/goldfinch-fixture-worktree/node_modules

  # 3. Launch the pre-flight build against a scratch profile. XDG_CONFIG_HOME
  #    relocates Electron's whole userData root (dev-profile.js derives the
  #    "-dev"-suffixed dir FROM Electron's default userData path, which is itself
  #    $XDG_CONFIG_HOME/goldfinch on Linux) — this isolates the fixture from the
  #    real ~/.config/goldfinch-dev profile.
  mkdir -p /tmp/goldfinch-fixture-xdg
  cd /tmp/goldfinch-fixture-worktree
  XDG_CONFIG_HOME=/tmp/goldfinch-fixture-xdg GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation

  # 4. In the running (pre-Flight-1) app: open goldfinch://settings, set Home page to
  #    https://fixture.example.net, click Save (forces the settings row write the
  #    spec's precondition needs), then quit the app cleanly (close the window —
  #    NOT a kill signal, so will-quit's snapshot/save path runs).

  # 5. Verify the fixture (spec precondition: version 2, custom home page, no
  #    searchEngine key at all — this build predates leg 1's schema):
  sqlite3 /tmp/goldfinch-fixture-xdg/goldfinch-dev/app.db \
    "select payload from documents where store='settings';"
  #   expect: {"version":2,"homePage":"https://fixture.example.net",...} with no
  #   "searchEngine" key present.

  # 6. Run the upgrade spec against this SAME scratch profile, but on the current
  #    (Flight-1) working tree — NOT the worktree — per the spec's second
  #    precondition ("Flight 1 build launched against that same scratch profile"):
  cd ~/projects/goldfinch
  XDG_CONFIG_HOME=/tmp/goldfinch-fixture-xdg GOLDFINCH_AUTOMATION_ADMIN=1 \
    GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation

  # 7. Cleanup after the run (both are scratch, safe to remove):
  git -C ~/projects/goldfinch worktree remove /tmp/goldfinch-fixture-worktree
  rm -rf /tmp/goldfinch-fixture-xdg
  ```
  Not run end-to-end this session (Developer scope stops at documenting the procedure per Implementation Guidance #9 — behavior-spec execution is FD-owned); the worktree-add/symlink/launch mechanics were reasoned from `src/shared/dev-profile.js`'s actual `XDG_CONFIG_HOME`-derivation logic and the spec's own stated Preconditions, not assumed.
- **Deviations from spec**: none blocking.
  - AC "a test exercises the `sel:search` path's URL construction if the existing suite has a seam for it" — no seam exists (`sel:search` dispatch lives in `renderer.js`'s page-context handler, outside `navigation-controller.test.js`'s harness); noted rather than building a new harness, per the AC's own instruction and the leg's "no new DOM-driven harness" constraint.
  - The settings-page duplication-check test (`settings-page-shared-scripts.test.js`) is scoped to the new `search-engine-group` fieldset specifically, not the whole `settings.html` document — an early version whole-document-scanned and false-positived on the pre-existing spellcheck note's unrelated "downloads a one-time dictionary from **Google**" copy. Same false-positive-literal-grep lesson leg 1's flight log already flagged for `require('electron')`; scoping to the new fieldset region fixed it cleanly.
  - No status/message paragraph was added for the search-engine control (unlike home page's text-input+Save+status pattern) — selection applies immediately on radio `change` with no separate Save step, matching the checkbox controls' pattern (shields, bookmarks-bar, spellcheck all have no status line either) rather than home-page's. Within the leg's "Acceptable variations: copy tweaks and radio-group layout details" allowance — not a deviation from any stated AC.
- **Out-of-scope defects found**: none new. The MCP-reachability smoke observation above is flagged for the FD's own re-verification, not filed as a squawk — it may be entirely a property of this sandboxed session's networking (a same-pattern control HTTP server on a different port worked fine) rather than a real product defect, and I touched no automation-binding code this leg.

**2026-08-24 — Session resumed (prior session cut off after leg 2 implementation).** State verified consistent: leg 1 `landed`, leg 2 `in-flight` with only the FD-run behavior-acceptance criterion open, all work uncommitted on `flight/01-search-engine-preference` with HEAD still at `c8563f3` (the pre-flight commit the upgrade fixture needs). Crew file re-validated. Proceeded straight to the acceptance gate.

**2026-08-24 — Behavior acceptance, spec 1: `search-engine-preference` — PARTIAL, 7/8 pass, 1 inconclusive.** Run log: `tests/behavior/search-engine-preference/runs/2026-08-24-22-41-08.md`. Live two-agent mode (Sonnet Executor + Validator). Passed: explicit Google default rendered adjacent to the home-page control with per-engine descriptions; address-bar search → Google; selecting DuckDuckGo takes effect immediately in the same window (new tab) and in a second window (Ctrl+N) with no restart; home page and engine independent (setting one leaves the other); both preferences survive a clean quit/relaunch (Orchestrator-run out-of-band relaunch, key rotation handled). **Inconclusive, checkpoint 6 (context-menu "Search for …")**: the page-context menu is a chrome-owned sheet outside `AUTOMATABLE_MENU_TYPES` — refused for every op at every tier and not composited by `captureWindow` — so the item could neither be driven nor observed; no OS-level input tool on this machine. Apparatus gap, not a product observation; structural coverage stands in (the `sel:search` dispatch calls the identical `toUrl` verified end-to-end through the address bar in four checkpoints). Two apparatus findings recorded for future specs: `openTab`/`navigate` refuse `goldfinch://` by design — internal pages are reached via the chrome `kebabActionSettings` seam; the original Executor's transcript was lost after checkpoint 6 and a replacement was re-spawned with the established facts (no state drift).

**2026-08-24 — Behavior acceptance, spec 2: `search-engine-upgrade` — PARTIAL, 4/5 pass, 1 fail (known issue).** Run log: `tests/behavior/search-engine-upgrade/runs/2026-08-24-23-17-56.md`. Fixture produced fresh per the spec from a worktree at `c8563f3` (pre-flight build set the home page to `https://fixture.example.net`, clean quit; row verified `version 2`, no `searchEngine` key). On the Flight-1 build: home page untouched with Google explicit in Settings; search → Google with no user choice; stored row `version 3` with `searchEngine: "google"` and `homePage` pinned explicit (Executor read timestamped before the step-5 mutation; Validator re-read byte-identical); DuckDuckGo → `duckduckgo.com`. **Failed, checkpoint 2 (new tab opens on the home page)**: the first Ctrl+T after launch opened Google, not the fixture home page. This is **squawk 0005** (`homePageCache` never boot-seeded), pre-existing and deferred to M16 F2 per the flight spec — confirmed three ways: the squawk's report describes exactly this; the run's supplementary Ctrl+T after a `settings-changed` broadcast opened `fixture.example.net`; and an Orchestrator differential on the pre-flight build (`c8563f3`, fresh profile, same home page, relaunch, Ctrl+T in the boot window) reproduced the identical Google miss. The upgrade is neutral for this row. **Spec premise error**: step 2 was authored against the pre-correction belief that the boot read seeds the cache — the row should be re-authored to pin steady-state (post-broadcast) new-tab behavior, or first-after-launch once squawk 0005 lands; left as-is this run so the record shows real behavior.

**2026-08-24 — FD ruling: leg 2 lands with dispositions recorded.** Every product claim the apparatus could observe passed across both specs. The one fail is a tracked, deferred, out-of-scope defect proven pre-existing by differential; the one inconclusive is an apparatus limitation with structural coverage and a ten-second manual check available (select text → right-click → "Search for …" → expect a `duckduckgo.com` tab). Per the methodology's known-issue disposition, leg 2 → `landed`; both dispositions carried to the flight debrief. Flagged to the operator in the session summary for override. Both specs promoted `draft` → `active` with their first run recorded.

**2026-08-24 — Flight-end review (Phase 2d): 1 cycle, `[HANDOFF:confirmed]`, no blocking issues.** Reviewer (Sonnet) independently ran `npm test` (3714/3714), `npm run typecheck`, `npm run lint` — all clean — checked every acceptance criterion in both leg artifacts against the diffs, read both run logs in full and judged the behavior-test dispositions honestly recorded and evidence-consistent, and grepped the change set for operator-path/username leaks (none). FD's own verification run matched (3714 pass, 0 fail; typecheck + lint clean). Two non-blocking notes: (1) flight.md check-offs and status were still pending — closed out in this commit; (2) one non-reproducible unit failure was seen when the suite ran concurrently with typecheck/lint in a single batched call (no failing test name emitted; 4 isolated re-runs clean) — pre-existing test-infrastructure sensitivity to parallel npm scripts, noted for the debrief, not attributable to this flight. Both legs → `completed`; flight → `landed`; Flight 1 and mission criteria 1 and 4 checked off in the mission. Single flight-end commit follows; PR opened.

---

