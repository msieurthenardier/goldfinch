# Flight Log: Welcome Branding and Alignment

**Flight**: [Welcome Branding and Alignment](flight.md)

## Reconnaissance Report

Source: the Flight 2 debrief (`../02-welcome-surface/flight-debrief.md`, 2026-08-25) — the items it routed to Flight 3, walked against `main` at `bb053d3`.

| Item | Classification | Evidence | Recommendation |
|------|----------------|----------|----------------|
| Settings Clear buttons unstyled | `confirmed-live` | `src/renderer/pages/settings.css` has rules for `#home-page-save` and `.settings-btn` but none for `#home-page-clear` or `#search-engine-clear`; `settings.html:45`, `:59` carry no class | Fix in leg 1 (DD3) |
| Welcome page visually underwhelming | `confirmed-live` | `src/renderer/styles.css` welcome block comment: "Presentable and unstyled — Flight 3 owns the visual language"; no brand mark on the surface | Leg 1 first pass (DD1), HAT (leg 2) |
| Burner note as a first-class element | `confirmed-live` | `welcome-controller.js` builds `#welcome-burner-note` with `className = 'muted hidden'` — 13 px dim prose | Leg 1 (DD4) |
| Layout constraints (full-bleed under `#webviews`, `.hidden` toggling) | `confirmed-live` (constraint, not work) | `styles.css` `#welcome-surface { position: absolute; inset: 0 }`; blocks toggled by `classList.toggle('hidden', …)` in `render()` | Carried as DD2/DD6 |
| Behavior row: home-first-then-engine on a both-unset tab | `confirmed-live` (premise corrected) | No spec row exercises it — `welcome-first-launch` steps 4–6 choose the engine first; `submitHome` attaches unconditionally (`welcome-controller.js` `submitHome`). The F2 debrief's claim that "the next new tab will offer it again" does not hold: `tab-controller.js` `openNewTab` — `if (home != null) return createTab(home, container)` — never reaches the welcome path once a home page is set; the abandoned choice returns only via `navigation-controller.js` `handoffSearch` | New spec `welcome-home-first` in leg 1, authored to the real mechanism |
| Behavior row: burner welcome shows the app-wide note | `already-satisfied` | `tests/behavior/welcome-home-routing.md` step 5 — Expected Result "The burner tab is a welcome tab carrying the burner note ("saved for all of Goldfinch") in the burner jar"; passed on run `2026-08-25-02-45-35` (10/10) | Retire as new work; keep the spec in the leg-1 re-run net (its row will exercise the DD4 restyle) |

*(runtime decisions recorded here during execution)*

---

### Flight Director Notes

**2026-08-25 — Flight designed.** Operator direction at the interview: branded & minimal; first-pass leg plus a HAT leg; author the debrief's behavior rows in leg 1. Recon retired one of the two rows (burner note — already asserted by `welcome-home-routing` step 5). Spec `welcome-home-first` drafted with the flight.

**2026-08-25 — Flight design review: 1 cycle, needs rework → fixed; second pass scoped to the rewritten spec.** **[high]** the draft `welcome-home-first` steps 3–5 asserted that Ctrl+T after a home-first Set re-offers the engine block — a category-level trace; the value-level trace (`openNewTab` returns `createTab(home, container)` the moment `currentHomePage()` is non-null, which the synchronous `chrome-welcome-set` broadcast guarantees before the next action) shows a plain home-page tab, with the abandoned choice returning only through `handoffSearch`. Rewritten: step 3 pins the plain tab (engine still `null`), step 4 the handoff to a new welcome tab with the pending query and no provider URL (diffed `enumerateTabs`), step 5 the choice running the search in place. The Flight 2 debrief's contrary sentence is annotated in this flight's Contributing section rather than rewritten. **[medium]** DD1 now forbids `display:none`/`visibility:hidden` on the native radios (AT-safe hiding only) and adds `role="radiogroup"` to `#welcome-engine-options`. **[low]** DD3 states the explicit `margin-left` `#home-page-clear` needs (the Save gap is Save's own rule, not `.settings-btn`'s); DD7 corrected to one line of headroom. Confirmed sound: the DD2 id list is verbatim-complete against the controller; the asset reuse is CSP-safe (`img-src 'self'`); DD6's anti-white-flash citation; DD8 matches the HAT rules and ~40 prior HAT legs; leg 1 is low-risk by the tiering. Suggestion carried as a follow-up: add the welcome surface to `scripts/a11y-audit.mjs`'s audited states after the HAT settles the design.

**2026-08-25 — Flight design review, second pass (scoped to the rewrite): approve with changes → applied.** All four rewritten rows traced TRUE at the value level (row 2 same-tab attach with `searchEngine` untouched; row 3 `openNewTab` → `createTab(home, container)`, plain tab; row 4 `toUrl` null → `handoffSearch` → `openWelcomeTab({reasons:['search'], pendingQuery})` beside the untouched tab, heading string exact, no `attachView`; row 5 `submitEngine` → `attachView(tab, buildSearchUrl(…))` on the same tab). **[medium]** row 4's "tab count +1 … diff the full tab list" conflated two apparatuses that disagree: `enumerateTabs` filters `wcId !== number` so the viewless welcome tab is absent until it attaches — the row now says strip count +1 from the chrome DOM and, separately, no new `enumerateTabs` entry. Confirmed: DD1's AT-safe constraint is correct for Chromium's tree; the radiogroup addition cannot regress any spec's a11y read; DD3's margin note and DD7's budget figure are accurate. Flight status → `ready` on operator approval.

**2026-08-25 — Flight open.** Crew file `leg-execution.md` validated earlier this session (Crew/Interaction Protocol/Prompts present). Flight status `ready` → `in-flight`. Branch `flight/03-welcome-branding` from `main` at `bb053d3` (planning commit `bcbc0d7`); squawk turnaround PR #169 (crew-file apparatus notes) is open, not yet merged — rebase before the leg-1 behavior runs if it lands first. 2 legs (1 autonomous, 1 HAT), 0 completed.

**2026-08-25 — Leg 1 `first-pass-visual-design` designed.** Risk tier: **LOW** — additive CSS and wrapper DOM on one surface, one CSS variant plus two class attributes in Settings, structural tests, a CLAUDE.md note. No shared interface (the controller's deps and API are untouched), no schema, no lifecycle or cache change (DD2 freezes the DOM contract and `render`/`settle`/`show` are not modified), no security surface, nothing reversed from Flight 2. Per-leg design review skipped; the flight-end Reviewer covers the code, and the acceptance gate is three FD-run behavior specs on the restyled build. Citations audited against `bb053d3`. Leg status → `ready`. `[HANDOFF:review-needed]`

**2026-08-25 — Leg 1 `first-pass-visual-design` implemented.** Leg status `ready` → `in-flight` → `landed`.

Files touched (code + tests; leg/flight artifacts and CLAUDE.md separate):
- `src/renderer/chrome/welcome-controller.js` — additive wrapper DOM only, in the build section: a `.welcome-column` wrapper; a `header.welcome-brand` (`img.welcome-mark` + the existing `#welcome-heading`, moved inside it unchanged + a new `p.welcome-tagline`); `#welcome-burner-note` moved directly under the brand header, class changed from `muted hidden` to `welcome-notice hidden` (id and text unchanged — the single duplicate burner-note block that existed in the pre-restyle source was removed, keeping one `burnerNote` reference, the same one `render()` toggles); `#welcome-home-block` and `#welcome-engine-block` each gained `.welcome-card`; `#welcome-engine-options` gained `className = 'welcome-engine-grid'`, `role="radiogroup"`, `aria-labelledby="welcome-engine-heading"`. `render`/`settle`/`show`/`unsetReasons`/`submitHome`/`submitEngine` and the radio `change` listeners are byte-identical to before this leg.
- `src/renderer/styles.css` — the placeholder welcome block (`#welcome-surface` … `#welcome-engine-status`, ~65 lines) replaced wholesale with the branded block (DD1/DD4/DD6): centered `.welcome-column` (max-width 600px), `.welcome-brand`/`.welcome-mark`/`.welcome-tagline`, `.welcome-notice` (accent-muted left border), `.welcome-card` (bg-2, border, radius 10), `.welcome-engine-grid` (2-col, collapsing to 1 col under 560px via `@media (max-width: 560px)`), `.welcome-engine-row` as a CSS-grid radio-card (radio in column 1 spanning both rows, label/description in column 2 — no absolute positioning, radio fully visible and in normal flow), selected/focus state via `:has(input:checked)` / `:has(input:focus-visible)`. `#welcome-engine-heading` gained `overflow-wrap: anywhere` for the pending-query edge case.
- `src/renderer/pages/settings.css` — added `.settings-btn--secondary` (transparent fill, `1px solid var(--border)`, `--fg` text, `--bg-2` hover — inherits radius/padding/font/focus-ring from `.settings-btn`) and `#home-page-clear { margin-left: 8px }`.
- `src/renderer/pages/settings.html` — `class="settings-btn settings-btn--secondary"` added to `#home-page-clear` and `#search-engine-clear`; no other change (settings.js does no class manipulation on either button — verified by grep).
- `test/unit/search-engines.test.js` — new test: DOM-contract structural test (DD2) asserting all ten contract ids, the `welcome-engine-` radio id prefix, the `welcome-engine-row` class, the radiogroup role + aria-labelledby, and ≥3 `classList.toggle('hidden'` calls inside `render(tab)`.
- `test/unit/settings-page-shared-scripts.test.js` — new test: both Clear buttons carry `settings-btn settings-btn--secondary`; `settings.css` defines `.settings-btn--secondary` and `#home-page-clear`'s `margin-left: 8px`.
- `CLAUDE.md` — welcome-surface note (line ~70) extended with the DOM-contract-is-frozen rule, the full contract id list, and a pointer to the structural test.

Gates (before → after this leg's diff, `node --test` over `test/unit/**`):
- `npm test`: **3763 → 3765** tests, all passing. Wall-clock: baseline `time npm test` real **0m3.374s**; after, `time timeout 120 npm test` real **0m3.564s**.
- `npm run typecheck` — clean, no output.
- `npm run lint` — clean, no output.
- `git diff --stat src/renderer/renderer.js` — empty (untouched), confirmed.
- `grep -nE 'display:\s*none|visibility:\s*hidden' src/renderer/styles.css` — all hits are pre-existing rules unrelated to the welcome block (`webview.hidden`, `.tab-fav`, panel/menu toggles, etc.); the two hits inside the welcome block (`#welcome-surface.hidden`) target the whole panel container, never `input[type=radio]`. No radio is hidden by `display`/`visibility` anywhere in the file.

Hand-neuter check: temporarily replaced `engineStatus.id = 'welcome-engine-status';` (line 120) with a comment, ran `node --test test/unit/search-engines.test.js` → **1 test failed** (the new DOM-contract test, exactly as expected — `pass 32 / fail 1` of 33), confirming the test is load-bearing. Restored the line; re-ran → **33/33 passing** again.

Electron/Chromium and `:has()`: bundled `electron@^43.2.0` (installed `43.2.0`, Node 22.22.0 per `process.versions`) — Chromium in this Electron line supports `:has()` (spec'd since Chromium 105; Electron 43 ships a much newer Chromium). Used `:has(input:checked)` / `:has(input:focus-visible)` directly for the radio-card selected/focus state — no `input:checked + label` fallback needed.

Visual smoke: launched `XDG_CONFIG_HOME=<scratch dir> npm run dev` (Wayland/WSLg, `--enable-logging`) for ~8s on a fresh profile, confirmed the process tree came up cleanly (main + zygotes + gpu-process + renderer, only the expected Wayland/DRM warnings — no application errors, no welcome-controller/CSS parse errors in the log), then sent `SIGTERM` to the main Electron PID and removed the scratch profile — no process left running (`ps aux` confirmed clean). `captureWindow` was not used (no automation surface attached this run). Reading the CSS just written: a fresh both-unset profile should render a centered ~600px column under the toolbar — the gold Goldfinch mark and "Welcome to Goldfinch" heading, the tagline beneath, then two `--bg-2` cards with `--border` outlines (Home page card with its input/Set, and the Search engine card with the eight engines as a 2-column grid of `--bg-3` radio-cards, each with a visible native radio in the first grid column and bold label + muted description alongside) — no burner notice on a non-burner tab. The judgment of how this actually looks is left to the HAT (leg 2) per DD8.

Deviation from the leg spec (minor, no contract or behavior impact): guidance suggested visually de-emphasizing the radio via `opacity` + absolute positioning ("the radio may be visually de-emphasized with opacity and positioned, never display:none"). Implemented instead as a 2-column CSS grid per row (`grid-template-columns: auto 1fr`) with the radio in column 1 at full opacity, label/description in column 2 spanning two rows — same AT-safe outcome (radio fully in the accessibility tree, real `<label for>` association unchanged) without absolute-positioning fragility; de-emphasizing via opacity was optional per the guidance's "may."

Implementation note: moving the burner note relocates its single `const burnerNote = …` declaration from after the home block to directly under the new brand header (the original source had exactly one declaration, matching the leg's citation) — confirmed post-edit that only one `#welcome-burner-note` node is ever created and it is the same reference `render()` toggles (grep + read).

No divert conditions were hit — no DD2 contract id was renamed or removed.

**2026-08-25 — Leg 1 behavior acceptance, spec 1: `welcome-home-first` — PASS, 5/5 (first run; spec `draft` → `active`).** Run log: `tests/behavior/welcome-home-first/runs/2026-08-25-20-08-57.md`. On a fresh profile, setting the home page first attaches that same tab immediately with `searchEngine` left `null`; the next Ctrl+T is a plain home-page tab (no re-offer — the value-level trace from design review confirmed live); a typed search hands off to a new welcome tab carrying the query with no provider URL committed (chrome-strip count +1, `enumerateTabs` unchanged); choosing DuckDuckGo runs the search in that same tab and the row records both choices. Every row read the surface through the frozen DOM contract; the Validator confirmed no verdict depended on appearance. Spec nit fixed at the gate: row 4 now carries `[mixed-frame]`.

**2026-08-25 — Leg 1 behavior acceptance, spec 2: `welcome-first-launch` re-run — PASS, 7/7.** Run log: `tests/behavior/welcome-first-launch/runs/2026-08-25-20-20-29.md`. The first-launch outcome is unchanged by the restyle: both-unset welcome tab, explicit `null`/`null` row, bookmarks bar present on the surface, both hints in Settings, engine-first then home-page choices each writing only their own key. Spec notes for the next revision (not this flight): the empty bookmarks bar has no visual signature on the dark background (pre-seed a bookmark); rows 2 and 7 are `[mixed-frame]`-tagged but filesystem-only.

**2026-08-25 — Leg 1 behavior acceptance, spec 3: `welcome-home-routing` re-run — PASS, 10/10.** Run log: `tests/behavior/welcome-home-routing/runs/2026-08-25-20-38-40.md`. Zero functional regressions from the restyle across every new-tab path, the burner notice (DD4 observed: the note renders as a `welcome-notice` block with the app-wide text on a burner welcome tab), same-tab navigation by address and by bookmark, the cross-window broadcast (DD7 auto-attach observed again at step 8), and a real quit/relaunch (PID-attested; the on-disk session held exactly the six web tabs, no welcome record). New apparatus facts for the crew file (squawk 0012 follow-up): the welcome blocks hide by a `hidden` CSS class — the `.hidden` IDL property is a false signal, assert on computed style; burner tabs carry a `.tab-jar` swatch titled "Burner (burner)"; bookmark items are `button.bm-item`; the bookmarks bar renders only while a web-class tab is active; `evaluate` on an internal guest is refused ("internal-session excluded"); re-activate a viewless welcome tab by clicking its strip rect; `enumerateWindows` may gain a `sheetWcId` field after an external-engine navigation; `pressKey` takes `Enter`, not `Return`.

**2026-08-25 — FD ruling: leg 1 lands.** All three acceptance specs pass on every checkpoint on the restyled build; the DOM contract held as the regression net exactly as DD2/DD5 intended (no verdict touched appearance, no row needed re-authoring). FD verification after the Developer's hand-off: see the Phase 2d entry below. Leg 1 status `landed` (set by the Developer; confirmed). Leg 2 is the HAT — no autonomous legs remain → Phase 2d (flight-end review and commit of leg 1) before the HAT session, so the operator walks a committed build.

**2026-08-25 — Phase 2d: flight-end review of leg 1, 1 cycle, `[HANDOFF:confirmed]`.** Reviewer (Sonnet, no knowledge of the Developer's reasoning) independently ran `npm test` **3765 pass / 0 fail** (`real 0m3.6s`), typecheck clean, lint clean; reproduced the DOM-contract neuter check with a *different* id (deleting `homeInput.id = 'welcome-home-input'` → exactly the contract test fails, 32/33; restored, diff identical); leak grep clean over the diff and the three run logs. Confirmed byte-unchanged `render`/`settle`/`show`/`unsetReasons`/`submitHome`/`submitEngine` and the `change` listeners, `renderer.js` untouched, additive DOM only, radios AT-visible, radiogroup semantics, DD6 layout, `:has()` on Electron 43, `overflow-wrap` on the query heading, no description truncation, the Clear variant and `#home-page-clear`'s own margin, no `settings.js` change. **Methodology note from the Reviewer**: commenting out an id assignment leaves the string literal in the comment and a grep-shape test still passes — neuter checks on structural tests must delete the line. Leg 1 → `completed`. One commit; draft PR opened with leg 2 unchecked (the HAT). Scratch profiles removed; no Electron process left running.

**2026-08-25 — Leg 2 `hat-welcome-alignment` designed (HAT leg — interactive).** Lightweight per the HAT protocol: eight operator-verified states (A first launch, B engine choice, C home-only, D search handoff, E burner, F Settings, G narrow window, H overall) on the operator's own `npm run dev` build; fixes inline under DD8 with the DD2 contract binding every fix; no autonomous Developer/Reviewer cycle. Leg status → `ready`. The session starts with state A.

**2026-08-25 — HAT state A: operator feedback and FD classification.** Operator: "looks pretty good" plus four asks. (1) Bigger logo → look-and-feel FIX (CSS, single surface). (2) White background for the welcome surface, "not like the settings screens" → look-and-feel FIX, single surface, but a scoped light palette is needed (the chrome tokens are dark-only) — amends DD1's "dark-only like the rest of the chrome" by operator decision; the original line stands as written in the spec, this entry is the live ruling. (3) Don't auto-hide the engine block on selection — "just let it be selected" → **FEATURE** (behavior change: `submitEngine` → `settle()` currently re-renders and hides the block once the engine is set; `welcome-first-launch` step 4 asserts the disappearance) → scoped design review before implementation per DD8; the spec row is re-authored with the change. (4) Tagline gains "You can always change these in Settings." → copy FIX. Fixes 1/2/4 spawned as one Developer; the item-3 design review spawned in parallel (read-only).

**2026-08-25 — HAT fix (state A, items 1/2/4): Developer applied, gates green.** CSS-and-copy only; no DOM hook, id, class, or controller function touched.

Files touched:
- `src/renderer/styles.css` — welcome block only:
  - `.welcome-mark`: `48px → 96px` (both `width`/`height`); `margin-bottom` `12px → 16px` for headroom under the doubled mark; `-webkit-user-drag: none; user-select: none` unchanged. Header structure (`header.welcome-brand` centered, mark above `#welcome-heading` above `.welcome-tagline`) untouched.
  - `#welcome-surface`: added a scoped light palette as custom properties local to the selector — `--wl-bg: #ffffff; --wl-fg: #1e1f25; --wl-fg-dim: #5c5f6b; --wl-card: #f6f6f8; --wl-border: #d9dbe3; --wl-accent: var(--accent); --wl-accent-strong: color-mix(in srgb, var(--accent) 65%, black)` — then `color: var(--wl-fg); background: var(--wl-bg)` (background stays set, never transparent — DD6's anti-white-flash rule is unaffected, only the color changed). `--wl-accent-strong` exists because raw `--accent` gold (#f5c518) computes to ~1.6:1 contrast against white/`--wl-card` — well under the 3:1 non-text-UI floor — so it is unusable as a border/focus-ring color on the light surface; the 65%-toward-black mix computes to ~3.3:1, used everywhere gold appears as a border or outline inside the surface (selected radio-card border, all three focus-visible rings). The Set button's fill stays plain `--wl-accent` (=`--accent`) with `color: var(--accent-fg)` — already dark-on-gold at ~10:1, untouched, per the operator's "brand gold stays the accent."
  - Every other welcome rule remapped from the dark tokens to the local ones: `#welcome-surface .muted`, `.welcome-tagline` → `--wl-fg-dim` (~6.4:1 on white); `.welcome-notice` → `--wl-card` bg, `--wl-fg` text, `--wl-accent-strong` left border; `.welcome-card`, `.welcome-engine-row` → `--wl-card`/`--wl-border` and `--wl-bg`/`--wl-border` respectively (kept the original nesting relationship — engine-row reads lighter than its containing card, same direction as the old bg-2/bg-3 pair); `#welcome-home-input` → `--wl-bg`/`--wl-fg`/`--wl-border`, focus-visible → `--wl-accent-strong`; `#welcome-home-set` → `--wl-accent` fill (text unchanged); `.welcome-engine-row:has(input:checked)` border and `:has(input:focus-visible)` outline → `--wl-accent-strong`. `#welcome-home-status`/`#welcome-engine-status` carry no explicit color and inherit `--wl-fg` from `#welcome-surface` automatically. Nothing outside `#welcome-surface` was touched — verified by re-reading the full diff hunk list against the selector names.
- `src/renderer/chrome/welcome-controller.js` — one line: `tagline.textContent` now reads "Set up the two things Goldfinch won't guess for you. You can always change these in Settings." No engine name appears (checked against the no-duplication structural test's constraint). Build order, wrapper DOM, and every id/class/listener untouched.

Contrast spot-checks (WCAG formula, computed by hand against the final hex/mix values): body text `--wl-fg` on `--wl-bg` ≈ 15:1; muted text `--wl-fg-dim` on `--wl-bg` ≈ 6.4:1 (both clear the 4.5:1 floor); `--wl-accent-strong` (~`#5c4a09`) on white/`--wl-card` ≈ 3.3:1 (clears the 3:1 non-text floor for the selected-card border and focus rings); Set button text (`--accent-fg` on `--wl-accent`) ≈ 10:1.

Gates (before → after this fix, on top of leg 1's landed state):
- `npm test`: **3765 → 3765**, all passing, 0 failed (unchanged — no DOM hook or controller line touched). Wall-clock: `time timeout 120 npm test` real **0m3.504s**.
- `npm run typecheck`: clean, no output. Wall-clock **0m0.422s**.
- `npm run lint`: clean, no output. Wall-clock **0m3.502s**.

Not committed — this is an inline HAT-session fix; the operator reloads (`npm run dev`, fresh chrome relaunch for the CSS to take on the welcome tab) and re-checks state A before the FD commits at the end of the session.

**2026-08-25 — HAT item 3 design review (scoped, DD8 feature gate): 1 cycle, needs rework → fixed; cycle 2 skipped (every fix adopts the reviewer's own recommendation).** Proposed rule: `render` gates the engine block on `tab.welcome.reasons.has('search')` (why the tab was opened), not the live unset state. **[critical]** `submitEngine` already does `tab.welcome.reasons.delete('search')` before `settle(tab)` (a workaround for the `searchEngineCache` staleness window), so the rule would hide the block on the operator's own case (both-unset tab picks an engine). **[high]** syncing radio `.checked` from `currentSearchEngine()` inside the synchronous re-render races the same cache and can un-check the radio just clicked. Fix for both: stop mutating `reasons`; thread an override — `settle(tab, { search: id })` → `unsetReasons(tab, opts)` / `render(tab, opts)` read `'search' in opts ? opts.search : currentSearchEngine()`; `submitEngine` passes the chosen id; broadcast/show paths pass nothing. **[medium]** confirmation-text lifetime: FD ruling — state-derived in `render` (shown whenever the block is visible and an engine resolves), so tab switches and unrelated broadcasts don't wipe it. Confirmed moot: the "engine-only tab, no pending query" trade-off is unreachable today (every `search`-only record is created with a `pendingQuery`) — documented with a comment, not built. DD2 impact: none (no id/class/toggle change); structural tests updated to pin the new shape (`render` reads `reasons.has('search')`; `submitEngine` no longer contains `reasons.delete('search')`; the sync uses `.checked =` and never `.click(`). Spec rows affected: `welcome-first-launch` step 4 only (re-authored: the engine block stays with Brave selected). Risk: stays a leg-2 inline change.

**2026-08-25 — HAT feature (state A, item 3) applied.** Developer spawn implemented the design-reviewed rule in `src/renderer/chrome/welcome-controller.js`: choosing a search engine no longer hides the engine block on the welcome surface — it stays, showing the selection, with a confirmation line.

Files touched:
- `src/renderer/chrome/welcome-controller.js`:
  - `unsetReasons(tab, opts = {})` — `'search'` is now unset iff `reasons.has('search') && engine == null`, where `engine = 'search' in opts ? opts.search : currentSearchEngine()`; `home` unchanged.
  - `render(tab, opts = {})` — the engine block's visibility is now `tab.welcome && tab.welcome.reasons.has('search')` (why the tab was opened), not the live unset state; the home block keeps `unsetReasons(tab, opts).has('home')`. When shown, the radios sync to the resolved `engine` via `radio.checked = (radio.value === engine)` over a new `engineRadios` array collected at build time (never `.click()`). `#welcome-engine-status` is now state-derived: "Saved — you can always change this in Settings." when the block is shown and `engine != null`, cleared otherwise. Pending-query heading logic unchanged.
  - `settle(tab, opts = {})` — `if (unsetReasons(tab, opts).size > 0) { render(tab, opts); return; }`; the attach decisions are otherwise unchanged but resolve the same `engine` override for `buildSearchUrl`.
  - `submitEngine(id)` — no longer mutates `reasons` (the `tab.welcome.reasons.delete('search')` line is gone); on success it calls `settle(tab, { search: id })` instead. The pending-query branch is unchanged (it already passed `id` directly). Added a comment noting a `search`-only record is always created with a `pendingQuery` today (`navigation-controller.js`'s `handoffSearch`, the context-menu handoff), so "engine-only, no query" is unreachable — no defensive branch built.
  - `show(tab)` and the `onSettingsChanged` handler still call `settle(tab)` with no override (live cache) — unchanged.
  - No DOM-contract id, class hook, or `.hidden` toggle changed (`#welcome-engine-status` already existed); `tab-controller.js`, `navigation-controller.js`, and `renderer.js` untouched (confirmed via `git diff --stat`).
- `test/unit/search-engines.test.js` — the two existing structural regexes for `render(tab)`/`settle(tab)` were widened to `render(tab, ...)`/`settle(tab, ...)` (signature now takes `opts`); one existing assertion (`settle(tab)` calling `render(tab)`) widened to `render(tab` for the same reason. Four new structural tests added: (a) `render(tab)` gates the engine block on `reasons.has('search')`, not the toggle line reading `unset.has('search')`; (b) `submitEngine` no longer contains `reasons.delete('search')` and calls `settle(tab, { search: id })`; (c) the radio sync assigns `.checked =` directly and the file (comments stripped) never calls `.click(`; (d) `render(tab)`'s body contains the "Saved — you can always change this in Settings." confirmation string. Hand-neuter check: reintroduced `tab.welcome.reasons.delete('search')` + bare `settle(tab)` in `submitEngine` → exactly 1 new failure (36 pass / 1 fail of 37 in the file), confirming test (b) is load-bearing; restored, back to 37/37.
- `tests/behavior/welcome-first-launch.md` — step 4's Expected Results re-authored: "The engine block stays, showing Brave Search selected, with a saved confirmation beneath it; the home page block remains; the tab stays a welcome tab. A subsequent Ctrl+T opens another welcome tab with only the home page block." A one-line note added under Intent flagging the row changed at M16 F3 leg 2 (operator decision at the HAT). No other spec row touched.

Gates (before → after this fix, on top of the state-A CSS/copy fix):
- `npm test`: **3765 → 3769** (4 new structural tests), all passing, 0 failed. Wall-clock: `time timeout 120 npm test` real **0m3.504s → 0m3.441s**.
- `npm run typecheck`: clean, no output both before and after. Wall-clock **0m0.422s → 0m0.402s**.
- `npm run lint`: clean, no output both before and after. Wall-clock **0m3.502s → 0m3.265s**.

Not committed — this is an inline HAT-session feature; the operator reloads (`npm run dev`, fresh chrome relaunch) and re-checks the engine-selection behavior before the FD commits at the end of the session.

**2026-08-25 — HAT state A, item 5: operator feedback and FD classification.** Operator: the welcome home-page input requires `https://` — a bare `example.com` is rejected ("Enter a valid address."); "either mask that in or not require it". Cause: `submitHome` sends the raw text to `chrome-welcome-set`, whose validator (`settings-store.js` `VALIDATORS.homePage` → `isSafeTabUrl`) requires a scheme; the address bar already normalizes a bare domain via `navigation-controller.js` `toUrl`'s domain rule (`https://` prepended). Classification: input normalization — behavior on **two surfaces** (the welcome card and Settings' home-page Save must agree) → multi-surface trigger → lightweight Developer design-review pass before the implementing spawn (DD8). Proposal under review: a shared `normalizeHomePageInput(s)` in `src/shared/search-engines.js` (already served to Settings and imported by the chrome) applying `toUrl`'s domain rule; `submitHome` and Settings' Save normalize before writing; the store validator unchanged (still the gate); `toUrl` reuses the helper for its domain branch so the rule has one source.

**2026-08-25 — HAT state A: operator sign-off on items 1–4.** On the clean relaunch with the bigger mark, the white surface, the extended tagline, and the engine block staying visible with the selection and its confirmation line: "everything else looks good" — satisfied, pending item 5 (bare-domain home-page input), whose design review is in progress.

**2026-08-25 — HAT item 5 design review (multi-surface fix gate): 1 cycle, approve with changes → applied.** Traced through the proposed pipeline: `example.com` → `https://example.com` accepted; `example.com/path` accepted; `192.168.1.1` accepted (`isSafeTabUrl` is scheme-only); `localhost` still rejected (the domain rule needs a dot — a pre-existing gap shared with the address bar, out of this leg's scope, stated not changed); `about:blank` still rejected by the validator; `foo bar` still rejected. **[high]** `toUrl`'s first line carries an `about:` carve-out the helper deliberately lacks — the reuse must replace only the domain-regex line, never the scheme/`about:` guard, or a typed `about:blank` would fall through to a search; FD ruling: reuse proceeds with that constraint stated. **[medium]** Settings' Save sends `input.value` untrimmed today; routing it through the helper (which trims) is a deliberate consistency fix — written as an explicit AC. **[low]** reflecting the normalized value into the welcome input before the write is dead weight (`attachView` navigates immediately) — dropped; Settings reflects for free via the broadcast-before-resolve ordering. Confirmed: both Save buttons are `type=button` with click handlers (no native `type=url` gate before JS); no new mutation wrapper (squawk 0003's net unaffected); no spec row changes (every spec types a scheme'd URL); DD2 untouched. FD rulings on the reviewer's questions: reuse in `toUrl` yes (guard preserved); trim as an explicit AC yes; no new spec row — the HAT verifies the bare-domain case live.

**2026-08-25 — HAT fix (state A, item 5) applied.** A bare domain typed into a home-page field (`example.com`) now normalizes to `https://example.com` on both the welcome surface and Settings, per the design review above; the store validator is unchanged and stays the actual gate.

Files touched:
- `src/shared/search-engines.js` — new export `normalizeHomePageInput(input)`: trims, then leaves an empty string or an already-schemed value unchanged, prepends `https://` to a bare-domain-looking value (has a dot, no whitespace), else returns it unchanged. Doc comment states why it lives here (reuses this module's existing internal-page route and the chrome's existing import of it, not a topical fit) and that the rule is shared with `navigation-controller.js`'s `toUrl`.
- `src/renderer/chrome/navigation-controller.js` `toUrl` — the first line (the scheme/`about:` guard) is untouched byte-for-byte; only the domain-regex line was replaced with a call to `normalizeHomePageInput`, keeping the guard ahead of it so `about:blank` still returns unchanged rather than falling through to the search branch (the design review's [high] finding, now also pinned by a positive behavioral test — see below).
- `src/renderer/chrome/welcome-controller.js` `submitHome` — now computes `const value = normalizeHomePageInput(homeInput.value)` instead of a bare `.trim()`; no reflection into `homeInput`; `attachView(tab, value)` and the `welcomeSetPreference` write both use the normalized value. Error copy unchanged.
- `src/renderer/pages/settings.js` — the home-page Save handler now sends `normalizeHomePageInput(input.value)` instead of raw `input.value` (this also trims, an intended behavior change per the design review's [medium] finding). No reflection code added — the existing broadcast-before-resolve ordering already shows the persisted (normalized) value. Error copy unchanged.
- `src/renderer/renderer.js` — `normalizeHomePageInput` added to the `../shared/search-engines.js` import and threaded into both the `createNavigationController` and `createWelcomeController` deps objects.
- `test/unit/search-engines.test.js` — 10 new unit tests for `normalizeHomePageInput` (bare domain, domain+path, trimming, already-schemed http/https, `about:blank`, `localhost` documented gap, free text with spaces, empty string, null/undefined) plus 3 new structural (grep-shape) tests: `submitHome` calls `normalizeHomePageInput(`; the Settings home-page Save handler calls it; `toUrl` still contains `s.startsWith('about:')` on its guard line AND calls `normalizeHomePageInput(`.
- `test/unit/navigation-controller.test.js` — `normalizeHomePageInput` (the real function) added to the harness's imports and deps object (matching the `buildSearchUrl`/`capPendingQuery` precedent already there); new positive behavioral test asserting `toUrl('about:blank')` returns `'about:blank'` unchanged (there was none before this leg).

Dep-wiring choice: both `navigation-controller.js` and `welcome-controller.js` receive `normalizeHomePageInput` via their existing deps objects (the `buildSearchUrl`/`SEARCH_ENGINES` precedent), wired at their `renderer.js` call sites by extending the existing destructuring lines rather than adding new ones — `renderer.js` stayed at 1649 lines (its pre-existing count), 1 under the 1650-line `RENDERER_LINE_BUDGET`, so the "import directly instead" fallback was not needed for either controller.

Gates (before → after this fix, on top of the state-A engine-block feature fix):
- `npm test`: **3769 → 3783** (14 new tests: 10 unit + 3 structural + 1 behavioral), all passing, 0 failed. Wall-clock: `time timeout 120 npm test` real **0m3.441s → 0m3.490s**.
- `npm run typecheck`: clean, no output, both before and after. Wall-clock **0m0.382s**.
- `npm run lint`: clean, no output, both before and after. Wall-clock **0m3.178s**.

Not committed — this is an inline HAT-session fix; the operator reloads (`npm run dev`, fresh chrome relaunch) and re-checks the bare-domain case on both surfaces before the FD commits at the end of the session.

**2026-08-25 — HAT state A: operator sign-off, item 5 verified live.** On the clean relaunch the operator typed a bare domain into the welcome home-page card and it saved as `https://…` and attached: "pass". State A complete (items 1–5). Next: state B.

**2026-08-25 — Leg 2 status `ready` → `in-flight`** (the HAT session began at state A; status flipped late — recorded here rather than back-dated). Note on the leg's Context line "controller logic … is never touched": superseded for item 3 by its scoped design review (DD8's feature gate), which is the sanctioned path for a behavior change during the HAT; the leg artifact is left as written (immutable once in-flight) and this entry is the live ruling.

**2026-08-25 — HAT state B: operator sign-off.** Focus ring on Tab, selected-card border and the confirmation line on pick, descriptions readable: "pass". Next: state C.

**2026-08-25 — HAT state C: operator sign-off.** Engine set, home page cleared in Settings, Ctrl+T → welcome surface with the home card alone, centered: "pass". Next: state D.

**2026-08-25 — HAT state D: operator feedback and FD classification.** The search-only (and home-only) welcome shows the tagline "Set up the two things…", which reads wrong with one card; operator: reword for all three cases or make it dynamic. Classification: copy FIX, single surface; implemented as dynamic copy chosen in `render` from the blocks it is already toggling (copy only — no hook, decision, or toggle change, so DD2's contract is untouched and no spec row reads the tagline). Copy: both → "Set up the two things Goldfinch won't guess for you. You can always change these in Settings."; home only → "Choose where new tabs open. You can always change this in Settings."; search only → "Choose where your searches go. You can always change this in Settings." Developer spawned.

**2026-08-25 — HAT fix (state D) applied.** The welcome tagline is now dynamic, assigned in `render(tab, opts)` from the same `showHome`/`showEngine` booleans it already computes for the two block `classList.toggle('hidden', ...)` calls — no recomputation, no new decision. Copy only; no DOM hook, id, class, `.hidden` toggle, or any of `settle`/`show`/`submitHome`/`submitEngine` touched.

Files touched:
- `src/renderer/chrome/welcome-controller.js` — the tagline element built in the build section keeps its static initial text (`"Set up the two things Goldfinch won't guess for you. You can always change these in Settings."`) and a doc comment noting `render` reassigns it. `render(tab, opts)` now names `unset.has('home')` as `showHome` (previously inlined directly into the toggle call — same value, just captured) and, after computing `showEngine` for the engine block's own toggle, assigns `tagline.textContent`: both `showHome && showEngine` → the existing "two things" copy; `showHome` alone → "Choose where new tabs open. You can always change this in Settings."; `showEngine` alone → "Choose where your searches go. You can always change this in Settings."; neither (not expected to render) → falls back to the "two things" copy. No engine name or id appears in any of the three strings.
- `test/unit/search-engines.test.js` — two new grep-shape structural tests: `render(tab)` contains a `tagline.textContent = ` assignment; the controller source contains all three tagline copy strings verbatim.

Gates (before → after this fix, on top of the state-A item-5 fix):
- `npm test`: **3783 → 3785** (2 new structural tests), all passing, 0 failed. Wall-clock: `time timeout 120 npm test` real **0m3.536s → 0m3.388s**.
- `npm run typecheck`: clean, no output, both before and after. Wall-clock **0m0.384s → 0m0.376s**.
- `npm run lint`: clean, no output, both before and after. Wall-clock **0m3.189s → 0m3.208s**.

Not committed — this is an inline HAT-session fix; the operator reloads (`npm run dev`, fresh chrome relaunch) and re-checks state D (search-only and home-only welcome tabs) before the FD commits at the end of the session.

**2026-08-25 — HAT state D: operator sign-off (dynamic tagline verified: "works"); item 6 raised — FD classification: FEATURE, DD7 pivot.** Operator: clicking **Set** on the welcome home card navigates the tab immediately, which is jarring; the home page should apply on the next tab. This reverses Flight 2 DD7's "setting a home page is itself a navigation" (and leg 1's AC "on success, attaches the tab to that URL") and changes three spec rows (`welcome-home-routing` 8, `welcome-first-launch` 6, `welcome-home-first` 2). Per the agentic-workflow's mid-execution scope-change rule: the Flight 2 artifacts keep their original framing; this entry (and the scoped review below) is the live spec. Proposed redesign, symmetric with item 3: the welcome surface never navigates on its own — `submitHome` saves and re-renders (home card stays with the saved value reflected and a "Saved — new tabs will open here." line); `settle` no longer attaches to the home page when nothing is unset (the DD7 cross-window auto-attach goes with it — same jarring class); the only auto-navigation left is running a pending search after an engine is chosen. Scoped design review spawned before implementation (DD8).

**2026-08-25 — HAT state E: operator sign-off.** Burner welcome tab: the app-wide notice sits prominently above the card and reads as a deliberate notice: "pass". Next: F (Settings) while item 6's design review runs.

**2026-08-25 — HAT item 6 design review (DD7 pivot): 1 cycle, approve with changes → applied.** Confirmed at the value level: the `opts.home` override is required (same `homePageCache` round-trip staleness as item 3); `settle`'s dead-end `hide()` fallback becomes unreachable (reasons are never empty; both blocks reason-driven) — delete it; no module outside `welcome-controller.js` depends on the cross-window auto-attach. **[high]** `new-tab-default-routing`'s fixture relied on the auto-attach ("return to the boot tab — it attaches") and would leave zero web tabs for step 1 → FD ruling: fixture re-authored to navigate the boot welcome tab by typing the home address in its bar (manual navigation, still supported; keeps the "boot tab" identity). **[medium]** reflecting the saved value into the home input on every render can clobber in-progress edits → skip the assignment while `homeInput` has focus. **[medium]** `welcome-home-first`'s Intent narrative (attach-immediately, choice abandoned) is rewritten, not just its row. **[medium]** `welcome-home-routing` step 9 gains a clause that window 2's original welcome tab stays a welcome tab (baseline change vs the earlier run logs). **[low]** stale comments on the fallback removed. Suggestion adopted: `unsetReasons` deleted outright (no callers). Rulings on the questions: the "wiped by an unrelated broadcast" trade-off is accepted for the home card's status/error symmetrically with the engine card's; the Developer re-verifies `welcome-search-handoff`'s fixture for the same shape. Acceptance gate for the pivot: re-run `welcome-home-routing`, `welcome-first-launch`, `welcome-home-first`, and `new-tab-default-routing` at the end of the HAT session. Consequence stated to the operator: the cross-window auto-jump goes away with it.

**2026-08-25 — HAT state F: operator sign-off.** Settings: Clear buttons read as secondary beside Save; hints and status lines intentional; trimmed Save verified: "pass". Next: G (narrow window) once item 6's relaunch is up.

**2026-08-25 — HAT state G: premise correction.** The operator could not shrink the window below a point: `src/main/window-factory.js` sets `minWidth: 900, minHeight: 600` on every window (by design — not a chrome bug). The leg-1 single-column breakpoint (`@media (max-width: 560px)`) is therefore unreachable by window resizing — dead CSS at window level. The reachable narrow case is the minimum width with the media panel open (`--panel-w: 360px` → ~540 px content width), where the viewport-based media query still yields two columns in a ~540 px column. G re-posed to the operator as that case; the breakpoint is either re-based on the content width (a container query on `#welcome-surface`, Chromium supports `@container`) or removed as dead, per the operator's verdict.

**2026-08-25 — HAT state G: operator verdict → fix.** Even with the media panel open the grid stayed two columns (the viewport query never fires under `minWidth: 900`). FD ruling: look-and-feel FIX, `styles.css` only — `#welcome-surface` becomes a size container (`container-type: inline-size`) and the grid's collapse rule becomes `@container (max-width: 600px)`, so the surface's own width (≈540 px at the window minimum with the media panel open) drives the single column; the dead viewport query is removed. Developer spawned (concurrent with item 6's Developer — disjoint files).

**2026-08-25 — HAT feature (item 6) applied.** Developer spawn implemented the design-reviewed DD7 pivot in `src/renderer/chrome/welcome-controller.js`: `submitHome` no longer calls `attachView` on success — it calls `settle(tab, { home: value })` instead, saving and staying on the tab. `render(tab, opts)` now gates the home block's visibility on `tab.welcome.reasons.has('home')`, symmetric with the engine block (M16 F3 Leg 2, HAT item 3) — not on whether the preference is still unset — and, when shown, reflects the resolved home page into `homeInput.value` (skipped while `document.activeElement === homeInput`, so an external write never clobbers in-progress typing) with `#welcome-home-status` set to "Saved — new tabs will open here." when a value is resolved, cleared otherwise. `settle(tab, opts)` is rewritten to a single check — a pending query with a resolved engine attaches to the search URL; every other case re-renders — removing the old "attach to the home page once nothing is missing" branch and its dead-end `hide()` + `els.address.focus()` fallback. `unsetReasons` is deleted outright (no callers survive); its two override resolutions (`'search' in opts ? opts.search : currentSearchEngine()`, `'home' in opts ? opts.home : currentHomePage()`) are inlined at their two read sites in `render`/`settle`. `show`, `onSettingsChanged`, and `submitEngine` are unchanged in behavior (still call `settle`); their doc comments, and `render`'s, `settle`'s, and the header's, are updated to describe the new shape and note that a welcome tab shown in another window now re-renders with the saved value instead of auto-attaching (Flight 2 DD7's cross-window auto-attach retired at this leg). DD2 impact: none — no id, class, or `.hidden` toggle target changed; `renderer.js`, `tab-controller.js`, and `navigation-controller.js` untouched. Tests (`test/unit/search-engines.test.js`): the show→settle test's prose (mentioned the old defensive fallback) is corrected to describe the pending-search-only exception; five tests added — home visibility keys off `reasons.has('home')`; `submitHome` calls `settle(tab, { home: value })` and never `attachView`; `render` sets `#welcome-home-status` to "Saved — new tabs will open here."; `settle`'s body contains no `currentHomePage()` read and `attachView(` appears exactly once, gated on `query != null && engine != null`; `unsetReasons` no longer appears anywhere in the file. `test/unit/tab-controller.test.js` was grepped for a welcome-auto-attach-on-broadcast assertion — none exists, no change needed. Suite: 3785 → 3790 (+5, all new), 0 fail. Specs re-authored per the design review, each gaining an inline "changed at M16 F3 leg 2, HAT item 6" note: `welcome-home-routing` step 8 (Set now saves-and-stays, engine still DuckDuckGo) and step 9 (window 2's original welcome tab stays a welcome tab, re-rendering rather than auto-attaching) and step 10's restored-tab count (steps 6, 7, 9 only — the step-8 tab never attached, so it is not restored); `welcome-first-launch` step 6 (tab stays a welcome tab with the saved home page and Brave still selected); `welcome-home-first`'s Intent rewritten (both choices can be made on the same tab; a new tab never re-offers a made choice; the abandoned engine choice still returns only through the search handoff, on a later tab) and step 2 (same tab stays a welcome tab, engine card still offered), plus a new fixture-cleanliness note that the step-2 tab stays open in the background for the rest of the run; `new-tab-default-routing`'s fixture bullet re-authored to navigate the boot welcome tab via the address bar (manual navigation still attaches) rather than relying on the retired auto-attach. `welcome-search-handoff` was re-checked per the design review's ruling — every row already navigates via a typed address or a chosen engine, neither of which depended on the retired behavior — no change needed. Gates (before → after, same working tree): `npm test` 3785 → 3790 pass, 0 fail, `real 0m3.6s` → `real 0m3.3s`; `npm run typecheck` clean → clean (`real 0m0.4s`); `npm run lint` clean → clean (`real 0m3.3s`). Not committed per the session's instruction — left for the flight-end review.

**2026-08-25 — HAT fix (state G) applied.** `#welcome-surface` gained `container-type: inline-size` (its existing `position: absolute; inset: 0; overflow: auto` is unaffected — confirmed no layout/overflow change, still a plain absolutely-positioned scroll box, just now also a size query container). The dead `@media (max-width: 560px)` rule on `.welcome-engine-grid`/`.welcome-column` was replaced in place with `@container (max-width: 600px)` (same two declarations, unchanged). Threshold chosen as exactly 600px — `.welcome-column`'s own `max-width` — so it straddles the two reachable surface widths: with the media panel open at the window minimum the surface's available inline-size is ≈540px (900 window minWidth − 360 panel width), below 600, so the column (which would render at up to 600px) is narrower than that and the grid collapses to one column; with the panel closed the surface is the full window, minimum 900px, well above 600, so the grid stays two columns. No other rule touched.

Files touched: `src/renderer/styles.css` only (welcome block) — no JS, no DOM, no controller logic.

Gates:
- `timeout 120 npm test`: 3790 passing, 0 failed (count reflects concurrent in-flight work by another Developer on `navigation-controller.js`/`search-engines.js`/behavior specs; this leg's file, `styles.css`, has no dedicated unit tests and none were added — the change is a pure CSS breakpoint swap with no new hook/id/behavior to pin).
- `npm run lint`: clean, no output (ESLint scope is JS; no stylelint is configured in this project's `package.json`).
- `npm run typecheck`: clean, no output.

Not committed — per instruction, this session leaves the change uncommitted for the FD/operator to commit alongside the rest of the HAT session's work.

**2026-08-25 — HAT states G, H and item 6: operator sign-off — the HAT walkthrough is complete.** On the clean relaunch (3790/3790): Set stays put with the saved value and confirmation, Ctrl+T honors it (item 6); the grid collapses to one column at the window minimum with the media panel open (G, container query); overall first-run feel: "pass" (H). Every state A–H carries an operator sign-off. Acceptance gate for the pivot (per the item-6 review ruling): FD re-runs `welcome-home-first`, `welcome-first-launch`, `welcome-home-routing`, and `new-tab-default-routing` on this build; then Phase 2d review, commit, land.

**2026-08-26 — Spec premise correction before the gate run (value-level trace).** `welcome-first-launch` step 6 as re-authored for item 6 said "the engine card still shows Brave selected" — false: the Ctrl+T in that row opens a welcome record whose reasons are computed from the unset state (`welcomeReasons(null, 'brave')` → `{home}`), and the engine block is reason-driven since item 3, so that tab shows the home card only. Row corrected to say so before the run rather than discovering it as a FAIL.

**2026-08-26 — Leg 2 acceptance gate for the HAT's behavior changes: four FD-run specs, all PASS on the final build.** `welcome-home-first` **5/5** (`tests/behavior/welcome-home-first/runs/2026-08-26-02-00-25.md` — the re-authored row 2 pinned "Set saves and stays" with the record provably still viewless before and after); `welcome-first-launch` **7/7** (`welcome-first-launch/runs/2026-08-26-02-10-54.md` — rows 4 and 6 re-authored: the engine block stays with the selection; Set stays put; a value-level premise error in row 6's re-authored text was corrected before the run); `welcome-home-routing` **10/10** (`welcome-home-routing/runs/2026-08-26-02-29-57.md` — rows 8–10 re-authored; the retired cross-window auto-attach observed retired at step 8/9; a real relaunch restored exactly the four web tabs; the original Executor's transcript was lost after step 9 — third occurrence — and a replacement Executor briefed with the attested facts performed step 10); `new-tab-default-routing` **8/8** (`new-tab-default-routing/runs/2026-08-26-03-01-14.md` — the fixture re-authored for the pivot produced its "one boot tab in personal" premise; jar routing unchanged). Every verdict rested on rendered or API evidence; the Validators took their own observations at most rows. Apparatus facts for the crew file (squawk 0012 follow-up): the app hides welcome blocks by a `hidden` CSS class — the `.hidden` IDL property is a false signal; the bookmarks bar is app-wide and shows on every web-class or welcome tab (misread as a "stale title strip" once); rows that fold a follow-on action into the Expected Result cell should be split.

**2026-08-26 — FD ruling: leg 2 lands; the flight lands.** All eight HAT states carry operator sign-off; six inline changes (bigger mark; white surface with a scoped light palette; extended then dynamic tagline; engine block stays visible on selection; bare-domain home-page input normalized on both surfaces; Set saves and stays — the DD7 pivot; container-query grid collapse) each with structural tests, two of them through scoped design reviews, one through a lightweight multi-surface review; the four gate specs pass. Mission open question "Branding" resolved: a white, brand-marked welcome surface with the two choices as cards and a compact engine grid, coherent with the dark chrome by the gold accent. Phase 2d: FD verification + one flight-end Reviewer over the HAT diff before the commit (the HAT protocol commits on operator sign-off; the review is added because two of the six changes touched controller logic).

**2026-08-26 — Phase 2d: flight-end review of the HAT changes, 1 cycle, `[HANDOFF:confirmed]`.** Reviewer (Sonnet, no knowledge of the Developers' reasoning): `npm test` **3790 pass / 0 fail**, typecheck clean, lint clean; `renderer.js` 1649 / 1650; leak grep clean over the specs and run logs; the item-6 structural test reproduced red by reintroducing `attachView` in `submitHome` (3789/1), restored. Value-level traces confirmed the DD7 pivot's shape: only two `attachView` sites in the controller, both in `settle`'s pending-query branch; the `activeElement` guard present; `toUrl('about:blank')` unchanged and now positively tested; the DD2 id set intact; radios AT-visible; the light palette scoped to the surface; the container query in place of the dead viewport rule. **Correction (documentation, not code)**: the state-A fix entry's contrast arithmetic for the darkened gold was wrong — `color-mix(in srgb, var(--accent) 65%, black)` yields `#9f8010`, ≈3.8:1 on white and ≈3.5:1 on the card, not the `~#5c4a09` / "~3.3:1" recorded; both figures clear the 3:1 non-text floor, so the code stands. The Reviewer also noted the mission/flight checklists were still unchecked — they are the close-out below.

**2026-08-26 — Flight lands.** Leg 2 → `completed`; `flight.md` legs, checkpoints, criteria, and open questions checked; mission Branding question resolved; Flight 3 checked in the mission; flight status → `landed`. One commit on `flight/03-welcome-branding`; PR #170 marked ready. Scratch profiles removed; no Electron process running. `/flight-debrief` next. `[COMPLETE:flight]`
