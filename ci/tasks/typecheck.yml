---
# tsc --noEmit — mirrors the GitHub Actions "Type check" step (ci.yml).
platform: linux

image_resource:
  type: registry-image
  source:
    repository: node
    tag: "22"

inputs:
  - name: repo

run:
  path: bash
  args:
    - -ceu
    - |
      cd repo
      npm ci
      npm run typecheck
