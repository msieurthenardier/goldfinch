# Behavior Test Run: foreground-to-act — 2026-07-08-18-59-14

**Spec**: [tests/behavior/foreground-to-act.md](../../foreground-to-act.md)
**Status**: pass
**Started**: 2026-07-08T23:59:14Z
**Completed**: 2026-07-08T23:59:21Z
**Duration**: 00:00:07
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — screenshot pixels + MCP JSON payloads persisted)

## Summary
5 / 5 steps passed. 0 failed; 0 inconclusive.

Apparatus: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899`, jar key on the SDK transport. **Page substitution (within the spec's "two visually-distinct pages" allowance):** page **A = the served fixture** `http://127.0.0.1:8090/` (dark, orange heading, *and* editable — so Step 5's input is readDom-observable), page **B = example.org** (light "Example Domain"). The two are unmistakably distinct in a screenshot. Coordinate clicks use device pixels (CSS×DPR=1.25) per the finding in the sibling `mcp-drive-end-to-end` run.

## Step Results

### Step 1 — PASS
- **Actions taken**: `initialize`; `tools/list`.
- **Raw state**: all of `openTab, activateTab, enumerateTabs, captureScreenshot, click, typeText, readDom` present (`missing: []`).
- **Expected**: those tools present (presence-checked).
- **Verdict**: pass.
- **Evidence**: [step1.json](./2026-07-08-18-59-14/step1.json)

### Step 2 — PASS
- **Actions taken**: `openTab` fixture (A=11); `openTab` example.org (B=12, opened last).
- **Raw state**: both numeric wcIds; `enumerateTabs` → B `active:true`, A `active:false`.
- **Expected**: both non-null wcIds; B active, A not (A is the background target).
- **Verdict**: pass — background-target condition established.
- **Evidence**: [step2.json](./2026-07-08-18-59-14/step2.json)

### Step 3 — PASS
- **Actions taken**: with B foreground, `captureScreenshot(A)` (target the background tab); decoded PNG, judged pixels.
- **Raw state**: image len 109604. **Pixels show page A (the fixture): "MCP DRIVE END-TO-END FIXTURE" orange heading + the pristine ECHO/CLICK/KEY labels** — NOT example.org's "Example Domain", NOT blank/white.
- **Expected**: the screenshot shows page A's content, not blank, not page B — proving the engine foregrounded A before capturing (DD1 blank-capture/wrong-tab hazard defeated).
- **Verdict**: pass.
- **Evidence**: [step3-capture-A-background.png](./2026-07-08-18-59-14/step3-capture-A-background.png)

### Step 4 — PASS
- **Actions taken**: `enumerateTabs`.
- **Raw state**: A `active:true`, B `active:false`.
- **Expected**: A now active (the capture's foreground-to-act flipped the active tab); B no longer active.
- **Verdict**: pass — the `active` flag reflects the foregrounding side effect.
- **Evidence**: [step4.json](./2026-07-08-18-59-14/step4.json)

### Step 5 — PASS
- **Actions taken**: `activateTab(B)` (confirm B active); read B's DOM (before); `click(A, fieldDeviceX, fieldDeviceY)` at the background A; `typeText(A, "fg-to-act-input")`; `readDom(A)`; `enumerateTabs`; re-read B's DOM (after).
- **Raw state**: after `activateTab(B)` → B `active:true`, A `active:false`. Post-input: `document.activeElement.id="field"`, `field.value="fg-to-act-input"`, `echo="ECHO: fg-to-act-input"`, A `active:true`, B `active:false`, B's DOM `outerHTML` byte-identical before vs after (`bUnchanged:true`).
- **Expected**: input lands on A (readDom reflects it), foreground-to-act brought A to front (post-input A active); input did NOT land on B (B DOM unchanged).
- **Verdict**: pass — both capture (Step 3) and input honor foreground-to-act for a background target.
- **Evidence**: [step5.json](./2026-07-08-18-59-14/step5.json), [step5-A-after-input.png](./2026-07-08-18-59-14/step5-A-after-input.png)

## Orchestrator Notes
Foreground-to-act (DD1/DD5) holds on the native WebContentsView surface for both the capture path and the input path: capturing or driving input at a background tab foregrounds it first, the capture shows the correct tab (not blank, not the previously-active tab), and input lands on the target while the non-target tab is untouched. Coordinate clicks required device-pixel scaling (see the `mcp-drive-end-to-end` finding) — not a foreground-to-act concern, but noted for reproducibility.

## Closing Summaries

### Executor closing
Clean, fast run. The background-capture pixel discrimination (fixture vs example.org) was unambiguous, and the input side-effect (A gains focus + value, B byte-identical) is a strong Witnessed observable of foreground-to-act on input.

### Validator closing
No spec drift. Substituting the editable fixture for example.com as page A strengthened Step 5 (a real readDom-observable echo instead of typing into a fieldless page) while preserving the visual-distinctness the capture assertion needs.

## Operator Notes
Run on WSLg (DPR 1.25).
