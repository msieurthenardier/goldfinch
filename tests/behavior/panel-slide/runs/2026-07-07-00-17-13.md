# Behavior Test Run: panel-slide — 2026-07-07-00-17-13

**Spec**: [tests/behavior/panel-slide.md](../../panel-slide.md)
**Status**: pass
**Started**: 2026-07-07T00:17:13Z
**Completed**: 2026-07-07T00:32:40Z
**Duration**: 00:15:27
**Executor**: afe73252b9f2fd693 (general-purpose, live SendMessage continuation)
**Validator**: ac24f63dbc5cae3a3 (general-purpose, live SendMessage continuation)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/panel-slide/2026-07-07-00-17-13/` during the run (per-step JSON
geometry/stat sidecars; rendered frames were inline in agent transcripts — see apparatus note).
Re-derivable by re-running the spec.

## Summary

6 / 6 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec (drafted at Flight-9
planning, Architect-reviewed). Executed as Flight-9 Leg-1 (`01-slide-probe`) — the objective net for
SC7/#27/SC10. **Settled-state compositing is fully clean**; inter-frame slide smoothness is HAT-scoped
(Leg 2).

## Checkpoint Results

### Checkpoint 1 — PASS (baseline)
- **Actions**: fresh fixture tab (guest wcId 5, cold reload); baseline `#webviews` slot + panel
  states; baseline `captureWindow`.
- **Raw state**: guest full-width below toolbar; slot `{x:1,y:89,w:1398,right:1399}`; both panels
  collapsed (width 0); no residual strip; guest live (ticks 109→114).
- **Verdict**: pass — both agents independently confirmed full-width flush, panels collapsed, live.
- **Evidence**: Executor `step-1-baseline.json`; Validator own baseline `captureWindow` (anchor).

### Checkpoint 2 — PASS (media open + compress — the core property)
- **Actions**: open media; grab A; +2s; grab B; read slot/width/ticks.
- **Raw state**: media panel exactly 360px right; guest COMPRESSED FLUSH to its left edge, no
  gap/overlap; `#webviews` width 1398→1038 (Δ **−360** = panel width); guest genuinely re-bounded
  (fixture text clipped at the seam + a horizontal scrollbar appeared); LIVE (ticks 293→313).
- **Verdict**: pass — Validator's own capture: cream guest meets dark panel with no intervening
  strip; scrollbar + clip confirm real re-layout, not overlay; liveness confirmed.
- **Evidence**: `step-2-media-compress.json`; Validator media-open `captureWindow`.

### Checkpoint 3 — PASS (media close + restore)
- **Raw state**: media collapsed (0px); guest expanded to full width flush; slot back to
  `{x:1,w:1398,right:1399}` **byte-identical** to baseline; step-2 scrollbar + clip both gone (real
  re-expansion, not overlay removal).
- **Verdict**: pass — Validator capture pixel-equivalent to its baseline anchor; no residual strip.
- **Evidence**: `step-3-media-restore.json`; Validator restore `captureWindow`.

### Checkpoint 4 — PASS (privacy open on a REAL tracker page — the DD3 case)
- **Actions**: open cnn.com (guest wcId 6, ~12s load), refresh, open privacy; read `.ps-big` stats.
- **Raw state**: privacy panel 360px right, GENUINELY populated — **Trackers=3** (blocked:
  googlesyndication.com, doubleclick.net, optimizely.com), **Third-party domains=16** (cnn.io,
  mediamelon.com, brightline.tv…), Fingerprinting=0 — read from live DOM, not section presence;
  guest (cnn.com) compressed flush at slot 1038 (Δ−360), no gap/overlap.
- **Verdict**: pass — Validator's own capture shows real cnn.com (consent modal + headlines) with
  the non-zero stats visible in the panel; DD3/M04 genuine-population requirement met.
- **Evidence**: `step-4-privacy-populated.json`; Validator privacy `captureWindow`.
- **Validator note**: the guest|panel seam here is **dark-on-dark** (cnn.com dark theme vs dark
  panel) — judged flush by absence of any bright gap strip + no cross-boundary bleed, backed by the
  slot numbers. The crispest flush evidence in the run is step 2 (cream vs dark).

### Checkpoint 5 — PASS (cross-panel switch)
- **Raw state**: switch privacy→media: privacy collapsed (0px), media open (360px) as the SINGLE
  right panel; slot held at 1038 (single-panel compression, NOT ~678 double-inset); no residual
  privacy strip, no gap/overlap; media body repopulated with cnn.com's real images.
- **Verdict**: pass — Validator capture: exactly one panel inset, guest flush, no double-inset.
- **Evidence**: `step-5-cross-switch.json`; Validator switch `captureWindow`.

### Checkpoint 6 — PASS (return to baseline)
- **Raw state**: both panels collapsed; slot back to `{x:1,w:1398,right:1399}` byte-identical to
  baseline; no residual strip. (Active tab is now cnn.com — expected time-varying content; PANEL
  geometry judged, matches baseline.)
- **Verdict**: pass — Validator capture: no panel, guest full-width flush; the full
  open→compress→restore→switch→restore cycle returns cleanly to baseline geometry.
- **Evidence**: `step-6-return-baseline.json`; Validator return `captureWindow`.

## Orchestrator Notes

- **Mode**: live two-agent (SendMessage continuation available); both agents persisted across all six
  checkpoints. Cache `cold` (fresh fixture tab + reload).
- **Apparatus**: `mcp__goldfinch-development__*` namespace only (dev instance, `127.0.0.1:49252`,
  semi-permanent hash-persisted admin key — launched WITHOUT `GOLDFINCH_AUTOMATION_DEV_MINT`, so no
  re-mint; the standing project-scope `goldfinch-development` MCP kept working). Prod instance
  (`mcp__goldfinch__*`, 49152) never touched. Wiring litmus + OS-grab capture canary (find bar
  visible in pixels) passed pre-run.
- **Evidence-persistence quirk (carry-forward)**: `captureWindow` returns inline rendered PNG with no
  base64 text exposed to the Executor, so standalone `.png` files can't be written from the tool
  result (same limitation as the F8 menu-overlay run). The Executor saved per-step JSON
  geometry/stat sidecars; the **Validator's own `captureWindow` at each checkpoint** was the
  rendered-state evidence (a fresh observation the protocol prefers anyway). This made the Validator
  the sole rendered-state source — worked cleanly, but a Validator-side capture failure would have
  blocked every step; a capture-to-path apparatus variant would remove the dependency.
- **Settle**: awaited in-page `setTimeout` via `evaluate` (foreground `sleep` is blocked); 400ms
  after each toggle was ample for the 0.18s transition.
- **Operator interventions**: none (no failing checkpoints).
- **Variants**: the spec's two optional variants (video guest; narrow-window compression) were NOT
  run this pass — deferred to the Leg-2 HAT / future re-runs.

## Closing Summaries

### Executor closing
All 6 steps clean, no exceptions. Consistent quantitative thread: baseline slot 1398 → any single
panel open compresses to exactly 1038 (Δ−360 = the 360px panel width), guest flush, no gap/overlap;
every close returns byte-identical to baseline; cross-switch held 1038 (no double-inset). Reflow
corroboration beyond geometry: under compression the fixture guest genuinely re-laid-out (horizontal
scrollbar + clipped text line appeared, then vanished on restore) — proves re-layout, not overlay.
Liveness: ticks 293→313 across two compressed grabs. Privacy population from cnn.com's real blocked
trackers (3) + third-party domains (16), read from live DOM. No blocking anomalies (cnn.com consent
modal cosmetic). Apparatus carry-forward: captureWindow inline-PNG limit → JSON sidecars +
in-transcript frames.

### Validator closing
Core claim (panels inset/compress, not overlay) held on pixels at every checkpoint. The step-2
scrollbar/clip signal is the strongest evidence #webviews genuinely re-bounded — recommend the spec
note it as a tell. Liveness cleanly observable via the fixture's clock+ticks (keep requiring a
visible time-varying region). Step 4 (genuine population) was the sharpest and delivered
(Trackers=3, Third-party=16, real+named) — but it is network-dependent: offline or a CNN layout
change could force INCONCLUSIVE, so the spec should name a fallback tracker-heavy page / flag the
risk. Spec-quality gap: the dark-on-dark seam (cnn.com dark theme vs dark panel, steps 4–6) is far
less crisp than the cream-fixture seam (step 2) — recommend keeping the fixture as the PRIMARY
flush-seam assertion and treating real-page steps as population/single-inset assertions, or add a
seam-region pixel-column probe for dark-on-dark. Apparatus: Validator was the sole rendered-state
source (Executor couldn't persist PNGs) — worked, but the harness shouldn't depend on it. No
dissents. Clean run.

## Operator Notes

First Witnessed run of the Flight-9 panel-slide spec, as Leg-1's objective net. Confirms SC7's F1
"looks free" prediction on **settled** compositing; the **smoothness** call (inter-frame no-tear) is
the Leg-2 HAT's. The two Validator spec-quality flags (fixture-as-primary-flush-seam;
network-dependence fallback) were folded into the spec post-run.
