# Behavior Test Run: history-recording — 2026-07-12-19-37-28

**Spec**: [history-recording.md](../../history-recording.md)
**Status**: pass
**Started**: 2026-07-12T19:37:28Z
**Completed**: 2026-07-12T20:04:30Z (approx; teardown confirmed after final verdict)
**Duration**: ~00:27
**Executor**: live Sonnet sub-agent (SendMessage continuation, 8 steps + teardown)
**Validator**: live Sonnet sub-agent (SendMessage continuation, 8 verdicts)
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/history-recording/2026-07-12-19-37-28/` during the run; re-derivable by re-running the spec.

## Summary

**8 / 8 checkpoints passed.** 0 failed; 0 inconclusive. First run of this spec
(authored at Flight 1 design, mission 08). Every PASS was grounded in the
Validator's own fresh `node:sqlite` readOnly query of the dev-profile
`history.db` (or, for the sole-reader after-quit state, a timestamped
evidence file), with rendered-state corroboration for the zero-row steps.
Executor evidence and Validator independent reads agreed on every
cross-checked value (~20).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: fixture server up (3 titled pages, port 8000); app launched via `npm run dev:automation` with mint envs; admin key captured from stdout; admin SDK client connected (session jar-tier MCP tools insufficient for admin ops — scripted client used); `history.db` located under `~/.config/goldfinch-dev` (WAL live); baseline N0=2 (the boot's own startup navigation).
- **Expected**: initialize succeeds as admin; DB readable while app runs.
- **Verdict**: pass — Validator's own concurrent readOnly read returned N0=2 row-for-row; admin tier corroborated by the jar-tier refusal contrast.
- **Evidence**: `app-stdout.log`, `step1-baseline-N0-final.json`, `step1-enumerateTabs.json`, `step1-containers.json`, `step1-app-boot.png`, `mcp-call.mjs`

### Checkpoint 2 — PASS
- **Actions taken**: default-jar tab (jar `rename-test`, the resolved default) navigated to fixture URL A; load confirmed; DB queried.
- **Expected**: exactly one new URL-A row, jar_id = the jar's id, visited_at recent.
- **Verdict**: pass — single row `{id:3, jar_id:'rename-test', title:'History Fixture Page A', visited_at:+215ms after navigate}`; delta +1 attributable solely to URL A.
- **Evidence**: `step2-db-query.json`, `step2-enumerateTabs.json`, `step2-page-a.png`

### Checkpoint 3 — PASS
- **Actions taken**: URL-A rows re-queried; fixture `<title>` fetched via curl for comparison.
- **Expected**: row's title equals the fixture `<title>`; no second row.
- **Verdict**: pass — title byte-identical (`History Fixture Page A`), same row id 3, count for URL A = 1, total unchanged.
- **Evidence**: `step3-db-query.json`, `step3-fixture-title.txt`

### Checkpoint 4 — PASS
- **Actions taken**: `history.pushState` to `page-a.html?spa=1` via admin evaluate; URL change confirmed in-page and via enumerateTabs; DB queried.
- **Expected**: a new row for the in-page URL in the same jar.
- **Verdict**: pass — `{id:4, jar_id:'rename-test', url:…?spa=1, visited_at:+160ms}`; `did-navigate-in-page` records. (Null title is expected for pushState — no title event fires.)
- **Evidence**: `step4-pushState.json`, `step4-enumerateTabs.json`, `step4-db-query.json`

### Checkpoint 5 — PASS
- **Actions taken**: admin `openTab { jarId:'work' }` to fixture URL B; jar confirmed via enumerateTabs; DB queried.
- **Expected**: URL B's row carries the second jar's jar_id.
- **Verdict**: pass — `{id:5, jar_id:'work', title:'History Fixture Page B'}`; per-jar grouping rename-test:4 / work:1.
- **Evidence**: `step5-openTab.json`, `step5-enumerateTabs.json`, `step5-db-query.json`

### Checkpoint 6 — PASS
- **Actions taken**: N1=5 recorded; burner tab created via chrome evaluate `createTab(pageC, makeBurner())` (partition `burner:192081377`); load proven (enumerate title + rendered screenshot); DB queried three ways.
- **Expected**: zero URL-C rows; total unchanged by burner activity.
- **Verdict**: pass — URL-C exact/LIKE and `jar_id LIKE 'burner%'` all zero; total 5 = N1; full dump byte-identical to the step-5 ledger. The negative is meaningful: the page demonstrably loaded (rendered pixels + enumerate) before absence was asserted.
- **Evidence**: `step6-N1.json`, `step6-createBurner.json`, `step6-enumerateTabs.json`, `step6-burner-page-c.png`, `step6-db-query.json`

### Checkpoint 7 — PASS
- **Actions taken**: `goldfinch://settings` + `goldfinch://jars` opened via evaluate closed-set functions; both loads proven (enumerate titles + full-window capture); DB queried.
- **Expected**: zero rows with a `goldfinch://` URL.
- **Verdict**: pass — `url LIKE 'goldfinch%'` zero and `jar_id='internal'` zero; total unchanged at 5.
- **Evidence**: `step7-open-settings.json`, `step7-open-jars.json`, `step7-enumerateTabs.json`, `step7-internal-pages.png`, `step7-db-query.json`

### Checkpoint 8 — PASS
- **Actions taken**: quit via the kebab-Exit IPC path (`window.goldfinch.appQuit()` by admin evaluate — no signals); exit confirmed (pgrep clean, port released, WAL/SHM checkpointed away, db grew 4→32 KB); sole-reader query; relaunch with same envs (new keys minted); reconnect; re-query.
- **Expected**: A (titled) and B rows present after quit AND relaunch; relaunch creates no duplicate A/B rows.
- **Verdict**: pass — after quit: A and B verbatim, total 5; after relaunch: A×1, B×1, spa×1 (same ids/titles/visited_at — no duplicates); total 7 with ids 6–7 = the relaunch's own startup navigation (normal appends). Burner/internal exclusions held across the restart. Session restore did not re-open A/B.
- **Evidence**: `step8-quit.json`, `step8-db-after-quit.json`, `app-stdout-relaunch.log`, `step8-relaunch-chrome.json`, `step8-relaunch-enumerateTabs.json`, `step8-db-after-relaunch.json`

## Orchestrator Notes

- **Mode**: live two-agent (SendMessage continuation available) — both agents persisted across all 8 checkpoints. Cache mode: cold (fresh launch verified; no prior electron processes).
- Both crew agents on Sonnet per the project crew file.
- Apparatus resolution: the session's pre-wired goldfinch MCP tools authenticated at jar tier only; all admin-tier ops ran through a scripted SDK client (`mcp-call.mjs`) with the freshly minted admin key. Noted for future runs.
- No operator interventions; no failed checkpoints; no re-spawns.
- Teardown: relaunched app quit via the same appQuit path, fixture server stopped, ports confirmed released. Residue: this run's 7 rows in the dev-profile `history.db` (6 `rename-test`, 1 `work`).

## Closing Summaries

### Executor closing

Teardown complete (app quit via appQuit evaluate — the 'other side closed' HTTP race is the expected success signature; pgrep/port/WAL-checkpoint are the authoritative exit evidence; fixture server killed). Anomalies: (1) session-MCP key tier mismatch — pre-wired tools reached the fresh instance at jar tier; admin ops needed the scripted client with the minted adminKey; (2) /tmp scratch scripts can't resolve `@modelcontextprotocol/sdk` by name — import via absolute path into goldfinch/node_modules; (3) startup navigation writes 2 self-identifying rows per boot (google.com + `?zx=` variant, second title-null) — delta accounting kept assertions clean; (4) resolved default jar was `rename-test` (shared dev-profile state), not the seed default; (5) **`enumerateTabs` DOES list internal `goldfinch://` tabs (jarId 'internal'), contradicting its own tool description ("non-internal")** — helped step 7 but the doc/behavior divergence deserves a project look; (6) quit-races-response is inherent to driving appQuit over the transport; (7) one incidental module-scope evaluate error (expected closed-set behavior). No blocked observables, no retries, no flakiness.

### Validator closing

8/8 with every PASS grounded in an independent fresh DB read or durable timestamped evidence; zero disagreements with Executor evidence across ~20 cross-checked values. Spec-improvement notes: (1) name the `visits` table in the spec; (2) "default jar" is a resolved runtime value — capture at step 1 and assert against the captured id; (3) note the by-design title-null rows (startup `?zx=` artifact; pushState rows); (4) make positive-load evidence an explicit requirement before zero-row assertions; (5) state step 8's per-URL (not total-count) duplicate semantics — relaunch legitimately grows the total by its startup navigation; (6) name the 'other side closed' quit-race signature as expected. Concerns raised at READY that did not materialize: title-backfill timing, missing pushState hook, fragile N1 total. System verified end-to-end; no defects observed.

## Operator Notes

*(autonomous run — none)*
