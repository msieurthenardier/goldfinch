# Behavior Test Run: menu-overlay — 2026-07-06-22-07-02

**Spec**: [tests/behavior/menu-overlay.md](../../menu-overlay.md)
**Status**: pass
**Started**: 2026-07-06T22:07:02Z
**Completed**: 2026-07-06T22:29:55Z
**Duration**: 00:22:53
**Executor**: a808868d785b33a04 (general-purpose, Sonnet-class, live SendMessage continuation)
**Validator**: a1a14d43d6ef94d3e (general-purpose, Sonnet-class, live SendMessage continuation)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/menu-overlay/2026-07-06-22-07-02/` during the run; re-derivable by
re-running the spec.

## Summary

6 / 6 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec (drafted at Flight-8
planning, reconciled at Leg 5b); promoted `draft` → `active` on this pass.

## Checkpoint Results

### Checkpoint 1 — PASS (setup)
- **Actions taken**: Litmus (`getChromeTarget` → chrome wcId 1; `enumerateTabs` clean); FRESH
  fixture tab opened (guest wcId 4) + one cache-bypass reload (cold mode); geometry sweep
  (ticking region, `#outside-link` center (166,608), mid-page link); baseline `captureWindow`.
- **Raw state**: full-height guest below toolbar; live clock/ticks; no menu, no find bar.
- **Expected**: (setup) Litmus passes; baseline recorded (guest full-height, ticking-region
  location, link location, no menu, no find bar).
- **Verdict**: pass — baseline elements all recorded on rendered pixels; litmus corroborated.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`
- **Validator notes**: find-bar absence not yet authoritative (rule 8) — step 5 must establish
  presence first (it did).

### Checkpoint 2 — PASS (live-guest float — the anti-freeze property)
- **Actions taken**: kebab opened (chrome trigger click via evaluate; aria-expanded=true);
  TWO grabs BOTH while open, ~2s apart; menu left open for step 3.
- **Raw state**: menu composited top-right over the guest; guest visible + full-height around
  it (no push-down, no frozen band); ticks 346 → 348 between the open-grabs.
- **Expected**: menu composited over guest; guest visible/full-height; ticking region DIFFERS
  between grabs. [render-correct]
- **Verdict**: pass — open-grab pair shows the guest live under the open menu; a freeze-frame
  still could not produce the delta.
- **Evidence**: `step-2-screenshot.png`, `step-2-screenshot-2.png`

### Checkpoint 3 — PASS (dismiss-without-forwarding)
- **Actions taken**: sheet wcId probed via the background-tab-safe walk (sheet = wcId 5, first
  candidate; no tab ever activated); `click(5, 166, 608)` at the recorded `#outside-link`
  center; grab + `enumerateTabs` URL read.
- **Raw state**: menu gone, no residue; page did NOT navigate — URL unchanged (no
  `?navigated=1`), tick counter continued monotonically (no reload).
- **Expected**: menu dismissed; click NOT forwarded (URL unchanged, baseline framing);
  OS-pointer interception is the HAT's job. [render-correct]
- **Verdict**: pass — click dispatched at the exact recorded link coordinates into the probed
  sheet; dismissal on pixels; non-navigation triply corroborated (URL, framing, tick
  continuity).
- **Evidence**: `step-3-screenshot.png` (+ `step-1-snapshot.txt` for the coordinates)

### Checkpoint 4 — PASS (keyboard contract + focus return)
- **Actions taken**: keyboard open from chrome (trigger focus + ArrowDown — no click fallback
  needed); open-state reads (chrome aria-expanded; sheet roving via readDom(5)); ArrowDown on
  the sheet (roving moved Settings → Downloads, tabindex swap confirmed); Escape; post-state
  grab + chrome focus read.
- **Raw state**: open — Settings visibly focus-outlined, aria-expanded=true; after Escape —
  menu gone on pixels, kebab button visibly focus-ringed, `{ariaExpanded:'false',
  activeElementId:'kebab', hasFocus:true}`.
- **Expected**: while open: aria-expanded=true + focused item (roving); after Escape: dismissed
  on pixels, aria-expanded=false, focus returned to trigger.
- **Verdict**: pass — every clause pixel-evidenced with DOM corroboration; roving proven by an
  actual tabindex swap, stronger than the spec required.
- **Evidence**: `step-4-screenshot.png`, `step-4-screenshot-2.png`, `step-4-snapshot.txt`

### Checkpoint 5 — PASS (find-bar interplay, DD5)
- **Actions taken**: find opened (Ctrl+F to guest); find overlay probed (wcId 6); 'tick' typed
  (count 1/1); three grabs: pre-menu / container-menu-open / post-Escape.
- **Raw state**: find bar visible with query pre-menu (establishes compositing); ABSENT while
  the container menu is open (its slot shows plain guest background); RESTORED at the same
  bounds with query 'tick' and count intact after Escape. Input focus outline not restored
  (focus followed the escape contract to the chrome trigger).
- **Expected**: find bar visible pre-menu; HIDDEN while menu open; RESTORED at correct bounds
  with query intact. [render-correct]
- **Verdict**: pass — all three states on pixels; absence authoritative per the rule-8
  sequencing. Focus-return-to-input is not in the Expected Results (see UX note below).
- **Evidence**: `step-5-screenshot.png`, `step-5-screenshot-2.png`, `step-5-screenshot-3.png`

### Checkpoint 6 — PASS (return-to-baseline)
- **Actions taken**: find closed (Escape to overlay wcId 6); trigger-state sanity reads; final
  grab.
- **Raw state**: frame pixel-equivalent to baseline modulo time-varying content — no menu, no
  sheet residue/strip, no find bar, guest full-height at baseline framing; tick counter
  189 → 1029 monotonic across the whole run (no reload/navigation ever).
- **Expected**: pixel-equivalent to step-1 baseline (modulo time-varying content); sheet DOM
  may persist hidden — pixels govern.
- **Verdict**: pass — direct side-by-side with the step-1 baseline; only deltas are clock,
  ticks, and the automation-activity badge (expected mutable region).
- **Evidence**: `step-6-screenshot.png` (vs `step-1-screenshot.png`)

## Orchestrator Notes

- **Mode**: live two-agent (SendMessage continuation available); both agents persisted across
  all six checkpoints. Cache mode `cold` (spec default) — fresh fixture tab + one bypass
  reload.
- **Apparatus**: `mcp__goldfinch-development__*` namespace only (project-scope MCP → dev
  instance on 127.0.0.1:49252, semi-permanent admin key). The operator's production instance
  (`mcp__goldfinch__*`, port 49152) was never touched. Precondition deviation from the spec's
  launch line: the instance runs with `GOLDFINCH_AUTOMATION_ADMIN=1` but WITHOUT
  `GOLDFINCH_AUTOMATION_DEV_MINT` — the admin key is hash-persisted in the dev profile
  (semi-permanent key model, adopted this session); equivalent admin posture, no key re-mint.
- **Wiring litmus**: passed pre-run by the Orchestrator (chrome wcId + uniquely-named tab in
  `enumerateTabs`) and re-run by the Executor at step 1.
- **wcId map (this run)**: chrome=1, guest-under-test=4, sheet=5, find-overlay=6; pre-existing
  tabs 2/3 untouched throughout.
- **Evidence persistence quirk**: MCP `captureWindow` returns images inline; the Executor
  persisted PNGs by re-invoking captureWindow over the loopback HTTP transport
  (credential-free helper reading the operator's MCP client config at runtime; a first draft
  with an inlined token was denied by the permission classifier and never persisted). Each
  saved PNG is therefore a separate grab seconds after the in-band one; the step-2 liveness
  pair was taken as two SAVED grabs so the delta lives within one evidence pair.
- **Variants**: the spec's two optional variants (find-hide across other menu surfaces;
  kebab over an internal tab) were NOT run — both surfaces were verified live in the Leg-6
  HAT (steps 9 and 11) this same flight; recorded as deliberately deferred, not skipped
  silently.
- **Operator interventions**: none required (no failing checkpoints).
- **Spec updates from this run**: three wording tightenings applied post-run (step-2
  "both grabs while open"; step-1/3 recorded-coordinates dependency made explicit;
  automation-activity badge named as an expected mutable region) + status `draft` → `active`,
  Last Run stamped. See the spec's changelog note.

## Closing Summaries

### Executor closing
Clean run — no exceptions, no fallbacks; every "proven" driver in the spec behaved as
documented (keyboard-open, sheet pressKey Escape/arrows, both trigger ids). Probe walk found
both overlays on the first candidate (sheet 5, find 6) — cheap in practice, don't rely on the
ordering. Carry-forwards: (1) captureWindow-to-file needs the dual-grab helper pattern (or a
first-class capture-to-file op); (2) the automation-activity badge is a guaranteed per-op pixel
delta — future specs should name expected mutable regions; (3) a monotonic tick counter is
free, strong no-reload evidence — design it into fixtures deliberately; (4) find restored
without input focus after menu-Escape — matches the close-family design; pin it if "restored"
is ever meant to include focus. No WSLg compositing artifacts; window geometry stable
throughout; production namespace untouched.

### Validator closing
Strong run against a well-built spec: every verdict rested on rendered pixels with DOM as
corroboration only; the Executor never used sheet DOM for closed-state claims and established
compositing before every absence claim (rule-8 sequencing is load-bearing — keep it verbatim).
READY concerns all resolved in execution (open-grab pair; recorded-coordinates click; sheet
observability via the probe). Carry-forwards: UX note — after menu-Escape the restored find bar
loses input focus (documented close-family policy, but a user mid-find loses their typing
target; deserves a deliberate decision somewhere); the keyboard open path exceeded the spec —
make it explicit so it stays covered; the apparatus-limit note (OS-pointer interception =
HAT's job) was load-bearing in step 3 — without it the step would overclaim. Spec is
promotable from draft with the small tightenings (applied post-run).

## Operator Notes

First Witnessed run of the Flight-8 menu-overlay sheet; run as Leg-6 step 14 of
`flight/08-menu-overlay-sheet`. The HAT (steps 1–13, this flight) covered the OS-pointer and
focus-feel surfaces this spec deliberately scopes out.
