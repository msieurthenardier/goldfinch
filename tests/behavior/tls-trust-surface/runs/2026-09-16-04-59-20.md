# Behavior Test Run: tls-trust-surface — 2026-09-16-04-59-20

**Spec**: [tls-trust-surface.md](../../tls-trust-surface.md)
**Status**: fail (partial — 13 / 16 checkpoints pass; 3 fail, all dispositioned by the operator)
**Started**: 2026-09-16T04:59:20Z
**Completed**: 2026-09-16T11:05:00Z (approx.; three relaunches and four mid-run fix passes)
**Duration**: ~06:05:00 wall-clock (including fix passes)
**Executor**: Executor agent (Sonnet), live mode
**Validator**: Validator agent (Sonnet), live mode
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tls-trust-surface/2026-09-16-04-59-20/` during the run (107 files); re-derivable by re-running the spec.

## Summary

13 / 16 checkpoints passed. 3 failed; 0 inconclusive at close (two automation-inconclusive keyboard rows were resolved by operator by-eye confirmation). Four product findings were surfaced and fixed during the run (F1–F4; F1–F3 re-verified live in the same run, F4 fixed post-run for HAT verification); the fifth finding is issue #216 confirmed still live (checkpoints 14 and 15, one finding), left as a Known Issue by operator ruling.

| # | Result | Note |
|---|--------|------|
| 0 | PASS | precondition census |
| 1 | PASS | interstitial (3 runs, identical) |
| 2 | FAIL → PASS | F1 fixed mid-run; re-verified twice |
| 3 | FAIL → PASS | F2 fixed mid-run; re-verified twice |
| 4 | PASS | |
| 5 | PASS | proceed card refused to every op |
| 6 | FAIL → PASS | operator proceed ×2; F3 fixed mid-run |
| 7 | PASS | |
| 9 | FAIL | F4 (green lock before the first security push); fixed post-run, HAT verifies |
| 10 | PASS | |
| 11 | PASS | trusted fixture via NSS anchor (spike (g) honoured) |
| 12 | PASS | secure → cert failure reads `none` |
| 13 | PASS | trusted viewer; fingerprint identical |
| 14 | FAIL | #216 live by eye (automation inconclusive, escalated) |
| 15 | FAIL | same finding; card's own keyboard contract sound once focused |

Steps 8 and 16 were setup/cleanup rows (no judgment).

## Checkpoint Results

### Checkpoint 0 — PASS
- **Actions taken**: fresh attach; enumerateTabs; enumerateWindows.
- **Raw state**: 13 leftover tabs (earlier runs; ports 40123/40300, example.com, google, about:blank) — every row loadState ok, loadError null, security present (secure/insecure/none); window row sheetVisible false, activeTabWcId 18.
- **Expected**: Every row has `loadState: "ok"`, `loadError: null`, and a `security` value; the window row has `sheetVisible: false`.
- **Evidence**: `step-0-census.json`, `step-0-windows.json`
- **Verdict**: pass — all 13 rows ok/null/security present; sheetVisible false. Validator note: many pre-existing about:blank tabs — track new tabs by wcId.

### Checkpoint 1 — PASS
- **Actions taken**: openTab https://127.0.0.1:42525/ → wcId 20; polled census; captureScreenshot(chrome 1); readAxTree(chrome 1); enumerateTabs.
- **Raw state**: interstitial region: heading "This connection isn't private"; body "This site's security certificate is from an authority Goldfinch doesn't trust."; address `https://127.0.0.1:42525/`; code `ERR_CERT_AUTHORITY_INVALID (-202)`; buttons Retry / View certificate / Advanced; strip "127.0.0.1:42525 — failed to load". Census wcId20: cert-blocked, loadError {-202, ERR_CERT_AUTHORITY_INVALID}, security none.
- **Expected**: interstitial with heading, address, authority wording, raw code, three buttons; census cert-blocked / name / security none.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-census.json`
- **Verdict**: pass — Validator inspected the screenshot: non-blank interstitial, all lines and three buttons pixel-rendered; strip glyph present; census exact.

### Checkpoint 2 — FAIL (first run) → fix → re-run below
- **Actions taken**: captureScreenshot(chrome); readAxTree(chrome); evaluate chip attributes + address value.
- **Raw state**: strip entry "127.0.0.1:42525 — failed to load"; address bar `https://127.0.0.1:42525/`; chip aria-label/title "Site information, 127.0.0.1:42525"; chip `data-security="secure"`.
- **Expected**: strip glyph + host title; address bar intended address; the chip's name does not claim a secure connection.
- **Verdict**: fail — the Validator cropped/6× upscaled the chip: a solid GREEN CLOSED PADLOCK rendered on a cert-blocked page (rendered state carried the verdict; the neutral label text did not save it). Corroborated by `data-security="secure"`.
- **Evidence**: `step-2-screenshot.png`, `step-2-validator-chip-zoom.png`, `step-2-snapshot.txt`, `step-2-eval-chip.txt`
- **Operator decision**: fix + relaunch + resume from step 1 (rerun-checkpoint for 1 and 2).
- **Finding F1 (fixed mid-run)**: root cause — `load-failure-controller.js`'s push handler guarded both the address VALUE write and `updateAddressChip` behind `document.activeElement !== els.address`; a new tab focuses the address bar, so the chip never refreshed and kept the scheme-derived `secure` set at activation. Fix: chip update unconditional; only the value write guarded. Unit test added (and the F1-era test that pinned the blind spot inverted). Orchestrator probe before the fix: `activeElement: "address"`, `hasFocus: false`, `data-security: "secure"`.
- **Relaunch**: app killed by the pid holding :49707; relaunched with DEV_MINT (fresh key, same env-only handling); restored cert-blocked tab closed; fixtures untouched; wcIds renumbered.

### Checkpoint 1 (re-run) — PASS
- **Actions taken**: relaunch verified (new PID); baseline census 14 tabs (wcIds 2–15); openTab → wcId 17; polled; captureScreenshot(chrome 1); readAxTree; enumerateTabs.
- **Raw state**: identical interstitial to the first run; census wcId17 cert-blocked / ERR_CERT_AUTHORITY_INVALID (-202) / security none.
- **Evidence**: `step-1-screenshot-2.png`, `step-1-snapshot-2.txt`, `step-1-census-2.json`
- **Verdict**: pass — screenshot inspected; identical to the first run; census exact.

### Checkpoint 2 (re-run) — PASS
- **Actions taken**: captureScreenshot(chrome); readAxTree; evaluate chip attributes.
- **Raw state**: strip "127.0.0.1:42525 — failed to load"; chip label/title "Site information, 127.0.0.1:42525"; `data-security="none"` (was `secure`); address bar intended URL; activeElement `load-failure-heading`.
- **Evidence**: `step-2-screenshot-2.png`, `step-2-snapshot-2.txt`, `step-2-eval-chip-2.txt`
- **Verdict**: pass — chip zoom shows a neutral outline padlock (no green fill); strip glyph + host title; address bar intended URL. Fix confirmed by rendered-icon inspection (`step-2-validator-chip-zoom-2.png`).

### Checkpoint 3 — FAIL (first run) → fix → re-run below
- **Actions taken**: evaluate click `#load-failure-view-cert`; enumerateWindows (sheetWcId 18, sheetVisible true); readAxTree(sheet 18) admitted; captureScreenshot(sheet 18) admitted; openssl fingerprint/dates/subject/issuer.
- **Raw state**: dialog "Certificate"; status "Not trusted — ERR_CERT_AUTHORITY_INVALID"; Issued to CN=127.0.0.1; Issued by CN=Goldfinch Fixture Throwaway CA; valid 2026-09-16T04:24:33Z → 2026-09-23T04:24:33Z; SHA-256 `3E:14:C5:40:87:4D:51:07:9A:57:3C:83:11:86:23:31:C5:5E:0E:DC:31:32:3B:42:A6:79:89:A9:2E:97:8F:52` = openssl; Chain[0] and Chain[1] BOTH "CN=Goldfinch Fixture Throwaway CA → CN=Goldfinch Fixture Throwaway CA".
- **Expected**: readable card; subject/issuer/validity/fingerprint match; chain of two entries (server → CA); not-trusted status.
- **Verdict**: fail — every clause except the chain met; the chain shows the CA duplicated and never the server leaf.
- **Evidence**: `step-3-windows.json`, `step-3-sheet-snapshot.txt`, `step-3-sheet-screenshot.png`, `step-3-openssl.txt`
- **Operator decision**: fix + relaunch + resume from step 1.
- **Finding F2 (fixed mid-run)**: `certificate-summary.js`'s `buildNodeChain`/`buildElectronChain` start at the ISSUER (skipping the leaf) and terminate on object identity — Electron hands back a distinct object for a self-signed CA's own `issuerCert`, so the CA is pushed twice. Fix: chain begins with the leaf; termination by value (subject === issuer, or repeated fingerprint); unit-pinned.
- **Relaunch #2**: app killed by the :49707 pid; relaunched with DEV_MINT (fresh key, env-only); restored cert-blocked tab closed; fixtures untouched. Orchestrator ruling: steps 1–3 re-run as ONE batched Executor turn (checkpoints 1–2 had passed on unchanged code; only step 3's chain rows changed); the Validator judges all three with the viewer card still open for fresh reads.

### Checkpoints 1–3 (re-run #2, batched) — PASS, PASS, PASS
- **Actions taken**: relaunch verified (PID 541203); openTab → wcId 17; captures; chip eval; View certificate click; sheet 18 readAxTree/captureScreenshot admitted; openssl.
- **Raw state**: step 1 identical interstitial + census; step 2 chip `data-security="none"`, label neutral, activeElement heading; step 3 card: status "Not trusted — ERR_CERT_AUTHORITY_INVALID", Issued to CN=127.0.0.1, Issued by the throwaway CA, 7-day validity, SHA-256 identical to openssl, Chain[0] "CN=127.0.0.1 → CN=Goldfinch Fixture Throwaway CA", Chain[1] "CN=Goldfinch Fixture Throwaway CA → CN=Goldfinch Fixture Throwaway CA".
- **Evidence**: `step-1-*-3.*`, `step-2-*-3.*`, `step-3-*-3.*`
- **Verdicts**: 1 pass (identical interstitial); 2 pass (chip zoom neutral outline — F1 stable across relaunch; `step-2-validator-chip-zoom-3.png`); 3 pass (Chain[0] leaf → CA, Chain[1] CA → CA; fingerprint identical; F2 confirmed by sheet screenshot).

### Checkpoint 4 — PASS
- **Actions taken**: openTab about:blank → wcId 19; activateTab 17; enumerateWindows; captureScreenshot(chrome); readAxTree.
- **Raw state**: sheetVisible false (sheetWcId 18 persists hidden); interstitial heading + three buttons projected again on wcId 17.
- **Expected**: `sheetVisible: false`; the interstitial is projected again on the first tab.
- **Evidence**: `step-4-windows.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`

### Checkpoint 5 — PASS
- **Actions taken**: evaluate click `#load-failure-advanced`; enumerateWindows; readAxTree/evaluate/click on sheet wcId 18; captureScreenshot(chrome). Card left open.
- **Raw state**: sheetVisible true; all three sheet ops → `{isError:true, value:'automation: secret-sheet — wcId 18 is a chrome-owned secret/overlay sheet and is never automatable (any tier)'}` (byte-identical).
- **Expected**: `sheetVisible: true`; all three ops refused — the proceed card is not readable or drivable by automation at the admin tier.
- **Evidence**: `step-5-windows.json`, `step-5-refusals.json`, `step-5-screenshot.png`
- **Verdicts 4/5**: pass — sheet hidden and interstitial re-projected (screenshot inspected); all three sheet ops refused byte-identically (rule 10: the refusal is the observable). Validator footnote: the refusal string `secret-sheet` is the engine's generic non-allowlisted-menuType wording, not cert-override-specific — the spec requires a refusal, not a code.

### Checkpoint 6 — FAIL (first run, operator-performed) → fix → re-run below
- **Operator action**: confirmed focus started on "Back to safety"; clicked "Proceed to 127.0.0.1:42525 (unsafe)".
- **Actions taken (Executor post-state)**: polled census; captureScreenshot(chrome); readAxTree; chip eval; readDom(17) `#auth-state`; enumerateTabs; enumerateWindows.
- **Raw state**: census wcId17 {url unchanged, loadState ok, loadError null, security overridden, title "web-compat client-cert fixture"}; sheetVisible false; `#auth-state` = `no-client-cert`; strip ordinary; chip label/title "Site information, 127.0.0.1:42525", `data-security="none"`; a11y tree has no "secure"/"override" text anywhere.
- **Expected**: interstitial gone, fixture page shows; sheet hidden; strip ordinary; the chip's name says not secure and mentions the overridden certificate; census ok / null / overridden / url unchanged.
- **Evidence**: `step-6-screenshot.png`, `step-6-snapshot.txt`, `step-6-eval-chip.txt`, `step-6-guest-dom.txt`, `step-6-census.json`, `step-6-windows.json`
- **Verdict**: fail — every clause met except the chip: `data-security="none"`, neutral label, neutral outline glyph (zoomed) while the census read `overridden`. Validator evidence note: `captureScreenshot(chromeWcId)` does not composite the visible guest — the fixture page's presence rests on the `#auth-state` DOM read, not the chrome screenshot; for VISIBLE-guest confirmation use `captureWindow` or a guest-wcId capture.
- **Operator decision**: fix + relaunch + redo steps 1–6 (override memory is session-only; the operator clicks Proceed again).
- **Finding F3 (fixed mid-run)**: main pushes `tab-did-navigate` before `tab-security`; the chrome's did-navigate handler refreshes the chip with the STALE `tab.security`, and `site-security-controller.js`'s `onTabSecurity` only stored the value — the chip always lagged one push. Fix: the push handler refreshes the chip for the active tab (`updateAddressChip` injected; one renderer line, re-pinned).
- **Relaunch #3**: app killed by the :49707 pid; relaunched with DEV_MINT (fresh key, env-only); restored failed tab closed (15 healthy tabs remain); fixtures untouched. Steps 1–5 re-run as ONE batched Executor turn (evidence suffix `-4`); the operator repeats the Proceed click for step 6.

### Checkpoints 1–5 (re-run #3, batched) — PASS ×5
- **Actions taken**: relaunch verified (PID 555597); openTab → wcId 18; captures; chip eval; View certificate (sheet 19 readable); tab switch (about:blank wcId 20) and back; Advanced → refusals on sheet 19.
- **Raw state**: all five identical in substance to the passing re-run #2 (chip `none`; Chain[0] leaf → CA; refusals byte-identical); card left open for step 6 (target tab wcId 18).
- **Evidence**: `step-1-*-4.*` … `step-5-*-4.*`
- **Verdicts**: all five pass (chip zoom neutral; Chain leaf → CA; refusals byte-identical; F1/F2 stable across the third relaunch).

### Checkpoint 6 (re-run) — PASS (operator-performed)
- **Operator action**: focus started on "Back to safety" (confirmed); clicked "Proceed to 127.0.0.1:42525 (unsafe)".
- **Actions taken (Executor post-state)**: polled census; captureScreenshot(chrome); captureWindow (visible guest); readAxTree; chip eval; readDom(18) `#auth-state`; enumerateTabs; enumerateWindows.
- **Raw state**: census wcId18 {url unchanged, ok, null, overridden, title "web-compat client-cert fixture"}; sheetVisible false; `#auth-state` = `no-client-cert`; strip ordinary; chip label/title "Site information, 127.0.0.1:42525, not secure — certificate error overridden", `data-security="overridden"`; the a11y tree's only "secure"/"override" match is the chip.
- **Evidence**: `step-6-screenshot-4.png`, `step-6-window-4.png`, `step-6-snapshot-4.txt`, `step-6-eval-chip-4.txt`, `step-6-guest-dom-4.txt`, `step-6-census-4.json`, `step-6-windows-4.json`
- **Verdict**: pass — window composite shows the fixture page ("Auth state: no-client-cert"); chip zoom shows a RED open/broken padlock; label "not secure — certificate error overridden"; census overridden; F3 confirmed (`step-6-validator-chip-zoom-6-window.png`).

### Checkpoint 7 — PASS
- **Actions taken**: evaluate `openSiteInfoOverlay()`; enumerateWindows; readAxTree(sheet 19) admitted; captureScreenshot(sheet 19).
- **Raw state**: card "Site information"; host 127.0.0.1:42525; Connection = "Not secure — certificate error overridden (HTTPS)"; Trackers blocked 0; Permissions 0; actions "Certificate", "Site settings →".
- **Expected**: readable card; host; Connection row not secure with the certificate error overridden; a Certificate action.
- **Evidence**: `step-7-windows.json`, `step-7-sheet-snapshot.txt`, `step-7-sheet-screenshot.png`

### Checkpoint 9 (step 8 setup folded in) — FAIL → fixed post-run (F4), HAT re-verifies
- **Actions taken**: step 8: openTab about:blank → wcId 21, activateTab 18, sheetVisible false; step 9: openTab https://127.0.0.1:42525/ → wcId 22; polled; enumerateTabs; captureWindow; captureScreenshot(chrome); readAxTree; readDom(22); chip eval.
- **Raw state**: census wcId22 settled {ok, null, overridden, title "web-compat client-cert fixture"}; no interstitial nodes in the a11y tree; `#auth-state` no-client-cert; chip overridden label; window composite shows the page. Executor timing note: the instant `loadState` first read `ok` the row briefly showed `security: 'none'`, `title 'New tab'` (the `tab-security` push had not landed); settled within ~1–2 s. Evidence-handling note: the Executor overwrote `step-9-census.json` with the settled read instead of saving the transient as a separate file — recorded here.
- **Expected**: page loads directly with no interstitial; census ok / overridden.
- **Evidence**: `step-8-windows.json`, `step-9-census.json`, `step-9-window.png`, `step-9-screenshot.png`, `step-9-snapshot.txt`, `step-9-guest-dom.txt`, `step-9-eval-chip.txt`
- **Verdict 7**: pass — sheet screenshot inspected; Connection "Not secure — certificate error overridden (HTTPS)"; Certificate action present.
- **Verdict 9**: fail — the per-row clauses pass (page loads directly, census overridden, no interstitial), but the chip zoom in `step-9-window.png` shows a GREEN closed lock while the same moment's label read "overridden" — a rendered-vs-DOM divergence the Validator weighed under rule 7 and the spec's Intent (one "not secure" vocabulary for overridden pages).
- **Operator decision**: fix now, continue steps 10–16 on this build, re-verify by eye at the HAT leg.
- **Finding F4 (fixed post-run)**: the window capture was taken in the load window BEFORE the first `tab-security` push landed; `updateAddressChip`'s fallback for an unpushed `tab.security` was the SCHEME rule (`https:` → `secure`, green) — a claim of trust before verification, on every https tab, including an overridden origin. Fix: unpushed state renders `none` (neutral) — "the chip never claims a state main has not pushed"; unit-pinned. Orchestrator ruling for the rest of the run: chip reads only after the census `security` settles; transient reads saved separately, never overwritten.

### Checkpoint 10 — PASS
- **Actions taken**: navigate wcId 22 → http://127.0.0.2:42443/links.html; settle; chip eval; site-info card read; captureWindow; census; card closed by tab switch (wcId 23).
- **Raw state**: census insecure; chip "Site information, 127.0.0.2:42443, not secure", `data-security="insecure"`; Connection "Not secure (HTTP)"; no Certificate action (HTTP).
- **Expected**: chip not secure (no certificate mention); Connection not secure (HTTP); census insecure.
- **Evidence**: `step-10-eval-chip.txt`, `step-10-sheet-snapshot.txt`, `step-10-sheet-screenshot.png`, `step-10-window.png`, `step-10-census.json`

### Checkpoint 11 — PASS
- **Actions taken**: navigate wcId 22 → https://127.0.0.1:42279/ (trusted); settle; captureWindow; readAxTree; chip eval; census; site-info card read; card closed (wcId 24).
- **Raw state**: census secure; no interstitial; chip "Site information, 127.0.0.1:42279", `data-security="secure"`; Connection "Secure (HTTPS)"; actions Certificate + Site settings.
- **Expected**: no interstitial; chip not "not secure"; census secure/ok; Connection secure (HTTPS) with Certificate.
- **Evidence**: `step-11-window.png`, `step-11-snapshot.txt`, `step-11-eval-chip.txt`, `step-11-census.json`, `step-11-sheet-snapshot.txt`, `step-11-sheet-screenshot.png`

### Checkpoint 12 — PASS
- **Actions taken**: navigate wcId 22 → https://localhost:42525/ (the drive op returned isError:true "ERR_CERT_AUTHORITY_INVALID (-202) loading …"); census; chip eval; captureScreenshot; readAxTree; navigate back to T2; settle.
- **Raw state**: census {cert-blocked, ERR_CERT_AUTHORITY_INVALID, security none} — not the stale secure; chip "Site information, localhost:42525", `data-security="none"`; interstitial heading + code line; after navigating back: {ok, secure}.
- **Expected**: interstitial; census none (not stale secure) / cert-blocked; chip not secure; back to secure on T2.
- **Evidence**: `step-12-census.json`, `step-12-eval-chip.txt`, `step-12-screenshot.png`, `step-12-snapshot.txt`, `step-12-census-after.json`

### Checkpoint 13 — PASS
- **Actions taken**: enumerateWindows precheck; evaluate `openCertificateViewer()`; readAxTree(sheet 19); captureScreenshot(sheet); openssl on server-trusted.pem.
- **Raw state**: status "Trusted"; Issued to CN=127.0.0.1; Issued by CN=Goldfinch Fixture Trusted CA; 7-day validity; SHA-256 `2C:D0:5A:56:BF:BA:2B:D1:E6:CA:12:19:A0:46:34:9C:E7:33:4B:FB:74:83:96:DB:51:BF:AC:6A:5E:A6:DC:9B` = openssl; Chain[0] leaf → Trusted CA; Chain[1] Trusted CA → itself.
- **Expected**: readable viewer; subject/issuer; fingerprint equal; chain of two; status trusted.
- **Evidence**: `step-13-windows.json`, `step-13-sheet-snapshot.txt`, `step-13-sheet-screenshot.png`, `step-13-openssl.txt`
- **Verdicts 10–13**: all pass. 10: red open-lock chip "not secure" without certificate wording; Connection "Not secure (HTTP)"; no Certificate action. 11: green closed lock — the first CORRECT green in the run (trusted CA); Connection "Secure (HTTPS)" + Certificate. 12: neutral chip and `security: none` on the fresh interstitial (no stale secure), back to secure on T2. 13: trusted viewer, fingerprint identical, leaf → Trusted CA chain — F2 generalises beyond the untrusted fixture. Validator note: insecure and overridden share the red open-lock glyph and differ by label text (the spec's one-vocabulary intent; not a defect).

### Checkpoint 14 — INCONCLUSIVE (automation) → operator by-eye: FAIL (#216 persists)
- **Actions taken**: openTab about:blank → wcId 25; `#address.focus()`; value set; REAL pressKey Enter → {failed, ERR_CONNECTION_REFUSED (-102), none}; focus reads after failure, after F6, after Tab.
- **Raw state**: a11y/activeElement sequence textbox → heading "Connection refused" → heading (F6) → button Retry (one Tab) — but `document.hasFocus()` FALSE at every reading → `[BLOCKED:apparatus-focus]` (squawk 0076 rule applied for the first time).
- **Expected**: focused node is the panel heading; F6 heading; Tab reaches Retry; `[a11y]`; HAT-only: visible ring on Retry.
- **Validator (automation)**: inconclusive — escalated for by-eye confirmation.
- **Operator by-eye**: typed `http://127.0.0.2:43023/` + Enter → panel appears with NO focus ring; F6 and Tab do nothing visible. "#216 still." → **FAIL**: leg 1's #216 fix (`chromeNavPending` + reactive chrome-blur reassert) does NOT resolve the live symptom on this rig; the unit pins hold but the model they pin is incomplete.
- **Evidence**: `step-14-focus-0.txt`, `step-14-focus-1.txt`, `step-14-focus-2.txt`, `step-14-focus-tab-1.txt`, `step-14-screenshot.png`, `step-14-census.json`

### Checkpoint 15 — INCONCLUSIVE (automation) → operator by-eye: PASS
- **Actions taken**: navigate wcId 25 → https://localhost:42525/ → cert-blocked (ERR_CERT_AUTHORITY_INVALID); F6; Tab ×3 → Retry, View certificate, Advanced (hasFocus false throughout); pressKey Enter → `sheetVisible: false` (the card did NOT open under key injection); tab-switch setup.
- **Expected**: fresh interstitial with either cert error; keyboard reaches Advanced; Enter opens the card.
- **Validator (automation)**: inconclusive — Enter-on-button under injection is not distinguishable from the OS-focus condition.
- **Operator by-eye**: after first clicking the panel to get focus (the #216 workaround), F6 → Tab → Tab → Tab landed a visible ring on Advanced, Enter OPENED the card with focus on Back to safety, Escape closed it. → **PASS**; the injected-Enter miss is an apparatus gap (`pressKey` Enter activates the address bar's keydown listener but not a `<button>`'s activation path — likely the missing `char` event), recorded for the crew file.
- **Evidence**: `step-15-snapshot.txt`, `step-15-focus-0.txt`, `step-15-focus-tab-1/2/3.txt`, `step-15-windows.json`, `step-15-windows-after.json`

## Orchestrator Notes

- **Mode**: live two-agent mode (`SendMessage` available); cache mode `cold`. Batched Executor turns were used for re-runs of already-passed checkpoints after each relaunch (1–3, 1–5) and for adjacent low-coupling rows (4–5, 7–9, 10–13, 14–15); the Validator judged each row individually.
- **Rig**: `GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation` (WSLg, ozone wayland), WITHOUT `--insecure-tls-fixtures`; admin key captured by script into a chmod-600 scratchpad file and loaded only via `GOLDFINCH_MCP_ADMIN_KEY`; never printed. Fixtures: untrusted TLS `{T}`=42525, trusted TLS `{T2}`=42279 (`--cert-set trusted`), HTTP `{P}`=42443 on `127.0.0.2`, refused `{Q}`=43023. The trusted CA was imported into `~/.pki/nssdb` before launch and removed at cleanup (`certutil -L` confirmed).
- **Three out-of-band relaunches** (after F1, F2, F3), each with a fresh key at the same path, the restored failed tab closed, and both agents rebriefed; the Executor verified each new process by PID.
- **Operator interventions**: CP2 fail → fix + relaunch + resume from 1; CP3 fail → same; CP6 fail → fix + relaunch + redo 1–6 (second Proceed click); CP9 fail → fix now, continue, verify at HAT; CP14/15 → by-eye confirmation, then Known Issue (#216).
- **Rendered observable**: `captureScreenshot(chromeWcId)` for chrome-DOM panels (hidden guest), `captureWindow` once the guest is visible (steps 6/9/10/11) — the Validator's correction after the first CP6 pass.
- **Settle-before-read ruling** (from CP9): chip and census reads only after `security` settles; transient reads saved separately. The Executor overwrote `step-9-census.json` with the settled read (noted).
- **Apparatus findings for the crew file**: (a) `pressKey Enter` on the chrome activates the address bar's keydown path but NOT a `<button>` (the card did not open under injection; the operator's real Enter did) — likely the missing `char` event; (b) the `navigate` drive op returns `isError:true` with the net error text for a blocked TLS load — the tab state is the observable; (c) the `document.hasFocus()` escalation rule (squawk 0076) fired for the first time and did its job: the a11y/activeElement agreement was a false positive.
- **Product findings**: F1 (`load-failure-controller.js` push handler skipped the chip update while the address bar held focus), F2 (`certificate-summary.js` chain walk skipped the leaf and duplicated a self-signed CA), F3 (`site-security-controller.js` `tab-security` handler stored but never refreshed the chip; main pushes `tab-did-navigate` first), F4 (`updateAddressChip` fell back to the scheme rule before the first push — a green lock before verification; also the popup's Connection row), #216 (still live; leg 1's `chromeNavPending` fix disarms at `did-fail-load`, before the error document's own commit can re-steal focus — the residual gap leg 1 itself recorded).

## Closing Summaries

### Executor closing

Three relaunches, each independently verified via `ps aux` (new PID + start time) before touching MCP: 535230 → 541203 → 555597. Each fix landed cleanly and held on every subsequent re-run: (1) the chip's `data-security` stopped lagging the census on a cert-blocked tab; (2) the viewer's chain rows stopped duplicating the CA and show the server leaf first; (3) the chip's post-override wording says "not secure — certificate error overridden". Settle-before-read: a census read taken the instant `loadState` flips to `ok` can transiently show a stale `security` and a placeholder title before the `tab-security` push lands — seen once at step 9, never after the settle-poll discipline. The `pressKey` Enter-on-button gap: `document.hasFocus()` read false at all 8 focus readings across steps 14–15 (`[BLOCKED:apparatus-focus]` each time); DOM/AX focus order looked internally correct, but Enter on the focused Advanced button did not open the card — indistinguishable from automation alone between an OS-focus artefact and a button-handler bug; flagged for by-eye verification. The `navigate` tool surfaces a blocked cert navigation as `isError:true` with the error text in `value`. Leftover tabs from earlier runs never interfered. No apparatus gaps; every required observable was reachable through the attach client.

### Validator closing

Five distinct findings, all in the certificate-error/security-indicator surface, with one common shape: the census/backend state was correct at essentially every checkpoint while the RENDERED chrome affordance (chip icon, keyboard focus) lagged or diverged. Rendered-state-over-internal-state did real work: F1, F2, F3 were invisible to DOM-only or census-only checks and surfaced by zooming pixels. F4 (a fresh tab at an already-overridden origin rendered the green lock before the state arrived) is a new false-POSITIVE security indicator — a worse class than a false negative — and should be tracked as a blocker for this spec's status, not folded into known issues. Checkpoints 14/15: #216 confirmed still live by direct operator observation; the `hasFocus()` escalation rule caught it correctly — activeElement/a11y agreement was a false positive at both rows; step 15's "works after a manual click" was judged a failure of the row's premise, not a pass. Spec-quality: add an explicit "no preliminary click" constraint to keyboard-operability rows; keep the icon vocabulary (neutral grey none, green secure, red open lock for insecure/overridden) as the contract once F4 is verified; the chain rendering is confirmed across two CA/cert pairs.

## Operator Notes

- #216: "leave it as a known issue" (the operator will bring look-and-feel opinions to the HAT leg).
- F4 verification and the F1–F4 chip vocabulary are HAT items; the spec's keyboard rows gain the "no preliminary click" clause at the HAT's spec re-author step; the spec stays `draft` until then.
