# Behavior Test Run: mcp-loopback-origin-guard — 2026-06-14-14-28-59

**Spec**: [tests/behavior/mcp-loopback-origin-guard.md](../../mcp-loopback-origin-guard.md)
**Status**: pass
**Started**: 2026-06-14T14:28:59Z
**Completed**: 2026-06-14T14:35:00Z
**Duration**: ~00:06:00
**Executor**: a9e475880bfe1b884
**Validator**: ab9e6b44b9a6bd9fd
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/mcp-loopback-origin-guard/2026-06-14-14-28-59/` during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. SC7's transport-defense half is behavior-test-backed: the server binds loopback-only and the Origin/Host guard denies (403) every non-loopback Host, non-loopback Origin, and the DNS-rebinding shape, while allowing legitimate loopback (incl. port-mismatched loopback) requests through to the SDK. A real transport defect was surfaced in the course of Step 7 (see Orchestrator Notes / Finding) but it does not affect the guard verdict.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: `ss -tlnp | grep 7777`
- **Raw state**: `LISTEN 0 511 127.0.0.1:7777 0.0.0.0:* (electron pid 543725)` — single IPv4 listener bound to loopback; no `0.0.0.0:7777`, no `[::]:7777`.
- **Expected**: a listener on `127.0.0.1:7777` specifically, not `0.0.0.0`/`[::]`.
- **Verdict**: pass — Validator independently re-ran `ss`; bind is loopback-only (the `0.0.0.0:*` is the remote-peer column, not the bind address).
- **Evidence**: `step-1-ss.txt`

### Checkpoint 2 — PASS
- **Actions taken**: `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7777/mcp` (loopback, no Origin)
- **Raw state**: `406`
- **Expected**: NOT 403 (guard allowed); a non-403 4xx from the SDK is acceptable.
- **Verdict**: pass — 406 ≠ 403; the request passed the guard and reached the SDK (which rejected the bare-GET shape downstream). Independently reconfirmed.
- **Evidence**: `step-2-curl.txt`

### Checkpoint 3 — PASS
- **Actions taken**: `curl … -H 'Host: evil.example' …`
- **Raw state**: `403`
- **Expected**: 403 (Host-loopback clause denies).
- **Verdict**: pass — independently reconfirmed 403.
- **Evidence**: `step-3-curl.txt`

### Checkpoint 4 — PASS
- **Actions taken**: `curl … -H 'Origin: http://evil.example' …` (Host stays loopback)
- **Raw state**: `403`
- **Expected**: 403 (Origin-present-non-loopback clause denies, isolated from Host).
- **Verdict**: pass — independently reconfirmed 403.
- **Evidence**: `step-4-curl.txt`

### Checkpoint 5 — PASS
- **Actions taken**: `curl … -H 'Host: 127.0.0.1:7777' -H 'Origin: http://attacker.example' …`
- **Raw state**: `403`
- **Expected**: 403 — the load-bearing SC7 / DNS-rebinding control (Origin clause defeats a loopback-Host rebind).
- **Verdict**: pass — independently reconfirmed 403. The Origin clause fires even though Host is loopback.
- **Evidence**: `step-5-curl.txt`

### Checkpoint 6 — PASS
- **Actions taken**: `curl … -H 'Host: 127.0.0.1:9999' …` (loopback host, mismatched port)
- **Raw state**: `406`
- **Expected**: NOT 403 — deliberate port-agnostic allow (`bareHost` strips the port; DD3 keys on loopback-ness of the host, not the port).
- **Verdict**: pass — 406 ≠ 403; allowed past the guard as designed. Independently reconfirmed.
- **Evidence**: `step-6-curl.txt`

### Checkpoint 7 — PASS (primary observable) — supplementary observable uncapturable
- **Actions taken**: attempted enumerateTabs (T0) via a fresh MCP client → connect failed; fired the guard-denied request (`Host: evil.example`) → 403; attempted enumerateTabs (T1) → connect failed identically.
- **Raw state**: the **403 deny** (primary HTTP observable) was obtained and independently reconfirmed. T0/T1 could **not** be captured: a fresh `initialize` returns `-32600 "Server already initialized"`; `ss -tnp | grep 7777` shows no live TCP session holder.
- **Expected**: `[mixed-frame]` — primary observable is the 403 deny; supplementary is T1==T0 (no side effect).
- **Verdict**: pass — per the `[mixed-frame]` rule the primary observable (403, reconfirmed) carries the verdict; the request was 403'd at the guard *before* any SDK/tool dispatch, so tab state logically cannot have changed. The supplementary T0/T1 comparison is **missing due to a transport defect** (see Finding), not contradictory evidence — not downgraded to inconclusive because the load-bearing 403 is present and uncontradicted.
- **Evidence**: `step-7-enumerate.json`, `step-7-blocker-diagnosis.txt`
- **Validator notes**: to make the supplementary observable capturable in future runs, the transport needs a reusable session handle or a stateless mode so enumerateTabs can be sampled before/after the denied request.

## Orchestrator Notes

- **Mode**: re-spawn (SendMessage unavailable). For this fully-deterministic shell/http/mcp spec, run as one Executor (performed all 7 steps, per-step structured reports) + one independent Validator (judged all 7, independently re-running each curl). The Witnessed separation (actor ≠ judge, both independent of the orchestrator) is preserved; the per-checkpoint re-spawn was collapsed because the steps are idempotent deterministic probes against shared external state. Cache mode: cold (trivially satisfied — fresh curl connections, no browser).
- **FINDING (real defect, surfaced live; does NOT affect this spec's verdict)**: the MCP server's single shared **stateful** `StreamableHTTPServerTransport` (`src/main/automation/mcp-server.js`) binds **one session for the app's lifetime** and does not support reconnection or a second client. After the first client connects and disconnects, a fresh `initialize` is refused with `-32600 "Server already initialized"` and the original session id is unrecoverable (`-32001 "Session not found"`). Both crew agents independently confirmed this. The earlier V2/V3 example-client run burned the single session, which is why the Step-7 enumerateTabs probe could not connect. **Impact**: SC7 (this spec) is unaffected — the guard works. But SC6's `mcp-drive-end-to-end` run requires a fresh session (app restart) to connect, and real consumers that reconnect/multi-connect are locked out until restart. Escalated to the Flight Director for a fix decision before Leg 6 lands.

## Closing Summaries

### Executor closing
Steps 1–6 executed cleanly with definitive observations. Step 7 hit an apparatus blocker rooted in the server's single stateful transport being stuck "already initialized" from a prior client this app-run, with no recoverable session id and no live session holder; captured full diagnostics and the 403 for the guard-denied request. The no-side-effect property is inferable from the code path (guard 403s before the SDK) but was not measurable as a T0/T1 diff.

### Validator closing
All six curl/bind observations independently reconfirmed; the guard is correct across the full Host/Origin/rebinding/port matrix. Step 7's primary 403 observable holds; the supplementary tab-state comparison is a known apparatus gap caused by the stateful transport refusing a second session — recommend a reusable session handle or stateless mode so future runs can sample tab state around a denied request.

## Operator Notes

The guard (SC7 transport half) is verified. The transport single-session/no-reconnect defect is the actionable outcome of this run — carried to the Flight Director's Leg-6 fix decision.
