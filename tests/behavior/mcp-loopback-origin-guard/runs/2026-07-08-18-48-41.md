# Behavior Test Run: mcp-loopback-origin-guard — 2026-07-08-18-48-41

**Spec**: [tests/behavior/mcp-loopback-origin-guard.md](../../mcp-loopback-origin-guard.md)
**Status**: pass
**Started**: 2026-07-08T23:48:41Z
**Completed**: 2026-07-08T23:49:05Z
**Duration**: 00:00:24
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — raw HTTP status + MCP tab-state payloads persisted)

## Summary
7 / 7 steps passed. 0 failed; 0 inconclusive.

Apparatus: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899` (loopback), driven by `curl` (http frame) + `ss` (bind frame) + the Goldfinch admin/jar MCP client (side-effect frame). `chrome-devtools` MCP not used.

## Step Results

### Step 1 — PASS
- **Actions taken**: `ss -tlnp | grep 8899`; checked for any `0.0.0.0` / `::` wildcard bind on 8899.
- **Raw state**: `LISTEN 0 511 127.0.0.1:8899 0.0.0.0:* users:(("electron",pid=3636434))`. No `0.0.0.0:8899` / `[::]:8899` listener found.
- **Expected**: A listener on `127.0.0.1:$PORT` specifically — not `0.0.0.0` and not `::`.
- **Verdict**: pass — the server binds loopback-only. SC7 bind check + peer-address-clause proxy satisfied.
- **Evidence**: [step1-bind.txt](./2026-07-08-18-48-41/step1-bind.txt)

### Step 2 — PASS
- **Actions taken**: `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8899/mcp` (loopback Host auto, no Origin).
- **Raw state**: HTTP **401**.
- **Expected**: NOT 403 — the guard allowed it (a non-403 4xx from the SDK/auth layer still proves the guard passed).
- **Verdict**: pass — 401 ≠ 403; the request passed the origin guard and was then rejected by the auth gate (no key), which is the expected downstream behavior, not an origin verdict.
- **Evidence**: [step2-loopback-no-origin.status](./2026-07-08-18-48-41/step2-loopback-no-origin.status)

### Step 3 — PASS
- **Actions taken**: `curl -H 'Host: evil.example' … /mcp` (curl replaces the auto Host).
- **Raw state**: HTTP **403**.
- **Expected**: 403 — the Host-loopback clause fired.
- **Verdict**: pass — non-loopback Host denied despite the socket connecting to 127.0.0.1.
- **Evidence**: [step3-nonloopback-host.status](./2026-07-08-18-48-41/step3-nonloopback-host.status)

### Step 4 — PASS
- **Actions taken**: `curl -H 'Origin: http://evil.example' … /mcp` (Host stays loopback).
- **Raw state**: HTTP **403**.
- **Expected**: 403 — the Origin-present-non-loopback clause fired, isolated from the Host clause.
- **Verdict**: pass.
- **Evidence**: [step4-nonloopback-origin.status](./2026-07-08-18-48-41/step4-nonloopback-origin.status)

### Step 5 — PASS
- **Actions taken**: `curl -H 'Host: 127.0.0.1:8899' -H 'Origin: http://attacker.example' … /mcp` (DNS-rebinding shape: loopback Host, hostile Origin).
- **Raw state**: HTTP **403**.
- **Expected**: 403 — the Origin clause defeats the rebinding shape even though the Host passes the loopback test. Load-bearing SC7 control.
- **Verdict**: pass.
- **Evidence**: [step5-dns-rebind.status](./2026-07-08-18-48-41/step5-dns-rebind.status)

### Step 6 — PASS
- **Actions taken**: `curl -H 'Host: 127.0.0.1:9999' … /mcp` (mismatched port, still loopback host).
- **Raw state**: HTTP **401** (not 403).
- **Expected**: NOT 403 — allowed; `bareHost` strips the port so `127.0.0.1:9999` normalizes to loopback. Port-agnostic rule (DD3).
- **Verdict**: pass — 401 ≠ 403; guard allowed the mismatched-port loopback host, auth gate then returned 401.
- **Evidence**: [step6-loopback-mismatch-port.status](./2026-07-08-18-48-41/step6-loopback-mismatch-port.status)

### Step 7 — PASS
- **Actions taken**: legitimate jar MCP client `enumerateTabs` (T0); fired the Step-3 denied shape (`Host: evil.example` → 403); `enumerateTabs` again (T1).
- **Raw state**: T0 = T1 = `[{"wcId":2,"url":"https://www.google.com/","title":"Google","jarId":"default","active":true}]`; denied-request status 403; `EQUAL true`.
- **Expected**: T1 equals T0 — the 403'd request reached no tool (no state change).
- **Verdict**: pass — the guard runs ahead of MCP dispatch; the denied request produced no observable side effect.
- **Evidence**: [step7-sideeffect.txt](./2026-07-08-18-48-41/step7-sideeffect.txt)

## Orchestrator Notes
Single-agent Witnessed self-run under Flight-05 leg-04 (SC6 parity sweep on the native WebContentsView surface). Every load-bearing assertion persisted as a raw HTTP status file / MCP payload capture under the evidence dir, judged from artifacts rather than prose. The peer-address deny clause is unit-backed + bind-proxied per the spec's Scope-honesty note, not behavior-tested here.

## Closing Summaries

### Executor closing
Clean run. Bind is loopback-only; all three deny clauses reachable via curl (Host, Origin, rebinding shape) returned 403; both pass clauses returned 401 (auth-gate downstream), correctly ≠ 403; the side-effect probe confirmed the guard is ahead of the SDK.

### Validator closing
No spec drift. The spec's "assert not-403, not 200" guidance was necessary: bare `/mcp` requests draw 401 from the auth gate, which the run correctly distinguished from the guard's 403.

## Operator Notes
Run on WSLg; port pinned to 8899 for the leg-04 batch.
