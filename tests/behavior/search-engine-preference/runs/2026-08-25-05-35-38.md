# Behavior Test Run: search-engine-preference — 2026-08-25-05-35-38

**Spec**: [search-engine-preference.md](../search-engine-preference.md)
**Status**: partial (7 / 7 automated checkpoints pass; checkpoint 6 operator-performed, pending)
**Started**: 2026-08-25T05:35:38Z
**Completed**: 2026-08-25T05:55:37Z
**Duration**: 00:19:59
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/search-engine-preference/2026-08-25-05-35-38/` during the run; re-derivable by re-running the spec.

## Summary

7 / 7 automated checkpoints passed (steps 1–5, 7, 8). 0 failed; 0 inconclusive. Checkpoint 6 (page-context "Search for …") is operator-performed by spec and awaits the operator's hand check on the leg-2 build. Against the leg-2 build with the re-authored fixture (engine Google, home page unset): the Settings control shows Google selected and the home page field empty with its unset hint; selecting DuckDuckGo leaves the home page untouched; address-bar searches from a new welcome tab (step 4), a second window (step 5), and after a full quit/relaunch (step 8) all commit to `duckduckgo.com` with the encoded query; setting the home page leaves the engine untouched; and both preferences survive restart independently.

## Checkpoint Results

### Setup row (fixture, re-authored by M16 F2 leg 2) — completed, no judgment
- **Actions taken**: Settings via the seam; clicked the Google radio; row read `searchEngine: "google"`, `homePage: null` (home page deliberately left unset).
- **Evidence**: `step-0-settings.png`, `step-0-settings-row.json`

### Checkpoint 1 — PASS
- **Actions taken**: On the Settings tab: `captureWindow`, guest `captureScreenshot`, `readAxTree`, `enumerateTabs`.
- **Raw state**: Home page control (empty field, Save/Clear, unset hint) with the Search engine group directly beneath: 8 radios with descriptions, only Google checked (a11y and pixels agree). Strip: the boot welcome tab plus the active Settings tab.
- **Expected**: A search engine control is visible adjacent to the home page control, with Google selected explicitly. Each engine option carries a one-line description. [a11y]
- **Verdict**: pass — the engine group sits directly beneath the home-page control, Google the only filled radio (a11y agrees), one-line descriptions on every option.
- **Evidence**: `step-1-screenshot.png`, `step-1-screenshot-2.png`, `step-1-snapshot.txt`, `step-1-tabs.json`
- **Validator notes**: accessible names and checked states exposed correctly; descriptions rendered as visible static text (aria-describedby association not traced).

### Checkpoint 2 — PASS
- **Actions taken**: Activated the boot welcome tab; chrome address-bar search `zephyr quartz alpha`; after-probe, `enumerateTabs`, `#address.value`, `captureWindow`, `readAxTree`.
- **Raw state**: The welcome tab navigated in place (acquired wcId 4; no new tab). Goldfinch committed `https://www.google.com/search?q=zephyr%20quartz%20alpha&sei=…` (in the tab title, the interstitial’s body text, and `continue=`); Google served its `/sorry/` anti-automation interstitial, which the address bar shows.
- **Expected**: The tab navigates to a Google search URL; the address bar shows a `google.com` search URL containing the encoded query.
- **Verdict**: pass — carried by the rendered tab title and the interstitial’s own body text, both showing the committed `www.google.com/search?q=zephyr%20quartz%20alpha` URL; the `/sorry/` page is Google’s environmental noise, not the system under test.
- **Evidence**: `step-2-before.json`, `step-2-probe.json`, `step-2-tabs.json`, `step-2-screenshot.png`, `step-2-snapshot.txt`
- **Validator notes**: consider tightening the row to accept a SERP or an interstitial whose `continue=` decodes to the matching search URL (the same note applied to `search-engine-upgrade` step 3).

### Checkpoint 3 — PASS
- **Actions taken**: Activated Settings; clicked the DuckDuckGo radio from a fresh guest capture; captured guest + window screenshots and the a11y tree; supplementary row read.
- **Raw state**: DuckDuckGo the only checked radio (pixels and a11y); the home page field still empty with its unset hint; row `searchEngine: "duckduckgo"`, `homePage: null`.
- **Expected**: The control shows DuckDuckGo selected. The home page field's value is unchanged.
- **Verdict**: pass — DuckDuckGo the only selected radio; the home page control pixel-identical to its step-1 rendering (empty, same hint).
- **Evidence**: `step-3-locate.png`, `step-3-screenshot-2.png`, `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-settings-row.json`
- **Validator notes**: judged the field from rendered pixels, per the apparatus notes.

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+T (new tab opened as a welcome tab — home page unset), clicked the address bar, typed `zephyr quartz beta`, Enter; before/after probes, `enumerateTabs`, window screenshot, a11y tree.
- **Raw state**: Address bar `https://duckduckgo.com/?q=zephyr+quartz+beta&ia=web` (read twice); the welcome tab attached a view and navigated in place (3 tabs before and after); live DuckDuckGo results rendered; same window, no restart.
- **Expected**: The address bar shows a `duckduckgo.com` search URL containing the encoded query — no restart occurred.
- **Verdict**: pass — address bar `https://duckduckgo.com/?q=zephyr+quartz+beta&ia=web` with live results rendered; tab-list lineage (same windowId, wcId 4 unchanged down to its ephemeral query token, wcId 2 intact, welcome tab attaching as wcId 5) shows no relaunch.
- **Evidence**: `step-4-before.json`, `step-4-probe.json`, `step-4-tabs.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`
- **Validator notes**: lineage alone judged sufficient here; asked for an explicit PID attestation on restart-adjacent steps (supplied from step 5 on).

### Checkpoint 5 — PASS
- **Actions taken**: Ctrl+N from window 1; located the new window (windowId 2, chrome wcId 6) via `enumerateWindows`; before probe on its chrome; clicked its address bar, typed `zephyr quartz gamma`, Enter; after probe, direct value read, window screenshot, a11y tree; global `enumerateTabs`.
- **Raw state**: Window 2 opened on a welcome tab showing only the Home block (engine already set); after commit its address bar read `https://duckduckgo.com/?q=zephyr+quartz+gamma&ia=web` (probe and direct read identical), live results rendered; window 1's three tabs unchanged.
- **Expected**: The second window's address bar shows a `duckduckgo.com` search URL containing the encoded query — the choice reached a window that was not the one that set it.
- **Verdict**: pass — window 2 (a distinct windowId/chrome wcId) rendered live DuckDuckGo results at `https://duckduckgo.com/?q=zephyr+quartz+gamma&ia=web`; window 1 did not perform the search; same PID throughout rules out a relaunch as the propagation path.
- **Evidence**: `step-5-windows.json`, `step-5-before.json`, `step-5-locate.png`, `step-5-probe.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-tabs.json`
- **Validator notes**: clean pass; the PID attestation supplied the corroboration requested at checkpoint 4.

### Checkpoint 6 — OPERATOR-PENDING (operator-performed row, not run in this session)
- **Actions taken**: none — the page-context "Search for …" sheet is outside `AUTOMATABLE_MENU_TYPES` (squawk 0007 re-authored this row as operator-performed).
- **Expected**: A tab opens on a `duckduckgo.com` search URL containing the selected phrase — the context-menu entry point uses the same chosen engine.
- **Verdict**: not rendered — pending the operator's hand check (the 2026-08-24 debrief verified it by hand on Flight 1's build; Flight 2 did not change the `sel:search` → `toUrl` path when an engine is set).
- **Evidence**: —

### Checkpoint 7 — PASS
- **Actions taken**: Activated Settings; typed `https://example.com` into the Home page field; re-captured before clicking Save; clicked Save; captured guest + window screenshots, a11y tree, supplementary row read.
- **Raw state**: Home page field `https://example.com` with the hint now reading "Saved" (the unset hint gone); a11y exposes the value; DuckDuckGo still the only checked radio; row `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`.
- **Expected**: The home page field shows the new value; the search engine control still shows DuckDuckGo — setting one never changes the other.
- **Verdict**: pass — rendered Settings shows `https://example.com` with "Saved" beneath and DuckDuckGo still the only selected radio; the persisted row carries both values together, so saving one left the other untouched.
- **Evidence**: `step-7-locate.png`, `step-7-presave.png`, `step-7-screenshot-2.png`, `step-7-screenshot.png`, `step-7-snapshot.txt`, `step-7-settings-row.json`
- **Validator notes**: clean pass.

### Checkpoint 8 — PASS
- **Actions taken**: (Quit/relaunch performed by the Orchestrator — see Orchestrator Notes.) Re-sourced the re-minted key; verified the restored topology; opened Settings via the seam (guest wcId 6) and captured screenshot, a11y tree, supplementary row; Ctrl+T (opened a regular tab on `https://example.com/` — a home page is now set, so no welcome tab); typed `zephyr quartz delta`, Enter; after probe, direct value read, window screenshot, a11y tree.
- **Raw state**: Session restore rebuilt both windows. Settings after relaunch: home page `https://example.com`, DuckDuckGo the only checked radio; row `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`. The new tab navigated in place to `https://duckduckgo.com/?q=zephyr+quartz+delta&ia=web` with live results rendered.
- **Expected**: Settings shows DuckDuckGo selected and `https://example.com` as home page; the search commits to a `duckduckgo.com` URL — both preferences survived restart independently.
- **Verdict**: pass — post-relaunch Settings renders `https://example.com` and DuckDuckGo selected; the search rendered live results at `https://duckduckgo.com/?q=zephyr+quartz+delta&ia=web`; the persisted row carries both values into the new process. The restart was real: PID attestation plus two independent signals (the row’s `automationKeyHashes` changed between steps 7 and 8; wcIds renumbered under session restore).
- **Evidence**: `step-8-topology.json`, `step-8-settings.png`, `step-8-settings-snapshot.txt`, `step-8-settings-row.json`, `step-8-probe.json`, `step-8-screenshot.png`, `step-8-snapshot.txt`
- **Validator notes**: the re-minted key hash and wcId renumbering corroborate the attested quit+relaunch beyond the PID claim.


## Orchestrator Notes

- **Why this re-run**: the spec's fixture row was re-authored by M16 F2 leg 2 (set the engine to Google explicitly; leave the home page unset) because Flight 2 makes both preferences unset by default and routes unset-preference new tabs to the chrome-owned welcome surface. This run is the leg's acceptance re-run of the Flight-1 spec against the leg-2 build; the prior `2026-08-24` run remains the Flight-1 record.
- **Execution mode**: live two-agent mode (SendMessage continuation available); cache mode `cold` — fresh `xdg-sep` profile, fixture applied immediately before the run. Crew: Executor and Validator both Sonnet per the project crew file; Accessibility Validator not spawned (project has it disabled); the base Validator judged the `[a11y]` row functionally.
- **Apparatus**: attach-only CLI over `scripts/lib/mcp-client.mjs` with this instance's freshly minted admin key (port 49707); session-registered goldfinch MCP tools forbidden to both agents (they point at a different live browser). Settings reached via the chrome `kebabActionSettings` seam. On the leg-2 build, Ctrl+T with the home page unset opens a **welcome tab** (no webContents until a navigation attaches one) — steps 4 and 5 therefore exercised the welcome-tab → in-place attach path rather than a home-page tab, and both committed to `duckduckgo.com` with the tab count unchanged. That is a stronger reading of the row than the Flight-1 run, not a deviation.
- **Restart attestation**: the Validator asked (checkpoint 4) for an explicit process attestation on restart-adjacent rows. Supplied from checkpoint 5 on: Electron main PID 2075192 (started 00:35:25) held the profile's `app.db` unchanged through checkpoint 7; for step 8 the Orchestrator sent it SIGTERM (clean exit in ~2 s), relaunched `npm run dev:automation` against the same `xdg-sep` profile at 00:52:09 (new PID 2079491), re-minted the automation key, and re-briefed the Executor on the restored topology (both windows restored by session restore; wcIds renumbered).
- **Checkpoint 6** is operator-performed by spec (squawk 0007) and was not run; disposition is recorded on the leg as an operator hand check pending, same as the sibling `welcome-search-handoff` step 7.
- **Google `/sorry/` interstitial** on the step-2 scripted search again; judged (as on the 2026-08-25 `search-engine-upgrade` run) on the committed address-bar URL plus the rendered title/`continue=` target. DuckDuckGo commits rendered live results with no interstitial.

## Closing Summaries

### Executor closing
_Not collected — the Executor's agent transcript was no longer resumable when `[CLOSING]` was sent after its step-8 report (the same transcript-loss failure seen on the Flight-1 run of this spec). Its final per-step notes stand in:_ topology after the relaunch matched the Orchestrator's attested facts exactly; on the leg-2 build the step-8 search landed on the tab Ctrl+T had opened on the home page (`https://example.com`), whereas steps 2/4/5 started from a welcome tab — both are "the same tab navigates in place", from different starting states. No fallbacks were needed on the second window's chrome (`evaluate` allowed on wcId 6); the Settings layout did not shift between typing and Save, but re-capturing before the click remains cheap insurance.

### Validator closing
All seven judged checkpoints (1–5, 7, 8) passed on rendered evidence; step 6 remains operator-pending as designed. Notes for future runs: (1) the Google `/sorry/` interstitial appeared as flagged and the tab-title/`continue=` fallback was sufficient — keep it documented permanently rather than re-discovering it; (2) restart verification improved mid-run — step 4 relied on tab-list lineage alone, steps 5 and 8 carried an explicit Orchestrator PID attestation, which should be the default for any restart/no-restart judgment; (3) the home-page field's a11y `value` is unreliable — judged every home-page check from rendered pixels, per the apparatus notes, and that should stay a standing instruction; (4) the re-authored fixture (engine pre-set to Google) worked cleanly, and the Flight-2 welcome-tab routing did not interfere — new tabs and the second window's first tab were chrome-rendered welcome tabs that navigated in place, exactly as briefed. No spec-quality gaps or evidence losses; no inconclusive verdicts.

## Operator Notes
_None yet — checkpoint 6 (context-menu search) awaits the operator's hand check on the leg-2 build._
