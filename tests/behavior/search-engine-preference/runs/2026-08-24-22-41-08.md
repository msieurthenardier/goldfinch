# Behavior Test Run: search-engine-preference — 2026-08-24-22-41-08

**Spec**: [search-engine-preference.md](../search-engine-preference.md)
**Status**: partial
**Started**: 2026-08-24T22:41:08Z
**Completed**: 2026-08-24T23:15:03Z
**Duration**: 00:33:55
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/search-engine-preference/2026-08-24-22-41-08/` during the run; re-derivable by re-running the spec.

## Summary

7 / 8 checkpoints passed. 0 failed; 1 inconclusive (checkpoint 6 — the page context-menu sheet is not automatable or observable through the MCP apparatus; apparatus gap, not a product observation). Every product claim the apparatus could observe passed: explicit Google default in Settings, immediate effect in the address bar, same-window and cross-window propagation without restart, home-page independence, and survival of both preferences across a clean quit/relaunch.

## Checkpoint Results

### Checkpoint 1 — PASS (on rerun; first attempt aborted on an apparatus gap, see Orchestrator Notes)
- **Actions taken**: Opened `goldfinch://settings` via the chrome renderer's own Settings action (`evaluate` on the chrome wcId → `kebabActionSettings()`, the `globalThis` dogfooding seam that runs `createTab('goldfinch://settings', null, { trusted: true })`). New internal tab wcId 4 activated. Captured whole-window screenshot, guest screenshot, and a11y tree of the settings tab.
- **Raw state**: Startup & appearance section rendered without scrolling. Home page text input (`https://www.google.com`) + Save button; directly beneath, a "Search engine" fieldset with 8 radios (DuckDuckGo, Brave Search, Startpage, Mojeek, Qwant, Ecosia, Google, Bing), each with a bold name and a one-line grey description. Google radio visibly filled; a11y tree: Google radio checked `true`, all seven others `false`.
- **Expected**: A search engine control is visible adjacent to the home page control, with Google selected explicitly. Each engine option carries a one-line description. [a11y]
- **Verdict**: pass — rendered screenshot shows the fieldset directly below the home-page control in the same section, Google selected, one-line descriptions on every option; a11y snapshot corroborates (group role named "Search engine" via legend, labelled home-page textbox in the same parent container).
- **Evidence**: `step-1-screenshot.png`, `step-1-screenshot-2.png`, `step-1-snapshot.txt`, `step-1-tabs.json` (attempt-1 captures preserved with an `-attempt1` suffix)
- **Validator notes**: a11y looks reasonable (fieldset has group role + accessible name via `<legend>`; home-page textbox labelled). Per-option description text is a fairly low-contrast grey-on-dark — legible at normal size, possibly marginal for low-vision users.
- **Operator decision**: rerun-checkpoint (Orchestrator, autonomous — apparatus route, not a system fault)

### Checkpoint 2 — PASS
- **Actions taken**: Activated the Google web tab (wcId 2); clicked the chrome address bar (`#address`), Ctrl+A, `typeText` `zephyr quartz alpha` through the real input path, `pressKey` Enter. Captured tab record, whole-window screenshot, chrome a11y tree.
- **Raw state**: Tab committed to `https://www.google.com/search?q=zephyr%20quartz%20alpha&sei=…`, title "zephyr quartz alpha - Google Search". Rendered address bar shows the same URL; the result page loaded live (network resolved). Settings tab unaffected.
- **Expected**: The tab navigates to a Google search URL; the address bar shows a `google.com` search URL containing the encoded query.
- **Verdict**: pass — rendered address bar shows `https://www.google.com/search?q=zephyr%20quartz%20alpha&sei=…`; live results and tab title confirm the tab itself navigated.
- **Evidence**: `step-2-screenshot.png`, `step-2-snapshot.txt`, `step-2-tabs.json`
- **Validator notes**: none — clean pass.

### Checkpoint 3 — PASS
- **Actions taken**: Activated the Settings tab (wcId 4); located the DuckDuckGo radio from a guest-relative screenshot and clicked it (`click` at guest coordinates). No Save clicked. Captured whole-window + guest screenshots, a11y tree, DOM (supplementary).
- **Raw state**: DuckDuckGo radio filled, Google unfilled, all six others unselected; a11y tree: DuckDuckGo checked `true`, all seven others `false`. Home page field still renders `https://www.google.com` (rendered-pixel evidence — the textbox exposes no `value` property in this Chromium a11y snapshot and text inputs don't reflect `.value` to markup).
- **Expected**: The control shows DuckDuckGo selected. The home page field's value is unchanged.
- **Verdict**: pass — DuckDuckGo radio filled and all others unfilled in the rendered screenshot; the home page field is pixel-identical to its step-1 rendering.
- **Evidence**: `step-3-screenshot.png`, `step-3-screenshot-2.png`, `step-3-snapshot.txt`, `step-3-dom.html`
- **Validator notes**: the home-page textbox exposes no queryable `.value` in this Chromium a11y snapshot, so the unchanged-value claim rests on screenshot comparison — the correct approach under the rendered-state rule; no concern.

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+T on the chrome (new tab wcId 5 opened on the home page, `https://www.google.com/`); focused the address bar, Ctrl+A, `typeText` `zephyr quartz beta`, Enter. Captured tab record, whole-window screenshot, chrome a11y tree, `#address.value`.
- **Raw state**: Tab committed to `https://duckduckgo.com/?q=zephyr+quartz+beta&ia=web`, title "zephyr quartz beta at DuckDuckGo"; rendered address bar shows the same; live DuckDuckGo results loaded. Tab strip still holds the step-2 and step-3 tabs. Main process PID unchanged since launch — no restart.
- **Expected**: The address bar shows a `duckduckgo.com` search URL containing the encoded query — no restart occurred.
- **Verdict**: pass — rendered address bar shows the `duckduckgo.com/?q=zephyr+quartz+beta` URL with live results; the tab strip shows all prior tabs still present, corroborating continuity alongside the Orchestrator’s PID attestation.
- **Evidence**: `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-tabs.json`
- **Validator notes**: none — the tab-strip lineage is itself decent rendered-state evidence for the no-restart clause.

### Checkpoint 5 — PASS
- **Actions taken**: Ctrl+N on window 1's chrome → window 2 (windowId 2, chrome wcId 6, tab wcId 7 on the home page). Drove window 2's own address bar: click `#address`, Ctrl+A, `typeText` `zephyr quartz gamma`, Enter — every call targeted window 2's wcIds. Captured tabs, windows, `captureWindow {windowId:2}`, window 2's chrome a11y tree, `#address.value`.
- **Raw state**: Window 2's tab committed to `https://duckduckgo.com/?q=zephyr+quartz+gamma&ia=web` ("zephyr quartz gamma at DuckDuckGo"), rendered in its address bar with live results. `enumerateWindows`: two distinct windows; window 1's three tabs untouched.
- **Expected**: The second window's address bar shows a `duckduckgo.com` search URL containing the encoded query — the choice reached a window that was not the one that set it.
- **Verdict**: pass — `step-5-windows.json` shows two distinct windows; window 2’s own capture renders the `duckduckgo.com/?q=zephyr+quartz+gamma` URL with live results.
- **Evidence**: `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-tabs.json`, `step-5-windows.json`
- **Validator notes**: none — window identity and rendered URL are corroborated by separate evidence files.

### Checkpoint 6 — INCONCLUSIVE (apparatus gap)
- **Actions taken**: Opened `https://example.com` in window 1 (wcId 10); double-clicked "Example" (selection confirmed via `window.getSelection()`); right-clicked the selection — the registry reported the page-context sheet open (`sheetVisible:true`, `sheetWcId:3`). Every op against the sheet (`readAxTree`, `readDom`, `click`, `pressKey` ArrowDown/Enter, `evaluate`) was refused verbatim: `automation: secret-sheet — wcId 3 is a chrome-owned secret/overlay sheet and is never automatable (any tier)`. `captureWindow` did not composite the menu. Dismissed the sheet with a tab switch.
- **Raw state**: No new tab appeared before/during/after (five tabs unchanged). The "Search for …" item was never observed or activated. Executor signalled `[STEP:6:exception]`.
- **Expected**: A tab opens on a `duckduckgo.com` search URL containing the selected phrase — the context-menu entry point uses the same chosen engine.
- **Verdict**: inconclusive — rule 5 carried: the right-click reached the app and the page-context sheet opened (registry `sheetVisible:true`), but the automation surface refuses every op against that sheet type by design and `captureWindow` does not composite it, so the "Search for …" item could neither be driven nor observed — a structural apparatus gap, not evidence the feature is broken. The absence of a new tab is explained by the item never being clickable, not by clicking it and getting the wrong engine.
- **Evidence**: `step-6-screenshot-before.png`, `step-6-screenshot-menu.png`, `step-6-refusals.txt`, `step-6-tabs-before.json`, `step-6-tabs-during-menu.json`, `step-6-tabs-after.json`, `step-6-windows-after-cleanup.json`
- **Validator notes**: spec-level gap — step 6 assumes the page context menu is automatable/observable via the MCP surface; it is not, by design (`AUTOMATABLE_MENU_TYPES` allowlists only `bookmarks-overflow` and `bookmark-edit`). Options: (a) allowlist page-context overlays for behavior-test purposes, (b) an OS-level input fallback, or (c) rewrite the step to invoke the same command via a driveable path.
- **Operator decision**: continued (Orchestrator, autonomous — steps 7–8 do not depend on step 6; disposition for the leg is recorded in Orchestrator Notes)

### Checkpoint 7 — PASS
- **Actions taken** (replacement Executor — see Orchestrator Notes): verified no state drift; activated the Settings tab; clicked into the Home page input, Ctrl+A, `typeText` `https://example.com`, clicked Save. Captured whole-window + guest screenshots and the a11y tree.
- **Raw state**: Home page input renders `https://example.com` with a "Saved" status line beneath it. Search engine group unchanged — a11y tree: DuckDuckGo checked `true`, all seven others `false`; screenshots corroborate.
- **Expected**: The home page field shows the new value; the search engine control still shows DuckDuckGo — setting one never changes the other.
- **Verdict**: pass — one rendered frame shows the Home page field at `https://example.com` with a "Saved" status and the DuckDuckGo radio still filled, all others unfilled.
- **Evidence**: `step-7-pre-screenshot-2.png`, `step-7-screenshot.png`, `step-7-screenshot-2.png`, `step-7-snapshot.txt`
- **Validator notes**: none — the single screenshot captures both fields simultaneously, so independence is verifiable in one frame.

### Checkpoint 8 — PASS
- **Actions taken**: *Out-of-band (Orchestrator)*: confirmed the Electron main PID held the scratch profile's `app.db`, sent SIGTERM, process exited in ~1s (exit 0); on-disk settings row read post-quit: `version 3`, `homePage: https://example.com`, `searchEngine: duckduckgo`. Relaunched `dev:automation` against the same `XDG_CONFIG_HOME`; new admin key minted; MCP re-bound on 49707; session restore recreated both windows (new wcIds). *Executor*: re-sourced the new key; verified the restored topology (no drift); opened Settings via the `kebabActionSettings` seam (new tab wcId 7); captured whole-window + guest screenshots and the a11y tree; then on the example.com tab focused the chrome address bar, Ctrl+A, `typeText` `zephyr quartz delta`, Enter; captured tabs, whole-window screenshot, chrome a11y tree, `#address.value`.
- **Raw state**: Settings (relaunched process): Home page field renders `https://example.com`; a11y tree: DuckDuckGo checked `true`, all seven others `false`; screenshot shows DuckDuckGo filled. Address-bar search committed to `https://duckduckgo.com/?q=zephyr+quartz+delta&ia=web` ("zephyr quartz delta at DuckDuckGo"), rendered in the address bar with live results.
- **Expected**: Settings shows DuckDuckGo selected and `https://example.com` as home page; the search commits to a `duckduckgo.com` URL — both preferences survived restart independently.
- **Verdict**: pass — post-relaunch Settings screenshot shows `https://example.com` and the DuckDuckGo radio filled; the search screenshot shows the `duckduckgo.com/?q=zephyr+quartz+delta` URL with live results. The pre-relaunch on-disk row read plus the SIGTERM/exit-0 record make this a genuine restart survival, not in-memory state.
- **Evidence**: `step-8-screenshot.png`, `step-8-screenshot-2.png`, `step-8-snapshot.txt`, `step-8-tabs.json`, `step-8-screenshot-3.png`, `step-8-snapshot-2.txt`
- **Validator notes**: none — good chain of custody (pre-relaunch DB read → clean exit → post-relaunch rendered state).

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation available). Cache mode `cold` — fresh scratch profile launched for this run; the Executor opened a fresh tab for step 1 rather than reusing the boot tab.
- **Crew**: Executor and Validator both Sonnet, per the project crew file. Accessibility Validator not spawned (project has it disabled); the base Validator judged the `[a11y]` row functionally and noted a11y observations inline.
- **Apparatus**: the crew drove the instance under test ONLY through a small attach-only CLI over `scripts/lib/mcp-client.mjs` with the run's freshly minted admin key (port discovered live — it bound on the configured 49707 this time). The session-registered goldfinch MCP tools were explicitly forbidden to both agents: a prior run established those registrations point at a different live browser (the operator's production instance), and the fresh profile's minted key would not have matched them anyway.
- **Checkpoint 1 rerun**: first attempt could not reach `goldfinch://settings` — `openTab` creates untrusted tabs (internal scheme rejected), `navigate` refuses `goldfinch://` by design, and the kebab menu is a non-automatable sheet. Orchestrator ordered a rerun via the chrome renderer's `kebabActionSettings` seam (on `globalThis`, listed as a dogfooding hook in `src/renderer/renderer.js`) invoked with `evaluate` on the chrome wcId — the same route the last `bookmarks-jar-scoping` run used. Apparatus route, not a system fault. Side-finding confirmed: a tab switch dismisses a stuck sheet; a chrome-targeted Escape does not.
- **Checkpoint 6 disposition**: INCONCLUSIVE on an apparatus gap, not a product observation. The page-context menu is a chrome-owned sheet outside `AUTOMATABLE_MENU_TYPES` (`src/main/automation/resolve.js`), refused for every op at every tier, and `captureWindow` does not composite it; no OS-level input tool exists on this machine (checked `xdotool`/`ydotool`/`wtype`). Orchestrator continued (steps 7–8 independent of 6). Structural coverage stands in for the real-environment observation: the context-menu `sel:search` dispatch calls the identical `toUrl` (`src/renderer/renderer.js`, `page-context` case) that checkpoints 2, 4, 5 and 8 verified end-to-end through the address bar. **The row remains operator-verifiable by hand in seconds** (select text on any page → right-click → "Search for …" → observe a `duckduckgo.com` tab); recommended as a one-line operator check before the flight debrief closes it out, or the spec row should be re-authored to a driveable path. Recorded as a known apparatus limitation for the flight log.
- **Executor re-spawn before step 7**: the original Executor's transcript could not be resumed (`No transcript found`) after checkpoint 6. Re-spawned a replacement with the full spec, the established apparatus facts, and the prior steps' outcomes; it verified no state drift before acting. Steps 7–8 are the replacement's.
- **Step 8 out-of-band procedure**: Orchestrator confirmed the main PID owned the scratch `app.db`, sent SIGTERM (exit 0 in ~1s), read the on-disk row (`version 3`, `homePage` and `searchEngine` explicit), relaunched on the same `XDG_CONFIG_HOME`, captured the new `AUTOMATION_DEV_MINT` key, rewrote the crew's env file, and briefed the Executor on the restored topology (session restore recreated both windows; internal Settings tab not restored). Note for the `session-restore` spec: this procedure worked as authored there.
- **Validator spec-level notes at READY** (none blocking): "adjacent" judged as same-section grouping; "encoded query" accepted under any valid URL encoding (Google committed `%20`, DuckDuckGo `+` after its own redirect appended `&ia=web`); process-continuity clauses judged from tab-strip lineage plus the Orchestrator's PID attestation.
- **Apparatus notes for future specs on this surface**: a11y textbox nodes in this Chromium build expose no `value` property — the address bar's committed string is read authoritatively via `evaluate` on the chrome wcId and corroborated by the rendered screenshot; the Settings home-page field's value is judged from rendered pixels. `openTab` lands in the last-focused window — focus the intended window first. Ctrl+T / Ctrl+N via `pressKey` on the chrome wcId work first time.

## Closing Summaries

### Executor closing
(Original Executor, steps 1–6, lost its transcript before closing; its per-step `executor_notes` are folded into the checkpoint records above. Replacement Executor, steps 7–8:) Handoff continuity was clean — the inherited apparatus facts (chrome/tab wcIds, address-bar rect, radio coordinates, click-vs-evaluate rules) were accurate and needed no correction. Step 7: clicking Save (not Enter) produced an immediate "Saved" status; the radio group was unaffected. Step 8: the rotated key worked on first source, the port was unchanged, and the briefed post-relaunch topology matched exactly. Apparatus notes to carry forward: the Settings tab's wcId is not stable across the app lifecycle (session restore does not restore internal tabs — reopen via the `kebabActionSettings()` seam and rediscover via `enumerateTabs`); the Home page textbox a11y node exposes no `value` property in this build (stable quirk — screenshot is the only reliable read); `#address.value` via `evaluate` on the chrome wcId is the authoritative committed-URL read; no sandbox/loopback issues this session. Step 6's context-menu limitation is a standing apparatus constraint that will recur for any step relying on native context menus.

### Validator closing
7/8 passed with clean, mutually corroborating rendered-state evidence (screenshots and a11y snapshots agreed with each other and with `raw_state` at every step); the core hypothesis — the engine preference propagates immediately across entry points and windows, survives restart, and is independent of the home-page preference — is demonstrated. Step 6 is a structural gap, not a "retry" case: `AUTOMATABLE_MENU_TYPES` excludes page context menus and `captureWindow` does not composite them; options are (a) allowlist page-context overlays for behavior-test purposes, (b) an OS-level input fallback, or (c) re-author the step to drive the same command through an automatable path. Pattern flagged: the Home page textbox exposes no queryable value via a11y or DOM serialization, so every "home page changed/unchanged" judgment rested on screenshot comparison — fine under the rendered-state rule, but a blocker if a future spec must assert that value without a legible screenshot. The mid-run Executor re-spawn was indistinguishable in report and evidence quality — a positive resilience signal for the Witnessed pattern. The `[READY]` concerns ("adjacent", "no restart occurred", "a window that was not the one that set it") all proved judgeable in practice. Accessibility: the search-engine fieldset has a `<legend>`-backed group role and real labelled radios; per-option description text is low-contrast grey-on-dark — legible, but worth a look if contrast becomes an explicit criterion.

## Operator Notes

- **2026-08-24, flight debrief — checkpoint 6 operator-verified by hand.** With DuckDuckGo selected, the operator selected text on a page → right-click → "Search for …" and observed a `duckduckgo.com` search tab open. This closes the apparatus-inconclusive row as a pass at the operator-observation level; the run's recorded status stays `partial` (run logs are terminal), and the flight criterion is checked off in `flight.md` with this note as its evidence.
