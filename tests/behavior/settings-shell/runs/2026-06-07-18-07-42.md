# Behavior Test Run: settings-shell — 2026-06-07-18-07-42

**Spec**: [tests/behavior/settings-shell.md](../../settings-shell.md)
**Status**: pass
**Started**: 2026-06-07T23:02:00Z
**Completed**: 2026-06-07T23:07:42Z
**Duration**: ~00:05:42
**Executor**: Flight Director (manual CDP drive via `scripts/cdp-driver.mjs` + node-CDP guest attach)
**Validator**: Flight Director (same) — see Orchestrator Notes re: Witnessed-pattern deviation

## Summary
12 / 12 steps passed. 0 failed; 0 inconclusive. The DD2 CSP-subresource spike (deferred from leg 2) is
**confirmed PASS** as part of this run; the chrome + guest a11y gates are clean; the menu/tab regression
spot-checks and the `tab-scheme-guard` core invariant hold. One a11y regression surfaced and was fixed
mid-run (the `region`/`role="search"` finding — see Step 6 and Orchestrator Notes).

Apparatus per DD8: drove the running app on CDP `:9222` via the committed `scripts/cdp-driver.mjs` and a
node-CDP attach to the `goldfinch://settings` guest (`type: webview` in the flat `/json` list). The
`chrome-devtools` MCP was **not** used (it launches its own browser → false pass).

## Step Results

### Step 1 — PASS
- **Actions taken**: `node scripts/cdp-driver.mjs eval '1+1'`; confirmed a renderer target (`index.html`).
- **Raw state**: `2`; `/json` lists `page file://…/index.html` + webview targets.
- **Expected**: Returns `2`; a Goldfinch renderer present.
- **Verdict**: pass.

### Step 2 — PASS
- **Actions taken**: opened Settings via the kebab handler (`#kebab-settings`.click() → trusted
  `createTab('goldfinch://settings', null, {trusted:true})`).
- **Raw state**: webviews = `[google persist:goldfinch, goldfinch://settings/ goldfinch-internal]`;
  `addressValue="goldfinch://settings/"`, `addressReadonly=true`, `chipState="internal"`.
- **Expected**: a tab opens to `goldfinch://settings`; active partition `goldfinch-internal`; address shows
  the internal URL.
- **Verdict**: pass.

### Step 3 — PASS
- **Actions taken**: node-CDP attach to the `goldfinch://settings` guest; read `<nav>` links + `<section>`/
  `<h2>` set.
- **Raw state**: 5 nav links `[Appearance#appearance, Privacy & Shields#privacy, On startup#startup,
  Downloads#downloads, About#about]`; 5 sections with matching ids + `<h2>`s; `h1="Settings"`;
  `nav[aria-label]="Settings sections"`; `nav position: sticky`.
- **Expected**: persistent left section-nav with the 5 links + 5 titled sections; recognizable settings area.
- **Verdict**: pass.

### Step 4 — PASS `[a11y]`
- **Actions taken**: focused the Privacy nav link in the guest and activated it; observed chrome address +
  chip; re-read guest scroll-spy after settle.
- **Raw state**: nav link focusable (`document.activeElement === link`); chrome address →
  `goldfinch://settings/#privacy`, chip stayed `internal`, still read-only; scroll-spy `aria-current` set
  (1 link). With stub placeholder content all 5 sections fit the viewport (`scrollY:0`), so the activated
  anchor doesn't visibly scroll — the **documented DD1 trade-off** ("all sections render at once — fine for
  a stub"); Flight-6 controls will grow content and engage scrolling.
- **Expected**: focus reaches the nav link; activating moves to the section; section nav is keyboard-operable.
- **Verdict**: pass (anchor nav operable + URL/chip coherent; no-scroll is by-design for the stub).

### Step 5 — PASS (setup)
- **Actions taken**: `npm run a11y -- --target=goldfinch://settings` against the open shell.

### Step 6 — PASS `[a11y]`
- **Actions taken**: read the guest-target and chrome a11y results.
- **Raw state**: guest (`goldfinch://settings`) → **No NEW violations** vs the pinned `ACCEPTED` baseline.
  Chrome → initially **4 NEW** `region` violations on `#address` (the chip exposed `#address-wrap` as
  un-landmarked; DD4's assumption that it was pre-accepted was wrong). **Fixed mid-run**: `role="search"` on
  `#address-wrap` (source change in `src/renderer/index.html`); reloaded from disk; chrome re-audit →
  **No NEW violations**.
- **Expected**: no NEW WCAG A/AA violations vs the baseline (shell + chrome).
- **Verdict**: pass (after the `role="search"` fix; both chrome and guest clean).
- **Operator decision**: fixed inline, re-verified (the fix is a new source change committed with this run).

### Step 7 — PASS
- **Actions taken**: with the Settings tab active, read `#address-chip` in the chrome.
- **Raw state**: `chipState="internal"`, `aria-label="Secure Goldfinch page"`.
- **Expected**: an internal-page identity chip, distinct from the web chip; not a web origin/lock.
- **Verdict**: pass.

### Step 8 — PASS
- **Actions taken**: activated a web tab (`https://example.org/`); read the chip.
- **Raw state**: `chipState="web"`, `aria-label="Site information, example.org"`; address editable
  (`readOnly=false`).
- **Expected**: a web-page site-info chip (connection + origin), distinct from the internal chip.
- **Verdict**: pass.

### Step 9 — PASS
- **Actions taken**: clicked the web chip; read the popup.
- **Raw state**: popup open, `role="dialog"`; text = "example.org / Connection HTTPS / Trackers blocked 0 /
  Permissions 0 / Site settings →"; "Site settings →" `<button>` present and focused on open.
- **Expected**: popup shows origin + connection + a `tab.privacy` summary + "Site settings →"; `0`/empty is
  a valid pass for a fresh site.
- **Verdict**: pass (graceful `0` with `net` null — no crash).

### Step 10 — PASS
- **Actions taken**: activated the popup's "Site settings →".
- **Raw state**: popup closed; `#toggle-privacy aria-expanded="true"`, privacy panel visible.
- **Expected**: the existing Shields/privacy panel opens; the popup closes.
- **Verdict**: pass.

### Step 11 — PASS
- **Actions taken**: re-activated the `goldfinch://settings` tab; set `#address` to `https://example.org/`
  and dispatched Enter (the bar is read-only to the user; programmatic value + the address-Enter handler is
  the spec's "or invoke navigate" path).
- **Raw state**: before — 3 webviews, active = "Settings — Goldfinch", settings src `goldfinch://settings/`.
  After — 4 webviews (count +1), settings src **still** `goldfinch://settings/`, a new
  `https://example.org/` tab with partition `persist:goldfinch`.
- **Expected**: a new normal tab opens to the web URL; the internal tab stays on `goldfinch://settings`; tab
  count +1.
- **Verdict**: pass (the internal-tab navigation lock works).

### Step 12 — PASS `[a11y]`
- **Actions taken**: Escape on the open popup; then mutual-exclusion (open popup → open kebab) and
  outside-click (pointerdown on `#address`).
- **Raw state**: Escape → popup hidden + focus returned to `#address-chip`; opening the kebab closed the
  popup (kebab open); pointerdown outside → popup hidden.
- **Expected**: popup closes on Escape / outside-click; routes through the shared `menuController`; focus
  returns appropriately.
- **Verdict**: pass.

## Orchestrator Notes
- **Witnessed-pattern deviation**: this run was executed and judged by the **Flight Director** driving
  `cdp-driver` + a node-CDP guest attach, **not** the two-live-agent Executor+Validator crew the
  `/behavior-test` skill normally spawns. Rationale: the agentic-workflow live-verification step ran with
  the operator present and the app already up; every step was confirmed with concrete CDP state reads
  (DOM structure, computed styles, tab partitions, ARIA attributes, a11y-audit exit state) rather than
  model judgment. The independent-validator guarantee is weaker than a true Witnessed run; the compensating
  control is that every verdict above cites a raw machine-read value, not an impression. A future formal
  `/behavior-test settings-shell` (Witnessed crew) can supersede this log.
- **DD2 CSP-subresource spike — CONFIRMED PASS**: `settings.css` applied (`body` computed
  `background-color: rgb(30,31,37)` = `#1e1f25`) and `settings.js` executed (`aria-current` set by the
  scroll-spy `IntersectionObserver`), both served same-origin under the **unchanged** `INTERNAL_CSP`
  (`default-src 'self'; …`). No CSP relaxation / fallback needed.
- **Regression spot-checks (live)**: kebab + container menus' keyboard contract intact after the leg-1
  hoist (open-to-first via Enter/ArrowDown, open-to-last via ArrowUp, ArrowDown/Home/End movement,
  Escape close + focus-return) → covers `kebab-menu` / `unified-tab-controls` / `tab-keyboard-operability`
  / `menu-dismissal` core. `tab-scheme-guard` core invariant: a web origin's `window.open('goldfinch://…')`
  and `<iframe src="goldfinch://…">` produced **no** new internal tab and no web-tab navigation (internal
  tab count unchanged at 1) — the boundary survives the leg-2 serving + leg-4 lock changes. Full 13-vector
  `tab-scheme-guard` re-run not performed: flight 5 did not modify `isSafeTabUrl` / `will-navigate` / the
  trusted-embedder path / the internal session CSP (reviewer-confirmed), and those invariants are covered
  by `test/unit/url-safety.test.js` (passing) + flight-4's 13/13.
- **Evidence**: captured as inline CDP JSON reads in the flight-log / this run log. No screenshot/eval-dump
  directory was written (`/tmp/behavior-tests/goldfinch/settings-shell/<ts>/` — local, gitignored, cheap to
  regenerate by re-running).

## Closing Summaries

### Executor closing
All driving went through `scripts/cdp-driver.mjs` (chrome renderer) and a small node-CDP attach for the
`goldfinch://settings` guest (it surfaces as `type: webview` in the flat `/json` list — DD8's guest read
path holds). No environment hiccups; the app launched clean on `:9222` (Electron 42.3.3).

### Validator closing
Every step had a machine-readable observable, which made judgment unambiguous. The one real defect (the
`region`/`#address` a11y regression) was caught only by the live chrome a11y gate — the offline unit tests
can't see it — which is exactly why this flight's acceptance is live. The `role="search"` landmark is the
correct, semantic fix. Spec quality is good; consider adding an explicit "stub content fits the viewport →
anchor nav won't visibly scroll" note to step 4 so a future runner doesn't read no-scroll as a failure.

## Operator Notes
Flight-Director-driven live verification during `/agentic-workflow`. HAT (leg 8) to follow for subjective
feel.
