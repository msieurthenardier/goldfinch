# Behavior Test Run: settings-shell — 2026-07-08-23-03-59

**Spec**: [tests/behavior/settings-shell.md](../../settings-shell.md)
**Status**: pass (2 SPEC-DRIFT notes; a11y-audit step apparatus-limited)
**Started**: 2026-07-08T23:00:00
**Completed**: 2026-07-08T23:10:00
**Executor**: Leg-1c batch executor (SC3 parity sweep, self-witnessed single-agent)
**Surface**: native `WebContentsView` — chrome `wcId=1`, internal settings guest `wcId=12`
(`jarId:"internal"`), menu-overlay sheet `wcId=11`. Admin MCP over `127.0.0.1:8899`.

## Summary
The `goldfinch://settings` shell renders a recognizable, accessible settings area (5-link left
nav + 5 titled sections), the address-bar chips distinguish internal vs web pages, the site-info
popup summarizes the tab's privacy data and dismisses through the shared `menuController`, and the
internal-tab navigation lock holds. **10 of 12 steps pass on the native surface.** Two steps carry
**SPEC-DRIFT** notes (both reflect the Flight-7 "Site settings" rewire / the read-only internal
address-bar lock — the settings-shell spec explicitly defers the rewire to Flight 7 in its
Out-of-Scope, so its Flight-4-era expectations are stale, not regressed). The **axe a11y audit
(steps 5-6) cannot run** — the Flight-6 internal-session exclusion refuses `injectScript` on the
internal guest, so the harness errors out (APPARATUS-LIMIT; structural a11y corroborated via
`readAxTree`). No `LIKELY-REGRESSION`.

**Apparatus notes (internal guest).** `evaluate`/`injectScript` are **refused** on the internal
session (`internal-session excluded`); `readDom`/`readAxTree`/`captureScreenshot`/coordinate
`click`/`typeText`/`pressKey` all work. The guest AX tree **never** exposes a `focused` property,
so guest focus is not directly observable — keyboard operability is judged by observable *effects*
(section navigation, input value). Text-input values are DOM **properties** (not serialized to
`readDom` `outerHTML`), so input values are read from `captureScreenshot`. The internal settings
tab **is** enumerable/readable as `guestWcId=12` (as this spec requires) — note this is the same
enumerability the `kebab-menu` run flagged against `internal-session-exclusion` (DD5); cross-spec
tension for the FD, but a precondition satisfied here.

Evidence: `/tmp/behavior-tests/goldfinch/settings-shell/2026-07-08-23-03-59/`.

## Step Results

### Step 1 — PASS (precondition probe)
`tools/list` includes `getChromeTarget`; `getChromeTarget()` → `{wcId:1, kind:'chrome'}` (numeric).

### Step 2 — PASS (open Settings via kebab)
- **Path used:** the trusted UI path — coordinate `click` on `#kebab` (chrome), then the
  menu-overlay sheet's **Settings** `[role=menuitem]` activated via a tight `evaluate`-`.click()`
  on the sheet (`wcId=11`). *(Activation must be done immediately after opening — intervening
  `readDom` probes blur/dismiss the sheet; a tight click succeeds.)*
- **Raw:** a tab opens to `goldfinch://settings/`; `enumerateTabs` includes it as
  `{wcId:12, jarId:"internal", url:"goldfinch://settings/"}`; chrome address chip flips to
  `data-state="internal"`.
- **Verdict:** PASS. Recorded `guestWcId=12`.

### Step 3 — PASS (recognizable shell)
- **Raw (`readDom`+`readAxTree` on guest 12):** persistent left nav with **5** links — Appearance
  (`aria-current="true"`), Privacy & Shields, Automation, On startup, About — and **5** titled
  `<section>`s with ids `appearance/privacy/automation/startup/about`, each with a matching `<h2>`.
  AX exposes all 5 as `link` roles.
- **Verdict:** PASS — recognizable, structured settings area.

### Step 4 — PASS (section nav keyboard-operable) / focus-ring pixel INCONCLUSIVE-APPARATUS-LIMIT
- **Raw:** clicking a nav link navigates + the scroll-spy sets `aria-current` (e.g. click Privacy
  → `aria-current=privacy`; the section scrolls into view per `captureScreenshot`). Keyboard:
  after focusing the nav (click) + `ShiftTab` + `Enter`, `aria-current` changed section
  (automation → appearance) — a **keyboard-only** action moved the current section. Guest keyboard
  delivery independently proven: clicking the home-page input + `Ctrl+A` + `typeText` replaced its
  value (observed via `captureScreenshot`).
- **Verdict:** PASS — the section nav is keyboard-operable (native anchor links; `settings.js`:
  "fully navigable without this script"). The **focus ring** / focused-node is
  **INCONCLUSIVE-APPARATUS-LIMIT**: the guest AX exposes no `focused` property, `evaluate` is
  refused on the internal session, and WSLg does not composite `:focus-visible` under injected
  focus. `[a11y]`

### Steps 5-6 — INCONCLUSIVE-APPARATUS-LIMIT (axe audit blocked by internal-session exclusion)
- **Raw:** `npm run a11y -- --target=goldfinch://settings` →
  `injectScript(axe) failed for state "guest:goldfinch://settings": automation: injectScript —
  internal-session excluded`.
- **Verdict:** the F8 axe harness injects `axe` into the guest via `injectScript`, which the
  **Flight-6 internal-session hardening now refuses** on internal sessions — so the axe rule
  engine cannot run against `goldfinch://settings` over this surface. Classify
  **INCONCLUSIVE-APPARATUS-LIMIT** (a tooling casualty of the DD5 security change — **FD note**:
  the internal-target axe path needs an alternate injection route or an allow-listed exception).
  Structural a11y is corroborated (Step 3/4: correct roles, names, `aria-current`, operable nav),
  but this is not a substitute for the axe WCAG sweep. `[a11y]`

### Step 7 — PASS (internal identity chip)
- **Raw (chrome `#address-chip`):** `data-state="internal"`, `aria-label="Secure Goldfinch page"`
  — an internal-page identity chip, NOT a web origin/lock.
- **Verdict:** PASS.

### Step 8 — PASS (web site-info chip)
- **Raw (web tab `https://example.com/` active):** chip `data-state="web"`, `data-secure="true"`,
  `aria-label="Site information, example.com"` — a web-page chip with origin + secure indicator,
  distinct from the internal chip.
- **Verdict:** PASS.

### Step 9 — PASS (site-info popup)
- **Raw:** `click` the web chip → the menu-overlay sheet's `#sheet-popup` opens with text
  `"example.com  Connection HTTPS  Trackers blocked 0  Permissions 0  Site settings →"` — origin +
  connection (HTTPS) + the tab's privacy summary (0/0, valid for a fresh site per spec) + the
  **Site settings →** action. Not blank/crashed.
- **Verdict:** PASS.

### Step 10 — PASS (per current design) / SPEC-DRIFT (destination changed)
- **Raw:** clicking **Site settings →** navigated the existing settings tab to
  `goldfinch://settings/#privacy` (address shows `#privacy`, tab 12 active). The slide-out
  `#privacy-panel` stayed **collapsed** (did NOT open).
- **Verdict:** the popup **is** a thin entry point (not a duplicate) — the step's *intent* holds —
  but it now opens the **settings-page Privacy section**, not the slide-out panel the spec text
  describes. This is the **Flight-7 "Site settings" rewire** that settings-shell's own Out-of-Scope
  defers to Flight 7, and which `toolbar-pins` step 8 codifies (`#privacy` settings tab, panel does
  NOT open). **SPEC-DRIFT** (settings-shell step-10 text is stale; behavior correct per the newer
  design). Not a regression.

### Step 11 — PASS (lock invariant) / SPEC-DRIFT (mechanism changed to read-only address bar)
- **Raw:** with the settings tab active, the chrome address bar reads
  `value="goldfinch://settings/#privacy"` **`readOnly=true`**. `click` + `Ctrl+A` + `typeText
  "https://example.org/"` did **not** change it (still the settings URL); `Enter` opened **no** new
  tab (count 10 → 10); the settings tab **stayed** `goldfinch://settings/#privacy`. On a **web**
  tab the address bar is `readOnly=false` (editable) — so the read-only lock is **internal-only**.
- **Verdict:** the load-bearing invariant **holds** — the internal Settings tab does not navigate
  to a web URL. But the lock is now implemented as a **read-only address bar on internal tabs**
  (stronger), so the spec's specific "typing a web URL opens a new normal tab" affordance no longer
  applies (you cannot type). **SPEC-DRIFT** (lock re-mechanized; web tabs unaffected).
  **FD note:** confirm the read-only internal address bar (no type-to-spawn-tab) is the intended
  replacement for the Flight-4 "type → new tab" UX — the invariant is stronger but the affordance
  changed.

### Step 12 — PASS (popup dismissal)
- **Raw (deterministic):** `#sheet-popup` closed → `click` chip → **open** (`visible=true`) →
  `pressKey(sheetWcId, 'Escape')` → **closed** (`hidden`); chrome `document.activeElement` returns
  to `#address-chip` (the trigger).
- **Verdict:** PASS — the popup routes through the shared `menuController` (Escape closes, focus
  returns appropriately). `[a11y]`

## Closing Summary
The settings shell (SC6) and the address-bar chip system render and behave correctly on the native
`WebContentsView`: recognizable 5-section shell, internal vs web chips, a working site-info popup
with correct summary + shared-dismiss, and an intact internal-tab navigation lock. Overall
**PASS**. Two SPEC-DRIFT notes (step 10 "Site settings →" now opens the settings `#privacy` page
per the Flight-7 rewire; step 11 lock re-mechanized as a read-only internal address bar) — both are
stale-spec artifacts of landed Flight-7/Flight-6 changes, not regressions, though the FD should
confirm the step-11 UX-affordance change is intended. Steps 5-6 (axe audit) are
**INCONCLUSIVE-APPARATUS-LIMIT**: the Flight-6 internal-session exclusion refuses `injectScript`,
so the axe harness cannot audit the internal guest — a tooling gap the FD should note. No
`LIKELY-REGRESSION`.
