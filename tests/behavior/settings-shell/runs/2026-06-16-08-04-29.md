# Behavior Test Run: settings-shell — 2026-06-16-08-04-29

**Spec**: [tests/behavior/settings-shell.md](../settings-shell.md)
**Status**: pass
**Started**: 2026-06-16T08:05:25
**Completed**: 2026-06-16T08:07:40
**Mode**: FD-driven focused live pass (Flight 6 leg 7 `verify-integration`) — cited machine-read evidence. First run on the **admin MCP surface** (post-migration); supersedes CDP-`:9222`/`cdp-driver.mjs`.
**Apparatus**: admin MCP client over `127.0.0.1:49707`, app via `npm run dev:automation` (no `:9222`). Chrome target via `getChromeTarget()` → `wcId 1`; the `goldfinch://settings` guest reached via its own `wcId` from `enumerateTabs` (admin `allowInternal` keeps the internal tab enumerable). Two distinct targets — chrome `wcId` for the chip, guest `wcId` for the shell.

## Summary
Focused live pass: the admin surface opens the internal Settings page via the kebab (⋮ → Settings), the internal guest is **admin-enumerable and DOM/a11y-readable**, the settings shell renders as a recognizable accessible structure, and the chrome address-bar chip reflects the internal identity. **Core checkpoints passed** (this spec is the internal-guest + chrome-observe proof, per DD4).

## Step Results

### Step 2 — open Settings (kebab → Settings) → internal guest enumerable — PASS
- **Actions**: `click(wcId, 1372, 63)` (kebab) → `readAxTree` confirms menu items ["Settings","Exit"] → `click(wcId, 1300, 104)` (the "Settings" item, coordinate located via `captureWindow`) → `enumerateTabs`.
- **Raw state**: a new tab `goldfinch://settings/` appears in `enumerateTabs` (admin) as **wcId 6** — i.e. the internal guest is enumerable under the admin identity (`allowInternal`), alongside the 3 web tabs.
- **Verdict**: PASS.
- **Evidence**: `after-settings-click.png`, `menu-confirm.png`.
- **Note**: a blind Settings-item click at x=1265 missed (menu closed, no open); x=1300 (row center) landed. Recorded as the coordinate-tuning the migrated spec's "locate via `captureWindow`" rule anticipates.

### Step (shell) — recognizable accessible settings shell — PASS
- **Actions**: `readDom(6)` + `readAxTree(6)` on the guest.
- **Raw state**: title "Settings — Goldfinch"; DOM 20.5 KB; `<nav>` present; **6 `<section>`s with 6 `<h2>`s**; a11y tree of **962 nodes** including **6 `link`s** (the section-nav). A persistent left section-nav + titled sections — the recognizable shell.
- **Verdict**: PASS. [a11y]
- **Evidence**: `settings-guest-dom.html`, `settings-guest-ax.json`.

### Step (chip) — chrome address-bar chip reflects the internal identity — PASS
- **Actions**: `readDom(1)` on the chrome while the settings tab is active.
- **Raw state**: the chrome address area shows `goldfinch://settings` (the internal-page identity, read via the chrome `wcId` — distinct from the guest target).
- **Verdict**: PASS.

## Orchestrator Notes
FD-driven via an ephemeral admin-MCP harness (deleted after the run). This pass proves the DD4 internal-guest + chrome-observe capability end-to-end on the admin surface: the admin engine enumerates the `goldfinch://settings` guest, reads its DOM/a11y by `wcId`, and reads the chrome chip via `getChromeTarget`. The internal-tab navigation-lock checkpoint (typing a web URL in the internal tab opens a new normal tab) and the full chip-popup interactions were not exercised in this focused pass — they are good candidates for the leg-8 HAT (operator-driven). The Settings menu-item open required coordinate tuning (1265 → 1300), a normal artifact of blind coordinate clicks the migrated spec's `captureWindow`-locate rule covers.

## Closing Summaries
### Executor closing
Opening Settings via the kebab, enumerating the internal guest, and reading both the guest shell and the chrome chip all worked over the admin MCP surface — the migration's dual-target framing (chrome `wcId` + guest `wcId`) is correct and drivable.
### Validator closing
The internal guest is genuinely admin-enumerable and readable; the shell structure (nav + 6 titled sections) and the internal chip are both observable. Spec semantics preserved across the migration.

## Evidence
Ephemeral (never committed — internal/settings DOM may carry operator-visible config): `/tmp/behavior-tests/goldfinch/settings-shell/runs/2026-06-16-08-04-29-evidence/` — `menu-confirm.png`, `after-settings-click.png`, `settings-guest-dom.html`, `settings-guest-ax.json`. Re-derive by re-running.
