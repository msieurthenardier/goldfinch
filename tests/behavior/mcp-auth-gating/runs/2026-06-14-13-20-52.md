# Behavior Test Run: mcp-auth-gating — 2026-06-14-13-20-52

**Spec**: [tests/behavior/mcp-auth-gating.md](../../mcp-auth-gating.md)
**Status**: pass
**Started**: 2026-06-14T18:18:00Z
**Completed**: 2026-06-14T18:21:00Z
**Duration**: ~00:03:00
**Mode**: FD-driven machine-read (the mission's accepted standard; M02 debrief). Apparatus: `curl` HTTP-status probes against the loopback MCP transport, app launched via `npm run dev:automation` + the leg-5 env-gated auto-mint-to-stdout. Not the `:9222` CDP path; not `chrome-devtools` MCP.

## Summary
All auth-gate assertions passed. Three launches: a clean-profile **no-mint** instance (off-by-default), a **mint** instance (jar key), and a **mint + `GOLDFINCH_AUTOMATION_ADMIN`** instance (admin key). Ports overridden via `GOLDFINCH_MCP_PORT` (7797–7799) because a stale server held the default 7777 (recorded as an apparatus note, not a product defect).

## Step Results

### Off-by-default — PASS
- **Actions**: clean profile (`--user-data-dir=/tmp/gf-clean-off`), launched WITHOUT `GOLDFINCH_AUTOMATION_DEV_MINT` (surface stays off); POST `/mcp` initialize bearing a long fabricated key.
- **Raw state**: HTTP **401**. No `AUTOMATION_DEV_MINT` line printed (surface never enabled).
- **Expected**: a request is rejected while `automationEnabled` is false.
- **Verdict**: pass — surface does nothing until opted in.

### Opt-in + valid jar key accepted — PASS
- **Actions**: mint launch (`GOLDFINCH_AUTOMATION_DEV_MINT=1`), captured the printed jar key; POST `/mcp` initialize with `Authorization: Bearer <jarKey>`.
- **Raw state**: HTTP **200** (full initialize handshake).
- **Expected**: not 401 (accepted past the auth gate).
- **Verdict**: pass — exceeded ("200", a complete handshake).

### Missing / wrong / empty-Bearer → 401 — PASS
- **Raw state**: no `Authorization` → **401**; `Authorization: Bearer wrongwrongwrong` → **401**; `Authorization: Bearer ` (no token) → **401**.
- **Verdict**: pass — all three rejected.

### Guard-first (bad-origin + valid key → 403, not 401) — PASS
- **Actions**: POST with the valid jar key AND `Origin: http://evil.example`.
- **Raw state**: HTTP **403** (origin guard fired before the auth gate).
- **Verdict**: pass — guard-first invariant holds with the auth gate composed (the auth 401 never masks the origin 403).

### Admin inert unless env-set — PASS
- **Actions**: mint + `GOLDFINCH_AUTOMATION_ADMIN=1` launch → captured admin key; presented it to (a) the admin-env-SET server and (b) an admin-env-UNSET server.
- **Raw state**: admin key vs admin-env-SET server → **200**; same admin key vs admin-env-UNSET server → **401**; the admin launch's jar key → **200**.
- **Verdict**: pass — the admin tier exists only when the env gate is set; the key is inert otherwise.

## Orchestrator Notes
- Port override (`GOLDFINCH_MCP_PORT` 7797–7799) used because a stale goldfinch held 7777 (`[mcp] failed to start automation server: … port 7777 is in use`). Apparatus-only; the gate behavior is port-agnostic.
- The mint path persists `automationEnabled=true`, so off-by-default was verified on a **clean profile** (separate `--user-data-dir`).
- Evidence: `/tmp/behavior-tests/goldfinch/mcp-auth-gating/2026-06-14-13-18-09/auth-gating-probes.txt` (ephemeral, not committed).

## Operator Notes
SC8 auth-gate half is live-confirmed end to end over the real transport.
