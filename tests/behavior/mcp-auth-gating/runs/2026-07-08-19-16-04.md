# Behavior Test Run: mcp-auth-gating — 2026-07-08-19-16-04

**Spec**: [tests/behavior/mcp-auth-gating.md](../../mcp-auth-gating.md)
**Status**: pass
**Started**: 2026-07-08T19:16:04-05:00 (00:16:04Z)
**Completed**: 2026-07-08T19:22:00-05:00
**Duration**: ~00:06:00 (three launches)
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — raw HTTP status codes + SDK accept results + stdout mint lines persisted)

## Summary
10 / 10 steps passed across the three env-gated launches (Run A: no mint; Run B: mint, admin unset; Run C: mint + admin). 0 failed; 0 inconclusive.

The auth gate is the decisive verdict on every request (all loopback, no hostile Origin/Host, so SC7's origin guard passes first — guard-first ordering respected). Apparatus: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899`; raw `curl` for the Bearer-shape rejection probes; the SDK client (Bearer header via `requestInit.headers`) for the accept checks. Each run is a **separate launch** (the `GOLDFINCH_AUTOMATION_DEV_MINT` / `GOLDFINCH_AUTOMATION_ADMIN` env is read at process start), never a mid-run toggle.

**DD3/DD4 caveat honored:** in dev the surface is auth-enabled by the in-memory dev-enable override, so Run A's keyless/fabricated 401s are the **"no valid key"** verdict, not "surface disabled" (true off-by-default is a packaged-build observation, deferred to leg 7 / HAT).

## Step Results

### Run A — no valid key (launched WITHOUT `GOLDFINCH_AUTOMATION_DEV_MINT`)

### Step 1 — PASS
- **Actions taken**: confirmed the Run-A launch printed **no** `AUTOMATION_DEV_MINT` line; `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8899/mcp` (keyless loopback).
- **Raw state**: no mint line in `/tmp/l4-appA.log`; keyless request → **HTTP 401**.
- **Expected**: no mint line (no valid key exists); keyless loopback → 401 (the auth 401, not the origin 403).
- **Verdict**: pass — the gate rejects a keyless request; loopback + no hostile Origin/Host, so it is the auth verdict.
- **Evidence**: [runA-step1-nomint.txt](./2026-07-08-19-16-04/runA-step1-nomint.txt), [runA-step1-keyless.status](./2026-07-08-19-16-04/runA-step1-keyless.status)

### Step 2 — PASS
- **Actions taken**: `curl -H 'Authorization: Bearer deadbeef-not-a-real-key' … /mcp`.
- **Raw state**: **HTTP 401**.
- **Expected**: 401 — a well-formed Bearer that resolves to no identity (`validateKey → null`), no key minted this run.
- **Verdict**: pass — never reaches session routing.
- **Evidence**: [runA-step2-fabricated.status](./2026-07-08-19-16-04/runA-step2-fabricated.status)

### Run B — opt-in + per-key gate (mint, `GOLDFINCH_AUTOMATION_ADMIN` unset)

### Step 3 — PASS
- **Actions taken**: relaunched with `GOLDFINCH_AUTOMATION_DEV_MINT=1`; read the mint line.
- **Raw state**: `AUTOMATION_DEV_MINT {"key":"<jar-key redacted>","adminKey":null}` — jar key non-empty, **`adminKey` null**.
- **Expected**: one mint line; non-empty jar key; `adminKey` null (admin env unset).
- **Verdict**: pass.
- **Evidence**: [runB-step3-mintline.txt](./2026-07-08-19-16-04/runB-step3-mintline.txt)

### Step 4 — PASS
- **Actions taken**: SDK client with `Authorization: Bearer <jar-key>`; `connect()` (initialize); `enumerateTabs`.
- **Raw state**: initialize succeeded; `enumerateTabs` → JSON array (len 1), not `isError`.
- **Expected**: handshake completes (not 401); benign tool call returns an array, not an auth error.
- **Verdict**: pass — the minted jar key on the transport completes the handshake + a benign call.
- **Evidence**: [runB-step4-jar-accept.txt](./2026-07-08-19-16-04/runB-step4-jar-accept.txt)

### Step 5 — PASS
- **Actions taken**: `curl` with no `Authorization` header.
- **Raw state**: **HTTP 401**.
- **Expected**: 401 — enabling the surface does not drop the key requirement.
- **Verdict**: pass.
- **Evidence**: [runB-step5-missing-key.status](./2026-07-08-19-16-04/runB-step5-missing-key.status)

### Step 6 — PASS
- **Actions taken**: `curl -H 'Authorization: Bearer totally-wrong-garbage-key' … /mcp`.
- **Raw state**: **HTTP 401**.
- **Expected**: 401 — well-formed Bearer, non-matching token.
- **Verdict**: pass.
- **Evidence**: [runB-step6-wrong-key.status](./2026-07-08-19-16-04/runB-step6-wrong-key.status)

### Step 7 — PASS
- **Actions taken**: `curl -H 'Authorization: Bearer ' … /mcp` (scheme, empty token).
- **Raw state**: **HTTP 401**.
- **Expected**: 401 — `parseBearer` returns `''` for a tokenless header.
- **Verdict**: pass.
- **Evidence**: [runB-step7-empty-bearer.status](./2026-07-08-19-16-04/runB-step7-empty-bearer.status)

### Step 8 — PASS
- **Actions taken**: `curl -H 'Authorization: Bearer admin-shaped-but-no-admin-tier' … /mcp` (Run B has admin env unset).
- **Raw state**: **HTTP 401**.
- **Expected**: 401 — no admin tier exists (`mintAdminKey` returned null, `automationAdminKeyHash` unset, `validateKey` called with `adminEnabled:false`).
- **Verdict**: pass — the admin tier does not exist unless the env var was set at launch.
- **Evidence**: [runB-step8-admin-inert.status](./2026-07-08-19-16-04/runB-step8-admin-inert.status)

### Run C — admin tier accepted (mint + `GOLDFINCH_AUTOMATION_ADMIN=1`)

### Step 9 — PASS
- **Actions taken**: launch with both `GOLDFINCH_AUTOMATION_DEV_MINT=1` and `GOLDFINCH_AUTOMATION_ADMIN=1`; read the mint line.
- **Raw state**: `AUTOMATION_DEV_MINT {"key":"<jar-key redacted>","adminKey":"<admin-key redacted>"}` — **non-null `adminKey`**.
- **Expected**: non-null `adminKey` (admin tier exists because the env was set at process start).
- **Verdict**: pass.
- **Evidence**: [runC-step9-mintline.txt](./2026-07-08-19-16-04/runC-step9-mintline.txt)

### Step 10 — PASS
- **Actions taken**: SDK client with `Authorization: Bearer <adminKey>`; `connect()`; `enumerateTabs`.
- **Raw state**: initialize succeeded; `enumerateTabs` → array (len 5), not `isError`.
- **Expected**: handshake succeeds; the admin key resolves to the `admin` identity (`validateKey` with `adminEnabled:true`) and is admitted.
- **Verdict**: pass — the same admin-key shape that was 401 in Run B (env unset) is accepted here (env set); the only difference is the launch environment.
- **Evidence**: [runC-step10-admin-accept.txt](./2026-07-08-19-16-04/runC-step10-admin-accept.txt)

## Orchestrator Notes
Three separate launches were used (env is read at process start): Run A (no mint), Run B (mint, admin unset), Run C (mint + admin, served by the initial leg-04 apparatus instance). Every request was loopback with no hostile Origin/Host, so the SC7 origin guard passed and the auth gate was the decisive verdict throughout — 401 = rejected at the gate, accept = admitted into routing. The admin-inert-unless-env-set property holds in both directions (401 in Run B, accepted in Run C).

## Closing Summaries

### Executor closing
Clean 10/10. The relaunch discipline (admin off vs admin on as separate launches) exercised the env-gated admin tier correctly: the same admin-shaped Bearer is 401 without the env and accepted with it. The raw HTTP status codes are persisted per probe.

### Validator closing
No spec drift. The DD3/DD4 dev-enable-override caveat was correctly applied — Run A's 401s are the no-valid-key verdict (the surface is auth-enabled in dev), not a disabled-surface verdict; true off-by-default remains a packaged-build / HAT observation.

## Operator Notes
Run on WSLg; port pinned to 8899. Three launches; each mint line captured at its own launch.
