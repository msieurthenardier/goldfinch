# Behavior Test Run: welcome-home-routing — 2026-08-25-20-38-40

**Spec**: [welcome-home-routing.md](../welcome-home-routing.md)
**Status**: pass
**Started**: 2026-08-25T20:38:40Z
**Completed**: 2026-08-25T21:05:57Z
**Duration**: 00:27:17
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-home-routing/2026-08-25-20-38-40/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed. 0 failed; 0 inconclusive. Re-run at the Mission 16 Flight 3 leg-1 gate on the restyled build (brand mark, cards, engine grid, the burner note as a notice): every verdict rested on ids, computed style, committed URLs, a11y checked-state, and on-disk payloads — none of which the restyle touched — and none was discounted on appearance. Clearing the home page routes every new-tab path (Ctrl+T, second window, burner) to the welcome surface with the home block only; the burner tab carries the app-wide notice; typing an address and clicking a bookmark navigate the welcome tab itself in place; setting the home page from the surface ends the welcome routing in every window; no welcome tab is restored across a real quit/relaunch and the boot window's first Ctrl+T honors the home page.

## Checkpoint Results

### Setup row (fixture) — completed, no judgment
- **Actions taken**: Settings via the `kebabActionSettings` seam: home page `https://example.com` + Save (re-captured — the status line shifted the layout ~14 px), DuckDuckGo radio, "Show bookmarks bar" toggle on; `openTab` to `https://example.org`, chrome `#star` click (bookmark persisted immediately; the read-only edit sheet dismissed by a tab switch). Row read: `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`, `bookmarksBarEnabled: true`; `bookmarks` table holds `https://example.org/` in `personal`. The boot welcome tab stayed a dormant viewless strip entry — Settings, not it, was the shown tab when both preferences became set (DD7 auto-attach applies to the shown tab only). `#bookmarks-bar` renders only while a web-class tab is active (display none on the internal Settings tab) — a pre-existing rule, not a toggle bug. Focus left on Settings for step 1.
- **Evidence**: `step-0-settings-initial.png`, `step-0-settings.png`, `step-0-settings-snapshot.txt`, `step-0-bookmark-sheet.png`, `step-0-bookmarks-bar-webtab.png`, `step-0-bookmarks.json`, `step-0-settings-row.json`, `step-0-end.png`

### Checkpoint 1 — PASS
- **Actions taken**: Scrolled Settings to the top; re-captured; clicked the home page Clear button; guest + window screenshots, a11y tree.
- **Raw state**: Home page field empty; status line "No home page chosen — new tabs will open the welcome page until you set one."; DuckDuckGo the only checked radio; strip unchanged.
- **Expected**: The home page field is empty and a status line explains that new tabs will open the welcome page. The search engine control still shows DuckDuckGo. [a11y]
- **Verdict**: pass — empty home field with the explanatory status line (screenshot + a11y agree); DuckDuckGo still checked, the other seven false; `[a11y]` judged functionally.
- **Evidence**: `step-1-locate.png`, `step-1-screenshot-2.png`, `step-1-screenshot.png`, `step-1-snapshot.txt`
- **Validator notes**: the restyled grid did not affect the judgment — a11y checked-state was the signal.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document (plus a programmatic present-vs-absent check).
- **Raw state**: `homePage` present with JSON `null`; `searchEngine: "duckduckgo"`; `updated_at` newer than the fixture row.
- **Expected**: `[mixed-frame]` `homePage` is present and `null`; `searchEngine` is `"duckduckgo"`.
- **Verdict**: pass — `homePage` present with JSON `null` (key present, not defaulted-absent); `searchEngine: "duckduckgo"`; the row postdates the fixture.
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: none.

### Checkpoint 3 — PASS
- **Actions taken**: Before probe; Ctrl+T on the chrome; window screenshot, a11y tree, probe (plus a className/computed-style follow-up), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 3 → 4; a new "Welcome to Goldfinch" tab at index 3 selected (distinct from the dormant boot tab at index 0); viewless (`activeTabWcId: null`, not enumerated); address bar empty, placeholder, writable; surface shows the Home page card only — engine block and burner note carry the `hidden` class (display none, zero rect). The `.hidden` IDL property is not the app's mechanism — className/computed display and the screenshot are.
- **Expected**: A new tab opens titled "Welcome to Goldfinch" showing the welcome surface with the **home page** block only (no search-engine block); the address bar is empty, shows its placeholder, and is writable; the tab strip shows one more tab than before.
- **Verdict**: pass — strip 3 → 4 with the new selected "Welcome to Goldfinch" tab; address bar empty, placeholder, writable; home card visible (display flex), engine block and burner note display none; screenshot corroborates.
- **Evidence**: `step-3-before.json`, `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`, `step-3-tabs.json`, `step-3-windows.json`
- **Validator notes**: the `.hidden` IDL property is a false signal on this build (CSS-class hiding) — computed style/className carries block visibility; carried forward to steps 4/5/7.

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+N; `enumerateWindows` (window 2, chrome wcId 5); probe on wcId 5 by className/computed display; `captureWindow {windowId:2}`, a11y tree, `enumerateTabs`; window 1 strip re-probed.
- **Raw state**: Window 2 opened on a single viewless "Welcome to Goldfinch" tab: home card visible, engine block and burner note hidden; address bar empty/writable; window 1 unchanged (4 tabs). The Executor flagged an "E Example Domain" strip under the address bar in both windows — it is the bookmarks bar with the fixture bookmark (app-wide), the element step 7 clicks.
- **Expected**: The second window's initial tab is a welcome tab (home block only) — the cleared preference reached a window that did not make the change.
- **Verdict**: pass — window 2's single initial tab is a selected "Welcome to Goldfinch" welcome tab with the home block only (computed style + screenshot); the window never touched Settings, so the cleared preference propagated; window 1 unaffected.
- **Evidence**: `step-4-windows.json`, `step-4-probe.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-tabs.json`
- **Validator notes**: the bookmarks-bar strip (fixture bookmark, app-wide) registered as step 7's target.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe; `openNewTab(makeBurner())` via the chrome seam; window screenshot, a11y tree, probe (selected tab attributes, block className/computed display, note text), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 4 → 5; the new selected tab carries the burner jar swatch (`#ff8c42`, title "Burner (burner)") — an orange dot vs green; the surface shows the notice "This choice is saved for all of Goldfinch." (display block, styled as a notice) above the home card; engine card hidden; bookmarks bar suppressed on the burner tab; viewless.
- **Expected**: The burner tab is a welcome tab carrying the burner note ("saved for all of Goldfinch") in the burner jar.
- **Verdict**: pass — the new selected tab carries the "Burner (burner)" jar swatch (orange vs green) and its welcome surface shows the notice "This choice is saved for all of Goldfinch." (display block); the notice restyle did not affect the judgment.
- **Evidence**: `step-5-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs.json`, `step-5-windows.json`
- **Validator notes**: none.

### Checkpoint 6 — PASS
- **Actions taken**: Activated the step-3 welcome tab (strip index 3) by clicking its strip rect; before probe + `enumerateTabs`; clicked the address bar, Ctrl+A, typed `example.net`, Enter; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 5 → 5, selectedIndex 3 before and after; committed `https://example.net/` (two reads); the surface hidden; the tab attached as wcId 6 (absent before); Example Domain rendered.
- **Expected**: That **same** tab navigates to `https://example.net/` — the tab count is unchanged and the tab keeps its strip position; the welcome surface is gone from it.
- **Verdict**: pass — committed `https://example.net/` (two reads); count 5 → 5 and index 3 → 3 — the same record acquired wcId 6 and navigated in place; the surface display none.
- **Evidence**: `step-6-before.json`, `step-6-tabs-before.json`, `step-6-screenshot.png`, `step-6-snapshot.txt`, `step-6-probe.json`, `step-6-tabs-after.json`, `step-6-windows.json`
- **Validator notes**: none.

### Checkpoint 7 — PASS
- **Actions taken**: Ctrl+T (a new welcome tab, home block only, bookmarks bar rendered); before probe, `enumerateTabs`, screenshot; clicked the `button.bm-item` "Example Domain" on `#bookmarks-bar`; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`.
- **Raw state**: Strip 6 → 6, selectedIndex 5 → 5; committed `https://example.org/` (two reads); the surface hidden; the welcome tab attached as wcId 7, the fixture's example.org tab (wcId 3) untouched; filled star, Example Domain rendered.
- **Expected**: The new welcome tab itself navigates to `https://example.org/` (same tab, count unchanged) — bookmarks were reachable from the welcome surface.
- **Verdict**: pass — committed `https://example.org/` (two reads); count 6 → 6, index 5 → 5 — the same record (now wcId 7) navigated in place; the bookmark was clicked on a live bar element while the surface showed.
- **Evidence**: `step-7-before.json`, `step-7-tabs-before.json`, `step-7-before.png`, `step-7-screenshot.png`, `step-7-snapshot.txt`, `step-7-probe.json`, `step-7-tabs-after.json`
- **Validator notes**: none.

### Checkpoint 8 — PASS
- **Actions taken**: Ctrl+T (window 1) and before-probes on both windows; typed `https://example.com` in the welcome home block and clicked Set; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`, window 2 after-probe; switched to the Settings tab, guest screenshot + a11y tree.
- **Raw state**: Window 1: strip 7 → 7, index 6 → 6; committed `https://example.com/`; surface hidden; attached as wcId 8. Window 2's shown welcome tab auto-attached itself to the new home page on the broadcast (wcId 9, title "Example Domain", `activeTabWcId` null → 9) — DD7, observed again. Settings: home field `https://example.com`, no hint (only shown when empty); DuckDuckGo the only checked radio.
- **Expected**: The welcome surface leaves the tab: the tab is now on `https://example.com/`. Settings (open it) shows `https://example.com` in the home page field; the search engine is still DuckDuckGo.
- **Verdict**: pass — committed `https://example.com/` on the same tab (7 → 7, index 6), surface gone; Settings shows `https://example.com` with DuckDuckGo still the sole checked radio.
- **Evidence**: `step-8-before.json`, `step-8-w2-before.json`, `step-8-screenshot.png`, `step-8-snapshot.txt`, `step-8-probe.json`, `step-8-tabs.json`, `step-8-windows.json`, `step-8-w2-after.json`, `step-8-settings.png`, `step-8-settings-snapshot.txt`
- **Validator notes**: window 2's welcome tab auto-attached on the broadcast (DD7) — the documented baseline shift for step 9, not part of this row.

### Checkpoint 9 — PASS
- **Actions taken**: Before probe + `enumerateTabs` on window 2 (its single tab was already the step-8 auto-attached example.com tab); Ctrl+T on window 2's chrome (wcId 5); window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Window 2 strip 1 → 2; the new tab (wcId 10) opened directly on `https://example.com/` with no welcome surface; wcId 9 unchanged. Same PID throughout (2236462) — no restart.
- **Expected**: The new tab opens on `https://example.com/` — the set preference reached the other window without a restart.
- **Verdict**: pass — window 2's count 1 → 2 with the new selected tab committed to `https://example.com/` and no welcome surface; PID unbroken since before the fixture, so the preference reached the window live.
- **Evidence**: `step-9-before.json`, `step-9-tabs-before.json`, `step-9-screenshot.png`, `step-9-snapshot.txt`, `step-9-probe.json`, `step-9-tabs-after.json`, `step-9-windows.json`
- **Validator notes**: window 2 already held one example.com tab from step 8's DD7 auto-attach — "the new tab" is the second; judged as written.

### Checkpoint 10 — PASS
- **Actions taken**: (Quit/relaunch performed by the Orchestrator — see Orchestrator Notes.) Re-sourced the re-minted key; verified the restored strips from the chrome DOM in both windows; restored screenshot; Ctrl+T in window 1; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`.
- **Raw state**: Restored: window 1 four "Example Domain" tabs (no welcome title), window 2 two; no welcome or burner tab anywhere. Ctrl+T: strip 4 → 5, the new tab (wcId 9) committed to `https://example.com/` with no welcome surface at any point.
- **Expected**: No welcome tab was restored (the restored tabs are only the web tabs from step 6–9). The new tab opens on `https://example.com/` — the boot window honors the home page on its first new tab.
- **Verdict**: pass — the on-disk session held exactly the six web tabs with no welcome/burner/Settings record; the restored strips carry no welcome title; Ctrl+T in the restored window committed `https://example.com/` with no welcome panel at any point; the PID attestation corroborates a genuine restart.
- **Evidence**: `step-10-tabs-prequit.json`, `step-10-session-doc.json`, `step-10-windows-restored.json`, `step-10-tabs-restored.json`, `step-10-before.json`, `step-10-w2.json`, `step-10-restored.png`, `step-10-screenshot.png`, `step-10-snapshot.txt`, `step-10-probe.json`, `step-10-tabs-after.json`
- **Validator notes**: clean close.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh scratch profile; the fixture (home page, DuckDuckGo, bookmarks bar on, one bookmark) folded in as a setup row before step 1. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled; the `[a11y]` row judged functionally).
- **Purpose**: Flight 3 leg-1 regression net on the restyled build — every row reads ids and a11y/computed state; no verdict depended on appearance. Step 5 doubles as the observed check of the burner notice (DD4 restyle): the note rendered as a `welcome-notice` block with the app-wide text.
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden. Steps 2 was dispatched in parallel with checkpoint 1's judgment, and step 3 with checkpoint 2's (reads that cannot disturb live state). New facts for the crew file: the `.hidden` IDL property is NOT the app's mechanism — blocks hide by a `hidden` CSS class; read `className`/`getComputedStyle().display`/rect, and gate inner-block reads on `#welcome-surface`'s own display; the strip tab element carries a `.tab-jar` swatch (`title="Burner (burner)"`, `#ff8c42`) that identifies a burner tab; the bookmarks bar renders only while a web-class tab is active (display none on the internal Settings guest); bookmark items are `button.bm-item` in `#bookmarks-bar`; the bookmarks bar is app-wide (its fixture item appears in every window).
- **Fixture on this build**: the boot both-unset welcome tab stayed a dormant viewless strip entry throughout — Settings, not it, was the shown tab when both preferences became set (DD7 auto-attach applies to the shown welcome tab only). It was never activated; the stored session excluded it (DD9).
- **DD7 auto-attach observed again at step 8**: window 2's shown welcome tab attached itself to the new home page on the broadcast (wcId 9, `activeTabWcId` null → 9), so step 9's "new tab" was the second example.com tab in that window — judged as written, as on the 2026-08-25-02-45-35 run.
- **Restart attestation (step 10)**: PID 2236462 held the profile's `app.db` from before the fixture through checkpoint 9; SIGTERM, clean exit in ~2 s; the stored `session` document read from disk held exactly the six web tabs (four in window 1, two in window 2) and no welcome/burner/Settings record — DD9 observed on disk; relaunch on the same profile at 16:02:52 local (new PID 2242470), key re-minted, crew env rewritten, restored topology (both windows, wcIds renumbered) briefed and saved as evidence.

## Closing Summaries

### Executor closing
All ten steps produced consistent, corroborating evidence (dual address reads matched every time); no apparatus flakiness; the post-relaunch key re-mint was handled by re-sourcing the env. Carry forward: `#welcome-surface` and its blocks hide by a `hidden` CSS class, not the `hidden` attribute — assert on computed style, never the `.hidden` property; burner tabs carry a `.tab-jar` swatch (title "Burner (burner)") — the positive jar signal when titles coincide; bookmark items are `button.bm-item`; the bookmarks bar renders only for web-content tabs. _Orchestrator note: the closing's final bullet ("no welcome tab ever appeared across the full run") contradicts the Executor's own step 3–5 and 7–8 reports and is true only of step 10 (no welcome tab restored, none on the first Ctrl+T); the per-step reports are the record._

### Validator closing
10/10 with no FAIL or INCONCLUSIVE; a re-run of an already-passing spec against the restyled build with zero functional regressions — every judgment rested on state the restyle did not touch. Apparatus finding worth folding into the crew notes: the welcome blocks' `.hidden` IDL property reads false even when hidden by CSS class — className/computed display plus screenshots are the trustworthy signal (a validator on the raw property would have misjudged steps 3–5 and 7). The DD7 cross-window auto-attach at step 8 played out exactly as the notes flag; step 9 judged as written. No independent fresh observations were needed; no spec-quality concerns beyond the `[READY]` notes.

## Operator Notes
_None._
