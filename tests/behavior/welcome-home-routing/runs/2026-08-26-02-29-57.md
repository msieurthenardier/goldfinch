# Behavior Test Run: welcome-home-routing — 2026-08-26-02-29-57

**Spec**: [welcome-home-routing.md](../welcome-home-routing.md)
**Status**: pass
**Started**: 2026-08-26T02:29:57Z
**Completed**: 2026-08-26T03:02:08Z
**Duration**: 00:32:11
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-home-routing/2026-08-26-02-29-57/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed. 0 failed; 0 inconclusive. Gate re-run on the final Mission 16 Flight 3 HAT build for the three rows re-authored for the DD7 pivot: Set on the welcome card saves and stays (row 8), other windows' welcome tabs re-render with the saved value instead of auto-navigating (row 9), and a real relaunch restores exactly the web tabs — never-attached welcome and burner records excluded (row 10). Every other claim (Clear routing every new-tab path to the welcome surface, the burner notice, same-tab navigation by address and by bookmark) unchanged and re-observed; the Validator grounded every verdict in its own observation.

## Checkpoint Results

### Setup row (fixture) — completed, no judgment
- **Actions taken**: Settings via the `kebabActionSettings` seam: home page `https://example.com` + Save ("Saved"), DuckDuckGo radio, "Show bookmarks bar" on; `openTab` to `https://example.org`, chrome `#star` click (bookmark persisted; the read-only edit sheet dismissed by a tab switch). Row: `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`, `bookmarksBarEnabled: true`; `bookmarks` holds `https://example.org/` in `personal`. On this build nothing auto-attached when both preferences became set — the boot welcome tab stayed a viewless strip entry (index 0). `#bookmarks-bar` renders on the web tab, not on the internal Settings guest. Settings left active for step 1. A stale `sheetWcId` field (`sheetVisible: false`) appeared after the dismissed sheet — the known transient.
- **Evidence**: `step-0-settings-initial.png`, `step-0-before-save.png`, `step-0-after-save.png`, `step-0-settings.png`, `step-0-settings-snapshot.txt`, `step-0-scrolled.png`, `step-0-bookmarksbar-toggled.png`, `step-0-before-bookmark.png`, `step-0-bookmark-sheet.png`, `step-0-after-dismiss.png`, `step-0-bookmarks.json`, `step-0-settings-row.json`, `step-0-end.png`

### Checkpoint 1 — PASS
- **Actions taken**: Scrolled Settings to the top; re-captured; clicked the home page Clear button; guest + window screenshots, a11y tree.
- **Raw state**: Home page field empty (pixels; the a11y node exposes no value); status line "Cleared — new tabs will open the welcome page until you set one."; DuckDuckGo the only checked radio.
- **Expected**: The home page field is empty and a status line explains that new tabs will open the welcome page. The search engine control still shows DuckDuckGo. [a11y]
- **Verdict**: pass — the Validator's own screenshot: empty home field with the "Cleared — …" status line beneath; DuckDuckGo the only checked radio (a11y parsed); `[a11y]` judged functionally.
- **Evidence**: `step-1-locate.png`, `step-1-pre-click.png`, `step-1-screenshot-2.png`, `step-1-screenshot.png`, `step-1-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document.
- **Raw state**: `homePage` present with JSON `null`; `searchEngine: "duckduckgo"`; `updated_at` reflects the step-1 Clear.
- **Expected**: `[mixed-frame]` `homePage` is present and `null`; `searchEngine` is `"duckduckgo"`.
- **Verdict**: pass — `homePage` present with JSON `null`, `searchEngine: "duckduckgo"`; `updated_at` reflects the Clear. (The Validator mistyped its signal line and corrected it in the same reply; the JSON verdict is `pass`.)
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: none.

### Checkpoint 3 — PASS
- **Actions taken**: Before probe; Ctrl+T on the chrome; window screenshot, a11y tree, probe (className/computed display), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 3 → 4; the new "Welcome to Goldfinch" tab at index 3 selected (the dormant boot tab still at index 0); viewless; address bar empty, placeholder, writable; the surface shows the Home page card only (engine block `hidden`), tagline "Choose where new tabs open…". The Executor flagged the "E Example Domain" strip below the toolbar — it is the bookmarks bar with the fixture bookmark, app-wide.
- **Expected**: A new tab opens titled "Welcome to Goldfinch" showing the welcome surface with the **home page** block only (no search-engine block); the address bar is empty, shows its placeholder, and is writable; the tab strip shows one more tab than before.
- **Verdict**: pass — the Validator's own screenshot: a new fourth "Welcome to Goldfinch" tab selected, the Home page card only, address bar empty with its placeholder; strip 3 → 4 by probe; the bookmarks-bar strip is app-wide, not a stale title.
- **Evidence**: `step-3-before.json`, `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`, `step-3-tabs.json`, `step-3-windows.json`
- **Validator notes**: none.

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+N; `enumerateWindows` (window 2, chrome wcId 5); probe on wcId 5; `captureWindow {windowId:2}`, a11y tree, `enumerateTabs`; window 1 strip re-probed.
- **Raw state**: Window 2 opened on a single viewless "Welcome to Goldfinch" tab with the home card only, tagline "Choose where new tabs open…"; address bar empty/writable; window 1 unchanged (4 tabs). The bookmarks bar (app-wide) shows the fixture bookmark in window 2 as well.
- **Expected**: The second window's initial tab is a welcome tab (home block only) — the cleared preference reached a window that did not make the change.
- **Verdict**: pass — window 2 (a distinct windowId/chrome wcId) shows a single welcome tab with the home card only (the Validator's own screenshot + probe); the window never made the change, so the cleared preference propagated; window 1 unaffected.
- **Evidence**: `step-4-windows.json`, `step-4-probe.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-tabs.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe; `openNewTab(makeBurner())` via the chrome seam; window screenshot, a11y tree, probe (selected tab attributes, block classes/display, note text, bookmarks bar), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 4 → 5; the new selected tab carries the `.tab-jar` swatch (`#ff8c42`, "Burner (burner)"); the surface shows the notice "This choice is saved for all of Goldfinch." as a banner under the tagline, above the home card; engine block hidden; bookmarks bar suppressed on the burner tab; viewless.
- **Expected**: The burner tab is a welcome tab carrying the burner note ("saved for all of Goldfinch") in the burner jar.
- **Verdict**: pass — the Validator's own screenshot: the selected fifth tab carries the orange burner swatch ("Burner (burner)") and the surface shows the banner "This choice is saved for all of Goldfinch." above the home card.
- **Evidence**: `step-5-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs.json`, `step-5-windows.json`
- **Validator notes**: none.

### Checkpoint 6 — PASS
- **Actions taken**: Activated the step-3 welcome tab (index 3) by clicking its strip rect; before probe + `enumerateTabs`; clicked the address bar, Ctrl+A, typed `example.net`, Enter; window screenshot, a11y tree, `enumerateTabs`, `enumerateWindows`, probe (address read twice).
- **Raw state**: Strip 5 → 5, index 3 → 3; committed `https://example.net/` (two reads); surface hidden; the tab attached as wcId 6 (absent before); Example Domain rendered.
- **Expected**: That **same** tab navigates to `https://example.net/` — the tab count is unchanged and the tab keeps its strip position; the welcome surface is gone from it.
- **Verdict**: pass — count 5 → 5 and index 3 → 3 (the decisive same-tab evidence); committed `https://example.net/`; the surface hidden; the other two welcome tabs unaffected — in-place navigation, not a replacement.
- **Evidence**: `step-6-before.json`, `step-6-tabs-before.json`, `step-6-screenshot.png`, `step-6-snapshot.txt`, `step-6-tabs-after.json`, `step-6-windows.json`, `step-6-probe.json`
- **Validator notes**: none.

### Checkpoint 7 — PASS
- **Actions taken**: Ctrl+T (a new welcome tab, home card only, bookmarks bar rendered with its one item); before probe, `enumerateTabs`, screenshot; clicked the `button.bm-item` "Example Domain"; window screenshot, a11y tree, `enumerateTabs`, probe (address read twice).
- **Raw state**: Strip 6 → 6, index 5 → 5; committed `https://example.org/` (two reads); surface hidden; the welcome tab attached as wcId 7, the fixture's example.org tab (wcId 3) untouched; filled star. The Executor retracted its steps 3–5 "stale title strip" flag — it was the bookmarks bar's item all along.
- **Expected**: The new welcome tab itself navigates to `https://example.org/` (same tab, count unchanged) — bookmarks were reachable from the welcome surface.
- **Verdict**: pass — count 6 → 6 and index 5 → 5 (decisive); committed `https://example.org/` with a filled star; the surface gone; bookmarks were reachable and actionable from the welcome surface via the bar.
- **Evidence**: `step-7-before.json`, `step-7-bmbar-before.json`, `step-7-tabs-before.json`, `step-7-before.png`, `step-7-screenshot.png`, `step-7-snapshot.txt`, `step-7-tabs-after.json`, `step-7-probe.json`
- **Validator notes**: the Executor's retraction of its earlier "stale strip" note was commentary, not weighted.

### Checkpoint 8 — PASS
- **Actions taken**: Ctrl+T (window 1) and before-probes on both windows; typed `https://example.com` in the welcome home card and clicked Set; window screenshot, a11y tree, probe, `enumerateTabs`, `enumerateWindows`, window 2 probe + screenshot; switched to the Settings tab, guest screenshot + a11y tree.
- **Raw state**: Window 1: strip 7 → 7, same tab, still a viewless welcome tab (address bar empty, no `enumerateTabs` entry) with the field showing `https://example.com` and "Saved — new tabs will open here."; engine block hidden. Window 2's original welcome tab re-rendered with the saved value and confirmation — no navigation (the retired DD7 auto-attach, observed retired). Settings: home field `https://example.com`; DuckDuckGo the only checked radio; Settings' own status line empty on a fresh open (only Save/Clear write it).
- **Expected**: The welcome surface stays on the tab: the home page field shows `https://example.com` with a saved confirmation beneath it; the tab is still a welcome tab. Settings (open it) shows `https://example.com` in the home page field; the search engine is still DuckDuckGo.
- **Verdict**: pass — the welcome surface stayed on the same tab (7 → 7, index 6; `enumerateTabs` byte-identical before/after — no navigation) showing `https://example.com` with "Saved — new tabs will open here."; Settings renders the value with DuckDuckGo still the sole checked radio.
- **Evidence**: `step-8-before.json`, `step-8-tabs-before.json`, `step-8-w2-before.json`, `step-8-screenshot.png`, `step-8-snapshot.txt`, `step-8-probe.json`, `step-8-tabs.json`, `step-8-windows.json`, `step-8-w2-after.json`, `step-8-w2-screenshot.png`, `step-8-settings.png`, `step-8-settings-snapshot.txt`
- **Validator notes**: window 2's state captured here belongs to row 9 and was not weighted.

### Checkpoint 9 — PASS
- **Actions taken**: Before probe + `enumerateTabs` on window 2; Ctrl+T on its chrome (wcId 5); window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`; then re-activated window 2's original welcome tab and probed/captured it.
- **Raw state**: Window 2 strip 1 → 2; the new tab (wcId 8) opened directly on `https://example.com/` with no welcome surface. The original welcome tab is still a welcome tab showing the saved value and confirmation, its address bar empty — it never navigated (`activeTabWcId` null while active). Same PID throughout (2328323).
- **Expected**: The new tab opens on `https://example.com/` — the set preference reached the other window without a restart. Window 2's original welcome tab is still a welcome tab (it now shows the saved home page; it no longer auto-navigates).
- **Verdict**: pass — window 2's new tab (wcId 8) rendered `https://example.com/` live (PID unbroken, no restart); its original welcome tab is still a welcome tab showing the saved value and confirmation with an empty address bar — it did not auto-navigate.
- **Evidence**: `step-9-before.json`, `step-9-tabs-before.json`, `step-9-screenshot.png`, `step-9-snapshot.txt`, `step-9-probe.json`, `step-9-tabs-after.json`, `step-9-windows.json`, `step-9-orig-probe.json`, `step-9-orig-screenshot.png`, `step-9-orig-windows.json`
- **Validator notes**: none.

### Checkpoint 10 — PASS
- **Actions taken**: (Quit/relaunch performed by the Orchestrator — see Orchestrator Notes; replacement Executor.) Read-only topology check against the attestation; probes on both windows' chrome; restored screenshot; Ctrl+T in window 1; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`.
- **Raw state**: Restored: window 1 three "Example Domain" tabs, window 2 one; no welcome title anywhere; the surface hidden in both. Ctrl+T: strip 3 → 4, the new tab (wcId 7) committed to `https://example.com/` with the surface hidden; every restored tab unchanged.
- **Expected**: No welcome tab was restored (the restored tabs are only the web tabs: the fixture's `example.org` tab and the tabs from steps 6, 7, and 9 — the step-8 tab and window 2's original welcome tab are not web tabs, since they never attached). The new tab opens on `https://example.com/` — the boot window honors the home page on its first new tab.
- **Verdict**: pass — the on-disk session held exactly the four web tabs (no welcome/burner/Settings record); the restored strips carry no welcome title; Ctrl+T in the boot window committed `https://example.com/` with the surface hidden; the replacement Executor's report is corroborated by the Orchestrator's own attestation dumps.
- **Evidence**: `step-10-tabs-prequit.json`, `step-10-session-doc.json`, `step-10-windows-restored.json`, `step-10-tabs-restored.json`, `step-10-before.json`, `step-10-w2.json`, `step-10-restored.png`, `step-10-screenshot.png`, `step-10-snapshot.txt`, `step-10-probe.json`, `step-10-tabs-after.json`
- **Validator notes**: no continuity concern from the Executor replacement.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh scratch profile; the fixture folded in as a setup row before step 1. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled; the `[a11y]` row judged functionally). Step 2 was dispatched in parallel with checkpoint 1's judgment.
- **Purpose**: third of four gate runs for the HAT's behavior changes — rows 8, 9, and 10 were re-authored for the DD7 pivot (Set stays put; other windows' welcome tabs no longer auto-navigate; the step-8 tab is no longer a web tab at restore). All three passed on rendered evidence; every other row unchanged and re-observed. The Validator took its own screenshot at nearly every step.
- **Fixture on this build**: with both preferences set in Settings nothing auto-attached — the boot welcome tab stayed a dormant viewless strip entry throughout, never activated, excluded from the stored session.
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden; block visibility by className/computed display; strip counts from the chrome DOM. The Executor initially misread the bookmarks bar's one item ("E Example Domain") as a stale title strip in steps 3–5 and retracted it at step 7 — for the crew file: the bookmarks bar is app-wide and shows on every web-class or welcome tab. The stale `sheetWcId` field appeared after the dismissed bookmark-edit sheet (`sheetVisible: false`) — the known transient.
- **Executor transcript loss (third occurrence in this project's runs)**: the original Executor could not be resumed for step 10 after its step-9 report; a replacement Executor was spawned with the established facts (relaunch attestation, on-disk session, restored topology, apparatus rules) and performed step 10 — the Flight-1 recovery shape. The original's closing summary is therefore not available; its per-step reports are the record.
- **Restart attestation (step 10)**: PID 2328323 held the profile's `app.db` from before the fixture through checkpoint 9; SIGTERM, clean exit in ~2 s; the stored `session` document held exactly the four web tabs (fixture example.org, example.net, example.org in window 1; example.com in window 2) — no welcome/burner/Settings record; relaunch on the same profile at 21:57:10 local (new PID 2351790), key re-minted, crew env rewritten, restored topology briefed and saved as evidence.

## Closing Summaries

### Executor closing
_The original Executor's transcript was lost after its step-9 report (not resumable); no closing summary from it. Its per-step reports (fixture and steps 1–9) stand, including its step-7 retraction of the "stale title strip" note (it was the bookmarks bar's item). The replacement Executor's closing follows:_ Step 10 clean: the read-only check matched the attested restored topology exactly; Ctrl+T produced a real web tab (wcId 7) committed to `https://example.com/` (two identical reads) with the surface hidden; no welcome tab restored in either window; the attach-only CLI with the re-minted key worked for every tool used.

### Validator closing
10/10, every verdict grounded in the Validator's own screenshots and probes; no INCONCLUSIVE needed. The two re-authored rows behaved exactly as the DD7-pivot brief described, propagating live to other windows without auto-navigation; the restart excluded every never-attached welcome/burner record and restored exactly the four web tabs; coincident `example.*` titles were never treated as sufficient — strip index/count pairs decided every same-tab claim; the Executor replacement raised no continuity concern.

## Operator Notes
_None._
