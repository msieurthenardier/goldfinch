# Behavior Test Run: welcome-home-routing — 2026-08-25-02-45-35

**Spec**: [welcome-home-routing.md](../welcome-home-routing.md)
**Status**: pass
**Started**: 2026-08-25T02:45:35Z
**Completed**: 2026-08-25T03:14:30Z
**Duration**: 00:28:55
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-home-routing/2026-08-25-02-45-35/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed. 0 failed; 0 inconclusive. On an existing profile: clearing the home page turns every new-tab path (Ctrl+T, a second window, a burner tab) into a welcome tab in the right jar showing only the home-page block; typing an address or clicking a bookmark navigates that same tab in place (count and strip index unchanged, the record acquiring its view); setting the home page from the welcome surface reaches Settings, the on-disk row, and the other window live; across a clean quit/relaunch no welcome tab is restored and the boot window's first new tab honors the home page (squawk 0005 closed). One side finding: Settings' status line goes stale after an external write — squawk 0010.

## Checkpoint Results

### Setup row (fixture) — completed, no judgment
- **Actions taken**: Settings via the `kebabActionSettings` seam: home page `https://example.com` + Save, DuckDuckGo radio (first click missed — the "Saved" status line insertion shifted the layout ~24 px; re-captured and re-clicked); navigated the Google tab to `https://example.org`, clicked the chrome star. The bookmark-edit sheet opened; every write op against the sheet was refused (`secret-sheet`) — the Executor read `src/main/automation/resolve.js`/`engine.js` and found sheets admit only `captureScreenshot`/`readDom`/`readAxTree` even for allowlisted menu types. Dismissed via tab switch. The Executor reported the bookmark as not created after querying the `documents` store.
- **Orchestrator resolution**: the star click creates the bookmark immediately (`bookmarks-client.js` `activateStar` → `bridge.bookmarkAdd`; the sheet's Done only closes it) and the profile's `bookmarks` table holds the `https://example.org/` row in the `personal` jar — fixture satisfied. Two apparatus corrections for the crew notes: (1) allowlisted sheets are **read-only** to automation — no click/type/key ever resolves a sheet wcId; (2) re-capture Settings before every click — the status line's appearance shifts controls below it.
- **Evidence**: `step-0-settings-initial.png`, `step-0-settings.png`, `step-0-settings-snapshot.txt`, `step-0-bookmark-sheet.png`, `step-0-settings-row.json`

### Checkpoint 1 — PASS
- **Actions taken**: Activated the Settings tab; clicked the home page **Clear** button; captured whole-window + guest screenshots and the a11y tree.
- **Raw state**: Home page field rendered empty; status line reads exactly "Cleared — new tabs will open the welcome page until you set one."; a11y radios: DuckDuckGo `true`, all seven others `false`.
- **Expected**: The home page field is empty and a status line explains that new tabs will open the welcome page. The search engine control still shows DuckDuckGo. [a11y]
- **Verdict**: pass — both screenshots render an empty Home page field with the exact status line beneath it and DuckDuckGo still the only selected radio.
- **Evidence**: `step-1-pre-clear.png`, `step-1-screenshot.png`, `step-1-screenshot-2.png`, `step-1-snapshot.txt`
- **Validator notes**: the bookmark-fixture false alarm did not bear on this row.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` query of the scratch profile’s settings row at 02:55:39Z, saved verbatim; no UI interaction.
- **Raw state**: `homePage` present, `null`; `searchEngine` present, `"duckduckgo"`; twelve top-level keys (version … bookmarksBarEnabled, searchEngine).
- **Expected**: `[mixed-frame]` `homePage` is present and `null`; `searchEngine` is `"duckduckgo"`.
- **Verdict**: pass — the stored row shows `homePage: null` and `searchEngine: "duckduckgo"`; the filesystem observable carries the mixed-frame verdict.
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: none.

### Checkpoint 3 — PASS
- **Actions taken**: Ctrl+T on window 1’s chrome; `enumerateTabs` before/after (the welcome record has no web contents, so the MCP tab list omits it — strip state read from chrome DOM); `captureWindow`; chrome `readAxTree`; chrome `evaluate` of address-bar and panel state.
- **Raw state**: Strip: 3 tabs (was 2), "Welcome to Goldfinch" selected. Address bar `value ""`, placeholder "Search or enter address", `readOnly false`. Panel visible with heading "Welcome to Goldfinch" and one block — Home page label, empty input, "Set", hint "Or just type an address above."; engine block empty (`engineBlock: 0`).
- **Expected**: A new tab opens titled "Welcome to Goldfinch" showing the welcome surface with the **home page** block only (no search-engine block); the address bar is empty, shows its placeholder, and is writable; the tab strip shows one more tab than before.
- **Verdict**: pass — screenshot and a11y tree agree: a third, selected "Welcome to Goldfinch" tab; an editable, empty address bar with its placeholder; a single Home page block; a full-text scan of the snapshot finds no engine names or "Search engine" text.
- **Evidence**: `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-tabs.json`
- **Validator notes**: none.

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+N on window 1’s chrome; `enumerateWindows` (window 2 booted on the first poll, chrome wcId 5, `activeTabWcId: null`); `captureWindow {windowId:2}`; window 2’s chrome `readAxTree` + `evaluate` probe.
- **Raw state**: Window 2: one tab, "Welcome to Goldfinch", selected; address bar empty with placeholder, `readOnly false`; panel visible with the Home page block only (`engineBlock: 0`) — composition identical to window 1’s welcome tab.
- **Expected**: The second window's initial tab is a welcome tab (home block only) — the cleared preference reached a window that did not make the change.
- **Verdict**: pass — window 2 (distinct chrome wcId 5, last-focused) opened with a single, active "Welcome to Goldfinch" tab, empty writable address bar, Home page block only.
- **Evidence**: `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-windows.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: `evaluate` `openNewTab(makeBurner())` on window 1’s chrome (the container menu is a non-automatable sheet; this is the code path its item runs); `captureWindow`; chrome `readAxTree`; `evaluate` probe.
- **Raw state**: Strip: 4 tabs, the new 4th selected, titled "Welcome to Goldfinch"; a11y node with description "Burner (burner)" for the active tab’s jar indicator; the dot renders orange vs green on the others. Panel visible: Home page block plus the line "This choice is saved for all of Goldfinch."; engine block empty. Bookmarks bar not visible (setting off).
- **Expected**: The burner tab is a welcome tab carrying the burner note ("saved for all of Goldfinch") in the burner jar.
- **Verdict**: pass — the active 4th tab shows an orange jar dot with an a11y description "Burner (burner)", and the panel renders the burner note; a welcome tab in the burner jar.
- **Evidence**: `step-5-screenshot.png`, `step-5-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 6 — PASS
- **Actions taken**: Activated window 1’s step-3 welcome tab by clicking its strip button (chrome); recorded a before-probe (tabs/selectedIndex/titles); typed `example.net` in the chrome address bar, Enter; after-probe, `enumerateTabs`, `captureWindow`, chrome `readAxTree`.
- **Raw state**: Tab count 4 → 4; selected index 2 → 2; only the 3rd strip title changed ("Welcome to Goldfinch" → "Example Domain"); `#address.value` = `https://example.net/`; `welcomeVisible: false`; `enumerateTabs` shows a new wcId 6 at `https://example.net/` (the record that had no wcId now has one). Screenshot: the 3rd tab renders the example.net page with lock and star icons; the burner welcome tab is untouched.
- **Expected**: That **same** tab navigates to `https://example.net/` — the tab count is unchanged and the tab keeps its strip position; the welcome surface is gone from it.
- **Verdict**: pass — before/after probes show tab count 4 → 4 and selected index 2 → 2; `#address.value` is `https://example.net/`; the welcome panel is hidden; the record acquired wcId 6 in place. The burner welcome tab is untouched.
- **Evidence**: `step-6-before.json`, `step-6-after.json`, `step-6-tabs.json`, `step-6-screenshot.png`, `step-6-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 7 — PASS
- **Actions taken**: *Setup*: enabled the bookmarks bar with Ctrl+Shift+B (existing toggle) and located the `Example Domain` item. *Step*: Ctrl+T (5th tab, welcome); before-probe; clicked the bookmark on the chrome; after-probe, `enumerateTabs`, `captureWindow`, `readAxTree`.
- **Raw state**: Tab count 5 → 5; selected index 4 → 4; the bookmarks bar was visible on the welcome tab before the click; `#address.value` = `https://example.org/`; `welcomeVisible` true → false; `enumerateTabs` shows a new wcId 7 at example.org for that tab; other tabs unchanged; the burner welcome tab still viewless. Screenshot: the example.org page with a filled star and the bar below the address bar.
- **Expected**: The new welcome tab itself navigates to `https://example.org/` (same tab, count unchanged) — bookmarks were reachable from the welcome surface.
- **Verdict**: pass — the bar was rendered on the welcome tab before the click; after it, same tab (5 → 5, index 4 → 4), `https://example.org/` committed, panel hidden, new wcId 7 in place.
- **Evidence**: `step-7-bar.png`, `step-7-before.json`, `step-7-after.json`, `step-7-tabs.json`, `step-7-screenshot.png`, `step-7-snapshot.txt`
- **Validator notes**: the Ctrl+Shift+B setup action was excluded from judgment.

### Checkpoint 8 — PASS
- **Actions taken**: Ctrl+T (6th tab, welcome); before-probe; on the chrome, clicked the welcome panel’s input, typed `https://example.com`, clicked Set; after-probe, `enumerateTabs`, `captureWindow` (windows 1 and 2); activated Settings and captured guest screenshot + a11y tree; read the settings row.
- **Raw state**: Tab count 6 → 6; selected index 5 → 5; strip title "Welcome to Goldfinch" → "Example Domain"; `#address.value` = `https://example.com/`; panel hidden; new wcId 8 in place. Settings: Home page field renders `https://example.com`; DuckDuckGo still the only checked radio. On-disk: `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`. Side observations: Settings’ status line still reads the earlier "Cleared — …" text (the field is correct) — Orchestrator logs a squawk; window 2’s welcome tab auto-attached to example.com on the broadcast (DD7 designed behavior) — noted for step 9’s baseline.
- **Expected**: The welcome surface leaves the tab: the tab is now on `https://example.com/`. Settings (open it) shows `https://example.com` in the home page field; the search engine is still DuckDuckGo.
- **Verdict**: pass — the same tab (6 → 6, index 5) is on `https://example.com/` with the panel gone; Settings’ field shows `https://example.com` and DuckDuckGo stays selected; the on-disk row agrees.
- **Evidence**: `step-8-before.json`, `step-8-after.json`, `step-8-tabs.json`, `step-8-screenshot.png`, `step-8-window2-check.png`, `step-8-screenshot-2.png`, `step-8-snapshot.txt`, `step-8-settings-row.json`
- **Validator notes**: the Settings status line still shows the earlier "Cleared — …" text while the field holds a value — outside this row’s Expected Result; tracked as squawk 0010.

### Checkpoint 9 — PASS
- **Actions taken**: Before-probe on window 2’s chrome (its one tab already on example.com via DD7 auto-attach); Ctrl+T on window 2; after-probe, `enumerateTabs`, `captureWindow {windowId:2}`, `readAxTree`.
- **Raw state**: Window 2 tab count 1 → 2; the new tab active; `#address.value` = `https://example.com/`; `enumerateTabs`: wcId 9 (existing) and wcId 10 (new) both on example.com; `welcomeVisible: false`; no welcome panel appeared.
- **Expected**: The new tab opens on `https://example.com/` — the set preference reached the other window without a restart.
- **Verdict**: pass — window 2’s new tab (wcId 10) opened directly on `https://example.com/` with no welcome panel; the preference set in window 1 reached window 2 without a restart.
- **Evidence**: `step-9-before.json`, `step-9-after.json`, `step-9-tabs.json`, `step-9-screenshot.png`, `step-9-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 10 — PASS
- **Actions taken**: *Out-of-band (Orchestrator)*: pre-quit row read (`version 3`, `homePage: https://example.com`, `searchEngine: duckduckgo`, `restoreSession: true`); confirmed the Electron main PID held the scratch `app.db`; SIGTERM → exit in ~1 s; post-quit row unchanged; the stored `session` document held exactly six web tabs (window 1: example.org, example.net, example.org, example.com; window 2: example.com ×2) — no welcome record, no burner, no Settings tab (DD9, structural). Relaunched on the same `XDG_CONFIG_HOME`; new key minted; crew env rewritten; restored topology read: window 1 four web tabs (chrome wcId 1), window 2 two (chrome wcId 2), welcome panel hidden in both. *Executor*: re-sourced the new key; verified the restored topology; captured both windows before touching anything; then Ctrl+T in window 1 with before/after probes, `enumerateTabs`, `captureWindow`, `readAxTree`.
- **Raw state**: Restored: window 1 = 4 web tabs, window 2 = 2, `welcomeVisible: false` in both, no burner tab anywhere (screenshots + a11y agree). Ctrl+T: window 1 count 4 → 5, new tab active, `#address.value` = `https://example.com/`, new wcId 9, no welcome panel at any point.
- **Expected**: No welcome tab was restored (the restored tabs are only the web tabs from steps 6–9). The new tab opens on `https://example.com/` — the boot window honors the home page on its first new tab.
- **Verdict**: pass — the restored topology is exactly the six web tabs from steps 6–9 with no welcome, burner, or Settings tab; the boot window’s first Ctrl+T opened directly on `https://example.com/` (new wcId 9) with no welcome panel.
- **Evidence**: `step-10-restored.json`, `step-10-screenshot.png`, `step-10-screenshot-2.png`, `step-10-tabs-restored.json`, `step-10-before.json`, `step-10-after.json`, `step-10-tabs.json`, `step-10-screenshot-3.png`, `step-10-snapshot.txt`
- **Validator notes**: none.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh scratch profile launched for the run. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled); the base Validator judged the `[a11y]` row functionally.
- **Apparatus**: the attach-only CLI over `scripts/lib/mcp-client.mjs` with the run's minted admin key (port 49707); the crew read the crew file's Project Apparatus Notes (squawk 0008) before `[READY]`. Session-registered goldfinch MCP tools forbidden. The welcome surface, tab strip, address bar, and bookmarks bar are chrome DOM — driven and observed entirely through the chrome wcId (`click`/`typeText`/`pressKey` + `evaluate` probes + `captureWindow` + `readAxTree`); Settings (internal page) via the `kebabActionSettings` seam and guest-coordinate clicks; the burner tab via the `openNewTab(makeBurner())` seam published by leg 1 (the container menu is a non-automatable sheet). Small before/after `evaluate` probes (tab count, selected index, titles, `#address.value`, `welcomeVisible`) were the decisive evidence for every "same tab, count unchanged" row — screenshots alone could not distinguish tabs whose titles coincide ("Example Domain" on example.org/.net/.com).
- **A welcome tab is invisible to `enumerateTabs`** (no web contents) — the strip is read from chrome DOM; `enumerateWindows` reports `activeTabWcId: null` for it. Recorded for the crew notes. `enumerateTabs`' `windowId` argument returns rows for all windows — treat it as informational.
- **Fixture setup (folded into checkpoint 1)**: the star click persists a bookmark immediately (`activateStar` → `bookmarkAdd`); the bookmark-edit sheet only edits/closes. The Executor's "bookmark not created" flag came from querying the `documents` store — the `bookmarks` table held the row. Apparatus correction learned: allowlisted sheets admit **only** `captureScreenshot`/`readDom`/`readAxTree` — no write op ever resolves a sheet wcId (`resolve.js` / `engine.js` `allowSheet`); the crew notes' "bookmark-edit is automatable" holds for reads only. Second correction: re-capture the Settings page before every click — the status line's appearance shifts the controls beneath it by ~24 px.
- **Checkpoint 7 setup**: the bookmarks bar was enabled with the existing Ctrl+Shift+B toggle (`toggle-bookmarks-bar`) inside the checkpoint; the Validator excluded it from judgment.
- **Checkpoint 8 side findings**: (a) Settings' `#home-page-status` kept the earlier "Cleared — …" text after the welcome surface's Set — the field and the row were correct; logged as **squawk 0010** (defect, routine). (b) Window 2's welcome tab attached itself to the newly set home page on the broadcast — DD7's designed auto-attach; it changed step 9's baseline (Ctrl+T there opened a second tab, which is what the row judged). Spec authors: an earlier step's broadcast can shift a later step's "before" state across windows — say so in the row when it matters.
- **Step 10 out-of-band**: pre-quit row read; SIGTERM to the PID holding the scratch `app.db` (exit ~1 s); the stored `session` document held exactly the six web tabs and no welcome/burner/Settings record (DD9 observed on disk, not inferred); relaunch on the same `XDG_CONFIG_HOME`; new key; crew env rewritten; restored topology briefed and matched the Executor's fresh read exactly. The `session-restore` procedure is now proven twice.
- **Squawk 0005 closed by observation**: checkpoint 10's first Ctrl+T in the boot window honored the home page — the boot seed works.

## Closing Summaries

### Executor closing
Clean across all 10 steps. (1) Sheets are read-only to automation, categorically — `click`/`typeText`/`pressKey`/`dragPointer`/`evaluate`/`injectScript` never pass `allowSheet`; only `captureScreenshot`/`readDom`/`readAxTree` do — so the crew-file claim that the bookmark-edit sheet can be clicked is wrong for write ops; a star click alone persists a bookmark; a tab switch dismisses a stuck sheet. (2) Settings layout shifts after Save/Clear (status line insertion, ~20–24 px) — re-screenshot before the next click. (3) Status-line staleness across write paths (squawk 0010). (4) DD7 auto-attach materializes another window's pending welcome tab when the home page is set — designed, but it silently shifts a later step's baseline. Environment: no MCP drops; the out-of-band relaunch restored exactly the briefed topology; `enumerateTabs`' `windowId` argument is informational, not a filter; no burner tab was restored.

### Validator closing
Clean run — every row had strong, agreeing evidence; no inconclusives. Spec-quality: (1) the one real defect (stale status line) could not fail a row because step 8 asserts only the field value — consider a "status line matches current state" assertion in a revision. (2) The `[mixed-frame]` row-per-observable shape worked cleanly. (3) Negative assertions ("no engine block", "surface gone") were confirmed by grepping the full a11y dump for engine names — worth an explicit apparatus hint in the spec so Executors capture it proactively. (4) Before/after `selectedIndex` + tab-count probes were the decisive evidence for same-tab rows (titles coincide) — worth naming in AUTHORING.md as the preferred evidence shape. (5) The out-of-band relaunch procedure worked exactly as documented. No apparatus problems this run.

## Operator Notes

_(none yet)_
