# Behavior Test Run: multi-window-shell — 2026-07-15-05-54-21 (run 2 — PASS, spec → active)

**Spec**: [multi-window-shell.md](../../multi-window-shell.md) (draft + the three run-1 errata)
**Status**: pass
**Started**: 2026-07-15T05:54:21Z
**Completed**: 2026-07-15T06:05:00Z (approx)
**Duration**: ~00:10 (single pass, no repairs)
**Executor**: fresh agent (batched 1–5, 6–9)
**Validator**: fresh agent (single judgment pass; PNGs inspected directly)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/multi-window-shell/2026-07-15-05-54-21/` (90 files, 3 PNGs).

## Summary

**9 / 9 checkpoints PASS** — the M09 Flight 6 multi-window shell verified
end-to-end on the authored sequence, no repair scripts. The DD-series
claims are directly evidenced, not inferred:

- **New Window (real classifier path)**: Ctrl+N → accessor retargeted to
  the new chrome (deterministic last-focused seed — no OS-focus read);
  exactly one fresh boot home tab; window 1 untouched (window-scoping
  proven from both sides); rendered chrome complete [render-correct].
- **Roaming menus (DD7)**: the ONE sheet rendered window 2's kebab (six
  items, New window first), aria-expanded true on W2's trigger and false
  on W1's, Escape's channel-7 routed to the ATTACHMENT window.
- **Move to new window (DD5)**: same sheet rendered the tab-context menu;
  the moved tab arrived in window 3 with **the SAME wcId** (re-parent
  path — the spec's discriminator; destroy/recreate excluded), work jar
  intact, active, NO boot tab; source strip closed ranks with a sane
  next-activation.
- **Live history survived the re-parent** (the run-1 unproven claim,
  pinned this run by the step-2 commit-settle gate): goBack on the same
  wcId → page 2, goForward → page 3, no repair.
- **Close-one-of-N (DD3)**: app alive, dead chromes refused
  `no-such-contents` (the M4 deferred destroy), accessor
  membership-validated (unchanged, then first-record fallback).
- **Whole-window capture (DD4)**: two Ctrl+Shift+T reopens restored the
  closed window's tabs LIFO, NEW wcIds, **appended** (the append-sentinel
  rule), with full `navEntries` fidelity (goBack on the second reopen →
  page 2).
- **Quit-on-last**: pid exited before the first 0.5 s poll; endpoint
  connection-refused (not a hang); orderly log tail.

## Known-benign artifact (carried; codify on next spec touch)

`[tab-create] navigationHistory.restore rejected: ERR_ABORTED` — exactly
once per run (2/2 deterministic), timestamped at the SECOND (multi-entry)
reopen's renderer boot; end states fully correct both runs. Validator
disposition: benign-with-carry, do not gate; worth codifying an expected
count (0–1, benign) in step 8's Expected Results on the next spec touch,
and a HAT eye in case a future Electron changes the promise's behavior.

## Apparatus notes (debrief carries)

- The port pin (49707) fails "in use" despite `ss` showing free — 2/2
  reproducible; free-fallback → 49709 both runs. Launch-script port-check
  quirk; carry to tooling notes so it isn't re-diagnosed per run.
- Step 6's `openTab` census shows a transient "New tab" title if read
  before commit — harmless here (step 8 re-reads); add a settle poll if a
  future variant asserts T3's title immediately.

## Checkpoint Results (compact)

| # | Verdict | Anchor evidence |
|---|---------|-----------------|
| 1 | PASS | step1-boot-bracket-enumerateTabs-raw.txt (D only) |
| 2 | PASS | step2-commit-settle-gate-raw.txt, step2-history-gate-raw.txt ({length:2}) |
| 3 | PASS | step3-getChromeTarget-poll-raw.txt (C2=5), step3-window2-chrome.png |
| 4 | PASS | step4-readAxTree-S-raw.txt (menu, New window first, focus) |
| 5 | PASS | step5-boot-bracket-w3-enumerateTabs-raw.txt (EXACTLY wcId 4 — same-wcId) |
| 6 | PASS | step6-t2-page3.png, step6-window3-chrome.png, goBack→page2 raw |
| 7 | PASS | step7-dead-C2-evaluate-raw.txt (no-such-contents; app alive) |
| 8 | PASS | step8-reopen{1,2}-w1-strip-raw.txt (LIFO append), step8-R2-after-goBack-readDom-raw.txt (page2) |
| 9 | PASS | step9-shell-liveness-raw.txt (pid exit <0.5 s; conn refused) |

## Orchestrator Notes

- First-run-of-draft discipline: run 1 surfaced three spec errata
  (commit-settle gate, boot-census settle, read-order note — see
  [run 1's log](2026-07-15-05-32-46.md)); FD folded them and ordered a
  full fresh re-run rather than judging the repaired evidence (the F1
  precedent). Spec `draft` → `active` on this run.
- Key hygiene: adminKey held in a keyholder process env, pulled per call —
  never in files/argv; mint line redacted from the persisted stdout log.
