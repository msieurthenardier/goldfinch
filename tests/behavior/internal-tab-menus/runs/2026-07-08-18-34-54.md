# Behavior Test Run: internal-tab-menus — 2026-07-08-18-34-54

**Spec**: [tests/behavior/internal-tab-menus.md](../../internal-tab-menus.md)
**Status**: pass
**Started**: 2026-07-08T18:34:00-05:00
**Completed**: 2026-07-08T18:38:00-05:00
**Duration**: ~00:04:00
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed)

## Summary
7 / 7 rows passed (row 1 setup + rows 2–7 checkpoints). 0 failed; 0 inconclusive. **First run of this spec (was: never).**

Native WebContentsView surface. Admin MCP surface on 8899. Chrome wcId 1, menu-overlay sheet wcId 3. Active tab throughout: the internal **Settings** page (`goldfinch://settings/`, chrome address chip `data-state="internal"`, aria-label "Secure Goldfinch page"). Internal tabs are not enumerable and `evaluate` refuses internal wcIds — so internal-tab checks re-based on chrome trigger state + `captureWindow` pixels, per the spec.

**Capture path — OS-grab (authoritative).** `captureWindow` composited the menu-overlay sheet's menus (rows 2/4) and the site-info popup (row 6) above the opaque internal view — the OS-grab path is active, so render-above presence is authoritative.

## Step Results

### Step 1 — PASS (setup)
- **Actions**: `getChromeTarget()` → wcId 1; opened kebab → **Settings** (activated the Settings item on sheet wcId 3 via DOM click); `captureWindow` + `readDom(chrome)`.
- **Raw state**: Settings internal page renders in `#webviews` (pixels); `#address-chip` `data-state="internal"`, aria-label "Secure Goldfinch page". Sheet wcId = 3.
- **Verdict**: pass (setup).
- **Evidence**: [step1-settings-active.png](../../../../tmp/behavior-tests/goldfinch/internal-tab-menus/2026-07-08-18-34-54/step1-settings-active.png)

### Step 2 — PASS [render-correct]
- **Actions**: opened the kebab (⋮) via `click(1,1372,63)`; `captureWindow`, `readDom(sheet=3)`, `readDom(chrome)`.
- **Raw state**: **Pixels** — the kebab menu (Settings/Downloads/Print/Exit) is composited **above** the Settings page at the top-right (the exact F3 occlusion regression, now protected on the sheet path); internal view live underneath. **Corroborating** — `#sheet-menu` `data-menu-type="kebab"`, `role="menu"`; kebab `aria-expanded="true"`.
- **Expected**: kebab menu composited above the live internal view; sheet `data-menu-type="kebab"`; kebab `aria-expanded="true"`.
- **Verdict**: pass.
- **Evidence**: [step2-kebab-over-internal.png](../../../../tmp/behavior-tests/goldfinch/internal-tab-menus/2026-07-08-18-34-54/step2-kebab-over-internal.png)

### Step 3 — PASS
- **Actions**: `pressKey(sheet=3, 'Escape')`; `readDom(chrome)`.
- **Raw state**: kebab `aria-expanded="false"`; `document.activeElement.id === "kebab"` (Escape refocus). Settings renders at full bounds.
- **Expected**: menu gone; live Settings full bounds; kebab `aria-expanded="false"` and holds focus.
- **Verdict**: pass.

### Step 4 — PASS [render-correct]
- **Actions**: opened the container (▾) menu via `click(1,538,22)`; `captureWindow`, `readDom(sheet)`, `readDom(chrome)`.
- **Raw state**: **Pixels** — the container menu ("OPEN NEW TAB IN…" Default/Personal/Work/Banking/Shopping/Seed1–5…) composited above the live Settings page. **Corroborating** — `#sheet-menu` `data-menu-type="container"`; `#new-tab-menu` `aria-expanded="true"`.
- **Expected**: container menu above the live Settings page; `data-menu-type="container"`; `#new-tab-menu aria-expanded="true"`.
- **Verdict**: pass.
- **Evidence**: [step4-container-over-internal.png](../../../../tmp/behavior-tests/goldfinch/internal-tab-menus/2026-07-08-18-34-54/step4-container-over-internal.png)

### Step 5 — PASS
- **Actions**: `pressKey(sheet=3, 'Escape')`; `readDom(chrome)`.
- **Raw state**: `#new-tab-menu` `aria-expanded="false"`; Settings at full bounds.
- **Expected**: menu gone; live Settings full bounds; `#new-tab-menu aria-expanded="false"`.
- **Verdict**: pass.

### Step 6 — PASS [render-correct]
- **Actions**: clicked the address chip (🔒) via `click(1,180,63)` (chip rect center); `captureWindow`; then `pressKey(sheet=3,'Escape')` + `readDom(chrome)`.
- **Raw state**: **Pixels** — the site-info popup renders from the sheet **above** the live internal view, anchored under the chip, showing the **secure-page note** "You're viewing a secure Goldfinch page." (internal-page note variant, no action row — DD7). On Escape, focus returns to `#address-chip`. (Note: the site-info popup renders in a separate sheet popup element, not `#sheet-menu`; pixels were the authoritative observable, corroborated by the chip refocus.)
- **Expected**: site-info popup above the live internal view with the secure-page note; Escape dismisses and refocuses the chip.
- **Verdict**: pass.
- **Evidence**: [step6-siteinfo-over-internal.png](../../../../tmp/behavior-tests/goldfinch/internal-tab-menus/2026-07-08-18-34-54/step6-siteinfo-over-internal.png)

### Step 7 — PASS [render-correct]
- **Actions**: `pressKey(chrome=1, 'M', ['control'])` to open the media panel; `captureWindow`; read `#webviews` rect; then `Control+M` again to close; `captureWindow`; re-read rect.
- **Raw state**: `#webviews` width **1398 → 1038 → 1398** (panel open → close). With the panel open, the internal Settings view reflowed to the region beside the panel — no overlap, dead band, or clipping (the F3 `sendActiveBounds` regression class); the panel correctly shows "No media found on this page yet." (internal page). On close, the internal view restored to the full region.
- **Expected**: internal Settings view reflows beside the panel (no overlap/dead-band/clip); restores on close.
- **Verdict**: pass.
- **Evidence**: [step7-panel-open-internal.png](../../../../tmp/behavior-tests/goldfinch/internal-tab-menus/2026-07-08-18-34-54/step7-panel-open-internal.png)

## Orchestrator Notes
- Menu items on the sheet were activated by DOM `.click()` in `evaluate` (sheet is `menu-overlay.html`, not an internal-session view, so `evaluate` is allowed there); menu dismissal was `pressKey(sheet=3,'Escape')`. The internal tab's own state was read only via chrome (`data-state`, `aria-expanded`, `activeElement`) + pixels, since `evaluate`/`readDom` refuse the internal guest.
- **Variant (Downloads tab)** not run — optional; the Settings-page coverage above exercises the full row set. The kebab item list shown in row 2 confirms Downloads is reachable the same way.

## Closing Summaries

### Executor closing
First-ever run, full pass. The Flight-3 regression class is protected on the current sheet substrate: the kebab, container, and site-info menus all render **above** the opaque internal `goldfinch://settings` view (not occluded), dismiss cleanly with focus return, and the internal view **resizes** with the media panel (1398↔1038). The retired freeze-frame mechanism is gone — menus composite over the live internal page from the menu-overlay sheet.

### Validator closing
Every render-above check passed on the authoritative OS-grab path, each corroborated by the sheet's `data-menu-type` and the chrome trigger's `aria-expanded`. Site-info's secure-page-note variant for internal pages is correct (DD7). No WSLg sub-frame blip rose to a failure. Spec is accurate to the current surface; recommend promotion `draft → active`.

## Operator Notes
Verdict PASS (7/7). Recommend promoting this spec to `active`. The F3 occlusion + no-resize regression class is netted on the live surface.
