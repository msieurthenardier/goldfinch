# Behavior Test Run: unified-tab-controls — 2026-07-08-22-03-37

**Spec**: [tests/behavior/unified-tab-controls.md](../../unified-tab-controls.md)
**Status**: pass (with one INCONCLUSIVE-APPARATUS sub-observation on Enter-activation — see below)
**Started**: 2026-07-09T03:03:37Z
**Completed**: 2026-07-09T03:20:00Z
**Executor**: F06 SC3 behavior-test Executor (Witnessed self-run — admin MCP client, evidence-judged)
**Validator**: (self, artifact-judged — verdicts cite raw reads: `activeElement`, roving `tabindex`, `aria-expanded`, computed `outline`, jar-dot `backgroundColor`, `enumerateTabs`, AX tree, screenshots)

## Summary
The unified golden pill (`+ | ▾`) works by both mouse and keyboard on the landed F5 build. All 8 steps meet
their acceptance. Venue: Wayland ozone, **devicePixelRatio = 1** (screenshot 1400×900 == CSS px), so
coordinate clicks used CSS coords 1:1. The container menu renders as a separate overlay **sheet
WebContentsView (wcId 7)**; menu items were driven by `evaluate` `.click()` (mouse) and `pressKey` on the
sheet wcId (keyboard), per the apparatus sheet-wcId probe.

**One sub-observation (INCONCLUSIVE-APPARATUS, not a step failure):** synthetic **Enter** via `pressKey` does
not trigger the native-button **default-action click** on the `+` button or on container **menuitems** (both
activate reliably via **Space**); the `▾` button's explicit keydown handler *does* open the menu on Enter. The
spec permits "Enter **and/or** Space", so the keyboard-operability contract is met via Space. Whether Enter
should also activate is left to FD — most likely a `sendInputEvent`-Enter-vs-native-default-action apparatus
nuance rather than an app regression (Enter events demonstrably reach handlers, since `▾` opens on Enter).

## Step Results

### Step 1 — PASS (probe)
- `tools/list` includes `getChromeTarget`. `getChromeTarget()` → numeric chrome wcId=1. Baseline tab count = 4
  (Google selected) via `readAxTree(1)`.

### Step 2 — PASS (pill present, golden, hugging tabs) `[a11y]`
- The pill is a `DIV` with computed `background-color: rgb(245,197,24)` == `--accent` (#f5c518, golden). Rect
  x=983 w=54, sitting **immediately right of the tablist** (tablist right edge = 979; gap ~4px), and **left of
  the window controls** (x=1261) — left-aligned in the strip, not far-right. It contains two operable controls:
  `#new-tab` (`aria-label="New tab"`, "+") and `#new-tab-menu` (`aria-label="New tab in a container"`,
  `aria-haspopup="menu"`, `aria-expanded="false"`, "▾").

### Step 3 — PASS (mouse — new tab)
- `click(1, 999, 23)` on `+` → tab count 4→5; new tab became active (`activeIdx` last); active tab has **no**
  `.tab-jar` dot (default container) via `readDom`.

### Step 4 — PASS (mouse — container tab)
- The `▾` control's `aria-expanded` was **true** while the menu was open (`readDom(1)`). Located the sheet
  (wcId 7), DOM-clicked the **"Personal"** menuitem → a new tab opened (jarId=`personal`), count +1, and its
  strip button shows a `.tab-jar` dot with `backgroundColor: rgb(76,175,80)` == #4caf50 == Personal's color.
  `aria-expanded` returned to false after activation.
- Evidence: `step4-menu.png` (overlay sheet with colored jar dots), `step4-final.json`.

### Step 5 — PASS (keyboard — new tab + focus ring) `[a11y]`
- Focus anchor `click(1, 60, 22)` on a tab, then `pressKey('Tab')` → `document.activeElement.id === "new-tab"`
  (the `+` is keyboard-reachable). Computed style on the focused `+`: `outline: solid 2px rgb(30,31,37)` —
  **NOT `outline:none`**; a near-black 2px ring on the golden fill (#f5c518) is ≈10:1 contrast, well above the
  ≥3:1 the spec (DD2) requires. A focused-vs-unfocused `captureWindow()` pixel diff over the pill region shows
  a real delta (max channel diff 215, 364 changed px) — the ring renders (the WSLg byte-identical limit does
  **not** apply on this Wayland/DPR-1 venue). **Space** activates the `+`: isolated test 8→9 tabs (one new
  tab). *(Enter did not activate — see Summary; classified INCONCLUSIVE-APPARATUS, spec allows Space.)*
- Evidence: `step5-unfocused.png`, `step5-focused.png`, `step5.json`.

### Step 6 — PASS (keyboard — container tab) `[a11y]`
- Focus anchor + two `pressKey('Tab')` → focus on `#new-tab-menu`. `pressKey('Enter')` **opens the menu**
  (`aria-expanded` → true) and focus lands **inside the sheet** on the first menuitem "Default"
  (`role=menuitem`, `tabindex=0`; siblings `tabindex=-1`) — not stranded on `<body>`. `pressKey('ArrowDown')`
  on the sheet advances roving focus Default→Personal→Work. **Space** on "Work" opens a new tab (9→10),
  jarId=`work`, `.tab-jar` dot `backgroundColor: rgb(33,150,243)` == #2196f3 == Work's color; menu closed;
  `document.activeElement !== document.body`. *(Enter on the menuitem left the menu open / no tab — same
  Enter observation.)*
- Evidence: `step6-space.json`.

### Step 7 — PASS (keyboard — menu dismiss / focus restore) `[a11y]`
- Re-opened the `▾` menu by keyboard (Enter, `aria-expanded` → true), then `pressKey('Escape')` on the sheet →
  menu closes (`aria-expanded` → **false**) and focus is **restored to the `#new-tab-menu` trigger**
  (`document.activeElement.id === "new-tab-menu"`, not `<body>`).

### Step 8 — PASS (regression guard — tablist contract) `[a11y]`
- `readAxTree(1)`: exactly **1 `tablist`**, 10 `tab` children, exactly **1 selected**. DOM: the tablist
  `.contains(#new-tab)` and `.contains(#new-tab-menu)` are **both false** — the pill's `+`/`▾` are **not**
  members of the tablist. The pill restructure did not fold non-tab controls into the tablist.

## Evidence
`/tmp/behavior-tests/goldfinch/unified-tab-controls/2026-07-08-22-03-37/` — `step1-2.json`, `step3.json`,
`step4-menu.png`, `step4-final.json`, `step5-*.png/json`, `step6-space.json`, `step6-final.json`, `step7.json`,
`step8.json`.

## Orchestrator Notes
**Witnessed-pattern deviation**: single-agent Executor+Validator self-run; every verdict cites a raw machine
read. **Apparatus learning:** the pill shifts position as tabs are added/removed — re-locate `#new-tab` /
`#new-tab-menu` via `getBoundingClientRect` before each click. The container menu is a separate sheet
WebContentsView; probe its wcId (was 7 this run) and drive items via `evaluate`/`pressKey` on the sheet.
**Finding for FD:** synthetic Enter does not activate the `+` button or container menuitems (Space does; `▾`
opens on Enter) — classified INCONCLUSIVE-APPARATUS (likely a sendInputEvent-Enter vs native default-action
nuance), not asserted as an app regression.
