# Behavior Test Run: tab-keyboard-operability — 2026-07-14-18-39-32

**Spec**: [tab-keyboard-operability.md](../../tab-keyboard-operability.md)
**Status**: partial
**Started**: 2026-07-14T18:39:32Z
**Completed**: 2026-07-14T20:20:00Z (approx)
**Duration**: ~01:40
**Executor**: sub-agent (Sonnet), live SendMessage continuation
**Validator**: sub-agent (Sonnet), live SendMessage continuation

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/tab-keyboard-operability/2026-07-14-18-39-32/`
during the run; re-derivable by re-running the spec.

## Summary

**8 pass / 1 inconclusive** (of 9 rows; Step 2 is a setup row folded into
Step 3). First run since the M09 Flight 2 extension (Step 8 keyboard reorder;
Step 9 reorder-chord no-hijack). The one inconclusive is Step 3's visible
focus-ring delta — the **pre-classified WSLg apparatus limit**
(`document.hasFocus()` stays false under injected input, suppressing
`:focus-visible` paint; same signature the 2026-07-08 run pre-classified;
`.tab:focus-visible` is declared in styles.css and a real ring was captured
on a native rig on 2026-06-06). **No regression indicated**; affirmative
ring re-verification rides the mission HAT flight (native rig).

## Checkpoint Results

### Checkpoint 1 — PASS
`tools/list` includes `getChromeTarget`; chrome wcId 1 numeric.
Evidence: `step1-tools-list.txt`, `step1-getChromeTarget.txt`.

### Checkpoint 3 — INCONCLUSIVE (apparatus limit; setup Step 2 folded in)
- Setup: 3 tabs at distinct fixture paths (page1–3; default tab navigated to
  page1 — clean count).
- **Reachability half AFFIRMED**: ShiftTab entry from the address anchor
  landed on a `role=tab` inside the `tablist` (the selected tab — roving
  entry), AX + DOM agree.
- **Focus-ring visual: evidence gap** — the focused tab renders
  pixel-identically in the before/after frames (the only frame delta is an
  unrelated zoom-control reveal). `el.matches(':focus-visible') === false`,
  `document.hasFocus() === false` under injected input — the WSLg
  signature pre-classified as INCONCLUSIVE-APPARATUS-LIMIT in run
  2026-07-08-22-44-33. Product CSS declares the ring; not the
  `outline:none`-with-no-replacement failure mode.
- **Operator decision**: continued; native-rig ring check → HAT flight.
Evidence: `step3-focused/unfocused-screenshot.png`, `step3-axtree-tabs.json`,
`step3-focused-outline-style.txt`.

### Checkpoint 4 — PASS
Arrows move-and-activate (exercised at the wrap edges — walk started on the
last tab, same shape as the prior run); Home/End jump; exactly one
`aria-selected` after every press; focus never on `<body>`; the address bar
tracked each selected tab's distinct URL (the load-bearing discriminator).
Evidence: `step4-axtree-summary.json`, `step4-state-perpress.txt`.

### Checkpoint 5 — PASS
Keyboard Delete closed the focused tab (3→2); focus fell to a sibling tab
(never `<body>`); remaining tab selected with its content active; never zero
tabs. Evidence: `step5-before/after.txt`, `step5-axtree-tabs.json`.

### Checkpoint 6 — PASS
Each tab exposes a real `button` named "Close tab: {title}";
`aria-keyshortcuts` = `Delete Control+Shift+ArrowLeft Control+Shift+ArrowRight`
(AX + DOM agree). Evidence: `step6-close-affordances.json`,
`step6-dom-attrs.txt`.

### Checkpoint 7 — PASS
Single `tablist` with exactly the live tabs; one selected; roving entry
landed on the SELECTED tab — discriminating this run, since it was the
second of two DOM nodes. Evidence: `step7-structure.json`,
`step7-entrywalk-raw.txt`, `step7-postentry-focused.json`.

### Checkpoint 8 — PASS (new — M09 F2 keyboard reorder)
Chord moved the focused tab exactly one slot (confirmed in DOM order AND AX
tablist child order), focus retained, exactly one selected throughout;
`#tab-status` (`role=status`, `aria-live=polite`) read "Tab moved to
position 2 of 3" then "3 of 3" on restore; both chord directions driven.
- Branch note: the moved tab WAS the selected tab — the structurally forced
  branch (keyboard entry lands on the selected tab; auto-activating arrows
  re-couple focus and selection). The selection-decoupling branch is
  keyboard-unreachable in this widget; it was verified in the M09 F2 leg-1
  live spot-check and by the pointer-driven `tab-reorder` spec.
- **A11y caveat (non-blocking, actionable)**: `#tab-status` is created
  lazily on first reorder. A live region inserted at the same instant as its
  first content is not reliably announced by screen readers — live regions
  should pre-exist empty. Flagged to flight review as a small fix / F24
  live-regions candidate.
Evidence: `step8-before/after1/after2.txt`, `step8-axtree-summary.json`.

### Checkpoint 9 — PASS (extended — reorder chord in the no-hijack set)
Three probes, all null-effect on the strip: (A) address bar focused — with a
built-in positive control (Delete visibly consumed a character from the
input, proving key delivery; input restored byte-identical); (B1) chrome
renderer with focus on `<body>` — the exact document-level-handler hijack
vector, strongest case, new this run; (B2) keys delivered to the guest
webContents. DOM order, count, selection, `enumerateTabs`, and `#tab-status`
byte-identical to baseline after every probe.
Evidence: `step9-baseline.txt`, `step9-A/B1/B2-after.txt`,
`step9-axtree-final-summary.json`.

## Orchestrator Notes

- Live two-agent mode; cache-cold (fresh app launch; free-fallback port per
  the pin-if-free rule). Fresh agents (first run since the extension).
- Step 2 (setup row) folded into checkpoint 3 per protocol.
- Operator decisions: continue at Step 3 (apparatus limit, pre-classified).
- Carried to the mission HAT flight: native-rig focus-ring verification
  (existing item, reaffirmed).
- Carried to flight review: the `#tab-status` lazy-creation live-region
  caveat (small fix candidate).

## Closing Summaries

### Executor closing

All 9 steps executed; the only anomaly was Step 3's pre-classified rig limit
(hasFocus false under injected input); every later discriminator (AX, DOM
attributes, enumerateTabs, address value, #tab-status) was unaffected. 52
evidence files; no keys written anywhere. Step 9 was run three ways (address
input with positive control, chrome-body, guest delivery) to cover distinct
hijack vectors.

### Validator closing

Steps 1, 4–9 pass on independently re-read evidence; Step 3 inconclusive on
the ring visual only (reachability affirmed; no regression indicated —
classification consistent with the prior run). Observations: Step 8's lazy
live region (F24 candidate); Step 4 adjacency exercised at wrap edges; Step
8's decoupling branch structurally unreachable via keyboard (covered
elsewhere). The Step 9 positive control (visible Delete consumption in the
focused input) is exactly how a "nothing happened" row avoids false passes —
recommend keeping it as a standing pattern.

## Operator Notes

Run disposition: the extended contract holds; the single inconclusive is an
environment limitation with an existing native-rig HAT item and does not
gate the leg (consistent with the 2026-07-08 precedent). Fix candidate
(#tab-status pre-existing empty region) routed to the flight-end review
cycle.
