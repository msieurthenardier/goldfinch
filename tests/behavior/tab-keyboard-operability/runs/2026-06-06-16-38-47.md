# Behavior Test Run: tab-keyboard-operability — 2026-06-06-16-38-47

**Spec**: [tests/behavior/tab-keyboard-operability.md](../tab-keyboard-operability.md)
**Status**: pass
**Started**: 2026-06-06T16:38:47Z
**Completed**: 2026-06-06T16:55Z
**Duration**: ~00:16:00
**Executor**: a2d28c115c8c8e501
**Validator**: a80feeb872a196d68
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-keyboard-operability/2026-06-06-16-38-47/` during the run; re-derivable by re-running the spec.

## Summary
7 / 7 checkpoints passed. 0 failed; 0 inconclusive. (Step 2 = setup row, skipped.) F22 (tab-strip keyboard & screen-reader operability) verified live.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Raw state**: `:9222` reachable; renderer **page** target (`file://…/src/renderer/index.html`) selected, not a webview guest.
- **Verdict**: pass — correct target discipline (the #1 false-pass guard); guests present but not selected.

### Checkpoint 2 — SKIPPED (setup)
- Opened 3 tabs at `:8080/?n=1|2|3`. Note: `:8080` was a pre-existing **Concourse** instance (not the project's `a11y-media` fixture), which ignored `?n` and served one shell — collapsing the three fixture tabs to the same title/address. Only Google-vs-fixture was address-discriminable. Load-bearing a11y observables (aria-selected/activeElement/AX tree) were unaffected.

### Checkpoint 3 — PASS  [a11y]
- **Raw state**: one trusted `Tab` landed focus on a `role=tab` (`aria-selected=true`); AX focusedNodes = [RootWebArea 'Goldfinch', tab]. Screenshots show a clear focus delta — unfocused: active tab has only the inset accent top-bar; focused: same tab gains a full 2px `#f5c518` `:focus-visible` outline.
- **Verdict**: pass (rendered-state precedence — screenshot delta weighed above DOM). Gold-on-dark ring ≫ 3:1; not stranded on body.
- **Evidence**: `step-3-unfocused.png`, `step-3-focused.png`, `step-3-axtree.txt`.

### Checkpoint 4 — PASS  [a11y]
- **Raw state**: ArrowRight→tab-1 (addr `https://www.google.com/`); ArrowLeft→tab-4 (addr `http://127.0.0.1:8080/`); Home→first; End→last. Each press moved focus AND activation together; exactly one `aria-selected`; address bar tracked the selected tab; never stranded on body.
- **Verdict**: pass. (Coverage note: arrow traversal exercised only the Google↔fixture wrap boundary — the one address-discriminable pair — because the fixture collapsed the middle tabs; assertions met, traversal coverage thinner than the step text implies.)
- **Evidence**: `step-4-detail.json`.

### Checkpoint 5 — PASS  [a11y]
- **Raw state**: `Delete` on focused tab → count 4→3; focus moved to a sibling `role=tab` (`aria-selected=true`), not body; window never left with zero tabs.
- **Verdict**: pass. **Evidence**: `step-5-axtree.txt`.

### Checkpoint 6 — PASS  [a11y]
- **Raw state**: each tab's close control is a `role=button` named `Close tab: {title}` (matches `/close tab/i`, includes the title); DOM `.tab-close` is `<button>`; each tab carries `aria-keyshortcuts="Delete"`.
- **Verdict**: pass. **Evidence**: `step-6-axtree.txt`.

### Checkpoint 7 — PASS  [a11y]
- **Raw state**: single `tablist` "Open tabs", childCount=3 = exactly the live tabs; AX selected=1; DOM tabindex: only the selected tab `0`, others `-1`; Shift+Tab walk from `#address` reaches exactly one tab = the selected one.
- **Verdict**: pass — roving tabindex correct (entry = selected tab). The in-driver single-Tab probe landing on `#new-tab` was a documented `blur()` artifact, superseded by the clean Shift+Tab walk.
- **Evidence**: `step-7-axtree.txt`, `step-7-roving-walk.json`.

### Checkpoint 8 — PASS (negative / no-hijack)
- **Raw state**: with `#address` focused, Arrow/Delete left count + selection unchanged; with the active `<webview>` focused, Delete left them unchanged. The strip's handlers did not fire from outside the strip.
- **Verdict**: pass — no document-level key hijack (handler correctly scoped to `els.tabs`).

## Orchestrator Notes
- **Mode**: consolidated single-pass Witnessed — `SendMessage` unavailable, so one independent Executor performed all steps with per-step evidence, then one independent Validator judged all checkpoints (act/judge separation preserved; matches the project's standardized fallback from the Flight 2/3 debriefs). Cache: cold (renderer hard-reloaded before Step 1).
- **Apparatus**: raw CDP over Node global WebSocket attached to the running `:9222`, renderer target, **trusted** `Input.dispatchKeyEvent` (not synthetic). chrome-devtools MCP correctly avoided (launches its own browser).
- **Accessibility Validator** (optional crew) not spawned — `Enabled:false`; the base Validator judged the `[a11y]` rows with rendered-state precedence.
- Run was part of the `verify-a11y` leg, alongside the `npm run a11y` axe sweeps.

## Closing Summaries
### Executor closing
Environment healthy; renderer attached first try; all keys trusted. One non-defect artifact: a Step-7 single-Tab probe was contaminated by Chromium's `blur()` sequential-focus start point (landed on `#new-tab`); re-probed cleanly with a Shift+Tab walk. The `:8080` fixture served Concourse for every `?n`, so the three fixture tabs shared a title — URLs/address-bar still distinct enough for the assertions.

### Validator closing
All checkpoints pass; the keyboard/AT contract holds end-to-end. Two spec/fixture quality notes (not implementation defects): (1) the runtime fixture collapsed the "distinct URL per tab" precondition (identical title + normalized address), so only Google-vs-fixture was discriminable; (2) consequently Step 4 only traversed the one discriminable boundary, never the middle tabs. Recommend a fixture that stamps a per-tab title/URL. (Spec Step 2 has been tightened accordingly.)

## Operator Notes
Verify-leg run. The behavior test verified F22's load-bearing assertions decisively (ARIA roles/state, roving tabindex, focus ring, Delete-close, no-hijack — none depend on distinct URLs). Spec promoted `draft → active`; Step 2 tightened to avoid query-collapsing fixtures.
