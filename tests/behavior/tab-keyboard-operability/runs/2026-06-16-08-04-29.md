# Behavior Test Run: tab-keyboard-operability — 2026-06-16-08-04-29

**Spec**: [tests/behavior/tab-keyboard-operability.md](../tab-keyboard-operability.md)
**Status**: pass
**Started**: 2026-06-16T08:04:29
**Completed**: 2026-06-16T08:05:10
**Mode**: FD-driven focused live pass (Flight 6 leg 7 `verify-integration`) — cited machine-read evidence (M02 standard). First run on the **admin MCP surface** (post-migration); supersedes the prior CDP-`:9222` apparatus.
**Apparatus**: admin MCP client (MCP SDK `StreamableHTTPClientTransport`, `Authorization: Bearer <admin key>`) over the loopback transport `127.0.0.1:49707`, app launched via `npm run dev:automation` (`GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=49707`, **no `--remote-debugging-port`**). Chrome target via `getChromeTarget()` → `wcId 1`. NOT the `chrome-devtools` MCP, NOT `:9222`.

## Summary
Focused live pass: the migrated apparatus drives the chrome tab strip end-to-end. Trusted keyboard input fires native focus traversal + the renderer's keydown handlers; the a11y tree reports correct `tablist`/`tab`/`aria-selected` semantics; keyboard close works. **All core checkpoints passed.** (A focused pass per operator election — the chrome trusted-input + a11y-read primitives were independently proven in the leg-2 `chrome-drive-spike`.)

## Step Results

### Step (setup) — three distinct tabs — PASS
- **Actions**: `openTab` × 3 (`https://example.com/`, `example.org/`, `example.net/`) over the admin surface; `enumerateTabs`.
- **Raw state**: 4 tabs — `www.google.com`, `example.com`, `example.org`, `example.net` (distinct addresses, so activation is observable).

### Step 3 — focus the tab strip — PASS
- **Actions**: `click(wcId, 120, 20)` (focus anchor on the first tab), `readAxTree`.
- **Verdict**: focused node role `tab` name "Google"; a single `tablist` is present; exactly one tab `selected` ("Google"). [a11y]

### Step 4 — arrow / Home / End move + activate — PASS
- **ArrowRight** → focused + selected tab moves to "Example Domain" (example.com) — exactly one `aria-selected`. `Home` → selects first ("Google"); `End` → selects last ("Example Domain"). Each press relocates focus to a `tab` (never stranded on `<body>`). [a11y]
- **Evidence**: `02-arrowright.png`.

### Step 5 — keyboard close — PASS
- **Actions**: `pressKey(wcId, 'Delete')` on the focused tab; `enumerateTabs` before/after.
- **Verdict**: tab count 4 → 3; focus moved to a sibling `tab` ("Example Domain"), not lost to `<body>`; a remaining tab stays selected. Window never left with zero tabs.
- **Evidence**: `03-after-delete.png`.

### Step 7 — tablist structure — PASS
- **Raw state** (post-close a11y tree): exactly **1 `tablist`**, **3 `tab`** children, **1 selected**. Roving-tabindex entry reachable.

## Orchestrator Notes
FD-driven via an ephemeral admin-MCP harness (written to the repo, run, deleted — never committed). The focus-anchor rule (a `click` before keyboard-only sequences, added to the migrated spec) held: arrow/Home/End/Delete all drove correctly once a tab was focused. Steps 6 (close-button accessible name) and 8 (no-hijack negative) were not exercised in this focused pass — full coverage is available via a complete run or the leg-8 HAT; the primitives they rely on are proven.

## Closing Summaries
### Executor closing
The admin MCP surface drove the chrome tab strip exactly as the old CDP path did: trusted `pressKey` produced real focus traversal + tab activation, `readAxTree` returned faithful `tablist`/`tab`/`aria-selected` state. No apparatus friction.
### Validator closing
Spec semantics preserved across the migration; the a11y assertions (roles, single selection, focus retention) are all satisfiable through `readAxTree(wcId)`. The migration is sound.

## Evidence
Ephemeral (never committed — PII/bloat policy): `/tmp/behavior-tests/goldfinch/tab-keyboard-operability/runs/2026-06-16-08-04-29-evidence/` — `ax-after-click-tab.json`, `01-tab-focused.png`, `02-arrowright.png`, `03-after-delete.png`. Re-derive by re-running.
