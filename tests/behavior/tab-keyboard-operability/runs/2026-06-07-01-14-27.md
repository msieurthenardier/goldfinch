# Behavior Test Run: tab-keyboard-operability — 2026-06-07-01-14-27

**Spec**: [tests/behavior/tab-keyboard-operability.md](../../tab-keyboard-operability.md)
**Status**: pass
**Started**: 2026-06-07-01-14-27 (UTC)
**Executor**: ace4f05b18cbc40df
**Validator**: afeb770c7b6b8dd3a
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-keyboard-operability/2026-06-07-01-14-27/` during the run; re-derivable by re-running the spec.

## Summary

8 / 8 checkpoints passed (Step 2 is a setup row). **Regression gate GREEN** — the flight's tab-strip restructure (pill moved out of `#tabs` to sit after the tabs; frameless `#window-controls` added) did NOT break the tablist / roving-tabindex / Arrow-Home-End / keyboard-close / no-hijack contract established in mission 01. Run on the live frameless build on `:9222` via raw CDP-over-WebSocket with trusted `Input.dispatch*`; the Validator independently re-observed the static structure (Steps 6–7) first-hand.

## Checkpoint Results

### Checkpoint 1 — PASS
`:9222` reachable; renderer (`index.html`) target present (guest-target false-pass trap ruled out — `#tabs` only exists in the chrome renderer).

### Checkpoint 2 — PASS (setup)
4 tabs opened to distinct URLs (example.com/.org/.net/iana.org); address bar + webview src distinct per tab (example.* share the title "Example Domain", but URL is the load-bearing discriminator per the spec).

### Checkpoint 3 — PASS [a11y]
Reached a tab by keyboard (ShiftTab backward from `#address` through the relocated pill + window-controls + toolbar, landing on the SELECTED tab on the 8th press). activeElement role=tab, inside `#tabs`, not body. Focus-ring delta clear: `step3-focused-tab3.png` shows a full gold outline ring around the focused tab vs only the bottom accent when merely selected (`step3-unfocused-tab3.png`). No `outline:none` regression.

### Checkpoint 4 — PASS [a11y]
ArrowRight/ArrowLeft move + activate with wrap-around (idx3→idx0→idx3), Home→first, End→last; each press leaves exactly one `aria-selected` = the focused tab, address bar + active webview track selection, focus never on body. Automatic-activation roving contract intact.

### Checkpoint 5 — PASS [a11y]
Delete on the focused tab: count 4→3, focus moved to a sibling tab (role=tab, not body), a remaining tab selected + active, never zero. Validator independently re-observed the persisted 3-tab state.

### Checkpoint 6 — PASS [a11y]
Independently re-verified: each tab's close affordance is a `<button>` with `aria-label="Close tab: <title>"` (matches /close tab/i, includes title); each tab carries `aria-keyshortcuts="Delete"`. (Identical labels are a content artifact of example.* sharing a title; the template interpolates correctly.)

### Checkpoint 7 — PASS [a11y]
Independently re-verified via CDP: exactly 1 `role=tablist` (`#tabs`) with 3 children all `role=tab`; exactly 1 `aria-selected`; tabindex `[-1,-1,0]` (selected tab = sole roving entry). **`#newtab-pill.closest('#tabs')===null` and `#window-controls.closest('#tabs')===null`** (both exist but live outside the tablist; pill confirmed positioned AFTER `#tabs`). The restructure did not contaminate the tablist child set — the heart of this regression.

### Checkpoint 8 — PASS
No-hijack: with `#address` focused, Arrow/Delete left count + selection unchanged (focus stayed in the address bar); with the active `<webview>` focused, Delete left count unchanged. Tab key handlers are scoped to the strip, not document-level.

## Orchestrator Notes

- **Mode**: `SendMessage` absent → single-pass independent Executor + independent Validator (Witnessed separation), consistent with the other two runs this session.
- **Apparatus**: raw CDP-over-WebSocket (`/tmp/cdp.mjs`, extended with Arrow/Home/End/Delete/Backspace trusted keys, smoke-tested by the Orchestrator before the run). chrome-devtools MCP not used.
- **Keyboard reachability note**: ShiftTab from `#address` reaches the strip's roving entry in 8 presses because the relocated pill + frameless window-controls + back/reload toolbar buttons now sit in tab order between the address bar and the strip. The roving entry correctly lands on the SELECTED tab. (Plain forward Tab from page-top is captured by the `<webview>` guest — a pre-existing webview focus-trap, see the `unified-tab-controls` run's follow-up note.)
- Run against the post-HAT build (240px tabs, pill-after-tabs, frameless chrome).

## Closing Summaries

### Executor closing
No anomalies. Full contract preserved: single tablist with only role=tab children, pill + window-controls outside it; roving tabindex (0 on selected); arrows move+activate with wraparound; Home/End jump; Delete closes + moves focus to a sibling tab (never zero); named close buttons + aria-keyshortcuts; no-hijack holds; visible focus-ring delta. Note: active webview is marked by absence of `.hidden` (display:flex), not an `.active` class.

### Validator closing
Regression gate GREEN — keyboard/SR contract survived the restructure. Highest-risk surfaces (strip purity + roving tabindex + close-button names) re-verified first-hand via CDP, not taken on the Executor's word. Transient keyboard behaviors judged from the report and consistent with the persisted state (4 set up → 3 surviving after one keyboard close; none lost to the no-hijack Deletes).

## Operator Notes
_Regression run as part of the Flight 1 verify-integration session, post-HAT._
