# Behavior Test Run: tab-keyboard-operability — 2026-07-08-22-44-33

**Spec**: [tests/behavior/tab-keyboard-operability.md](../../tab-keyboard-operability.md)
**Status**: pass
**Started**: 2026-07-08T22:42:00
**Completed**: 2026-07-08T22:45:00
**Executor**: Leg-1c batch executor (SC3 parity sweep, self-witnessed single-agent)
**Surface**: native `WebContentsView` chrome renderer (`wcId=1`), admin MCP over `127.0.0.1:8899`

## Summary
8/8 checkpoints satisfied on the native surface. The tab strip is fully keyboard- and
screen-reader-operable: ShiftTab lands on the roving-tabindex selected tab (`role="tab"`,
`aria-selected="true"`), Arrow/Home/End move **and activate** (address bar + `aria-selected`
track, exactly one selected, focus never stranded on `<body>`), keyboard `Delete` closes the
focused tab with focus moving to a sibling `tab` (never zero tabs), each close control is a
`button` with a meaningful name (`Close tab: {title}`), the strip is a single `tablist`, and
neither the address bar nor web content hijacks the strip's arrow/Delete handlers. The one
`[a11y]` **focus-ring pixel delta** (Step 3) is **INCONCLUSIVE-APPARATUS-LIMIT** on WSLg
(focused-vs-unfocused `captureWindow` frames are byte-identical — the compositor does not paint
`:focus-visible` under injected focus, the pre-classified rig limit); keyboard **reachability**
of the ring's target is independently confirmed via the AX tree + `document.activeElement`.

Evidence: `/tmp/behavior-tests/goldfinch/tab-keyboard-operability/2026-07-08-22-44-33/`
(`evidence.json`, `s3-before.png`, `s3-after.png`).

## Step Results

### Step 1 — PASS (precondition probe)
`tools/list` → 27 tools, **includes** `getChromeTarget`. `getChromeTarget()` →
`{wcId:1, kind:'chrome', url:file://…/index.html}` — numeric chrome `wcId=1`. Recorded.

### Step 2 — setup (distinct-URL tabs)
Opened 3 tabs to distinct addresses on top of the initial Google tab: `enumerateTabs` →
`https://www.google.com/`, `https://example.com/`, `https://example.org/`, `https://example.net/`
(4 tabs). The load-bearing discriminator is the **address-bar value** (distinct per tab) even
though example.{com,org,net} share the title "Example Domain".

### Step 3 — PASS (keyboard focus into strip) / focus-ring pixel INCONCLUSIVE-APPARATUS-LIMIT
- **Raw**: focus anchor `click(1, #address center)`, then `pressKey(1,'ShiftTab')` ×8 →
  `document.activeElement` = `{cls:"tab active", aria-label:"Example Domain", role:"tab",
  aria-selected:"true"}`; AX focused node = `{role:"tab", name:"Example Domain"}`. The strip is
  reached backward (the strip is above the toolbar in DOM order; forward Tab enters the toolbar).
- **Verdict**: **PASS** for the reachability half — a keyboard user lands on the selected tab as a
  `tab` inside a `tablist`. The **visible focus-ring delta** is **INCONCLUSIVE-APPARATUS-LIMIT**:
  the before/after `captureWindow` frames are **byte-identical** (64763 == 64763 bytes), i.e. no
  `:focus-visible` paint delta is composited under injected focus on WSLg (matches the kebab-menu
  and pre-classified rig limit). `s3-after.png` shows the active tab's gold active-tab outline,
  but that styling is present in both frames (active-tab, not focus-ring). `[a11y]`

### Step 4 — PASS (arrow/Home/End move + activate)
Sequence of `readAxTree` + address-bar reads after each key (focus started on the last tab,
example.net):
| key | selected | address | activeEl | selCount |
|-----|----------|---------|----------|----------|
| (start) | Example Domain | https://example.net | tab "Example Domain" | 1 |
| ArrowRight | Google | https://www.google.com/ | tab "Google" | 1 |
| ArrowLeft | Example Domain | https://example.net/ | tab "Example Domain" | 1 |
| Home | Google | https://www.google.com/ | tab "Google" | 1 |
| End | Example Domain | https://example.net/ | tab "Example Domain" | 1 |
- **Verdict**: **PASS** — each arrow press moves focus to the adjacent tab **and activates** it
  (address-bar value + `aria-selected` change together), exactly **one** `aria-selected="true"`
  throughout, Home/End jump to first/last, no press strands focus on `<body>` (activeEl always a
  `tab`). Arrow navigation wraps (last→first / first→last). `[a11y]`

### Step 5 — PASS (keyboard close)
- **Raw**: focused tab before = `tab "Example Domain"`; `pressKey(1,'Delete')` → tab count
  **4 → 3**; AX focused node after = `{role:"tab", name:"Example Domain"}` (focus moved to a
  sibling **tab**, not `<body>`); a remaining tab stays `aria-selected="true"`; never zero tabs.
- **Verdict**: **PASS.** `[a11y]`

### Step 6 — PASS (close affordance semantics)
- **Raw**: AX buttons named `Close tab: Google`, `Close tab: Example Domain`, `Close tab:
  Example Domain` — each a `button` role with a meaningful, tab-identifying accessible name
  (matches `/close tab/i` + the tab title). Each `tab` node carries `aria-keyshortcuts="Delete"`
  (close shortcut discoverable). No unnamed/`generic` close control.
- **Verdict**: **PASS.** `[a11y]`

### Step 7 — PASS (strip structure)
- **Raw**: exactly **1** `tablist`; 3 `tab` children (== live tab count); exactly **1** selected;
  all tabs `focusable` (roving tabindex — ShiftTab in Step 3 landed on the *selected* tab, not the
  first DOM node).
- **Verdict**: **PASS.** `[a11y]`

### Step 8 — PASS (no hijack)
- **Raw (address bar focused)**: `click(1, #address)` → activeEl = `#address` textbox; then
  `ArrowRight`/`ArrowLeft`/`Delete` → selected tab **unchanged** (Example Domain), tab count
  **unchanged** (3). The strip's arrow/Delete handlers do not fire while the address bar holds
  focus.
- **Raw (web content focused)**: `click(1, 200, 820)` into the guest region → chrome
  `activeElement` leaves the strip; `Delete` → selected tab + count **unchanged** (3).
- **Verdict**: **PASS** — tab handlers are scoped to the strip, no `document`-level hijack. (The
  address-bar guard is the strong observation; the web-content guest is a separate
  `WebContentsView` so chrome's `activeElement` reads empty there — corroborating, not primary.)

## Closing Summary
The F22 keyboard/AT contract holds on the native `WebContentsView` chrome: focus reaches the strip,
arrows move+activate with a single selection and live address-bar tracking, keyboard close keeps
focus on a sibling tab, close controls are named buttons, the tablist structure is correct, and the
handlers are strip-scoped (no global hijack). Overall **PASS**. The sole non-affirmative checkpoint
is the Step-3 focus-ring **pixel** delta, classified **INCONCLUSIVE-APPARATUS-LIMIT** (WSLg does not
composite `:focus-visible` under injected focus — byte-identical frames); reachability is confirmed
by the AX tree and `activeElement`, so no regression is indicated. No `LIKELY-REGRESSION` findings.
