# Behavior Test Run: spellcheck — 2026-07-08-20-07-43

**Spec**: [tests/behavior/spellcheck.md](../../spellcheck.md)
**Status**: partial
**Started**: 2026-07-08T20:05:00
**Completed**: 2026-07-08T20:10:00
**Duration**: 00:05:00
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
The **opt-in state proxy** (settings.json `spellcheck` + Appearance checkbox `aria-checked`) toggles cleanly both directions (steps 3, 6 PASS). Step 2's clean Default-OFF baseline is INCONCLUSIVE (profile started with `spellcheck:true`; on WSLg "no spelling section" cannot distinguish OFF from dict-not-loaded). Steps 4 (squiggle) and 5 (live suggestions/correction) are INCONCLUSIVE-WSLg, dispositioned per the spec's top-of-file block. **Caveat:** the internal Settings guest was drivable only because of the internal-session-exclusion regression (see below).

## Step Results

### Step 1 — PASS (probe)
`tools/list` includes the driven tools; `getChromeTarget()` → numeric chrome `wcId 1`.

### Step 2 — INCONCLUSIVE (baseline not clean + WSLg engagement-unobservable)
- **Actions**: read `settings.json`; typed `teh mistaek wrng` in a web-tab editable field (wc8), right-clicked it, read the chrome/sheet page-context menu.
- **Raw state**: `settings.json spellcheck:true` at run start (NOT the fresh-profile `false` the step assumes — no profile reset performed in this batch). Right-click menu on the misspelled word = `["Paste","Undo","Inspect"]` — **no** spelling-suggestions section.
- **Verdict**: inconclusive — the "OFF by default on a fresh profile" framing needs a profile reset/restart not done here; and on WSLg a missing spelling section is indistinguishable from the dict-not-loaded case, so absence of engagement can't be attributed to the OFF gate. The **state proxy** direction is instead confirmed by steps 3/6.
- **Evidence**: [state-proxy.json](./2026-07-08-20-07-43/state-proxy.json)

### Step 3 — PASS (enable via Settings → Appearance)
- **Actions**: located the **Spellcheck** toggle in a `captureWindow` frame of the Settings page (wc9), computed guest device coords (guest DPR 1.25, toolbar offset ~88 → window `(875,633)` ⇒ guest device `(875,545)`), `click(9,875,545)`; re-read `settings.json` + `readAxTree(9)`.
- **Raw state**: `settings.json spellcheck: false → true`; AX checkbox `name:"Spellcheck", checked: false → true`. Help text: *"Enabling spellcheck downloads a one-time dictionary from Google. Reload open tabs to enable."* (the conservative reload-open-tabs copy).
- **Expected**: checkbox checked, `settings.json spellcheck===true`, reload-open-tabs wording.
- **Verdict**: pass — all three observables confirmed. **NB:** driving the internal Settings checkbox was only possible because internal-session tabs are (regressively) drivable; `evaluate(9)` is refused, so coords were located visually.
- **Evidence**: [state-proxy.json](./2026-07-08-20-07-43/state-proxy.json), [settings-window.png](./2026-07-08-20-07-43/settings-window.png)

### Step 4 — INCONCLUSIVE-WSLg (squiggle render, dispositioned)
- Not asserted as a pass — the red squiggle does not paint into a `captureWindow` frame on WSLg (spec's top-of-file block; Leg-2 premise-audit). HAT/macOS-authoritative. Flagged, not failed.

### Step 5 — INCONCLUSIVE-WSLg (dict-not-loaded)
- **Actions**: with spellcheck ON, right-clicked the misspelled word on the web tab; read the page-context menu.
- **Raw state**: menu renders (`["Paste","Undo","Inspect"]`) but **no** spelling-suggestions section — `dictionarySuggestions` empty, i.e. the one-time `.bdic` dictionary did not load on WSLg (F8 Leg-4 finding).
- **Verdict**: inconclusive-WSLg — an empty suggestion list is the **dict-not-loaded case**, explicitly "not a failure" per the spec. The suggestion→`correctMisspelling` round-trip could not be exercised (no suggestion to choose). macOS/HAT-authoritative for the native speller content.

### Step 6 — PASS (disable round-trips OFF)
- **Actions**: `click(9,875,545)` on the Spellcheck toggle (from ON); re-read `settings.json` + `readAxTree(9)`.
- **Raw state**: `settings.json spellcheck: true → false`; AX checkbox `checked: true → false`.
- **Expected**: checkbox unchecked, `settings.json spellcheck===false` (persisted).
- **Verdict**: pass.
- **Evidence**: [state-proxy.json](./2026-07-08-20-07-43/state-proxy.json)

## Orchestrator Notes
Single-agent convenience sweep. The Settings guest (internal session) was driven via coordinate `click` — feasible only because of the internal-session-exclusion regression documented in the `kebab-menu` run (`/tmp/behavior-tests/goldfinch/_internal-session-exclusion-regression/`). `evaluate(9)` is refused, so the checkbox was located visually from a `captureWindow` frame and a guest-DPR (1.25) coordinate model. Profile restored: `spellcheck` returned to its original `true`.

## Closing Summaries

### Executor closing
The spellcheck opt-in **state machine** is sound: the Appearance checkbox drives `settings.json spellcheck` and its own `aria-checked` in both directions, and the conservative reload-open-tabs help copy is present. The engine-level squiggle + live dictionary suggestions are not observable on WSLg (dict-not-loaded / no squiggle paint), consistent with the spec's dispositions.

### Validator closing
Two structural notes for the FD: (1) the clean Default-OFF baseline (step 2) needs a fresh-profile launch to assert properly — recommend a dedicated reset-then-launch run. (2) This spec *assumes the internal Settings guest is drivable*, which directly contradicts `internal-session-exclusion.md`; today it is drivable only because of the exclusion regression. Once the regression is fixed, this spec's settings-toggle steps will need an out-of-band driver or become HAT — the spec should be reconciled with the exclusion contract.

## Operator Notes
`spellcheck` restored to original `true` at run end.
