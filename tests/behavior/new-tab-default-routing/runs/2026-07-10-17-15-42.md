# Behavior Test Run: new-tab-default-routing — 2026-07-10-17-15-42

**Spec**: [new-tab-default-routing.md](../../new-tab-default-routing.md)
**Status**: pass
**Started**: 2026-07-10T17:15:42Z
**Completed**: 2026-07-10T17:42:00Z (approx)
**Duration**: ~00:26
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/new-tab-default-routing/2026-07-10-17-15-42/`
(21 raw MCP response files) during the run; re-derivable by re-running the spec.

## Summary

8 / 8 checkpoints passed. 0 failed; 0 inconclusive. First run of the EXTENDED
8-step revision (steps extended at M06 F3 Leg 5 per the 2026-07-10-04-43-55 run's
Validator notes: explicit reads in steps 1/5, the step-7 post-add `jarsGetDefault`
assertion, and the new step-8 second-jar-no-claim control). Re-passing gates M06 F3
Leg 5 (verify-integration) as the standing routing regression net.

## Checkpoint Results

### Checkpoint 1 — PASS
- Boot state: exactly one tab (wcId 2) `jarId: personal`; `jarsGetDefault()` →
  personal; `jarsList()` → personal + work. Live flag agrees with tab assignment
  (the extension's new explicit reads).
- **Evidence**: `step-1-enumerateTabs.json`, `step-1-jarsGetDefault.json`,
  `step-1-jarsList.json`

### Checkpoint 2 — PASS
- `openTab` (jarId omitted) → wcId 3, `jarId: personal`.
- **Evidence**: `step-2-openTab.json`, `step-2-enumerateTabs.json`

### Checkpoint 3 — PASS
- `jarsSetDefault({id:'work'})` → `true`.
- **Evidence**: `step-3-setDefault.json`

### Checkpoint 4 — PASS
- `openTab` (jarId omitted) → wcId 4, `jarId: work`; pre-existing tabs unchanged
  (no-restart corroborated by tab-set continuity over one live session).
- **Evidence**: `step-4-openTab.json`, `step-4-enumerateTabs.json`

### Checkpoint 5 — PASS
- Both removes `{ok:true, removed:{...}, wiped:true}`; `jarsList()` → `[]`;
  `jarsGetDefault()` → Burner identity (id `burner`). Supplementary (not
  asserted): the three staged tabs closed on their jars' deletion and a fallback
  burner tab appeared (wcId 5, `burner-265131764`) — expected Flight-3
  tabs-close-on-delete behavior, pinned by the sibling spec
  `jar-delete-closes-tabs`; an incidental live cross-confirmation of that spec's
  result on an independent stage.
- **Evidence**: `step-5-remove-work.json`, `step-5-remove-personal.json`,
  `step-5-jarsList.json`, `step-5-jarsGetDefault.json`, `step-5-enumerateTabs.json`

### Checkpoint 6 — PASS
- `openTab` (jarId omitted) → wcId 6, `jarId: burner-876789790` — matches
  `burner-<n>`, not bare `burner`, not persistent; judged on the FRESHLY opened
  tab, deliberately distinguished from the step-5 fallback tab (wcId 5), whose
  distinct burner id additionally corroborates fresh-burner-per-open.
- **Evidence**: `step-6-openTab.json`, `step-6-enumerateTabs.json`

### Checkpoint 7 — PASS
- `jarsAdd({name:'Fresh'})` → id `fresh`; `openTab` (omitted) → wcId 7
  `jarId: fresh`; `jarsGetDefault()` → fresh (the extension's new explicit
  post-add assertion). Empty-registry precondition independently verified at
  step 5.
- **Evidence**: `step-7-jarsAdd.json`, `step-7-openTab.json`,
  `step-7-enumerateTabs.json`, `step-7-jarsGetDefault.json`

### Checkpoint 8 — PASS (new step)
- `jarsAdd({name:'Second'})` → id `second`; `jarsGetDefault()` STILL resolves
  `fresh` — adding into a non-empty registry does not move the flag. This is the
  falsification control distinguishing genuine auto-claim-on-empty (step 7) from
  an always-default-new-jars bug.
- **Evidence**: `step-8-jarsAdd.json`, `step-8-jarsGetDefault.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation**; Sonnet both roles per crew
  file. Cache mode `cold` — fresh scratch profile + fresh launch (third
  independent stage of the session, per the Leg 5 per-run staging ruling); torn
  down after the run.
- Port: free-port fallback 49709 again (fourth consecutive observation on this
  rig).
- The color observation both agents flagged (`Second` and `Fresh` both
  `#b06ef5`) has a design explanation, verified against the store: these adds
  went through the raw `jarsAdd` wrapper with NO color argument, and the store
  clamps a missing/unsafe color to its `FALLBACK_COLOR` `#b06ef5`
  (jars.js `cleanColor`). The management page's create flow always passes an
  explicit palette color, so no rotation logic exists to be broken. Not a
  defect; dot distinguishability for page-created jars is HAT-observable.
- No operator interventions; no failing checkpoints; no re-spawns; preconditions
  Orchestrator-verified.

## Closing Summaries

### Executor closing

Clean 8/8: single MCP session for the whole run (survived the step-5 tab churn;
chrome target stable at wcId 1 throughout); every call first-attempt; ~1s
settles always sufficient. Notes: evaluate results are double-JSON-encoded (the
tool stringifies the resolved string) — log writers unwrap one layer; both
removes returned `wiped:true` beyond the spec's `{ok:true,...}` minimum; the
heads-up Flight-3 closure behavior materialized exactly as forewarned, and the
coordinator's wcId-disambiguation instruction kept the fallback tab from
contaminating step 6's observation. Admin key never written anywhere.

### Validator closing

8/8 PASS, every verdict from directly read evidence; zero discrepancies between
reports and evidence across the run. The step-8 extension earns its place — a
well-constructed falsification pair; keep the pattern for future flag-semantics
specs. Carry-forwards: (1) the replacement-burner-tab-on-empty behavior is
correct-looking but unpinned — consider a one-line assertion in
`jar-delete-closes-tabs` (note: its checkpoint 4 already asserts exactly this;
carried as confirmation, not a gap); (2) step 6 could optionally assert burner-id
distinctness from existing burner tabs; (3) the duplicate-color observation
(resolved as by-design above); (4) header Last Run updated with this run.

## Operator Notes

*(none — autonomous run; the flight debrief carries the spec-revision items)*
