# Behavior Test Run: new-tab-default-routing — 2026-08-25-05-04-03

**Spec**: [new-tab-default-routing.md](../new-tab-default-routing.md)
**Status**: pass
**Started**: 2026-08-25T05:04:03Z
**Completed**: 2026-08-25T05:34:28Z
**Duration**: 00:30:25
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/new-tab-default-routing/2026-08-25-05-04-03/` during the run; re-derivable by re-running the spec.

## Summary

8 / 8 checkpoints passed (re-run at the Mission 16 Flight 2 leg-2 gate; checkpoint 1 on a redo of the fixture after a fix). Jar routing of new tabs is unchanged by the welcome-surface rewrite: boot tab in the personal default; `openTab` follows the live default flag; moving the flag reroutes without a restart; an emptied registry falls through to the Burner identity and yields fresh `burner-<n>` tabs; the first jar added to an empty registry auto-claims the default; a second does not move it. The re-authored fixture's first form (home page only) did not attach the boot welcome tab — it carries both reasons on a fresh profile — and exposed a product gap (`show()` never settled a background welcome record); both were fixed at the gate and the run restarted on a fresh profile.

## Checkpoint Results

### Setup row (fixture, re-authored by M16 F2 leg 2) — completed, no judgment
- **Actions taken**: FIRST ATTEMPT (fixture as re-authored): set the home page via Settings, returned to the boot welcome tab, closed Settings — the boot tab stayed viewless showing the engine block (`enumerateTabs` → `[]`). The Executor traced it: a fresh boot record carries `reasons {home, search}` and `settle()` attaches only when nothing is unset; `show()` never settles. Fixture premise error + a product gap → fix at the gate; the run restarts on a fresh profile after the fix (see the second attempt below).
- **Evidence (attempt 1)**: `step-0-tabs-attempt1.json`, `step-0-screenshot-attempt1.png`
- **SECOND ATTEMPT (after the fix, fresh profile `xdg-ntdr2`)**: Settings via the seam — home page `https://example.com` + Save, then the Google radio; row read `homePage` set, `searchEngine: "google"`; returned to the boot welcome tab → it attached to `https://example.com/` (wcId 3, jar personal) on being shown; closed Settings. Tab list: exactly one web tab. The `show()`→`settle()` fix proved itself here.
- **Evidence**: `setup-settings-before.png`, `setup-settings-after-save.png`, `setup-settings-after-google.png`, `step-0-settings-row.json`, `step-0-tabs.json`, `step-0-screenshot.png`

### Checkpoint 1 — PASS (on the redo fixture)
- **Actions taken**: `enumerateTabs`; chrome `evaluate` of `jarsGetDefault()` and `jarsList()` (JSON-serialized).
- **Raw state**: One tab: wcId 3, `https://example.com/`, `jarId: personal`. `jarsGetDefault()` → the personal container; `jarsList()` → personal + work.
- **Expected**: Exactly one boot tab exists and its `jarId` is `personal` (the fresh-seed default), not `default` and not a `burner-*` id. `jarsGetDefault()` resolves the personal container; `jarsList()` resolves personal + work — the live flag agrees with tab assignment.
- **Verdict**: pass — exactly one tab, `jarId: personal`; `jarsGetDefault()` personal; `jarsList()` personal + work; the three files are jointly sufficient.
- **Evidence**: `step-1-tabs.json`, `step-1-default.json`, `step-1-jars.json`
- **Validator notes**: judged from the redo files (attempt-1 files suffixed).

### Checkpoint 2 — PASS
- **Actions taken**: `openTab {url}` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 4 at `https://example.com/` with `jarId: personal` (active); wcId 3 unchanged.
- **Expected**: The new tab's `jarId` is `personal`.
- **Verdict**: pass — the new tab (wcId 4) carries `jarId: personal`, matching the default.
- **Evidence**: `step-2-result.json`, `step-2-tabs.json`
- **Validator notes**: none.

### Checkpoint 3 — PASS
- **Actions taken**: Chrome `evaluate` of `jarsSetDefault({ id: 'work' })`, then a read-only `jarsGetDefault()`.
- **Raw state**: `jarsSetDefault` resolved `true`; `jarsGetDefault()` now the work container.
- **Expected**: Resolves `true`.
- **Verdict**: pass — resolved `true`; the follow-up read shows the work container as default.
- **Evidence**: `step-3-result.json`, `step-3-default.json`
- **Validator notes**: none.

### Checkpoint 4 — PASS
- **Actions taken**: `openTab {url}` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 5 with `jarId: work` (active); the two personal tabs unchanged.
- **Expected**: The new tab's `jarId` is `work` — the moved flag governs routing with no restart.
- **Verdict**: pass — the new tab (wcId 5) carries `jarId: work`; prior tabs untouched; no restart.
- **Evidence**: `step-4-result.json`, `step-4-tabs.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: Chrome `evaluate`: `jarsRemove` work, `jarsRemove` personal, `jarsList()`, `jarsGetDefault()`; `enumerateTabs` for context.
- **Raw state**: Both removes `{ ok: true, removed: …, wiped: true }`; `jarsList()` `[]`; `jarsGetDefault()` `{ id: "burner", name: "Burner" }`. Context: the jar-scoped tabs closed with their jars and the last-tab backfill opened one burner tab (wcId 6, `burner-35595380`).
- **Expected**: Both removes resolve `{ ok: true, … }`; `jarsList()` resolves `[]`; `jarsGetDefault()` resolves the Burner identity, id `burner`.
- **Verdict**: pass — both removes `{ ok: true, … }`, `jarsList()` `[]`, `jarsGetDefault()` the literal `burner` identity (distinct from a `burner-<n>` instance).
- **Evidence**: `step-5-remove-work.json`, `step-5-remove-personal.json`, `step-5-jars.json`, `step-5-default.json`, `step-5-tabs.json`
- **Validator notes**: the backfilled burner tab is context for step 6, not this row.

### Checkpoint 6 — PASS
- **Actions taken**: `openTab {url}` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 7 with `jarId: burner-299224257` — a fresh `burner-<n>`, distinct from the earlier `burner-35595380` and from `burner` itself.
- **Expected**: The new tab's `jarId` matches `burner-<n>` (NOT id `burner` itself, and not any persistent jar).
- **Verdict**: pass — the new tab is a fresh `burner-<n>` instance, distinct from `burner` and from the earlier burner tab.
- **Evidence**: `step-6-result.json`, `step-6-tabs.json`
- **Validator notes**: none.

### Checkpoint 7 — PASS
- **Actions taken**: Chrome `evaluate` `jarsAdd({ name: 'Fresh' })`; `openTab {url}` with `jarId` omitted; `enumerateTabs`; `jarsGetDefault()`.
- **Raw state**: `jarsAdd` → `{ id: "fresh", … }`; new tab wcId 8 with `jarId: fresh`; `jarsGetDefault()` → the fresh container.
- **Expected**: `jarsAdd` resolves a container with id `fresh`; the newly opened tab's `jarId` is `fresh`; `jarsGetDefault()` resolves the `fresh` container.
- **Verdict**: pass — `jarsAdd` returned id `fresh`, the new tab routed to `fresh`, and `jarsGetDefault()` resolved `fresh`: auto-claim on an empty registry.
- **Evidence**: `step-7-add.json`, `step-7-result.json`, `step-7-tabs.json`, `step-7-default.json`
- **Validator notes**: none.

### Checkpoint 8 — PASS
- **Actions taken**: Chrome `evaluate` `jarsAdd({ name: 'Second' })`, `jarsGetDefault()`, `jarsList()`.
- **Raw state**: `jarsAdd` → `{ id: "second", … }`; `jarsGetDefault()` → still the `fresh` container; `jarsList()` → fresh + second.
- **Expected**: `jarsAdd` resolves a container with id `second`; `jarsGetDefault()` still resolves the `fresh` container, NOT `second`.
- **Verdict**: pass — `jarsAdd` returned id `second` and `jarsGetDefault()` still resolved the literal `fresh`; the registry was non-empty, so the flag stayed put.
- **Evidence**: `step-8-add.json`, `step-8-default.json`, `step-8-jars.json`
- **Validator notes**: none — closes the spec.

## Orchestrator Notes

- **Execution mode**: live two-agent mode; cache mode `cold`; fresh profiles (two — the run restarted after the gate fix). Crew: Executor + Validator, Sonnet. The spec's observables are API results (`enumerateTabs` rows, `window.goldfinch.jars*` return values via chrome `evaluate`, JSON-serialized) — the cited JSON files are the rendered-state equivalent; the Validator required literal return values, not paraphrases, and judged the `burner` vs `burner-<n>` and `fresh` vs `second` distinctions on the literal ids.
- **Fixture premise error → second fix at the gate.** Leg 2 re-authored this spec's fixture ("set the home page; the boot tab attaches") after auditing it against DD5, but a fresh boot welcome record carries `reasons {home, search}` and `settle()` attaches only when nothing is left unset — so the boot tab correctly kept showing the engine block. The Executor traced it in `welcome-controller.js`/`renderer.js`. Under it, a product gap: `show()` never settled, so a background welcome tab shown after all preferences were set elsewhere would render an empty panel and never attach (DD7 says it attaches). FD ruling: fix Developer made `show(tab)` → `settle(tab)` (structural test; suite 3762 → 3763), the fixture bullet now sets BOTH preferences, and — because the chrome bundle loads at window creation — the run restarted on a fresh profile (`xdg-ntdr2`). On the redo the boot tab attached on being shown, proving the fix. Attempt-1 evidence kept with an `-attempt1` suffix.
- **Step 5 side observation**: removing both jars closed their tabs and the last-tab backfill opened one burner tab — existing behavior, not this spec's claim.
- **Apparatus**: the Settings layout shifts after Save (hint text disappears) — re-capture before each click; `gf.mjs` + chrome-seam `evaluate` covered every observable.

## Closing Summaries

### Executor closing
8/8 on jar routing; no routing defects. The real finding was the fixture trace: `welcomeReasons(null, null)` bakes both reasons into the boot record; `attachView` fires only from the submit paths or `settle()`, and `settle()` requires zero unset reasons; before the fix `show()` never settled, so attempt 1's home-page-only fixture left `enumerateTabs` empty across repeated waits. The corrected fixture (both preferences) attached cleanly after the relaunch with the patched bundle. Step 5: three jar-scoped tabs collapsed into one fresh burner tab on removal — worth a look if tab preservation on jar removal matters elsewhere. Apparatus: the Settings layout shifts after Save; re-screenshot before each click.

### Validator closing
8/8. The fixture correction is worth carrying to the debrief: setting only one of the two boot-blocking preferences left the tab stuck viewless with no signal — a silent hang, not a fast fail — fixed product-side and in the fixture. Spec-quality: the step-1 boot-tab precondition is fixture-driven, not a numbered action — make the two-preference fixture and the attachment confirmation explicit in the spec (done: the fixture bullet now says so). The literal-id assertions were well specified; the read-only restriction never blocked a verdict.

## Operator Notes

_(none yet)_
