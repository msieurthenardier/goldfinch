# Behavior Test Run: new-tab-default-routing — 2026-07-10-04-43-55

**Spec**: [new-tab-default-routing.md](../../new-tab-default-routing.md)
**Status**: pass
**Started**: 2026-07-10T04:43:55Z
**Completed**: 2026-07-10T05:12:00Z (approx)
**Duration**: ~00:28
**Executor**: sub-agent adb8d8de7204317da (Sonnet)
**Validator**: sub-agent afd4fa78dacc47ad0 (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/new-tab-default-routing/2026-07-10-04-43-55/` during
the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec (authored
at M06 Flight 2 design); spec flips `draft` → `active`. Run gates M06 F2 Leg 3
(verify-integration) acceptance.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: enumerateTabs; getChromeTarget (chrome wcId 1); evaluate `jarsGetDefault()` / `jarsList()` (validator-requested live-flag tie-in).
- **Raw state**: exactly one boot tab, `jarId: "personal"`; `jarsGetDefault()` → the personal container; `jarsList()` → personal + work (fresh seed).
- **Expected**: Exactly one boot tab exists and its `jarId` is `personal` — not `default`, not `burner-*`.
- **Verdict**: pass — tab assignment AND live flag both evidence `personal`.
- **Evidence**: `step-1-enumerate.json`, `step-1-eval-getdefault.json`, `step-1-eval-list.json`

### Checkpoint 2 — PASS
- **Actions taken**: openTab (url only, `jarId` omitted) → wcId 3; enumerateTabs.
- **Raw state**: new tab `jarId: "personal"`.
- **Expected**: The new tab's `jarId` is `personal`.
- **Verdict**: pass.
- **Evidence**: `step-2-opentab.json`, `step-2-enumerate.json`

### Checkpoint 3 — PASS
- **Actions taken**: chrome-eval `jarsSetDefault({ id: 'work' })` (first attempt lost to executor-side shell quoting — expression corrupted in transit, not an app fault; retried via heredoc JSON-RPC body).
- **Raw state**: resolved `true`.
- **Expected**: Resolves `true`.
- **Verdict**: pass — retry authoritative; checkpoint 4 independently confirmed the mutation.
- **Evidence**: `step-3-setdefault.json`, `step-3-request.json`

### Checkpoint 4 — PASS
- **Actions taken**: openTab (`jarId` omitted) → wcId 4; enumerateTabs.
- **Raw state**: new tab `jarId: "work"`; pre-existing tabs unchanged (`personal`).
- **Expected**: The new tab's `jarId` is `work` — moved flag governs routing, no restart.
- **Verdict**: pass — flag move affected only newly opened tabs, as intended.
- **Evidence**: `step-4-opentab.json`, `step-4-enumerate.json`

### Checkpoint 5 — PASS
- **Actions taken**: chrome-eval `jarsRemove({id:'work'})`, `jarsRemove({id:'personal'})`, `jarsList()`, `jarsGetDefault()`.
- **Raw state**: both removes `{ ok: true, removed: {...}, wiped: true }`; `jarsList()` → `[]`; `jarsGetDefault()` → `{ id: "burner", name: "Burner", color: "#ff8c42" }`.
- **Expected**: Both `{ ok: true, ... }`; registry empty; Burner identity (id `burner`) is the default.
- **Verdict**: pass — every clause directly evidenced.
- **Evidence**: `step-5-remove-work.json`, `step-5-remove-personal.json`, `step-5-list.json`, `step-5-getdefault.json`

### Checkpoint 6 — PASS
- **Actions taken**: openTab (`jarId` omitted) → wcId 5; enumerateTabs.
- **Raw state**: new tab `jarId: "burner-604573697"` (literal string captured).
- **Expected**: `jarId` matches `burner-<n>` — NOT `burner` itself, not a persistent jar.
- **Verdict**: pass — matches `^burner-\d+$`; registry known-empty from checkpoint 5.
- **Evidence**: `step-6-opentab.json`, `step-6-enumerate.json`

### Checkpoint 7 — PASS
- **Actions taken**: chrome-eval `jarsAdd({ name: 'Fresh' })`; openTab (`jarId` omitted) → wcId 6; enumerateTabs.
- **Raw state**: `jarsAdd` → `{ id: "fresh", name: "Fresh", color: "#b06ef5", partition: "persist:container:fresh" }`; new tab `jarId: "fresh"`.
- **Expected**: `jarsAdd` resolves container id `fresh`; new tab's `jarId` is `fresh` (auto-claimed flag).
- **Verdict**: pass — Validator took its own fresh observation (`jarsGetDefault()` → the `fresh` container) to close the causal auto-claim clause, combined with checkpoint 5's empty-registry baseline.
- **Evidence**: `step-7-add.json`, `step-7-opentab.json`, `step-7-enumerate.json`, `validator-step7-getdefault.json` (validator fresh observation)

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (SendMessage available); both agents
  persisted across all 7 checkpoints. Sonnet for both roles per crew file.
- Cache mode: `cold` — satisfied structurally by a freshly launched app instance on a
  fresh scratch profile (Orchestrator-staged preconditions: `XDG_CONFIG_HOME` scratch,
  `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 npm run dev:automation`).
- MCP surface bound on free-port fallback **49709** (configured 49707 unavailable —
  same rig behavior as F1 Leg 4 D2). Apparatus: curl-driven Streamable HTTP JSON-RPC
  with the launch-minted admin key; per-run scratch keys die with the instance.
- No operator interventions; no failing checkpoints; no re-spawns.
- Preconditions were Orchestrator-verified rather than operator-confirmed (autonomous
  flight execution under the operator's standing directive; fixture is a scratch
  profile, no operator data at risk).

## Closing Summaries

### Executor closing

Clean run, single MCP session throughout, no reconnects, no port drift, no app
crashes (wcIds monotonic 2→6). Apparatus findings: (1) the `evaluate` tool rejects
top-level `await` with only a generic "Script failed to execute" — the no-await form
works because the tool awaits a returned Promise; the generic error hides
SyntaxError-vs-thrown distinction and cost a diagnostic cycle (possible app-side
improvement). (2) One executor-side shell-quoting corruption (step 3, first attempt);
heredoc-body pattern adopted for all subsequent evaluate calls, no recurrence.
Behavioral observations (raw): pre-existing tabs retain their jarIds after their jar
is removed+wiped; first jar added to an empty registry becomes default without an
explicit set; burner-state `jarsGetDefault()` returns the identity object without a
`partition` field (identity marker, not a jar).

### Validator closing

7/7 PASS, high confidence, no polite fails. Spec-quality items for the next revision:
(1) Step 5's Expected Results exceed its Actions (the list/get-default reads should be
explicit actions); (2) Step 7's "auto-claims the flag" causal clause is
under-observable as written — recommend an explicit post-add `jarsGetDefault()`
expected result, or better, a follow-on step adding a second jar into a NON-empty
registry and asserting the default does NOT move (only way to distinguish auto-claim
from an always-default-new-jars bug); (3) codify step 1's live-flag read into the
spec. **Carry-forward finding for the flight** (outside spec scope): after removal+
wipe, surviving tabs (wcId 2/3/4) still report `jarId: "personal"`/`"work"` — dangling
references to deleted jars. Known DD2 trade-off in the flight spec (tabs-close-on-
delete is Flight 3/5 scope), but the `wiped: true`-alongside-live-tabs semantics
deserves a documented answer when that flight lands.

## Operator Notes

*(none — autonomous run; flight debrief will carry the spec-revision items)*
