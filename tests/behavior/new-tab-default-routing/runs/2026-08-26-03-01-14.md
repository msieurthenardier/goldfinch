# Behavior Test Run: new-tab-default-routing — 2026-08-26-03-01-14

**Spec**: [new-tab-default-routing.md](../new-tab-default-routing.md)
**Status**: pass
**Started**: 2026-08-26T03:01:14Z
**Completed**: 2026-08-26T03:14:34Z
**Duration**: 00:13:20
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/new-tab-default-routing/2026-08-26-03-01-14/` during the run; re-derivable by re-running the spec.

## Summary

8 / 8 checkpoints passed. 0 failed; 0 inconclusive. Gate re-run on the final Mission 16 Flight 3 HAT build with the fixture re-authored for the DD7 pivot (the boot welcome tab is navigated by address because the surface no longer auto-attaches once both preferences are set). Jar routing of new tabs is unchanged: seed routing, live flag move, empty-registry fallthrough to Burner with fresh `burner-<n>` tabs, auto-claim on an empty registry and no claim on a non-empty one.

## Checkpoint Results

### Setup row (fixture, re-authored M16 F3 leg 2) — completed, no judgment
- **Actions taken**: Settings via the seam: home page `https://example.com` + Save, Google radio (row read `homePage: "https://example.com"`, `searchEngine: "google"`); returned to the boot welcome tab by clicking its strip tab — probe confirmed it was still a welcome tab (surface block, address empty) with both preferences set, i.e. no auto-attach on this build; typed `https://example.com` in the address bar + Enter → the tab attached as wcId 4 on `https://example.com/`, jar `personal`; `closeTab` on Settings → exactly one web tab; strip count 1.
- **Evidence**: `step-0-settings-initial.png`, `step-0-settings.png`, `step-0-settings-snapshot.txt`, `step-0-settings-row.json`, `step-0-welcome-probe.json`, `step-0-tabs-after-nav.json`, `step-0-tabs-end.json`, `step-0-strip-end.json`, `step-0-end.png`

### Checkpoint 1 — PASS
- **Actions taken**: `enumerateTabs`; `jarsGetDefault()` and `jarsList()` via `evaluate` on the chrome.
- **Raw state**: Exactly one tab, `jarId: "personal"` (wcId 4 on `https://example.com/`); `jarsGetDefault()` → personal; `jarsList()` → personal + work.
- **Expected**: Exactly one boot tab exists and its `jarId` is `personal` (the fresh-seed default), not `default` and not a `burner-*` id. `jarsGetDefault()` resolves the personal container; `jarsList()` resolves personal + work (the fresh seed) — the live flag agrees with tab assignment.
- **Verdict**: pass — exactly one tab in `personal`; `jarsGetDefault()` personal; `jarsList()` personal + work; all three agree.
- **Evidence**: `step-1-tabs.json`, `step-1-jarsGetDefault.json`, `step-1-jarsList.json`
- **Validator notes**: evidence files cross-checked verbatim.

### Checkpoint 2 — PASS
- **Actions taken**: `openTab` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 5 on `https://example.net/`, `jarId: "personal"`.
- **Expected**: The new tab's `jarId` is `personal`.
- **Verdict**: pass — `openTab` with `jarId` omitted produced wcId 5 in `personal`.
- **Evidence**: `step-2-openTab.json`, `step-2-tabs.json`
- **Validator notes**: verified against the evidence files.

### Checkpoint 3 — PASS
- **Actions taken**: `jarsSetDefault({id:'work'})` via `evaluate`; supplementary `jarsGetDefault()`.
- **Raw state**: Resolved `true`; `jarsGetDefault()` now resolves work.
- **Expected**: Resolves `true`.
- **Verdict**: pass — resolved `true`; the flag now points at work.
- **Evidence**: `step-3-jarsSetDefault.json`, `step-3-jarsGetDefault.json`
- **Validator notes**: the evidence file holds the bare literal `true`.

### Checkpoint 4 — PASS
- **Actions taken**: `openTab` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 6 on `https://example.org/`, `jarId: "work"`; the two earlier tabs still `personal`.
- **Expected**: The new tab's `jarId` is `work` — the moved flag governs routing with no restart.
- **Verdict**: pass — the new tab (wcId 6) followed the moved flag into `work` with no restart; the earlier tabs stayed `personal`.
- **Evidence**: `step-4-openTab.json`, `step-4-tabs.json`
- **Validator notes**: only the new tab picked up the flag — not a blanket reassignment.

### Checkpoint 5 — PASS
- **Actions taken**: `jarsRemove` work then personal, `jarsList()`, `jarsGetDefault()` via `evaluate`; informational `enumerateTabs`.
- **Raw state**: Both removes `{ok: true, removed: …, wiped: true}`; `jarsList()` `[]`; `jarsGetDefault()` `{id: "burner", name: "Burner"}`. (Informational: the three tabs in the removed jars closed; one `burner-<n>` tab remained — out of scope.)
- **Expected**: Both removes resolve `{ ok: true, ... }`; `jarsList()` resolves `[]`; `jarsGetDefault()` resolves the Burner identity, id `burner` — the registry is empty and the live flag has fallen through to Burner.
- **Verdict**: pass — both removes `{ok: true, …}`; `jarsList()` `[]`; `jarsGetDefault()` the Burner identity (`burner`); the closed-tabs side effect is out of scope.
- **Evidence**: `step-5-jarsRemove-work.json`, `step-5-jarsRemove-personal.json`, `step-5-jarsList.json`, `step-5-jarsGetDefault.json`, `step-5-tabs.json`
- **Validator notes**: the leftover `burner-535235501` tab is distinct from step 6's fresh one.

### Checkpoint 6 — PASS
- **Actions taken**: `openTab` with `jarId` omitted; `enumerateTabs`.
- **Raw state**: New tab wcId 8, `jarId: "burner-111425967"` — a fresh `burner-<n>`, distinct from the earlier burner tab's id.
- **Expected**: The new tab's `jarId` matches `burner-<n>` — Burner-as-default yields a fresh evaporating burner tab (NOT id `burner` itself, and not any persistent jar).
- **Verdict**: pass — the new tab's `jarId` `burner-111425967` matches `burner-<n>`, is not `burner` itself nor a persistent jar, and differs from the pre-existing burner tab — a genuinely fresh evaporating tab.
- **Evidence**: `step-6-openTab.json`, `step-6-tabs.json`
- **Validator notes**: none.

### Checkpoint 7 — PASS
- **Actions taken**: `jarsAdd({name:'Fresh'})`; `openTab` with `jarId` omitted; `enumerateTabs`; `jarsGetDefault()`.
- **Raw state**: `jarsAdd` → id `fresh`; the new tab (wcId 9) `jarId: "fresh"`; `jarsGetDefault()` → fresh — auto-claimed on the empty registry.
- **Expected**: `jarsAdd` resolves a container with id `fresh`; the newly opened tab's `jarId` is `fresh`; `jarsGetDefault()` resolves the `fresh` container — the first persistent jar added into an empty registry auto-claimed the default flag.
- **Verdict**: pass — `jarsAdd` → `fresh`; the new tab routed to `fresh`; `jarsGetDefault()` → fresh — auto-claimed on the empty registry, live for routing.
- **Evidence**: `step-7-jarsAdd.json`, `step-7-openTab.json`, `step-7-tabs.json`, `step-7-jarsGetDefault.json`
- **Validator notes**: none.

### Checkpoint 8 — PASS
- **Actions taken**: `jarsAdd({name:'Second'})`; `jarsGetDefault()`.
- **Raw state**: `jarsAdd` → id `second`; `jarsGetDefault()` still fresh, not second.
- **Expected**: `jarsAdd` resolves a container with id `second`; `jarsGetDefault()` still resolves the `fresh` container, NOT `second` — adding a jar into an already-non-empty registry does not move the default flag, distinguishing genuine auto-claim-on-empty-registry (step 7) from an always-default-new-jars bug.
- **Verdict**: pass — `jarsAdd` → `second`; `jarsGetDefault()` still `fresh` — the flag did not move on a non-empty registry; distinguishes auto-claim-on-empty from always-default-new-jars.
- **Evidence**: `step-8-jarsAdd.json`, `step-8-jarsGetDefault.json`
- **Validator notes**: none.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh scratch profile; the re-authored fixture folded in as a setup row. Crew: Executor + Validator, Sonnet. Steps 3–4, 5–6, and 7–8 dispatched as pairs (each pair is one action chain), judged separately.
- **Purpose**: fourth of four gate runs for the HAT's DD7 pivot — this spec's fixture relied on the retired auto-attach (caught at the item-6 design review); the re-authored fixture (manual navigation of the boot welcome tab) produced exactly the "one web boot tab in `personal`" premise. The Executor's step-0 probe recorded the premise directly: with both preferences set the boot tab was still a welcome tab until navigated.
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707, no fallback); `jars*` wrappers via `evaluate` on the chrome; `openTab` returns a bare numeric wcId; the Settings guest driven by screenshot coordinates. Out-of-scope side effect observed and not judged: removing both jars closed their open tabs and left one `burner-<n>` tab.

## Closing Summaries

### Executor closing
All eight rows plus the re-authored fixture executed cleanly; no blocks. Apparatus notes held throughout (attach-only CLI; Settings guest driven by screenshot coordinates with re-capture between clicks; `openTab` returns a bare numeric wcId; the `jars*` wrappers resolve cleanly as async from the chrome). Informational: after step 5's two removes the three tabs in the removed jars closed and one `burner-<n>` tab remained — the spec's out-of-scope "deleting a jar closes its open tabs" behavior, not judged.

### Validator closing
8/8, every verdict confirmed by reading the evidence files (contents matched the raw state verbatim); no fresh observation needed. The fixture re-authoring held up — the "one boot tab, personal" premise materialized. The step-5 jar-deletion side effect was correctly out of scope. No apparatus anomalies; a pure API/tab-state spec, no rendered-frame judgment calls.

## Operator Notes
_None._
