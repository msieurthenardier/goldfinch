# Behavior Test Run: settings-controls — 2026-06-07-21-23-58

**Spec**: [tests/behavior/settings-controls.md](../../settings-controls.md)
**Status**: pass
**Started**: 2026-06-08T02:18:00Z
**Completed**: 2026-06-08T02:23:58Z
**Duration**: ~00:06:00
**Executor**: Flight Director (manual CDP drive via `scripts/cdp-driver.mjs` + node-CDP guest attach)
**Validator**: Flight Director (same) — Witnessed-pattern deviation noted below

## Summary
All steps passed. Settings-page controls (global Shields toggles + editable home page) persist, take effect,
and stay two-way consistent with the slide-out Shields panel; the origin-checked bridge admits only the
internal guest. Apparatus per DD5: drove `:9222` via `scripts/cdp-driver.mjs` + a node-CDP attach to the
`goldfinch://settings` guest; persistence read from the filesystem (`userData/{settings,shields}.json`); the
`chrome-devtools` MCP was **not** used.

## Step Results

### Step 1 — PASS
- cdp-driver `eval 1+1` → `2`; opened Settings; guest attachable. The Privacy & Shields section shows **5 real
  checkboxes** (`shield-enabled/block/strip/isolate/farble`) and the On-startup section shows a real home-page
  **input + Save**. The guest bridge `window.goldfinchInternal` exposes `version, settingsGet, settingsSet,
  onSettingsChanged, shieldsGet, shieldsSet, onShieldsChanged`.

### Step 2 — PASS
- `shieldsGet()` from the guest matched `userData/shields.json` (all true initially); `settingsGet('homePage')`
  → `https://www.google.com` (first-run default, no `settings.json` yet).

### Steps 3–5 — PASS (Shields persist + two-way sync) `[a11y]`
- **settings → store**: flipped `#shield-block` in the guest (change event) → `shields.json` `block: true→false`.
- **settings → panel**: the chrome Shields panel's "Block trackers" `[role=switch]` read `aria-checked="false"`
  — reflects the settings change (the `shields-changed` fan-out reached the chrome).
- **panel → settings**: clicked "Block trackers" in the chrome panel → `shields.json` `block:true`, and the
  guest `#shield-block.checked === true` — reflects the panel change (fan-out reached the guest). Both
  directions consistent. (Synchronous post-click `aria-checked` reads the pre-render value — a known timing
  artifact; the authoritative `shields.json` + guest state confirm the result.)

### Steps 6 + 8 — PASS (home page persist + take-effect) `[a11y]`
- Set `https://example.com/` in the settings input + Save → status **"Saved"**; `userData/settings.json`
  written atomically as `{"version":1,"homePage":"https://example.com/"}`.
- Opened a new tab (`#new-tab`) → it loaded **`https://example.com/`** (not the old default) — the home setting
  took effect via the chrome `currentHomePage()` cache updated by the `settings-changed` broadcast.

### Step 7 — PASS (validation)
- Set `javascript:alert(1)` → rejected; status **"Not saved: … TypeError: invalid value for "homePage""** (the
  store's throw propagated through the bridge `invoke` to the UI); `settings.json` unchanged
  (`https://example.com/` retained).

### Steps 9–10 — PASS (origin-check security)
- **Web tab** (`https://www.google.com`): `typeof window.goldfinchInternal === 'undefined'` — the privileged
  bridge is absent in web tabs (different preload).
- **Chrome page context**: `ipcRenderer`, `require`, `window.goldfinchInternal` all `undefined`, and
  `window.goldfinch.settingsSet` absent — the chrome **page** surface cannot invoke `internal-*` channels.
- The only context with the bridge is the origin-locked settings guest; the main-side
  `registerInternalHandler` sender check (`senderFrame.origin` + internal-session, strict) is the
  authoritative boundary and is unit-tested (the mandatory wrapper test, 211/211).
- **In-session vector** (web content loaded *inside* the internal session calling the bridge) was **not
  driven** — hard post-Flight-5 (nav lock + immutable `webPreferences`). Asserted structurally + by the
  unit-tested main-side check, per DD5. **Gap logged, not papered over.**

### Steps 11–12 — PASS (a11y) `[a11y]`
- `npm run a11y` (chrome) and `npm run a11y -- --target=goldfinch://settings` (guest, with the wired controls)
  both **No NEW violations** vs the pinned `ACCEPTED` baseline. The `<fieldset>/<legend>` Shields grouping +
  the labelled home-page input kept the guest clean.

## Regression (live spot-checks)
- **tab-scheme-guard core**: a web origin's `window.open('goldfinch://settings')` created **no** new internal
  tab (internal-session webview count unchanged at 1) — the scheme boundary survives the flight's `main.js`
  handler changes.
- **settings shell** (Flight-5): nav + 5 sections still render (the guest a11y audit attached + ran clean).

## Orchestrator Notes
- **Witnessed-pattern deviation**: Flight-Director-driven (cdp-driver + node-CDP + filesystem reads), not the
  two-live-agent Executor+Validator crew. Compensating control: every verdict cites a raw machine-read value
  (DOM/checkbox state/`aria-checked`/`shields.json`/`settings.json`/`typeof`), not model judgment. A future
  formal `/behavior-test settings-controls` can supersede this log.
- **SC7 + SC8 verified.** Offline gates 211/211 (lint, typecheck clean).

## Closing Summaries
### Executor closing
Bridge round-trips and the two-way Shields sync worked first try; the home-page take-effect was visible by
opening a new tab. Filesystem reads of `settings.json`/`shields.json` are the cleanest persistence evidence.
### Validator closing
The validation-error surfacing (store `throw` → bridge `invoke` reject → settings status text) is the
end-to-end proof that the secured write path reports failures rather than dropping them. Security posture is
sound for the drivable vectors; the in-session vector is honestly logged as argued, not driven.

## Operator Notes
Flight-Director-driven live verification during `/agentic-workflow`. HAT (leg 7) to follow.
