# Behavior Test Run: settings-controls — 2026-07-08-23-16-25

**Spec**: [tests/behavior/settings-controls.md](../../settings-controls.md)
**Status**: pass
**Started**: 2026-07-08T23:12:00
**Completed**: 2026-07-08T23:18:00
**Executor**: Leg-1c batch executor (SC3 parity sweep, self-witnessed single-agent)
**Surface**: native `WebContentsView` — chrome `wcId=1`, internal settings guest `wcId=12`
(`jarId:"internal"`). Admin MCP over `127.0.0.1:8899`. Dev profile `~/.config/goldfinch-dev`.

## Summary
The wired Shields toggles and home-page control are operable from `goldfinch://settings`, changes
**persist** to `shields.json`/`settings.json` and **take effect**, and stay **consistent** with the
chrome slide-out Shields panel. **Steps 1-8 all PASS** on the native surface. **Steps 9-12 are the
spec's explicit F8-eval defers** (security script-runtime reads / page-context IPC in 9-10; the
axe-injection a11y harness in 11-12) — carried as deferred, not run. No `LIKELY-REGRESSION`.

**Apparatus notes.** Toggle/checkbox `checked` and text-input `value` are DOM **properties** that
do not serialize into `readDom` `outerHTML` after interaction, so live control state is judged from
the **authoritative store files** (`shields.json`/`settings.json`) plus `captureScreenshot`; the
chrome panel's `role="switch"` `aria-checked` **does** serialize and is read via `readDom`. Guest
focus is not observable (AX exposes no `focused`, `evaluate` refused on internal), so keyboard
operability is judged by observable effect (store write after a `Space`/`typeText`).

Evidence: `/tmp/behavior-tests/goldfinch/settings-controls/2026-07-08-23-16-25/`.

## Step Results

### Step 1 — PASS (precondition probe)
`tools/list` includes `getChromeTarget` **and** `enumerateTabs`; `getChromeTarget()` →
`{wcId:1, kind:'chrome'}` (numeric); the `goldfinch://settings` guest is enumerable as
`guestWcId=12`; `readDom(12)` shows real Shields toggle controls (`#shield-block` etc.,
`type="checkbox"`) and a home-page input (`#home-page-input`) — not placeholders.

### Step 2 — PASS (guest ↔ shields.json match)
- **Raw:** `shields.json` = `{enabled,block,strip,isolate,farble} = true` (all), `pausedSites:[]`;
  the guest Privacy & Shields toggles render all-on. Noted global key `block=true`.

### Step 3 — PASS (keyboard flip)
- **Raw:** `click` the Block-trackers switch (guest, mouse-flip + focus) → `shields.json` `block`
  **true → false**; then `pressKey(12,'Space')` (keyboard, toggle focused) → `block` **false →
  true**. `mouseFlipped=true`, `keyboardFlipped=true`.
- **Verdict:** PASS — the toggle is **keyboard-operable** (`Space` flipped the focused switch), and
  both flips wrote through the bridge to the store. `[a11y]`

### Step 4 — PASS (persisted)
- **Raw:** `shields.json` reflects each flip (read from disk between actions) — the change
  persisted through the origin-checked bridge to the store.

### Step 5 — PASS (settings ↔ panel consistency)
- **Raw:** flipped `block` to **false** from the settings guest (`shields.json` `block=false`),
  then opened the chrome slide-out **Shields panel** on a web tab (`Ctrl+Shift+P`). `#privacy-body`
  renders `Block trackers` as `<button role="switch" aria-checked="false">` while Shields/Strip/
  Isolate/Farble are `aria-checked="true"` — the panel reflects the **same** `block=false` set from
  settings.
- **Verdict:** PASS — the `shields-changed` broadcast reached **both** renderers (settings guest +
  chrome panel are consistent). (`block` restored to `true` afterward.)

### Step 6 — PASS (home page set by keyboard, persists)
- **Raw:** `settings.json` `homePage` `"https://www.google.com"` → after focusing the home input +
  `Ctrl+A` + `typeText("https://example.com/")` + commit → `homePage="https://example.com/"`.
- **Verdict:** PASS — keyboard-operable, persisted. `[a11y]`

### Step 7 — PASS (unsafe home page rejected)
- **Raw:** typed `javascript:alert(1)` + commit → `settings.json` `homePage` **unchanged**
  (`"https://example.com/"`, the prior valid value) — rejected by validation (`isSafeTabUrl`).
- **Verdict:** PASS.

### Step 8 — PASS (home takes effect)
- **Raw:** new tab via `Ctrl+T` → tab count 10 → 11; the new active tab opened to
  `https://example.com/` — the **newly-set** home page, not the old hardcoded default.
- **Verdict:** PASS — the home setting takes effect at tab-create.

### Steps 9-10 — DEFERRED (F8-eval, per spec)
Security origin-check assertions: `typeof window.goldfinchInternal === undefined` in a web guest
(9) and the page-context `ipcRenderer.invoke('internal-settings-set', …)` privilege-escalation
probe (10). Both need an in-page `evaluate` on a web/guest context to drive; `evaluate` exists on
chrome here but is **refused on the internal session**, and driving page-context IPC is not
expressible on the current surface. Carried as the spec's F8-deferred checkpoints (intent
preserved). Not run.

### Steps 11-12 — DEFERRED (F8-eval axe harness, per spec)
The `npm run a11y -- --target=goldfinch://settings` axe injection audit. As found in the
`settings-shell` run, the Flight-6 internal-session exclusion **refuses `injectScript`** on the
internal guest, so the axe engine cannot run against `goldfinch://settings` over this surface —
consistent with the spec marking these as F8-deferred. Not run.

## Closing Summary
The SC7 controls (global Shields toggles + home page) are operable from `goldfinch://settings`,
persist to disk, take effect (new-tab home), and stay consistent with the slide-out Shields panel
via the `shields-changed` broadcast; unsafe home values are rejected. Steps 1-8 **PASS**. Steps
9-12 remain the spec's F8-eval defers (security IPC probes + axe harness), the latter additionally
blocked by the Flight-6 internal-session `injectScript` refusal. Overall **PASS**, no
`LIKELY-REGRESSION`. (Store mutations — `homePage`, transient `block` flips — restored/to-be-restored
from the pre-run backup.)
