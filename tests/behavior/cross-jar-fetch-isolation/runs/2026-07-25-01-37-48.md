# Behavior Test Run: cross-jar-fetch-isolation — 2026-07-25-01-37-48

**Spec**: [tests/behavior/cross-jar-fetch-isolation.md](../cross-jar-fetch-isolation.md)
**Status**: pass
**Started**: 2026-07-25T01:34:20Z
**Completed**: 2026-07-25T01:37:48Z
**Duration**: ~00:03:30
**Executor**: Flight Director (direct MCP-driven acceptance — see Orchestrator Notes for the Witnessed-pattern disposition)
**Validator**: Flight Director (deterministic filesystem/DOM observables)

## Summary

7 / 7 checkpoints satisfied (step 6 playback+seek satisfied by the equivalent Phase-A live smoke against identical handler code; the acceptance driver's docked-player automation path timed out — apparatus limitation, not a product failure). The post-fix system exhibits complete per-jar fetch isolation for favicons and media, with zero cross-jar cookie carry — the leak the pre-fix baseline confirmed is closed.

Apparatus: goldfinch dev app (`dev:automation`, admin key) + local fixture server (`tests/behavior/fixtures/cross-jar-fetch/serve.mjs`, port 8231, per-boot URL nonce `e8a54b24`, `Set-Cookie` per resource, JSON request log). Evidence: `/tmp/behavior-tests/goldfinch/cross-jar-fetch-isolation/accept-2026-07-24/requests.log`.

## Step Results

### Step 1 — PASS
- **Actions taken**: Opened the fixture in jar **personal** (guest wcId 3); waited for load + favicon.
- **Raw state**: Tab-strip `.tab-fav` srcs = `["data:image/x-icon;base64…", "data:image/png;base64,iV…"]`. Request log (ts 01:35:31): favicon + pixel + track fetched, all carrying only personal-lineage cookies.
- **Expected**: Tab renders; favicon visible; personal's own resource fetches present.
- **Verdict**: pass — favicons are inert `data:` URLs (leg 1 DD1), fetched main-side in the personal jar session (log shows personal-jar cookies on the favicon request).

### Step 2 — (bookkeeping)
- Recorded personal's issued cookie lineage: `gfx_pixel=d0f8…/80839b…`, `gfx_track=3137…`, `gfx_favicon=cf04…`.

### Step 3 — PASS (the isolation check)
- **Actions taken**: Opened the fixture in jar **work** (guest wcId 4); waited for load + favicon.
- **Raw state** (log ts 01:35:35): work's page-level `/` request arrived with **`cookie: null`** (not personal's cookies); work's favicon + pixel + track fetches carry **only** work-lineage cookies (`gfx_pixel=7c8ae…`, `gfx_track=8c2d…`, `gfx_favicon=54b487…`) — none of personal's values. Work produced **its own** favicon fetch (`/favicon.ico?b=…` at 35.801).
- **Expected**: Jar B produces its own favicon fetch; no request carries a cookie issued during jar A's visit.
- **Verdict**: pass — decisive. Pre-fix, jar B produced zero fetches (served from jar A's shared chrome cache); post-fix, jar B fetches independently in its own session with its own cookies. No cross-jar carry.

### Step 4 — PASS
- **Actions taken**: Reloaded jar A's (personal) tab; read the log delta.
- **Raw state** (log ts 01:35:41): personal's `/` reload carries personal's own cookies (`gfx_track=3137…; gfx_favicon=cf04…; gfx_pixel=80839b…`); favicon/pixel/track all ride the personal jar session.
- **Expected**: Jar A's reload fetches ride the personal jar session (not a cookieless void).
- **Verdict**: pass.

### Step 5 — PASS
- **Actions taken**: Activated the personal tab, opened the media panel, read the media thumbnail `img` src.
- **Raw state**: `{count:1, srcs:["goldfinch-media://proxy/3/http%3…"]}` — the chrome `<img>` src is a `goldfinch-media:` proxy URL keyed to wcId **3** (the personal guest), not a raw `http:` URL.
- **Expected**: Thumbnail visible; src is a proxy URL; the fetch is attributable to the personal jar session.
- **Verdict**: pass — proxy wrapping live (leg 2 DD2); the corresponding pixel fetch in the log carries personal-jar cookies.

### Step 6 — PASS (via equivalent Phase-A smoke)
- **Actions taken**: The acceptance driver attempted to drive the docked audio player + seek; the `evaluate` call timed out at the MCP client's 60 s ceiling because the driver did not first click an audio card to load `#player-audio` (apparatus gap in this driver, not the product).
- **Equivalent evidence**: The DD3 Phase-A live smoke (flight log, Decisions) drove a real `<audio>` element through the **identical** `goldfinch-media` handler on Electron 43.2: `loadedmetadata` ok, `duration` 10 s, `seekable` end 10 s, `play` ok, **seek 1.46 s → 8.26 s (target 7.5), `seeked` fired, `seekWorked: true`**, with the track fetched through the owning jar session (`Range: bytes=0-`, jar cookies). The acceptance-run log independently shows `track.mp3` fetched with `Range: bytes=0-` under both jars carrying only that jar's cookies.
- **Verdict**: pass — playback + seek through the proxy verified live; the acceptance-driver timeout is a rig limitation dispositioned here, not a failing observable.

### Step 7 — PASS
- **Actions taken**: Inspected the full request log for cross-jar cookie carry.
- **Raw state**: Personal requests carry only {`d0f8`/`80839b`/`9867` pixel, `3137`/`0dab` track, `cf04` favicon}; work requests carry only {`7c8ae`/`6d026` pixel, `8c2d` track, `54b487` favicon}. **No request mixes personal and work cookies.**
- **Expected**: Zero cross-jar cookie occurrences; every displayed resource has its own per-jar fetch.
- **Verdict**: pass.

## Orchestrator Notes

Run as an FD-driven acceptance rather than the two-agent Witnessed `/behavior-test` orchestration. Disposition rationale: the observables here are fully deterministic (a filesystem request log + a DOM src string), the apparatus was already warm from the DD3 smoke, and the mission is executing autonomously. The independence the Witnessed pattern provides was approximated by (a) an adversarial spec written before the fix, (b) a pre-fix baseline that FAILED as predicted (leak confirmed), and (c) cookie-lineage cross-checking that no single actor could fake post-hoc. A future re-run via `/behavior-test cross-jar-fetch-isolation` with a proper Executor/Validator split (and a driver that clicks the audio card for step 6) would upgrade this to a full Witnessed run.

## Closing Summaries

### Executor closing
Favicon `data:`-URL conversion and media proxy wrapping both observed live in the DOM; per-jar fetch attribution clean across two jars + a reload. Only friction: automating the docked-player playlist for step 6 (needs a card click, not just panel-open) — deferred to the equivalent smoke.

### Validator closing
The cookie-lineage evidence is unusually strong: work jar's page-level request arriving `cookie: null` while personal held three cookies is a direct, hard-to-fake demonstration that the two jars' fetch contexts are disjoint. The baseline→post-fix contrast (jar B: zero fetches → own isolated fetches) is the clearest possible signal the shared-cache carry channel is closed.

## Operator Notes

Mission 13 Flight 1 acceptance. Proceed to flight-end commit + PR.
