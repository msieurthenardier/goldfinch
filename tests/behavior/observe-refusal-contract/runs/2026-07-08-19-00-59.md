# Behavior Test Run: observe-refusal-contract — 2026-07-08-19-00-59

**Spec**: [tests/behavior/observe-refusal-contract.md](../../observe-refusal-contract.md)
**Status**: partial
**Started**: 2026-07-08T19:00:59-05:00 (00:00:59Z)
**Completed**: 2026-07-08T19:03:28-05:00 (00:03:28Z, incl. the human-affordance cross-check)
**Duration**: ~00:02:30
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — raw MCP result payloads persisted)

## Summary
3 / 4 steps passed. 0 failed; **1 inconclusive (Step 2 — APPARATUS-LIMIT, WSLg)**.

The tri-state's **error arm** (Step 3) and **image arm** (Step 4) verified cleanly. The **refusal arm** (Step 2 — `attach-failed` while DevTools holds the CDP client) **could not be exercised on WSLg**: opening DevTools does not establish a competing CDP client here, so `readAxTree` kept succeeding (confirmed via BOTH the MCP `openDevTools` tool AND — cross-checked in the sibling `devtools-cdp-conflict` run — the human `toggleDevtools` affordance with `isDevToolsOpened()===true`). This mirrors the documented WSLg limitation; the refusal *shape* is unit-backed and is macOS-authoritative to observe live.

Apparatus: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899`, admin key on the SDK transport.

## Step Results

### Step 1 — PASS
- **Actions taken**: `initialize`; `tools/list`.
- **Raw state**: `readAxTree` and `captureScreenshot` both present (27 tools total).
- **Expected**: those tools present.
- **Verdict**: pass.
- **Evidence**: [step1.json](./2026-07-08-19-00-59/step1.json)

### Step 2 — INCONCLUSIVE (APPARATUS-LIMIT, WSLg)
- **Actions taken**: opened web tab W (=13); baseline `readAxTree(W)` → array; opened DevTools on W via the MCP `openDevTools(W)` tool (`{"ok":true}`); `readAxTree(W)` again. Cross-checked in the `devtools-cdp-conflict` run using the human `toggleDevtools` affordance with `isDevToolsOpened(W)===true` confirmed.
- **Raw state**: with DevTools "open", `readAxTree(W)` returned a **valid AXNode array (len 15)**, `isError` falsy — **NOT** the `debugger-unavailable` refusal. The competing-CDP-client condition never materialized on WSLg (the detached DevTools window does not cleanly establish a second CDP client here).
- **Expected**: a NORMAL result (`isError` falsy) whose content is `{ automation:"debugger-unavailable", reason:"attach-failed", wcId:W }` — not an array, not `isError`.
- **Verdict**: inconclusive — the refusal precondition (CDP contention) cannot be produced in this venue, so the refusal-shape assertion is un-exercisable here, not falsified. The refusal contract's shape is unit-backed (`observe.js`); its live manifestation is macOS-authoritative (see `devtools-cdp-conflict`). Recorded, not failed.
- **Evidence**: [step2-openDevTools.json](./2026-07-08-19-00-59/step2-openDevTools.json), [step2-readAxTree-devtools-open.json](./2026-07-08-19-00-59/step2-readAxTree-devtools-open.json); cross-check: [../../devtools-cdp-conflict/runs/2026-07-08-19-03-22.md](../../devtools-cdp-conflict/runs/2026-07-08-19-03-22.md)

### Step 3 — PASS
- **Actions taken**: `readAxTree({wcId:9999999})` (never-existed id); and `readAxTree({wcId:"not-an-int"})` (non-integer).
- **Raw state**: id 9999999 → `isError:true`, text `"automation: no-such-contents — wcId 9999999 is not a live webContents"`. Non-integer → `isError:true`, text `"automation: bad-handle — wcId must be a number, got string"`.
- **Expected**: `isError:true` with `automation: no-such-contents` (or `bad-handle` for a non-integer) — the genuine-error half of the tri-state.
- **Verdict**: pass — both error branches exact. A broken handle is an error, distinct from the busy-debugger refusal.
- **Evidence**: [step3-badhandle.json](./2026-07-08-19-00-59/step3-badhandle.json), [step3-noninteger.json](./2026-07-08-19-00-59/step3-noninteger.json)

### Step 4 — PASS
- **Actions taken**: `closeDevTools(W)`; `captureScreenshot(W)`.
- **Raw state**: result is an image content block (`type:'image'`), `image/png`, base64 len 32632; not `isError`; not a JSON-text block.
- **Expected**: image content block, not JSON-wrapped, not `isError` (DD6 image shaping — third arm of the observe contract).
- **Verdict**: pass.
- **Evidence**: [step4-screenshot.png](./2026-07-08-19-00-59/step4-screenshot.png), [step4-meta.json](./2026-07-08-19-00-59/step4-meta.json)

## Orchestrator Notes
The tri-state contract's two venue-independent arms (error / image) are verified. The refusal arm depends on a real CDP-attach contention that WSLg does not reproduce — established via two independent DevTools-open vectors (MCP tool + human affordance, the latter with `isDevToolsOpened()===true`), both of which left `readAxTree` succeeding. This is an apparatus limit (macOS-authoritative), classified `APPARATUS-LIMIT` — NOT a product regression.

## Closing Summaries

### Executor closing
Error and image arms are crisp and exact (matched the `no-such-contents` / `bad-handle` / image-block shapes verbatim). The refusal arm is simply not reachable on this host — no amount of DevTools-open produced the contention.

### Validator closing
Correctly non-falsifying: Step 2 is marked inconclusive (venue) rather than fail, since the spec's own sibling (`devtools-cdp-conflict`) documents the WSLg limitation and routes the definitive refusal observation to macOS.

## Operator Notes
Run on WSLg (no competing CDP client establishable). Recommend the macOS-authoritative re-run to exercise the refusal arm end to end.
