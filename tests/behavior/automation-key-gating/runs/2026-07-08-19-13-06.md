# Behavior Test Run: automation-key-gating — 2026-07-08-19-13-06

**Spec**: [tests/behavior/automation-key-gating.md](../../automation-key-gating.md)
**Status**: fail
**Started**: 2026-07-08T19:07:00-05:00 (settings guest opened)
**Completed**: 2026-07-08T19:13:30-05:00
**Duration**: ~00:06:00
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — readDom outerHTML + readAxTree + settings.json + screenshot persisted)

## Summary
**FAIL — the DD9 toggle-OFF key-generation gate is not observable / not enforced on this build.** With persisted `automationEnabled=false` on disk, the rendered "Enable automation surface" toggle is **ON** (readAxTree `checked="true"`, visually filled — see screenshot), and **every jar's mint button (`Generate key`/`Rotate key`) AND the admin `Rotate admin key` button render ENABLED** (no `disabled` attribute in readDom outerHTML; no `disabled` property in readAxTree). The DD9 contract requires those mint buttons to be **disabled while the toggle is OFF**. The prior run (2026-06-17) passed Steps 1-3 with mint disabled while OFF, so this is a **behavior change on this branch → classify LIKELY-REGRESSION**.

Keyless jars' **Revoke** buttons ARE correctly `disabled` (readDom `disabled=""` + readAxTree `disabled=true`), confirming the disabled-state rendering path works — so the mint buttons genuinely lack a disabled state, this is not a read artifact.

Apparatus: `npm run dev:automation` with `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=8899`. Admin MCP client. Settings guest opened via the trusted `createTab('goldfinch://settings',null,{trusted:true})` path (the exact function the kebab→Settings item invokes; the MCP `openTab` tool cannot open `goldfinch://` — it passes only url+jarId and `isSafeTabUrl` rejects the scheme). Guest enumerated as `{wcId:19,url:'goldfinch://settings/',jarId:'internal'}`.

## Step Results

### Step 1 — FAIL (the OFF-state precondition does not render)
- **Actions taken**: connected admin client; `getChromeTarget()` → chrome wcId 1; read `automationEnabled` + key hashes from `~/.config/goldfinch/settings.json`; opened Settings; `enumerateTabs` → guestWcId 19; `readDom(19)` + `readAxTree(19)`.
- **Raw state**: disk `automationEnabled=false`; disk `automationKeyHashes={}` (EMPTY) and `automationAdminKeyHash=""` (EMPTY). BUT the rendered UI shows the `default` jar as **"key set" / "Rotate key"** and the admin block as **"Admin key set"** — i.e. the minted hashes live in-memory (they authenticate) but were **not flushed to disk**. And the enable toggle renders **checked=true** (AX) despite disk false.
- **Expected**: `getChromeTarget` numeric ✓; store `automationEnabled===false` (persisted OFF) ✓ on disk; a non-empty `default` key hash (fixture present); the Keys subsection renders.
- **Verdict**: fail — two precondition divergences: (a) the persisted toggle is OFF on disk but the **UI renders the toggle ON** (`checked=true`), so the "toggle OFF" UI the test needs is not reproduced; (b) the `default` key hash is **absent from settings.json** (mint did not persist it) even though the UI shows `key set`. The store is not the authoritative mirror of the UI here.
- **Evidence**: [FINDING-summary.json](./2026-07-08-19-13-06/FINDING-summary.json), [settings-automation-section.png](./2026-07-08-19-13-06/settings-automation-section.png), [guest-settings-ax.json](./2026-07-08-19-13-06/guest-settings-ax.json)

### Step 2 — FAIL (toggle-OFF jar gating not enforced) `[a11y]`
- **Actions taken**: `readDom(19)` + `readAxTree(19)`; extracted every jar row's mint + Revoke button state.
- **Raw state**: keyed jars → mint **"Rotate key"** with **no disabled** (readDom) / **no disabled, focusable=true** (readAxTree); their Revoke enabled. Keyless jars → mint **"Generate key"** with **no disabled**; their Revoke **`disabled`** (readDom `disabled=""`, readAxTree `disabled=true`).
- **Expected**: toggle OFF ⇒ every jar's mint button (`Rotate key`/`Generate key`) is **disabled**; keyed jar Revoke enabled; keyless jar Revoke disabled.
- **Verdict**: fail — the mint buttons are **ENABLED** while the persisted toggle is OFF. Only the Revoke gating (hasKey) is correct. The DD9 mint-gate is not enforced (the rendered toggle is ON, and mint follows the toggle-ON/effective state rather than the persisted-OFF value the spec pins).
- **Evidence**: [guest-settings-dom.html](./2026-07-08-19-13-06/guest-settings-dom.html), [FINDING-summary.json](./2026-07-08-19-13-06/FINDING-summary.json)

### Step 3 — FAIL (toggle-OFF admin gating not enforced) `[a11y]`
- **Actions taken**: read the `#automation-admin` block via `readDom(19)` + `readAxTree(19)`.
- **Raw state**: admin block shown (`GOLDFINCH_AUTOMATION_ADMIN=1`), `#automation-admin-status` = "Admin key set", mint button **"Rotate admin key"** with **no disabled** (enabled), admin Revoke enabled.
- **Expected**: toggle OFF ⇒ the admin mint button is **disabled**; admin Revoke follows `adminKeySet` only.
- **Verdict**: fail — the admin mint button is **ENABLED** while the persisted toggle is OFF (same regression as Step 2). Admin Revoke (enabled, admin key set) is correct.
- **Evidence**: [guest-settings-dom.html](./2026-07-08-19-13-06/guest-settings-dom.html)

### Step 4 — INCONCLUSIVE (APPARATUS-LIMIT) `[a11y]`
- **Actions taken**: attempted to flip the enable toggle via coordinate `click(19, deviceX, deviceY)` (toggle switch ≈ CSS (873,692) → device (1091,865)).
- **Raw state**: after the click, disk `automationEnabled` unchanged (`false`), AX `checked` unchanged (`true`), admin mint still enabled — the coordinate click did not register on the toggle.
- **Expected**: persisted `automationEnabled` flips to `true`; mint buttons become enabled live.
- **Verdict**: inconclusive — coordinate clicking the internal settings guest is unreliable in this venue (device-pixel + guest-viewport origin; same limitation the prior run recorded for the live toggle-flip). Also moot: the mint buttons are already enabled (Steps 2-3), so the "flip ON → enables" transition cannot be distinguished from the broken baseline. Not driven to a conclusion.
- **Evidence**: (toggle-flip experiment — no state change observed)

### Step 5 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven (depends on Step 4's live flip, which is apparatus-limited).
- **Expected**: flip OFF ⇒ mint disabled again, live.
- **Verdict**: inconclusive — live toggle-flip not drivable via coordinate click in this venue.

### Step 6 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven (coordinate-click Revoke on the guest; same apparatus limit).
- **Expected**: Revoke succeeds while OFF; `default` row → `no key`, hash removed from store.
- **Verdict**: inconclusive — coordinate-click interaction on the guest unreliable; and the store↔UI mirror is already broken (mint hashes not on disk), so a store-read witness would be unreliable regardless.

## Orchestrator Notes

**HEADLINE FINDING — LIKELY-REGRESSION (FD triage).** With persisted `automationEnabled=false` (disk), the rendered "Enable automation surface" toggle is **ON** (readAxTree `checked="true"`; visually filled, matching the ON Shields toggles in the screenshot) and **all mint buttons are enabled** (`Generate key`/`Rotate key` per jar + `Rotate admin key`). This breaks the DD9 "mint disabled while persisted toggle OFF" contract that this spec exists to verify, and the toggle↔store consistency that `settings-automation` Step 3 asserts. The prior run (2026-06-17) passed Steps 1-3 (mint disabled while OFF), so the behavior changed on this branch. Most plausible mechanism: the rendered `#automation-enabled` checkbox and the DD9 gate now follow the **effective (dev-override-enabled) surface state** rather than the **persisted** `automationEnabled` — the spec's "Why testable in DEV" note explicitly requires the *persisted* value to drive the checkbox and the gate so the dev UI reproduces the production "toggle OFF" surface. **Not confirmed attributable to this flight's Leg-2 keyboard-bridge change** (leg 2 touches cross-view keyboard nav, not settings) — could be pre-existing on the branch; FD to triage.

**Secondary finding.** `mintJarKey`/`mintAdminKey` (via the dev auto-mint) do **not persist** the key hashes to `settings.json` — disk `automationKeyHashes` stays `{}` and `automationAdminKeyHash` `""` while the keys authenticate and the UI shows `key set` / `Admin key set`. This breaks the spec's "authoritative store read" witness (the store no longer mirrors the minted key state).

**Apparatus limit.** Coordinate clicks on the internal `goldfinch://settings` guest are unreliable here (device-pixel scaling + guest viewport origin), so the live toggle-flip / revoke steps (4-6) are inconclusive — consistent with the prior run.

## Closing Summaries

### Executor closing
The readDom + readAxTree + screenshot triangulate cleanly on the failure: disabled-rendering works (keyless Revoke `disabled`), so the mint buttons genuinely lack disabled while the toggle renders ON and the disk says OFF. The store no longer mirrors either the toggle or the key state in dev, which also undermines the spec's authoritative-witness plan.

### Validator closing
This is a genuine contract violation, not a spec-drift: the spec's DD9 gate is explicit and prior-passing. Recommend FD confirm whether the toggle/gate should read persisted vs effective, and whether the mint→settings.json persistence gap is intended.

## Operator Notes
Run on WSLg (DPR 1.25); admin profile `~/.config/goldfinch`. The toggle-OFF UI that this test needs is not reproducible in dev on this build.
