# Behavior Test Run: automation-key-gating — 2026-06-17-17-23-30

**Spec**: [tests/behavior/automation-key-gating.md](../automation-key-gating.md)
**Status**: partial
**Started**: 2026-06-17T17:22:00
**Completed**: 2026-06-17T17:24:00
**Duration**: ~00:02:00
**Executor**: FD-driven (machine-read evidence; mission standard — no Witnessed crew elected)
**Validator**: FD-driven

## Summary
The **load-bearing gating contract (Steps 1-3) PASSES** via machine-read evidence: with the persisted toggle OFF, every jar mint button AND the admin mint button are `disabled`, while Revoke follows `hasKey` only. The live toggle-flip steps (4-6) are **carried** — flipping the toggle requires trusted input into the **internal `goldfinch://settings` guest**, and the FD apparatus cannot derive the toggle's click geometry without vision/coordinate-math under WSLg (and `evaluate`-on-internal is correctly blocked by the F9 DD2 HIGH guard, so the toggle can't be flipped programmatically). Apparatus: `dev:automation` on `:52351`, admin key; Settings opened by clicking the chrome's own `#kebab-settings` button via the chrome renderer.

## Step Results

### Step 1 — PASS (precondition)
- **Raw state**: `getChromeTarget()` → chrome wcId 1 (surface live via dev override). Dev-profile `~/.config/goldfinch-dev/settings.json`: `automationEnabled === false` (toggle OFF) AND `automationKeyHashes.default` present (the revoke-while-off fixture) AND `automationAdminKeyHash` present. Settings opened (`#kebab-settings` click) → guest `goldfinch://settings` wcId 4; `readDom(4)` shows the `#automation` section + `automation-jars`.
- **Verdict**: pass — toggle OFF + `default` key present; Keys subsection renders.

### Step 2 — PASS (toggle-OFF jar gating)
- **Raw state** (from `readDom(4)` `outerHTML`, attribute-correct): the four jar mint buttons all `disabled` — `Rotate key` (the keyed `default` jar) + 3× `Generate key` (keyless jars). Revoke buttons: `default` jar **enabled** (`hasKey`), keyless jars **disabled** (`!hasKey`).
- **Verdict**: pass — toggle OFF ⇒ every jar mint disabled; Revoke gated by `hasKey` only, not the toggle.

### Step 3 — PASS (toggle-OFF admin gating)
- **Raw state**: `<button id="automation-admin-mint" ... disabled="">Rotate admin key</button>` — admin mint **disabled**. Admin Revoke **enabled** (an admin key exists → `adminKeySet`, independent of the toggle).
- **Verdict**: pass — toggle OFF ⇒ admin mint disabled; admin Revoke follows `adminKeySet`.

### Step 4 — CARRIED (live enable on flip-ON)
- **Why not executed**: flipping the toggle requires trusted input (`click(4, x, y)`) into the internal settings guest; deriving the toggle's pixel geometry needs a vision/coordinate step the FD apparatus doesn't have under WSLg, and `evaluate(4, …)` (which could `.click()` it) is **correctly refused** (internal-session exclusion, F9 DD2 — the security guard working as designed). Carried to a future run with a coordinate-capable apparatus (or a guided HAT).

### Step 5 — CARRIED (live disable on flip-OFF)
- Same reason as Step 4.

### Step 6 — CARRIED (revoke-while-off)
- The Revoke-enabled-while-OFF *state* is verified statically in Step 2 (default Revoke enabled with toggle OFF). The *action* (click Revoke → key removed from store) is carried (same coordinate-click limitation).

## Orchestrator Notes
FD-driven via `node` over `scripts/lib/mcp-client.mjs` (admin key). Settings was opened without coordinate-clicking by invoking the chrome's own `#kebab-settings` button through the chrome renderer (`evaluate(chromeWc,'document.getElementById("kebab-settings").click()')`) — a trusted same-renderer click, NOT eval-on-the-internal-guest. The **contract this spec exists to prove — toggle-OFF disables all mint controls while Revoke remains hasKey-gated — is fully verified** (Steps 1-3). The carried steps (live flip + revoke action) are UI-interaction steps; their non-execution is an **apparatus limitation** (coordinate-click geometry under WSLg + the internal-session eval exclusion), not a product defect. Carry to a guided HAT or a vision/coordinate-capable run.

## Closing Summaries
### Executor closing
readDom over the internal settings guest (an admin read-only op, allowed) gives the rendered `disabled` attributes directly — the gating contract is observable without any clicking. The toggle flip is the only part needing trusted geometry.
### Validator closing
Core contract solid. The internal-session eval exclusion (correctly) prevents programmatically flipping the toggle, which is why the live-flip steps need a real-input apparatus. Recommend a brief guided HAT to close Steps 4-6, or accept the static gating proof as sufficient for SC8's opt-in model.

## Operator Notes
_(pending — decide whether Steps 4-6 warrant a guided HAT or the static gating proof suffices)_
