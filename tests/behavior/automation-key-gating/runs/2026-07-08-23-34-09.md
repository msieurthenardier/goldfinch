# Behavior Test Run: automation-key-gating — DD9 OFF-branch witness — 2026-07-08-23-34-09

**Spec**: [tests/behavior/automation-key-gating.md](../../automation-key-gating.md)
**Status**: pass (OFF-branch witness only — Steps 1-3 of the spec's toggle-OFF gating contract)
**Started**: 2026-07-08T23:33:00
**Completed**: 2026-07-08T23:35:00
**Executor**: Leg-1c batch executor (SC3 parity sweep — DD9 OFF-branch witness)
**Surface**: native `WebContentsView` — chrome `wcId=1`, internal settings guest. Admin MCP over
`127.0.0.1:8899`. Dev profile `~/.config/goldfinch-dev`.

## Scope
This is the **DD9 mint-gate OFF-branch witness** requested for the Flight-6 parity sweep — it
witnesses the toggle-**OFF** gating contract (spec Steps 1-3): with `automationEnabled` persisted
**false**, every key-**mint** button is disabled while **Revoke** stays governed by `hasKey`. The
live toggle-flip Steps 4-6 (which the spec's own prior runs marked apparatus-limited) are **not**
in scope for this witness.

## Setup (per the witness protocol)
1. Tore down the running app; backed up `settings.json`
   (`/tmp/behavior-tests/settings.json.DD9BAK`).
2. Set `automationEnabled=false` in `~/.config/goldfinch-dev/settings.json`, **keeping** the jar key
   hashes (`default`, `personal`, `work`) and the admin key hash intact.
3. Relaunched admin-wired (`GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1
   GOLDFINCH_MCP_PORT=8899 npm run dev:automation`). The **dev-enable override** (DD3/DD4) keeps the
   surface bound + the harness connected **while the persisted toggle is OFF** — the app still shows
   "Connected — listening on 127.0.0.1:8899", which is exactly what makes the OFF-state UI
   observable over MCP.
4. Opened Settings (kebab → Settings), navigated to the Automation section.

## Witness Result — PASS

**Authoritative store (`settings.json`):** `automationEnabled=false`; jar hashes present for
`default`, `personal`, `work`; admin key hash present. (The fixture for "Revoke-while-off": keyed
jars exist while the toggle is OFF.)

**Rendered UI (guest `readDom` + `captureScreenshot` — `automation-keys-OFF.png`):**

- **Enable-automation toggle renders OFF.** `<input type="checkbox" id="automation-enabled">` — no
  `checked` (rendered grey/left in the screenshot).
- **Every mint button is DISABLED** (`allMintDisabled=true`) — read from the `disabled` **attribute**
  in `readDom` outerHTML and corroborated visually (greyed):
  - keyed jars `Default` / `Personal` / `Work` → **`Rotate key`** `disabled`;
  - keyless jars (`Banking`, `Shopping`, `Seed1`, … 9 rows) → **`Generate key`** `disabled`;
  - the env-gated admin mint → **`Rotate admin key`** `disabled`.
- **Revoke follows `hasKey`, NOT the toggle** (no auto-revoke on toggle-off):
  - `Default` / `Personal` / `Work` **Revoke enabled** (`disabled=false`, bright/gold) — they have
    keys;
  - keyless jars' **Revoke disabled**;
  - the admin **Revoke enabled** (admin key hash present).

This is the DD9 contract exactly: **toggle OFF ⇒ all mint (Generate/Rotate + admin) buttons
disabled; Revoke stays available for jars/admin that hold a key** because toggle-off does not
auto-revoke. The keys survived the toggle being OFF (hashes still present).

Evidence: `/tmp/behavior-tests/goldfinch/automation-key-gating/2026-07-08-23-34-09/`
(`evidence.json`, `automation-keys-OFF.png`, `window-OFF.png`, `keys-dom.html`).

## Teardown
`settings.json` restored from the pre-witness backup — `automationEnabled` back to **true** (see the
batch-level restore verification). No source modified.

## Verdict
**PASS** — the DD9 mint-gate OFF branch holds: the persisted OFF toggle disables all key-mint
controls while leaving Revoke governed solely by key presence, and does not auto-revoke existing
keys. No `LIKELY-REGRESSION`.
