# Behavior Test Run: closed-tab-reopen — 2026-07-15-01-51-04 (regression re-run)

**Spec**: [closed-tab-reopen.md](../../closed-tab-reopen.md) (UNMODIFIED — regression re-run)
**Status**: pass
**Started**: 2026-07-15T01:51:04Z
**Completed**: 2026-07-15T02:12:00Z (approx)
**Duration**: ~00:20
**Executor**: fresh agent (batched 1–5, 6–9)
**Validator**: fresh agent (single judgment pass, all 9)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/closed-tab-reopen/2026-07-15-01-51-04/` during the run; re-derivable by re-running the spec.

## Purpose

Single-window regression gate for the **F6 window-registry conversion**
(M09 Flight 6, leg 2 `window-registry-and-routing`). Spec unmodified from
the first run ([2026-07-14-21-48-13](2026-07-14-21-48-13.md), 9/9 PASS).

## Summary

**9 / 9 checkpoints passed — REPRODUCES.** Same shapes on the
registry-converted build: empty-stack chord byte-identical no-op; reopen
with url + jar + strip position + LIVE history round-trip
(goBack→page2/goForward→page3 on the restored view); all three capture
points delivered (chrome-focus chord, guest-delivered chord through the
guest `before-input-event` forwarder, sheet-open chord through the sheet
accelerator — the roaming/registry-re-pointed paths); burner and internal
tabs structurally never captured (byte-identical no-ops, zero grep hits);
jar-deleted fallback reopened into the default jar with the
user-visible announcement byte-identical to the first run.

Improvements over the first run's conditions: this run used the fresh
scratch profile (the first run's non-fresh-profile ruling did not recur),
and step 2's fresh-boot precondition held literally.

## Differences vs first run — accepted/documented

- Step 9 exercised the row's literal variant (Personal remained default;
  fallback to Personal). The first run had exercised a stronger variant
  (deleting the resolved default). Both are within the row's parameterized
  wording.
- Step 6: the Executor's first menu-closed poll used a DOM
  `querySelectorAll` count (stays 5 — close HIDES the sheet, nodes remain
  in DOM) — an instrument error, self-flagged; the verdict-relevant
  instrument (readAxTree menuitems 5→0 + `#sheet-menu.hidden` + pixels)
  matches the first run exactly.
- WSLg `hasFocus:false` on guest click — the standing apparatus deviation;
  delivery was by construction (chord addressed to the guest wcId).
- Step 7's burner no-op ran with an empty stack — the spec rules that
  valid (burner is structurally never captured regardless).

## Checkpoint Results (compact — regression mode)

| # | Verdict | Anchor evidence |
|---|---------|-----------------|
| 1 | PASS | step1-tools-list-raw.txt, step1-chrome-target-raw.txt (C=1) |
| 2 | PASS | step2-before/after-{tabs,dom}-raw.txt (byte-identical) |
| 3 | PASS | step3-jars-raw.txt (Personal+Work), step3-final-tabs-raw.txt (W=3, 3 entries), step3-dom-order-raw.txt (P=1) |
| 4 | PASS | step4-poll-reopen-raw.txt (W2=4 page3 work at P), step4-poll-back/forward-raw.txt |
| 5 | PASS | step5-poll-reopen-raw.txt (guest-delivered → 6 page2 work) |
| 6 | PASS | step6-poll-reopen-raw.txt (sheet-open → 9 page1), step6-axtree-after-raw.txt (menuitems 0), step6-sheet-state-raw.txt (.hidden) |
| 7 | PASS | step7-before/after-{tabs,dom}-raw.txt (no-op; page4 zero hits) |
| 8 | PASS | step8-before/after-{tabs,dom}-raw.txt (no-op; settings/internal zero hits) |
| 9 | PASS | step9-poll-reopen-raw.txt (13 page5 PERSONAL = default), step9-tabstatus-raw.txt (fallback announcement) |

## Orchestrator Notes

- Regression mode (fresh Executor batched + fresh Validator single-pass),
  part of the leg-2 regression TRIPLE. Scratch profile; fixtures on :8000;
  admin key env-only (Executor verified absent from every evidence file
  and scrubbed its monitor transcript).
