# Behavior Test Run: closed-tab-reopen — 2026-07-14-21-48-13

**Spec**: [closed-tab-reopen.md](../../closed-tab-reopen.md)
**Status**: pass
**Started**: 2026-07-14T21:48:13Z
**Completed**: 2026-07-14T23:40:00Z (approx)
**Duration**: ~01:52
**Executor**: sub-agent (Sonnet), live SendMessage continuation
**Validator**: sub-agent (Sonnet), live SendMessage continuation

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/closed-tab-reopen/2026-07-14-21-48-13/`
during the run; re-derivable by re-running the spec.

## Summary

**9 / 9 steps PASS** — first run of this spec (M09 Flight 4 leg 3). The
run proved live the whole unit-invisible surface: the close→capture→reopen
round trip; `navigationHistory.restore()` fidelity on a fresh
`WebContentsView` (real goBack/goForward); all three capture points (chrome
keydown, guest `before-input-event`, menu-overlay sheet incl. async menu
dismissal); the empty-stack no-op as an explicit positive control;
structural non-capture of burner AND internal tabs; and the jar-deleted
fallback with its announcement.

## Checkpoint Results (abridged — all pass; per-step verdicts in the
transcript evidence)

- **1** Probe: all ten tools present; chrome wcId C=1; fresh boot confirmed
  (stack structurally empty per DD1).
- **2** Empty-stack no-op: chord delivered ({ok:true}) and provably changed
  nothing — before/after enumerateTabs AND DOM order byte-identical
  (Validator re-diffed independently).
- **3 (setup)** W = 3-entry-history Work tab at page3, DOM index P=1.
  **Environment deviation (ruled, recorded)**: the dev profile was NOT
  fresh-scratch — jars work (DEFAULT)/h6-probe/tab-test, no Personal.
  Orchestrator ruled proceed; step 9 judged against its parameterized
  wording.
- **4** Chrome-focus reopen, full fidelity: W2 at page3, jarId work, DOM
  index P; goBack → page2 (history genuinely rebuilt on the fresh view);
  goForward → page3.
- **5** Guest-delivered reopen: chord addressed INTO the guest produced the
  chrome-level reopen (G-src's page2 entry, jarId work); the guest's own
  page untouched. WSLg hasFocus deviation accepted (delivery by
  construction).
- **6** Sheet-open reopen: one keypress on the probed sheet wcId reopened
  S-src's entry AND dismissed the menu (async, polled; screenshots
  Validator-inspected; axtree menuitem count 5→0).
- **7** Burner exclusion: burner minted via the dogfooding seam, closed via
  the SAME capturing path — chord no-oped byte-identically; page4's URL
  appears NOWHERE across all polls. Positive persist-jar allowlist proven.
- **8** Internal exclusion: settings tab opened via the trusted route,
  closed — chord no-oped byte-identically; goldfinch://settings and jarId
  internal 0 hits everywhere.
- **9** Jar-deleted fallback — a STRONGER case than authored: the deleted
  jar was the resolved DEFAULT, forcing live default re-resolution
  (work → h6-probe) before fallback. Reopened tab at page5 in the NEW
  default (never the dead jar); `#tab-status` = "Reopened tab — its cookie
  jar no longer exists; reopened in the default jar".

## Orchestrator Notes

- Live two-agent mode; cache-cold; fresh crew (first run); free-fallback
  port.
- Deviations (none verdict-affecting): non-fresh dev profile (ruled;
  future runs wanting the literal seed should point XDG_CONFIG_HOME at a
  scratch dir); WSLg hasFocus; teardown residue — the work jar was
  re-added and re-defaulted after step 9, but its pre-run
  `retentionDays: 14` was not restorable (now 30) and its stored data was
  wiped by the delete (dev-profile side effect, recorded).
- **Finding for future runs**: every reopen in this run landed at the LAST
  strip position, where `insertAt(P)` is indistinguishable from append —
  position preservation rests on the leg-2 live check's mid-strip case. A
  future run/variant should close a mid-strip tab with neighbors on both
  sides. (Spec-polish candidate.)
- Validator run summary + accepted-deviation ledger are in the step-9
  verdict notes; Executor teardown record in its step-9 report.

## Operator Notes

First run green end-to-end; spec transitions `draft` → `active`. The
mission criterion's keyboard half is now behavior-test-backed; the menu
half lands with the context-menu flight (flight DD3).
