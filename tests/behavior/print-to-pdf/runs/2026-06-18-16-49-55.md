# Behavior Test Run: print-to-pdf — 2026-06-18-16-49-55

**Spec**: [tests/behavior/print-to-pdf.md](../../print-to-pdf.md)
**Status**: partial
**Started**: 2026-06-18T16:30:00Z
**Completed**: 2026-06-18T16:49:55Z
**Duration**: ~00:20:00
**Executor**: Flight-Director-driven (script Executor agent over the goldfinch example MCP client)
**Validator**: Flight Director (independent act/judge split preserved)

**Evidence (ephemeral, local-only, not committed)**: raw tool results captured in-agent during the run; re-derivable by re-running.

## Summary

1 / 2 judged checkpoints PASS (the core `printToPDF` → valid-PDF assertion). 1 not-live-reachable (step 3 — internal-tab refusal: the automation surface cannot obtain an internal wcId; the jar-façade + op-local guards are unit-proven, live confirmation deferred to HAT).

> **Apparatus deviation (Orchestrator note):** same as the `page-zoom` run — driven via goldfinch's own example MCP client over Bash (script-Executor + Flight-Director Validator), port 49801. The `printToPDF` PDF assertion was confirmed under BOTH the **admin** key (initial run) and a **default-jar** key (leg-05 re-confirmation) — proving SC8 jar-scoped parity.

## Checkpoint Results

### Checkpoint 1 — PASS (setup)
- **Actions taken**: ensured a web tab on `https://example.com` (wcId 3, default jar).
- **Raw state**: tab present.
- **Expected**: (setup — no judgment).
- **Verdict**: pass.

### Checkpoint 2 — PASS
- **Actions taken**: `printToPDF{3}` (confirmed under admin AND under the default-jar key); base64 decoded and inspected.
- **Raw state**: base64 length 19568; decoded byteLength 14674; first 8 bytes ASCII `%PDF-1.4`; tail contains `%%EOF`; `isError:false`.
- **Expected**: non-empty base64; decoded bytes begin with `%PDF-` and end near `%%EOF` — a structurally valid PDF from live web content.
- **Verdict**: pass — `printToPDF` renders the active tab's web content to a valid PDF and returns its bytes as base64 (plain JSON text, not an image block, per DD4). Jar-scoped parity confirmed: the default-jar key produced the same valid PDF on its own tab (leg-05 re-confirmation).

### Checkpoint 3 — SKIPPED (not live-reachable)
- **Actions taken**: attempted `printToPDF` against an internal `goldfinch://settings` wcId under a jar key.
- **Raw state**: internal wcId not obtainable via the surface (`openTab "goldfinch://settings"` → null; `enumerateTabs` filters internal). Under the jar key, an **out-of-jar** wcId correctly returns `automation: out-of-jar` (leg-05 re-confirmation), demonstrating the jar-façade refusal path on the nearest reachable case.
- **Expected**: `printToPDF` against the internal tab with a jar key refused/no-op via the jar façade (`resolveContentsForJar`), as a clean refusal.
- **Verdict**: internal target not reachable via the automation surface (boundary working). The jar-façade internal-exclusion and the op-local internal guard are **unit-proven** (`automation-scope.test.js`, `automation-print.test.js`). Live confirmation of the internal case deferred to HAT (human-opened settings).

## Orchestrator Notes
- Apparatus deviation (above): example-client drive + Flight-Director Validator; port 49801.
- The leg-05 re-confirmation under the default-jar key is the SC8-parity evidence: `printToPDF` on the jar's own tab → valid PDF; out-of-jar → `automation: out-of-jar`.
- Spec-quality finding (shared with `page-zoom` step 7): the internal-target step is not reachable via the pure automation surface; defer to HAT.

## Operator Notes
- **2026-06-18 — SC2 manual native-print confirmed** by the operator: `Ctrl+P` opens the OS print
  dialog and **Save as PDF** produces a file, works as expected. (Internal-step HAT confirmation
  tracked in the `hat-and-alignment` leg.)
