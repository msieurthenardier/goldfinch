# Behavior Test: Bookmarks — Jar Ownership, Burner Inertness, and Jar Lifecycle

**Slug**: `bookmarks-jar-scoping`
**Status**: active
**Created**: 2026-07-30
**Last Run**: 2026-07-31-19-35-58 — 17/17 pass ([run log](bookmarks-jar-scoping/runs/2026-07-31-19-35-58.md))

## Intent

Verifies that bookmarks are owned by the cookie jar rather than the application: the bar, the star, and address-bar suggestions all reflect only the active tab's jar and swap contents when the active tab's jar changes; the same URL can be bookmarked independently in two jars; burner jars are inert for bookmarking; and a jar's bookmarks die with the jar but survive an identity wipe. This needs a behavior test because the central observable is *rendered chrome state changing in response to a tab switch* across multiple live jars and windows — a cross-process, cross-surface behavior that unit tests structurally cannot see. The lifecycle clauses need a real jar being really deleted, with a real session behind it.

## Preconditions

- App running via the canonical admin dev launch (`docs/dev-testing.md`); MCP key is **admin-tier**, confirmed by a successful `getChromeTarget` — not merely by tab enumeration succeeding.
- **The binding reaches the instance under test — verify this, do not assume it.** Probe by opening a tab and confirming it appears in `enumerateTabs`, and confirm the tab set you see is the fixture you built. This is a **silent-failure class**: a registered MCP client with a statically pinned key and a stale endpoint returns well-formed, entirely plausible results from *a different running goldfinch* — in the 2026-07-31 run, from the operator's own production browser on another port. Nothing about the response shape reveals it. Driving that instance would act on a real personal browsing session. Prefer attaching via `scripts/lib/mcp-client.mjs`'s `connectAutomation()` with a freshly minted key over any statically configured client, per `docs/dev-testing.md`'s attach-don't-pin rule.
- Two persistent jars exist and are distinguishable in the chrome (the run below names them `personal` and `work`); a third, disposable jar exists for the deletion checkpoints so no fixture jar is destroyed mid-run.
- **At least one fixture jar must NOT be the app's default jar**, and the run must know which is which (`jarsGetDefault()`). Checkpoint 8c is undetectable otherwise — see its own note.
- Bookmarks bar **enabled** in Settings (app-wide toggle) — several checkpoints assert bar contents, and the toggle is deliberately *not* jar-scoped.
- No pre-existing bookmarks in either fixture jar (fresh or cleared).
**⚠ SUPERSEDED 2026-08-06 by M15 F3 leg 1 — read this before following the line below.** The sheet is no longer refused at every tier. `resolve.js` now admits **exactly** `readDom` / `readAxTree` / `captureScreenshot` while the sheet's **current** menuType is `bookmarks-overflow` or `bookmark-edit`; `evaluate` / `injectScript` / `openDevTools` remain refused unconditionally. So sheet CONTENT under those two menuTypes is now agent-readable, and steps that only need to *read* it should no longer be marked operator-performed. Steps that **type into, click inside, or dismiss** a sheet are still operator-performed (no admitted op drives input). 
  *(Superseded original:)* - **Operator present**, though less than this spec once assumed. The menu-overlay sheet (popovers, context menus, suggestion dropdowns) refuses ALL automation operations at EVERY tier by design (`automation: secret-sheet`, `src/main/automation/resolve.js`), keyed on the shared sheet `WebContentsView`'s wcId identity rather than on content — so every step that **types into, clicks inside, or dismisses** a sheet is operator-performed. Two further operator-only cases: **`Ctrl`+click** (the `click` primitive builds no modifiers field — only `pressKey` accepts modifiers), and the two absence clauses stills cannot carry (the instantness of a reflow, and the absence of a stale frame on jar switch).
- **Apparatus capabilities this spec previously understated** — all verified in the 2026-07-31 run:
  - `captureWindow` **composites the overlay sheet into the image**, so sheet *contents* (popover fields, context-menu items, suggestion rows) are readable even though the sheet can never be clicked. Trigger with automation, read from the capture.
  - `window.goldfinch.windowCreate()` **is** reachable via `evaluate` on the chrome target — a second window does not need the operator, contrary to earlier wording here.
  - `captureWindow` does not steal focus (confirm via `enumerateWindows`'s `lastFocused` around the call); **`readAxTree` does** — it is foreground-first, so it must never be used to observe a window passively.
  - Confirm dialogs on internal pages (jars page, settings) are ordinary in-page DOM, not the refused sheet — directly clickable.
  - `evaluate` is **refused outright on internal pages**; drive them with `click`/`readDom`/`readAxTree`.
  - `click` coordinates on any **tab** wcId are guest-viewport-relative; subtract the chrome offset (~89px, and internal pages suppress the bar so the same offset applies there) from any Y read off a window screenshot.
  - The `Bookmarks bar` and star accessibility nodes live on the **chrome** target's tree, not the guest tab's — querying the tab returns nothing and looks like a defect.
- **Restart-spanning steps (13–15)**: after each relaunch the automation key is re-minted, so the apparatus must be re-pointed. Whoever drives the run should signal explicitly when the apparatus is back online rather than leaving a judging agent to discover reachability by trial.

## Observables Required

- browser (rendered chrome UI: bookmarks bar, address-bar star, suggestions sheet — measured via the goldfinch MCP chrome target; screenshots primary, accessibility tree secondary)
- browser (per-tab jar identity — measured via the goldfinch MCP tab enumeration, which carries `jarId`)
- browser (per-jar stored bookmark truth — measured via `window.goldfinch.bookmarksGet({ jarId })` evaluated on the chrome target; **await it** — it is a Promise and silently yields `{}` if not awaited, a documented false-negative trap from the Flight 1 runs)
- filesystem / shell (direct `app.db` table inspection via `sqlite3`, with the app **down** — measured via Bash). Required, not optional: post-deletion and orphaned-row assertions are unreachable from the chrome target, because a deleted jar has no tab through which to query it. This mirrors the direct-inspection procedure `sqlite-store-migration` already establishes.

## Steps

| # | Actions | Expected Results |
|---|---------|------------------|
| 1 | Open a tab in the `personal` jar and navigate it to a stable public page (e.g. `https://example.com/`). Bookmark it via the star. Operator names it `Personal Copy` and confirms. | The bar shows exactly one item, `Personal Copy`; the star is filled. Stored truth for `personal` holds one entry; stored truth for `work` is empty. |
| 2 | Open a second tab in the `work` jar, navigate it to the **same** URL, and activate it. | On activation the bar re-renders **empty** and the star returns to **outline** — the same URL is not bookmarked in this jar. No bookmark data from `personal` is visible anywhere in the chrome. |
| 3 | With the `work` tab active, bookmark the page via the star. Operator names it `Work Copy` and confirms. | The bar shows exactly one item, `Work Copy`. Stored truth: `personal` holds one entry titled `Personal Copy`, `work` holds one titled `Work Copy` — same URL, independent entries, neither overwritten. |
| 4 | Switch the active tab back and forth between the `personal` and `work` tabs several times, capturing the bar and star after each switch. | The bar contents and star state track the active tab's jar on **every** switch — `Personal Copy` / filled, then `Work Copy` / filled — with no frame showing the other jar's contents. *(Operator confirms live that no stale contents flash between switches — a null-result observation stills cannot carry.)* |
| 5 | In the `personal` tab, rename its bookmark to `Personal Renamed` via right-click on the bar item. Then switch to the `work` tab and back. | The rename is visible in `personal`. The `work` jar's bar is unchanged by it (`Work Copy`), and its stored entry is byte-identical to before — a mutation in one jar never reaches another. |
| 6 | With the `personal` tab active, type a prefix matching the bookmark into the address bar and observe the suggestions. Then repeat in the `work` tab with the same prefix. | Each jar surfaces **only its own** bookmark row, with the bookmark indicator, and neither surfaces the other's. *(Inverts `bookmarks-omnibox` checkpoint 4, which asserted the superseded app-scoped behavior.)* |
| 7 | Open a burner tab and navigate it to a normal web page. Capture the chrome before and after activating it. | The bookmarks bar is **not rendered at all** in the burner tab, and no star is visible. The guest area reflows to reclaim the bar's height. *(Operator confirms the reflow is instant with no animation — the never-animate-guest-layout invariant; an absence stills cannot prove.)* |
| 8 | With the burner tab active, press `Ctrl+D`, then right-click the page background. Then switch back to the `personal` tab. **Then perform the differential control**: right-click the page background in the `personal` tab, by the same mechanism and at the same coordinates. | Nothing observable happens on `Ctrl+D`: no popover, no star, no bar. The burner's page context menu offers **no** bookmark item at all (not a present-but-inert one) — enumerate every visible item rather than asserting the absence. **The control must show a bookmark item in the `personal` menu.** Without it this step proves nothing: an `Inspect`-only menu is consistent with both "burner suppresses bookmarking" and "this app's page-background menu is sparse everywhere", and the second reading would mean the suppression is untested. Stored truth for every jar is unchanged — no bookmark created anywhere, and **read the burner's own jarId explicitly** to confirm no row exists under it (burner jars have no delete path, so such a row would be permanently unreachable). The `personal` tab's bar and star return intact on switch back. |
| 8b | Activate an internal page (`goldfinch://settings`), capturing before and after. Then return to the `personal` tab. | The bar is suppressed exactly as in the burner case, and the guest reflows; no star. On return to `personal` the bar comes back with identical contents. *(Operator confirms both reflows are instant.)* |
| 8c | **From a fixture tab whose jar is NOT the app's default jar** — read `jarsGetDefault()` first and choose accordingly — middle-click a bar item, then Ctrl+click another (Ctrl+click is operator-performed; the click primitive cannot hold modifiers). Ensure that jar has two bar items; add a second via the bridge as setup if needed. | Both open in background tabs **in the bookmark's own jar** — not the default jar and not a burner — with the originating tab still active. Jar identity confirmed per new tab from the enumeration's `jarId`. **The non-default choice is what makes this checkpoint work at all**: the defect it guards against is a null container resolving to the *current default jar*, so run from the default jar and correct routing is indistinguishable from the bug — it passes vacuously either way. |
| 8d | With the **same URL** still bookmarked independently in both jars (steps 1–3), open that URL in a **background** tab in the `work` jar (site serves a favicon) while a `personal` tab stays active. | The `personal` jar's stored entry for that URL is byte-identical before and after the background tab's favicon arrives — a background tab's favicon writes only onto its **own** jar's same-URL bookmark, never the other jar's. *(Same-URL is the point: with distinct URLs even the unfixed flat-list code would pass this step.)* |
| 9 | Open a second window (via `windowCreate()` on the chrome target — no operator needed) and put a tab in the `personal` jar there, on the bookmarked URL. Remove the bookmark from **window A** — either via the star popover (operator-performed) or issued from window A's chrome, which exercises the same broadcast path. Observe window B without touching it. | Window B's `personal` bar and star update to reflect the removal with zero interaction in window B — cross-window sync still holds **within** a jar. **Observe B with `captureWindow` only**: `readAxTree` is foreground-first and would focus B, destroying the very property under test. Prove non-interference rather than assuming it — check `lastFocused` immediately before and after each capture and confirm it never moved. If B is focused or clicked at any point, the observation is void and the step must be re-run. |
| 10 | Re-bookmark in `personal`. Then, in window A, switch to a `work` tab and mutate the `work` bookmark. Observe window B, still showing `personal`, without touching it. | Window B's bar is **unaffected** by the `work`-jar mutation — the invalidation does not cross jars. *(Negative counterpart to checkpoint 9; a before/after pair on window B carries it.)* |
| 11 | In the disposable third jar, create a bookmark. Then perform a full **identity wipe** of that jar from the jars page (not a delete). | The jar's bookmarks **survive** the wipe — stored truth still holds the entry, and a tab in that jar still shows it on the bar. Cookies/storage/cache/history are cleared as before. |
| 12 | In the same disposable jar, use the **Bookmarks** data-class clear control. | That jar's bookmarks are removed; its bar renders empty. Every other jar's stored bookmarks are untouched. |
| 13 | **Capture every jar's full stored rows first** (all fields, as the before-state). Re-create a bookmark in the disposable jar and leave a live tab open in it, then **delete the jar itself** from the jars page. Quit the app and inspect the `bookmarks` table directly with `sqlite3` (read-only; leave `-wal`/`-shm` alone). | The jar's bookmarks are removed with it. Evidence all three, separately — the first two are the ones with teeth and neither is implied by the third: **(a)** `SELECT DISTINCT jar_id` returns *only* the surviving jars, proving no row was stranded under a burner or any other identifier; **(b)** the surviving jars' rows match the captured before-state at **field level** — id, url, title, icon, position, added_at — not merely by row count; **(c)** no row carries the deleted jar's id, checked by the planted row's specific id as well as by jar filter. *(A weak reading — "both jars still have some rows" — satisfies the sentence while missing row-level corruption entirely.)* Also confirm the jar's open tab closed, as the delete dialog warns. |
| 14 | With the app still down from step 13, relaunch and re-create a jar with the **same name** as the deleted one. Open a tab in it. | **First, save a jars-listing snapshot proving the new jar claimed the same recycled id** — this is mandatory evidence, not a narrated aside. The whole checkpoint is meaningless if the app assigned a fresh or suffixed id instead, and a run that only asserts it in prose has to be judged inconclusive on its own premise. Then: the new jar's bar renders **empty** — a present-but-empty band, the real-jar case, not the absent band of burner/internal tabs — and `bookmarksGet` returns `[]`. The deleted jar's bookmarks do not reappear. *(Spanning a restart, this verifies **storage truth only**: the restart clears any in-memory chrome cache, so the live in-session eviction path — delete then recreate **without** restarting, where a cache failing to evict on `jars-changed` would serve the dead jar's rows — is not exercised here and is pinned by unit test instead. Do not read this checkpoint as evidence for it.)* |
| 15 | Quit the app and relaunch. Inspect both fixture jars. | Each jar's bookmarks return with their own names, order, and icons intact, still separated by jar — jar ownership survives a restart. |

## Out of Scope

- Star state sync across navigation, in-page address changes, and tab switches within a single jar (see `bookmarks-star-sync`)
- Bar rendering detail, overflow menu, tooltips, middle-click, and row-level validation of stored data (see `bookmarks-bar`)
- Suggestion ranking, dedup against history, and the bookmark indicator's appearance (see `bookmarks-omnibox`)
- Drag interactions (Flight 3)
- Jar export — the roadmap goal motivating jar ownership, but not built in this flight
