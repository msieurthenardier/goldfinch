# Behavior Test Run: tab-surface-geometry — 2026-07-08-18-30-51

**Spec**: [tests/behavior/tab-surface-geometry.md](../../tab-surface-geometry.md)
**Status**: pass
**Started**: 2026-07-08T18:30:00-05:00
**Completed**: 2026-07-08T18:34:00-05:00
**Duration**: ~00:04:00
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed)

## Summary
8 / 8 rows passed (row 1 setup + rows 2–8 checkpoints). 0 failed; 0 inconclusive. **First run of this spec (was: never).**

Native WebContentsView surface. Admin MCP surface on 8899. Chrome wcId 1, guest wcId 2 (ticking liveness fixture `tests/behavior/fixtures/menu-overlay/` served at `http://127.0.0.1:8822/`), menu-overlay sheet wcId 3, find overlay wcId 11.

**Capture path — OS-grab (authoritative).** `captureWindow` composited the menu-overlay sheet (row 3) AND the find-overlay view (row 7) in the same run — so the OS-grab (`desktopCapturer`) path is active and overlay presence/absence checks are authoritative (absence-authoritativeness rule satisfied). No WSLg-fallback degradation observed.

## Step Results

### Step 1 — PASS (setup)
- **Actions**: `getChromeTarget()` → wcId 1; opened the ticking fixture in the Default jar (guest wcId 2); `captureWindow` baseline.
- **Raw state**: baseline frame shows the guest full-height with the ticking clock/counter; no menu.
- **Verdict**: pass (setup).

### Step 2 — PASS
- **Actions**: second `captureWindow` ~2.5s after step 1; compared the ticking region.
- **Raw state**: grab A clock `18:31:05` / `ticks: 15`; grab B clock `18:31:07` / `ticks: 17` (md5 differ).
- **Expected**: guest content composited in `#webviews`; ticking region differs between grabs (live at baseline).
- **Verdict**: pass — counter advanced; guest live.
- **Evidence**: [step1-baseline-a.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step1-baseline-a.png), [step2-baseline-b.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step2-baseline-b.png)

### Step 3 — PASS [render-correct]
- **Actions**: opened the kebab (⋮) via `click(1,1372,63)`; two `captureWindow` grabs ~2.2s apart; read `aria-expanded` on the kebab trigger.
- **Raw state**: kebab menu composited **above** the guest at the top-right, flush at the guest's top edge; guest visible/live/full-height around it; grab A `18:31:35`/`ticks:45`, grab B differs (md5) — guest ticking under the menu; kebab `aria-expanded="true"`.
- **Expected**: menu composited above a live full-height guest; ticking differs between grabs; no push-down/blanking; kebab `aria-expanded="true"`.
- **Verdict**: pass.
- **Evidence**: [step3-menu-a.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step3-menu-a.png)

### Step 4 — PASS
- **Actions**: dismissed via `pressKey(sheetWcId=3, 'Escape')`; `captureWindow`; read kebab `aria-expanded`.
- **Raw state**: menu gone from pixels; guest live at full bounds (`18:31:59`/`ticks:69`); kebab `aria-expanded="false"`.
- **Expected**: menu gone; guest still live full bounds; `aria-expanded="false"`.
- **Verdict**: pass.
- **Evidence**: [step4-dismissed.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step4-dismissed.png)

### Step 5 — PASS [render-correct]
- **Actions**: opened the media side panel (Ctrl+M); `captureWindow`; read `#webviews` rect before/after.
- **Raw state**: `#webviews` width reflowed **1398 → 1038** (guest slot narrowed by the panel width, x=1..1039); the panel tiles the content area beside the guest with no overlap/gap/dead-band; the guest content re-laid-out to the narrower width (internal horizontal scrollbar appeared). Guest still ticking (`18:32:25`).
- **Expected**: guest reflows to the region beside the panel; no overlap/gap/clip; content re-lays-out narrower.
- **Verdict**: pass.
- **Evidence**: [step5-panel-open.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step5-panel-open.png)

### Step 6 — PASS
- **Actions**: closed the panel (Ctrl+M); `captureWindow`; read `#webviews` rect.
- **Raw state**: `#webviews` width restored to **1398** (full); subsequent full-guest renders (steps 7/8) show no residual inset or dead band.
- **Expected**: guest restores to full `#webviews` region; no residual inset / dead band.
- **Verdict**: pass.

### Step 7 — PASS [render-correct]
- **Actions**: recorded guest bounds; opened find (`pressKey(guestWcId=2, 'f', ['control'])`); `captureWindow`; re-read guest bounds.
- **Raw state**: `#webviews` rect **identical** before/after (`{x:1,y:89,width:1398,height:810}`); the `[ Find… ] ↑ ↓ ✕` bar is composited **over** the guest at the top-right; guest content at the same position (not pushed/inset), not clipped behind the bar (clock `18:33:01`/`ticks:131` full-height).
- **Expected**: find bar floats over a FULL-bounds guest; guest-bounds probe equals the pre-open probe (never inset); bar visible, guest not broken/clipped.
- **Verdict**: pass.
- **Evidence**: [step7-find-open.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step7-find-open.png)

### Step 8 — PASS
- **Actions**: closed find (`pressKey(findOverlayWcId=11, 'Escape')`); `captureWindow`; re-read guest bounds.
- **Raw state**: find bar gone from pixels; `#webviews` rect **unchanged** (`width:1398`); guest live full bounds (`18:33:44`/`ticks:174`).
- **Expected**: find bar gone; guest bounds UNCHANGED from steps 1/7 (never shrank).
- **Verdict**: pass.
- **Evidence**: [step8-find-closed.png](../../../../tmp/behavior-tests/goldfinch/tab-surface-geometry/2026-07-08-18-30-51/step8-find-closed.png)

## Orchestrator Notes
- Menu dismissal used `pressKey` on the sheet (wcId 3); find dismissal used `pressKey` on the find overlay (wcId 11, discovered by a background-tab-safe probe walk — `readDom` returned `find-overlay.html`). Guest-bounds "probe" used the chrome `#webviews` `getBoundingClientRect()` (the slot the active guest view is sized to) plus pixel corroboration.
- Liveness measured by md5 of successive full-window PNGs plus a direct visual read of the ticking counter across grabs (no pixel-diff tooling on this rig; the counter advancing is the authoritative tell).

## Closing Summaries

### Executor closing
First-ever run of this spec, full pass. The three F3/F7/F8 geometry+compositing behaviors hold on the WebContentsView surface: (1) the kebab menu composites above a LIVE full-height guest via the menu-overlay sheet (no freeze/hide — the ticking counter kept advancing under the open menu); (2) the media panel reflows the guest to the beside-region (1398→1038) and restores it (→1398) with clean tiling; (3) the find bar floats over a full-bounds guest with the `#webviews` rect byte-identical open vs closed.

### Validator closing
Clean render-correct evidence. The retired freeze-frame behavior is confirmed gone — menu-open-≠-guest-hidden holds by the advancing ticks under the menu. No WSLg sub-frame blip rose to a failure. OS-grab path composited both overlays, so absence checks were authoritative. Spec is accurate to the current surface; recommend promotion `draft → active`.

## Operator Notes
Verdict PASS (8/8). Recommend promoting this spec to `active` — it ran clean on the intended surface.
