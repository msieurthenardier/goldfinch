# Behavior Test Run: devtools-cdp-conflict — 2026-07-08-19-03-22

**Spec**: [tests/behavior/devtools-cdp-conflict.md](../../devtools-cdp-conflict.md)
**Status**: partial (recorded finding — WSLg inconclusive-tolerated per DD8; not a pass/fail gate)
**Started**: 2026-07-08T00:03:22Z
**Completed**: 2026-07-08T00:03:28Z
**Duration**: 00:00:06
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — raw MCP result payloads persisted)

## Summary
This is a **recorded finding**, not a pass/fail acceptance gate (per the spec's Out of Scope). **Observation on WSLg:** with Chromium DevTools genuinely open on tab W via the **human affordance** (`toggleDevtools` → `wc.openDevTools({mode:'detach'})`, with `wc.isDevToolsOpened()===true` confirmed), the MCP `readAxTree(W)` **still returned a valid AXNode array — NO `attach-failed` refusal, no error** — and the CDP-free ops (`evaluate`, `injectScript`) kept working. Per **DD8 (macOS-authoritative)** this WSLg result **does not settle** the finding either way: on WSLg the detached DevTools window/CDP client does not cleanly establish the competing single-client contention. The definitive open→refused→close→restored cycle remains a macOS observation.

Confound-free venue confirmed: launched via `dev:automation` with **no `--remote-debugging-port`** (`:9222` NOT listening); DevTools opened via the human affordance, **not** the MCP `openDevTools`/`closeDevTools` tools (which are the prior M03 vector).

Apparatus: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899`, admin key on the SDK transport; DevTools driven via the admin chrome target (`getChromeTarget()` → chrome wcId 1, `evaluate(chrome, window.goldfinch.toggleDevtools({webContentsId:W}))`).

## Step Results

### Step 1 — PASS (venue confirmed)
- **Actions taken**: `ss -ltn | grep 9222`; `initialize`; `tools/list`; `getChromeTarget()`.
- **Raw state**: `:9222` **NOT LISTENING** (confound-free); 27 tools incl `readAxTree`; `getChromeTarget()` → numeric chrome `wcId 1` (the human-affordance drive path is available).
- **Expected**: no `--remote-debugging-port`; `tools/list` ≥ 21 incl `readAxTree`; numeric chrome wcId.
- **Verdict**: pass — venue is confound-free and the human affordance is drivable.
- **Evidence**: [step1.json](./2026-07-08-19-03-22/step1.json)

### Step 2 — PASS (baseline)
- **Actions taken**: opened web tab W (=17); `readAxTree(W)` with DevTools closed.
- **Raw state**: JSON-text **array**, len 15; not `isError`; not the refusal.
- **Expected**: baseline a11y read succeeds absent contention.
- **Verdict**: pass — so any later refusal would be attributable to DevTools.
- **Evidence**: [step2-baseline.json](./2026-07-08-19-03-22/step2-baseline.json)

### Step 3 — RECORDED (WSLg: no conflict; inconclusive-tolerated)
- **Actions taken**: opened DevTools on W via the **human affordance** (`evaluate(chrome, window.goldfinch.toggleDevtools({webContentsId:17}))`); polled `window.goldfinch.isDevtoolsOpen({webContentsId:17})` until `true`; then `readAxTree(W)`; also `evaluate(W,'1+1')` and `injectScript(W,…)`.
- **Raw state**: `isDevtoolsOpen(W) === "true"` (DevTools genuinely open). `readAxTree(W)` → **array(len=15)** (outcome (b): successful AXNode array — NO conflict). `evaluate(W,'1+1')` → `2` (not isError); `injectScript(W,…)` → `{"ok":true}` (not isError).
- **Expected**: RECORD the outcome — one of (a) `attach-failed` refusal, (b) success array (no conflict), (c) `isError`; also confirm CDP-free ops keep working. Never "must return attach-failed"; on WSLg never settle the finding.
- **Verdict**: recorded — **outcome (b) on WSLg**: DevTools open (confirmed) did NOT induce `attach-failed`; the in-process debugger a11y read succeeded, and the CDP-free ops were unaffected (DD7). Per DD8 this is inconclusive-tolerated (macOS-authoritative) — the CDP single-client contention did not genuinely establish on WSLg, matching the prior-run caveat.
- **Evidence**: [step3-devtools-open.json](./2026-07-08-19-03-22/step3-devtools-open.json)

### Step 4 — RECORDED (no-op restoration)
- **Actions taken**: closed DevTools via the human affordance (`toggleDevtools` again); polled `isDevtoolsOpen(W)` until `false`; `readAxTree(W)`.
- **Raw state**: `isDevtoolsOpen(W) === "false"`; `readAxTree(W)` → array(len=15), not isError.
- **Expected**: a11y read returns an array again (success restored); RECORD if not.
- **Verdict**: recorded — success both before and after; since Step 3 established no conflict, this is a no-op restoration (as anticipated for WSLg).
- **Evidence**: [step4-devtools-closed.json](./2026-07-08-19-03-22/step4-devtools-closed.json)

## Orchestrator Notes
Re-staged onto the M04-Flight-3 human affordance (F12/`#toggle-devtools` → `toggle-devtools` IPC → `wc.openDevTools({mode:'detach'})`), driven here via the admin chrome target's `toggleDevtools` global — the same IPC a human F12 triggers, NOT the MCP `openDevTools` tool. Improvement over the prior run: `wc.isDevToolsOpened()===true` was **positively confirmed** before the a11y read (open is async on WSLg — it settles ~1.5s after the toggle), so this run establishes that DevTools was genuinely open yet `readAxTree` still succeeded. Still WSLg-inconclusive per DD8: whether a competing CDP client genuinely bound cannot be settled here. Recommend the macOS-authoritative run to close the mission Open Question.

## Closing Summaries

### Executor closing
Confound-free venue (no `:9222`). Positively confirmed DevTools open state (unlike the prior run's ambiguity), and still observed `readAxTree` success — so on WSLg the open DevTools window does not contend for the CDP client. CDP-free ops (evaluate/injectScript) unaffected throughout, consistent with DD7.

### Validator closing
Correctly recorded, not gated. The stronger evidence this run adds is the confirmed `isDevToolsOpened()===true` prior to the successful a11y read; the definitive refusal cycle stays macOS-authoritative.

## Operator Notes
Run on WSLg. This is a recorded finding for the flight log / mission Open-Question closure; not a flight-acceptance gate.
