# Behavior Test Run: devtools-cdp-conflict — 2026-06-17-16-25-30

**Spec**: [tests/behavior/devtools-cdp-conflict.md](../devtools-cdp-conflict.md)
**Status**: partial
**Started**: 2026-06-17T16:25:16
**Completed**: 2026-06-17T16:26:25
**Duration**: ~00:01:09
**Executor**: FD-driven (machine-read evidence; mission behavior-test standard — no Witnessed crew elected)
**Validator**: FD-driven (recorded-finding spec; no pass/fail gate)

## Summary
Recorded finding captured. The run executed **confound-free** (`dev:automation`, **no `--remote-debugging-port`**; `:9222` not listening; MCP on `:52347`; `tools/list` = **21** tools incl. `readAxTree`/`openDevTools`/`closeDevTools`). The DevTools-open affordance (leg 2's `openDevTools`/`closeDevTools` MCP tools) is live and returns `{"ok":true}`. **The expected `attach-failed` conflict did NOT reproduce**: `readAxTree(W)` returned a valid AXNode array (len 13) at baseline, **while DevTools was "open"**, and after close — success throughout, no `debugger-unavailable`/`attach-failed`, no `isError`. **Caveat (load-bearing for interpretation):** under WSLg the detached DevTools window did not cleanly materialize (the app log shows `blink.mojom.WidgetHost`/`blink.mojom.Widget` "Message rejected" errors at the open time), so it is **inconclusive whether a competing CDP client was genuinely established** on the contents. Net: the API-level affordance works; the live `attach-failed` observation is **not settled** by this run (WSLg-limited) and remains **unit-tested-only**.

## Step Results

### Step 1 — PASS (venue confound-free)
- **Actions taken**: Launched `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=52347 npm run dev:automation`. Confirmed `:9222` NOT listening; `:52347` listening. Connected admin-keyed MCP client; `initialize`; `tools/list`.
- **Raw state**: `:9222` clear; `toolCount=21`; `hasDevToolsTools=true` (`readAxTree`,`openDevTools`,`closeDevTools` all present).
- **Expected**: no `--remote-debugging-port`; `tools/list` ≥ 21 incl. the three tools; else halt.
- **Verdict**: pass — confound-free venue confirmed.

### Step 2 — PASS (baseline read succeeds)
- **Actions taken**: `openTab({url:'http://127.0.0.1:8044/'})` (admin → default container) → wcId **W=3**; waited 2s for load; `readAxTree(3)` with DevTools closed.
- **Raw state**: baseline `readAxTree(3)` → `array(len=13)`, RootWebArea name "DTC Fixture", url `http://127.0.0.1:8044/`. Not `isError`, not a refusal.
- **Expected**: baseline returns a JSON-text array (absent contention, the a11y read works).
- **Verdict**: pass.

### Step 3 — RECORDED (no conflict observed; inconclusive on DevTools attach)
- **Actions taken**: `openDevTools(3)` over MCP → `{"ok":true}`; waited 1.5s; `readAxTree(3)` over MCP.
- **Raw state**: `openDevTools` → `{"ok":true}`. `readAxTree(3)` under DevTools → **`array(len=13)`** (success), NOT `attach-failed`, NOT `isError`. App log: `blink.mojom.WidgetHost`/`Widget` "Message rejected" errors at the open time (DevTools detached window did not cleanly render under WSLg).
- **Expected (spec)**: "RECORD the outcome — never must-return-attach-failed." Outcome = (b) successful AXNode array (coexistence) **with the caveat** that DevTools may not have fully established its CDP client (WSLg widget failure), so this is not a clean proof of coexistence.
- **Verdict**: recorded. The `attach-failed` branch was **not** triggered live; whether that is true coexistence or a non-materialized DevTools window is unresolved in this environment.

### Step 4 — PASS (read still succeeds after close)
- **Actions taken**: `closeDevTools(3)` over MCP → `{"ok":true}`; waited 1.5s; `readAxTree(3)`.
- **Raw state**: `closeDevTools` → `{"ok":true}`; `readAxTree(3)` → `array(len=13)`.
- **Expected**: array again (success). Met (it never stopped succeeding).
- **Verdict**: pass — no lingering contention.

## Orchestrator Notes
- FD-driven per the mission's accepted behavior-test standard (machine-read evidence; the two-agent Witnessed pattern was not elected for this recorded-finding spec). Driver: a throwaway `node` script over `scripts/lib/mcp-client.mjs` (admin key), since deleted.
- The recorded-finding **objective is met** (the spec asks only that the outcome be recorded). The **substantive result deviates from OQ4's "confirmed expected attach-failed"**: live, the conflict did not manifest. The honest read is **inconclusive**, dominated by the WSLg detached-DevTools-window materialization failure — a clean `attach-failed` observation needs a non-WSLg display (or a docked DevTools mode that reliably materializes). The `attach-failed` branch stays **unit-tested-only**.
- Evidence (driver stdout `DTC_RESULT` JSON, app log excerpt) captured to the ephemeral `/tmp/behavior-tests/goldfinch/devtools-cdp-conflict/2026-06-17-16-25-30/` path conceptually; not committed (per Evidence Handling). The key values are inlined above.

## Closing Summaries
### Executor closing
The affordance works end-to-end at the MCP layer (`openDevTools`/`closeDevTools` → `{"ok":true}`, jar/admin-gated, no `:9222`). The environment (WSLg) could not reliably render the detached DevTools window, so the CDP-contention condition the spec exists to observe was not dependably staged.

### Validator closing
Spec did its job as a *recorded finding* — every step's "record the outcome" expectation was satisfiable and satisfied. But the finding is **inconclusive on the substantive question** (does DevTools-open force `attach-failed`?). Recommend the spec stay archived with this run as the record, and that any future definitive observation be taken on a non-WSLg display. The `attach-failed` path remains covered by unit tests only.

## Operator Notes
_(pending operator review at the HAT/verify leg or debrief)_
