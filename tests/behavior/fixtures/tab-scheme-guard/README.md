# tab-scheme-guard fixture

Deliberately-hostile test fixture for the `tab-scheme-guard` behavior test.

## Serve

Run from this directory:

```
python3 -m http.server
```

The trigger page is then reachable at **http://127.0.0.1:8000/**.
