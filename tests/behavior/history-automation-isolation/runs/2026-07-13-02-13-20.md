# Behavior Test Run: history-automation-isolation — 2026-07-13-02-13-20

**Spec**: [history-automation-isolation.md](../../history-automation-isolation.md)
**Status**: pass
**Started**: 2026-07-13T02:13:20Z
**Completed**: 2026-07-13T03:20:00Z (approx)
**Duration**: ~01:05 (two launches)
**Executor**: live Sonnet sub-agent (SendMessage continuation)
**Validator**: live Sonnet sub-agent (SendMessage continuation)
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/history-automation-isolation/2026-07-13-02-13-20/` (30 files); re-derivable by re-running the spec.

## Summary

**7 / 7 checkpoints passed** (first run). The live key→jar binding — the
one property the unit suites cannot pin — held end-to-end across two
launches, against a deliberately retained ~50k-row foreign population
(isolation-despite-scale). Every verdict was grounded in the Validator's
OWN fresh readOnly DB reads (full-row audits, not spot checks) and, for
the refusal/success contrast, its own independently built MCP client.

## Checkpoint Results

### Checkpoint 1 — PASS
Drift check honored (resolved default `rename-test`, live-read); run-1
launch (jar key only; adminKey correctly null); three Auklet marker
visits staged via the live recorder, DB-confirmed in the default jar;
the work jar's 20 pre-existing zebrafinch rows adopted (FD-approved) as
run-1's cross-checked foreign markers.

### Checkpoint 2 — PASS
Jar-key `getHistory` (no jarId, limit 50) → `{ jarId: 'rename-test',
visits: 50 }`; ALL 50 rows independently DB-audited to
`jar_id='rename-test'` (zero mismatches, incl. deep seeded-population
rows); Auklet markers top-3; zero foreign hits.

### Checkpoint 3 — PASS
Search `auklet` → exactly the complete 3-row own-jar marker set; search
`zebraf` → clean EMPTY success while the 20 zebraf rows provably existed
in `work` at judgment time (foreign data exists + query live + zero
leakage — the full negative-evidence bar).

### Checkpoint 4 — PASS
Jar key + `jarId:'work'` → `isError` with the exact
`automation: out-of-jar` code and NO data — reproduced live by the
Validator's own client (`{ isError: true, content: [one text block] }`),
against a foreign jar proven to hold rows.

### Checkpoint 5 — PASS
Explicit OWN jarId → success; visit-id sequence byte-identical to
checkpoint 2's implicit call. Contrasted with checkpoint 4 (same client,
same key), pinning the discrimination to the jarId VALUE.

### Checkpoint 6 — PASS
Run 2 (mint+admin) minted both keys; Petrel markers staged in `work` via
admin `openTab({jarId:'work'})` (DB-confirmed). Admin read of `work` →
exactly its 22-row population (Petrel + zebrafinch, zero rename-test
leakage — all 22 rows audited); admin read of `rename-test` → Auklet
present, zero work rows. History persisted across the relaunch (run-1
rows returned in run 2). Deferred step-3 re-check discharged: run-2 JAR
key, query `petrel` → EMPTY with DB-proven foreign existence.

### Checkpoint 7 — PASS
Admin, no jarId → `automation: bad-args — jarId required` (exact);
unknown jarId → `automation: unknown-jar` (exact); no data with either.
With checkpoint 6, the three admin arg shapes produce three distinct
outcomes — the codes discriminate by cause.

## Orchestrator Notes

- Live two-agent mode, cache-cold, Sonnet crew. Two launches per the
  spec (jar-key run; admin run). FD-approved staging order: Auklet run 1
  (jar's own navigation), Petrel run 2 (admin openTab) with the step-3
  foreign-search re-check discharged post-staging.
- **Deviations ruled by the Validator (both non-blocking, recorded)**:
  (1) run teardowns were SIGTERM-based — `appQuit` is not an automation
  TOOL (raw `unknown-tool` refusal captured) and the chrome-evaluate quit
  path is admin-tier, unavailable to run 1's jar-only key by the very
  confinement under test; future spec runs should pre-declare jar-key-run
  teardown mechanics. (2) The Executor tore down run 2 BEFORE the
  checkpoint-6/7 verdicts — not evidence loss (raw captures complete and
  DB-corroborated) but it removed the Validator's live-reproduction
  option for the admin run; sequencing-discipline note for the debrief.
- Teardown confirmed clean: no processes left, ports 49707/8000
  released; history.db touched only through the live recorder.

## Closing Summaries

### Executor closing
All 7 steps, no exceptions. Notables: `appQuit` absent from the 28-tool
surface (evidence preserved); each boot's initial google.com load adds
+1 default-jar row (delta-accounted 50012→50016→50017); staging order as
approved; one teardown-script wrinkle (pkill matched its own shell,
exit 144 — state re-verified, fixture server then killed by pid).

### Validator closing
7/7 with full independent verification: fresh DB reads for every
verdict (complete-row audits), own MCP client reproductions for the
refusal/success contrast. Isolation negatives all met the foreign-data-
exists + query-live + zero-leakage bar; markers/deltas only. Debrief
observations: delta narratives should enumerate boot-traffic rows; the
out-of-jar message discloses only the caller's own binding (acceptable,
noted); the optional paging variant was not run. Recommend PASS +
spec Last Run update.

## Operator Notes

*(autonomous run — none)*
