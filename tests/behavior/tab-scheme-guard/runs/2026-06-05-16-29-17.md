# Behavior Test Run: tab-scheme-guard — 2026-06-05-16-29-17

**Spec**: [tests/behavior/tab-scheme-guard.md](../../tab-scheme-guard.md)
**Status**: partial
**Started**: 2026-06-05T16:29:17Z
**Completed**: 2026-06-05T16:42Z
**Duration**: ~00:13:00
**Executor**: a084d76e5d4c20dfc
**Validator**: a4997de70f9075124

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-06-05-16-29-17/` during the run; re-derivable by re-running the spec. Files: `step-a/b/c/d/f-targets.txt`, `final-renderer.png`.

## Summary

5 / 6 judged checkpoints passed (Step A = setup). 0 failed; 1 inconclusive (media vector — structurally unreachable). Every hostile-scheme vector that could be exercised was blocked in the live app, and the legitimate-https control opened normally.

## Step Results

### Step A — SETUP (confirmed)
- **Actions taken**: On the renderer page target, called `createTab('http://127.0.0.1:8000/')` to open the fixture.
- **Raw state**: webview target `http://127.0.0.1:8000/` "Tab Scheme Guard Trigger Page"; body had `trigger-page-loaded`; address bar `http://127.0.0.1:8000/`; 2 tabs.
- **Verdict**: setup good — valid starting state for B–F.
- **Evidence**: `step-a-targets.txt`

### Step B — window.open('file:///etc/passwd') — PASS
- **Raw state**: no `file://` target appeared; webview list unchanged `["https://www.google.com/","http://127.0.0.1:8000/"]`; no new tab.
- **Expected**: no tab renders /etc/passwd; no webview has a file: URL; address bar never file://.
- **Verdict**: PASS — file: `window.open` fully blocked.
- **Evidence**: `step-b-targets.txt`

### Step C — window.open('javascript:…') then window.open('data:…') — PASS
- **Raw state**: one new `about:blank` webview appeared (empty: `{url:"about:blank",title:"",body:""}`); no target became `javascript:`/`data:`; no title "pwned"; no "injected" content.
- **Expected**: neither loads — no javascript:/data: URL on any webview, no "pwned"/"injected".
- **Verdict**: PASS — the dangerous schemes were dropped to a benign empty `about:blank`; no payload navigated or rendered.
- **Evidence**: `step-c-targets.txt`

### Step D — in-page `window.location='file:///etc/passwd'` (critical will-navigate vector) — PASS
- **Raw state**: fixture target STILL at `http://127.0.0.1:8000/` (title unchanged); zero `file:*passwd` targets; webview list unchanged. The fixture did not navigate away.
- **Expected**: active tab does NOT render /etc/passwd; fixture webview url not file://; positive anchor — remains on the original trigger-page URL.
- **Verdict**: PASS — the main-process `will-navigate` guard cancelled the in-page file: navigation cleanly (the vector the `createTab` gate alone would miss).
- **Evidence**: `step-d-targets.txt`

### Step E — media-open of a crafted `file:` media item — INCONCLUSIVE
- **Raw state**: no `openMediaAsTab` global; media panel reported "No media found"; the fixture `<video src=file:///etc/passwd>` had `currentSrc=""`, `error=4` (MEDIA_ELEMENT_ERROR — never loaded). No file: tab opened.
- **Expected**: media item does not open a tab with local file contents; no webview navigates to file:.
- **Verdict**: INCONCLUSIVE — the vector was not exercisable: `file:` media never enters the media catalog (consistent with `webview-preload.js:68` excluding non-fetchable sources), so there is no media-open path to drive. Absence of a file: tab here is the absence of a triggering action, not proof of interception. **Structurally low-risk**: since file: media is never cataloged, the media-open file: scenario isn't reachable in practice; the media-open path that IS reachable (http(s) media) flows through the same `createTab` guard proven in Steps B–D.
- **Evidence**: (media state captured in Executor report; bracketed by `step-d`/`step-f` dumps)

### Step F — CONTROL window.open('https://example.com/') — PASS
- **Raw state**: new webview `https://example.com/` "Example Domain"; address bar `https://example.com/`; 4 tabs; screenshot shows the active Example Domain tab with real page body.
- **Expected**: a new tab opens and renders example.com; address bar https://example.com/.
- **Verdict**: PASS — legitimate https `window.open` opens normally; no over-blocking.
- **Evidence**: `step-f-targets.txt`, `final-renderer.png`

## Orchestrator Notes

- **Mode**: consolidated single-pass Witnessed (one Executor performed all vectors against the live app via raw CDP; one independent Validator judged all from the Executor's raw state). Used instead of per-step `SendMessage` live continuation, which was not reliably available in this orchestration context. The act/judge separation — the Witnessed integrity property — was preserved (distinct agents `a084…` and `a499…`).
- **Apparatus**: raw Chrome DevTools Protocol over a native Node WebSocket against the already-running Electron instance at `:9222` (Goldfinch 0.3.0 / Electron 33.4.11). chrome-devtools MCP was not used because it would launch its own browser rather than attach to the app's existing CDP endpoint. Guest `<webview>` screenshots were not retrievable via CDP (connection hung); target `/json` dumps + the renderer screenshot were the evidence.
- **Promotion decision**: spec left `draft` (not promoted to `active`) — the run is a **partial** (one inconclusive), and integrity-wise a partial should not be promoted as if all-green. The inconclusive (Step E) is a spec-quality issue, not a guard failure: it tests a structurally-unreachable scenario. **Follow-up**: refine Step 6 of the spec (drop/replace the file:-media-open vector, which can't be cataloged; optionally verify the reachable http(s) media-open path rejects a crafted dangerous URL) and re-run to promote.

## Closing Summaries

### Executor closing
Drove the live Electron app via raw CDP without launching a new browser. All six action sequences executed; raw target/url/title state captured after each. Only friction: guest-webview screenshots hung over CDP in this Electron setup (renderer-page screenshot worked).

### Validator closing
Every exercised dangerous-scheme vector (B/C/D) was blocked and the control (F) passed — a strong positive for the F1 enforcement, now proven in the running app (not just unit-tested). Step E is honestly inconclusive: no live media-open of a file: source was triggerable, so the guard's behavior on that path was not demonstrated. The spec's Step 6 should be refined to test a reachable media-open path.

## Operator Notes

Core result: Flight 1's F1 hostile-page URL guard is **verified in the live app** for the `window.open` and in-page `will-navigate` vectors (the genuinely hostile-page-reachable surface), with no over-blocking of legitimate navigation. The media-open file: vector is structurally unreachable and is a spec-refinement follow-up, not a defect.
