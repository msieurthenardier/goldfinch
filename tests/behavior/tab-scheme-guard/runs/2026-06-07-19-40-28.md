# Behavior Test Run: tab-scheme-guard — 2026-06-07-19-40-28

**Spec**: [tests/behavior/tab-scheme-guard.md](../../tab-scheme-guard.md)
**Status**: pass
**Started**: 2026-06-07T19:40:28Z
**Completed**: 2026-06-07T19:50Z
**Duration**: ~00:10:00
**Executor**: a2f1c463caeb3f535
**Validator**: a902fc96d815b018a
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-06-07-19-40-28/` during the run (step-{3,4,6,7,8,10,12,13}.png); re-derivable by re-running the spec.

## Summary
**13 / 13 checkpoints passed.** 0 failed; 0 inconclusive. Every hostile vector — `file:`/`javascript:`/`data:` (window.open, self-navigation, media-open) and the four `goldfinch://` internal-scheme spoofs (window.open, self-navigation, iframe embed, cross-origin fetch) — was blocked. All three positive controls passed: the `https:` control opened example.com; the trusted kebab→Settings path opened `goldfinch://settings` on the `goldfinch-internal` partition and rendered the stub; and an app-path reload re-rendered the stub with the URL unchanged.

This run **extends the prior partial run** (2026-06-05, 5/1 inconclusive) to full coverage of the Flight-4 internal-scheme boundary (steps 8–13, new) and resolves the prior Step-6 inconclusive.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: `curl` CDP `/json` + the trigger page.
- **Raw state**: CDP returned a Goldfinch window (page target, index.html); trigger `http://127.0.0.1:8000/` HTTP 200 with the hostile fixture HTML.
- **Verdict**: pass — both responsive, no halt.

### Checkpoint 2 — PASS
- **Actions taken**: opened a tab to `http://127.0.0.1:8000/`, hard-reloaded once.
- **Raw state**: single webview src=`http://127.0.0.1:8000/` (persist:goldfinch); address bar = `http://127.0.0.1:8000/`; marker `trigger-page-loaded`.
- **Verdict**: pass — trigger page rendered in active tab with correct address bar.

### Checkpoint 3 — PASS (file: window.open)
- **Raw state**: tabs stayed 1; no `<webview>` with file: src; address bar unchanged; 0 etc/passwd CDP targets (the one file: target is the app's own `index.html` shell, not a tab).
- **Verdict**: pass — all three negative criteria met. Evidence: `step-3.png`.

### Checkpoint 4 — PASS (javascript:/data: window.open)
- **Raw state**: a new tab opened but is `about:blank` (empty title/body); trigger title stayed `Tab Scheme Guard Trigger Page` (NOT `pwned`); no webview carried a javascript:/data: URL; nothing rendered `injected`.
- **Verdict**: pass — dangerous schemes neutralized to a blank tab. Evidence: `step-4.png`.
- **Validator notes**: a benign stray `about:blank` tab was spawned; the spec's criteria permit it (they forbid the dangerous URL/content, which a blank tab satisfies). If "no tab at all" is the intent, the spec should say so.

### Checkpoint 5 — PASS (file: self-navigation)
- **Raw state**: active tab unchanged on `http://127.0.0.1:8000/`; guest still on trigger; 0 etc/passwd targets. `will-navigate` cancelled the file: self-nav.
- **Verdict**: pass.

### Checkpoint 6 — PASS (media open-as-tab)
- **Raw state**: the `file:///etc/passwd` media item IS cataloged and the "Pop out to new tab" action is present + clickable; after clicking, tabs stayed 1, no new tab, no file: navigation, 0 etc/passwd targets.
- **Verdict**: pass — media-open opened no local-file tab. Evidence: `step-6.png`.
- **Validator notes**: the vector is now **reachable-but-guarded** (popout routes through `createTab` on the untrusted branch → `isSafeTabUrl` rejects file:), a stronger result than the spec's prior "MAY be structurally unreachable" note. **Spec update recommended.**

### Checkpoint 7 — PASS (https: control)
- **Raw state**: new active tab url/src=`https://example.com/`, `<h1>`=`Example Domain`; address bar=`https://example.com/`.
- **Verdict**: pass — the guard does not blanket-block legitimate `window.open`. Evidence: `step-7.png`.

### Checkpoint 8 — PASS (goldfinch:// window.open)
- **Raw state**: no new tab; no webview carries goldfinch:// src; both webviews remain persist:goldfinch (none on goldfinch-internal); no goldfinch:// CDP target; no Settings tab.
- **Verdict**: pass — untrusted goldfinch:// open blocked at `createTab`/`isSafeTabUrl`. Evidence: `step-8.png`.

### Checkpoint 9 — PASS (goldfinch:// self-navigation)
- **Raw state**: active tab unchanged on the http trigger; no goldfinch:// navigation.
- **Verdict**: pass — session-aware `will-navigate` rejected goldfinch:// from the web origin.

### Checkpoint 10 — PASS (goldfinch:// iframe embed)
- **Raw state**: `#goldfinch-embed-result` = "iframe injected (src=goldfinch://settings)" (button fired); iframe present with src=`goldfinch://settings/` but `contentDocument.body.innerText` is empty — the stub did NOT render inside it; no goldfinch:// CDP target created by the embed.
- **Verdict**: pass — embed neutralized (CSP `frame-ancestors 'none'` + web session has no handler). Evidence: `step-10.png`.

### Checkpoint 11 — PASS (goldfinch:// cross-origin fetch)
- **Raw state**: `#goldfinch-fetch-result` = "rejected: TypeError: Failed to fetch" — the fetch rejected rather than resolving with the stub body.
- **Verdict**: pass — scheme not fetchable from a web origin.

### Checkpoint 12 — PASS (trusted open, positive)
- **Actions taken**: drove the **real** kebab (⋮) → **Settings** click (`#kebab-settings`, whose handler calls `createTab('goldfinch://settings', null, { trusted: true })`).
- **Raw state**: new active tab url/src=`goldfinch://settings/` on partition `goldfinch-internal`, title `Settings — Goldfinch`; address bar=`goldfinch://settings/`; guest innerText "Settings\n\nComing soon."; tab in the strip with a working close button.
- **Verdict**: pass — the trusted path opens and renders the stub. Evidence: `step-12.png`.

### Checkpoint 13 — PASS (reload, positive)
- **Actions taken**: `activeTab().webview.reload()` (the app reload path, not a raw CDP Page.reload).
- **Raw state**: after reload, address bar=`goldfinch://settings/`, partition `goldfinch-internal`, guest re-rendered "Settings\n\nComing soon."; URL/scheme unchanged.
- **Verdict**: pass — internal tab reloads "like any other tab". Evidence: `step-13.png`.

## Orchestrator Notes
- **Mode**: single-pass Executor + independent single-pass Validator (the project's established Witnessed realization, Flight 1–3 precedent) — `SendMessage` is absent this session, so the live re-spawn-per-checkpoint loop was not used. The Executor performed all 13 steps reporting per-step raw state; the Validator judged all 13 from the structured raw state (executor advisory notes filtered out per the separation rule). Independence preserved: the agent that acted never judged.
- **Apparatus discipline (DD8)**: driven via the committed `scripts/cdp-driver.mjs` + `curl`/node-CDP against the already-running `:9222`. The `chrome-devtools` MCP was explicitly forbidden and not used (it would launch its own browser → false pass — the standing Goldfinch trap). Apparatus instruction issued directly by the Orchestrator (per the skill–project boundary), overriding the crew file's generic "chrome-devtools available" hint.
- **Cache**: cold — the Executor closed two leftover smoke-test tabs and started from a clean trigger-page state.
- **Cross-checks**: the Flight Director separately confirmed (before this run) the served CSP header `default-src 'self'; base-uri 'none'; object-src 'none'; frame-ancestors 'none'` via CDP Network read-back, and the internal preload's context isolation (`window.goldfinchInternal.version===1` while `window.goldfinch`/`require` are undefined in the guest).

## Closing Summaries

### Executor closing
All 13 steps performed via cdp-driver/raw CDP only (no browser MCP). Every dangerous/internal vector blocked; the https control and the trusted kebab→Settings + reload positives succeeded. One advisory anomaly: the javascript:/data: `window.open` in Step 4 spawned a stray but fully-blank `about:blank` tab (no dangerous content); the file: and goldfinch: `window.open` vectors created no tab at all. The file: media vector (Step 6) is reachable (cataloged + clickable) but guarded at `createTab`.

### Validator closing
13/13 PASS. Every attack vector blocked; all three positive controls succeeded. Two spec-quality flags for the authors: (1) Step 4 permits a benign stray `about:blank` tab under the current criteria — make "no tab at all" explicit if that's the intent; (2) Step 6's file: media vector is now reachable-but-guarded, so the spec's "MAY be structurally unreachable" note is stale and should describe a live, guarded vector.

## Operator Notes
Run executed as Flight-4 leg `verify-integration` (SC5). Promotes the spec `draft → active`. The two Validator spec-quality flags are folded into the spec in the same leg.
