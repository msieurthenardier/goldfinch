# Behavior Test Run: tab-scheme-guard — 2026-06-05-17-43-36

**Spec**: [tests/behavior/tab-scheme-guard.md](../../tab-scheme-guard.md)
**Status**: pass (key vectors; partial spec coverage — see note)
**Started**: 2026-06-05T17:43:36Z
**Completed**: 2026-06-05T17:48Z
**Executor**: abbbf1ce067cb0f7e
**Validator**: a725b2700f66830de

**Evidence (ephemeral, local-only)**: `/tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-06-05-17-43-36/` (B1–B4 target dumps).

## Summary

Re-run of the F1 hostile-URL guard on the upgraded **Electron 42.3.3**, exercising the key vectors (the genuinely hostile-page-reachable surface). 4 / 4 exercised checkpoints passed — **F1 enforcement still holds post-upgrade**. (This run exercised the core vectors, not the full 7-step spec; the `javascript:`/`data:` window.open steps and the structurally-unreachable media Step 6 were not re-driven — see note.)

## Step Results

### B1 — PASS (setup)
- Hostile fixture loaded at `http://127.0.0.1:8000/` ("Tab Scheme Guard Trigger Page").

### B2 — PASS (in-page `will-navigate` file: — the critical vector)
- In-page `window.location='file:///etc/passwd'` did NOT navigate the webview to `file://`; it stayed at `http://127.0.0.1:8000/`; zero `file:*passwd` targets. The main-process `will-navigate` guard holds on E42.

### B3 — PASS (window.open file:)
- `window.open('file:///etc/passwd')` produced no `file://` webview target and no new tab. Blocked.

### B4 — PASS (control)
- `window.open('https://example.com/')` opened a new webview at `https://example.com/`. No over-blocking.

## Orchestrator Notes
- Mode: consolidated single-pass Witnessed (Executor `abbb…` / Validator `a725…`), raw CDP at `:9222`, alongside the `core-browsing-shields` run.
- **Spec status unchanged (`draft`)**: this was a post-upgrade regression re-confirmation of the key vectors, not a full all-step run. The spec stays `draft` pending the standing Step-6 refinement (the media-open `file:` vector is structurally unreachable — mission Known Issue). Promotion to `active` awaits that refinement + a full-spec green run.

## Closing Summaries
### Executor closing
Re-drove the F1 vectors on E42; the in-page nav and window.open file: vectors are blocked, control opens.
### Validator closing
All four exercised checkpoints pass — the Electron upgrade did not regress the F1 guard. Full-spec coverage (incl. javascript:/data: and the media vector) not exercised this run.
