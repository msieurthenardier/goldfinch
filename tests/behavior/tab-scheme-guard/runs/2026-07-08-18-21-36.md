# Behavior Test Run: tab-scheme-guard — 2026-07-08-18-21-36

**Spec**: [tests/behavior/tab-scheme-guard.md](../../tab-scheme-guard.md)
**Status**: pass
**Started**: 2026-07-08T18:21:00-05:00
**Completed**: 2026-07-08T18:26:00-05:00
**Duration**: ~00:05:00
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed)

## Summary
13 / 13 steps passed. 0 failed; 0 inconclusive.

Native WebContentsView surface. Admin MCP surface on 8899. Fixture served at `http://127.0.0.1:8811/` from `tests/behavior/fixtures/tab-scheme-guard/`. All hostile vectors driven by clicking the **trigger page's own buttons** (`click(guestWcId=7, x, y)` in guest-local coords = window − (1,89), the `#webviews` origin); outcomes observed via `enumerateTabs` / `readDom(7)` (status elements) / screenshots. The harness never called `window.open`/`fetch`/navigation itself.

## Step Results

### Step 1 — PASS
- **Actions taken**: Connected admin client; `tools/list`; `getChromeTarget()`; `curl` fixture; `openTab('http://127.0.0.1:8811/')`.
- **Raw state**: `tools/list` includes `getChromeTarget, openTab, enumerateTabs, readDom`. `getChromeTarget()` → `{wcId:1}`. Fixture HTTP 200. Trigger tab opened as wcId 7; `enumerateTabs` shows it on `http://127.0.0.1:8811/`.
- **Expected**: named tools present; chrome wcId numeric; HTTP 200; trigger renders; web-guest wcId recorded.
- **Verdict**: pass.

### Step 2 — PASS
- **Actions taken**: `readDom(7)` + `captureWindow()`.
- **Raw state**: `readDom(7).url = http://127.0.0.1:8811/`, `title = "Tab Scheme Guard Trigger Page"`; screenshot shows the `trigger-page-loaded` marker and all hostile-vector buttons.
- **Verdict**: pass.
- **Evidence**: [step2-trigger-loaded.png](../../../../tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-07-08-18-21-36/step2-trigger-loaded.png)

### Step 3 — PASS
- **Actions taken**: Clicked `#open-file` (`window.open('file:///etc/passwd')`). Observed `enumerateTabs`, `readDom(7)`.
- **Raw state**: No new tab; tab set unchanged; no tab on a `file:` URL; trigger guest stayed on `http://127.0.0.1:8811/`.
- **Expected**: No tab renders `/etc/passwd`; no `file:` tab.
- **Verdict**: pass.

### Step 4 — PASS
- **Actions taken**: Clicked `#open-javascript` then `#open-data`.
- **Raw state**: The javascript button spawned a **neutralized `about:blank`** tab (title "New tab", NOT "pwned", NOT a `javascript:` URL) — this also confirms clicks land and `window.open` fires. The data button produced no `data:` tab and no "injected" content.
- **Expected**: Neither loaded; blank `about:blank` popup acceptable; no `javascript:`/`data:` URL; no "pwned"/"injected".
- **Verdict**: pass.

### Step 5 — PASS
- **Actions taken**: Clicked `#nav-file` (`window.location='file:///etc/passwd'`).
- **Raw state**: Guest (wcId 7) still on `http://127.0.0.1:8811/`; no `file:` tab. The `will-navigate` guard cancelled the navigation (positive anchor: stayed on the original http URL).
- **Verdict**: pass.

### Step 6 — PASS
- **Actions taken**: Opened the media panel (Ctrl+M). It cataloged the crafted `<video src=file:///etc/passwd>` as a "passwd" VIDEO item with play/pop-out(↗)/download actions. Clicked the pop-out (↗) action.
- **Raw state**: No new tab; tab set unchanged; no `file:` tab. (The `popout()` → `createTab` untrusted branch's `isSafeTabUrl` gate rejects `file:`.)
- **Expected**: Media item does not open a tab with local file contents; no `file:` tab.
- **Verdict**: pass. (Note: also witnessed the media panel reflowing the guest narrower — corroborates the panel-resizes-guest geometry, asserted in tab-surface-geometry.)
- **Evidence**: [step6-media-panel.png](../../../../tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-07-08-18-21-36/step6-media-panel.png)

### Step 7 — PASS
- **Actions taken**: Clicked `#open-control` (`window.open('https://example.com/')`).
- **Raw state**: A new tab (wcId 9) opened on `https://example.com/`, active — `enumerateTabs` confirms.
- **Expected**: New tab opens and renders example.com; guard does not over-block.
- **Verdict**: pass — no over-block; also re-confirms the click apparatus fires window.open.

### Step 8 — PASS
- **Actions taken**: (After closing clutter tabs for a clean assertion — the pre-existing internal settings tab from staging was closed so "no goldfinch:// tab" is unambiguous.) Clicked `#open-goldfinch` (`window.open('goldfinch://settings')`).
- **Raw state**: No new tab; no tab carries a `goldfinch://` URL; trigger still active. (`setWindowOpenHandler`→IPC→`createTab` untrusted→`isSafeTabUrl` rejects `goldfinch://`.)
- **Verdict**: pass.

### Step 9 — PASS
- **Actions taken**: Clicked `#nav-goldfinch` (`window.location='goldfinch://settings'`).
- **Raw state**: Guest (7) still on `http://127.0.0.1:8811/`; no `goldfinch://` tab. Session-aware `will-navigate` rejected the internal scheme from the web origin.
- **Verdict**: pass.

### Step 10 — PASS
- **Actions taken**: Clicked `#embed-goldfinch` (injects `<iframe src="goldfinch://settings">`). Read `#goldfinch-embed-result` via `readDom(7)` + `captureScreenshot`.
- **Raw state**: `#goldfinch-embed-result` = `"iframe injected (src=goldfinch://settings)"` (button fired). The injected iframe renders **fully blank** (orange-bordered black box in the screenshot); no "Settings — coming soon" / settings content. (A "Settings" substring in `readDom` traced to the fixture's own HTML comment, not the frame — verified by context inspection.)
- **Expected**: Iframe does NOT render the settings stub; `#goldfinch-embed-result` confirms the button fired.
- **Verdict**: pass.
- **Evidence**: [step10-11-iframe-blank.png](../../../../tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-07-08-18-21-36/step10-11-iframe-blank.png)

### Step 11 — PASS
- **Actions taken**: Clicked `#fetch-goldfinch` (page `fetch('goldfinch://settings')`). Read `#goldfinch-fetch-result` via `readDom(7)`.
- **Raw state**: `#goldfinch-fetch-result` = `"rejected: TypeError: Failed to fetch"` — NOT `resolved`.
- **Expected**: reads `rejected: …`, not `resolved`.
- **Verdict**: pass.

### Step 12 — PASS
- **Actions taken**: Opened kebab (⋮) via `click(1,1372,63)`; activated **Settings** on the menu-overlay sheet (wcId 3) via DOM click. Observed `enumerateTabs` + `captureWindow`.
- **Raw state**: New tab wcId 10 on `goldfinch://settings/`; the Settings page renders (Appearance / Privacy & Shields / Automation, "Connected — listening on 127.0.0.1:8899"); tab appears in the strip and is closeable.
- **Expected**: A tab opens to `goldfinch://settings` and the settings page renders (trusted embedder path).
- **Verdict**: pass. (Spec-drift note: page is a full Settings UI, not a "coming soon" stub.)
- **Evidence**: [step12-settings-trusted.png](../../../../tmp/behavior-tests/goldfinch/tab-scheme-guard/2026-07-08-18-21-36/step12-settings-trusted.png)

### Step 13 — PASS
- **Actions taken**: `reload(10)` (the internal settings tab).
- **Raw state**: `{"ok":true}`; the internal guest stays on `goldfinch://settings/` (re-renders). Session-aware `will-navigate` allows the internal session's own reload.
- **Expected**: Tab reloads, re-renders stub, stays `goldfinch://settings`.
- **Verdict**: pass.

## Orchestrator Notes
- Menu items activated via the sheet's DOM `.click()` (not raw coordinate clicks) — the sheet coordinate origin is offset ~88px; this is an activation-mechanism choice and does not touch the hostile-vector clicks, which were all real coordinate clicks on the trigger guest.
- Clutter tabs (staged from earlier specs + a control tab) were closed before Steps 8–11 so the "no goldfinch:// tab exists" assertion was clean.

## Closing Summaries

### Executor closing
Full pass, all three enforcement points exercised on the live surface: the `createTab`/`isSafeTabUrl` renderer gate (window.open + media pop-out), the main-process session-aware `will-navigate` guard (self-navigation), and the internal-scheme web-unreachability (embed blank + fetch rejected), while the trusted embedder path (kebab→Settings, reloadable) still works and the `https:` control is not over-blocked.

### Validator closing
Every dangerous vector produced either no tab or a neutralized `about:blank`; no `file:`/`javascript:`/`data:`/`goldfinch:` URL or content ever reached a webview. The `#goldfinch-embed-result`/`#goldfinch-fetch-result` status elements made the embed/fetch outcomes unambiguous. Spec's "Settings — coming soon" stub text is stale (full Settings UI now) — cosmetic.

## Operator Notes
Verdict PASS on the native WebContentsView surface. Layered scheme boundary intact.
