# Behavior Test Run: welcome-home-first — 2026-08-26-02-00-25

**Spec**: [welcome-home-first.md](../welcome-home-first.md)
**Status**: pass
**Started**: 2026-08-26T02:00:25Z
**Completed**: 2026-08-26T02:11:43Z
**Duration**: 00:11:18
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-home-first/2026-08-26-02-00-25/` during the run; re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. Acceptance gate for the M16 F3 leg-2 DD7 pivot (HAT item 6) on the final HAT build: Set on the welcome home card saves and stays — the same viewless record keeps the surface with the saved value reflected, a confirmation, and the engine card still offered; the next Ctrl+T is a plain home-page tab; a typed search hands off to a new welcome tab carrying the query with no provider URL committed; choosing DuckDuckGo runs it in place and the row records both choices.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Observe only: window screenshot, chrome a11y tree, DOM probe (strip, address bar, surface + gated block classes/computed display, tagline, engine heading, radios), `enumerateWindows`, `enumerateTabs`.
- **Raw state**: One strip tab "Welcome to Goldfinch" selected; address bar empty, placeholder, writable; surface visible with both cards (home flex, engine block), burner note hidden; tagline "Set up the two things…"; 8 radios none checked; `activeTabWcId: null`, `enumerateTabs` `[]`.
- **Expected**: Exactly one tab, titled "Welcome to Goldfinch", showing the welcome surface with **both** the home page block and the search engine block; the address bar is empty and writable; no committed URL.
- **Verdict**: pass — one selected "Welcome to Goldfinch" tab; both cards visible by computed display; address bar empty and writable; no committed URL (viewless record, `enumerateTabs` empty); rendered evidence cited.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-probe.json`, `step-1-windows.json`, `step-1-tabs.json`
- **Validator notes**: none.

### Checkpoint 2 — PASS
- **Actions taken**: Before probe + `enumerateTabs`; clicked `#welcome-home-input` by rect, typed `https://example.com`, clicked `#welcome-home-set`; window screenshot, a11y tree, after probe, `enumerateTabs`, `enumerateWindows`, read-only settings row.
- **Raw state**: Strip 1 → 1, same viewless record (`enumerateTabs` empty, `activeTabWcId` null before and after); address bar still empty; the surface still showing: home input `https://example.com`, status "Saved — new tabs will open here.", engine card still offered with no radio checked; row `homePage: "https://example.com"`, `searchEngine: null`.
- **Expected**: That **same** tab stays a welcome tab: the home page field shows `https://example.com` with a saved confirmation beneath it; the engine card is still offered (no engine chosen). `[mixed-frame]` The settings row reads `homePage: "https://example.com"`, `searchEngine: null` — the engine choice was not made for the user.
- **Verdict**: pass — the same single tab stayed a welcome tab (address bar still empty; the record still viewless before and after); the home field shows the saved value with the "Saved — new tabs will open here." confirmation; the engine card still offered with nothing chosen; the row corroborates `homePage` set, `searchEngine: null`.
- **Evidence**: `step-2-before.json`, `step-2-tabs-before.json`, `step-2-screenshot.png`, `step-2-snapshot.txt`, `step-2-probe.json`, `step-2-tabs-after.json`, `step-2-windows.json`, `step-2-settings-row.json`
- **Validator notes**: the report gave the status element's id and text rather than its DOM position — strong enough; no impact.

### Checkpoint 3 — PASS
- **Actions taken**: Ctrl+T; window screenshot, a11y tree, probe, `enumerateTabs`, `enumerateWindows`, read-only settings row.
- **Raw state**: Strip 1 → 2 (the background welcome tab plus the new tab), selectedIndex 1; `#address.value` `https://example.com/`; surface display none; `enumerateTabs` exactly one real entry (wcId 2, active); row `searchEngine: null`.
- **Expected**: A **plain** tab opens on `https://example.com/` — no welcome surface, no engine block: new tabs do not re-offer the abandoned engine choice (the original welcome tab from step 2 stays open in the background, unaffected). `[mixed-frame]` The settings row still reads `searchEngine: null`.
- **Verdict**: pass — a genuinely new tab (strip 1 → 2, the original welcome title unchanged in the strip) on `https://example.com/` with the surface hidden; one real `enumerateTabs` entry (wcId 2); the row still `searchEngine: null`.
- **Evidence**: `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`, `step-3-tabs.json`, `step-3-windows.json`, `step-3-settings-row.json`
- **Validator notes**: none.

### Checkpoint 4 — PASS
- **Actions taken**: `enumerateTabs` + probe before; clicked the address bar, Ctrl+A, typed `zephyr quartz order`, Enter; window screenshot, a11y tree, probe, `enumerateTabs`, `enumerateWindows` after.
- **Raw state**: Strip 2 → 3 (chrome DOM), the new welcome tab active with the engine card only (home card hidden), heading *Where should we search for "zephyr quartz order"?*, tagline "Choose where your searches go…", no radio checked; `enumerateTabs` unchanged (wcId 2 example.com, now inactive) — no new entry, no provider host; `activeTabWcId: null`; the transient `sheetWcId` field appeared (`sheetVisible: false`).
- **Expected**: `[mixed-frame]` The `example.com` tab is untouched; a **new** welcome tab opens beside it and is the active tab — strip count +1 read from the chrome DOM strip — showing the search engine block only, headed with the pending query (*Where should we search for "zephyr quartz order"?*); no provider URL is committed anywhere — `enumerateTabs` before/after: no new entry, no provider host.
- **Verdict**: pass — strip 2 → 3 with the new welcome tab active beside the untouched example.com tab; engine card only, heading verbatim with the query; `enumerateTabs` unchanged (no new entry, no provider host).
- **Evidence**: `step-4-tabs-before.json`, `step-4-before.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-probe.json`, `step-4-tabs-after.json`, `step-4-windows.json`
- **Validator notes**: the transient `sheetWcId` (`sheetVisible: false`) bears on nothing.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe + `enumerateTabs`; clicked the DuckDuckGo radio by rect; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`, read-only settings row.
- **Raw state**: Strip 3 → 3, same index, title "zephyr quartz order at DuckDuckGo"; committed `https://duckduckgo.com/?q=zephyr+quartz+order&ia=web` (twice); surface hidden; the welcome tab attached as wcId 4 (active), example.com (wcId 2) untouched; row `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`.
- **Expected**: That **same** welcome tab navigates to a `duckduckgo.com` URL containing the encoded query (tab count unchanged) — the abandoned choice, made later, runs the search that prompted it. `[mixed-frame]` The settings row reads `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`.
- **Verdict**: pass — the same tab (3 → 3, same index) committed `https://duckduckgo.com/?q=zephyr+quartz+order&ia=web` (two reads) and attached as wcId 4; example.com untouched; the row reads both preferences set.
- **Evidence**: `step-5-before.json`, `step-5-tabs-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs-after.json`, `step-5-windows.json`, `step-5-settings-row.json`
- **Validator notes**: none.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh empty `XDG_CONFIG_HOME`. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled).
- **Purpose**: first of four gate re-runs for the DD7 pivot made at the HAT (the welcome surface never navigates on its own except to run a pending search); row 2 is the re-authored row and passed on rendered + DOM evidence with the record provably still viewless before and after.
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden; block visibility read by className/computed display (never the `.hidden` IDL property); strip counts from the chrome DOM; `enumerateTabs` diffs as the supplementary negative. The transient `sheetWcId` field (`sheetVisible: false`) appeared from step 4 — the documented resolved-sheet quirk, not an error.
- **Spec note**: row 2's "beneath it" is positional and unnamed — the Validator judged on the element's id and text; name `#welcome-home-status` in a future revision.

## Closing Summaries

### Executor closing
All five steps clean on the fresh profile, every action ok on the first attempt. The DD7 pivot behaved exactly as the re-authored spec says: Set stayed on the same viewless record (`enumerateTabs` empty, `activeTabWcId` null before and after), the next tab was plain, the handoff tab was engine-only, the DuckDuckGo pick navigated in place. Carry forward: `enumerateWindows` gained `sheetWcId: 3` (`sheetVisible: false`) from step 4 with no sheet action taken — the known transient field; a welcome tab's wcId appears only once a navigation attaches it, so the chrome strip + `#welcome-surface` display remain the only reliable reads until then; double address-bar reads were stable.

### Validator closing
5/5 with no ambiguity; the re-authored row 2 was cleanly distinguishable in evidence. The apparatus facts held exactly as documented; the mixed-frame tie-break was never exercised (no browser/filesystem disagreement); row 4's "read the strip from the chrome DOM" framing was essential and followed; the Executor judged visibility by computed display and correctly declined to assert inner blocks under a hidden surface. Spec quality: name the confirmation element (`#welcome-home-status`) rather than "beneath it". No INCONCLUSIVE calls, no independent observations needed.

## Operator Notes
_None._
