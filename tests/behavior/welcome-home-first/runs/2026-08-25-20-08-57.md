# Behavior Test Run: welcome-home-first — 2026-08-25-20-08-57

**Spec**: [welcome-home-first.md](../welcome-home-first.md)
**Status**: pass
**Started**: 2026-08-25T20:08:57Z
**Completed**: 2026-08-25T20:21:02Z
**Duration**: 00:12:05
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-home-first/2026-08-25-20-08-57/` during the run; re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. First run of the spec, on the Flight 3 leg-1 restyled build: a fresh profile boots to a both-unset welcome tab; setting the home page first attaches that same tab immediately with the engine left `null`; the next Ctrl+T is a plain home-page tab (no re-offer); a typed search hands off to a new welcome tab carrying the query with no provider URL committed; choosing DuckDuckGo runs the search in that same tab and the row records both choices. Spec promoted `draft` → `active`.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Observe only: window screenshot, chrome a11y tree, DOM probe (strip, address bar, block flags, engine heading, radios, mark), `enumerateWindows`, `enumerateTabs`.
- **Raw state**: One strip tab "Welcome to Goldfinch", selected; address bar empty, placeholder shown, `readOnly`/`disabled` false; `#welcome-home-block` and `#welcome-engine-block` visible, burner note hidden; heading "Search engine"; radiogroup with 8 radios, none checked; 48×48 brand mark rendered; `activeTabWcId: null`, `enumerateTabs` `[]`.
- **Expected**: Exactly one tab, titled "Welcome to Goldfinch", showing the welcome surface with **both** the home page block and the search engine block; the address bar is empty and writable; no committed URL.
- **Verdict**: pass — one strip tab titled "Welcome to Goldfinch"; both blocks visible (8-option radiogroup, none checked); address bar empty, writable, placeholder shown; no committed URL (`enumerateTabs` empty, a viewless record).
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-probe.json`, `step-1-windows.json`, `step-1-tabs.json`
- **Validator notes**: the brand mark and restyled option cards are the Flight 3 surface — appearance out of scope; judged on ids and hidden-state only.

### Checkpoint 2 — PASS
- **Actions taken**: Before probe (1 strip tab, engine block still showing; `enumerateTabs` empty); clicked `#welcome-home-input` by rect, typed `https://example.com`, clicked `#welcome-home-set`; window screenshot, a11y tree, after probe, `enumerateTabs`, read-only settings row.
- **Raw state**: Strip count 1 → 1, same index 0, title "Welcome to Goldfinch" → "Example Domain"; `#address.value` `https://example.com/`; welcome surface hidden; the tab now enumerates as wcId 2 on `https://example.com/`; row `homePage: "https://example.com"`, `searchEngine: null`.
- **Expected**: That **same** tab navigates to `https://example.com/` (tab count unchanged, same strip position); the welcome surface is gone from it. `[mixed-frame]` The settings row reads `homePage: "https://example.com"`, `searchEngine: null` — the engine choice was not made for the user.
- **Verdict**: pass — the same tab (index 0, count 1 → 1) navigated to `https://example.com/` with the welcome surface gone (rendered); the row reads `homePage` set and `searchEngine: null` — both frames agree.
- **Evidence**: `step-2-before.json`, `step-2-tabs-before.json`, `step-2-screenshot.png`, `step-2-snapshot.txt`, `step-2-probe.json`, `step-2-tabs-after.json`, `step-2-settings-row.json`
- **Validator notes**: `enumerateTabs` gained the entry (wcId 2) only after the navigation attached a webContents, as the spec describes.

### Checkpoint 3 — PASS
- **Actions taken**: Ctrl+T on the chrome; window screenshot, a11y tree, probe, `enumerateTabs`, read-only settings row.
- **Raw state**: Strip 1 → 2, the new tab active on `https://example.com/` with a real wcId (3); `#welcome-surface` hidden (the singleton engine-block element beneath it still carried its last render state, moot under a hidden ancestor); no welcome UI rendered; row `searchEngine: null`.
- **Expected**: A **plain** tab opens on `https://example.com/` — no welcome surface, no engine block: new tabs do not re-offer the abandoned engine choice. `[mixed-frame]` The settings row still reads `searchEngine: null`.
- **Verdict**: pass — Ctrl+T opened a second, plain strip tab on `https://example.com/` with a real wcId; `#welcome-surface` hidden and no welcome UI rendered; row `searchEngine: null` — the abandoned choice stayed abandoned.
- **Evidence**: `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`, `step-3-tabs.json`, `step-3-settings-row.json`
- **Validator notes**: `#welcome-engine-block` read `hidden=false` under the hidden surface (residue on the singleton overlay element — its last render state); rendered ground truth carried the verdict. Spec authors: scope block-flag probes under the `#welcome-surface` flag, or the residual reading can mislead without the screenshot cross-check.

### Checkpoint 4 — PASS
- **Actions taken**: `enumerateTabs` + probe before; clicked the address bar, Ctrl+A, typed `zephyr quartz order`, Enter; window screenshot, a11y tree, probe, `enumerateTabs`, `enumerateWindows` after.
- **Raw state**: Strip 2 → 3 (chrome DOM), the new "Welcome to Goldfinch" tab active (index 2), address bar empty; surface visible with the engine block only, heading *Where should we search for "zephyr quartz order"?*, 8 radios none checked; `enumerateTabs` unchanged (wcId 2 and 3 on example.com; 3 now inactive) — no new entry, no provider host; `activeTabWcId: null`.
- **Expected**: The `example.com` tab is untouched; a **new** welcome tab opens beside it and is the active tab — strip count +1 **read from the chrome DOM strip** (the welcome tab has no wcId yet and does not appear in `enumerateTabs`) — showing the search engine block only, headed with the pending query (*Where should we search for "zephyr quartz order"?*); no provider URL is committed anywhere — separately diff `enumerateTabs` before and after: no new entry, no provider host.
- **Verdict**: pass — strip 2 → 3 with the new "Welcome to Goldfinch" tab active beside the untouched example.com tab; engine block only, headed verbatim with the pending query, 8 unchecked radios (rendered); `enumerateTabs` identical before/after — no new entry, no provider host; address bar empty.
- **Evidence**: `step-4-tabs-before.json`, `step-4-before.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-probe.json`, `step-4-tabs-after.json`, `step-4-windows.json`
- **Validator notes**: `activeTabWcId: null` with a DOM-active welcome tab is the documented apparatus quirk, not an anomaly.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe + `enumerateTabs`; clicked the DuckDuckGo radio-card label by rect; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`, read-only settings row.
- **Raw state**: Strip 3 → 3, same index 2, title now "zephyr quartz order at DuckDuckGo"; `#address.value` `https://duckduckgo.com/?q=zephyr+quartz+order&ia=web` (twice); surface hidden; the welcome tab attached as wcId 5 (active), the two example.com entries byte-identical; live DuckDuckGo results rendered; row `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`.
- **Expected**: That **same** welcome tab navigates to a `duckduckgo.com` URL containing the encoded query (tab count unchanged) — the abandoned choice, made later, runs the search that prompted it. `[mixed-frame]` The settings row reads `homePage: "https://example.com"`, `searchEngine: "duckduckgo"`.
- **Verdict**: pass — the same strip position now shows the committed `https://duckduckgo.com/?q=zephyr+quartz+order&ia=web` with live results rendered, count unchanged at 3; the example.com tabs byte-identical; row reads both preferences set — the abandoned choice was finally recorded.
- **Evidence**: `step-5-before.json`, `step-5-tabs-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs-after.json`, `step-5-windows.json`, `step-5-settings-row.json`
- **Validator notes**: clean run end to end, no frame divergence at any row; the row-4 apparatus quirk resolved at row 5 once the tab attached wcId 5.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh empty `XDG_CONFIG_HOME`, the first launch is the test. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled).
- **Apparatus**: attach-only CLI over `scripts/lib/mcp-client.mjs` with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden. The branch carries the crew file without squawk 0012's notes (PR #169 unmerged at run time) — the missing facts were briefed per spawn. Welcome tabs read from the chrome DOM strip; `enumerateTabs` diffs as the supplementary negative evidence (row 4).
- **Purpose**: leg-1 acceptance for Flight 3 — the spec reads ids and a11y states through the frozen DOM contract (DD2), so it doubles as the regression net for the restyle; the Validator confirmed no verdict depended on appearance (brand mark, card radios present and irrelevant).
- **Apparatus notes for the crew file** (squawk 0012 follow-up): `pressKey` accepts `Enter`, not `Return`; the singleton `#welcome-surface` keeps inner blocks' last `hidden` state while the surface itself is hidden — gate every inner-block flag on the surface's own flag (step 3 read `#welcome-engine-block` visible under a hidden surface; rendered state carried the verdict); row 4 should carry `[mixed-frame]` for consistency with rows 2/3/5 (spec nit, fixed at the leg-1 commit).
- No relaunch in this spec; no operator rows.

## Closing Summaries

### Executor closing
All five steps clean via the attach-only CLI; no blocks, no relaunches, no dropped connection. Carry forward: `pressKey` rejects `Return` (`unknown key`) — its vocabulary is `Tab, Enter, Escape, Space, Arrow*, Home, End, Delete, Backspace, ShiftTab` or a single character; the shared welcome-surface DOM keeps inner blocks' class state when hidden as a whole — always gate inner-block reads on `#welcome-surface`'s own hidden flag. Everything else matched the briefed topology: welcome tabs invisible to `enumerateTabs` until attached; `activeTabWcId: null` reliably flagged the viewless record; address-bar double reads stable; the settings row (`documents`, `store='settings'`) tracked null/null → set/null → set/null → set/null → set/set.

### Validator closing
5/5 with no frame divergence; every row's screenshot, a11y snapshot, and settings/`enumerateTabs` diffs agreed, so no independent observation was needed. Spec-quality: row 4 has the same viewless-tab split as rows 2/3/5 but lacks the `[mixed-frame]` tag — tag it for mechanical frame-aware judgment. The step-3 residual `#welcome-engine-block` flag under a hidden surface was resolved by rendered state; a per-tab-scoped probe would remove the judgment call. Row 5's network carve-out was not needed (results rendered). The frozen DOM contract held as a regression net through the restyle — no verdict touched appearance.

## Operator Notes
_None._
