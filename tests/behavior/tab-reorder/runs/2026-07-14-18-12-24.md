# Behavior Test Run: tab-reorder — 2026-07-14-18-12-24

**Spec**: [tab-reorder.md](../../tab-reorder.md)
**Status**: pass
**Started**: 2026-07-14T18:12:24Z
**Completed**: 2026-07-14T19:35:00Z (approx)
**Duration**: ~01:23
**Executor**: sub-agent (Sonnet), live SendMessage continuation
**Validator**: sub-agent (Sonnet), live SendMessage continuation

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/tab-reorder/2026-07-14-18-12-24/` during the
run; re-derivable by re-running the spec.

## Summary

**9 / 9 checkpoints passed** — first run of this spec (M09 Flight 2 leg 3
verify-integration). Zero inconclusive, zero needs-human-recheck: Step 4's
WSLg window-geometry caveat proved inapplicable this run (real, non-placeholder
position values), so the no-window-move check was a substantive hard pass.

## Checkpoint Results

### Checkpoint 1 — PASS
- Presence probe: all five spec-driven tools present (`getChromeTarget`,
  `evaluate`, `dragPointer`, `pressKey`, `click`); chrome wcId 1 numeric.
- Note: the Executor initially reported a 28-tool total; the Validator's
  independent count of the saved evidence is **29**, matching the repo pin
  (docs + unit tests). Miscount, no registry drift.
- Evidence: `step1-tools-list.txt`, `step1-getChromeTarget.txt`

### Checkpoint 2 — PASS (setup)
- 5 fixture tabs + the default boot tab = 6 tabs, titles pairwise distinct
  (three independent channels agree: axtree, enumerateTabs URLs, DOM titles).
  Baseline geometry {564, 260, 1432, 942} — real values, placeholder caveat
  not in play.
- Evidence: `step2-order-baseline.json`, `step2-axtree-tabs.json`

### Checkpoint 3 — PASS (the flight's headline)
- `dragPointer` dragged the 3rd-from-left tab past the last tab's midpoint:
  predicted drop index 5 by direct midpoint computation from the recorded
  rects; observed DOM order `[tab-1, tab-2, tab-4, tab-5, tab-6, tab-3]` —
  dragged tab last. DOM read, axtree sequence, and the screenshot (Validator-
  inspected) all agree. Dragged tab became selected (drag-start activation,
  Chrome parity). All transforms cleared — rect re-read shows plain slot
  positions.
- Evidence: `step3-before-rects.json`, `step3-after-order.json`,
  `step3-screenshot.png`

### Checkpoint 4 — PASS
- Window geometry byte-identical to baseline after the drag (Validator diffed
  the raw reads). WSLg placeholder caveat exercised-and-inapplicable this
  run: position values are real, so this is a hard pass, not
  needs-human-recheck.
- Evidence: `step4-geometry.json` vs `step2-geometry-raw.txt`

### Checkpoint 5 — PASS
- `Ctrl+Shift+ArrowLeft` (via `pressKey` modifiers param) moved the focused
  tab exactly one slot; focus stayed; selection unchanged; every tab's
  `aria-keyshortcuts` carries the chord alongside `Delete`.
- Branch note (Validator): the "selection unchanged" clause was exercised
  only in its focused==selected branch — structurally forced, because the
  roving tabindex parks focus on the active tab and plain arrows in this
  tablist navigate AND activate (re-coupling focus and selection). The
  non-degenerate branch may be structurally unreachable in this widget; the
  spec's conditional wording covers it.
- Evidence: `step5-before.json`, `step5-after.json`, `step5-axtree-tabs.json`

### Checkpoint 6 — PASS (DD1 discriminator)
- Forced DOM-last ≠ creation-last divergence, then closed a neither-last
  active tab: the surviving selected tab matched the **DOM-order-last**
  prediction (tab-3) and differed from the creation-order-last one (tab-6) —
  the pre-DD1 `[...tabs.keys()].pop()` behavior would have picked the wrong
  tab. Predictions were pinned in evidence BEFORE the close (good
  discriminating-test hygiene).
- Evidence: `step6-before.json` (incl. predictions), `step6-after.json`,
  `step6-enumtabs-raw.txt`

### Checkpoint 7 — PASS (click-model A)
- Synthetic `MouseEvent('click')` with no preceding pointerdown activated a
  background tab (`isTrusted:false` captured as the positive control) —
  the AT default-action path survives the DD2 refactor.
- Evidence: `step7-dispatch-raw.txt`, `step7-axtree-tabs.json`

### Checkpoint 8 — PASS (click-model B)
- Real trusted click on a background tab body: exactly one selected tab in
  the end-state (single activation). Per the flight's design-review ruling,
  the two-set-point suppression-flag internals are code-review territory;
  this row asserts the end-state only.
- Evidence: `step8-result.json`, `step8-axtree-tabs.json`

### Checkpoint 9 — PASS (click-model C)
- ✕-click then middle-click on verified-background tabs: each removed exactly
  its target (5→4→3), and after each close the selected tab was the tab
  active immediately before it — no flash-activation at end-state
  granularity (a sub-frame transient is unobservable over this apparatus;
  same limitation class the spec HAT-scopes for mid-drag motion).
- Evidence: `step9-result.json`, `step9-axtree-tabs.json`

## Orchestrator Notes

- Live two-agent mode; cache-cold (fresh app launch, free-fallback port per
  the pin-if-free precondition; 49707 remains externally held on this rig).
- Fresh agents (first run of this spec).
- Cancel-restore and mid-drag motion legibility were NOT exercised —
  unconditionally HAT-scoped per the spec (atomic drag op; no cancelable
  intermediate state) — carried on the mission HAT list.
- Run-log hygiene: the step-1 evidence embeds an absolute home path inside
  the chrome URL; quoted here only in scrubbed form.

## Closing Summaries

### Executor closing

All 9 steps executed with zero MCP errors; `dragPointer` defaults (12 steps,
4ms pacing) were sufficient for a reliable end-state on the first attempt.
Apparatus notes: the `pressKey` modifiers parameter (`['control','shift']`)
drives the reorder chord (no composite key name needed); the Shift+Tab
backward walk remains the reliable route into the tablist. Final state: 3
tabs, app left running for the next test.

### Validator closing

Spec quality is high: direct-computation clauses (Step 3), pinned-prediction
discriminators (Step 6), and positive controls (Step 7's isTrusted:false)
made most verdicts independently re-derivable from raw numbers. Watch items
carried to the run log: Step 5's selection clause exercised only in its
degenerate branch (possibly structurally unavoidable); Step 9's granularity
note; the Executor tool-count miscount corrected to 29.

## Operator Notes

First run green end-to-end; spec transitions `draft` → `active`. HAT items
accumulated this flight: mid-drag motion legibility, Escape/pointercancel
cancel-restore, and (carried from flight 1) native maximize/restore.
