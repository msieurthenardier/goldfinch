# Behavior Test Run: web-compat-basic-auth — 2026-07-28-19-58-55

**Spec**: [web-compat-basic-auth.md](../../web-compat-basic-auth.md)
**Status**: pass
**Started**: 2026-07-28T19:58:55Z
**Completed**: 2026-07-28T20:35:00Z (approx; incl. one app restart to clear the per-jar auth cache)
**Executor**: live agent (admin-keyed SDK-client apparatus; HAT mode — operator performed Esc, credential typing, sheet reading)
**Validator**: live agent (independent; read live fixture log + evidence directly)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/web-compat-basic-auth/2026-07-28-19-58-55/`.

## Summary

**7 / 7 checkpoints passed.** Step 1 pass-with-finding (D1, below); step 4 pass after an operator-confirmed reconciliation; step 6 pass on retry after a structural first-attempt blocker that itself yielded two security positives. HAT witnessing was decisive throughout — three verdicts were carried or cured by operator testimony that machine evidence could not supply.

## Checkpoint Results (condensed)

1. **PASS (with finding D1)** — sheet raised promptly, chrome-owned, content not loaded. Origin display cured by operator quote at the step-6 re-challenge: host correct, realm verbatim; **port not displayed** → defect D1, inline fix in flight, closure requires a one-prompt re-verify.
2. **PASS** — stronger than spec: the guest has **no committed document** while the prompt pends (reads hang; nothing to leak into). Mid-step sheet auto-hide resolved as the occlusion design (window blur; challenge survived; refocus re-presented — operator verified live).
3. **PASS** — operator Esc: clean dismissal, committed "unauthorized" body, zero Authorization headers ever sent (fixture-log-proven).
4. **PASS** — operator-typed credentials loaded the protected page; log shows challenge → all-true. Reconciliation: a mismatched-credential entry 22s earlier was the operator's own mistype (confirmed) — incidentally proving reject-and-re-challenge live (P4).
5. **PASS** — password absent from the DOM in plain **and** Base64 `user:pass` forms; username echoed once by design; validator read the full document exhaustively.
6. **PASS (retry)** — first attempt structurally blocked: per-jar credential cache auto-answered (O1) and the cross-jar workaround was correctly refused (`out-of-jar`, P1). After an app restart (fresh cache; the pending work-jar challenge died with quit — P5): fresh challenge → one-session vaultUnlock → vaultAnswerAuth → `{answered:true, id, origin}` with **no credential fields**, sheet closed with no re-present, page loaded, log all-true. Operator witnessed the sheet closing on its own ("worked").
7. **PASS** — no-challenge negative: `{answered:false, reason:'no-challenge'}` in 3ms, clean.

## Findings Inventory (Validator closing, adopted)

- **D1 (defect, fix in flight)**: auth sheet omits the port — displays `127.0.0.1`, spec/security intent requires `127.0.0.1:8091`. Targeted re-verify required post-fix.
- **O1 (design observation)**: Basic-auth credentials cache per-jar and silently auto-answer for the session; no re-challenge possible on that origin+realm within the jar. Correct Chromium behavior — but decide deliberately whether jar data-clear should be surfaced (vault-story interaction).
- **O2 (design observation)**: blur-close/refocus-re-present occlusion behavior verified live twice; each re-present issues a benign duplicate challenge request.
- **P1–P5 (security positives, all live-proven)**: out-of-jar refusal; credentials-never-in-DOM in strongest form; vault fill-only contract everywhere; wrong-credential reject-and-re-challenge; quit-with-pending-challenge cancels.

## Spec/Apparatus Improvements (S1–S4 + Executor)

S1: step 6 as authored is unreachable after any prior submit in the jar — provision fresh-challenge setup and name the key material (jar automation key ≠ MCP admin key ≠ X25519 vault key). S2: fixture field is `authMatched`, not "match". S3: mandate absolute-line log baselines (a windowed baseline silently excluded a material entry). S4: promote the capture-during-prompt timeout to documented apparatus behavior with the sheetVisible+operator-witness substitution rule. Executor: driver sessions are per-invocation and vault state is per-session — unlock→list→answer must share one connection; pending-prompt hangs of evaluate/readDom/navigate are themselves usable "content not loaded" evidence (with hard timeouts).

## Orchestrator Notes

Live two-agent HAT mode; verdicts 1–3 and 4–5 batched while operator actions proceeded in parallel (pacing deviation, logged). App restarted once mid-run (auth-cache clear; keys persisted). Operator's accidental alt-tab produced an unplanned live verification of the blur/refocus occlusion contract. Vault access key was operator-pasted into chat against guidance (operator accepted the risk; loopback-only; revocation at session end committed).

## Operator Notes

Sheet "looked like a standard built-in login"; keyboard feel good; Esc clean; watched vaultAnswerAuth close the sheet unaided. Revoke the vault access key + delete the key files at session end.
