# Behavior Test Run: downloads-surface — 2026-07-08-19-41-07

**Spec**: [tests/behavior/downloads-surface.md](../../downloads-surface.md)
**Status**: pass
**Started**: 2026-07-08T19:41:07
**Completed**: 2026-07-08T19:42:30
**Duration**: 00:01:23
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
Steps 1–6 PASS on native `WebContentsView`. A single navigation to the octet-stream fixture produced exactly one new record (no double-`will-download`), the jar key is refused admin-only, the on-disk file exists at the reported path, and the same-filename dedup produced a distinct ` (1)` uniquePath. Step 7 (cross-restart persistence) skipped — the in-run harness does not restart the app; HAT checkpoint.

**Download-trigger mechanism used:** primary — `python3 -m http.server` rooted at `tests/behavior/fixtures/`, serving `download-fixture.bin` as `application/octet-stream` (Content-Length 4096). No fallback needed.

## Step Results

### Step 1 — PASS (baseline)
- **Actions**: `downloadsList` (admin).
- **Raw state**: returns an array, count **N = 1** (one persisted record id 15 from a prior run — a `sample-image.png`), no error.
- **Verdict**: pass — tool discoverable, model reads cleanly.

### Step 2 — PASS (setup, trigger #1)
- **Actions**: `navigate(3, http://127.0.0.1:8000/downloads/download-fixture.bin)`; waited 3.5 s to settle. Silent default-save (no dialog).
- **Verdict**: setup — download fired and saved silently.

### Step 3 — PASS (single new record, distinct id)
- **Actions**: `downloadsList` (admin).
- **Raw state**: count **1 → 2**. New record `id:16` (distinct from prior id 15), `url` = fixture, `filename:"download-fixture.bin"`, `state:"completed"`, `received:4096 === total:4096` (>0), `savePath` non-empty (`~/download-fixture.bin`), `mime:"application/octet-stream"`.
- **Expected**: N→N+1 (not N+2), distinct id, filename matches, completed, non-empty savePath, received===total>0.
- **Verdict**: pass — exactly one record from one trigger; **no** Flight-5 double-`will-download` recurrence.
- **Evidence**: [downloads-full.json](./2026-07-08-19-41-07/downloads-full.json)

### Step 4 — PASS (filesystem corroboration)
- **Actions**: `stat` the new record's `savePath`.
- **Raw state**: `~/download-fixture.bin` exists, **size 4096** (non-zero).
- **Expected**: file exists with non-zero size.
- **Verdict**: pass — the model's `completed`/`savePath` is corroborated on disk. *(On this rig `app.getPath('downloads')` resolves to the home directory; paths anonymized here.)*

### Step 5 — PASS (jar refused admin-only)
- **Actions**: `downloadsList` with the **jar** key.
- **Raw state**: `isError:true`, `automation: admin-only — downloadsList (app-level downloads view) is restricted to the admin identity`.
- **Expected**: refused with the distinct admin-only error.
- **Verdict**: pass — `downloadsList` does not widen the jar surface's reach (SC8 gating half).

### Step 6 — PASS (same-filename dedup — wrong-filename regression guard)
- **Actions**: `navigate(3, <same fixture URL>)` a second time; waited; `downloadsList` (admin).
- **Raw state**: count **2 → 3**. New record `id:17`, same `filename` base, `savePath:"~/download-fixture (1).bin"` — a **distinct** on-disk path with the ` (1)` uniquePath suffix. Both id 16 and id 17 present distinctly.
- **Expected**: both records distinct (distinct ids, same filename), second savePath carries the ` (n)` suffix / distinct path.
- **Verdict**: pass — the model dedups the on-disk path rather than overwriting/mis-naming; the Flight-5 wrong-filename HAT defect does not recur. Both ` (1)` files corroborated on disk at 4096 bytes.
- **Evidence**: [downloads-full.json](./2026-07-08-19-41-07/downloads-full.json)

### Step 7 — SKIPPED (persistence, HAT)
- In-run harness does not restart the app; persistence (DD3) is a HAT checkpoint per the spec's own note. Not executed.

## Orchestrator Notes
Single-agent convenience sweep. Fixture served over `python3 -m http.server` (primary octet-stream mechanism). Test-created files (`download-fixture.bin`, `download-fixture (1).bin`) removed at batch teardown; the prior-run `sample-image.png` (id 15) left untouched.

## Closing Summaries

### Executor closing
The app-level downloads model captures a live `WebContentsView` download exactly once, persists a real on-disk path, dedups same-filename downloads with the uniquePath suffix, and gates the list to admin. No double-`will-download` and no wrong-filename regression. Clean pass.

### Validator closing
Every load-bearing assertion corroborated against a second source (the filesystem `stat`). Nothing relied on a page-DOM read of the internal `goldfinch://downloads` page (correctly out of scope). No concerns.

## Operator Notes
`savePath` values anonymized to `~/…` in this committed log (raw paths are in the local-only evidence dir).
