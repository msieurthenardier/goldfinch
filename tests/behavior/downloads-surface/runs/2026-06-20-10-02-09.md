# Behavior Test Run: downloads-surface — 2026-06-20-10-02-09

**Spec**: [tests/behavior/downloads-surface.md](../../downloads-surface.md)
**Status**: pass
**Started**: 2026-06-20T10:02:09
**Completed**: 2026-06-20T10:06
**Duration**: ~00:04:00
**Mode**: scripted live integration smoke (not the Witnessed `/behavior-test` multi-agent run — the
leg permits the equivalent scripted smoke; the formal Witnessed run remains the Flight Director's option)
**Apparatus**: Goldfinch MCP automation surface (loopback), admin key + jar key, attach+env-key model
**Driver**: an ephemeral Node script over `scripts/lib/mcp-client.mjs` (same transport as
`scripts/a11y-audit.mjs`); not committed.

## Summary
4 / 4 judged checkpoints passed (steps 1, 3, 4, 5). Steps 1–2 are setup; step 6 (restart persistence)
was not exercised in this run (HAT/macOS checkpoint per the spec). The **primary** `.bin`/octet-stream
fixture mechanism triggered the download — no fallback needed.

## Environment
- App: `GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation` (WSLg, X11/`:0`).
- MCP server **bound port 49709** (free-fallback off the default 49707 — captured live from the listening
  socket; not hardcoded).
- Fixture server: `python3 -m http.server 8000 --directory tests/behavior/fixtures`. Fixture
  `download-fixture.bin` (4096 bytes, deterministic) served at
  `http://127.0.0.1:8000/downloads/download-fixture.bin` as **`application/octet-stream`** (verified via
  `curl -D -`), which Chromium downloads rather than renders. **Primary mechanism — Content-Disposition
  fallback not required.**

## Step Results

### Tool discovery — PASS
- **Raw state**: `listTools` returned **27** tools; `downloadsList` present.
- **Verdict**: pass — total tool count 27 confirmed; `downloadsList` discoverable.
- **Evidence**: `tools.json`

### Step 1 (baseline `downloadsList`, admin) — PASS
- **Actions taken**: admin-key `downloadsList`.
- **Raw state**: returned an empty array; baseline count `N = 0`.
- **Expected**: returns an array (possibly empty), no error — tool discoverable, model reads cleanly.
- **Verdict**: pass.
- **Evidence**: `step1-baseline.json`

### Step 2 (open tab + navigate to fixture, silent download) — SETUP (no judgment)
- **Actions taken**: `openTab about:blank` → `wcId 3`; `navigate(wcId, fixtureURL)`.
- **Raw state**: `navigate` returned `ERR_FAILED (-2) loading '…/download-fixture.bin'` — **expected**:
  the navigation became a `will-download` (the response body is consumed as a download, not rendered as a
  page), so the navigation itself "fails" while the download proceeds. The download completed **silently**
  (no native save dialog — DD5 silent default-save). No fallback mechanism was needed.

### Step 3 (`downloadsList` shows the new completed record) — PASS
- **Raw state**: list grew to `N+1 = 1`; new record:
  `{ filename: "download-fixture.bin", state: "completed", savePath: "~/download-fixture.bin",
  received: 4096, total: 4096 }`.
- **Expected**: `N+1` records; new record has fixture filename (sanitized), `state: 'completed'`,
  non-empty `savePath`, `received === total > 0`.
- **Verdict**: pass — filename matches the (sanitization-clean) fixture name; terminal `completed`;
  `savePath` non-empty; `received === total === 4096`.
- **Evidence**: `step2-3-after.json`, `step3-record.json` *(the ephemeral evidence holds the real absolute
  savePath; anonymized to `~/…` here per the public-repo path policy)*

### Step 4 (`stat` the `savePath`) — PASS
- **Raw state**: file exists on disk, `size: 4096`, `isFile: true`.
- **Expected**: file exists with non-zero size — `savePath` corroborated against the filesystem.
- **Verdict**: pass — size matches `total` (4096), `completed`/`savePath` corroborated by a second source.
- **Evidence**: `step4-stat.json`

### Step 5 (jar key refused `downloadsList`) — PASS
- **Actions taken**: connected with the **jar key**, called `downloadsList`.
- **Raw state**: refused (`isError`) with
  `automation: admin-only — downloadsList (app-level downloads view) is restricted to the admin identity`.
- **Expected**: refused with the **distinct admin-only** error (SC8 gating half).
- **Verdict**: pass — the message is the distinct admin-only refusal (mirrors `getChromeTarget` /
  `captureWindow`), **not** a generic 401 or "not a function" (the "jewel of the gate" edge case).
- **Evidence**: `step5-jar-refusal.json`

### Step 6 (restart persistence) — NOT RUN
- The restart-persistence checkpoint (DD3) was not exercised in this scripted run; per the spec it is a
  skip-allowed/HAT checkpoint. Deferred to the HAT / macOS-authoritative pass.

## Orchestrator Notes
- Scripted live integration smoke chosen (leg-permitted) in lieu of the multi-agent Witnessed run.
- MCP port read live from the listening socket (49709) rather than assumed — the doc-noted free-fallback
  off 49707 occurred.
- The on-disk `savePath` was `$HOME` on this WSLg box (no XDG downloads dir → Electron falls back to
  `$HOME`, the leg-1 spike finding). The real absolute path is in the ephemeral evidence only; committed
  artifacts use `~/…`.

## Evidence
Ephemeral (NOT committed):
`/tmp/behavior-tests/goldfinch/downloads-surface/2026-06-20-10-02-09/` —
`tools.json`, `step1-baseline.json`, `step2-3-after.json`, `step3-record.json`, `step4-stat.json`,
`step5-jar-refusal.json`, `result.json`. Re-derive by re-running the smoke.

## Disposition
A **live** run exercised the real SC7/SC8 observables including the DD5 silent save → the spec status is
set to **`active`**. (The WSLg seeded-store fallback was NOT used; the live trigger fired and completed.)
