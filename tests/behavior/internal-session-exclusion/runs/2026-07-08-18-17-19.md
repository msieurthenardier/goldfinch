# Behavior Test Run: internal-session-exclusion — 2026-07-08-18-17-19

**Spec**: [tests/behavior/internal-session-exclusion.md](../../internal-session-exclusion.md)
**Status**: pass
**Started**: 2026-07-08T18:16:00-05:00
**Completed**: 2026-07-08T18:18:05-05:00
**Duration**: ~00:02:05
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed; no separate Validator spawned for this batch)

## Summary
5 / 5 steps passed. 0 failed; 0 inconclusive.

Native WebContentsView surface (Flight-3 substrate, Leg-2 keyboard-bridge fix in working tree).
App launched admin-wired (`GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_MCP_PORT=8899 npm run dev:automation`). The internal-session guard was exercised with the **jar key** (steps 3–5), since the guard only fires for non-admin identities (`resolveContents` with `allowInternal:false`); the **admin key** was used only out-of-band (step 4 apparatus) to discover the internal guest's wcId.

## Step Results

### Step 1 — PASS
- **Actions taken**: Connected the MCP client; `tools/list`.
- **Raw state**: Tool set included `openTab, enumerateTabs, readDom, readAxTree, captureScreenshot, navigate, captureWindow, getChromeTarget, evaluate, …` (27 tools total).
- **Expected**: `tools/list` includes the tools this spec drives (`openTab`, `enumerateTabs`, `readDom`, `readAxTree`, `captureScreenshot`) — presence-checked.
- **Verdict**: pass — all named tools present.

### Step 2 — PASS
- **Actions taken**: Opened the kebab (⋮) via `click(chromeWcId=1, 1372, 63)`; activated **Settings** on the menu-overlay sheet (wcId 3) via a DOM click (`[role=menuitem]` matching /Settings/). Confirmed via admin `enumerateTabs` and `captureWindow`.
- **Raw state**: `enumerateTabs` (admin) listed a new tab `{wcId:4, url:"goldfinch://settings/", title:"Settings — Goldfinch", jarId:"internal", active:true}`. The window screenshot shows the internal Settings page fully rendered (Appearance / Privacy & Shields / Automation sections; "Connected — listening on 127.0.0.1:8899").
- **Expected**: A tab opens to `goldfinch://settings` and renders the settings stub; the internal guest now exists.
- **Verdict**: pass — internal tab live and rendered. **Spec-drift note**: the spec anticipated a "Settings — coming soon" stub; the actual page is a full Settings UI. The load-bearing assertion (internal tab exists and renders) holds.
- **Evidence**: [step2-settings-open.png](../../../../tmp/behavior-tests/goldfinch/internal-session-exclusion/2026-07-08-18-17-19/step2-settings-open.png) (evidence dir: `/tmp/behavior-tests/goldfinch/internal-session-exclusion/2026-07-08-18-17-19/`)

### Step 3 — PASS
- **Actions taken**: With the **jar key** (default jar), called `enumerateTabs`.
- **Raw state**: `[{wcId:2, url:"https://www.google.com/", jarId:"default", active:false}]` — only the web tab; the `goldfinch://settings` internal guest (wcId 4) is absent.
- **Expected**: Returned array does NOT include the `goldfinch://settings` internal guest; internal session filtered from enumeration; normal web tabs still listed.
- **Verdict**: pass — internal guest excluded; web tab still listed.

### Step 4 — PASS
- **Actions taken**: Took the internal guest's wcId (**I = 4**, obtained out-of-band from the admin `enumerateTabs`). With the **jar key**, called `navigate(4, "https://example.com")`, `readDom(4)`, `captureScreenshot(4)`, `readAxTree(4)`.
- **Raw state**: Each of the four returned `isError:true` with content text: `automation: internal-session — wcId 4 belongs to the internal goldfinch://settings session and cannot be driven`.
- **Expected**: Each of the four returns `isError:true` whose content text contains `automation: internal-session`; the internal tab is never navigated/read/captured/a11y-read (guard fires at resolve time).
- **Verdict**: pass — all four rejected at resolve time with the exact `internal-session` code; no side effect (internal tab stayed on `goldfinch://settings`, confirmed in step 5's window state).

### Step 5 — PASS
- **Actions taken**: Control. With the **jar key**, took the web tab wcId (**W = 2**, default jar) and called `navigate(2, "https://example.com")`, `readDom(2)`, `captureScreenshot(2)`, `readAxTree(2)`.
- **Raw state**: `navigate` → `{"ok":true}`; `readDom` → `{url:"https://example.com/", title:"Example Domain", html:…}`; `captureScreenshot` → image content (32036 bytes, non-error); `readAxTree` → AX node array (RootWebArea "Example Domain", url https://example.com/).
- **Expected**: All four succeed (no `isError`) with their success shapes.
- **Verdict**: pass — all four succeeded, confirming the step-4 rejections are the internal-session guard specifically, not a tool/transport failure.
- **Evidence**: [step5-webtab-shot.png](../../../../tmp/behavior-tests/goldfinch/internal-session-exclusion/2026-07-08-18-17-19/step5-webtab-shot.png)

## Orchestrator Notes
- Single-agent Witnessed run (Executor also judged). App launched admin-wired per the leg instructions; the guard-under-test is jar-key behavior, so steps 3–5 used `GOLDFINCH_MCP_KEY` (jar mode), with the admin key used only to read the internal wcId out of band.
- Menu items on the menu-overlay sheet (wcId 3) were activated via DOM `.click()` in `evaluate` rather than coordinate clicks: the sheet's coordinate origin is offset ~88px below the window (guest region), so raw window-y coordinates land on the wrong item. (Learned when a window-y=109 click on the sheet hit "Exit" and quit the app during bring-up — a self-inflicted operator error, not an app defect.)

## Closing Summaries

### Executor closing
Clean full pass. The DD5 trust boundary holds across the real MCP transport on the real internal session: the `goldfinch://settings` guest is both excluded from jar-key enumeration AND rejected at resolve time (`automation: internal-session`) when its wcId is supplied directly to navigate/readDom/captureScreenshot/readAxTree. The web-tab control succeeds on all four, isolating the rejection to the internal-session guard.

### Validator closing
The exact discriminated error string (`automation: internal-session — wcId N belongs to the internal goldfinch://settings session and cannot be driven`) matches the spec's expected `resolve.js` throw. No BLOCKING security concern. Spec's "Settings — coming soon" stub language is stale (page is now a full Settings UI) — recommend the spec author refresh the step-2 expected text.

## Operator Notes
BLOCKING security spec — verdict PASS. Trust boundary intact on the native WebContentsView surface.
