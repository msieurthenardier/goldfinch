# Behavior Test Run: toolbar-pins — 2026-07-08-23-26-43

**Spec**: [tests/behavior/toolbar-pins.md](../../toolbar-pins.md)
**Status**: pass
**Started**: 2026-07-08T23:24:00
**Completed**: 2026-07-08T23:32:00
**Executor**: Leg-1c batch executor (SC3 parity sweep, self-witnessed single-agent)
**Surface**: native `WebContentsView` — chrome `wcId=1`, internal settings guest `wcId=4`, menu
sheet `wcId=3`. Admin MCP over `127.0.0.1:8899`. Dev profile `~/.config/goldfinch-dev`.

## Summary
The pin/unpin system for Media, Shields, and DevTools behaves correctly: pins render as toolbar
icons, unpinning removes the icon live while the keyboard shortcut still works, pin state persists
to `settings.json` `toolbarPins` and is two-way with the settings Appearance toggles, "Site
settings →" opens the settings page (not the slide-out panel), and DevTools is the default-unpinned
third item that is inert-not-hidden on internal tabs and unpinnable via the in-DOM right-click menu.
**13 of 14 steps PASS on the native surface.** Step 9 (the axe a11y harness) is the spec's
**F8-deferred** shell harness — the settings-target half is additionally blocked by the Flight-6
internal-session `injectScript` refusal (see `settings-shell`); carried as deferred. No
`LIKELY-REGRESSION`.

**Apparatus notes.** Pin buttons expose `aria-pressed` as an **attribute** (serializes in
`readDom` — read reliably); `settings.json` `toolbarPins` is the authoritative persistence witness.
`pressKey` has **no `F12`** in its key set (function keys unsupported) — the DevTools shortcut was
driven via the documented alternate **`Ctrl+Shift+I`** (`pressKey('I', ['control','shift'])`).
Baseline for the run was set by editing `settings.json` `toolbarPins.devtools=false` + relaunch, so
the default-unpinned DevTools assertions hold.

Evidence: `/tmp/behavior-tests/goldfinch/toolbar-pins/2026-07-08-23-26-43/`.

## Step Results

### Step 1 — PASS (toolbar baseline)
- **Raw (chrome via `evaluate`):** `#toggle-media` present, **visible** (`hidden=false`),
  label "Media", `.tb-badge` present; `#toggle-privacy` present, visible, label "Shields";
  `#toggle-devtools` present but **`.hidden`** with `aria-pressed="false"` — DevTools defaults
  **UNPINNED** (DD4).
- **Verdict:** PASS.

### Step 2 — PASS (Appearance pins)
- **Raw (guest `readDom` + screenshot):** Appearance shows pin toggles — `#pin-media`
  `aria-pressed="true"` (filled pushpin), `#pin-shields` `aria-pressed="true"`, `#pin-devtools`
  `aria-pressed="false"` (outline pushpin — unpinned). `goldfinch://settings` enumerable as
  `guestWcId=4`.
- **Verdict:** PASS. `[a11y]`

### Step 3 — PASS (keyboard unpin Media)
- **Raw:** `#pin-media` `aria-pressed` true → (mouse click, focuses) false → **`Space` (kbd)** true
  → **`Space` (kbd)** false. `keyboardToggles=true`, ends **unpinned**.
- **Verdict:** PASS — the pin is keyboard-operable (`Space` flips the focused toggle). `[a11y]`

### Step 4 — PASS (persisted)
- **Raw:** `settings.json` `toolbarPins.media=false`.

### Step 5 — PASS (toolbar reflects live)
- **Raw:** chrome `#toggle-media` now `hidden=true` (icon removed); `#toggle-privacy` still visible.
- **Verdict:** PASS — two-way live reflect.

### Step 6 — PASS (unpinned keeps shortcut)
- **Raw:** with Media unpinned, focus-anchor `click` in chrome + `Ctrl+M` → `#media-panel` opens
  (not `collapsed`).
- **Verdict:** PASS — the shortcut is independent of the toolbar icon.

### Step 7 — PASS (re-pin Media)
- **Raw:** re-clicking the Media Appearance pin → `aria-pressed="true"`, `settings.json`
  `media=true`, chrome `#toggle-media` visible again.
- **Verdict:** PASS.

### Step 8 — PASS ("Site settings →" opens settings page)
- **Raw:** web-chip popup → **Site settings →** → address `goldfinch://settings/#privacy`; the
  slide-out `#privacy-panel` did **not** open (`collapsed`).
- **Verdict:** PASS — matches the Flight-7 rewire (consistent with `settings-shell` step 10).

### Step 9 — DEFERRED (F8 axe harness)
The `npm run a11y` chrome + `--target=goldfinch://settings` axe injection audit — the spec's
F8-deferred harness (run separately, not on the MCP surface). The **settings-target** half is
blocked by the Flight-6 internal-session exclusion (`injectScript` refused on the internal guest —
see `settings-shell` steps 5-6). Carried as deferred; not run. `[a11y]`

### Step 10 — PASS (pin DevTools via Appearance)
- **Raw:** `#pin-devtools` `aria-pressed` false → click → **true** (unpinned → pinned; the inverse
  default of Media/Shields).
- **Verdict:** PASS. `[a11y]`
  *(Note: a first attempt missed due to a click-settle timing issue on the guest; re-clicked with a
  longer settle and it toggled reliably.)*

### Step 11 — PASS (persist + live un-hide)
- **Raw:** `settings.json` `toolbarPins.devtools=true`; chrome `#toggle-devtools` now **visible**
  (no longer `.hidden`); it is a **toggle button** with `aria-pressed="false"` (NOT `aria-expanded`
  — no in-page panel; DevTools window closed).
- **Verdict:** PASS. `[a11y]`

### Step 12 — PASS (unpinned shortcut still opens DevTools)
- **Raw:** on a **web** tab, `Ctrl+Shift+I` → `#toggle-devtools` `aria-pressed` **false → true** —
  DevTools **opened** for the active web tab (reflected live via `devtools-state-changed`, readable
  even while the button was hidden/unpinned). Toggling again → back to `false` (closed). `evaluate`
  kept working throughout (no CDP conflict for `evaluate` on this rig).
- **Verdict:** PASS — the shortcut is independent of pin state (DD2/DD4). *(F12 itself is not
  expressible via `pressKey`; the documented alternate `Ctrl+Shift+I` was used. The detached
  DevTools-window materialization + the readAxTree CDP conflict are `devtools-cdp-conflict`'s scope,
  macOS-authoritative.)*

### Step 13 — PASS (inert-not-hidden on internal — DD5)
- **Raw:** DevTools pinned; on the `goldfinch://settings` internal tab, `#toggle-devtools` stays
  **visible** (`hidden=false`); clicking it → `aria-pressed` stays `false` (no-op); `Ctrl+Shift+I`
  with the internal tab active → `aria-pressed` stays `false` (opens nothing on `goldfinch://`).
- **Verdict:** PASS — inert, not hidden; no throw/toggle on internal. `[a11y]`

### Step 14 — PASS (right-click → in-DOM Unpin DevTools)
- **Raw:** right-click `#toggle-devtools` → the custom **`#page-context-menu`** opens in the sheet
  (`data-menu-type="page-context"`, a single **"Unpin DevTools"** `role="menuitem"` — the in-DOM
  on-brand menu, MCP-drivable). Activating it → `settings.json` `toolbarPins.devtools=false`,
  `#toggle-devtools` hidden live.
- **Verdict:** PASS — equivalent to the Appearance-toggle unpin; the in-DOM menu is drivable since
  the Leg-5 migration. *(Tight right-click→activate is required — intervening DOM probes blur/dismiss
  the sheet.)*

## Closing Summary
The full pin/unpin system — Media + Shields default-pinned, DevTools default-unpinned; keyboard-
operable Appearance toggles; persistence to `toolbarPins`; live two-way toolbar reflect; shortcuts
independent of pin state; DevTools inert-not-hidden on internal tabs; the in-DOM right-click Unpin
menu; and "Site settings →" opening the settings page — all hold on the native `WebContentsView`.
**PASS** (13/14; step 9 the F8-deferred axe harness, settings-target additionally blocked by the
Flight-6 internal-session `injectScript` refusal). No `LIKELY-REGRESSION`. (`settings.json`
`toolbarPins` mutations restored from the pre-run backup at batch end.)
