# Behavior Test Run: welcome-first-launch — 2026-08-25-04-22-08

**Spec**: [welcome-first-launch.md](../welcome-first-launch.md)
**Status**: pass
**Started**: 2026-08-25T04:22:08Z
**Completed**: 2026-08-25T04:47:37Z
**Duration**: 00:25:29
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-first-launch/2026-08-25-04-22-08/` during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed (checkpoint 3 on a rerun after a fix at the gate). A brand-new profile boots to a single welcome tab offering both blocks with an empty, writable address bar and no committed URL; the row is pinned `version 3` with both keys explicitly `null`; the bookmarks bar shows on the welcome surface; Settings shows both fields unset — each with its hint, once the missing home-page hint was added; choosing an engine removes only its block and the next new tab offers only the home block; a search then goes straight to the chosen engine; setting the home page from the surface navigates that tab and ends the welcome surface for new tabs; the final row holds exactly the two choices.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Observation only — `captureWindow`, chrome `readAxTree`, chrome `evaluate` probe, `enumerateTabs`.
- **Raw state**: One strip tab, "Welcome to Goldfinch", selected; address bar empty with placeholder, `readOnly false`; panel visible with h2 "Welcome to Goldfinch", a Home page block (empty input, Set, hint) and a Search engine block (h3, 8 radios with descriptions, none checked); `enumerateTabs` empty (no web contents); no committed URL anywhere.
- **Expected**: Exactly one tab, titled "Welcome to Goldfinch", showing the welcome surface with **both** the home page block and the search engine block; the address bar is empty with its placeholder and is writable; no tab has committed any URL. [a11y]
- **Verdict**: pass — one tab, both blocks, 8 unselected radios with descriptions, writable empty address bar with placeholder; a11y tree and pixels agree; no committed URL.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-probe.json`, `step-1-tabs.json`
- **Validator notes**: heading structure h2 "Welcome to Goldfinch" → labelled Home page group → "Search engine" heading over the radiogroup; no rendered-vs-DOM divergence.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` query at 04:25:56Z, saved verbatim; no UI interaction.
- **Raw state**: `version: 3`; `homePage: null` and `searchEngine: null` both present as JSON `null` (keys not missing); twelve top-level keys.
- **Expected**: `[mixed-frame]` The row exists with `version: 3`, `homePage: null`, and `searchEngine: null` — both keys present and explicitly unset (pinned at first load, not merely absent).
- **Verdict**: pass — the Validator’s own `sqlite3` read matched: `version 3`, both keys present as explicit JSON `null`.
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: none.

### Checkpoint 3 — FAIL (first attempt) → fixed at the gate, rerun below
- **Actions taken**: Opened Settings via the seam; scrolled to and clicked "Show bookmarks bar"; captured the Settings guest + a11y tree; clicked the first strip tab to return to the welcome tab; probe + `captureWindow`.
- **Raw state**: Settings: home page field empty with Save/Clear beside it and **no hint text beneath it**; all 8 engine radios unchecked with the hint "No search engine chosen — the welcome page will ask the first time you search."; the bookmarks-bar toggle on. Welcome tab: an empty bookmarks-bar row rendered beneath the address bar; both blocks still shown; `barVisible: true`.
- **Expected**: The bookmarks bar is visible on the welcome tab (empty, but present) — the bar is not suppressed on this surface. Settings shows an empty home page field and no engine selected, each with its hint.
- **Verdict**: fail — the bookmarks-bar half held (an empty bar rendered on the welcome tab; toggle on), but the Settings half did not: the home-page field renders no hint when unset while the engine block renders its unset hint; rendered pixels and the a11y dump agree.
- **Evidence**: `step-3-settings-before.png`, `step-3-screenshot-2.png`, `step-3-snapshot.txt`, `step-3-probe.json`, `step-3-screenshot.png`
- **Validator notes**: a genuine UI asymmetry a user would notice — the engine block has an explicit unset hint, the home-page field does not.
- **Operator decision**: rerun-checkpoint after a fix (Orchestrator, autonomous — the gap is in this leg's own surface: DD6 defined the engine hint and the post-Clear status text but no persistent unset hint for the home page; a Developer was spawned to add `HOME_UNSET_HINT` and make the status line reflect state on load and on every broadcast, which also subsumes squawk 0010)

### Checkpoint 3 (rerun, after the fix) — PASS
- **Actions taken**: Closed the Settings tab and reopened it via the seam (fresh wcId 3, freshly served from disk); captured guest screenshot + a11y tree; verified the bookmarks toggle read-only; returned to the welcome tab; probe + `captureWindow`.
- **Raw state**: Home page field empty with the new hint "No home page chosen — new tabs will open the welcome page until you set one." beneath it; all 8 engine radios unchecked with their hint; bookmarks-bar toggle still on; welcome tab shows the empty bookmarks bar and both blocks; `barVisible: true`.
- **Expected**: (as above)
- **Verdict**: pass — the fresh Settings tab renders the new home-page hint beneath the empty field and the engine hint beneath the unchecked radios; the empty bookmarks bar is rendered on the welcome tab with both blocks intact; a11y and pixels agree.
- **Evidence**: `step-3-screenshot-2.png`, `step-3-snapshot.txt`, `step-3-toggle-check.png`, `step-3-probe.json`, `step-3-screenshot.png` (attempt-1 captures preserved with an `-attempt1` suffix)
- **Validator notes**: fix confirmed; wording consistent with the engine hint’s style; attempt-1 evidence retained, superseded.
- **Fix record**: `src/renderer/pages/settings.js` home-page IIFE gained `HOME_UNSET_HINT` and a `reflect(value)` helper applied on load and on every broadcast (symmetric with the engine block); structural test added in `settings-page-shared-scripts.test.js`; suite 3761 → 3762. Broadcast ordering verified: `internal-settings-set` broadcasts before it returns, so the clicking page’s local message lands after the broadcast-driven state. Squawk 0010 subsumed (→ in-progress).

### Checkpoint 4 — PASS
- **Actions taken**: Clicked the Brave Search radio in the welcome panel (chrome); probe, `captureWindow`, `readAxTree`, `enumerateTabs`; then Ctrl+T; probe + `captureWindow`.
- **Raw state**: After the click: engine block hidden (a11y tree: zero radio nodes, no "Search engine" node), home block shown, panel visible, the tab still viewless (`enumerateTabs` lists only Settings). After Ctrl+T: a third tab, a welcome tab showing only the home block.
- **Expected**: The engine block disappears; the home page block remains; the tab stays a welcome tab. A subsequent Ctrl+T opens another welcome tab with only the home page block.
- **Verdict**: pass — after choosing Brave Search the panel shows only the home block (a11y grep: zero radios, no engine names), the tab is still the same viewless welcome tab; Ctrl+T opens a welcome tab with only the home block.
- **Evidence**: `step-4-probe.json`, `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-tabs.json`, `step-4-probe-2.json`, `step-4-screenshot-2.png`
- **Validator notes**: the probe’s `engineRadios` DOM count is misleading for absence (counts hidden nodes) — the a11y grep is the reliable check.

### Checkpoint 5 — PASS
- **Actions taken**: Before-probe on the 3rd (welcome) tab; chrome address-bar search `zephyr quartz fresh`; after-probe, `enumerateTabs`, `#address.value`, `captureWindow`, `readAxTree`.
- **Raw state**: Same tab (index 2) navigated in place: title → "zephyr quartz fresh - Brave Search", `welcomeVisible: false`, committed URL `https://search.brave.com/search?q=zephyr%20quartz%20fresh`, new wcId 5; live Brave results rendered (Brave’s own AI-answer widget errored server-side — unrelated). No welcome surface appeared.
- **Expected**: The tab navigates to a `search.brave.com` search URL containing the query — the chosen engine is live; no welcome surface appears for a search.
- **Verdict**: pass — the same tab committed `https://search.brave.com/search?q=zephyr%20quartz%20fresh` with a live results page; no welcome-surface content on the active tab (a11y grep).
- **Evidence**: `step-5-before.json`, `step-5-after.json`, `step-5-tabs.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`
- **Validator notes**: positive-absence check performed — the only "Welcome to Goldfinch" matches are the still-open first tab’s strip label.

### Checkpoint 6 — PASS
- **Actions taken**: Ctrl+T (4th tab, welcome with the home block only); typed `https://example.com` in the panel’s input and clicked Set (chrome); after-probe, `enumerateTabs`, `captureWindow`; Ctrl+T again; probe, `enumerateTabs`, `captureWindow`.
- **Raw state**: Set navigated the same tab (index 3) to `https://example.com/` (new wcId 6), panel hidden; the next Ctrl+T (5th tab, wcId 7) opened directly on `https://example.com/` with no welcome surface. The background first welcome tab kept its title (not activated; DD7’s auto-attach acts on the shown welcome tab).
- **Expected**: The tab is now on `https://example.com/`. Ctrl+T opens `https://example.com/` — the welcome surface no longer appears for new tabs.
- **Verdict**: pass — the Set tab committed `https://example.com/` (real page rendered, panel hidden) and the next Ctrl+T opened directly on `https://example.com/` (wcId 7 already committed) with no welcome panel.
- **Evidence**: `step-6-before.json`, `step-6-after.json`, `step-6-tabs.json`, `step-6-screenshot.png`, `step-6-probe-2.json`, `step-6-tabs-2.json`, `step-6-screenshot-2.png`
- **Validator notes**: the still-open first welcome tab was correctly left untouched by this row.

### Checkpoint 7 — PASS
- **Actions taken**: Read-only `sqlite3` query at 04:46:22Z, saved verbatim; no UI interaction.
- **Raw state**: `version: 3`; `homePage: "https://example.com"` (stored as typed — the navigation normalized to a trailing slash); `searchEngine: "brave"`; no cross-contamination between the keys; `bookmarksBarEnabled: true` from step 3.
- **Expected**: `[mixed-frame]` `homePage: "https://example.com"`, `searchEngine: "brave"` — each choice wrote exactly its own key.
- **Verdict**: pass — the Validator’s own `sqlite3` read matched: `homePage: "https://example.com"`, `searchEngine: "brave"`, each key holding exactly its own choice.
- **Evidence**: `step-7-settings-row.json`
- **Validator notes**: `homePage` is stored as typed (no trailing slash) while navigation normalizes — a storage-vs-navigation detail, not a spec violation.

## Orchestrator Notes

- **Execution mode**: live two-agent mode; cache mode `cold` — the profile was created by this launch; NO fixture setup (the first launch is the test). Crew: Executor + Validator, Sonnet; no Accessibility Validator; the crew read the crew file's Project Apparatus Notes before `[READY]`; the Executor's pre-`[READY]` check was read-only.
- **Apparatus**: the attach-only CLI with the run's minted admin key (port 49707). The welcome surface and strip are chrome DOM — driven and observed via the chrome wcId; Settings via the `kebabActionSettings` seam with guest-coordinate clicks and a `scroll` to reach the bookmarks-bar toggle. Two apparatus notes for the crew file: the `scroll` tool takes `dx`/`dy` (its own error text says `deltaX`/`deltaY`); DOM-count probes (`querySelectorAll(...).length`) do not reflect visibility — hidden-class checks and, decisively, an a11y-tree scan are the reliable absence signals (the Validator judged step 4 on a zero-radio a11y count, not the probe's `engineRadios: 8`).
- **Checkpoint 3 — FAIL, fixed at the gate, rerun PASS.** The engine block rendered its unset hint; the home-page field rendered none (DD6 had defined only the post-Clear status text). FD ruling: fix in this leg's own surface — `HOME_UNSET_HINT` plus a `reflect(value)` helper applied on load and on every broadcast (symmetric with the engine block), a structural test (suite 3761 → 3762), and the ordering check that `internal-settings-set` broadcasts before it returns so the clicking page's local message still wins. The fix also subsumes **squawk 0010** (stale status after an external write) — now `in-progress`, sign-off with the flight Reviewer. A fresh Settings tab picked up the fix without a relaunch (internal pages are served from disk per request); the profile was not disturbed.
- **DD7 auto-attach scope observed**: the background first welcome tab did not attach when the home page was set from another welcome tab (only the shown welcome tab auto-attaches; the spec row did not assert otherwise). Noted for the debrief.
- **Environment**: `search.brave.com` resolved live (Brave's own AI-answer widget errored server-side — unrelated); `homePage` is stored as typed (no trailing slash) while navigation normalizes.

## Closing Summaries

### Executor closing
Clean end to end. Pitfalls recorded: the `scroll` tool's real parameters are `wcId,x,y,dx,dy` (its runtime error names `deltaX`/`deltaY`); DOM-count probes stay at 8 radios after the block is hidden — the hidden-class check and the a11y tree's absence of any radio node were the reliable signals. The welcome surface behaved predictably: hidden-class toggling was a reliable signal, the welcome tab never carries a wcId until it navigates, and both the Set flow and the engine-radio flow produced real navigations (title change, wcId, committed address). The checkpoint-3 rerun validated on a freshly reopened Settings tab with no relaunch. `homePage` is stored without a trailing slash while the address bar normalizes. No environment hiccups.

### Validator closing
7/7; step 3's first-observation fail was a genuine, reproducible UI asymmetry, fixed and re-verified with matching pixels and a11y. `[mixed-frame]` rows: the fresh-read cross-check is cheap and worth keeping. Rule 7 earned its keep at step 4 — the DOM probe's `engineRadios: 8` would have produced a false read; a11y/rendered checks are load-bearing for "disappeared" claims. The negative-claim guidance ("grep the a11y dump for engine names") turned "nothing there" into a citable zero. `enumerateTabs → []` for a welcome tab reads as a red flag out of context — future specs on this surface should name it. Debrief pattern: the home-page/engine hint asymmetry suggests the two blocks were built in different passes without a parity check — worth a sweep of the surface's state matrix (e.g. a saved-confirmation state).

## Operator Notes

_(none yet)_
