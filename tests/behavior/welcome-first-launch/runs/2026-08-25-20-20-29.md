# Behavior Test Run: welcome-first-launch — 2026-08-25-20-20-29

**Spec**: [welcome-first-launch.md](../welcome-first-launch.md)
**Status**: pass
**Started**: 2026-08-25T20:20:29Z
**Completed**: 2026-08-25T20:38:38Z
**Duration**: 00:18:09
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-first-launch/2026-08-25-20-20-29/` during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. Re-run at the Mission 16 Flight 3 leg-1 gate on the restyled build (brand mark, cards, engine grid): every row read the surface through the frozen DOM contract and every verdict rested on rendered evidence; no verdict touched appearance. The first-launch outcome is unchanged by the restyle — both-unset welcome tab, explicit `null`/`null` row, bookmarks bar present on the surface, hints in Settings, engine-first then home-page choices each writing only their own key.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Observe only: window screenshot, chrome a11y tree, DOM probe (strip, address bar, surface + gated inner blocks, engine heading/radios, `#bookmarks-bar`), `enumerateWindows`, `enumerateTabs`.
- **Raw state**: One strip tab "Welcome to Goldfinch" selected; a11y: heading "Welcome to Goldfinch", textbox "Home page", radiogroup "Search engine" with 8 unchecked radios; address bar empty, placeholder shown, writable; surface visible with both cards, burner note hidden; `#bookmarks-bar` hidden; `activeTabWcId: null`, `enumerateTabs` `[]`.
- **Expected**: Exactly one tab, titled "Welcome to Goldfinch", showing the welcome surface with **both** the home page block and the search engine block; the address bar is empty with its placeholder and is writable; no tab has committed any URL. [a11y]
- **Verdict**: pass — the screenshot shows one selected "Welcome to Goldfinch" tab, both cards (empty home field + Set; 8 unchecked engine cards), the address bar with its placeholder and no committed URL; the `[a11y]` row judged functionally (named heading, textbox, radiogroup with named radios; writable address field).
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-probe.json`, `step-1-windows.json`, `step-1-tabs.json`
- **Validator notes**: the screenshot alone corroborates the row; no DOM/rendered divergence.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document, plus a `json_type` disambiguation query (present-null vs absent).
- **Raw state**: Row present: `version: 3`, `homePage: null`, `searchEngine: null` — both keys present with explicit JSON `null`.
- **Expected**: `[mixed-frame]` The row exists with `version: 3`, `homePage: null`, and `searchEngine: null` — both keys present and explicitly unset (pinned at first load, not merely absent).
- **Verdict**: pass — the Validator read the row JSON itself: `version: 3`, `homePage: null`, `searchEngine: null`, both keys present with literal `null`.
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: the Executor's present-null disambiguation is confirmed by the raw JSON.

### Checkpoint 3 — PASS
- **Actions taken**: Settings via the `kebabActionSettings` seam (guest wcId 2; `evaluate` on the internal guest refused by design — screenshot + a11y only); scrolled to and clicked the "Show bookmarks bar" toggle; returned to the welcome tab by clicking its strip tab; window screenshot, a11y tree, probe.
- **Raw state**: Settings: home field empty with hint "No home page chosen — new tabs will open the welcome page until you set one."; no radio checked with hint "No search engine chosen — the welcome page will ask the first time you search."; toggle OFF → ON. Welcome tab: `#bookmarks-bar` class `hidden` → `''`, rendered as a 30 px empty strip under the address bar; the surface shifted down 30 px; both cards visible.
- **Expected**: The bookmarks bar is visible on the welcome tab (empty, but present) — the bar is not suppressed on this surface. Settings shows an empty home page field and no engine selected, each with its hint.
- **Verdict**: pass — Settings renders the empty home field and no checked radio, each with its hint verbatim; on the welcome tab the bookmarks bar is present (layout shifted 30 px, `#bookmarks-bar` un-hidden with a rect) — an empty bar has no visual signature against the dark background, so the verdict rests on the layout shift plus the class flag.
- **Evidence**: `step-3-settings-1.png`, `step-3-settings-snapshot-1.txt`, `step-3-settings-scrolled.png`, `step-3-settings-2.png`, `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`
- **Validator notes**: spec robustness — adding one bookmark before this row would make the bar visible by content rather than inference.

### Checkpoint 4 — PASS
- **Actions taken**: Clicked the Brave Search radio-card label by rect; captured window, a11y tree, probe, `enumerateTabs`/`enumerateWindows`; Ctrl+T; captured the same set again.
- **Raw state**: After Brave: the welcome tab stays active and viewless (`activeTabWcId: null`, no new entry); the engine card gone, the home card remains; `#welcome-engine-brave` checked. After Ctrl+T: a third strip tab "Welcome to Goldfinch" active, showing the home card only; still viewless.
- **Expected**: The engine block disappears; the home page block remains; the tab stays a welcome tab. A subsequent Ctrl+T opens another welcome tab with only the home page block.
- **Verdict**: pass — after Brave the welcome tab stays active showing only the home card (engine card gone); Ctrl+T opens a third "Welcome to Goldfinch" tab, active, home card only; both viewless as expected.
- **Evidence**: `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-probe.json`, `step-4-tabs.json`, `step-4-windows.json`, `step-4-screenshot-2.png`, `step-4-snapshot-2.txt`, `step-4-probe-2.json`, `step-4-tabs-2.json`, `step-4-windows-2.json`
- **Validator notes**: clean pass; the new tab lacks the engine block because the engine is now globally set (live re-check), not by inheritance.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe + `enumerateTabs`; clicked the address bar, Ctrl+A, typed `zephyr quartz fresh`, Enter; window screenshot, a11y tree, probe (address read twice), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 3 → 3, same position; committed `https://search.brave.com/search?q=zephyr+quartz+fresh&conversation=…` (twice); surface hidden; the welcome tab attached as wcId 4 (active); live Brave results rendered. `enumerateWindows` gained a `sheetWcId: 3` field with `sheetVisible: false` (a resolved sheet, never enumerated as a tab).
- **Expected**: The tab navigates to a `search.brave.com` search URL containing the query — the chosen engine is live; no welcome surface appears for a search.
- **Verdict**: pass — the address bar shows the committed `search.brave.com` search URL with the query and live Brave results render; no welcome surface; the tab attached as wcId 4 on commit.
- **Evidence**: `step-5-before.json`, `step-5-tabs-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs-after.json`, `step-5-windows.json`
- **Validator notes**: `&conversation=` is Brave's own parameter; the transient `sheetWcId: 3` (`sheetVisible: false`) left no visible artifact.

### Checkpoint 6 — PASS
- **Actions taken**: Ctrl+T (home-only welcome tab); typed `https://example.com` in the welcome home block and clicked Set; captured window, a11y tree, probe, `enumerateTabs`; Ctrl+T again; captured the same set.
- **Raw state**: After Set: the same tab (index 3, strip 4 → 4) attached as wcId 5 on `https://example.com/`, surface hidden, Example Domain rendered. After Ctrl+T: a fifth tab, new wcId 6, on `https://example.com/`, active; no welcome surface.
- **Expected**: The tab is now on `https://example.com/`. Ctrl+T opens `https://example.com/` — the welcome surface no longer appears for new tabs.
- **Verdict**: pass — after Set the active tab renders Example Domain at `https://example.com/`; the next Ctrl+T opens a new tab on `https://example.com/` with no welcome surface — both directly visible in rendered pixels.
- **Evidence**: `step-6-before.json`, `step-6-screenshot.png`, `step-6-snapshot.txt`, `step-6-probe.json`, `step-6-tabs.json`, `step-6-screenshot-2.png`, `step-6-snapshot-2.txt`, `step-6-probe-2.json`, `step-6-tabs-2.json`
- **Validator notes**: clean pass, no ambiguity.

### Checkpoint 7 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document plus the key-presence check.
- **Raw state**: Row `homePage: "https://example.com"`, `searchEngine: "brave"` (both present, text); `updated_at` later than step 2; `bookmarksBarEnabled: true` from step 3; every other key unchanged.
- **Expected**: `[mixed-frame]` `homePage: "https://example.com"`, `searchEngine: "brave"` — each choice wrote exactly its own key.
- **Verdict**: pass — the Validator read the row itself: `homePage: "https://example.com"`, `searchEngine: "brave"`, `version` 3 unchanged, `updated_at` later than step 2 — each choice wrote exactly its own key.
- **Evidence**: `step-7-settings-row.json`
- **Validator notes**: `bookmarksBarEnabled: true` is step 3's expected side effect, not contamination.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh empty `XDG_CONFIG_HOME`, the first launch is the test. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled; the `[a11y]` row judged functionally).
- **Purpose**: Flight 3 leg-1 regression net — the restyle must not change any observable this spec reads. Steps 1 and 2 were dispatched as one Executor turn (observe-only); step 7 was dispatched in parallel with checkpoint 6's judgment (a filesystem read cannot disturb live state).
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden. The branch's crew file lacks squawk 0012's notes (PR #169 unmerged at run time); the facts were briefed per spawn. New facts for the crew file: `evaluate` on an internal guest is refused ("internal-session excluded") — screenshot + a11y only; re-activate a viewless welcome tab by clicking its strip rect (`.tab` by title); `enumerateWindows` may gain a `sheetWcId` field (`sheetVisible: false`) after an external-engine navigation — a resolved transient sheet, not an error; nested double quotes in inline `evaluate` JSON need the file + JSON-encode pattern.
- **Spec notes** (for the next revision, not this run): step 3's empty bookmarks bar has no visual signature on the dark background — the verdict rested on the 30 px layout shift plus the class flag; pre-seeding one bookmark would make it visible by content. Rows 2 and 7 carry `[mixed-frame]` but are filesystem-only — drop the tag or add a token browser check.

## Closing Summaries

### Executor closing
All seven steps executed with the requested apparatus, cache-cold from a fresh profile; no blocks. Carry forward: `evaluate` on the Settings guest is refused hard ("internal-session excluded") — screenshot + a11y suffice, don't try DOM eval on internal guests; clicking a viewless welcome tab's strip rect (`.tab` by title, chrome DOM) is the reliable way to re-activate it (no `activateTab` id exists); `enumerateWindows` gained a `sheetWcId` field (with `sheetVisible: false`, never enumerated as a tab) after the Brave navigation — a resolved transient sheet, expect it on external-engine rows and don't treat it as an error; inline CLI JSON with nested double quotes (attribute selectors) needs the write-expression-to-file + JSON-encode pattern.

### Validator closing
7/7 with no divergence between DOM/a11y state and rendered pixels at any step. The Executor's evidence discipline let every verdict be rendered by direct observation (screenshots viewed, row JSON read) rather than prose. The pre-briefed apparatus facts all manifested exactly as predicted. Spec quality: step 3's empty bar is weak to verify by pixels (pre-seed a bookmark); rows 2 and 7 are `[mixed-frame]`-tagged but filesystem-only; the negative claims in steps 5–6 were carried by unambiguous positive evidence, so the INCONCLUSIVE fallback was never needed.

## Operator Notes
_None._
