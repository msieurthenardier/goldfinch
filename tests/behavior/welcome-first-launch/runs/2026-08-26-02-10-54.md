# Behavior Test Run: welcome-first-launch — 2026-08-26-02-10-54

**Spec**: [welcome-first-launch.md](../welcome-first-launch.md)
**Status**: pass
**Started**: 2026-08-26T02:10:54Z
**Completed**: 2026-08-26T02:29:56Z
**Duration**: 00:19:02
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-first-launch/2026-08-26-02-10-54/` during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. Gate re-run on the final Mission 16 Flight 3 HAT build for the two rows re-authored at the HAT: row 4 (choosing an engine keeps the engine card visible with the selection and a confirmation; the next new tab offers the home card only) and row 6 (Set on the welcome home card saves and stays; the next new tab opens the home page). Every other first-launch claim unchanged and re-observed; the Validator took its own screenshots and two DB reads.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Observe only: window screenshot, chrome a11y tree, DOM probe, `enumerateWindows`, `enumerateTabs`.
- **Raw state**: One selected "Welcome to Goldfinch" tab; address bar empty, placeholder, writable; surface visible with both cards (home flex, engine block), burner note hidden; tagline "Set up the two things…"; 8 radios none checked, radiogroup; bookmarks bar hidden; viewless (`activeTabWcId: null`, `enumerateTabs` empty).
- **Expected**: Exactly one tab, titled "Welcome to Goldfinch", showing the welcome surface with **both** the home page block and the search engine block; the address bar is empty with its placeholder and is writable; no tab has committed any URL. [a11y]
- **Verdict**: pass — corroborated by the Validator's own screenshot: one tab, both cards, eight distinct engine cards unselected, address bar empty with placeholder; `[a11y]` judged functionally.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`, `step-1-probe.json`, `step-1-windows.json`, `step-1-tabs.json`
- **Validator notes**: the eight radios are the curated table, not duplication.

### Checkpoint 2 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document.
- **Raw state**: `version: 3`, `homePage: null`, `searchEngine: null` — both keys present with explicit JSON `null`.
- **Expected**: `[mixed-frame]` The row exists with `version: 3`, `homePage: null`, and `searchEngine: null` — both keys present and explicitly unset (pinned at first load, not merely absent).
- **Verdict**: pass — the Validator's own `sqlite3` read confirms `version: 3`, `homePage: null`, `searchEngine: null` present as explicit keys.
- **Evidence**: `step-2-settings-row.json`
- **Validator notes**: independently confirmed.

### Checkpoint 3 — PASS
- **Actions taken**: Settings via the seam (guest wcId 2): screenshot + a11y; scrolled to and clicked "Show bookmarks bar"; returned to the welcome tab by clicking its strip rect; window screenshot, a11y tree, probe.
- **Raw state**: Settings: home field empty with hint "No home page chosen — new tabs will open the welcome page until you set one."; no radio checked with hint "No search engine chosen — the welcome page will ask the first time you search."; toggle off → on. Welcome tab: `#bookmarks-bar` `hidden`/none → `''`/flex, 30 px rect at y 89; the surface shifted to y 119; both cards visible. The empty dark bar now reads as a band above the white surface.
- **Expected**: The bookmarks bar is visible on the welcome tab (empty, but present) — the bar is not suppressed on this surface. Settings shows an empty home page field and no engine selected, each with its hint.
- **Verdict**: pass — the Validator pixel-sampled both screenshots: a ~30 px dark band (y 90–118) present at step 3 and absent at step 1, matching `#bookmarks-bar`'s rect and class change; Settings shows the empty field and unchecked radios, each with its hint verbatim.
- **Evidence**: `step-3-settings-1.png`, `step-3-settings-snapshot-1.txt`, `step-3-settings-scrolled.png`, `step-3-settings-2.png`, `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-probe.json`
- **Validator notes**: the band is the toolbar colour extended, not a contrasting stripe — confirmed by pixel sampling.

### Checkpoint 4 — PASS
- **Actions taken**: Clicked the Brave Search radio-card label by rect; captured window, a11y tree, probe, `enumerateTabs`/`enumerateWindows`; Ctrl+T; captured the same set; a supplementary scrolled screenshot of the first welcome tab (tab re-activated, then the Ctrl+T tab restored).
- **Raw state**: After Brave: the welcome tab stays viewless and active; the engine card stays visible with Brave checked and "Saved — you can always change this in Settings." beneath the grid; the home card remains. After Ctrl+T: a third tab, a welcome tab with the home card only (engine block `hidden`), tagline "Choose where new tabs open…".
- **Expected**: The engine block stays, showing Brave Search selected, with a saved confirmation beneath it; the home page block remains; the tab stays a welcome tab. A subsequent Ctrl+T opens another welcome tab with only the home page block.
- **Verdict**: pass — rendered: the engine card stays with Brave filled and gold-bordered, "Saved — you can always change this in Settings." beneath the grid, the home card above, the tab still a welcome tab; Ctrl+T opened a third tab with the home card only and the home-only tagline.
- **Evidence**: `step-4-screenshot.png`, `step-4-snapshot.txt`, `step-4-probe.json`, `step-4-tabs.json`, `step-4-windows.json`, `step-4-screenshot-2.png`, `step-4-snapshot-2.txt`, `step-4-probe-2.json`, `step-4-tabs-2.json`, `step-4-windows-2.json`, `step-4-screenshot-scrolled.png`
- **Validator notes**: clean match to the HAT pivot text; no DOM/rendered divergence.

### Checkpoint 5 — PASS
- **Actions taken**: Before probe + `enumerateTabs`; clicked the address bar, Ctrl+A, typed `zephyr quartz fresh`, Enter; window screenshot, a11y tree, probe (address read three times), `enumerateTabs`, `enumerateWindows`.
- **Raw state**: Strip 3 → 3, same slot; committed `https://search.brave.com/search?q=zephyr+quartz+fresh&conversation=…`; surface hidden; the welcome tab attached as wcId 4 (active); live Brave results rendered; the transient `sheetWcId` field present (`sheetVisible: false`).
- **Expected**: The tab navigates to a `search.brave.com` search URL containing the query — the chosen engine is live; no welcome surface appears for a search.
- **Verdict**: pass — the address bar committed the `search.brave.com` search URL with the query and live Brave results render; no welcome surface; the chosen engine is live.
- **Evidence**: `step-5-before.json`, `step-5-tabs-before.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-probe.json`, `step-5-tabs-after.json`, `step-5-windows.json`
- **Validator notes**: `&conversation=` is Brave's own parameter, not a Goldfinch artifact.

### Checkpoint 6 — PASS
- **Actions taken**: Ctrl+T (home-only welcome tab); typed `https://example.com` in the welcome home card and clicked Set; captured window, a11y tree, probe, `enumerateTabs`/`enumerateWindows`; Ctrl+T again; captured the same set.
- **Raw state**: After Set: the tab stayed a viewless welcome tab (strip 4 unchanged, address bar empty, no `enumerateTabs` entry) with the field showing `https://example.com` and "Saved — new tabs will open here.", no engine card. After Ctrl+T: a fifth tab, real wcId 5, on `https://example.com/`; no welcome surface.
- **Expected**: The tab stays a welcome tab: the home page field shows `https://example.com` with a saved confirmation (this tab was opened for the home page only — the engine was already set — so no engine card is shown). Ctrl+T opens `https://example.com/` — the welcome surface no longer appears for new tabs.
- **Verdict**: pass — rendered: after Set the tab is still "Welcome to Goldfinch" with the field showing the saved value and "Saved — new tabs will open here.", no engine card; the next Ctrl+T opened a fifth tab rendering example.com with no welcome surface.
- **Evidence**: `step-6-before.json`, `step-6-tabs-before.json`, `step-6-screenshot.png`, `step-6-snapshot.txt`, `step-6-probe.json`, `step-6-tabs.json`, `step-6-windows.json`, `step-6-screenshot-2.png`, `step-6-snapshot-2.txt`, `step-6-probe-2.json`, `step-6-tabs-2.json`
- **Validator notes**: matches the DD7-pivoted row precisely.

### Checkpoint 7 — PASS
- **Actions taken**: Read-only `sqlite3` select of the settings document (payload + `updated_at`).
- **Raw state**: `homePage: "https://example.com"`, `searchEngine: "brave"`; `bookmarksBarEnabled: true` from step 3; other keys unchanged.
- **Expected**: `[mixed-frame]` `homePage: "https://example.com"`, `searchEngine: "brave"` — each choice wrote exactly its own key.
- **Verdict**: pass — the Validator's own `sqlite3` read: `homePage: "https://example.com"`, `searchEngine: "brave"` — each choice wrote exactly its own key; `bookmarksBarEnabled` persisted from step 3.
- **Evidence**: `step-7-settings-row.json`, `step-7-settings-row-raw.txt`
- **Validator notes**: 7/7; no unintended reset of unrelated settings.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation); cache mode `cold` — fresh empty `XDG_CONFIG_HOME`. Crew: Executor + Validator, Sonnet; no Accessibility Validator (project-disabled; the `[a11y]` row judged functionally). Steps 1–2 dispatched as one Executor turn; step 7 in parallel with checkpoint 6's judgment.
- **Purpose**: second of four gate runs for the HAT's behavior changes (item 3 — engine block stays; item 6 — Set stays put, DD7 pivot). Both re-authored rows passed on rendered evidence.
- **Spec premise correction before the run**: row 6's re-authored text had claimed "the engine card still shows Brave selected" on the Ctrl+T welcome tab — false at the value level (that record is opened for `home` only once the engine is set, and the engine block is reason-driven); corrected before the run.
- **Apparatus**: attach-only CLI with the run's minted admin key (port 49707); session-registered goldfinch MCP tools forbidden; block visibility by className/computed display; the empty bookmarks bar confirmed by pixel-sampling the 30 px band above the now-white surface. The transient `sheetWcId` field appeared from step 5 (`sheetVisible: false`).
- **Spec notes for the next revision**: rows 4 and 6 fold a follow-on Ctrl+T into the Expected Result cell — split into their own rows; the stored `homePage` is as typed (no trailing slash) while navigation normalizes — do not diff literally; Brave adds `&conversation=` to its search URL.

## Closing Summaries

### Executor closing
All seven steps without a block or exception. Carry forward: `enumerateWindows` gained `sheetWcId: 3` (`sheetVisible: false`) from step 5 and kept it — the known transient field (wcId 3 never enumerated as a tab); the eight radios are the curated table (the step-1 duplication worry was a false alarm); the row stores `homePage` exactly as typed (no trailing slash) while navigation normalizes to `https://example.com/` — don't diff the two literally; an empty bookmarks bar is a thin dark strip visually indistinguishable from the toolbar — the class/display/rect evidence carries it; the "Show bookmarks bar" toggle is below the fold on a 1080-tall Settings page. The apparatus notes held throughout.

### Validator closing
7/7 with independent screenshots, a11y reads, and two direct DB queries corroborating every step; no DOM-vs-rendered divergence. The eight radios are the curated table; the empty bar's 30 px band was confirmed by pixel sampling; the HAT-revised rows 4 and 6 were met precisely; Brave's `&conversation=` parameter is engine-side. Spec quality: rows 4 and 6 fold a follow-on action into the Expected Results cell — split them in a future revision.

## Operator Notes
_None._
