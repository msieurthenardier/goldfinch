# Behavior Test Run: page-zoom — 2026-06-18-16-49-55

**Spec**: [tests/behavior/page-zoom.md](../../page-zoom.md)
**Status**: partial
**Started**: 2026-06-18T16:30:00Z
**Completed**: 2026-06-18T16:49:55Z
**Duration**: ~00:20:00
**Executor**: Flight-Director-driven (script Executor agent over the goldfinch example MCP client)
**Validator**: Flight Director (independent act/judge split preserved)

**Evidence (ephemeral, local-only, not committed)**: raw tool results captured in-agent during the run; re-derivable by re-running.

## Summary

6 / 7 checkpoints PASS. 0 FAIL. 1 not-live-reachable (step 7 — internal-tab refusal: the automation surface structurally cannot obtain a `goldfinch://settings` wcId; op-local guard is unit-proven, live confirmation deferred to HAT).

> **Apparatus deviation (Orchestrator note):** the formal two-agent `/behavior-test` apparatus expects the goldfinch MCP surface to be a **registered session MCP**, which it is not in this session (and can't be added without a reload). Per operator decision, the Flight Director drove the live gate via goldfinch's **own example MCP client** (`scripts/lib/mcp-client.mjs`) over Bash: a script-Executor agent performed the actions and reported raw state; the Flight Director rendered the Validator verdicts. The Witnessed act/judge separation is preserved (distinct actor vs. judge). Run on port **49801** (WSL2 default 49707 is held by an external Windows service). Key: **admin** (env-gated dev mint).

## Checkpoint Results

### Checkpoint 1 — PASS (setup)
- **Actions taken**: `openTab https://example.com` (default jar); `enumerateTabs`.
- **Raw state**: new tab wcId 3, jarId "default", active.
- **Expected**: (setup — no judgment).
- **Verdict**: pass — tab created.

### Checkpoint 2 — PASS
- **Actions taken**: `getZoom{3}`; `evaluate{3, "window.devicePixelRatio"}`.
- **Raw state**: factor 1.0; devicePixelRatio 1.
- **Expected**: factor ≈1.0; dpr at baseline.
- **Verdict**: pass.

### Checkpoint 3 — PASS
- **Actions taken**: `pressKey{3, "=", ["control"]}` ×2 (drives the guest `before-input-event`); `getZoom`; `evaluate` dpr.
- **Raw state**: factor 1.25; dpr 1.25.
- **Expected**: factor >1.0 AND dpr increased — keyboard zoom-in (page-focused) reaches the page.
- **Verdict**: pass — confirms the main-side `before-input-event` capture drives guest zoom live.

### Checkpoint 4 — PASS
- **Actions taken**: `pressKey{3, "0", ["control"]}`; `getZoom`; `evaluate` dpr.
- **Raw state**: factor 1.0; dpr 1.
- **Expected**: factor back to ≈1.0; dpr back to baseline.
- **Verdict**: pass.

### Checkpoint 5 — PASS
- **Actions taken**: `setZoom{3, 1.5}`; `getZoom`; `evaluate` dpr.
- **Raw state**: returned factor 1.5; getZoom 1.5; dpr 1.5.
- **Expected**: getZoom ≈1.5 AND dpr increased.
- **Verdict**: pass — the MCP tool applies zoom to web content.

### Checkpoint 6 — PASS
- **Actions taken**: `openTab https://example.org` jarId "work" (different origin, different jar); `getZoom{4}` with tab 1 still at 1.5.
- **Raw state**: wcId 4 (jar "work"); getZoom{4} factor 1.0; getZoom{3} still 1.5.
- **Expected**: getZoom(wcId2) ≈1.0 — zoom did not leak across jars.
- **Verdict**: pass — no cross-jar leak (separate jar sessions). (Consistent with the leg-1 DD1 finding: cross-jar is the asserted isolation boundary; same-origin same-jar sharing is per-origin-per-session and out of scope here.)

### Checkpoint 7 — SKIPPED (not live-reachable)
- **Actions taken**: attempted to obtain a `goldfinch://settings` internal wcId via the automation surface.
- **Raw state**: `openTab "goldfinch://settings"` → null (refused); `enumerateTabs` filters internal tabs out — no internal wcId obtainable via the surface.
- **Expected**: `setZoom(internalWcId, 1.5)` under admin refused by the op-local internal guard.
- **Verdict**: not reachable via the automation surface — which is itself the boundary working (the surface structurally cannot target internal pages). The op-local internal guard (admin path, `allowInternal:true`) is **unit-proven** in `test/unit/automation-zoom.test.js`. Live confirmation deferred to HAT, where a human opens settings out-of-band and the test reads its wcId.

## Orchestrator Notes
- Apparatus deviation (above): example-client drive + Flight-Director Validator.
- Port 49801 (49707 collision with an external Windows service under WSL2 loopback).
- Spec-quality finding: step 7 assumes an internal wcId can be obtained and fed to the op; via the pure automation surface it cannot. The spec's "open it via the trusted path" implies an out-of-band (human) open — reachable only in HAT, not an autonomous automation run. No spec change required; noted for the HAT pass.

## Operator Notes
- **2026-06-18 — Step 7 (internal-tab no-op) HAT-confirmed.** Operator opened `goldfinch://settings`
  and verified `Ctrl +`/`Ctrl 0` and address-bar hover produce no zoom / no control, with no errors —
  the internal-tab boundary holds on the user path (complements the unit-proven op-local guard).
