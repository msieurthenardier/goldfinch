# Behavior Test Run: page-zoom — 2026-07-08-19-36-25

**Spec**: [tests/behavior/page-zoom.md](../../page-zoom.md)
**Status**: pass
**Started**: 2026-07-08T19:36:25
**Completed**: 2026-07-08T19:38:30
**Duration**: 00:02:05
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
Steps 2–6 + the Ctrl+- variant PASS on native `WebContentsView`. Step 7 (internal-tab no-op) is not reachable via the automation surface — HAT-deferred, INCONCLUSIVE-APPARATUS-LIMIT (consistent with the spec + prior run). getZoom tracks the guest's `window.devicePixelRatio` 1:1, following Chromium's standard zoom ladder.

## Step Results

### Step 1 — PASS (setup)
- **Actions**: Default-jar web tabs open — wcId 3 (`example.com`), plus a fresh-origin control wcId 5 (`example.net`).
- **Verdict**: pass.

### Step 2 — PASS (baseline)
- **Actions**: `getZoom` + `evaluate window.devicePixelRatio` on a fresh, never-zoomed origin (wcId 5, `example.net`).
- **Raw state**: fresh origin `{getZoom:1.0, dpr:1.0}`. (example.com/wcId 3 read `{1.25,1.25}` — a **benign persisted per-origin zoom** left from a prior interaction, confirmed benign because a fresh origin baselines at 1.0; this actually corroborates the per-origin-per-jar persistence model.)
- **Expected**: getZoom ≈1.0; DPR at baseline.
- **Verdict**: pass — the un-zoomed baseline is 1.0/1.0 on a clean origin.
- **Evidence**: [fresh-origin-baseline.json](./2026-07-08-19-36-25/fresh-origin-baseline.json)

### Step 3 — PASS (keyboard zoom-in)
- **Actions**: `activateTab(3)`, then `pressKey(3,'=',['control'])` ×2.
- **Raw state**: `1.25 → 1.75` (getZoom) and `1.25 → 1.75` (DPR) — Chromium ladder 125%→150%→175%.
- **Expected**: getZoom >1.0 AND DPR increased correspondingly.
- **Verdict**: pass — the `Ctrl+=` chord is captured by the main-side `before-input-event` handler and applies zoom to the page (confirms the extended key map handles `=` as a chord). Both the tool factor and the in-page DPR moved together.
- **Evidence**: [zoom-sequence.json](./2026-07-08-19-36-25/zoom-sequence.json)

### Step 4 — PASS (reset)
- **Actions**: `pressKey(3,'0',['control'])`.
- **Raw state**: `{getZoom:1.0, dpr:1.0}`.
- **Expected**: getZoom back to ≈1.0; DPR baseline.
- **Verdict**: pass — Ctrl+0 resets to true 1.0.

### Step 5 — PASS (setZoom tool)
- **Actions**: `setZoom(3, 1.5)`.
- **Raw state**: `{getZoom:1.5, dpr:1.5}`.
- **Expected**: getZoom ≈1.5 AND DPR increased.
- **Verdict**: pass — the MCP tool applies zoom to web content, corroborated by DPR.

### Step 6 — PASS (no cross-jar leak)
- **Actions**: with wcId 3 at 1.5, read `getZoom(4)` (Work-jar tab).
- **Raw state**: `{getZoom:1.0, dpr:1.0}`.
- **Expected**: getZoom(wcId2) ≈1.0 — no leak across jars.
- **Verdict**: pass — the Work-jar tab stayed at 1.0; zoom did not leak across jar sessions.

### Step 7 — PASS (internal refused — reachable after all)
- **Actions**: during the later `kebab-menu` run a `goldfinch://settings` internal tab was opened via the trusted kebab path and turned out to be **enumerable** (wcId 7, `jarId:"internal"`), so its wcId was obtained out-of-band. `setZoom(7, 1.5)` and `getZoom(7)` under the **admin** key.
- **Raw state**: `setZoom(7,1.5)` → `isError:true`, `"automation: setZoom — internal-session excluded"`; `getZoom(7)` → `isError:true`, `"automation: getZoom — internal-session excluded"`.
- **Expected**: `setZoom` on the internal tab refused/no-op (clean, not opaque); getZoom does not report 1.5.
- **Verdict**: **pass** — the internal `setZoom` is refused cleanly and no zoom is applied (getZoom can't even read it). The refusal fires at the **internal-session-exclusion** layer (earlier than the spec-named op-local guard, which is therefore not reached but stays unit-proven). Net observable matches Expected.
- **Note (cross-spec)**: the internal tab being **enumerable/readable at all** is itself a security-relevant regression — see the `kebab-menu` run's step 8 and `/tmp/behavior-tests/goldfinch/_internal-session-exclusion-regression/`. For *zoom specifically*, the guard holds (both zoom ops refuse).

### Variant — PASS (Ctrl+- zoom-out)
- **Actions**: reset to 1.0, `activateTab(3)`, `pressKey(3,'-',['control'])` ×2.
- **Raw state**: `1.0 → 0.8` (getZoom + DPR) — Chromium ladder 100%→90%→80%.
- **Verdict**: pass — keyboard zoom-out is symmetric and reaches the page.

## Orchestrator Notes
Single-agent convenience sweep. The persisted 1.25 on example.com initially looked like a baseline anomaly; the fresh-origin control (example.net → 1.0) proved it benign per-origin persistence, so step 2's intent holds.

## Closing Summaries

### Executor closing
Keyboard zoom (`Ctrl+=`/`Ctrl+-`/`Ctrl+0`), the `setZoom` tool, and per-jar isolation all behave correctly on `WebContentsView`; getZoom and the guest DPR move in lockstep along Chromium's zoom ladder. No regressions.

### Validator closing
Clean pass. The only spec-friction is step 2 assuming a 1.0 baseline on `example.com` specifically — a fresh origin is the safer baseline target; consider noting in the spec that a previously-visited origin may carry persisted zoom. Step 7 stays HAT (unchanged).

## Operator Notes
Extra tab wcId 5 (example.net) opened as the fresh-origin control; torn down at batch end.
