# Behavior Test Run: find-overlay-geometry — 2026-07-15-02-09-30 (regression re-run)

**Spec**: [find-overlay-geometry.md](../../find-overlay-geometry.md) (UNMODIFIED — regression re-run)
**Status**: pass
**Started**: 2026-07-15T02:09:30Z
**Completed**: 2026-07-15T02:27:00Z (approx)
**Duration**: ~00:17
**Executor**: fresh agent (batched)
**Validator**: fresh agent (single judgment pass, pixels inspected directly)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/find-overlay-geometry/2026-07-15-02-09-30/` during the run; re-derivable by re-running the spec.

## Purpose

Third leg of the F6 leg-2 single-window regression TRIPLE — chosen because
it covers the two surfaces the other runs leave dark: the **find-overlay
conversion** (inline `mainWindow` derefs → registry) and **class-1b
window-event routing** (`window-maximized-change` + `trigger-send-bounds`
to the window's own chrome).

## Summary

**All 7 required checkpoints PASS — REPRODUCES** (reference: 2026-07-02
run). Float-not-inset (0.0 px landmark delta), panel reflow flush with the
narrowed guest, maximize/restore event routing verified with direct
event-counter evidence at the chrome BOTH directions with the bar tracking
one genuine grow (2560×1392) and one genuine shrink (1400×900),
hidden-on-internal (zero bar pixels, absence authoritative), menu
hide/restore with query+count intact, clean close to a baseline-equivalent
frame. Optional step 8 reopen: PASS-with-note (see errata below). No
regression from the F6 registry conversion in this spec's coverage.

## New apparatus deviation (codify in spec caveats)

**WSLg Wayland maximize lag-by-one**: `win.maximize()/unmaximize()` flip
Electron state and fire events immediately, but the compositor applies
each bounds change one toggle LATE (the 2026-07-02 run resized
synchronously on the X11 launch path). Product-side routing is provably
correct (instrumented `window-maximized-change`/`trigger-send-bounds`
counters); judge geometry on the resizes that actually land. A run can END
with main believing "maximized" while bounds lag — venue, not product.

## Errata / spec-wording note

Step 8's optional "fresh/reset on next open": observed behavior is the
prior query re-seeded with FULL select-all (type-to-replace — chrome-held
per-tab `findText`, matching native find-bar semantics), not an emptied
input. Satisfies "reset-to-ready-for-overtype"; fails a literal
"empty query" reading. FD ruling: the observed behavior is the designed
one (per-tab findText restore is spec'd in step 6); the wording should say
"query re-seeded and fully selected" — errata for the next spec touch, not
a failure.

## Anomaly logged separately (not this spec's scope)

The fresh-profile boot opened TWO startup tabs (wcId 2+3, both the home
URL). The Validator confirmed zero contamination of any checkpoint (both
sat background/untouched; probe walk skipped them; guest-region geometry
depends only on the active tab). Triaged separately under the F6 flight
log (leg 2 gate).

## Checkpoint Results (compact — regression mode)

| # | Verdict | Anchor evidence |
|---|---------|-----------------|
| 1 | PASS | step1-baseline.png, step1-litmus-raw.txt |
| 2 | PASS | step2-find-open.png (0.0 px landmark delta) |
| 3 | PASS | step3-panel-open.png / step3-panel-closed.png |
| 4 | PASS | step4-restore-raw.txt / step4b-cycle-raw.txt (event counters), step4-restored.png / step4b-restored.png |
| 5 | PASS | step5-internal-tab.png (zero bar pixels) |
| 6 | PASS | step6-menu-open.png / step6-after-dismiss.png / step6-after-switch.png |
| 7 | PASS | step7-closed.png (baseline-equivalent) |
| 8 (opt) | PASS-with-note | step8-reopened.png (select-all re-seed) |

## Orchestrator Notes

- Regression mode (fresh Executor batched + fresh Validator single-pass
  with direct pixel inspection). Scratch profile; core-browsing-shields
  fixture on :8000; admin key env-only, mint line redacted from the
  persisted stdout log.
- Completes the leg-2 regression TRIPLE (with `tab-context-menu`
  2026-07-15-01-40-14 and `closed-tab-reopen` 2026-07-15-01-51-04) —
  all three REPRODUCE.
