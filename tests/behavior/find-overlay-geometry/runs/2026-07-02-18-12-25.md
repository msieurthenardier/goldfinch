# Behavior Test Run: find-overlay-geometry — 2026-07-02-18-12-25

**Spec**: [tests/behavior/find-overlay-geometry.md](../../find-overlay-geometry.md)
**Status**: pass
**Started**: 2026-07-02T18:12:25Z
**Completed**: 2026-07-02T18:38:00Z
**Duration**: 00:25:35
**Executor**: sub-agent (steps 1–6) + re-spawned sub-agent (step 7 — context loss recovery)
**Validator**: sub-agent (all checkpoints)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/find-overlay-geometry/2026-07-02-18-12-25/` during the run (27 files);
re-derivable by re-running the spec.

## Summary

6 / 6 checkpoints passed. 0 failed; 0 inconclusive. Run against the live Flight-7 build
(`flight/07-find-overlay-view` @ `e5daeca` + `0c357fc`), loopback MCP on a pinned port at the admin
tier. Apparatus-wiring litmus passed at Executor-READY and re-confirmed at step 1 (the Flight-4
mis-wiring failure did not recur). Live two-agent mode (SendMessage continuation); every verdict
grounded in screenshots the Validator inspected directly.

## Checkpoint Results

### Step 1 — SKIPPED (setup row)
- **Actions taken**: litmus re-confirmed (chrome wcId 1, admin, this instance); NEW Wikipedia tab
  opened per cold-cache protocol (guest wcId 5, Default jar); baseline captured (find closed).
- **Raw state**: guest top strip y≈84; header row y≈100-140; banner y≈180-260; title y≈307;
  full-height composite. Load-bearing baseline for step 2.
- **Evidence**: `step-1-screenshot.png`, `step-1-litmus.json`, `step-1-tabs.json`

### Checkpoint 2 — PASS (float-not-inset)
- **Actions taken**: Ctrl+F on guest; overlay wcId probed (= 3, found in the id-space GAP below the
  newest tabs — spec's "above" hint is wrong in direction, see errata); typed "goldfinch" into the
  overlay (corroborated: overlay `#find-count` = 1/65).
- **Raw state**: bar floats top-right (~x1008-1385, y92-138) ON TOP of guest content (overlaps the
  header links it occludes); ALL baseline landmarks at identical y — guest NOT pushed down; no
  band/inset anywhere.
- **Expected**: bar renders as a floating strip; guest content NOT pushed down. [render-correct]
- **Verdict**: pass — Validator inspected both screenshots; landmarks unchanged within tolerance;
  overlap of previously-visible header links is floating-overlay compositing, not an inset.
- **Evidence**: `step-2-screenshot.png`, `step-2-screenshot-1-after-ctrlf.png`, `step-2-probe.json`,
  `step-2-probe-wc3.json`, `step-2-overlay-dom.json`

### Checkpoint 3 — PASS (position-sync: panel)
- **Actions taken**: media panel toggled open/closed via `#toggle-media` (DOM-anchored locate).
- **Raw state**: panel open → bar reflowed to ~x645-1028, right edge flush with the NARROWED guest's
  right edge (not the window's); panel closed → bar restored to the full-width position matching
  step 2. No drift, no stranding.
- **Expected**: bar reflows with the narrower guest; restores on close. [render-correct]
- **Verdict**: pass — right-edge tracking unambiguous in the open-panel grab.
- **Evidence**: `step-3-screenshot.png`, `step-3-screenshot-2.png`, `step-3-locate.png`,
  `step-3-actions.json`

### Checkpoint 4 — PASS (position-sync: maximize/restore)
- **Actions taken**: maximize/restore via the custom `#win-max` control (title flipped
  Maximize↔Restore; window 1400x900 ↔ 2560x1392).
- **Raw state**: maximized → bar at the NEW guest's top-right (~x2165-2543), nothing stranded at the
  old bounds; restored → bar back at the step-2 position; exactly one bar in every grab.
- **Expected**: overlay tracks the guest's top strip at both sizes; not stranded. [render-correct]
- **Verdict**: pass — the guest genuinely reflowed (not scaled) at 2560px and the overlay tracked it,
  the stronger form of the assertion.
- **Evidence**: `step-4-screenshot.png`, `step-4-screenshot-2.png`, `step-4-actions.json`

### Checkpoint 5 — PASS (internal-tab hidden, DD7)
- **Actions taken**: kebab → Settings opened (single-script click sequence after Attempt 1 showed the
  menu does not survive an intervening capture); internal tab wcId 6 active.
- **Raw state**: full top strip of the guest region examined — zero find-bar pixels, no fragment/
  shadow/strip artifact; Settings renders full-height.
- **Expected**: overlay absent on the internal tab — hidden, not merely empty. [render-correct]
- **Verdict**: pass — absence is AUTHORITATIVE this run (steps 2-4 grabs proved the active capture
  path composites overlays), not best-effort under the WSLg caveat.
- **Evidence**: `step-5-screenshot.png`, `step-5-menu-open.png`, `step-5-actions.json`,
  `step-5-locate.json`

### Checkpoint 6 — PASS (freeze-hide/restore, DD5)
- **Actions taken**: switched back to the web tab (find restored per-tab with query + count — spec
  concern #4 incidentally evidenced); kebab opened; menu-open state DOM-bracketed BEFORE and AFTER the
  capture (the step-5 quirk did not recur); dismissed via Escape.
- **Raw state**: menu-open grab → menu visible, overlay GONE (the guest header text the bar occludes
  was visible in its exact strip); after dismiss → overlay restored at identical bounds, "goldfinch" +
  1/65 intact (DOM-corroborated).
- **Expected**: overlay hidden while frozen; restored at correct bounds on dismiss. [render-correct]
- **Verdict**: pass — both halves on rendered pixels; timing artifact ruled out by the DOM bracketing.
- **Evidence**: `step-6-screenshot.png`, `step-6-screenshot-2.png`, `step-6-after-switch.png`,
  `step-6-actions.json`

### Checkpoint 7 — PASS (close)
- **Actions taken**: Escape sent to the overlay wcId (by the re-spawned Executor — see Orchestrator
  Notes); final capture.
- **Raw state**: bar gone; previously-occluded header links fully visible; all highlighting cleared;
  frame pixel-equivalent to the step-1 baseline (+ the Settings tab in the strip); no residual
  strip/inset. Supplementary: overlay wc survives HIDDEN with stale "1/65" — the designed
  lazy-singleton lifecycle (reset-on-next-open contract), not composited.
- **Expected**: overlay gone; guest occupies the full region, no residual strip/inset.
- **Verdict**: pass — rendered state governs; clean return-to-baseline close.
- **Evidence**: `step-7-screenshot.png`, `step-7-overlay-probe.json`, `step-7-tabs.json`,
  `step-7-dom.json`

## Orchestrator Notes

- **Mode**: live two-agent (SendMessage continuation available). Both agents Sonnet per crew policy.
- **Cache mode**: cold (spec default) — freshly-launched instance; NEW tab opened for the run.
- **Apparatus**: no session goldfinch MCP registered; both agents used the repo's authenticated node
  client (`scripts/lib/mcp-client.mjs`) against the operator's live instance (pinned port, admin key)
  — the proven Flight-7 leg apparatus. Wiring litmus passed at READY and step 1.
- **Executor re-spawn before step 7**: the original Executor's transcript became unretrievable after
  step 6 (SendMessage resume error). Recovered per the skill's context-loss protocol: fresh Executor
  spawned with the spec + established apparatus + a precise current-state block; the re-spawn resumed
  blind with no verification capture needed and evidence quality did not degrade (Validator concur).
  Lesson: a mandatory CURRENT-STATE block in re-spawn prompts makes Witnessed-run recovery safe.
- **Setup-row handling**: row 1 folded per protocol (no Validator call); litmus checked by the
  Orchestrator.
- **Operator interventions**: none — no checkpoint failed.
- **Variants**: not run (kebab only; container ▾ and page-context freeze triggers unexercised this
  run). The Flight-7 HAT (leg 4, step 8) verified all three freeze triggers on-screen the same day.

## Closing Summaries

### Executor closing (re-spawn, step 7)
Clean single-action resume: the handoff state block was accurate; `pressKey` on the probed overlay
wcId remains the load-bearing technique. Key observable-taxonomy note: **hidden ≠ destroyed** — after
close, the overlay's DOM persists (stale count intact), so a DOM probe on the overlay wcId can NEVER
serve as a "find is closed" observable; pixels are the only closed-state authority, exactly as the
spec's Observables section warns. capture fidelity more than adequate; no anomalies, no connection
flakiness; steps 1–6 left no state drift.

### Validator closing
All six checkpoints pass on directly-inspected pixels; no verdict needed independent re-observation.
Most valuable emergent finding: **absence-authoritativeness** — because same-run grabs showed the
overlay compositing, overlay-absence checks (steps 5–7) became authoritative rather than best-effort;
the spec should codify this as a mechanical precondition. Errata/codification candidates in priority
order: (1) probe-direction errata ("above" → "around" the known ids — the overlay sat BELOW the
guest ids); (2) pixel-tolerance band for step 2 (≤5px noise, >10px = push-down fail); (3) the menu
DOM-bracketing technique (step 6's verdict depended on it); (4) DOM-anchored control location
(`#toggle-media`, `#win-max` + title-flip state confirmation). Coverage gaps (not failures): freeze
variants unexercised (1 of 3 triggers); reset-on-next-open contract unasserted (cheap final
reopen-check would close it); find-state persistence across tab switches is an implicit dependency
worth promoting to an explicit expected result. The spec is a sound, re-runnable regression net —
with the errata, a robust one.

## Operator Notes

Run executed during the Flight-7 leg-4 HAT (step 12), immediately after the HAT-1 fix (`e5daeca`)
landed. The on-screen HAT (steps 1–11) covered the interactive/judgment surface including the freeze
variants this run skipped; together they discharge the flight's verification plan.
