# Behavior Test Run: jar-data-surfaces — 2026-07-17-23-48-56

**Spec**: [jar-data-surfaces.md](../../jar-data-surfaces.md)
**Status**: partial
**Started**: 2026-07-17T23:48:56Z
**Completed**: 2026-07-18T01:20:00Z (approx; teardown verified)
**Duration**: ~01:30:00
**Executor**: sub-agent (Sonnet, live-continuation mode)
**Validator**: sub-agent (Sonnet, live-continuation mode)
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/jar-data-surfaces/2026-07-17-23-48-56/` during the run; re-derivable by re-running the spec.

## Summary

6 / 7 judged checkpoints passed. Checkpoint 6 FAILED on one of its four
clauses — the cookie-removal-by-age claim, which is **structurally
unobservable on a jar's first-ever sweep** (the deliberate, unit-pinned
cold-start stamp-before-expire ordering stamps every unseen cookie
`firstSeenMs=now` in the same pass) — and passed its other three clauses
(storage/history sweep live-validated; DD10 auto-repaint proven). Operator
disposition: **accepted as a spec premise gap, not a product defect**; the
spec's step 6 was amended in-conversation to the honest cold-start
semantics, and the live cookie-removal witness is **HAT-scoped**
(cross-session observation against a populated `cookie_seen` table).

## Checkpoint Results

### Checkpoint 2 — PASS (setup step 1 folded in)
- Fixture seeded in jar `work` (cookies `bt_fixture_alpha`/`bt_fixture_beta` + IndexedDB `bt_fixture_db` on https://example.com; jarId verified). Cookies panel lists all 10 jar cookies (8 real + 2 fixture) with name/domain/expiry; **DD7 hard gate held**: zero cookie-value occurrences in DOM/AX (validator grepped the actual seeded values); session read (10) matches the rendered rows identity-for-identity, including disambiguating two same-name OTZ cookies on different domains.
- Evidence: `step-2-cookies-panel.png`, `step-2-cookies-panel-dom.html`, `step-2-cookies-ax.txt`, `step-2-session-cookies.txt`

### Checkpoint 3 — PASS
- Per-cookie delete (no confirm — by design): `bt_fixture_alpha` gone from panel and session; `bt_fixture_beta` survives. (An unrelated real cookie synced in mid-test; identities, not raw counts, carried the verdict.)
- Evidence: `step-3-after-delete.png`, `step-3-after-delete-ax.txt`, `step-3-session-cookies.txt`

### Checkpoint 4 — PASS
- Site-data panel: 9 origins; exactly one ("https://example.com", the IndexedDB-seeded fixture) badged **Has stored data**; 8 history-derived origins badged **Visited — storage unconfirmed**; known-gap note present verbatim (all three clauses); zero usage/quota figures (grep-verified).
- Evidence: `step-4-sitedata-panel.png`, `step-4-sitedata-dom.html`, `step-4-sitedata-ax.txt`

### Checkpoint 5 — PASS
- Per-origin delete: example.com row **downgrades** to Visited (never disappears; history intact at 153 visits); `indexedDB.databases()` on the live fixture page returns `[]`; `bt_fixture_beta` cookie untouched (data-class boundary held). A 10→9 cookie-count shift was traced to an unrelated cookie's natural wall-clock expiry (validator cross-checked expiry timestamp vs file mtime).
- Evidence: `step-5-after-delete.png`, `step-5-after-delete-dom.html`, `step-5-idb-recheck.txt`, `step-5-session-cookies.txt`

### Checkpoint 6 — FAIL (disposition: accepted — spec premise gap; spec amended; HAT-scoped witness)
- Retention set to the 1-day floor via `window.goldfinch.jarsSetRetention({id:'work',days:1})` (signature verified pre-call). **Passed clauses**: history pruned 153→56 on the same pass; site-data origins 9→5 (aged-out origins removed — the storage sweep discriminated by last-activity age); freshly re-seeded fixture IndexedDB survived ("Has stored data" intact); **both panels repainted with NO manual refresh — the DD10 `jar-data-changed` sweep-completion broadcast proven live**. **Failed clause**: cookies 10→10, zero removed — `retention-sweep.js` `sweepCookies` cold-start-stamps every unseen cookie in the same pass (deliberate, pinned ordering preventing delete-what-you-just-stamped); on schema v2's first-ever boot no cookie can exceed any window. The Executor read the code and reported the code-predicted outcome BEFORE acting; the Validator independently read the same code and confirmed mechanism, not just symptom. Working-as-coded ≠ spec-as-written: honest FAIL.
- **Operator decision**: continue; amend spec (done — step 6 now states the first-run observable is cookie SURVIVAL/stamping, with removal-by-age unit-covered and the live witness HAT-scoped cross-session); carry the witness to the M10 HAT flight.
- Evidence: `step-6-preseed-state.txt`, `step-6-presweep.png`, `step-6-postsweep.png`, `step-6-postsweep-dom.html`, `step-6-post-cookies.txt`, `step-6-history-note.txt`

### Checkpoint 7 — PASS
- Manual Clear-cookies (real confirm dialog — region clears are gated, unlike per-item deletes): panel empty + session read `total=0`; site-data panel byte-identical (example.com IndexedDB survived); history unchanged at 56.
- Evidence: `step-7-after-clear.png`, `step-7-after-clear-dom.html`, `step-7-post-cookies.txt`, `step-7-sitedata-check.txt`

### Step 8 (teardown) — completed
- Clean quit via `appQuit()` bridge; backup sha256 verified pre-restore; profile restored — decisive rollback proof: restored `app.db` carries the pre-migration v1 schema (`documents` only, no `cookie_seen`); zero processes; evidence dir key-free (final sweep).

## Orchestrator Notes

- Live two-agent mode; cache cold; checkpoints 3-5 batched in one Executor
  turn (individually judged); operator decisions under the mission's
  autonomous pre-authorization.
- **Two apparatus incidents (Executor, both self-caught and recovered)**:
  (1) the briefed launch command redirected app stdout — which prints the
  dev-mint key line by design — straight into the evidence dir; the
  Executor redacted it in place before finalizing evidence. (2) That
  redaction overwrote the only key copy (no private copy first) → admin
  tier lost mid-run → SIGTERM (clean-exit-verified; the bridge was
  unreachable without a key) + relaunch + fixture-survival verification
  before proceeding. **Lesson (spec-header material for future runs):
  launch stdout goes to a PRIVATE non-evidence path; the evidence copy is
  an explicitly redacted derivative. Coordinate-click driving:
  re-screenshot immediately before every mutating click (hard rule).**
- **Validator practice worth normalizing** (its closing): when an Executor
  report leans on a code-predicted outcome, the Validator reads the cited
  source itself — mechanism confirmation, not symptom trust.
- Cookie-state dumps should log wall-clock read time inline (the
  checkpoint-5 expiry cross-check needed file-mtime reconstruction).

## Closing Summaries

### Executor closing
All steps executed with strong live evidence; step 6's storage/history side
fully validated (aged cleared, fresh survived, auto-repaint); cookie clause
unobservable on first-ever sweep — code-correct, spec-premise-breaking;
two incidents as above with the launch-stdout lesson; environment fully
torn down and verified.

### Validator closing
6/7 PASS with triangulated evidence (screenshot + DOM/AX + independent
session read agreeing on every passing checkpoint); DD7 held cleanly;
checkpoint 6's falsifying read was unambiguous and the amendment should
(and now does) split the claim into a live-witnessed storage/history half
and a cold-start cookie half whose removal path is unit-covered and
HAT-witnessed.

## Operator Notes

Run executed autonomously (M10 pre-authorization). The spec transitions to
`active` with the amended step 6; run 1 status `partial` stands as the
honest record. HAT flight carries: the live cookie-removal witness (a
sweep against day-old bookkeeping) and the two apparatus lessons.
