# Behavior Test Run: jar-key-revocation-on-delete — 2026-07-11-05-12-54

**Spec**: [jar-key-revocation-on-delete.md](../jar-key-revocation-on-delete.md)
**Status**: pass
**Started**: 2026-07-11T05:12:54Z
**Completed**: 2026-07-11T05:52:00Z (approx)
**Duration**: ~00:39:00 (wall clock incl. orchestration turns)
**Executor**: a797782fc16960e25 (Sonnet)
**Validator**: a3ac77445dd0b592c (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/jar-key-revocation-on-delete/2026-07-11-05-12-54/`
during the run; re-derivable by re-running the spec. Key material redacted from
all evidence files (grep-verified twice by the Executor).

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec —
the delete→revocation→401 chain is now live-witnessed: a keyed jar's live MCP
session survived a rename (positive control), then drew a bare auth-layer 401
on its very next request after the jar was deleted, and a fresh connect with
the same key was refused — no app restart, no session teardown; per-request
re-validation did the work.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Fresh scratch stage (`XDG_CONFIG_HOME` → run-local dir;
  operator's real profile and separately-running 49707 instance untouched);
  dual-mint launch (`GOLDFINCH_AUTOMATION_DEV_MINT=1` +
  `GOLDFINCH_AUTOMATION_ADMIN=1`, port pinned 49711); long-lived session daemon
  holding one SDK Client+transport per identity; jar-key initialize +
  enumerateTabs; admin client connect.
- **Raw state**: both keys printed non-null; jar-key session
  `a1e7b6f2…` established; enumerateTabs listed exactly the staged personal tab
  (wcId 2, jarId `personal` — also confirming the mint targeted the
  resolved-default jar); admin session live on a separate transport.
- **Expected**: both keys printed; jar-key initialize + enumerate succeed;
  admin connects; halt otherwise.
- **Verdict**: pass — all four expected results evidenced; safety
  preconditions (scratch stage, pinned loopback port) verified in evidence.
- **Evidence**: `step-1-mint-redacted.txt`, `step-1-jar-connect.json`,
  `step-1-jar-enumerateTabs.json`, `step-1-admin-connect.json`, `step-1-shell.txt`
- **Validator notes**: pinned jar-key sessionId as the continuity marker for
  steps 2/4.

### Checkpoint 2 — PASS
- **Actions taken**: admin chrome apparatus (`getChromeTarget` → wcId 1;
  `evaluate` → `window.goldfinch.jarsRename({ id: 'personal', name: 'Keyed Jar' })`);
  then enumerateTabs on the untouched jar-key session.
- **Raw state**: rename resolved the updated container record (name changed,
  id/partition unchanged); jar-key session's enumerate succeeded on the
  identical sessionId with a byte-identical tab listing; daemon status
  confirmed no reconnect.
- **Expected**: rename succeeds; jar-key session keeps working (positive
  control pinning step 4's rejection to DELETE specifically).
- **Verdict**: pass — the unchanged id/partition in the resolved record
  directly evidences the mechanism claim. Spec-drift note: `jarsRename`
  returns the record, not an `{ ok: true }` envelope (spec corrected post-run).
- **Evidence**: `step-2-jarsRename.json`, `step-2-jar-enumerateTabs.json`,
  `step-2-daemon-status.json`, `step-2-getChromeTarget.json`

### Checkpoint 3 — PASS
- **Actions taken**: admin chrome apparatus →
  `window.goldfinch.jarsRemove({ id: 'personal' })`; context-only admin
  enumerate after settle. Apparatus repair en route: the idle admin session had
  been pruned server-side (HTTP 404, `-32000` "No valid session") — a fresh
  admin client was attached; zero calls touched the jar-key fixture.
- **Raw state**: remove resolved verbatim
  `{"ok":true,"removed":{"id":"personal","name":"Keyed Jar",…},"wiped":true}`;
  the removed record carries the step-2 rename, confirming one jar identity
  flowed rename → delete. Context: personal tab closed; fallback tab opened in
  `work` (orphan sweep + default-flag move, as preconditions anticipated).
- **Expected**: `{ ok: true, wiped: true, … }`; tab closure observed as
  context only.
- **Verdict**: pass — both load-bearing fields present; admin reconnect judged
  an apparatus repair on the spec's apparatus-only identity.
- **Evidence**: `step-3-jarsRemove.json`, `step-3-admin-enumerateTabs.json`,
  `step-3-admin2-connect.json`, `step-3-shell.txt`

### Checkpoint 4 — PASS (mixed-frame)
- **Actions taken**: enumerateTabs on the SAME jar-key live session (untouched
  since step 2); session-independent curl POST with the revoked Bearer key
  (status, headers, and body variants).
- **Raw state**: SDK failure `{ name: 'Error', message: 'Streamable HTTP
  error: Error POSTing to endpoint: ', code: 401 }` with empty body —
  explicitly distinct from the step-3 pruned-session shape (404 +
  JSON-RPC body) and from any admitted in-band `isError`. Curl:
  `HTTP/1.1 401 Unauthorized`, 0-byte body. Same session was serving 200s in
  step 2; app never restarted.
- **Expected**: live-session call fails at the transport/auth layer; curl
  returns 401.
- **Verdict**: pass — both frames independently evidenced and agreeing; the
  primary (live-session SDK 401) carried the live-degradation half, the raw
  401 pinned the auth layer. The step-3 prune served as an in-run negative
  control discriminating 401-auth from 404-session-routing.
- **Evidence**: `step-4-jar-enumerateTabs-rejection.json`,
  `step-4-curl-status.txt`, `step-4-curl-headers.txt`, `step-4-curl-body.txt`,
  `step-4-daemon-status.json`, `step-4-shell.txt`

### Checkpoint 5 — PASS
- **Actions taken**: brand-new SDK client/transport with the revoked jar key;
  attempted initialize. Concurrent admin control call (apparatus health).
- **Raw state**: initialize failed with the same bare-401 transport error; no
  session ever registered; port verifiably LISTENing; the valid admin key's
  concurrent call was admitted — rejection is key-specific, not server-wide.
- **Expected**: initialize fails (transport-level auth rejection; 401); the
  revoked key admits nothing.
- **Verdict**: pass — auth rejection (not a connection error) proven by the
  live port + concurrent admitted control call; combined with step 4, the
  revoked key is refused on both surviving and fresh transports.
- **Evidence**: `step-5-jar2-connect-rejection.json`, `step-5-port-listen.txt`,
  `step-5-admin2-control.json`, `step-5-daemon-status.json`, `step-5-shell.txt`

## Orchestrator Notes

- Mode: live two-agent (SendMessage available); both agents persistent across
  all five checkpoints. Cache mode: cold (fresh scratch stage).
- Model: Sonnet for both agents per crew file.
- Spec was design-reviewed pre-run (premise audit); one SEV-1 caught and fixed
  before spawning: the chrome apparatus is admin-only, so the launch mints
  BOTH keys (the draft had withheld the admin key). That correction is why the
  run passed on the first attempt.
- No operator interventions; no failed checkpoints; no re-spawns. The one
  apparatus anomaly (admin idle-session prune) was repaired by the Executor
  within step 3 and disclosed in its report.
- Post-run spec corrections applied (status was `draft`): step-2 rename return
  shape; step-4 rejection-shape note (401-empty-body vs 404-JSON-RPC,
  promoting the accidental negative control into the spec); idle-pruning
  fixture-hazard precondition with the daemon-held-transport pattern named.

## Closing Summaries

### Executor closing

Clean 5/5 execution. Session persistence solved with a long-lived node daemon
holding the SDK clients (separate Client + StreamableHTTPClientTransport per
identity), driven over a loopback control port — recommended as the pattern
for any spec needing a live session as a multi-step fixture; it makes "same
session, no reconnect" mechanically provable. The idle admin-session prune
(404 mid-run while app/port verifiably alive) was repaired with a fresh
apparatus client and turned out evidentially fortunate: the run contains both
rejection shapes side by side, exactly the auth-vs-session-routing distinction
the mixed-frame step needed. Environment: `XDG_CONFIG_HOME`-based scratch
staging works cleanly; the fresh-stage boot tab satisfies the staged-tab
precondition; pinned port bound directly. Teardown gotcha (self-inflicted):
`pkill -f` on the launcher pattern matched the teardown shell itself — kill by
port-holder pid instead; the Electron main had also reparented after the
launcher died (known behavior, seen from the other direction). Key discipline:
plaintext only in shell vars/daemon env/a launch log shredded at teardown;
evidence grep-verified key-free twice. No operator data at risk; the
operator's live instance was never contacted.

### Validator closing

The property under test was genuinely observed — the delete→revocation→401
chain held end to end with per-request re-validation doing the work. Four
authoring recommendations (all applied post-run): fix the step-2 rename
return-shape drift; promote the accidental 401-vs-404 negative control into
the spec's expected shape; add the idle-pruning fixture hazard as a
precondition; name the daemon-held-transport pattern as canonical for
same-session fixtures. Evidence discipline was strong: verbatim resolved
values, redaction verified, session ids traceable step to step, and the step-5
concurrent admin control proving key-specificity. No inconclusive steps, no
frame conflicts; Out-of-Scope boundaries (tab closure context-only, admin key
apparatus-only) were respected by the Executor and held in judgment.

## Operator Notes

*(none — run executed under standing operator authorization to close the
mission's audit gaps; operator did not intervene)*
