# Behavior Test Run: welcome-search-handoff — 2026-08-25-04-48-18

**Spec**: [welcome-search-handoff.md](../welcome-search-handoff.md)
**Status**: partial
**Started**: 2026-08-25T04:48:18Z
**Completed**: 2026-08-25T05:03:23Z
**Duration**: 00:15:05
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/welcome-search-handoff/2026-08-25-04-48-18/` during the run; re-derivable by re-running the spec.

## Summary

6 / 7 checkpoints passed; 0 failed; 0 inconclusive; checkpoint 7 not run (operator-performed row — the page-context sheet is outside the automation surface; pending the operator's hand check). With a home page set and no engine: a plain new tab is not a welcome tab; a search hands off to a welcome tab beside the untouched page, showing the engine block headed with the pending query and committing no provider URL; choosing DuckDuckGo runs that search in the same slot; the row records the choice; the next search goes straight to DuckDuckGo; the Settings engine Clear unchecks every radio with its hint; a fresh handoff after Clear is escaped by typing an address in the same tab, discarding the query with no provider URL ever committed.

## Checkpoint Results

### Setup row (fixture) — completed, no judgment
- **Actions taken**: Settings via the seam; Home page `https://example.com` + Save; engine radios untouched; row read `homePage: "https://example.com"`, `searchEngine: null`. The original welcome tab (never activated) stayed viewless.
- **Evidence**: `step-0-settings.png`, `step-0-settings-row.json`

### Checkpoint 1 — PASS
- **Actions taken**: Before-probe; Ctrl+T on the chrome; after-probe, `enumerateTabs`, `captureWindow`, `readAxTree`.
- **Raw state**: Strip 2 → 3 tabs; the new tab (wcId 3) committed `https://example.com/`, rendered the Example Domain page, `welcomeVisible: false` — a plain new tab.
- **Expected**: The new tab opens on `https://example.com/` — with a home page set and no engine, a plain new tab is not a welcome tab.
- **Verdict**: pass — a new web-contents tab (wcId 3) opened on `https://example.com/`, rendered the page, no welcome panel; probe, tab list, and screenshot agree.
- **Evidence**: `step-1-before.json`, `step-1-probe.json`, `step-1-tabs.json`, `step-1-screenshot.png`, `step-1-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 2 — PASS
- **Actions taken**: Before-probe on the example.com tab; typed `zephyr quartz pending` in the chrome address bar, Enter; after-probe, `enumerateTabs`, `captureWindow`, `readAxTree`.
- **Raw state**: Strip 3 → 4: a new welcome tab appended beside Example Domain and active; its address bar empty; heading `Where should we search for "zephyr quartz pending"?`; engine block only (`homeBlock: false`); `enumerateTabs` shows wcId 2 (Settings) and wcId 3 (`https://example.com/`, unchanged) — no provider URL anywhere.
- **Expected**: A welcome tab opens **beside** the example.com tab (the page the user was on is untouched) showing only the search-engine block, headed with the pending query text `zephyr quartz pending`. No tab navigated to any search provider; the address bar of the welcome tab is empty.
- **Verdict**: pass — a viewless welcome tab appended beside the untouched example.com tab, heading `Where should we search for "zephyr quartz pending"?`, engine block only (a11y: zero "Home page"), empty address bar, no provider host anywhere in the tab list or a11y dump.
- **Evidence**: `step-2-before.json`, `step-2-probe.json`, `step-2-tabs.json`, `step-2-screenshot.png`, `step-2-snapshot.txt`
- **Validator notes**: the negative claim is well-supported by both the tab list and the a11y dump.

### Checkpoint 3 — PASS
- **Actions taken**: Before-probe; clicked the DuckDuckGo radio in the welcome panel (chrome); after-probe, `enumerateTabs`, `#address.value`, `captureWindow`, `readAxTree`.
- **Raw state**: Strip 4 → 4, selected index 3 → 3 (same slot, retitled "zephyr quartz pending at DuckDuckGo"); committed `https://duckduckgo.com/?q=zephyr+quartz+pending&ia=web`; new wcId 5 in place; panel hidden; live results rendered.
- **Expected**: That same welcome tab navigates to a `duckduckgo.com` search URL containing `zephyr quartz pending` (tab count unchanged).
- **Verdict**: pass — same slot (4 → 4 tabs, index 3 → 3), the welcome placeholder acquired wcId 5 at the `duckduckgo.com/?q=zephyr+quartz+pending` URL with live results; wcIds 2 and 3 untouched.
- **Evidence**: `step-3-before.json`, `step-3-probe.json`, `step-3-tabs.json`, `step-3-screenshot.png`, `step-3-snapshot.txt`
- **Validator notes**: the tab-count and same-slot evidence is exactly what same-tab rows need.

### Checkpoint 4 — PASS
- **Actions taken**: Read-only `sqlite3` query at 04:57:21Z, saved verbatim; no UI interaction (dispatched in parallel with checkpoint 3’s judgment — a read cannot disturb live state).
- **Raw state**: `version: 3`; `homePage: "https://example.com"` (unchanged from the fixture); `searchEngine: "duckduckgo"` (from the welcome-surface choice).
- **Expected**: `[mixed-frame]` `searchEngine` is `"duckduckgo"`; `homePage` is still `"https://example.com"`.
- **Verdict**: pass — the Validator’s own `sqlite3` read matched: `searchEngine: "duckduckgo"`, `homePage: "https://example.com"`, nothing else altered.
- **Evidence**: `step-4-settings-row.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: Ctrl+T (new tab on example.com, no panel); chrome address-bar search `zephyr quartz direct`; after-probe, `enumerateTabs`, `captureWindow`, `readAxTree`.
- **Raw state**: New tab wcId 6 opened on `https://example.com/`; the search committed `https://duckduckgo.com/?q=zephyr+quartz+direct&ia=web` in the same tab (5 → 5 tabs, index 4); `welcomeVisible: false` throughout; live results.
- **Expected**: The new tab (opened on example.com) navigates directly to a `duckduckgo.com` search URL — no welcome surface appears now that an engine is set.
- **Verdict**: pass — the new tab opened on example.com and the search committed the `duckduckgo.com` URL in the same slot; a11y grep for the handoff heading returned zero.
- **Evidence**: `step-5-before.json`, `step-5-probe.json`, `step-5-tabs.json`, `step-5-screenshot.png`, `step-5-snapshot.txt`
- **Validator notes**: positive-absence check clean.

### Checkpoint 6 — PASS
- **Actions taken**: (a) Settings: clicked the engine Clear; captured guest + a11y. (b) Ctrl+T then searched `zephyr quartz again`; probe, `enumerateTabs`, `captureWindow`. (c) On the resulting welcome tab typed `example.net`, Enter; probe, `enumerateTabs`, `#address.value`, `captureWindow`, `readAxTree`.
- **Raw state**: (a) All 8 radios unchecked; hint "No search engine chosen — the welcome page will ask the first time you search."; home page untouched. (b) A new welcome tab (7 tabs, index 6) beside the Ctrl+T tab with the heading `Where should we search for "zephyr quartz again"?`, engine block only; tab list holds only the two earlier DuckDuckGo tabs (wcId 5, 6) — no new provider URL. (c) The same tab (7 → 7, index 6) navigated to `https://example.net/` (wcId 8); panel and pending query gone; still no new provider URL.
- **Expected**: After Clear, the radio group shows no engine selected with a hint that the welcome page will ask. The search lands on a welcome tab with the pending query; typing an address navigates that same tab to `https://example.net/` and the pending query is gone with it — no provider URL was ever committed.
- **Verdict**: pass — all four sub-claims held: radios unchecked with the exact hint; the search landed on a welcome tab with the pending query (DuckDuckGo showed only a focus ring, not a selection); typing an address navigated that same slot to `https://example.net/`; the tabs-a → tabs-b diff shows no new provider URL for the query.
- **Evidence**: `step-6-settings-pre.png`, `step-6-settings.png`, `step-6-settings-snapshot.txt`, `step-6-probe-a.json`, `step-6-tabs-a.json`, `step-6-screenshot-a.png`, `step-6-probe-b.json`, `step-6-tabs-b.json`, `step-6-screenshot-b.png`, `step-6-snapshot.txt`
- **Validator notes**: the before/after tab-list diff was the decisive evidence for the negative claim, as flagged at READY.

### Checkpoint 7 — OPERATOR-PENDING (operator-performed row, not run in this session)
- **Actions taken**: none — the page-context "Search for …" sheet is outside `AUTOMATABLE_MENU_TYPES`; the row is operator-performed by the spec's own convention.
- **Expected**: A welcome tab opens in the source tab's jar with the selected text as its pending query; choosing an engine runs it.
- **Verdict**: not rendered — pending the operator's hand check (structural coverage: `sel:search` routes a null `toUrl` through `openWelcomeTab` with the selection as `pendingQuery`; unit-pinned structurally in leg 2).
- **Evidence**: —

## Orchestrator Notes

- **Execution mode**: live two-agent mode; cache mode `cold` — fresh profile launched for the run; fixture (home page only, via Settings) folded into checkpoint 1 as a setup row; the engine was left unset. Crew: Executor + Validator, Sonnet; no Accessibility Validator; the crew read the crew file's Project Apparatus Notes.
- **Apparatus**: the attach-only CLI with the run's minted admin key (port 49707). The welcome surface is chrome DOM — radios and buttons driven at chrome-relative coordinates from `getBoundingClientRect()` via `evaluate` on the chrome; Settings via the `kebabActionSettings` seam with guest-coordinate clicks (the engine Clear button sits beneath the 8th radio — locate it by rect, not a fixed pixel). Steps 4 (a filesystem read) was dispatched in parallel with checkpoint 3's judgment — a read cannot disturb live state. Evidence pattern that carried the negative claims (steps 2, 5, 6): full `enumerateTabs` snapshots before and after the action, diffed — no new provider URL — plus a11y greps for provider hosts and the handoff heading.
- **Step 7 — operator-pending**: the page-context "Search for …" sheet is outside `AUTOMATABLE_MENU_TYPES`; the row is operator-performed by the suite's convention (squawk 0007 precedent). Structural coverage: `sel:search` routes a null `toUrl` through `openWelcomeTab` with the selection as `pendingQuery` (leg 2, structurally pinned). Recommended ten-second check at the flight debrief: with the engine unset, select text → right-click → "Search for …" → a welcome tab in the source jar with the selection as its pending query; choose an engine → it runs.
- **Observation for the crew notes**: a freshly rendered welcome panel shows a keyboard-focus ring on its first radio (DuckDuckGo) — outline only, unchecked per a11y — not a selection; `#welcome-surface` `textContent` includes hidden blocks' text — use the hidden-class flags for visibility.

## Closing Summaries

### Executor closing
No exceptions across all six steps; step 7 withheld as operator-only. Apparatus notes: welcome-surface controls use chrome-relative coordinates (the same frame as `#address`) — no guest translation; the engine Clear button's position is layout-dependent (locate by rect); `#welcome-surface` textContent includes hidden blocks — the class-based `homeBlock`/`engineBlock` flags are the visibility signal; a cleared-engine welcome panel shows a focus ring on the first radio, unchecked per a11y. Handoff behavior observed end to end: a query with no engine always spawns a welcome tab beside the current one; the same tab transforms in place once an engine is chosen; the choice persists immediately (live behavior and the on-disk row); Clear resets the group with the documented hint; a pending query is discarded the instant an address is typed, with no provider URL ever transiently committed.

### Validator closing
6/6 automatable rows passed with corroborating evidence; no inconclusives. Patterns: (1) same-slot vs new-tab is the load-bearing mechanic and `enumerateTabs` wcId churn + `selectedIndex` resolved it cleanly every time — diff wcIds, not titles; (2) negative claims were the highest-risk rows and were resolved by diffing full tab-list snapshots across the step boundary — worth codifying as a standing apparatus note; (3) rendered and internal state never diverged this run; (4) the literal hint-string match is a good canary against copy drift. No spec defects; stable enough to re-run as a regression check unless the automation tier ever admits the context-menu sheet.

## Operator Notes

- **2026-08-25, flight debrief — checkpoint 7 operator-verified by hand.** On the Flight 2 build with the engine unset, the operator selected text on a page → right-click → "Search for …" and observed a welcome tab open carrying the selection as its pending query; choosing an engine ran the search. Functionally a pass at the operator-observation level; the run's recorded status stays `partial` (run logs are terminal). Operator feedback on the surface itself — the Settings Clear buttons are not properly styled and the welcome page looks underwhelming — routed to Flight 3 (branding/alignment).
