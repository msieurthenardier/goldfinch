# Behavior Test Run: page-context-menu — 2026-07-08-20-03-11

**Spec**: [tests/behavior/page-context-menu.md](../../page-context-menu.md)
**Status**: partial
**Started**: 2026-07-08T19:58:00
**Completed**: 2026-07-08T20:05:00
**Duration**: 00:07:00
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
First run of this draft spec on native `WebContentsView`. **10 of 12 steps PASS** — the custom on-brand page-context menu renders per-section (link/image/selection/editable), maps 1:1 to the cursor, is keyboard-navigable, no-ops on internal pages, and drives the toolbar Unpin with live flip + persistence. Step 9 (Shift+F10/ContextMenu keyboard invocation) is INCONCLUSIVE-APPARATUS-LIMIT (`pressKey` cannot emit those keys). Step 8's Escape focus-return lands on the chrome (kebab) rather than the spec-stated `#address` — a minor deviation, invariants hold. Chrome `wcId` 1, guest `wcId` 8 (fixture), sheet `wcId` 6.

## Step Results

### Step 1 — PASS (probe)
`tools/list` includes `getChromeTarget`, `click`, `pressKey`, `readDom`, `readAxTree`, `captureWindow`, `enumerateTabs`, `evaluate`; `getChromeTarget()` → numeric chrome `wcId 1`.

### Step 2 — PASS (setup)
Fixture `tests/behavior/fixtures/menu-overlay/index.html` opened (guest `wcId 8`); target rects read via `evaluate`. Guest `devicePixelRatio = 1.25` → guest clicks use device coords = CSS×1.25.

### Step 3 — PASS (link section) + 1:1 cursor
- **Raw state**: `click(8, 768, 415, {button:'right'})` on the mid-page link → sheet `#sheet-menu data-menu-type="page-context" role="menu"`, items **"Open link in new tab", "Copy link", "Inspect"**, 1 separator. Menu top-left rendered at **(768, 415)** = the exact right-click device coords.
- **Verdict**: pass — the custom on-brand menu (not the native OS menu; no native menu ever appears in the composite) with the correct link section.
- **Evidence**: [sections-and-cursor.json](./2026-07-08-20-03-11/sections-and-cursor.json)

### Step 4 — PASS (image section)
- Right-click the `<img>` → items **"Open image in new tab", "Copy image address", "Save image", "Inspect"**.

### Step 5 — PASS (selection section)
- Programmatic selection of the paragraph, right-click → items **"Copy", "Search for \"Selectable fixture text for t…\"", "Inspect"** (truncated selection text).

### Step 6 — PASS (editable section, editFlags gating)
- Focus `<input>`, right-click → items **"Paste", "Inspect"**. Cut/Copy/Undo/Redo **omitted** (their `editFlags` falsy at click time — no active selection), demonstrating the **omit-not-disable** contract.

### Step 7 — PASS (cursor 1:1)
- Menu top-left `(768, 415)` == right-click device coords `(768, 415)`, **0 px** deviation (sheet covers the guest region 1:1, no offset translation).

### Step 8 — PASS (roving nav) / minor deviation (Escape focus target)
- **Raw state**: roving on open = "Open link in new tab"; ArrowDown→"Copy link"; End→"Inspect"; Home→"Open link in new tab". Escape → chrome `document.activeElement.id="kebab"`, chrome `hasFocus=true`.
- **Verdict**: roving Arrow/Home/End nav passes. Escape closes and returns focus to the **chrome** (not stranded on `<body>`, not in the guest — the load-bearing invariants hold), but to `#kebab` (the previously-focused chrome element) rather than the spec-stated `#address` for a pointer-invoked menu. **Minor SPEC-DRIFT** (draft spec) — recorded, not failed.

### Step 9 — INCONCLUSIVE-APPARATUS-LIMIT (Shift+F10 / ContextMenu)
- **Raw state**: focused the Reload button (`#reload`) correctly; `pressKey(1,'F10',['shift'])` → `isError:true`, `"automation: unknown key F10 (known: Tab, Enter, Escape, Space, Arrows, Home, End, Delete, Backspace, ShiftTab, or a single letter/digit)"`. `ContextMenu` is likewise unsupported.
- **Verdict**: inconclusive-apparatus-limit — `pressKey` cannot emit F10/ContextMenu, so the chrome-focused keyboard invocation isn't drivable here (matches the spec's HAT note for the live keyboard render). Not a failure.

### Step 10 — PASS (no-op on internal pages)
- **Raw state**: opened `goldfinch://settings` (internal guest `wcId 9`); `click(9, 500, 400, {button:'right'})`; `captureWindow` before vs after = **0 changed pixels**; no fresh page-context menu on the sheet (persisted `data-menu-type` stayed "kebab").
- **Verdict**: pass — the internal guest emits no forwarded page-context menu (the main-side `!__goldfinchInternal` guard holds). Judged by pixels. *(Note: the internal guest is separately, regressively drivable — see the `kebab-menu` run — but the context-menu-forwarding guard specifically is intact.)*

### Step 11 — PASS (toolbar Unpin menu)
- **Raw state**: `click(1, 1207, 64, {button:'right'})` on `#toggle-media` → sheet `data-menu-type="page-context" role="menu"`, single item **"Unpin Media"** (focused). The custom menu in toolbar-mode, not a native Electron menu.

### Step 12 — PASS (Unpin activate + persist)
- **Raw state**: activated "Unpin Media" → `#toggle-media` gets `class="icon-btn hidden"`, `display:none` (immediate live flip); dev-profile `settings.json toolbarPins.media === false` (persisted); focus lands on `#address`.
- **Verdict**: pass — live flip + filesystem persistence + `#address` refocus. Pin restored at batch teardown (settings.json reset to `media:true`).

## Orchestrator Notes
Single-agent convenience sweep. The guest DPR (1.25) vs chrome DPR (1.0) split governs click scaling: chrome clicks use CSS coords 1:1; guest clicks use CSS×1.25. Sheet `wcId 6` reused (singleton). The Step-10 internal guest was reachable because internal tabs are (regressively) enumerable/drivable — but the context-menu-forwarding guard itself is intact.

## Closing Summaries

### Executor closing
Every per-section render (link/image/selection/editable + always-Inspect), the 1:1 cursor mapping, roving keyboard nav, the internal no-op, and the toolbar Unpin live-flip+persist pass cleanly on the F8 sheet. Only the Shift+F10 keyboard invocation (apparatus can't emit the key) and the exact Escape focus target (#kebab vs #address) are non-clean, neither a functional defect.

### Validator closing
Strong first run for a draft spec. Two items for the FD: (1) reconcile step 8's Escape focus-return expectation (`#address`) with observed behavior (returns to the previously-focused chrome element) — decide which is intended and update the spec. (2) Step 9 needs `pressKey` F10/ContextMenu support or stays HAT. Promote the spec from draft once step 8's expectation is settled.

## Operator Notes
Toolbar `media` pin restored to `true` at teardown.
