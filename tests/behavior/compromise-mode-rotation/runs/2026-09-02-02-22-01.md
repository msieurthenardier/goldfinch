# Behavior Test Run: compromise-mode-rotation — 2026-09-02-02-22-01

**Spec**: [compromise-mode-rotation.md](../../compromise-mode-rotation.md)
**Status**: fail
**Started**: 2026-09-02T02:22:01Z
**Completed**: 2026-09-02T03:10Z (approx)
**Duration**: ~00:48
**Executor**: sub-agent (live mode, one mid-run API-error resume)
**Validator**: sub-agent (live mode)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/compromise-mode-rotation/2026-09-02-02-22-01/`
during the run (30 files); re-derivable by re-running the spec.

## Summary

3 / 7 checkpoints passed; 1 failed; 3 inconclusive. Checkpoint 7's fail
was initially read as a product defect (empty revoked-keys list despite
real revocations) — **reclassified by post-run disk forensics: no product
defect; see the Post-Run Addendum** (the capture postdated an off-script
repeat rotation whose correct empty report had overwritten the witnessed
one; the populated-card claim is unwitnessed in this run).
The three inconclusives are evidence-collection failures (operator pacing
vs. automation sampling; unprovisioned probe material), not product
ambiguity — the Validator's closing supplies a consolidated re-run recipe
that converts all three into decidable verdicts. Run entered the flow from
the LOCKED page (locked-entry half of the matrix).

## Checkpoint Results

### Checkpoint 2 — PASS
- **Actions taken**: setup fold-in (v1 baseline snapshots + hashes;
  access-key fixture confirmed) + page reads in both lock states with a
  page-driven "Lock now" in between.
- **Raw state**: entry row ("Think a key or your master password leaked?"
  + "Rotate Everything…") rendered in both states at the bottom of the
  Settings flow; v1 manager with all three mrk slots + adminPublicKeyB64.
- **Expected**: entry row present in both states — lock-state matrix half 1.
- **Verdict**: pass — judged from rendered pixels in both screenshots.
- **Evidence**: `step-1-*`, `step-2-screenshot-unlocked.png`,
  `step-2-screenshot-locked.png`, `step-2-*.json`

### Checkpoint 3 — PASS
- **Actions taken**: clicked "Rotate Everything…" from the LOCKED page;
  read the confirm modal; toggled nothing.
- **Raw state**: R3 copy rendered **verbatim** (typographic apostrophes,
  em-dash intact); checkbox unchecked; Continue `disabled:true`, Cancel
  enabled; modal is page-level (sheetVisible stayed false).
- **Expected**: confirm modal matches ruled copy; checkbox gates Continue.
- **Verdict**: pass — gate's closed half proven here; open half witnessed
  at step 4 when the operator's Continue proceeded.
- **Evidence**: `step-3-screenshot-modal.png`, `step-3-modal-strings.json`

### Checkpoint 4 — INCONCLUSIVE
- **Actions taken**: operator attempt 1 — reuse probe → inline rejection
  witnessed; sheet later closed on window blur (discovered via automation:
  modal gone, sheetVisible false). Attempt 2 — straight to a genuine new
  password with a continuous 2s poll timeline running.
- **Raw state**: timeline shows one false→true transition (credential
  sheet), 20.0s continuous true, no dips; poll self-terminated with sheet
  still up.
- **Expected**: reuse rejected inline (verbatim copy), sheet stays open,
  accept + pending, sheetVisible true throughout.
- **Verdict**: inconclusive — rejection witnessed in substance (operator
  paraphrase: "had to be different") but not verbatim; the reject-then-
  accept single-session sequence was split across two sheet instances by
  the blur-close; pending-state not explicitly attested. Nothing
  contradicts expected behavior.
- **Validator notes**: the blur-dismissal of a security-critical
  credential sheet aborted cleanly (no disk mutation) but destroyed the
  witnessed sequence — spec or design should address focus loss.
- **Operator decision**: continued.

### Checkpoint 5 — INCONCLUSIVE
- **Actions taken**: (deviation) operator completed the whole flow —
  including the reveal acknowledgment — before the pre-ack capture;
  salvage capture ran post-ack.
- **Raw state**: disk facts fully verified — manager.json 834→493B,
  `version: 2`, mrk slots [master, recovery] only, admin slot AND
  adminPublicKeyB64 absent, kdf unchanged (N=131072/r=8/p=2); both
  on-disk `.gfvault` files rewritten (ciphertext differs); no
  journal/staged/tmp residue. Commit mtime 02:45:46.00Z — 4.03s after
  the poll's last (still-true) sample; ack not captured by automation.
- **Expected**: recovery sheet transition + rotation committed BEFORE the
  ack (commit-before-surfacing).
- **Verdict**: inconclusive — the rotation-committed half fully
  evidenced; the ordering half fell in a 53s automation blind window
  (poll self-terminated per its sample rule). Attestation covers that a
  reveal occurred; ordering rests on inference, which is not witness.
- **Operator decision**: continued.

### Checkpoint 6 — PASS
- **Actions taken**: operator acked the dismiss-locked reveal.
- **Raw state**: post-ack sheetVisible false; page unlocked with items.
- **Expected**: single sheet (no admin sheet follows), dismiss-locked,
  key shown once, profile unlocked throughout.
- **Verdict**: pass — sheet-content clauses on operator attestation
  ("recovery key held until I clicked the button", "no second sheet"),
  corroborated by post-ack automation state; exactly one disk rewrite =
  exactly one reveal event.
- **Evidence**: `step-7-*`, timeline post-ack sample.

### Checkpoint 7 — FAIL
- **Actions taken**: post-ack page capture (original tab, still live).
- **Raw state**: "Everything rotated" card present and correct in every
  respect except the one that matters: `ul.vault-compromise-card-list`
  has **zero children** — no "Admin key — Revoked" row, no per-vault
  rows — in both DOM and rendered pixels. This rotation demonstrably had
  revocations (pre-rotation admin pair on disk; Personal's minted access
  key `Z4IAKqmOpm4` listed pre-rotation, "No access keys." after).
- **Expected**: card with admin-key row ("Revoked") + per-jar rows.
- **Verdict**: fail — a user-level surfacing defect: the revocation
  report does not reach the renderer. The disk end-state is correct; the
  reporting surface is wrong. (Empty-list would be CORRECT for a
  rotation with nothing to revoke — not this one; the fix's regression
  test should pin that distinction.)
- **Evidence**: `step-7-screenshot.png`, `step-7-card-dom.json`
- **Operator decision**: continued (fix + re-witness required before the
  leg lands).

### Checkpoint 8 — INCONCLUSIVE
- **Actions taken**: four harness probes (Electron-free store against
  live + reconstructed-snapshot profiles) + operator-witnessed
  lock/unlock positive (8b).
- **Raw state**: P1 old access key vs LIVE → `VaultAuthError` (fails);
  P2 same key vs SNAPSHOT → opens, keyId matches (capture validity);
  P3a dummy admin key vs LIVE v2 → `VaultStateError 'no admin key
  provisioned'` (structural absence); P3b same vs v1 snapshot → GCM auth
  failure (absence is state-driven); P4 all envelopes/ciphertext differ
  from baseline. 8b: lock → operator unlock; timeline shows unlock sheet
  open/close with the page flipping to unlocked+items in-sample.
- **Expected**: every old credential fails; new master password + new
  recovery key both unlock.
- **Verdict**: inconclusive — access-key and admin classes PROVEN (the
  admin sever is stronger than spec anticipated: state-level, key-
  independent); original recovery key and original master password
  UNEVIDENCED (never capturable mid-run — a spec-provisioning defect);
  positives demonstrated at class level (app-layer unlock witnessed; the
  operator's third rotation live-proved recovery-branch auth) but
  generation-smeared by the off-script third rotation.
- **Evidence**: `step-8-script.mjs`, `step-8-probe-results.txt`,
  `step-8b-*`
- **Operator decision**: run complete.

## Orchestrator Notes

- **Mode**: live two-agent (SendMessage available); one Executor resume
  after a transient API connection loss during initial spawn (no state
  lost — attach had succeeded). Validator spawned in parallel with the
  Executor's cache-defeat (apparatus-abort guard already satisfied) — a
  deliberate protocol deviation, noted.
- **Hybrid witnessed adaptation**: operator sheet steps relayed through
  the Orchestrator as attestations (first-class for sheet content only);
  Executor page/disk evidence for everything else.
- **Entry state**: the flow was run from the LOCKED page (deliberate —
  R4's most interesting branch); the unlocked-entry half of the card's
  matrix qualifier was not exercised this run.
- **Deviations**: (1) operator completed the flow past the step-5 hold
  point → salvage protocol (timeline + post-state + mtimes) executed;
  (2) operator ran a spontaneous THIRD rotation via the RECOVERY branch —
  witnessing recovery-branch auth + its reuse rejection (bonus coverage
  no row claims) while smearing generation identity for step 8's
  positives; (3) checkpoints 4–6 verdicts requested as a batch.
- **Anomalies**: evidence dir found emptied between [READY] and step 2
  (origin unresolved; baseline re-captured pre-rotation, hash-verified);
  credential sheet dismisses on window blur (aborted attempt 1 cleanly —
  zero disk mutation — but see debrief note); step-4 sample-1 single-shot
  read proved worthless around operator pacing.
- Checkpoint 1 was a setup row (no judgment) — folded into checkpoint 2.

## Closing Summaries

### Executor closing (abridged; verbatim in transcript)
Five carry-forward apparatus laws: (1) timeline polls from the FIRST
operator step — single-shot samples around operator sheet work are near
worthless; (2) to pin commit-vs-ack mechanically, stat/hash the vault
files inside every poll tick alongside sheetVisible; (3) the vault page
keeps scroll position — locate every click target by
getBoundingClientRect after scrollTo(0,0) (first Lock-now click silently
missed at cached coords); (4) the completion card shifts the page layout
~170px — re-capture after any card-adding flow; (5) the Electron-free
store harness works against live and reconstructed dirs, but note
`load()` runs `vtxn.recover()` — technically write-capable on the live
profile. Evidence-dir wipe was survivable only because it preceded the
rotation. No crashes; transport stable; 30 evidence files.

### Validator closing (abridged; verbatim in transcript)
"The system under test behaved well; the run protocol was what failed."
Spec defects to fix: step 5's sheet-transition clause exceeds what the
boolean `sheetVisible` can express; step 8's preconditions don't
provision its own probes (an original recovery key structurally predates
the run — provision via a throwaway pre-run rotation, or add
operator-witnessed negatives at the sheets); step 7's both-entry
qualifier belongs in Variants; step 4's reject-then-accept composition
should tolerate (or forbid) a sheet abort; "every .gfvault" should read
"every .gfvault on disk". Consolidated re-run recipe: poll-until-false
with per-tick file stats; an explicit operator ack-gate held until the
Orchestrator confirms disk capture; keep app focus through step 4; cue
verbatim copy capture in advance; provision step-8 negatives; freeze the
profile between rotation and probes; guard the evidence dir with
second-location hashes. Items 1–2 decide checkpoint 5; 3–4 decide 4; 5–6
decide 8. No product changes needed beyond the checkpoint-7 card fix,
which requires its own re-witness.

## Operator Notes

Operator attestations recorded in-run: reuse rejection ("password had to
be different"), dismiss-locked reveal held to the button, no second
sheet, proper card presence, "unlocked, items are showing" at 8b, and
the third-run recovery-branch reuse rejection ("correctly rejected the
same password") with successful recovery-branch authentication.

## Post-Run Addendum (2026-09-02, Orchestrator)

Disk forensics during the fix investigation reclassified **checkpoint 7**:
`manager.json` was rewritten at 02:47:11Z by the operator's off-script
recovery-branch rotation — 85s after the witnessed rotation's commit and
BEFORE the step-7 page capture. That rotation's correct
`{admin:false, vaultIds:[]}` report overwrote the witnessed one
(last-rotation-wins by design), and the card faithfully rendered it. The
end-to-end wiring was proven intact by a headless composition of the real
modules. **No product defect; the populated-card claim is UNWITNESSED in
this run** (the verdict stands as rendered on the evidence available at
judgment time). Remediation shipped regardless: the card row model was
extracted pure with a full unit layer (populated + correct-empty), plus a
real cross-hop surface test. Re-run requirement: the step-7 capture must
precede any repeat rotation.
