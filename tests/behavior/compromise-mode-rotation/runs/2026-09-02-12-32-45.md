# Behavior Test Run: compromise-mode-rotation — 2026-09-02-12-32-45

**Spec**: [compromise-mode-rotation.md](../../compromise-mode-rotation.md)
**Status**: pass
**Started**: 2026-09-02T12:32:45Z
**Completed**: 2026-09-02T13:25Z (approx)
**Duration**: ~00:52
**Executor**: sub-agent (live mode)
**Validator**: sub-agent (live mode)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/compromise-mode-rotation/2026-09-02-12-32-45/`
(plus a second-location hash mirror) during the run; re-derivable by
re-running the spec.

## Summary

**7 / 7 judged checkpoints PASS** (checkpoint 1 is the setup row). This
re-run under the amended spec converted every run-1 inconclusive into a
decidable pass and re-witnessed run-1's failed checkpoint with its
failure mode mechanically excluded. Fixture phase additionally banked the
unlocked-entry populated-card witness and the old-recovery-key probe
material via the new throwaway-rotation precondition. Main flow entered
from the LOCKED page; the entry-state matrix closed within the run
(fixture card = unlocked half, main card = locked half).

## Checkpoint Results

### Setup (rows 1 + fixture phase) — recorded, no judgment
- Throwaway rotation (unlocked entry) with pre-provisioned admin +
  access key: commit-before-surfacing incidentally captured (fixture
  timeline tick 41: files rotated while sheet up); populated card
  captured pre-overwrite (`fixture-card.png`: "Admin key— Revoked" +
  "Personal— Revoked"); reveal key recorded as the provisioned "old
  recovery key". Post-throwaway re-provision (admin pair + access key
  `t2ahJMdSlT0`) then step-1 baselines: v1→n/a — v2 WITH admin pair,
  hashes mirrored to the second location.

### Checkpoint 2 — PASS
Entry row in both lock states. Locked half: pixels + DOM + a11y.
Unlocked half: exact-string DOM + a11y (mid-scroll screenshot missed the
row — an apparatus timing miss), carried per the project's a11y-as-
rendered-state law + functional corroboration (the operator's witnessed
unlocked-entry throwaway click), hardened post-run by the settled-scroll
exhibit `step-6-entry-row-unlocked.png`.

### Checkpoint 3 — PASS
Confirm modal from the LOCKED page: R3 copy verbatim (typographic marks
intact), checkbox unchecked gating a disabled Continue; page-level modal
(sheetVisible false); main poll already ticking before any operator
action.

### Checkpoint 4 — PASS (run-1 inconclusive, converted)
Reuse rejection attested VERBATIM ("Your new master password must be
different from your old one.") — read at moment of render under the
focus-hold protocol, relayed timestamped (12:51:35.067Z). Single sheet
instance proven by the timeline: sheetVisible true continuously ticks
21–129, zero dips, reject-then-accept on one span; acceptance
mechanically entailed by the commit at tick 39 off that same span.
Pending state directly attested post-run ("yes on working state").

### Checkpoint 5 — PASS (run-1 inconclusive, converted)
Commit-before-surfacing mechanically ordered inside one monotonic
timeline: tick 39 (12:50:38.723Z) shows manager rewritten (834→493B,
v2, slots [master,recovery], no admin pair — live reveal-up parse) and
both `.gfvault` files re-keyed (access envelope `t2ahJMdSlT0` dropped)
WHILE sheetVisible true; hashes stable ticks 39–129; sheet-false (ack)
at tick 130 (12:53:41.224Z) — commit precedes ack by 3m03s with no
inference. Ack-gate double-timestamped (reveal-up 12:51:35.067Z →
capture-confirm/ack-release 12:52:25.008Z → ack relay 12:54:08.097Z).

### Checkpoint 6 — PASS
Single reveal sheet (attested: "nothing after ack, no admin key"),
dismiss-locked ("reveal held" — survived two protocol focus switches),
shown once (one disk-rewrite event in 130 ticks = one reveal); page
landed unlocked from a locked entry with no unlock challenge — the
strongest form of the claim (the rotation credential alone carried the
transition).

### Checkpoint 7 — PASS (run-1 FAIL, re-witnessed clean)
Card captured 9s post-ack: full ruled lede, rows "Admin key— Revoked" +
"Personal— Revoked" (one row per vault with revocable keys; none for
Global/Work which had none — correct). Identity PROVEN, not assumed:
capture-time disk hashes equal the tick-39 commit hashes exactly — no
intervening rotation (run-1's overwrite failure mode mechanically
excluded). Entry matrix closed: this card = locked-entry witness; the
fixture card = unlocked-entry witness.

### Checkpoint 8 — PASS (run-1 inconclusive, converted)
Frozen-profile invariant hash-verified before/after probes and through
8b. Negatives with controls: P1 old access key VaultAuthError on live /
P2 success on its own generation's snapshot; P1b provisioned old
recovery key VaultAuthError (gcm) on live / control success on snapshot
(run-1's structural provisioning gap closed both halves); P3a dummy
admin key → VaultStateError 'no admin key provisioned' on live v2 /
P3b VaultAuthError gcm on the with-admin snapshot (state-driven).
Positive: witnessed unlock transition (unlock sheet ticks 20–22, page
unlocked-with-items at tick 23; operator attests the new master
password; disk untouched). Bonus attested wrong-entry rejection at the
unlock sheet (operator mistype, then success). New-recovery-class clause
bound only "where exercised" — deliberately not exercised under the
freeze; the on-script recovery-branch variant remains open as spec'd.

## Orchestrator Notes

- Live two-agent mode; fresh crew per re-run policy. Executor spawned
  first; one transient API-error resume in run 1's crew did not recur.
- Amended-spec protocol executed in full: throwaway-rotation
  provisioning; second-location hash mirror; poll-until-false with
  per-tick file stats/hashes; operator briefed (focus-hold,
  memorize-then-relay verbatim, ack-gate, freeze); ack-gate exchange
  triple-timestamped in `ack-gate-timestamps.txt`.
- Deliberate parallelizations (noted): Validator spawned during Executor
  cache-defeat; checkpoint 3 judged during the operator's step 4 (static
  captures, poll baseline predates operator's first touch).
- Checkpoints 4+5 and 6+7 sent as batches; step-6 attestations gathered
  as three targeted operator confirms.
- Raw observations for the record: the vault page's access-key list does
  not live-update after a mint (stale until re-render — squawk-class
  display anomaly; did not contaminate the card, which renders from the
  rotation report); the page auto-locked during the freeze window
  (normal timer; gave 8b a clean locked start); the blur asymmetry
  (credential sheet dies on blur, dismiss-locked reveal survives) is
  correct security behavior worth a unit pin.
- Entry-state scope: main flow LOCKED entry; unlocked half carried by
  the fixture phase.

## Closing Summaries

### Executor closing
Clean run: no crashes, no transport drops, no evidence loss, zero error
ticks across all three polls (fixture 42, main 130, 8b 23); 60+ evidence
files, every hash mirrored at capture. Apparatus laws carried to the crew
file: (1) scroll-settle refinement — instant scroll + scrollY===0 loop +
double-rAF + in-viewport rect check before any click; `ok:true` on a
click is never evidence it hit; (2) poll-until-false + per-tick file
hashing is the load-bearing pattern — start before the operator's first
action, always; (3) the vault page does not live-update access-key lists
— judge from disk envelopes; (4) completion-card reports can be
content-identical across rotations — the per-tick hash timeline is the
disambiguator; bank the card within seconds of the ack; (5) the
throwaway-rotation provisioning pattern (live negative + own-generation
positive control) should template every keyed probe; (6) keep the
before/after hash bracket in any live-profile harness run
(`load()`→`recover()` is write-capable in principle). Environment:
auto-lock raced the long freeze idle (harmless here); chrome-seam tab
route and attach-only client fully stable. The ack-gate sequence is the
run's spine — preserved verbatim in Checkpoint 5 above.

### Validator closing
"Run 1's diagnosis held — the system was never the problem, and a
protocol built to witness it properly returned 7/7. Every conversion was
achieved by evidence design, not looser judgment." Spec items for the
next revision (applied post-run, see spec): demote the pending-state
clause to if-noticed; codify memorize-then-relay in preconditions; make
capture-time hash identity step 7's standing evidence requirement; state
the old master password's deliberate exclusion from captured material;
write the provisioning order into step 1 explicitly. Standing practice
to keep: pre-touch poll-until-false with per-tick hashes, timestamped
ack-gate, hash-verified freeze, second-location mirror, fixture
witnesses banked at capture time. Design pins recommended: the
blur-asymmetry (reveal survives blur, credential sheet dies) and the
state-level admin sever. Open coverage: the on-script recovery-branch
variant (only run 1's off-script attestation exists); the stale
access-key list is a display-refresh defect independent of this test —
squawk recommended.

## Operator Notes

Operator performed: fixture provisioning (admin + access keys, two
generations), the throwaway rotation, the disciplined main rotation
(verbatim rejection relay, ack-gate hold honored), three step-6
attestations, and the witnessed 8b unlock (with an attested mistype
first — a bonus negative). Total operator time ≈ 45 minutes across both
phases.
