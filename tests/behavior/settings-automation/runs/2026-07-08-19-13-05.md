# Behavior Test Run: settings-automation — 2026-07-08-19-13-05

**Spec**: [tests/behavior/settings-automation.md](../../settings-automation.md)
**Status**: partial
**Started**: 2026-07-08T19:07:00-05:00
**Completed**: 2026-07-08T19:15:00-05:00
**Duration**: ~00:08:00
**Executor**: Flight-05 leg-04 behavior-test Executor (single-agent, Witnessed self-run)
**Validator**: (self, artifact-judged — readDom outerHTML + readAxTree + settings.json + screenshot persisted)

## Summary
Structural / readDom-observable assertions **PASS** (Steps 1, 2, 4, 9, and a partial live-session render in Step 11). **Step 3 FAILS** — the enable toggle and the persisted store are **inconsistent** (rendered toggle ON, disk `automationEnabled=false`), the same regression documented in the sibling `automation-key-gating` run. The coordinate-click interaction steps (5, 6, 7, 8, 10) and the staged-session teardown steps (12, 13) are **INCONCLUSIVE — APPARATUS-LIMIT** (coordinate clicks on the internal `goldfinch://settings` guest are unreliable in this WSLg venue; the prior 2026-06-15 run drove them in a venue where they worked).

Apparatus: `npm run dev:automation` with `GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_MCP_PORT=8899`. Admin MCP client. Settings guest opened via the trusted `createTab('goldfinch://settings',null,{trusted:true})` path (identical to the kebab→Settings action; the MCP `openTab` tool passes only url+jarId and cannot open the `goldfinch://` scheme). Guest = `{wcId:19,url:'goldfinch://settings/',jarId:'internal'}`.

## Step Results

### Step 1 — PASS
- **Actions taken**: connected admin client; `tools/list`; `getChromeTarget()`.
- **Raw state**: `getChromeTarget()` → `{wcId:1, kind:'chrome', url:'file:///…/src/renderer/index.html'}`; `tools/list` includes `getChromeTarget` (27 tools).
- **Expected**: `getChromeTarget` present + returns `{wcId, kind:'chrome', url}` with numeric wcId.
- **Verdict**: pass.
- **Evidence**: (readiness probe — chrome wcId 1)

### Step 2 — PASS
- **Actions taken**: opened Settings via the trusted `createTab` path; `enumerateTabs` → guest wcId 19; `readDom(19)`.
- **Raw state**: `goldfinch://settings` tab opened (partition internal); enumerable via the admin engine's `allowInternal`. The guest renders `<section id="automation">` with the enable toggle (`#automation-enabled`), address field (`#automation-address`), port field (`#automation-port`), the Keys subsection (`#automation-jars`), and the Activity area (`#automation-active-sessions` + `#automation-activity-log`).
- **Expected**: settings tab opens, enumerable; the automation section + its controls render.
- **Verdict**: pass.
- **Evidence**: [guest-settings-dom.html](./2026-07-08-19-13-05/guest-settings-dom.html)

### Step 3 — FAIL (toggle↔store inconsistency) `[a11y]`
- **Actions taken**: read `automationEnabled` from `~/.config/goldfinch/settings.json`; read the toggle's AX `checked` via `readAxTree(19)`.
- **Raw state**: disk `automationEnabled=false`; AX `#automation-enabled` (name "Enable automation surface") `checked="true"`; the toggle is visually filled/ON in the screenshot.
- **Expected**: the persisted `automationEnabled` and the toggle's AX `checked` **start consistent** (and flip together on click).
- **Verdict**: fail — they are **inconsistent at rest**: the store says OFF, the toggle renders ON. (See the `automation-key-gating` run for the full LIKELY-REGRESSION write-up: the rendered checkbox follows the effective dev-override-enabled state, not the persisted value.) The live-flip half was not driven (coordinate-click apparatus limit).
- **Evidence**: [FINDING-summary.json](./2026-07-08-19-13-05/FINDING-summary.json), [settings-automation-section.png](./2026-07-08-19-13-05/settings-automation-section.png), [guest-settings-ax.json](./2026-07-08-19-13-05/guest-settings-ax.json)

### Step 4 — PASS
- **Actions taken**: read `#automation-status` + `#automation-enabled-note` text via `readDom(19)`; address confirmed via status text + screenshot (the input `.value` is a property, not in outerHTML).
- **Raw state**: `#automation-status` = **"Connected — listening on 127.0.0.1:8899"**; screenshot MCP-address field = **"http://127.0.0.1:8899/mcp"**; `#automation-enabled-note` = **empty** (bound this launch).
- **Expected**: address `http://127.0.0.1:{port}/mcp` (port 8899); status `Connected — listening on 127.0.0.1:{port}` when bound; enabled-note empty when bound.
- **Verdict**: pass — the verbatim "Connected — listening on 127.0.0.1:8899" prefix + the address field both reflect the pinned port; the note is empty as expected when bound.
- **Evidence**: [settings-automation-section.png](./2026-07-08-19-13-05/settings-automation-section.png), [guest-settings-dom.html](./2026-07-08-19-13-05/guest-settings-dom.html)

### Step 5 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven — copy-address requires a coordinate `click` on the guest.
- **Expected**: `#automation-message` shows "Copied".
- **Verdict**: inconclusive — coordinate clicks on the internal guest are unreliable in this venue (device-pixel + guest-viewport origin).

### Step 6 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven — port validation requires focus-anchor click + typeText into the port field + click Save on the guest.
- **Expected**: out-of-range save shows an inline error + does not change persisted `automationPort`; valid save persists.
- **Verdict**: inconclusive — coordinate-click + type interaction on the guest not reliable here.

### Step 7 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven — find-port requires a coordinate click.
- **Expected**: the field is populated with a free port in 49152–65535 and persisted.
- **Verdict**: inconclusive.

### Step 8 — INCONCLUSIVE (APPARATUS-LIMIT; partial static read)
- **Actions taken**: static readDom of the `default` jar row only (the mint/rotate/revoke cycle needs coordinate clicks).
- **Raw state**: `default` row shows `key set` + mint button **"Rotate key"** + an enabled **Revoke**. (NB: per the regression, the mint button is enabled while the persisted toggle is OFF — the DD9 gate this step assumes is broken; see `automation-key-gating`.) The DD3 no-enable-on-mint invariant could not be exercised (mint not driven).
- **Expected**: DD9 gate with toggle ON permits minting; generate reveals a one-time key; rotate produces a new key; DD3 — mint does not change `automationEnabled`.
- **Verdict**: inconclusive — the mint/rotate/revoke cycle is coordinate-click-gated; and the DD9/toggle premise is itself broken on this build (Step 3 failure).

### Step 9 — PASS
- **Actions taken**: read `#automation-admin` presence + `hidden` attribute + `#automation-admin-status` via `readDom(19)`; also `#automation-key-reveal` presence.
- **Raw state**: `<div id="automation-admin">` present **without** `hidden` (admin block shown — `GOLDFINCH_AUTOMATION_ADMIN=1` set); `#automation-admin-status` = "Admin key set"; no persistent show-once reveal in the DOM at rest.
- **Expected**: `#automation-admin` shown (no `hidden`) when the env gate is set (hidden when unset); the show-once key is not re-fetchable after refresh.
- **Verdict**: pass — admin block correctly shown for the admin-env launch; no lingering reveal.
- **Evidence**: [guest-settings-dom.html](./2026-07-08-19-13-05/guest-settings-dom.html)

### Step 10 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven — admin mint/revoke requires coordinate clicks. Static read: `#automation-admin-mint` labeled "Rotate admin key" (admin key already minted), `#automation-admin-status` "Admin key set".
- **Expected**: mint reveals the admin key once + status "Admin key set"; revoke returns to "No admin key".
- **Verdict**: inconclusive — coordinate-click interaction not driven; static state consistent with an admin key present.

### Step 11 — PASS (partial — live jar session renders against the harness baseline)
- **Actions taken**: read `#automation-active-sessions` (baseline); staged a live **jar** session via a second authenticated MCP client (loopback initialize + a benign `enumerateTabs`); re-read `#automation-active-sessions`.
- **Raw state**: baseline = one row `admin / app / chrome / connected since 7:14:33 PM` (the harness's own admin session — the reflexivity baseline). After staging: **two rows** — the harness admin row AND a new **`jar / Default / connected since …`** row.
- **Expected**: with a staged jar session, `#automation-active-sessions` lists the harness admin row AND the staged jar row (named by its jar, with a "since" time).
- **Verdict**: pass (partial) — a real staged session renders in the viewer, identified as the `Default` jar, against the harness admin baseline. The chrome `#automation-indicator` badge-count / name-list half was not separately cross-read; the absolute zero-session frame is HAT-deferred (leg 9) per the spec.
- **Evidence**: [step11-active-sessions.txt](./2026-07-08-19-13-05/step11-active-sessions.txt)

### Step 12 — INCONCLUSIVE (partial)
- **Actions taken**: not fully driven — a second staged **admin** session + indicator `.admin` cross-read not performed.
- **Verdict**: inconclusive — degradable per the spec (admin matrix lives in `mcp-jar-scoping`). The harness admin session already carries the `.admin` class in the rendered `activity-session admin` row.

### Step 13 — INCONCLUSIVE (APPARATUS-LIMIT)
- **Actions taken**: not driven — proper (DELETE) vs ungraceful teardown distinction not staged.
- **Verdict**: inconclusive — degradable per the spec; the absolute return-to-empty is HAT-deferred (harness is always a live admin session).

## Orchestrator Notes
The SC9/SC8/SC10-structure assertions that are readDom/readAxTree-observable pass: the automation section renders fully (Step 2), the address/status honestly reflect the bound surface on port 8899 (Step 4), the admin block is correctly shown for the admin-env launch (Step 9), and a real staged jar session renders in the activity viewer against the harness admin baseline (Step 11). **Step 3 fails** on the toggle↔store consistency — the rendered enable toggle is ON while persisted `automationEnabled=false` — the same LIKELY-REGRESSION captured in `automation-key-gating` (rendered checkbox / DD9 gate follow the effective dev-override state, not the persisted value). The coordinate-click interaction steps (5,6,7,8,10) and the staged-session teardown matrix (12,13) are apparatus-limited on this WSLg venue and are marked inconclusive rather than guessed.

## Closing Summaries

### Executor closing
readDom/readAxTree/screenshot give solid PASS evidence for the section structure, the bound-surface address/status, the admin block, and a live staged-jar-session render. The interaction steps need reliable coordinate clicks into the internal guest, which this venue does not provide.

### Validator closing
The Step-3 inconsistency is the same underlying regression as `automation-key-gating` Steps 2-3 and should be triaged once — it also invalidates the DD9 premise of Step 8. The structural passes are unaffected by it.

## Operator Notes
Run on WSLg (DPR 1.25); admin profile `~/.config/goldfinch`. Guest coordinate-click interaction is the venue's main limitation.
