# Behavior Test Run: crash-and-hang-surfaces — 2026-09-17-00-22-28

**Spec**: [crash-and-hang-surfaces.md](../crash-and-hang-surfaces.md)
**Status**: partial — 14 pass / 1 inconclusive (row 6, by-eye clause → HAT) / 1 fail (row 12, fixed in-run and re-verified as 12b: pass)
**Started**: 2026-09-17T00:22:28Z
**Completed**: 2026-09-17T01:11:50Z
**Duration**: 0:49:22
**Executor**: live sub-agent (Sonnet), persistent across checkpoints
**Validator**: live sub-agent (Sonnet), persistent across checkpoints
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/crash-and-hang-surfaces/2026-09-17-00-22-28/` during the run; re-derivable by re-running the spec.

## Summary

14 / 16 numbered checkpoints passed. 1 failed (checkpoint 12 — defect F1: a recovery-paused window kept `booted: true`, and the tab census hung against its dead chrome; fixed during the run and re-verified as checkpoint 12b: PASS). 1 inconclusive (checkpoint 6 — three of four clauses passed; "the guest remains visible" under a `SIGSTOP`ped renderer is unobservable by this apparatus and is routed to the HAT leg's by-eye check). A second defect outside the rows (F2: the dump prune never reached Crashpad's `pending`) was found at the relaunch and fixed. Every crash-record line carried exactly the eight allowed fields with host-only origins, and no network egress was observed at any point.

## Checkpoint Results

### Checkpoint 0 — PASS
- **Actions taken**: enumerateWindows / enumerateTabs; crash-log line count; Crashpad counts; `ss -tnp` electron sample. **Precondition repair folded in**: two stale `failed` tabs (`http://127.0.0.1:1/`, left by the leg-4 a11y audit's session snapshot in the restored profile) were found; the Orchestrator ordered both closed — the one in THE window closed, the one in window 1 (wcId 8) did NOT (`closeTab` returned `false` twice; debrief item). Step 0 re-taken as `0b`.
- **Raw state**: THE window (windowId 2, chromeWcId 2): tabs wcId 9–16 all `ok` with numeric `pid`, active wcId 16; window row `booted: true`, numeric `chromePid`, `recoveryPaused: false`. `{N0}` = 18; Crashpad pending 46 / completed 0; zero electron TCP sockets.
- **Expected**: Every row `loadState: "ok"`; admin rows carry a numeric `pid`; the window row carries `chromePid` and `booted: true`.
- **Verdict**: pass — scoped to THE window; census fields are internal-state evidence, JSON sufficient.
- **Evidence**: `step-0b-windows.json`, `step-0b-census.json` (`step-0-*.json` = the pre-repair record)
- **Validator notes**: window 1's leftover failed tab flagged as a debrief item (out of scope).

### Checkpoint 1 — PASS
- **Actions taken**: openTab links.html → navigate form.html (a first take, wcId 18, raced the initial load and was discarded; re-done on wcId 19 with a settle poll); `kill -SEGV` → guest stayed `ok`; `kill -ABRT` → stayed `ok`; `kill -KILL` → crashed (signal that landed: SIGKILL). captureScreenshot(chrome), readAxTree, census, `ss` sample.
- **Raw state**: panel heading "This page was closed by the system", body "The system ended this page's process, often to free memory.", address `…/form.html`, code line `killed (9)`, Reload present; strip name "127.0.0.2:8991 — crashed"; census `crashed`, `loadError {9, killed}`, `pid: null`, `security: none`; `ss` loopback-only.
- **Expected**: crash panel with the reason wording, address, code line, Reload; census `crashed`/`pid: null`; strip glyph + "— crashed" (row notes: judged against the signal that landed).
- **Verdict**: pass — the Validator inspected the PNG itself; wording/codes match the KILL branch; the strip suffix derives from `loadState`, not the reason.
- **Evidence**: `step-1-screenshot.png`, `step-1-axtree.txt`, `step-1-census.json`, `step-1-ss.txt`, `step-1-log.json`, transient census files
- **Validator notes**: the discarded wcId 18 shared a renderer with wcId 19 and crashed with it; reconciled at step 2.

### Checkpoint 2 — PASS
- **Actions taken**: reconciliation (wcId 18 already absent); `evaluate` click on `#load-failure-reload`; settled census; captureWindow; readDom(19); `window.goldfinch.tabHistorySnapshot({ webContentsId: 19 })` via the chrome.
- **Raw state**: form.html rendered (heading, three inputs, button); census `ok` with a NEW numeric pid; history 2 entries, index 1 (links.html, form.html).
- **Expected**: page visible again; census `ok` with a new pid; two history entries at index 1.
- **Verdict**: pass — `step-2-window.png` inspected; all three clauses met.
- **Evidence**: `step-2-window.png`, `step-2-census.json`, `step-2-history.json`, `step-2-dom.txt`

### Checkpoint 3 — PASS
- **Actions taken**: navigate wcId 19 → links.html (settled; the renderer was reused); `kill -KILL`; `ss` sample; settled census; readAxTree/captureScreenshot of the chrome; enumerateTabs.
- **Raw state**: heading "This page was closed by the system", address `…/links.html`, code line `killed (9)`, Reload present; strip "… — crashed"; census `crashed`, `loadError {9, killed}`, `pid: null`; `ss` loopback-only.
- **Expected**: the killed wording and `killed (9)`; census `crashed`/`killed`/`9`.
- **Verdict**: pass — direct match, no substitution needed.
- **Evidence**: `step-3-screenshot.png`, `step-3-census.json`, `step-3-axtree.txt`, `step-3-ss.txt`

### Checkpoint 4 — PASS
- **Actions taken**: openTab about:blank → wcId 20 (active); enumerateTabs; captureScreenshot/readAxTree of the chrome.
- **Raw state**: wcId 20 ok/active; wcId 19 background, still `crashed`, `pid: null`; a11y tree has no load-failure heading; strip: wcId 19 "… — crashed" with the ⚠ glyph, new tab "New tab".
- **Expected**: the first tab stays crashed in the background with its strip mark; the active tab is ordinary; no panel.
- **Verdict**: pass — the PNG shows a blank new tab and the ⚠ mark on the background tab.
- **Evidence**: `step-4-screenshot.png`, `step-4-census.json`, `step-4-axtree.txt`

### Checkpoint 5 — PASS
- **Actions taken**: activateTab wcId 19; captureScreenshot/readAxTree of the chrome; enumerateTabs.
- **Raw state**: wcId 19 active and still `crashed`; heading/address/code line present in the a11y tree.
- **Expected**: the crash panel is projected again.
- **Verdict**: pass — the PNG shows the panel on the active tab, the strip's active tab carrying the ⚠ glyph.
- **Evidence**: `step-5-screenshot.png`, `step-5-census.json`, `step-5-axtree.txt`

### Checkpoint 6 — INCONCLUSIVE (3 of 4 clauses confirmed; guest-visibility clause routed to the HAT)
- **Actions taken**: reload via `#load-failure-reload` (settled `ok`, new pid); `kill -STOP <pid>`; one `click` (fire-and-forget — returned ok in 7 ms); census poll every 2 s → `hung` at ~16.1 s; readAxTree/captureScreenshot of the chrome; `captureWindow` and `captureScreenshot(19)` both refused with `capture-timeout` (a stopped renderer produces no frame) — the Validator reproduced the refusal independently.
- **Raw state**: bar "This page isn't responding" with **Wait** and **Kill and reload**; strip "keyboard-nav: links — not responding" with the hourglass glyph; census `hung`, `loadError: null`, pid unchanged.
- **Expected**: census `hung`; the bar with both buttons; the guest remains visible; the strip suffix.
- **Verdict**: inconclusive — census, bar, and strip PASS on rendered evidence; "the guest remains visible" is unobservable by this apparatus during a `SIGSTOP` (structural: the capture pipeline needs a fresh compositor frame within 3 s). Not a system failure.
- **Evidence**: `step-6-screenshot.png`, `step-6-axtree.txt`, `step-6-census.json`, `step-6-window.png.raw.json` (the capture error), the poll-phase transients
- **Validator notes**: recommend recording as pass-on-3-of-4 with the visibility clause checked by eye at the HAT.
- **Operator decision**: continued (the run brief pre-authorised an unattended run; disposition: HAT by-eye check — "links.html stays painted under the bar while the renderer is stopped").

### Checkpoint 7 — PASS
- **Actions taken**: `evaluate` click on `#hang-notice-wait`; computed-style read of `#hang-notice`; readAxTree/captureScreenshot of the chrome; enumerateTabs.
- **Raw state**: bar text/buttons absent from the a11y tree; `#hang-notice` display `none`, class `hidden`; census still `hung`, same pid; strip still "… — not responding".
- **Expected**: the bar is hidden; census still `hung`; the guest untouched (scoped to census/pid identity — the stopped guest's pixels stay uncapturable, step 6).
- **Verdict**: pass — the PNG shows no bar under the toolbar.
- **Evidence**: `step-7-screenshot.png`, `step-7-axtree.txt`, `step-7-census.json`

### Checkpoint 8 — PASS
- **Actions taken**: `kill -CONT`; census poll → `ok` in ~11 ms; computed-style re-read; readAxTree/captureScreenshot of the chrome; captureWindow (succeeds again); enumerateTabs.
- **Raw state**: links.html rendered, no bar; strip "keyboard-nav: links" with no suffix; census `ok` with the SAME pid (resumed, not restarted).
- **Expected**: the renderer answers; census `ok`; the bar stays hidden; the strip suffix is gone.
- **Verdict**: pass — all four clauses on rendered evidence.
- **Evidence**: `step-8-window.png`, `step-8-census.json`, `step-8-axtree.txt`, `step-8-screenshot.png`

### Checkpoint 9 — PASS
- **Actions taken**: navigate wcId 19 → busy.html; clicked "Busy loop 20 s" (located by `getBoundingClientRect`); census poll → `hung` at ~16.2 s; `evaluate` click on `#hang-notice-kill`; census poll every 500 ms recording every read → `ok` with a NEW pid at ~12.1 s; captureWindow (a transient "chrome window unavailable" at the reload boundary; retry succeeded), readAxTree, enumerateTabs.
- **Raw state**: post-kill sequence `hung` ×24 → `ok` ×1; `crashed` never appeared (grep across all 25 transients); bar gone; busy.html rendered fresh.
- **Expected**: hung → reloading → visible with a new pid; never `crashed`; bar gone.
- **Verdict**: pass — the never-crashed clause checked exhaustively.
- **Evidence**: `step-9-window.png`, `step-9-census-transient-post-kill-{1..25}.json`, `step-9-axtree.txt`, `step-9-census.json`
- **Orchestrator note**: ~12 s from the Kill click to the reload — `forcefullyCrashRenderer()` against a busy renderer is not instant; acceptable, worth a line in the docs.

### Checkpoint 10 — PASS
- **Actions taken**: read THE window's `chromePid`; census before; `kill -SEGV <chromePid>`; enumerateWindows polled every 500 ms for 15 s (30 reads saved); census after; captureWindow; captureScreenshot/readAxTree of the chrome; `ss`; a final enumerateWindows.
- **Raw state**: all 30 samples `booted: true` with the OLD chromePid (the crash+reload completed after the last sample, inside one 500 ms gap); `activeTabWcId` 19 before and after; the ordered wcId list identical; all 10 rows `ok`; composite shows strip/toolbar/active page; the strip lists all 10 tabs by name; `chromePid` changed (the Validator's own fresh read confirmed it — the Executor had not saved that post-loop read as a file).
- **Expected**: `booted` returns true with the same active tab; same wcIds in the same order, all `ok`; the composite shows strip/toolbar/page; a new `chromePid`.
- **Verdict**: pass — census identity, composite, and the pid change confirmed (the `booted: false` sample is a cadence expectation, not a product requirement).
- **Evidence**: `step-10-window.png`, `step-10-census-before.json`, `step-10-census-after.json`, `step-10-windows-transient-{1..30}.json`, `step-10-axtree.txt`
- **Validator notes**: process gap — post-loop reads must be saved as evidence files; enforced by the Orchestrator from step 11 on.

### Checkpoint 11 — PASS
- **Actions taken**: openTab ×2 → wcId 21, 22 (two renderer processes); `kill -KILL` to both in one command (KILL directly — SEGV/ABRT do not crash sandboxed guests on this build); settled census; chrome screenshot/a11y; `ss`; a +5 s census.
- **Raw state**: both `crashed`, `loadError {9, killed}`, `pid null` in both censuses (no self-reload); the active one's panel projected; two "127.0.0.2:8991 — crashed" strip entries.
- **Expected**: both `crashed`; the active one shows the panel; no tab reloaded on its own.
- **Verdict**: pass.
- **Evidence**: `step-11-screenshot.png`, `step-11-census-post.json`, `step-11-census.json`, `step-11-axtree.txt`, `step-11-settle-before.json`

### Checkpoint 12 — FAIL (defect F1: a paused window still reads `booted: true`; the tab census hangs against its dead chrome)
- **Actions taken**: `kill -KILL <chromePid>` ×4 from one driver (Orchestrator substitution — see notes), each next kill fired the instant the new `chromePid` appeared: sends at 0 / 267 / 534 / 806 ms; recoveries 1–3 at 263 / 531 / 799 ms. Cycle 4 polled 78 reads over 20 s. The driver's final calls hit the SDK's 60 s timeout; fresh connections: `enumerateWindows` answers in ~5 ms, `enumerateTabs` times out at 60 s (three attempts).
- **Raw state**: cycle 4 from read 2 to the end: `booted: true`, `chromePid: null`, `recoveryPaused: true`, persisting; `booted: false` never observed (the Validator grepped all 78 files). Window 1 unaffected. `ss` empty.
- **Expected**: the first three recover; after the fourth, `booted` stays false for the whole poll AND `recoveryPaused: true`; the app is alive.
- **Verdict**: fail — the literal `booted: false` clause did not hold; `recoveryPaused`/`chromePid: null` and app-alive held.
- **Evidence**: `step-12-windows-before.json`, `step-12-windows-transient-{1,2,3}-{1,2}.json`, `step-12-windows-transient-4-{1..78}.json`, `step-12-windows-post.json`, `step-12-enumeratetabs-timeout.json`, `step-12-log.json` (reconstructed from the driver's console), `step-12-ss.txt`
- **Validator notes**: the `enumerateTabs` hang is the more severe symptom of the same root cause.
- **Operator decision**: continued — fix-and-continue (the cadence the operator endorsed in Flight 2). Root cause: `chrome-recovery.js`'s pause path never clears `record.bootConfigServed`, so the census treats the dead chrome as booted and `executeJavaScript`s into it. Fix pass F1 spawned (clear the flag on pause; drop pushes to a paused record; pins). Rows 13–14 proceed on the current state (shell reads); the relaunch that carries the fix doubles as row 15; row 12 is re-run on the fresh window afterwards (recorded as 12b) and row 13's chrome-line sequence re-checked.

### Checkpoint 13 — PASS
- **Actions taken**: 11 new `crash-log.jsonl` lines extracted and parsed; key-set, origin/leak, and chrome-order assertions scripted.
- **Raw state**: every line has exactly `exitCode jarKind kind origin reason recovery ts windowId`; guest origins only `http://127.0.0.2:8991` / `:8992` with no path; no fixture name, title, or host path anywhere; chrome `recovery` = reloaded ×4 then paused ×1. Reconciliation: guest 6 (step 1 ×2 incl. the wcId-18 collateral, step 3, step 9 `crashed/133/reloaded` — the kill-reload path, step 11 ×2), chrome 5 (step 10 `crashed/139`, step 12 killed/9 ×4).
- **Expected**: ≥ 8 lines; exactly the eight keys; guest origin host-only with no leak; chrome lines `reloaded` ×4 then `paused` ×1.
- **Verdict**: pass — the Validator re-ran every check itself over `step-13-lines.jsonl` (key set, a broader leak list incl. "Google" and "127.0.0.1", an origin regex, the chrome order).
- **Evidence**: `step-13-lines.jsonl`, `step-13-summary.json`

### Checkpoint 14 — PASS
- **Actions taken**: dump directories listed (counts, newest names/mtimes, no contents); `.dmp` newer than run start counted; every `ss` sample concatenated and grepped; a process-tree-filtered `ss`.
- **Raw state**: pending 46 → 54 (+4 dump/meta pairs), completed 0; 4 new `.dmp`; zero non-loopback peers in any sample all run (the only sockets were the driver's own loopback MCP connections).
- **Expected**: at least one new `.dmp`; no request left the app.
- **Verdict**: pass — the Validator re-checked every sample line by line and took its own.
- **Evidence**: `step-14-dumps.txt`, `step-14-ss-all.txt`, `step-14-ss.txt`, `step-14-ss-tree.txt`
- **Validator notes**: 4 dumps vs 2 `crashed`-reason events — debrief note (Crashpad may write more than one artifact per trap); no security bearing.

### Checkpoint 15 — PASS
- **Actions taken**: relaunch attested (the `:49707` pid differs; wcIds renumbered); fresh key from the same protected path; enumerateWindows; enumerateTabs (6 ms); dump counts; `ss`.
- **Raw state**: 2 windows, both `booted: true`, `recoveryPaused: false`; 18 rows, 17 `ok` with numeric pids across every address the run touched (the crashed/hung tabs came back as ordinary loads); the one non-`ok` row is window 1's pre-run leftover (`127.0.0.1:1`, out of scope). Dumps unchanged (54/0).
- **Expected**: fresh boot; `booted: true`; the session restore brings the tabs back at their addresses.
- **Verdict**: pass — the Validator scanned all 18 rows itself.
- **Evidence**: `step-15-windows.json`, `step-15-census.json`, `step-15-dumps.txt`, `step-15-ss.txt`

### Checkpoint 12b — PASS (re-run of row 12 with fix F1 live; the 12/12b pair is the fix-verification record)
- **Actions taken**: same driver shape as 12 (four `kill -KILL` cycles paced on the `chromePid` change: sends at 0/269/530/796 ms; recoveries at 265/527/793 ms; cycle 4 polled 78 reads over ~19.9 s); fresh-connection enumerateWindows; fresh-connection enumerateTabs with a 15 s client timeout; `ss`.
- **Raw state**: cycle 4 from read 2 to the end: `booted: false`, `chromePid: null`, `recoveryPaused: true` (`booted: false` on 77 of 78 reads); enumerateTabs answered in 4 ms with the paused window contributing ZERO rows (window 1's six rows unchanged); enumerateWindows answers for both windows; `ss` loopback-only.
- **Expected**: (row 12's) the first three recover; after the fourth, `booted` stays false AND `recoveryPaused: true`; the app is alive.
- **Verdict**: pass — the Validator grepped all 78 cycle-4 files (77 `booted: false`; read 1 is the pre-signal race); both step-12 defects (the flag and the census hang) resolved by F1.
- **Evidence**: `step-12b-windows-transient-4-{1..78}.json`, `step-12b-windows-final.json`, `step-12b-census-post.json`, `step-12b-log.json`, `step-12b-windows-before.json`, `step-12b-ss.txt`

## Orchestrator Notes

- **Mode**: live two-agent (`SendMessage` available); cache `cold` (the spec opens its own fresh tabs).
- **Rig**: `npm run dev:automation` with admin + dev-mint, launched by the Orchestrator at `e81b9d6` + the uncommitted leg-4 working tree (a11y states, the `.lf-brand` contrast fix, the finalised spec); admin key captured by script into a `chmod 600` scratch file, loaded by env var only; fixtures on `127.0.0.2:8991` (`keyboard-nav`) and `127.0.0.2:8992` (`crash`).
- **Baselines at start**: `crash-log.jsonl` 18 lines (`{N0}`); `Crashpad/pending` 46 files, `completed` 0. Observation to carry: 46 pending dumps exceeds DD8's keep-20 — `pruneDumps` at ready may not be reaching `Crashpad/pending`; checked after the run.
- **Run scoping**: the restored profile held two windows; every row is scoped to the `lastFocused` window (windowId 2, chromeWcId 2) where the run's tabs land. `closeTab` returning `false` for a `failed` tab in the OTHER window is carried to the debrief (possible squawk: close refused for a background window's failed tab, or an admin-scope quirk).
- **SEGV/ABRT do not crash a sandboxed guest on this build (step 1)**: both left the guest at `ok` with the same pid; only SIGKILL crashed it. Leg 2's smoke (BEFORE leg 3 started `crashReporter`) SEGV'd a guest successfully; leg 3's smoke and this run (WITH the reporter) could not. Working hypothesis for the debrief: Crashpad's in-process signal handler inside the seccomp-sandboxed renderer intercepts SEGV/ABRT and the process neither dumps-and-exits nor returns to a crashed state — which would mean a REAL segfault in a web guest now presents as a hang (the bar), not a crash. Product-relevant; needs a spike (a page-triggered real crash, e.g. an allocation bomb or `chrome://crash`-equivalent) before the next flight. Not a failure of this spec's rows (the row notes anticipated the fallback).
- **Apparatus ceiling found (step 6)**: `captureWindow`/`captureScreenshot` on a `SIGSTOP`ped guest time out (no compositor frame) — the spec's "guest remains visible" clause under a stopped renderer is a by-eye observable; the crew file's apparatus notes should gain this fact (squawk candidate) and the spec's row 6 should mark the clause `[by-eye]`.
- **SEGV latency on the chrome view (step 10)**: the chrome renderer kept its OLD pid with `booted: true` for ~15 s after `kill -SEGV`, then crashed and recovered inside a single 500 ms sampling gap. Consistent with the guest finding: with `crashReporter` active, a SEGV runs Crashpad's in-process handler first (minidump capture) — on the unsandboxed chrome it completes after ~15 s and the process exits; on a sandboxed guest it apparently never completes. Debrief item (same root as the step-1 note). Practical consequence for row 12: the four crashes must be paced by the `chromePid` CHANGE, not by `booted: false`.
- **Step 12 substitution**: `kill -KILL` on the chrome for the crash loop instead of SEGV — the recovery cap is reason-agnostic (DD6) and SEGV's ~15 s Crashpad latency would have made the 60 s window a rig-timing test rather than a cap test; row 13's chrome-line assertions (`kind`, `recovery` sequence) are signal-independent.
- **Fix pass F1 (during the run)**: `chrome-recovery.js`'s pause branch now clears `bootConfigServed`; `queueChromeSend` drops pushes to a paused record; pins in `chrome-recovery.test.js`, `window-census.test.js`, `register-tab-ipc.test.js`; CLAUDE.md and `docs/mcp-automation.md` updated; 4980/4980. Carried into the relaunch that serves as row 15; row 12 re-run as 12b on the fresh window.
- **Relaunch (row 15, after checkpoint 14)**: the Orchestrator killed the app by the pid on `:49707`, shredded the old app log, relaunched with fix F1 in the tree, and re-captured the fresh key by script to the same protected path; the crew was re-briefed (renumbered topology, same key path). Attestation: the `:49707` pid changed; the census wcIds renumbered.
- **Defect F2 observed at the relaunch**: `Crashpad/pending` still held 54 files after ready — `pruneDumps` is not reaching the directory Crashpad writes (DD8 keep-20). Fix pass F2 spawned (walk `pending`/`completed`/`new`, prune dump+meta pairs, keep the newest 20, pinned). Not a row of this spec; carried as a leg-4 fix.
- **Tripwire variation (step 14)**: no proxy env at launch (the leg-1 spike showed a refusing proxy breaks the fixture loads on this rig); the Executor samples `ss -tnp` for non-loopback `electron` connections after each crash and in step 14.

## Closing Summaries

### Executor closing

1. SEGV/ABRT do not crash this build's sandboxed guest renderers; only SIGKILL does (steps 1, 3) — go straight to KILL for guest crashes.
2. `captureWindow`/`captureScreenshot` refuse with `capture-timeout` against a `SIGSTOP`ped renderer — "the guest remains visible" under a stopped renderer is a by-eye observable.
3. A SEGV against the chrome renderer takes ~15 s to register (Crashpad's in-process handler runs first); SIGKILL is near-instant. Corroborate a chrome crash by the `chromePid` change, not by catching `booted: false` between samples.
4. `openTab` returns a bare number (the wcId).
5. `navigate` right after `openTab` can race the still-loading page — poll until url/loadState/pid stabilise.
6. Same-origin, same-jar tabs are not guaranteed to share a renderer process — check pids before deciding how many signals a scenario needs.
7. Pre-fix, `enumerateTabs` wedged for the SDK's full 60 s when a window had no live chrome; `enumerateWindows` stayed fast — use it as the liveness proof and set an explicit short client timeout on `enumerateTabs` in doubtful states.
8. Any value read only after a polling loop ends must be saved as its own evidence file.
9. Window topology renumbers totally after a relaunch — re-resolve THE window by `lastFocused`.
No environment hiccups beyond the above; the fixture servers and the env-only key attach worked throughout.

### Validator closing

15 of 16 checkpoints PASS, one INCONCLUSIVE (row 6), one FAIL then confirmed fixed on re-run (row 12 → 12b). No verdict was rendered on Executor prose alone; every panel/bar/strip row was judged from a screenshot or a11y tree, and the security rows (13, 14) were re-derived from raw evidence.
Spec quality: row 6's guest-visibility clause should name the apparatus ceiling (a stopped renderer yields no frame) and become a HAT-only check or accept a proxy; row 12's `booted: false` wording is empirically correct and should stay, with a note that the first post-kill read can race the signal; row 13's illustrative event list undercounts when a collateral crash logs its own line — use "at least" framing.
Apparatus facts for the crew file: same-origin/same-jar tabs may share a renderer and crash collaterally; post-loop reads need their own evidence file; a paused window made `enumerateTabs` hang pre-fix (a product defect that looked like an apparatus one); independent re-checks over raw evidence for any security-invariant row.

## Operator Notes

(none — no operator action was required)
