# Behavior Test Run: kebab-menu — 2026-07-08-19-54-44

**Spec**: [tests/behavior/kebab-menu.md](../../kebab-menu.md)
**Status**: partial
**Started**: 2026-07-08T19:48:00
**Completed**: 2026-07-08T19:56:00
**Duration**: 00:08:00
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
10 of 12 steps PASS on native `WebContentsView` (sheet wcId = 6). **Step 8 FAILs on a security-relevant regression** — the internal `goldfinch://settings` tab appears in `enumerateTabs` (and is readable/drivable) in violation of the internal-session exclusion (the primary "Settings opens the internal tab" behavior itself passes). Step 9 (focus-ring pixel delta) is INCONCLUSIVE-APPARATUS-LIMIT on WSLg. Chrome `wcId` = 1, sheet `wcId` = 6.

## Step Results

### Step 1 — PASS (precondition probe)
`tools/list` includes all driven tools (27 total); `getChromeTarget()` → `{wcId:1, kind:'chrome', url:file://…index.html}` (numeric).

### Step 2 — PASS (kebab present)
- **Raw state**: `#kebab` — `aria-haspopup="menu"`, `aria-expanded="false"`, `aria-label="More"`, text `⋮`. Located at CSS rect right of the pin cluster (media/shields/devtools), last interactive control before the window controls.
- **Verdict**: pass. *Minor spec-drift:* the spec says "immediately to the right of the Shield button" — a DevTools pin now sits between Shield and the kebab, so the kebab is the last toolbar control but not immediately adjacent to Shield. Not a failure.
- **Evidence**: [menu-structure.json](./2026-07-08-19-54-44/menu-structure.json)

### Step 3 — PASS (mouse open, exactly 4 items)
- **Raw state**: `click(1,1373,64)` → `#kebab aria-expanded="true"`; sheet `#sheet-menu data-menu-type="kebab" role="menu"`, **4** `role="menuitem"`: **Settings, Downloads, Print…, Exit** (in order), 0 separators. No fifth item.
- **Verdict**: pass.

### Step 4 — PASS (focus on open)
- **Raw state**: on open, Settings has `tabindex="0"` and is `document.activeElement` (`activeElText:"Settings"`); others `tabindex="-1"`.
- **Verdict**: pass — focus moves into the menu to the first item, not stranded on `<body>`.

### Step 5 — PASS (arrow/Home/End nav)
- **Raw state**: ArrowDown Settings→Downloads→Print…→Exit→**Settings (wrap)**; ArrowUp from Settings→**Exit (wrap)**; Home→Settings; End→Exit. Roving `tabindex="0"` and `activeElement` move together, stay within the 4 items.
- **Verdict**: pass.

### Step 6 — PASS (Escape closes + restores focus)
- **Raw state**: `pressKey(6,'Escape')` → `#kebab aria-expanded="false"`, `document.activeElement.id="kebab"`, chrome `document.hasFocus()=true`.
- **Verdict**: pass.

### Step 7 — PASS (click-outside closes + swallow)
- **Raw state**: menu open → `click(6, 700, 500)` (guest region outside menu) → `#kebab aria-expanded="false"`; active tab URL unchanged (`https://example.com/`), tab count unchanged.
- **Verdict**: pass — menu dismissed and the outside click swallowed (no page navigation).

### Step 8 — FAIL (LIKELY-REGRESSION: internal-session exclusion broken)
- **Actions**: opened kebab, activated **Settings** (`evaluate(6,'document.activeElement.click()')`); read chrome address chip + `enumerateTabs`.
- **Raw state**: address chip `data-state` flipped **web → internal**, address `goldfinch://settings/`, menu closed — **the primary behavior works**. BUT `enumerateTabs` **lists** the internal tab (`wcId 7, jarId:"internal", url:goldfinch://settings/, active`) — count 4→5.
- **Expected**: Settings opens/activates the internal tab (chip `internal`, page renders) **AND** the internal tab does **NOT** appear in `enumerateTabs` (internal-session exclusion).
- **Verdict**: **fail** on the exclusion conjunct. Follow-up probes (see the flight-level finding below) show the internal tab is not only enumerated but **readable and drivable**: `readDom(7)`, `readAxTree(7)`, `captureScreenshot(7)`, `click(7)`, `pressKey(7)`, `typeText(7)`, `activateTab(7)` all succeed, and `navigate(7,"https://example.com")` **succeeded**, redirecting the privileged internal session to an arbitrary origin. Only `evaluate/getZoom/setZoom/findInPage/printToPDF` refuse `"internal-session excluded"`. This violates `internal-session-exclusion.md` (DD5). **Classification: LIKELY-REGRESSION (security-relevant), FD to triage — not fixed here.** The primary "Settings opens the internal tab" behavior is intact.
- **Evidence**: [menu-structure.json](./2026-07-08-19-54-44/menu-structure.json); flight-level breach dump at `/tmp/behavior-tests/goldfinch/_internal-session-exclusion-regression/` (SUMMARY.txt + raw op payloads).

### Step 9 — INCONCLUSIVE-APPARATUS-LIMIT (focus-ring pixels)
- **Actions**: anchored focus (`click` address bar), `pressKey(1,'Tab')` ×8 until `activeElement="kebab"` (traversal: address→zoom-out→zoom-in→zoom-reset→toggle-media→toggle-privacy→toggle-devtools→automation-indicator→kebab); captured unfocused vs focused `captureWindow`.
- **Raw state**: the two frames are **byte-identical** (same md5). captureWindow is confirmed live (menu-open vs -closed differs by 33 029 px), so the identical frames mean **no** focus styling painted for *either* the address bar losing focus or the kebab gaining it — i.e. WSLg does not composite `:focus-visible` under injected/synthetic focus.
- **Verdict**: inconclusive-apparatus-limit — the kebab is **keyboard-reachable** and focusable (Tab lands on `#kebab`; a11y half confirmed), but the **rendered focus-ring delta is not observable** via captureWindow on this rig under injected focus. HAT-authoritative for the ring pixels. Not failed.
- **Evidence**: [kebab-unfocused.png](./2026-07-08-19-54-44/kebab-unfocused.png), [kebab-focused.png](./2026-07-08-19-54-44/kebab-focused.png), [cap-menuclosed.png](./2026-07-08-19-54-44/cap-menuclosed.png), [cap-menuopen.png](./2026-07-08-19-54-44/cap-menuopen.png)

### Step 10 — PASS (Exit + Print present, not committed)
- **Raw state**: End roves to **Exit**; ArrowDown reaches **Print…**; both `role="menuitem"` with correct accessible names, keyboard-reachable.
- **Verdict**: pass — Exit and Print… present/selectable. **Exit NOT activated** (would quit); **no Print dialog raised** (asserted present/selectable only, per the modal-dialog trap).

### Step 11 — PASS (mutual exclusion)
- **Raw state**: open container → `#new-tab-menu aria-expanded="true"`, `#kebab="false"`, sheet `data-menu-type="container"` (14 items); then open kebab → kebab `true`, container `false`, sheet `data-menu-type="kebab"` (4 items); then open container → container `true`, kebab `false`, sheet `container`. Never both open (model-replace swap).
- **Verdict**: pass.

### Step 12 — PASS (Tab closes + restores focus)
- **Raw state**: menu open → `pressKey(6,'Tab')` → `#kebab aria-expanded="false"`, `activeElement.id="kebab"`, chrome `hasFocus=true`.
- **Verdict**: pass.

## Orchestrator Notes
Single-agent convenience sweep. Sheet wcId (6) discovered by the background-tab-safe probe walk (readDom on ids 6–12, skipping tabs 2–5 and chrome 1; id 6 returned `menu-overlay.html` markup). The internal-session regression surfaced at step 8 and was characterized with targeted follow-up probes (not part of the scripted steps). State restored afterward (polluted internal tab wc7 closed, normal tab reactivated).

## Closing Summaries

### Executor closing
The kebab menu's structure, four-item model, APG keyboard contract (arrow/Home/End/wrap, focus-on-open, Escape/Tab close + focus-restore), mouse open, click-outside dismissal + swallow, and container↔kebab mutual exclusion all pass cleanly on the F8 sheet. Two non-passes: step 8 exposes a **security-relevant internal-session-exclusion regression** (the internal tab is enumerable + readable + drivable + navigable — see the flight-level finding), and step 9's focus-ring is not pixel-observable on WSLg under injected focus.

### Validator closing
The step-8 finding is the load-bearing one for the FD: the DD5 internal-session guard is only applied to a subset of ops post-migration. Recommend the FD treat it as a blocking security regression and confirm which layer should reject internal wcIds for `readDom`/`readAxTree`/`captureScreenshot`/`click`/`navigate`/`activateTab`/`enumerateTabs`. Step 9 stays HAT for the ring pixels.

## Operator Notes
`internal-session-exclusion` breach captured under `/tmp/behavior-tests/goldfinch/_internal-session-exclusion-regression/` (local-only evidence).
