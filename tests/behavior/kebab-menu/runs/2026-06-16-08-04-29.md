# Behavior Test Run: kebab-menu — 2026-06-16-08-04-29

**Spec**: [tests/behavior/kebab-menu.md](../kebab-menu.md)
**Status**: pass
**Started**: 2026-06-16T08:05:10
**Completed**: 2026-06-16T08:05:25
**Mode**: FD-driven focused live pass (Flight 6 leg 7 `verify-integration`) — cited machine-read evidence. First run on the **admin MCP surface** (post-migration); supersedes CDP-`:9222`/`cdp-driver.mjs`.
**Apparatus**: admin MCP client over `127.0.0.1:49707`, app via `npm run dev:automation` (no `:9222`). Chrome target via `getChromeTarget()` → `wcId 1`. NOT the `chrome-devtools` MCP.

## Summary
Focused live pass of the kebab (⋮) overflow menu via the admin MCP surface: the menu opens by trusted click, exposes **exactly two items — "Settings" and "Exit"** — as a `menu`, and `Escape` closes it and restores focus to the trigger button. **Core APG menu-button checkpoints passed.**

## Step Results

### Step (open) — trusted click opens the menu with two items — PASS
- **Actions**: `getChromeTarget` → `wcId 1`; `click(wcId, 1372, 63)` (the kebab, rightmost toolbar control, coordinate located via `captureWindow`); `readAxTree`.
- **Raw state**: a `menu` role is present; menu items are exactly **["Settings", "Exit"]** — two items, correct labels, correct order.
- **Verdict**: PASS. [a11y]
- **Evidence**: `01-menu-open.png` (the dropdown renders below the kebab with Settings + Exit).

### Step (Escape) — Escape closes + restores trigger focus — PASS
- **Actions**: `pressKey(wcId, 'Escape')`; `readAxTree`.
- **Raw state**: zero `menuitem`s remain (menu closed); focused node is `button` "More" — focus restored to the kebab trigger.
- **Verdict**: PASS — the Escape-closes-and-restores-focus leg of the APG menu-button pattern holds. [a11y]
- **Evidence**: `02-after-escape.png`.

## Orchestrator Notes
FD-driven via an ephemeral admin-MCP harness (deleted after the run). The full spec's mouse-open / keyboard-open / arrow-navigation / outside-click-closes / focus-ring-delta checkpoints (12 total) were not all exercised in this focused pass; the open + two-items + Escape-restore core was driven and passed, and the trusted-click + a11y-read + focus primitives were proven in the leg-2 spike. Full coverage (incl. the keyboard-open APG path and the rendered focus-ring delta) is available via a complete run or the leg-8 HAT. The trigger's accessible name surfaced as "More".

## Closing Summaries
### Executor closing
Trusted `click` on the chrome kebab opened the real menu; `readAxTree` enumerated the two items; `pressKey Escape` closed it and returned focus to the trigger — the migrated apparatus drives the menu exactly as the CDP path did.
### Validator closing
The APG menu-button core (open → two items → Escape restores focus) is fully observable over the admin MCP surface. Spec semantics preserved.

## Evidence
Ephemeral (never committed): `/tmp/behavior-tests/goldfinch/kebab-menu/runs/2026-06-16-08-04-29-evidence/` — `00-before.png`, `01-menu-open.png`, `02-after-escape.png`, `ax-menu-open.json`. Re-derive by re-running.
