# Behavior Test Run: kebab-menu — 2026-06-07-09-56-42

**Spec**: [tests/behavior/kebab-menu.md](../kebab-menu.md)
**Status**: pass
**Started**: 2026-06-07T09:56:42Z
**Completed**: 2026-06-07T10:02:00Z
**Duration**: ~00:05:18
**Executor**: a9f0aa429e5946a31
**Validator**: a31dee04c5864c3e1

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/kebab-menu/2026-06-07-09-56-42/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed. 0 failed; 0 inconclusive.

Verifies mission **SC3** (kebab present in the toolbar right of Shield, exactly two items Settings + Exit) and **SC8** (keyboard-operable, ARIA-correct, visible focus ring). SC4 (Exit terminates the app) is verified manually outside this test; this run only confirmed the Exit item is present/focusable and deliberately never activated it.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Reloaded renderer (cold), `eval '1+1'`, `curl :9222/json`, counted `#tabs [role=tab]`.
- **Raw state**: `:9222` answers; renderer 'page' target = `file:///…/src/renderer/index.html` (+ a 'webview' guest 'Google'); tab count = 1.
- **Expected**: `:9222` responds, renderer target present, tab count recorded.
- **Verdict**: pass — Validator reproduced via fresh `/json`.

### Checkpoint 2 — PASS [a11y]
- **Raw state**: toolbar interactive order `[back, forward, reload, address, toggle-media, toggle-privacy, kebab]`, last = `kebab`; kebab `aria-label/title='More'`, text `⋮`, `aria-haspopup='menu'`, `aria-expanded='false'`; kebab rect left=1357 ≥ shield right=1351.
- **Expected**: kebab immediately right of Shield, last interactive control, accessible name + `aria-haspopup="menu"` + `aria-expanded="false"`.
- **Verdict**: pass — Validator reproduced exactly via fresh eval; ARIA correct.

### Checkpoint 3 — PASS [a11y]
- **Raw state**: trusted click on kebab → `aria-expanded='true'`; `[role=menu]` popup; exactly 2 `[role=menuitem]` = `['Settings','Exit']`; no third.
- **Expected**: menu opens; `role=menu` with exactly two `role=menuitem` (Settings, Exit).
- **Verdict**: pass — Validator confirmed via fresh trusted click; `step-3-screenshot.png` shows the two-item popup anchored under the kebab.
- **Evidence**: `step-3-screenshot.png`

### Checkpoint 4 — PASS [a11y]
- **Raw state**: on open, `activeElement.id='kebab-settings'` (role menuitem), not body.
- **Expected**: opening moves focus to the first item; not stranded on body.
- **Verdict**: pass — focus moved into menu onto first item.

### Checkpoint 5 — PASS [a11y]
- **Raw state**: from Settings — ArrowDown→Exit, ArrowDown→Settings (wraps), ArrowUp→Exit (wraps), Home→Settings, End→Exit; focus stayed within the two items.
- **Expected**: APG arrow nav between items (wrap recorded), Home/End jump, focus contained.
- **Verdict**: pass — Validator reproduced with fresh trusted keys; roving nav wraps both directions (APG-conformant).

### Checkpoint 6 — PASS [a11y]
- **Raw state**: trusted Escape → `aria-expanded='false'`, `activeElement.id='kebab'` (focus restored to trigger).
- **Expected**: Escape closes, `aria-expanded=false`, focus restored to trigger.
- **Verdict**: pass — correct focus management, confirmed fresh.

### Checkpoint 7 — PASS
- **Raw state**: reopened (`aria-expanded='true'`), then trusted click at address-bar center (outside menu) → `aria-expanded='false'`, `getComputedStyle(menu).display='none'`.
- **Expected**: outside click closes; `aria-expanded` returns to false.
- **Verdict**: pass — outside-click dismissal confirmed fresh.

### Checkpoint 8 — PASS
- **Raw state**: baseline tabs=1, webview src `https://www.google.com/`; after focusing Settings + trusted Enter → menu closed, tabs=1 (unchanged), src unchanged, no new tab/navigation/error.
- **Expected**: selecting Settings closes the menu and does nothing else (inert placeholder).
- **Verdict**: pass — Settings is a no-op that only closes the menu.

### Checkpoint 9 — PASS [a11y]
- **Raw state**: focused kebab computed `outline='rgb(245, 197, 24) solid 2px'` (gold), `boxShadow='none'`; after blur, no outline. Both PNGs inspected: focused shows a distinct gold ring around ⋮, unfocused none.
- **Expected**: visible focus-ring delta (not `outline:none`, not invisible).
- **Verdict**: pass — Validator independently Read both PNGs; clear rendered delta (rendered state, not just DOM).
- **Evidence**: `step-9-focused.png`, `step-9-unfocused.png`

### Checkpoint 10 — PASS [a11y]
- **Raw state**: `#kebab-exit` present, text `Exit`, role menuitem; after `exit.focus()`, `activeElement.id='kebab-exit'` (keyboard-reachable). Exit NOT activated; Escape closed the menu; app still responds (`document.title='Goldfinch'`).
- **Expected**: Exit present, named "Exit", keyboard-reachable; not triggered.
- **Verdict**: pass — present + focusable; harness intact (Exit never fired).

## Orchestrator Notes

- **Execution mode**: re-spawn (no `SendMessage` in this session) realized as the project's established **single-pass Executor + independent Validator** pattern (Witnessed separation preserved — the acting agent never judged; the Validator independently re-observed via the same apparatus, including reading the PNGs). Consistent with Flight-1 runs.
- **Apparatus**: committed `scripts/cdp-driver.mjs` (trusted `Input.dispatch*`, attach-don't-launch) against the live `:9222` renderer. The `chrome-devtools` MCP was deliberately NOT used (the standing Goldfinch false-pass trap). The leg-4 apparatus prep (`ArrowDown`/`ArrowUp` added to KEYS) was exercised by checkpoints 5 and made the APG vertical nav drivable.
- **Cache**: cold (renderer hard-reloaded before Step 1).
- **Accessibility Validator**: `Enabled:false` per project config; the base Validator judged the `[a11y]`-marked rows (keyboard operability, focus management, ARIA correctness) as the authority.

## Closing Summaries

### Executor closing
All 10 steps performed via `cdp-driver.mjs` on the standing app; renderer target (not the webview guest) confirmed throughout. Trusted clicks (computed rect centers) and trusted keys fired the real handlers. Exit was never activated, so the harness stayed up. Minor ops note: `DOMRect` doesn't JSON-serialize from `eval`, so rect fields were read individually.

### Validator closing
Independently reproduced every checkpoint with fresh observations via the same apparatus rather than trusting the Executor's report; weighted rendered state for Step 9 by Reading both PNGs. Implementation is APG-conformant: `role=menu`/`role=menuitem`, roving focus that wraps both directions, focus-into-menu on open, focus-restore-to-trigger on Escape, outside-click dismissal, and an inert Settings placeholder that mutates nothing. No spec defects; the menu persisting collapsed in the DOM (`display:none`) is consistent across all close paths.

## Operator Notes

Run drove the live app the operator/Flight Director launched (`npm run dev:debug`, `:9222`). SC3 + SC8 verified by this run; SC4 (Exit quits) remains a manual check (next).
