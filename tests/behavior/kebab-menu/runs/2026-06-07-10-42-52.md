# Behavior Test Run: kebab-menu — 2026-06-07-10-42-52

**Spec**: [tests/behavior/kebab-menu.md](../kebab-menu.md)
**Status**: pass
**Started**: 2026-06-07T10:42:52Z
**Completed**: 2026-06-07T10:50:00Z
**Duration**: ~00:07:00
**Executor**: a36112b3d2ce16eb6
**Validator**: a7d5054f6e2b5965c

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/kebab-menu/2026-06-07-10-42-52/` during the run; re-derivable by re-running the spec.

## Summary

12 / 12 checkpoints passed. 0 failed; 0 inconclusive.

Re-run after the flight-debrief added two checkpoints for the operator's HAT-introduced behaviors: **Step 11 (menu mutual exclusion)** and **Step 12 (Tab/Shift+Tab closes the kebab menu + restores focus)**. All 10 original checkpoints still pass; the two new checkpoints pass. This closes the debrief's top action item (the HAT behaviors now have a regression net).

## Checkpoint Results

All checkpoints PASS; the Validator independently re-drove each via trusted CDP (`scripts/cdp-driver.mjs` on the live `:9222`) rather than trusting the Executor's report, and Read both focus-ring PNGs for Step 9.

### Checkpoints 1–10 — PASS
Identical behavior to the prior run (`2026-06-07-09-56-42.md`): kebab present right of Shield + last toolbar control (2); opens with `role=menu` + exactly [Settings, Exit] (3); focus-into-first-item on open (4); APG arrow nav wraps both ways + Home/End (5); Escape closes + restores focus to trigger (6); outside-click closes (7); Settings inert — tab count + active src unchanged (8); visible gold focus-ring delta confirmed by both computed `outline` and the two PNGs (9); Exit present/focusable, never activated (10).

### Checkpoint 11 — PASS (NEW: menu mutual exclusion)
- **Raw state**: container open (`#new-tab-menu` aria-expanded=true) + kebab closed → click kebab → container CLOSED (aria-expanded=false) + kebab OPEN → click `▾` again → kebab CLOSED + container OPEN → Escape → both closed.
- **Verdict**: pass — opening either menu dismisses the other; never both open. Validator re-drove via the triggers' `aria-expanded` (the container menu has no `role=menu`, so its trigger is the authoritative open-state signal).

### Checkpoint 12 — PASS (NEW: Tab closes the kebab menu)
- **Raw state**: open kebab (first item focused) → trusted `Tab` → menu hidden, `aria-expanded=false`, `activeElement=#kebab`; reopen → trusted `Shift+Tab` → same (closed, focus on trigger).
- **Verdict**: pass — both Tab and Shift+Tab close the kebab menu and restore focus to the trigger (not stranded on a hidden menuitem or body).

## Orchestrator Notes

- **Execution mode**: single-pass Executor + independent Validator (Witnessed separation preserved; the Validator independently re-drove every checkpoint via the same apparatus). `SendMessage` absent.
- **Apparatus**: committed `scripts/cdp-driver.mjs` (trusted input, attach-don't-launch) on the live `:9222` renderer; `chrome-devtools` MCP deliberately not used.
- **Build under test**: `flight/2-kebab-menu` (kebab + `app-quit` IPC + the leg-5 HAT fixes), relaunched fresh on this branch for the re-run (the prior session's app instance was a different branch).
- **Cache**: cold (renderer hard-reloaded before Step 1).
- **Accessibility Validator**: `Enabled:false` per project config; the base Validator judged the `[a11y]` rows.
- **Spec tweak from this run**: Step 11's observable was tightened to read the container menu's open-state from `#new-tab-menu`'s `aria-expanded` (it has no `role=menu`), per the Validator's spec-quality note.

## Closing Summaries

### Executor closing
All 12 steps driven via `cdp-driver.mjs` on the standing app; renderer target (not the webview guest) used throughout. Trusted clicks (computed rect centers) and trusted keys fired the real handlers. Exit never activated. The two new checkpoints (11, 12) behaved exactly as the HAT fixes intend: mutual exclusion both directions, Tab/Shift+Tab close + focus-restore.

### Validator closing
Re-drove every checkpoint independently rather than trusting the report; read both PNGs for the focus-ring delta. Implementation is APG-conformant and the two HAT additions hold. One spec-quality note (now folded into the spec): the container menu lacks `role=menu`, so Step 11 should read its open-state from the trigger's `aria-expanded` — which I did. No defects.

## Operator Notes

Re-run requested during the flight debrief to give the HAT-introduced behaviors a committed regression net before marking the flight `completed`.
