# Behavior Test Run: tab-cycling — 2026-07-14-19-47-08

**Spec**: [tab-cycling.md](../../tab-cycling.md)
**Status**: pass
**Started**: 2026-07-14T19:47:08Z
**Completed**: 2026-07-14T21:35:00Z (approx)
**Duration**: ~01:48
**Executor**: sub-agent (Sonnet), live SendMessage continuation
**Validator**: sub-agent (Sonnet), live SendMessage continuation

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/tab-cycling/2026-07-14-19-47-08/` during the
run; re-derivable by re-running the spec.

## Summary

**11 / 11 steps PASS** — first run of this spec (M09 Flight 3 leg 2). All
three capture points verified live (chrome keydown: Steps 3/6/7;
guest `before-input-event`: Steps 4/5; sheet accelerator: Step 9);
visual-order resolution proven on both the jump path (Step 8, pinned
prediction + discriminating rerun) and the cycle path (Step 9); wrap both
directions; out-of-range jump no-op with a delivery positive control;
address-replace semantics; internal-guest non-trapping; single-tab
degenerate case.

## Checkpoint Results (abridged — per-step verdicts all pass)

- **1** Precondition probe: six tools present; chrome wcId C=1. (Early
  protocol wobble corrected: the Validator initially searched the project
  tree for evidence and wrote fresh observations INTO the repo — removed by
  the orchestrator; all evidence lives at the /tmp path. Verdict unaffected.)
- **2 (setup)** N=7 (boot tab + 6 fixtures), titles pairwise distinct,
  wcId↔tab-id mapping recorded.
- **3** Chrome-focus cycling with an unsubmitted address edit: cycle fired
  with `activeElement='address'`; the address value was REPLACED by the new
  active tab's URL (Chrome-parity; the design-review-corrected assertion).
- **4** Guest-delivered Ctrl+Tab cycled to the DOM-successor. Accepted
  apparatus deviation: `document.hasFocus()` is unsatisfiable under WSLg
  (false even in chrome with a working focused input — negative controls
  captured); delivery-into-guest is by tool construction (foreground-to-act
  + direct sendInputEvent), corroborated by chrome focus staying untouched.
- **5** Scroll-suppression stand-in (Ctrl+Tab per the documented KEY_MAP
  gap): guest scrollY 0 before and after; successor activated. The run log
  records honestly that scrollY is a weak discriminator for a Tab chord —
  the real PgDn/PgUp check awaits the KEY_MAP extension (follow-up below).
- **6** Jumps: Ctrl+1→pos1; Ctrl+7→actual last (N=7); out-of-range Ctrl+8
  delivered ({ok:true}) and provably no-op (byte-identical before/after
  order arrays); Ctrl+9→last from a non-last start.
- **7** Wrap: last+Ctrl+Tab→first; first+Ctrl+Shift+Tab→last.
- **8** Post-reorder jump resolves VISUAL order: reorder moved tab-2 to
  pos 3 (one-slot rule); pinned prediction matched; the Executor added a
  discriminating rerun from a neutral tab (Ctrl+3 → the MOVED tab, not the
  pre-reorder pos-3 holder) because the prescribed sequence left the target
  already active — deviation accepted as purely additive. Spec improvement
  noted below.
- **9** Sheet-open cycle: one Ctrl+Tab into the sheet (wcId probed per the
  background-tab-safe technique) closed the menu AND switched to the
  successor in the CURRENT post-reorder order — screenshots inspected by
  the Validator; third capture point closed.
- **10** Internal-tab non-trapping: Ctrl+Tab and Ctrl+1 delivered into the
  `goldfinch://settings` guest both moved the active tab away. Apparatus
  facts recorded: `evaluate` is refused on the internal session even for
  admin (policy, evidenced); the settings tab appends last, so the cycle
  observation composes with Step 7's independently-proven wrap.
- **11** Single-tab wrap: byte-identical before/after with delivery proven
  — "nothing changed" IS the pass per the spec's own note.

## Orchestrator Notes

- Live two-agent mode; cache-cold; fresh crew (first run). Free-fallback
  port per the pin-if-free rule.
- Operator decisions: none needed (no fails).
- **Follow-ups routed**: (a) `pressKey` KEY_MAP lacks `PageDown`/`PageUp` —
  automation-surface gap (flight-log Anomaly, maintenance candidate); when
  fixed, upgrade Step 5 to the real keys. (b) Spec polish: Step 8 should
  activate a neutral tab between reorder and jump (the Executor's rerun
  becomes the prescribed sequence); Step 10 variant could seat the internal
  tab mid-order. (c) Evidence-protocol reminder for future validators:
  evidence lives at /tmp, never the repo (early-run correction).

## Closing Summaries

Executor: step notes captured per checkpoint; the Step-8 discriminating
rerun and the Step-6 delivery positive control were the run's key
evidence-quality moves. Validator (run summary, Step 11 notes): 11/11 pass;
accepted apparatus deviations as recorded; spec-improvement suggestions
carried above.

## Operator Notes

First run green end-to-end; spec transitions `draft` → `active`. Mission
criterion "cycle and jump from the keyboard, chrome or web content" is now
behavior-test-backed.
