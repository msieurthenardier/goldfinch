# Behavior Test Run: tab-context-menu — 2026-07-16-06-33-26

**Spec**: [tab-context-menu.md](../../tab-context-menu.md)
**Status**: pass — **10/10, no repairs.** F8 leg 4 modifies this menu; **nothing is broken.**
**Started**: 2026-07-16T06:33:26Z
**Completed**: 2026-07-16T07:55:00Z (approx)
**Executor**: live sub-agent
**Validator**: live sub-agent, independent
**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/tab-context-menu/2026-07-16-06-33-26/` (31 files); re-derivable by
re-running the spec.

## Summary

**Why this run exists**: **M09 F8 leg 4 adds items to this menu.** Per DD8, `tab-context-model.js`
gained one **flat** item per **other** open window — `tab:move-window:<windowId>`, labelled
`Move to window "<title>"`, gated on the **same** `!isLastTab && !isInternal` pair as
`tab:move-new-window`. **Absent/empty `moveTargets` — the single-window case — must emit NOTHING**:
no header, no note, no empty submenu.

**10/10 PASS, no repairs. F8 broke nothing.** The menu is **byte-identical to its F5/F6 shape**:
**zero** `Move to window` strings across all 31 evidence files, with **single-window confirmed at six
independent checkpoints** spanning pre-flight → step 10. All F5/F6 behaviors intact: capture-at-open
dispatch, both ordered sweeps, duplicate-with-history, mid-strip reopen, Escape refocus. **DD8's
"absent/empty `moveTargets` emits NOTHING" holds.**

## Checkpoint Results

**Steps 1–10 — ALL PASS.** Every pass rests on DOM, a11y, **and** pixels agreeing verbatim; **no pass
rests on a pixel alone.** Steps 3 and 9 are **EXACT enumerations** — a leaked item fails them, a
missing item fails them — and their screenshots visibly discriminate (5-item vs 3-item menu).

The strongest instrument in the run, found by the Validator at pre-flight: **`menu-overlay.css:40`
carries its own `.hidden { display: none !important; }`** (the sheet document does not inherit
`styles.css`), so a hidden `#sheet-menu` is **genuinely gone from the a11y tree**. ⇒
**`readAxTree(sheetWcId)` discriminates open from closed in BOTH directions**, and a closed menu
renders **blank**. That is the opposite of a clause returning an image regardless of state.

## Headline finding — DD8's omission rule is UNFALSIFIABLE *within this spec*. The discriminating pair is CROSS-SPEC.

**This run's passes are valid F5/F6 assertions and must NOT be read as DD8 coverage.**

At one window, *"no `Move to window` item"* passes **identically** whether:
- the omission gate works, **or**
- the `move-targets` seed/push is entirely broken, **or**
- **the DD8 loop was never written at all.**

**No step in this spec ever expects that item PRESENT.** By this project's own standard — *an
instrument that discriminates nothing is a finding, not a pass* — the F8 leg-4 addition is
**unfalsifiable here**.

> **The presence side lives in `tests/behavior/tab-tearoff.md` row 8**, run green this same leg:
> **exactly one** `Move to window "…"` item with a **live-resolved** caption
> (`Move to window "Fixture Page 2 — tabstrip"`), verified on **DOM and `readAxTree` agreeing
> verbatim**, with `Move to new window` still present beside it.
>
> **Neither spec covers DD8 alone; together they do.** Absence is meaningless without presence, and
> presence lives in the other file. **This is precisely the coverage that disappears when either
> spec is read in isolation** — so it is now recorded in **both** files' Out of Scope, where a reader
> of *either* will see it.
>
> **Contrast worth drawing** (the Validator's): the `stackSize` rule **does** have a real
> discriminating pair inside this spec — Step 3's absence (empty stack) plus Step 9's presence
> (positive control). **DD8 has no such pair. That asymmetry is the gap.**

## Why this files `pass` and the sibling filed `partial` — the standard applied evenly

A sibling (`multi-window-shell`) was filed **`partial`** this same leg for a **pixel clause that
returns an image whether or not the menu is open**. That standard was applied here and **does not
trigger**: **no row in this spec is unfalsifiable.**

**The distinction is real and worth keeping**: DD8's gap is a **coverage gap OF the spec** — a
property no row claims — **not a nothing-discriminating clause WITHIN it** — a row that claims a
property and cannot fail. The first is an honest boundary; the second is a false witness. **That is
why this files `pass`, not `partial`.**

## Errata to fold (documentation-only; no re-run required)

1. **Single-window is now an UNSTATED PRECONDITION — and it should be ASSERTED, not assumed.**
   Post-F8, Steps 3/9's EXACT enumerations silently depend on exactly one window being open for the
   whole run. **The Preconditions never say so.** The Validator verified window count via
   `enumerateWindows` itself at six checkpoints rather than assuming it — the spec should require
   that rather than rely on a diligent validator.
2. **Observables Required omits `enumerateWindows`**, though Preconditions mandate it for sheet-wcId
   discovery.
3. **Step 9 confounds two omission rules**: a sole tab triggers **both** `isLastTab` **and**
   `tabsToRight === 0`, so it cannot isolate the `tab:close-right` rule. The spec's Out of Scope
   already concedes this ("implicit"). **Not a blocker — recorded so no one reads Step 9 as pinning
   `tabsToRight`.**
4. **Step 9's "the stack is non-empty from Step 9's own sweep"** is loose — the stack is already
   non-empty from Steps 4/5/6/8. Immaterial to judging.

## Orchestrator Notes

- **Port 49764**, **bind-probe verified**. Ran **concurrently** with `tab-reorder` on a separate rig
  (`:49763`) and separate scratch profile; both Executors confirmed they were bound to their own pid
  and never contacted the other. **No cross-talk** — `sendInputEvent` injects per-webContents.
- The **pre-existing `:8000`** fixture server (not ours) was used **read-only** and left running.
- **The Executor corrected the FD's briefing, and its mechanism is the right one.** I told both agents
  that chrome **and sheet** targets "never activate". **The sheet is not chrome**: `isChromeContents`
  is true only for *registered chrome* contents, so the sheet classifies as **`'guest'`** and
  `readAxTree`/`captureScreenshot` **do** enter the activate branch (`input.js:234`). It is safe only
  because `activateTab` → `chromeForTab(sheetWcId)` → `registry.getChromeForTab(7)` → **null** for the
  non-registry overlay → returns `false`: **no raise, no throw.** Same conclusion, different reason —
  and the reason matters, because the safety rests on the **overlay branch**, not on classification.
  **Both agents then confirmed it empirically** rather than leaving it as a source argument:
  `enumerateTabs` after step 3's `readAxTree(7)` still showed T4 active, and step 9's
  `captureScreenshot(7)` did not disturb the Escape that followed.
- **Key hygiene**: `$GF_ADMIN_KEY` only, never a literal; the launch log was grepped, not catted.
  **Zero operator-identity leaks across 31 evidence files.**

## Closing Summaries

### Executor closing
Formed its **own** prediction from `tab-context-model.js` before running — that `moveTargets` defaults
`[]`, the per-window loop sits **inside** the `!isLastTab && !isInternal` gate, and F8 should
therefore add **zero** rows at one window — and then measured exactly that. Caught at pre-flight that
`captureWindow` takes `windowId` (not a `wcId` overload) and that `ContextMenu`/`F10` are genuinely
absent from `KEY_MAP`, confirming the spec's documented apparatus gap rather than assuming it.

### Validator closing
Traced `moveTargets` to its source (`buildMoveTargets`, excluding the source window **by record
identity**) to form an independent expectation rather than trusting the briefing. Found the run's best
instrument (`menu-overlay.css`'s own `.hidden` rule) and its most important limit (DD8 unfalsifiable
here) **at pre-flight, before either could bias a verdict**. Refused in advance to accept "item X
absent" from a null-returning `querySelector` — indistinguishable from a wrong selector, and droppable
by `serialize` — requiring full enumeration instead.

## Operator Notes

*(none — run driven by the Flight Director)*
