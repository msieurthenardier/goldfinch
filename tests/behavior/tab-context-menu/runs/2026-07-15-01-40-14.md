# Behavior Test Run: tab-context-menu — 2026-07-15-01-40-14 (regression re-run)

**Spec**: [tab-context-menu.md](../../tab-context-menu.md) (UNMODIFIED — regression re-run)
**Status**: pass
**Started**: 2026-07-15T01:40:14Z
**Completed**: 2026-07-15T01:58:00Z (approx)
**Duration**: ~00:18
**Executor**: fresh agent (batched 1–5, 6–10)
**Validator**: fresh agent (single judgment pass, all 10)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-context-menu/2026-07-15-01-40-14/` during the run; re-derivable by re-running the spec.

## Purpose

Single-window regression gate for the **F6 window-registry conversion**
(M09 Flight 6, leg 2 `window-registry-and-routing`): with one window open,
behavior must be byte-identical post-conversion. Spec and expectations
unmodified from the first run
([2026-07-14-23-13-46](2026-07-14-23-13-46.md), 10/10 PASS).

## Summary

**10 / 10 checkpoints passed — REPRODUCES.** Every checkpoint matched the
first run's passing shape in behavioral substance: same menu compositions
per state (empty-stack Reopen omission, positive flip, sole-tab omissions,
omitted-never-disabled in the AX tree), same captured-tab-id close
semantics (active tab untouched), same ordered-sweep exact end states
(anchor active, cardinality 1 both ways), same duplicate adjacency +
two-direction history fidelity, same mid-strip positional reopen (between
original neighbors, not append), same Escape refocus to the invoking tab
without activation. The Validator independently recomputed click
coordinates against this run's own recorded rects for steps 4–10.

No regression from the registry conversion is evident in this spec's
coverage.

## Differences vs first run — all apparatus-level, none spec-relevant

- Sheet materialized at wcId 7 (vs 3) — lazier creation order this run;
  the probe walk found it per spec.
- `tools/list` now includes `dragPointer` (29 tools) — presence checks
  unaffected.
- App stdout printed no separate bound-port line; the Executor read the
  port (49709) from the electron pid's listening socket and proved it
  operationally with the step-1 probe.
- The KEY_MAP ContextMenu/F10 gap probes were not re-executed — the spec
  scopes the literal keypress Out of Scope and the first run's rejection
  evidence stands.

## Checkpoint Results (compact — regression mode)

| # | Verdict | Anchor evidence |
|---|---------|-----------------|
| 1 | PASS | step1-tools-list-raw.txt, step1-getChromeTarget-raw.txt (C=1) |
| 2 | PASS | step2-jarsseed-raw.txt (Personal+Work), step2-tabrects-raw.txt |
| 3 | PASS | step3-menu-poll-raw.txt (4 items, no Reopen), step3-atopen-enumtabs-raw.txt (T4 still active), step3-sheet-axtree-raw.txt |
| 4 | PASS | step4-after-enum-poll-raw.txt (captured T2 closed; T4 active), step4-after-domorder-raw.txt |
| 5 | PASS | step5-after-enum-poll-raw.txt + step5-after-domcount-raw.txt (exactly one, anchor active; menu had the Reopen flip) |
| 6 | PASS | step6-after-enum-poll-raw.txt + step6-after-domcount-raw.txt (survivors exact) |
| 7 | PASS | step7-after-domorder-raw.txt (DUP beside source), step7-afterback/afterfwd-enum-poll-raw.txt (page3 ↔ page5) |
| 8 | PASS | step8-after-domorder-raw.txt (reopened BETWEEN original neighbors) |
| 9 | PASS | step9-menu2-poll-raw.txt + step9-sheet-axtree-raw.txt (exact sole-tab items; zero disabled nodes) |
| 10 | PASS | step10-refocus-raw.txt (activeElement = invoking tab), step10-after-enum-raw.txt (no activation) |

## Orchestrator Notes

- Regression mode: fresh Executor (two step batches) + fresh Validator
  (single judgment pass over all evidence) rather than the per-checkpoint
  live loop — appropriate for an unmodified ACTIVE spec where the first
  run's shapes are the reference.
- Scratch-profile convention applied (fresh `XDG_CONFIG_HOME`); fixture
  `fixtures-tabstrip` on :8000; admin key held in env only.
- Part of the leg-2 regression TRIPLE (`tab-context-menu`,
  `closed-tab-reopen`, `find-overlay-geometry`).
