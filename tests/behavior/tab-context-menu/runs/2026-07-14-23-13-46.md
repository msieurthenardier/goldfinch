# Behavior Test Run: tab-context-menu — 2026-07-14-23-13-46

**Spec**: [tab-context-menu.md](../../tab-context-menu.md)
**Status**: pass
**Started**: 2026-07-14T23:13:46Z
**Completed**: 2026-07-14T23:32:02Z
**Duration**: 00:18:16
**Executor**: agent ab11ff8457149cca3 (live continuation)
**Validator**: agent a1ef29ad69557d7fb (live continuation)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/tab-context-menu/2026-07-14-23-13-46/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed. 0 failed; 0 inconclusive. No product defects surfaced. First run of this spec — status flips `draft` → `active`.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Apparatus probe: tools/list presence check (all 11 spec-required tools), `getChromeTarget`, jar seed read, boot-state enumerate.
- **Raw state**: Chrome target wcId 1 (C=1). Jar seed exactly Personal + Work (fresh scratch profile via `XDG_CONFIG_HOME`). Single boot tab (wcId 2, personal jar) — closed-tab stack provably empty at launch.
- **Expected**: Required tools present; numeric chrome wcId; fresh deterministic jar seed; single boot tab.
- **Verdict**: pass — all evidence read directly from the run dir; fresh-profile precondition (load-bearing for the Step 3 empty-stack omission) holds.
- **Evidence**: `step1-tools-list-raw.txt`, `step1-getChromeTarget-raw.txt`, `step1-jarsseed-raw.txt`, `step1-enumtabs-raw.txt`

### Checkpoint 2 — PASS (setup)
- **Actions taken**: Opened four Work-jar fixture tabs (`openTab(pageN.html, 'work')`, N=1..4) → wcIds 4–7 (T1–T4); read strip rects + DOM order.
- **Raw state**: Strip D, T1, T2, T3, T4 (five `.tab` elements, DOM order = open order); T4 active as last-opened; all four fixture tabs `jarId:"work"`; titles pairwise distinct; rects geometrically consistent.
- **Expected**: D + T1–T4 in the work jar, T4 active, rects recorded for coordinate clicks.
- **Verdict**: pass — mapping D=wcId2/tab-1 … T4=wcId7/tab-5 established; fixture reachability operationally proven.
- **Evidence**: `step2-opentabs-raw.txt`, `step2-enumtabs-raw.txt`, `step2-tabrects-raw.txt`

### Checkpoint 3 — PASS
- **Actions taken**: Right-click at background T2's rect center (trusted `click` with `button:'right'` on the chrome, C=1). Sheet wcId probe walk (skipping C and all guest wcIds) → sheet = wcId 3. Six ~200 ms menu polls; `readAxTree(3)`; enumerate at open; focus probe; Escape + hidden polls.
- **Raw state**: `#sheet-menu` un-hidden, `data-menu-type:"tab-context"`, aria-label "Tab menu"; items exactly [Close, Close other tabs, Close tabs to the right, Duplicate] with roles [menuitem, menuitem, menuitem, separator, menuitem] — **no Reopen item (empty stack)**. AX tree: role=menu "Tab menu", first item (Close) focused. enumerate at open: T4 still active, T2 still background. Escape re-hid the sheet.
- **Expected**: Menu opens for a background tab without activating it; empty-stack omission of `tab:reopen-closed`; focus lands on the first menuitem.
- **Verdict**: pass — menu open ≠ activation proven at the open moment; async-open polling discipline honored (never a single immediate read).
- **Evidence**: `step3-menu-poll-raw.txt`, `step3-sheet-axtree-raw.txt`, `step3-atopen-enumtabs-raw.txt`, `step3-sheet-probe-raw.txt`, `step3-afterescape-hidden-raw.txt` (+5 more)
- **Validator notes**: Sheet wcId 3 verified distinct from C and every guest wcId; reused for the rest of the run.

### Checkpoint 4 — PASS
- **Actions taken**: Re-opened the menu on background T2; read item rects on the sheet; coordinate-clicked Close's rect center (the sheet's Enter-doesn't-click apparatus nuance); 5 enumerate polls + DOM order + sheet-hidden reads.
- **Raw state**: wcId 5 (T2/page2) absent from enumerate across all polls; D, T1, T3, T4 survive; **T4 still active** — dispatch acted on the tab id captured at open, not `activeTab()`. Strip D, T1, T3, T4. Sheet re-hidden on activation.
- **Expected**: `tab:close` closes the CAPTURED background tab; active tab unchanged; menu closes.
- **Verdict**: pass — the captured-tab-id contract held; T2 captured to the closed-tab stack (work = persist jar), proven operationally at Steps 5/8.
- **Evidence**: `step4-menu-poll-raw.txt`, `step4-after-enumtabs-poll-raw.txt`, `step4-after-domorder-raw.txt`, `step4-after-sheethidden-raw.txt` (+4 more)

### Checkpoint 5 — PASS
- **Actions taken**: Right-click background T3 (strip D, T1, T3, T4; T4 active); menu poll (now 5 items — Reopen present, stack non-empty from Step 4); coordinate-clicked "Close other tabs"; 5 enumerate polls + DOM count + sheet-hidden reads.
- **Raw state**: End state exact BOTH ways: enumerate = exactly one entry (wcId 6/T3, `active:true`) and chrome DOM `.tab` count = 1. D, T1, and the previously-active T4 all closed; **anchor T3 became active**.
- **Expected**: Ordered sweep — anchor activated first, all non-anchor tabs closed, no mid-sweep fallback cascade (asserted via exact end-state cardinality).
- **Verdict**: pass — a fallback cascade would have left a non-anchor tab active or a spurious tab; both excluded. Bonus: this poll documents the Reopen item's positive flip ahead of Step 8.
- **Evidence**: `step5-menu-poll-raw.txt`, `step5-after-enumtabs-poll-raw.txt`, `step5-after-domcount-raw.txt` (+5 more)

### Checkpoint 6 — PASS
- **Actions taken**: Rebuilt strip (N1 wcId 8/page1, N2 wcId 9/page2; N2 active); right-click leftmost background T3; coordinate-clicked "Close tabs to the right"; end-state reads both ways.
- **Raw state**: Survivors exact: single enumerate entry (wcId 6/T3, `active:true`), DOM count 1. N1 + N2 both closed; anchor activated (the active N2 was among the targets). T3 remained the same live webContents (wcId 6) through both sweeps.
- **Expected**: Everything right of the anchor closes; anchor becomes active when the active tab was a target.
- **Verdict**: pass.
- **Evidence**: `step6-after-enumtabs-poll-raw.txt`, `step6-after-domcount-raw.txt`, `step6-menu-poll-raw.txt` (+6 more)

### Checkpoint 7 — PASS
- **Actions taken**: History build per spec (`navigate(6, page5)` — T3: page3 → page5, 2 entries, index 1); opened N3 (wcId 10, active); right-click background T3; coordinate-clicked "Duplicate"; enumerate polls + DOM order; then `goBack(11)` and `goForward(11)` probes on the duplicate.
- **Raw state**: DUP = wcId 11: same URL (page5), same jar (work), `active:true`, at DOM position 1 **directly beside the source** (strip T3, DUP, N3 — `insertAt: sourceIndex+1`). `goBack(11)` → page3 (the SOURCE's prior history entry); `goForward(11)` → page5. Source T3 unchanged throughout.
- **Expected**: Same-jar same-URL duplicate inserted beside the source, activated, WITH real navigation history (both directions probed).
- **Verdict**: pass — the `tab-history-snapshot` → `createTab(restoreHistory, insertAt)` chain carried real history, not just the current URL.
- **Evidence**: `step7-after-enumtabs-poll-raw.txt`, `step7-after-domorder-raw.txt`, `step7-afterback-enumtabs-poll-raw.txt`, `step7-afterfwd-enumtabs-poll-raw.txt` (+9 more)

### Checkpoint 8 — PASS
- **Actions taken**: Closed mid-strip DUP (`closeTab(11)`; strip T3, DUP, N3 → T3, N3; stack top = DUP); right-click T3; menu showed 5 items incl. "Reopen closed tab"; coordinate-clicked it; enumerate polls + DOM order reads.
- **Raw state**: New tab wcId 12 at page5/work, active; DOM order **tab-4, tab-10, tab-8** = T3, reopened, N3 — the reopened tab at its original mid-strip slot BETWEEN its original neighbors, not appended (append would read [tab-4, tab-8, new]).
- **Expected**: Menu reopen restores the most recent close at its original position among its neighbors (the F4 spec-polish rider — F4's own run could only exercise last-position reopens).
- **Verdict**: pass — the positional-reopen discriminator F4 couldn't make is now made; same-URL ambiguity (three tabs saw page5 this run) resolved via DOM ids + wcIds, never titles.
- **Evidence**: `step8-after-enumtabs-poll-raw.txt`, `step8-after-domorder-raw.txt`, `step8-menu-poll-raw.txt`, `step8-afterclose-enumtabs-poll-raw.txt` (+6 more)

### Checkpoint 9 — PASS
- **Actions taken**: Reduced to one tab via close-others (proven body); right-clicked the SOLE tab; 4 menu polls reading items + direct-children role sequence; `readAxTree(3)`; Escape.
- **Raw state**: Sole-tab menu items EXACTLY [Close, Duplicate, Reopen closed tab] — close-others and close-right **omitted** (single-tab rules); Reopen **present** (stack non-empty from this step's own sweep) — the positive control proving Step 3's absence was the empty-stack rule, not blanket omission. Role sequence menuitem/separator/menuitem/separator/menuitem — separators only BETWEEN populated sections, none leading or trailing (corroborated in the raw AX tree; no disabled nodes anywhere — omitted-never-disabled confirmed structurally).
- **Expected**: Single-tab omission rules; separator placement; omitted-not-disabled.
- **Verdict**: pass.
- **Evidence**: `step9-menu2-poll-raw.txt`, `step9-sheet-axtree-raw.txt`, `step9-aftersweep-enumtabs-poll-raw.txt` (+9 more)

### Checkpoint 10 — PASS
- **Actions taken**: Opened N4 (wcId 13, active). KEY_MAP gap probes: `pressKey(C,'ContextMenu')` and `pressKey(C,'F10',['Shift'])` — both live-rejected ("unknown key…"), captured as the documented substitution evidence. Right-click BACKGROUND tab (wcId 12/tab-10); menu open; `pressKey(3,'Escape')`; hidden polls; chrome `activeElement` probe; enumerate re-read.
- **Raw state**: After Escape: sheet hidden; `document.activeElement.closest('.tab').dataset.id = 'tab-10'` — DOM focus returned to the INVOKING tab specifically (not the address bar, not body); N4 (wcId 13) still `active:true`, wcId 12 still background — focus moved, activation did not.
- **Expected**: Escape returns focus to the invoking tab (captured `returnFocus`); no activation side-effect. Literal ContextMenu/F10 keypress is Out of Scope (KEY_MAP apparatus gap) — pointer trigger substitutes per the documented rationale; literal key press is HAT-scoped.
- **Verdict**: pass — refocus resolved to the invoking tab; the apparatus deviation behaved exactly as documented.
- **Evidence**: `step10-refocus-raw.txt`, `step10-after-enumtabs-raw.txt`, `step10-keymap-gap-probe-raw.txt`, `step10-afterescape-hidden-raw.txt` (+6 more)

## Orchestrator Notes

- **Mode**: live continuation (`SendMessage` available) — one Executor and one Validator held context across all 10 checkpoints; steps dispatched in batches (1 / 2–4 / 5–7 / 8–10) with verdicts rendered per step.
- **Scratch-profile convention applied** (first use, per the F4 debrief carry): app launched with `XDG_CONFIG_HOME` pointed at an empty per-run directory — the fresh jar seed and provably-empty closed-tab stack were preconditions this spec load-bears on, and they held without any mid-run ruling (contrast: F4's non-fresh-profile ruling).
- Port followed the spec's pin-if-free rule (49707 held outside the WSL netns; free-fallback → 49709). Fixture server: `fixtures-tabstrip` on :8000.
- Sheet item activation used coordinate clicks on the sheet webContents (rect-derived), per the established apparatus nuance that Enter doesn't click sheet menu items under synthetic key events.
- The Validator independently recomputed every click coordinate against the recorded rects across all ten steps (including all five sheet-item activation clicks) — all correct.

## Closing Summaries

### Executor closing

Clean run, no anomalies or environment hiccups; all evidence written under the run dir only. Strip reflow between steps was handled by re-reading rects after every mutation (tab widths changed 223 → 240 after Step 4). The KEY_MAP gap probes captured the full known-key list in the error text — useful for the BACKLOG KEY_MAP-extension item.

### Validator closing

10/10 PASS; every verdict rests on evidence files read directly; no step needed independent MCP observation. Spec-quality observations carried to the flight debrief: (1) the exact-cardinality sweep assertions are what make the mid-sweep fallback-cascade failure mode structurally detectable — a pattern to reuse; (2) positional assertions must key on `dataset.id`/wcId, never title/URL, when duplicates are in play — worth an explicit note in future specs; (3) Step 9's sole-tab right-click is on the ACTIVE tab, fine for the omission check but a one-line "(activation semantics not under test here)" would prevent conflation with Step 3; (4) Step 10's "reason: escape" phrase has no readable observable — the causal Escape→hidden sequence plus the refocus observable carried it; either drop the phrase or expose a dismissal-reason attribute; (5) the ContextMenu/F10 rejections add a second motivating datapoint (after PgDn/PgUp) for the KEY_MAP extension follow-up — once landed, a keyboard-trigger spec variant (incl. the double-fire dedupe assertion) is ready to activate.

## Operator Notes

_(none — autonomous run; mid-drag/menu interactions and the literal ContextMenu/F10 key remain HAT-walkthrough items)_
