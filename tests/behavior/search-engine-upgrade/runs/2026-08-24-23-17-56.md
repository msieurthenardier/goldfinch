# Behavior Test Run: search-engine-upgrade — 2026-08-24-23-17-56

**Spec**: [search-engine-upgrade.md](../search-engine-upgrade.md)
**Status**: partial
**Started**: 2026-08-24T23:17:56Z
**Completed**: 2026-08-24T23:29:06Z
**Duration**: 00:11:10
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/search-engine-upgrade/2026-08-24-23-17-56/` during the run; re-derivable by re-running the spec.

## Summary

4 / 5 checkpoints passed. 1 failed (checkpoint 2 — the first Ctrl+T after launch opened Google instead of the configured home page); 0 inconclusive. Every search-engine claim passed: the upgraded profile shows the fixture home page untouched with Google explicitly selected, searches still reach Google with no user choice, the stored row is migrated to `version 3` with `searchEngine` and `homePage` pinned explicit, and the default is a real, changeable selection (DuckDuckGo → `duckduckgo.com`). The checkpoint-2 failure is the pre-existing, deferred **squawk 0005** (`homePageCache` never boot-seeded) — reproduced identically on the pre-flight build in an Orchestrator differential (see Orchestrator Notes); not an upgrade regression.

## Fixture Provenance (Orchestrator)

Produced fresh for this run per the spec's Preconditions, immediately after the sibling `search-engine-preference` run:
1. `git worktree add <scratch>/fixture-worktree c8563f3` (the pre-Flight-1 commit; `node_modules` symlinked from the main checkout). Confirmed `src/main/settings-store.js` at that commit contains no `searchEngine`.
2. Launched the pre-flight build from the worktree with `XDG_CONFIG_HOME` pointed at an empty scratch directory (`GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_AUTOMATION_ADMIN=1 npm run dev:automation`). Its Settings page rendered no search-engine control (screenshot inspected).
3. Drove its Settings UI over the MCP surface: opened Settings via the `kebabActionSettings` chrome seam, clicked the Home page input, Ctrl+A, typed `https://fixture.example.net`, clicked Save.
4. Verified the row with the app still running, then quit cleanly (SIGTERM to the main process after confirming it held the scratch `app.db`; exit within 1s) and re-verified: `version: 2`, `homePage: "https://fixture.example.net"`, **no `searchEngine` key** (grep count 0).
5. Launched the Flight-1 build (current working tree) against the same `XDG_CONFIG_HOME`; new admin key minted; MCP bound on 49707. Session restore reopened the fixture session's Google tab. An Orchestrator pre-read of the row after boot already showed `version: 3`, `searchEngine: "google"`, `homePage` unchanged — recorded here as context only; step 4's verdict rests on the Executor's own read.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Opened `goldfinch://settings` via the chrome `kebabActionSettings` seam (new tab wcId 3); captured whole-window + guest screenshots and the a11y tree.
- **Raw state**: Home page field renders `https://fixture.example.net` (a11y tree also exposed `value='https://fixture.example.net'` this run). Directly beneath, a "Search engine" fieldset with 8 radios (DuckDuckGo, Brave Search, Startpage, Mojeek, Qwant, Ecosia, Google, Bing), each with a one-line description; Google rendered filled, all others empty; a11y: Google checked `true`, seven others `false`. Restore-session/bookmarks-bar toggles undisturbed.
- **Expected**: The home page field shows `https://fixture.example.net` — untouched by the upgrade. A search engine control is visible with Google selected explicitly. [a11y]
- **Verdict**: pass — rendered screenshot shows the fixture home page unchanged and Google as the only filled radio; the Validator independently parsed the a11y snapshot (textbox `value` present; only the Google radio `checked=true`). Rendered and internal state agree.
- **Evidence**: `step-1-screenshot.png`, `step-1-screenshot-2.png`, `step-1-snapshot.txt`, `step-1-tabs.json`
- **Validator notes**: no accessibility concerns — proper legend, accessible names match labels, descriptions exposed as associated static text. The two sub-claims were checked independently; consider splitting into separate rows for failure isolation.

### Checkpoint 2 — FAIL (known issue: squawk 0005 — see Orchestrator Notes)
- **Actions taken**: Ctrl+T on the chrome (`pressKey`); `enumerateTabs` twice 2s apart; chrome `evaluate` of `#address.value`; `captureWindow`.
- **Raw state**: New tab wcId 4 opened active on `https://www.google.com/` (title "Google"), stable across both enumerations; address bar reads the same; the screenshot shows the live Google homepage — not `https://fixture.example.net`.
- **Expected**: The tab opens on `https://fixture.example.net` — new-tab behavior unchanged.
- **Verdict**: fail — the new tab opened and rendered the live Google homepage at `https://www.google.com/`, not the configured `https://fixture.example.net`; rendered pixels and `enumerateTabs` agree and were stable across two reads, ruling out an in-flight navigation.
- **Evidence**: `step-2-screenshot.png`, `step-2-tabs.json`
- **Validator notes**: a real product-level finding, not an apparatus issue; flagged as a "likely regression distinct from the search-engine migration" — the Orchestrator’s differential check and the squawk record resolve that read (see Orchestrator Notes).
- **Operator decision**: continued (Orchestrator, autonomous — the remaining rows are the search-engine claims and do not depend on this row)

### Checkpoint 3 — PASS
- **Actions taken**: Focused the chrome address bar (`click` on `#address`), Ctrl+A, `typeText` `zephyr quartz upgrade`, Enter; captured tabs, whole-window screenshot, chrome a11y tree, `#address.value`.
- **Raw state**: Tab wcId 4 committed to `https://www.google.com/search?q=zephyr%20quartz%20upgrade&sei=…` ("zephyr quartz upgrade - Google Search"); address bar shows the same; live Google results rendered. Other tabs unchanged.
- **Expected**: The address bar shows a `google.com` search URL containing the encoded query — searches still reach Google without the user having chosen anything.
- **Verdict**: pass — screenshot and tab record agree on the `www.google.com/search?q=zephyr%20quartz%20upgrade` URL with a fully rendered Google SERP; the default reached Google with no user choice.
- **Evidence**: `step-3-screenshot.png`, `step-3-snapshot.txt`, `step-3-tabs.json`
- **Validator notes**: none.

### Checkpoint 4 — PASS
- **Actions taken**: Read-only `sqlite3` query of the scratch profile’s `documents` row (`store='settings'`) at 23:25:35Z, saved verbatim; no UI interaction. Ordering attested: step 5 not yet dispatched.
- **Raw state**: Top-level keys: version, homePage, toolbarPins, automationEnabled, automationKeyHashes, automationAdminKeyHash, automationPort, spellcheck, restoreSession, vaultAutoLockMinutes, bookmarksBarEnabled, searchEngine. `version = 3`; `homePage = "https://fixture.example.net"` (present, unchanged); `searchEngine = "google"` (present, explicit).
- **Expected**: `[mixed-frame]` The row carries `version: 3` with both `searchEngine` and `homePage` present as explicit values (`"google"`, `"https://fixture.example.net"`).
- **Verdict**: pass — filesystem observable carries the mixed-frame verdict: `version 3`, `homePage` unchanged, `searchEngine: "google"` newly present and explicit; the Validator’s independent `sqlite3` read was byte-identical; ordering (pre-user-choice) attested.
- **Evidence**: `step-4-settings-row.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: Activated the Settings tab; clicked the DuckDuckGo radio (guest ≈(884,258)); captured guest screenshot + a11y tree; activated the web tab; focused the chrome address bar, Ctrl+A, `typeText` `zephyr quartz changed`, Enter; captured tabs, whole-window screenshot, chrome a11y tree, `#address.value`. *Supplementary (Orchestrator-requested, not part of the row)*: one more Ctrl+T, then `enumerateTabs`.
- **Raw state**: DuckDuckGo radio filled, Google empty; a11y: DuckDuckGo checked `true`, seven others `false`. Search committed to `https://duckduckgo.com/?q=zephyr+quartz+changed&ia=web` ("zephyr quartz changed at DuckDuckGo"), rendered in the address bar with a live DuckDuckGo SERP. *Supplementary*: after the settings change, Ctrl+T opened a new tab on `https://fixture.example.net` — the configured home page — whereas step 2’s Ctrl+T (before any settings broadcast) had opened Google.
- **Expected**: The address bar shows a `duckduckgo.com` search URL — the formerly implicit default is a real, changeable selection on this profile.
- **Verdict**: pass — DuckDuckGo radio filled with Google empty; the address bar committed to the `duckduckgo.com/?q=zephyr+quartz+changed` URL with a live DuckDuckGo SERP; tab record agrees.
- **Evidence**: `step-5-screenshot-2.png`, `step-5-snapshot.txt`, `step-5-screenshot.png`, `step-5-snapshot-2.txt`, `step-5-tabs.json`, `step-5-tabs-supplementary.json`
- **Validator notes**: on the supplementary Ctrl+T (not weighed): its landing on `https://fixture.example.net` is inconsistent with a blanket "home page ignored on every new tab" reading of step 2 — step 2’s finding is confirmed for the tab it captured but looks state-dependent (first new tab after launch vs. after a settings interaction); recommended disambiguation before reporting it as a regression.

## Orchestrator Notes

- **Execution mode**: live two-agent mode (SendMessage continuation available); cache mode `cold` — the fixture profile was produced and the Flight-1 instance launched immediately before the run. Crew: Executor and Validator both Sonnet per the project crew file; Accessibility Validator not spawned (project has it disabled); the base Validator judged the `[a11y]` row functionally.
- **Apparatus**: same attach-only CLI over `scripts/lib/mcp-client.mjs` as the sibling `search-engine-preference` run, with this instance's freshly minted admin key (port 49707). Session-registered goldfinch MCP tools forbidden to both agents (they point at a different live browser). Settings reached via the chrome `kebabActionSettings` seam (`openTab`/`navigate` refuse `goldfinch://` by design).
- **Checkpoint 2 disposition — known issue, not a regression.** The observable failed as written (first Ctrl+T after launch → `https://www.google.com/`). Three independent lines resolve it to squawk 0005 (`squawks/0005-home-page-cache-never-boot-seeded.md`, status `deferred` to M16 Flight 2, out of this flight's scope per the flight spec and leg 2's artifact):
  1. The squawk's own report describes exactly this: new tabs in a window open the hardcoded Google "until some unrelated `settings-changed` broadcast happens to land in that window".
  2. The run's supplementary observation at step 5: after the DuckDuckGo selection (a `settings-changed` broadcast), Ctrl+T in the same window opened `https://fixture.example.net` — the cache had been seeded by the broadcast, as the squawk predicts.
  3. **Orchestrator differential on the pre-flight build (c8563f3, the fixture worktree), after the run**: on a fresh profile, set the home page to `https://fixture.example.net` via Settings, quit, relaunch, Ctrl+T in the boot window → the new tab opened on `https://www.google.com/` (session restore had reopened the earlier `fixture.example.net` tab alongside the Google tab; the fresh Ctrl+T tab was Google) — the identical first-tab miss on the pre-flight build. Control in the same session before quitting (after the Save broadcast): Ctrl+T → `https://fixture.example.net`.
  Disposition: the upgrade is neutral for this row — the pre-flight build has the identical first-tab miss — so the row measured a pre-existing defect, not a change the upgrade introduced. Recorded against the leg as a known issue with this run-log path; M16 Flight 2 owns the fix (the squawk's deferral note says not to fix standalone while that flight is pending). The Validator's step-2 note ("likely regression") and its later step-5 note (state-dependent, disambiguate before reporting as a regression) are both preserved above; the differential is the disambiguation it asked for.
- **Spec premise error to fix at re-authoring**: step 2's Expected Result assumes the first new tab after launch opens on the configured home page. That premise was false when the spec was drafted (2026-08-11) — the same false CLAUDE.md claim that leg 1 corrected the same day — and squawk 0005 was already on file. Per the M06 debrief ruling on spec premise-audits, the row should be re-authored to pin which new-tab open is under test (steady-state after a broadcast, or first-after-launch once squawk 0005 is fixed) rather than asserting the currently-defective path. Left as-is for this run so the record shows the real behavior.
- **Ordering discipline for the `[mixed-frame]` row**: step 4's filesystem read was timestamped (23:25:35Z) and attested to precede step 5's mutation; the Validator independently re-read the row and matched byte-for-byte. Good template.
- **Apparatus corrections**: this run's a11y tree DID expose a `value` on the Settings home-page textbox (both Executors of the sibling run saw none) — treat it as build/state-dependent and keep judging from rendered pixels. Address-bar rect and radio coordinates were stable across builds at this window size.

## Closing Summaries

### Executor closing
Apparatus held up throughout — the chrome seam, `#address` evaluate/click, and settings-tab drive-by-coordinates all worked as documented; no retries or sandbox toggles needed. Coordinates matched the sibling run's, so they appear stable across builds/launches at this window size. Correction to the apparatus notes: this run's a11y tree did expose a `value` on the Home page textbox (agreed with the screenshot in steps 1 and 5). The one substantive anomaly is checkpoint 2: Ctrl+T on the fresh window (only prior activity: opening Settings) opened `https://www.google.com/` instead of the configured home page, whereas the supplementary Ctrl+T after the DuckDuckGo selection opened `https://fixture.example.net` — the only state change between them was the search-engine radio write; flagged as a lead, not a judgment. All other steps landed clean (untouched home page + explicit Google radio with all 8 engines and descriptions; `google.com/search?q=…` before any engine change; `version: 3` row with both keys explicit, read strictly before step 5; immediate radio update and `duckduckgo.com/?q=…` after). No dialogs, stuck sheets, or crashes; WAL-mode `app.db` read cleanly via the sqlite3 CLI. 17 evidence files saved.

### Validator closing
4/5 pass, 1 fail, no inconclusives; every browser-frame step had corroborating screenshot + a11y/URL evidence and the mixed-frame step had a clean independent filesystem read. Step 2's failure is consistent with squawk 0005's description, and the step-5 supplementary Ctrl+T landing correctly after the settings write lines up with its "until a settings-changed broadcast lands" mechanism — the DuckDuckGo click likely seeded `homePageCache` lazily, making the next tab correct by accident rather than by design; strongly suggests a pre-existing defect surfacing, not a migration regression, pending the Orchestrator's differential. Spec-quality: (1) step 2 should pin which new-tab open is under test (first-after-launch vs steady-state) so a verdict isn't ambiguous about which code path fired; (2) step 1 bundles two independent claims — split for failure isolation; (3) the `[mixed-frame]` ordering pattern with attestation is a good template; (4) the home-page textbox a11y `value` is build-dependent — keep the caveat. No accessibility concerns beyond step 1's notes (well-labelled group, per-option descriptions exposed).

## Operator Notes

_(none yet)_
