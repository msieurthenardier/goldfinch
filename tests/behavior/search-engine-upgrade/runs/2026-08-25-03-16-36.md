# Behavior Test Run: search-engine-upgrade — 2026-08-25-03-16-36

**Spec**: [search-engine-upgrade.md](../search-engine-upgrade.md)
**Status**: pass
**Started**: 2026-08-25T03:16:36Z
**Completed**: 2026-08-25T03:25:10Z
**Duration**: 00:08:34
**Executor**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Validator**: general-purpose sub-agent (Sonnet), live across all steps via SendMessage
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/search-engine-upgrade/2026-08-25-03-16-36/` during the run; re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. Re-run at the Mission 16 Flight 2 leg-1 gate: the previous run (2026-08-24-23-17-56) failed checkpoint 2 on pre-existing squawk 0005; with `homePageCache` boot-seeded the row passes as authored — the upgraded profile's boot window opens its first new tab on the fixture home page. Squawk 0006 is closed by this run without re-authoring. Every other claim held again: home page untouched, Google explicit, searches reach Google with no choice, row migrated and pinned (`version 3`, both keys explicit), DuckDuckGo a real changeable selection.

## Fixture Provenance (Orchestrator)

Re-run at the Mission 16 Flight 2 leg-1 acceptance gate, on the branch `flight/02-welcome-surface` working tree (leg 1 implemented, uncommitted — `homePageCache` boot-seeded, squawk 0005). Fixture produced fresh per the spec: worktree at `c8563f3` (pre-Flight-1; `node_modules` symlinked), launched on an empty `XDG_CONFIG_HOME`, home page set to `https://fixture.example.net` through its Settings UI over the MCP surface (`kebabActionSettings` seam + guest clicks), verified with the app running and again after a clean quit (SIGTERM to the PID holding the scratch `app.db`): `version: 2`, `homePage: "https://fixture.example.net"`, no `searchEngine` key. The Flight 2 build was then launched on the same profile; an Orchestrator pre-read after boot showed `version: 3`, `searchEngine: "google"`, `homePage` unchanged (context only — step 4's verdict rests on the Executor's read). Session restore reopened the fixture session's Google tab.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Opened Settings via the `kebabActionSettings` seam (wcId 3); captured whole-window + guest screenshots and the a11y tree.
- **Raw state**: Home page textbox `https://fixture.example.net`; Save and (new) Clear beside it; directly below, the Search engine group with 8 radios and descriptions, Google the only one checked.
- **Expected**: The home page field shows `https://fixture.example.net` — untouched by the upgrade. A search engine control is visible with Google selected explicitly. [a11y]
- **Verdict**: pass — both screenshots render the fixture home page in the field and Google as the only filled radio; the a11y tree agrees (labelled fieldset/legend, exactly one checked).
- **Evidence**: `step-1-screenshot.png`, `step-1-screenshot-2.png`, `step-1-snapshot.txt`, `step-1-tabs.json`
- **Validator notes**: no rendered-vs-internal divergence; solid accessible structure.

### Checkpoint 2 — PASS (was FAIL on 2026-08-24 — squawk 0005; fixed by Flight 2 leg 1)
- **Actions taken**: Ctrl+T on the chrome; `enumerateTabs`; chrome `evaluate` (addr/tabs/welcomeVisible); `captureWindow`; chrome `readAxTree`.
- **Raw state**: New tab wcId 4 opened on `https://fixture.example.net` (title "New tab" — the domain does not resolve); address bar `https://fixture.example.net`; 3 strip tabs; welcome panel hidden.
- **Expected**: The tab opens on `https://fixture.example.net` — new-tab behavior unchanged.
- **Verdict**: pass — the address bar renders `https://fixture.example.net` in the new active tab and `enumerateTabs` agrees; the blank body is the unresolvable fixture domain, irrelevant to the row.
- **Evidence**: `step-2-tabs.json`, `step-2-screenshot.png`, `step-2-snapshot.txt`
- **Validator notes**: none.

### Checkpoint 3 — PASS
- **Actions taken**: Focused the chrome address bar, Ctrl+A, `typeText` `zephyr quartz upgrade`, Enter; `enumerateTabs`; `#address.value`; `captureWindow`; chrome `readAxTree`.
- **Raw state**: Goldfinch committed `https://www.google.com/search?q=zephyr%20quartz%20upgrade&sei=…` (preserved verbatim in the tab title and in the interstitial’s `continue=` parameter); Google’s server then redirected to its `/sorry/index` anti-automation interstitial, which the address bar shows. Host `google.com` and the encoded query are present in both.
- **Expected**: The address bar shows a `google.com` search URL containing the encoded query — searches still reach Google without the user having chosen anything.
- **Verdict**: pass — the rendered address bar carried the verdict: host `google.com` with the encoded query present; the `/sorry/` interstitial is Google’s server-side bot response, not Goldfinch behavior; the pre-redirect `/search?q=…` URL is preserved in the tab title and `continue=`.
- **Evidence**: `step-3-tabs.json`, `step-3-screenshot.png`, `step-3-snapshot.txt`
- **Validator notes**: Google’s anti-automation interstitial is an environment artifact after repeated scripted searches from this IP; the row would genuinely fail only if the host or the encoded query were absent.

### Checkpoint 4 — PASS
- **Actions taken**: Read-only `sqlite3` query at 03:22:11Z, saved verbatim; no UI interaction; step 5 not yet dispatched (attested).
- **Raw state**: `version: 3`; `homePage: "https://fixture.example.net"` (present); `searchEngine: "google"` (present, explicit); twelve top-level keys.
- **Expected**: `[mixed-frame]` The row carries `version: 3` with both `searchEngine` and `homePage` present as explicit values (`"google"`, `"https://fixture.example.net"`).
- **Verdict**: pass — the Validator’s own `sqlite3` read matched byte-for-byte: `version 3`, `homePage` unchanged, `searchEngine: "google"` explicit; ordering attested.
- **Evidence**: `step-4-settings-row.json`
- **Validator notes**: none.

### Checkpoint 5 — PASS
- **Actions taken**: Activated Settings; located the DuckDuckGo radio from a fresh guest capture and clicked it; captured guest screenshot + a11y tree; activated the web tab; chrome address-bar search `zephyr quartz changed`; `enumerateTabs`, `#address.value`, `captureWindow`, chrome `readAxTree`.
- **Raw state**: DuckDuckGo radio filled, Google empty; a11y: DuckDuckGo the only `true`. Search committed to `https://duckduckgo.com/?q=zephyr+quartz+changed&ia=web` ("zephyr quartz changed at DuckDuckGo"); no interstitial this time.
- **Expected**: The address bar shows a `duckduckgo.com` search URL — the formerly implicit default is a real, changeable selection on this profile.
- **Verdict**: pass — the rendered radio state (DuckDuckGo filled) and the rendered address bar (`duckduckgo.com/?q=zephyr+quartz+changed`) with a DuckDuckGo results page agree; the default is a live, changeable selection.
- **Evidence**: `step-5-locate.png`, `step-5-screenshot-2.png`, `step-5-snapshot.txt`, `step-5-tabs.json`, `step-5-screenshot.png`, `step-5-snapshot-2.txt`
- **Validator notes**: none — clean end-to-end confirmation.

## Orchestrator Notes

- **Execution mode**: live two-agent mode; cache mode `cold` (fresh fixture, fresh launch). Crew: Executor + Validator, Sonnet; no Accessibility Validator. The crew read the crew file's Project Apparatus Notes before `[READY]`.
- **Apparatus**: the attach-only CLI with the run's minted admin key (port 49707); Settings via the `kebabActionSettings` seam; guest-coordinate clicks re-derived per step (the Clear button is new beside Save — the DuckDuckGo radio happened not to move). Session-registered goldfinch MCP tools forbidden.
- **Checkpoint 2 regression closed**: the row that failed on 2026-08-24 (first Ctrl+T → Google) opened on `https://fixture.example.net`. Three independent proofs of the original diagnosis (squawk report, post-broadcast Ctrl+T, differential on `c8563f3`) plus this pass on the fixed build close **squawks 0005 and 0006** by observation; the flight Reviewer's sign-off rides the flight-end commit.
- **Checkpoint 3 environment artifact**: Google's `/sorry/index` anti-automation interstitial intercepted the request (repeated scripted searches from this IP today). The URL Goldfinch committed (`https://www.google.com/search?q=zephyr%20quartz%20upgrade&sei=…`) survives in the tab title and the interstitial's `continue=` parameter; the Validator passed the row on the rendered bar (host `google.com` + encoded query present) and named the observable that carried the verdict. **Spec wording note**: the row says "a `google.com` search URL"; a same-host redirect chain is a legitimate variant — loosen to "a `google.com` URL containing the encoded query" or note anti-bot interstitials as acceptable (Validator recommendation; squawk-sized re-author).
- **Checkpoint 4 ordering** attested (step 5 not dispatched); the Validator's own `sqlite3` read matched byte-for-byte.
- **Fixture by procedure** from the `c8563f3` worktree worked first time again (~3 min).

## Closing Summaries

### Executor closing
All five steps clean; apparatus held (no transport drops, no relaunches). Anomalies: Google's automated-traffic challenge page on step 3 — host and encoded query present in `continue=` and the tab title, DuckDuckGo on step 5 served a plain results page; the Clear button is present beside Save (not a spec row). Apparatus: `activateTab` switched between the Settings and web tabs reliably; radio coordinates were re-derived per step (unchanged at (884,258)); the settings row already showed the migration before step 1, as the fixture note said, and did not drift until step 5's explicit change.

### Validator closing
5/5, including step 2 which failed last run on squawk 0005 — good regression coverage: the spec caught the squawk once and now confirms the fix. Spec-quality: step 3's "google.com search URL" wording is slightly narrow — a same-host anti-bot redirect chain still proves the property; recommend loosening the wording or naming the interstitial as an acceptable variant. The `[mixed-frame]` rule on step 4 was clean (own fresh read matched byte-for-byte). Step 1's `[a11y]` row was easy: native fieldset/legend/radio markup with descriptions; the home-page textbox exposed its `value` this run. No new apparatus quirks.

## Operator Notes

_(none yet)_
