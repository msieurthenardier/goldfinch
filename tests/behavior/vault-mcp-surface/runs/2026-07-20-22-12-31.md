# Behavior Test Run: vault-mcp-surface — 2026-07-20-22-12-31

**Spec**: [tests/behavior/vault-mcp-surface.md](../../vault-mcp-surface.md)
**Status**: partial
**Method**: **Flight-Director-direct live verification — NOT the two-agent Witnessed pattern.**
Driven by the orchestrator over the live `goldfinch-development` MCP surface (a **work**-jar
key) against the running dev build (`npm run dev:automation`, port 49707, profile
`~/.config/goldfinch-dev`, throwaway). No independent Validator agent — the orchestrator was
both executor and judge, so this is a live smoke verification of the load-bearing assertions,
not a canonical Witnessed pass. The full Witnessed run (independent Validator + the admin/second-jar
identity steps) remains for an operator-run `/behavior-test vault-mcp-surface`.

## Environment

- Running dev build: our branch `flight/01-vault-core-and-automation`, MCP on 49707, **work** jar
  transport identity (confirmed: `getChromeTarget` refused `admin-only`; `enumerateTabs` showed only
  work-jar tabs).
- Fixture: a `work`-jar vault provisioned into the running profile via a one-off (setup manager →
  seed a `work` login at `http://127.0.0.1:8099` with a TOTP secret → mint the work vault access key).
  The running build picked up the externally-written vault with **no restart** (the access-key unlock
  path reads the `.gfvault` fresh per call).
- Login page served at `http://127.0.0.1:8099/` (the committed fixture page).

## Step Results

| # | Step | Result |
|---|------|--------|
| 1 | `vaultUnlock` (work per-jar access secret) | **PASS** — `{"unlocked":["work"]}`; unlocked only the work vault; the access key never echoed. |
| 2 | `vaultList` | **PASS** — one item, **metadata only** (`vaultId/id/title/origin/username/hasTotp:true`); no password, no TOTP secret. |
| 3 | `openTab` in work jar → fixture page | **PASS** — wcId 5, dom-ready at the fixture origin. |
| 4 | `vaultFill(wcId, itemId)` | **PASS (load-bearing)** — `{"filled":true,"id":"996af94f…"}`; **no password in the result** — the credential never crossed the MCP wire. |
| 5 | Read live DOM (`evaluate`) | **PASS (load-bearing)** — username = `work-user@example.com`; password field populated (len 22, `passwordMatchesExpected:true`); `stillOnPage:true` (form **not** auto-submitted). The real top-frame form was filled with the correct credential. |
| 6 | `vaultTotp(itemId)` | **PASS** — `985630`; **exact match** to an independent RFC 6238 computation (secret `JBSWY3DPEHPK3PXP`, window 59486182); only the code returned, no secret. |
| 7 | Second-jar sibling isolation | **NOT RUN** — single work-jar MCP connection; no second-jar transport key available. Structurally covered by step 8 + unit tests. |
| 8 | Filesystem absent-envelope scope | **PASS (load-bearing)** — `global.gfvault` envelope keyIds = `["mrk"]`; the work access keyId `AA1JnM-L7_c` is **absent** from `global.gfvault`; `work.gfvault` = `["mrk","AA1JnM-L7_c"]`; `manager.json` MRK wrapped under `["master","recovery","admin"]`. Scope is *absent envelope*, not a runtime refusal. |
| 9 | Admin audit read via `getChromeTarget` | **NOT RUN** — jar key; `getChromeTarget` is admin-only (correctly refused). Needs an admin transport identity. |
| 10 | Session-teardown re-lock | **PARTIAL/incidental** — an MCP disconnect between steps 2 and 3 tore down the session; the vault was locked again and required a fresh `vaultUnlock` (the prior unlock did not survive teardown). Observed, not a controlled assertion. |

## Summary

**6 of 10 steps PASS live (1, 2, 3, 4, 5, 6, 8 — counting 3 as setup), including BOTH load-bearing
assertions:** the live top-frame form fill with the correct credential and **no password on the wire**
(steps 4–5), and the file-level **absent-envelope** per-jar scope property (step 8), plus a
reference-verified TOTP (6) and metadata-only listing (2). Steps 7 (full sibling isolation) and 9
(admin audit read) were not run — they need a second-jar and an admin transport identity that this
single work-jar MCP connection cannot present; both are covered by the unit suites and, for scope,
by step 8's structural evidence. Step 10 was observed incidentally.

**Disposition:** the properties that ONLY a live run can establish — real-page fill, no wire leak,
correct live TOTP, on-disk structural scope — are **verified against the running app**. The canonical
Witnessed run (independent Validator, all 10 steps incl. admin/sibling identities) remains an
operator-run follow-up. Evidence was live tool output (recorded above); no ephemeral evidence dir kept.
