# Behavior Test Run: bookmarks-bar — 2026-07-29-18-23-40

**Spec**: [bookmarks-bar.md](../../bookmarks-bar.md)
**Status**: pass
**Started**: 2026-07-29T18:23:40Z
**Completed**: 2026-07-29T~23:15Z
**Duration**: ~04:50 (wall clock, incl. operator-assisted phases, one inline code fix + relaunch, and two Executor re-spawns)
**Executor**: 3 sequential sub-agent instances (Sonnet) — re-spawned twice on context expiry; state handed forward by the Orchestrator
**Validator**: 1 sub-agent (Sonnet), continuous across all 12 checkpoints

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/bookmarks-bar/2026-07-29-18-23-40/` during the run; re-derivable by re-running the spec.

## Summary

12 / 12 checkpoints passed. 1 checkpoint (8) FAILED on first attempt, was fixed inline (commit `5d2d098`), and passed on re-run. 0 inconclusive.

## Checkpoint Results

### Checkpoint 1 — PASS [a11y]
Settings shows a single combined "Startup & appearance" section: nav has exactly 4 links 1:1 with 4 `h2` sections, no separate "On startup"/"Appearance" entries anywhere in the DOM or ax tree; heading hierarchy clean (h1 > h2 > h3 "Pinned Icons"); bookmarks-bar toggle present, OFF, accessible name from visible text via `labelledby` plus sr-only group legend. Evidence: `step-1-snapshot.txt`, `step-1-screenshot.png`.

### Checkpoint 2 — PASS
Enabling the toggle put the bar under the toolbar in **both** windows from a single `settings-changed` broadcast, with identical 8-item content; page/guest area shrank cleanly (no clipping, overlap, or residual gap) against the pre-toggle window-B baseline. The no-animation clause was carried by live operator observation (stills cannot prove absence of a transition); Validator reasoned this explicitly. Evidence: `step-2-screenshot-0/1/2.png`, `step-2-snapshot.txt`.

### Checkpoint 3 — PASS
Ctrl+Shift+B from a browsing tab hid the bar (content reflowed up) and re-showed it (content back to original position); the Settings checkbox reflected the final state. **Method finding**: `readDom` HTML does NOT reflect live checkbox state (Chromium never rewrites the content attribute) — `readAxTree` is authoritative; adopted for the remainder of the run. Evidence: `step-3-screenshot-0/1/2.png`, `step-3-settings.png`, `step-3-snapshot.txt`.

### Checkpoint 4 — PASS
8 items in stored order, each with name + monogram icon (uniform monograms are correct for this fixture: seeds were storeside-added and unvisited; example.com serves no favicon) and a correctly-formed `title` attribute `"<Name>\n<URL>"`. Native tooltips proved un-capturable (no hover primitive in the MCP surface; synthetic DOM events can't reach the OS tooltip layer — honestly reported, not fabricated); the tooltip clause was carried by operator eyewitness. Evidence: `step-4-snapshot.txt`, `step-4-screenshot-1/2.png`.

### Checkpoint 5 — PASS
Left-clicking the MDN bar item navigated the **current tab** in place (same wcId, tab count unchanged 6→6) to `developer.mozilla.org/en-US/`; bar stayed visible. Evidence: `step-5-pre.txt`, `step-5-screenshot.png`, `step-5-snapshot.txt`, `step-5-post-tabs.txt`.

### Checkpoint 6 — PASS
Native middle-click on the Hacker News item opened a **background** tab (count 6→7, new tab `active:false`, focus and visible page stayed on MDN — confirmed both structurally and in pixels); activating it showed HN fully rendered. First live exercise of leg 1's `createTab` `background` option. Evidence: `step-6-pre.txt`, `step-6-screenshot-0/1/2.png`, `step-6-post.txt`.

### Checkpoint 7 — PASS
Bookmarks were added one at a time with an overflow check after each, locating the threshold precisely at 12: 10 items in-row + visible chevron, 2 overflowed (`class='bm-item hidden'`, absent from row pixels). Evidence: `step-7-add-log.txt`, `step-7-snapshot.txt`, `step-7-screenshot.png`.

### Checkpoint 8 — **FAIL** (first attempt) → **PASS** (post-fix re-run)
- **First attempt**: menu opened and the keyboard contract worked (operator-verified arrows + Escape; `aria-expanded` toggled correctly), but the sheet rendered **clipped at the window's right edge** with row labels word-wrapped into unreadable fragments. Operator eyewitness ruled out capture artifact. Validator failed the "listing the remaining bookmarks with their names" clause at the rendered-state level despite the DOM containing the full names. Evidence: `step-8-screenshot-1/2.png`, `step-8-snapshot.txt`.
- **Operator decision**: continue with inline fix, then rerun the checkpoint.
- **Fix** (`5d2d098`): root cause was `openOverflowMenu` using the left-edge anchor idiom (`leftAnchorOf`) for a trigger at the bar's far right — shrink-to-fit collapsed the available width, causing BOTH the wrapping and the overflow past the viewport. Switched to the kebab's right-aligned idiom (`rightSheetAnchor`), factored as a reusable `rightAnchorOf`; regression pinned in `bookmarks-bar.test.js`.
- **Re-run**: menu renders fully in-viewport with both names on clean unwrapped lines; operator re-confirmed no clipping, arrows, and Escape; clean closure. Evidence: `step-8-rerun-screenshot-1/2.png`.

### Checkpoint 9 — PASS
Right-clicking an overflow row opened the edit popover for exactly that bookmark (operator-confirmed); rename to `Renamed In Overflow` propagated with no refresh — store read and the reopened menu row both show the new name; chevron toggles cleanly. An unprompted icon change (null → real favicon) was investigated and cleared: byte/pixel forensics identified TypeScript's **own** glyph, the edit path provably never carries an icon (code-traced), and jar history contains a genuine post-creation visit to the exact URL — i.e. DD6 exact-URL passive refresh behaving correctly; never-visited siblings stayed `null`. Evidence: `step-9-screenshot-1/2/3.png`, `step-9-snapshot.txt`, `step-9-suspect-icon.png`.

### Checkpoint 10 — PASS
Bar right-click → URL edit (github.com → gitlab.com) verified in store, and clicking that item navigated to GitLab (settling at `about.gitlab.com` via GitLab's own redirect). Bar right-click → Remove dropped the count 12 → 11 (both counts explicitly citable per the Validator's pre-run watch-item), the Wikipedia entry vanished from the store, and the item disappeared from the bar with no refresh. Secondary: the overflow partition re-flowed correctly, promoting one item into the freed slot. Evidence: `step-10-snapshot.txt`, `step-10-screenshot-0/1/2/3/4.png`.

### Checkpoint 11 — PASS
After a genuine operator quit + relaunch: 11 bookmarks in identical order, **including every edit made during the run** (the rename, the GitLab URL, the removal), with the exact same icon distribution (3 populated, 8 null); bar still enabled; overflow partition identical; window B's independently-rendered bar matched, corroborating app-scoped state. Evidence: `step-11-screenshot.png`, `step-11-windowB-screenshot.png`, `step-11-chrome-dom.json`, `step-11-snapshot.txt`.

### Checkpoint 12 — PASS [mixed-frame]
With the app fully down (0 processes, no listener), the bookmarks `documents` row was overwritten — 5088 bytes of valid JSON → 36 bytes of garbage — all other store rows untouched. On relaunch: app booted normally (operator: "booted fine, bar is empty"), `bookmarksGet()` returned a clean `[]` (repaired-to-empty, not garbage, not an error), bar enabled with zero items, and **corruption stayed contained** (settings incl. `bookmarksBarEnabled`, both jars, downloads, and session/tabs all intact). Starring a page then created exactly one bookmark with a live favicon, star filled, bar childCount 1 → 2; the popover covered the bar in the Executor's frame, and the operator's eyewitness closed that gap ("confirmed, one item on the bar"). Evidence: `step-12-screenshot-1/2.png`, `step-12-chrome-dom.json`, `step-12-snapshot.txt`.

## Orchestrator Notes

- **Mode**: live two-agent; Validator persisted throughout, Executor re-spawned twice on context expiry with state handed forward (noted per protocol). Cache mode: cold; fixtures seeded by the Orchestrator via the chrome bridge (setup path, never the behavior under test).
- **Apparatus**: agents attached via the sanctioned script seam (`connectAutomation()` + admin key from the canonical dev-mint launch) wrapped in a scratchpad CLI helper, because the session MCP binding was stale/jar-tier. Instance identity and tier were re-probed after **every** relaunch (three times).
- **Apparatus findings carried forward**: (1) the overlay sheet refuses all ops at every tier by design — sheet interaction is permanently operator-assisted; (2) `readAxTree`, not `readDom`, is authoritative for live checkbox state; (3) `window.goldfinch.bookmarksGet()` is a Promise and silently yields `{}` if not awaited — a real false-negative trap for future specs; (4) no hover primitive exists, so native tooltips are un-assertable by automation; (5) no window-create/resize tools — multi-window and narrowing steps are operator-assisted.
- **Environment**: a stale Orchestrator-launched instance survived one operator quit and held port 49707 (the new instance took 49708, and the Executor's fallback found it); the Orchestrator killed the stale process before the corruption step to guarantee no live writer. The final relaunch was clean on the default port.
- **Evidentiary posture**: three clauses that stills structurally cannot carry (no-animation ×2, native tooltip) were carried by live operator observation against a standing flag instruction, with the Validator reasoning that choice explicitly each time and flagging the one terser confirmation.
- **One bug found and fixed mid-run** (checkpoint 8). No other defects surfaced across 12 checkpoints.

## Closing Summaries

### Executor closing
(Received post-close; three sequential instances.) Highlights: honest reporting of the sheet-undrivability and hover gaps rather than synthetic substitutes; the async-`bookmarksGet` trap discovered and flagged; the icon-provenance probe that cleared a suspected data-corruption bug by byte/pixel comparison; correct halt-and-report behavior when a mid-run state drift was detected rather than proceeding on a mismatched precondition.

### Validator closing
(Received post-close.) 12/12 with the one genuine defect caught at the rendered-state level and re-verified after fix. Both pre-run watch-items paid off: the step-9 "no refresh sneaking in" check confirmed a clean causal chain, and the step-10 before/after count requirement was satisfied explicitly rather than inferred.

## Operator Notes

Operator (msieurthenardier) participated live throughout: performed every sheet interaction (rename, URL edit, Remove, dismissals), the keyboard walks, both app quit/relaunch cycles, and supplied the eyewitness observations for the animation, tooltip, clipping, and final bar-item clauses. Run conducted as part of Flight 1 leg 5 (HAT) of mission 15-bookmarks.
