# Behavior Test Run: jar-delete-closes-tabs — 2026-07-10-22-24-45

**Spec**: [jar-delete-closes-tabs.md](../../jar-delete-closes-tabs.md)
**Status**: pass
**Started**: 2026-07-10T22:24:45Z
**Completed**: 2026-07-10T22:52:00Z (approx)
**Duration**: ~00:27
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/jar-delete-closes-tabs/2026-07-10-22-24-45/`
(15 raw JSON-RPC capture files incl. session provenance) during the run;
re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. Second run of this spec
(first: 2026-07-10-16-39-03, also 5/5). Run gates M06 F4 Leg 4
(verify-integration) as page-relayout regression insurance: the F4 relayout of
`goldfinch://jars` did not disturb the tab-closure / registry / last-tab-fallback
contract this spec pins. Same behavioral contract as run 1, zero payload-shape
drift — stable, not flaky-green.

## Checkpoint Results

### Checkpoint 1 — PASS
- openTab ×2 (`work` → wcIds 3, 4), openTab (`personal` → wcId 5); enumeration =
  boot tab (wcId 2, personal) + three new; exactly two work, two personal.
  Baseline recorded: work={3,4}, personal={2,5}.
- **Evidence**: `step1-openTab-work-1.json`, `step1-openTab-work-2.json`,
  `step1-openTab-personal-1.json`, `step1-enumerateTabs.json` (+ step0 baseline)

### Checkpoint 2 — PASS
- Chrome-eval `jarsRemove({id:'work'})` → `{ok:true, removed:{...}, wiped:true}`;
  post-remove enumeration exactly {2, 5}, both personal — work wcIds gone by
  IDENTITY, personal wcIds intact unchanged.
- **Evidence**: `step2-jarsRemove-work.json`, `step2-enumerateTabs.json`,
  `step2-getChromeTarget.json`

### Checkpoint 3 — PASS
- `jarsList()` → [personal only]; `jarsGetDefault()` → personal container
  (the "retained" branch — personal was already default in the fresh seed).
- **Evidence**: `step3-jarsList.json`, `step3-jarsGetDefault.json`
- **Validator notes**: repaired-vs-retained remains indistinguishable through
  this apparatus (no isDefault field) — the parenthetical is explanatory, the
  observable clause is what's judged (carried from run 1).

### Checkpoint 4 — PASS
- `jarsRemove({id:'personal'})` (last persistent jar, tabs open) →
  `{ok:true, ..., wiped:true}`. Immediate AND settled (~2s) enumerations both
  show exactly one tab: NEW wcId 6, `jarId: burner-311666002`, active. Personal
  wcIds {2,5} absent in both captures; never tabless; fallback tab was already
  present in the immediate capture (mid-load), stable by the settled one.
- **Evidence**: `step4-jarsRemove-personal.json`,
  `step4-enumerateTabs-immediate.json`, `step4-enumerateTabs-settled.json`

### Checkpoint 5 — PASS
- `jarsList()` → `[]`; `jarsGetDefault()` → Burner identity (id exactly
  `burner`, no partition field). Registry empty, Burner default — the
  zero-orphan end state.
- **Evidence**: `step5-jarsList.json`, `step5-jarsGetDefault.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (SendMessage); both agents
  persisted across all 5 checkpoints. Sonnet both roles per crew file.
- Cache mode `cold` — second independent fresh stage of the leg (own scratch
  profile + launch, staged after run 1's instance was fully torn down;
  re-claimed fallback port 49709 — sixth consecutive observation). Torn down
  after the run (Electron main pid killed directly; port verified dead).
- Key discipline held under pressure: the Executor's first attempt to stash the
  admin key in a helper file was denied by the permission layer; it switched to
  per-invocation shell-variable derivation from the launch log, headers only.
  No key material in any evidence file or report.
- No operator interventions; no failing checkpoints; no re-spawns.

## Closing Summaries

### Executor closing

Clean run: all five steps first-attempt, one MCP session throughout, protocol
exactly as documented from prior runs. Notes: (1) the last-tab fallback is FAST —
the burner tab existed already in the immediate post-remove enumeration
(mid-load); the settle-recapture confirmed convergence rather than existence;
(2) default-flag introspection is indirect (no isDefault field — repair vs
retention unobservable through the wrappers); (3) Burner default object shape
asymmetry (no partition field) reconfirmed; (4) `wiped:true` on both removes
(out of scope, recorded); (5) the key-stash denial noted above. Identity ledger:
WORK={3,4} gone at step 2; PERSONAL={2,5} gone at step 4; sole survivor wcId 6
in `burner-311666002`.

### Validator closing

5/5 PASS, every verdict from directly read evidence; zero discrepancies between
reports and on-disk evidence across the run. The step-0 baseline capture (not
spec-demanded) made the boot-tab attribution verifiable — keep as run practice.
The immediate+settled dual capture on step 4 is a good template for
fallback-expectation steps (distinguishes "already correct" from "converged").
**Scope caveat for the relayout gate**: this spec's evidence is entirely
state-level (enumerations + registry reads); per the project's own
"DOM correct ≠ render correct" invariant, this pass clears the
closure/registry/fallback CONTRACT but observes no composited pixels — the
rendered-layout half of the relayout risk is HAT-owned (M06 F4 leg 5), exactly
per the flight's DD9 split. Comparison to run 1: same outcome, no payload-shape
drift. No spec changes required.

## Operator Notes

*(none — autonomous run under the flight's verify-integration leg)*
