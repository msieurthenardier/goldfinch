# Behavior Test Run: jar-delete-closes-tabs — 2026-07-10-16-39-03

**Spec**: [jar-delete-closes-tabs.md](../../jar-delete-closes-tabs.md)
**Status**: pass
**Started**: 2026-07-10T16:39:03Z
**Completed**: 2026-07-10T16:58:00Z (approx)
**Duration**: ~00:19
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/jar-delete-closes-tabs/2026-07-10-16-39-03/` during
the run; re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec
(authored at M06 Flight 3 design); spec flips `draft` → `active`. Run gates M06 F3
Leg 5 (verify-integration) acceptance for DD6 (tabs-close-on-delete).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: openTab ×2 (`jarId: work` → wcIds 3, 4), openTab (`jarId:
  personal` → wcId 5), enumerateTabs.
- **Raw state**: 4 tabs — boot tab wcId 2 (personal) + wcIds 3/4 (work) + wcId 5
  (personal).
- **Expected**: Boot tab plus three new: exactly two `work`, at least two
  `personal`; record wcIds per jar.
- **Verdict**: pass — nominal counts exactly; baseline recorded work={3,4},
  personal={2,5}.
- **Evidence**: `step-1-openTab-work-1.json`, `step-1-openTab-work-2.json`,
  `step-1-openTab-personal-1.json`, `step-1-enumerateTabs.json`

### Checkpoint 2 — PASS
- **Actions taken**: chrome-eval `jarsRemove({id:'work'})`; enumerateTabs.
- **Raw state**: remove `{ok:true, removed:{id:'work',...}, wiped:true}`;
  enumeration shows only wcIds 2 and 5, both `personal`.
- **Expected**: `{ok:true,...}`; both recorded work wcIds GONE; no tab reports
  `work`; every personal wcId present unchanged.
- **Verdict**: pass — gone-ness judged by wcId identity against the step-1
  baseline, not by count; wcId 5's title change was page-load completion only.
- **Evidence**: `step-2-remove.json`, `step-2-enumerateTabs.json`,
  `step-2-getChromeTarget.json`

### Checkpoint 3 — PASS
- **Actions taken**: chrome-eval `jarsList()`, `jarsGetDefault()`.
- **Raw state**: list = [personal only]; getDefault = personal container.
- **Expected**: Only the personal jar; default resolves personal.
- **Verdict**: pass — both halves directly evidenced.
- **Evidence**: `step-3-jarsList.json`, `step-3-jarsGetDefault.json`
- **Validator notes**: "repaired or retained" is unobservable as written (no
  isDefault field on returned objects) — satisfied via the getDefault
  indirection either way; spec-quality item below.

### Checkpoint 4 — PASS
- **Actions taken**: chrome-eval `jarsRemove({id:'personal'})` (LAST persistent
  jar, tabs open); enumerateTabs immediately + second capture ~1s later.
- **Raw state**: remove `{ok:true, wiped:true}`; BOTH captures show exactly one
  tab — NEW wcId 6, `jarId: burner-725002854`, active.
- **Expected**: `{ok:true,...}`; every personal wcId gone; window NOT tabless;
  every remaining tab matches `burner-<n>`.
- **Verdict**: pass — fallback tab is fresh (new wcId, not a rebadged survivor);
  the delayed second capture rules out a transient race artifact.
- **Evidence**: `step-4-remove.json`, `step-4-enumerateTabs.json`,
  `step-4-enumerateTabs-2.json`

### Checkpoint 5 — PASS
- **Actions taken**: chrome-eval `jarsList()`, `jarsGetDefault()`.
- **Raw state**: list = `[]`; getDefault = `{id:'burner', name:'Burner',
  color:'#ff8c42'}` (no partition field — identity marker, not a jar).
- **Expected**: Registry empty; Burner identity (id `burner`) is default.
- **Verdict**: pass — both clauses directly evidenced; coherent with checkpoint
  4's burner fallback tab.
- **Evidence**: `step-5-jarsList.json`, `step-5-jarsGetDefault.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (SendMessage available); both
  agents persisted across all 5 checkpoints. Sonnet for both roles per crew file.
- Cache mode: `cold` — satisfied structurally by a freshly launched instance on a
  fresh scratch profile (Orchestrator-staged per the Leg 5 per-run staging
  ruling: this instance existed only for this run and was torn down after).
- MCP surface on free-port fallback **49709** (configured 49707 unavailable —
  third consecutive observation of this rig behavior). Apparatus: curl-driven
  Streamable HTTP JSON-RPC with the launch-minted admin key (key value never
  written to any report, evidence file, or artifact).
- Teardown note: `pkill` on the launcher alone left the Electron main process
  alive and bound (npm/launcher don't cascade signals) — the Electron main pid
  needed a direct kill; port verified dead afterward. Same behavior the Leg 5
  boot smoke recorded.
- No operator interventions; no failing checkpoints; no re-spawns.
- Preconditions Orchestrator-verified (autonomous flight execution under the
  operator's standing directive; scratch profile, no operator data at risk).

## Closing Summaries

### Executor closing

Clean end-to-end: single MCP session survived the whole run including both jar
deletions; all JSON-RPC calls HTTP 200 first-attempt. Key observations: (1)
post-delete enumeration timing — the FIRST enumerate after the last-jar remove
already showed the completed end state (closure + fallback compose synchronously
with, or faster than, the remove Promise resolution; no tabless or half-closed
intermediate ever observed); (2) fallback tab opened at the home URL with a
randomized `burner-<n>` suffix; (3) object-shape asymmetry — persistent jars
carry `partition`, the Burner identity does not; future specs must not assert a
uniform shape; (4) no isDefault field exists — default is observable only via
which container `jarsGetDefault` resolves; (5) rig note: foreground sleep is
blocked in the executor harness — `read -t 1` substitutes fine for settle waits.
No admin key material in any evidence file.

### Validator closing

5/5 PASS; every verdict corroborated by evidence files matching raw_state
byte-for-byte; no fresh observations needed. Spec-quality carry-forwards: (1)
step 3's "repaired or retained" disjunction is unobservable as written — needs a
flag-exposing observable or a delete-the-default-jar variant if the distinction
ever matters; (2) tighten step 1's "at least two" to "exactly two" now that the
nominal seed shape is confirmed; (3) the wcId-recording baseline (identity-based
gone-ness, not counts) is what makes the closure claims strong — keep as a
pattern for tab-lifecycle specs; (4) institutionalize the settle-then-recapture
step for "deletion triggers a fallback open" expectations rather than leaving it
to executor initiative; (5) incidental: `wiped:true` on both removes (out of
scope, pinned elsewhere); Burner's partition-less identity object worth pinning
explicitly in a Flight 4 data-controls spec.

## Operator Notes

*(none — autonomous run; the flight debrief carries the spec-revision items)*
