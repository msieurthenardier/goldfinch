# Behavior Test Run: popup-jar-inheritance — 2026-07-10-16-52-13

**Spec**: [popup-jar-inheritance.md](../../popup-jar-inheritance.md)
**Status**: pass
**Started**: 2026-07-10T16:52:13Z
**Completed**: 2026-07-10T17:12:00Z (approx)
**Duration**: ~00:20
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/popup-jar-inheritance/2026-07-10-16-52-13/` during
the run; re-derivable by re-running the spec.

## Summary

5 / 5 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec
(authored at M06 Flight 3 design); spec flips `draft` → `active`. Run gates M06 F3
Leg 5 (verify-integration) acceptance for DD7 (popup jar inheritance).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: openTab (`jarId: work`, fixture URL) → wcId 3; settle;
  enumerateTabs (doubles as the pre-step-2 delta baseline).
- **Raw state**: tab wcId 3 exists, `jarId: work`; full set {2 (boot, personal), 3}.
- **Expected**: The tab exists with `jarId` `work`.
- **Verdict**: pass — Validator independently read the evidence file; boot
  baseline (wcId 2 only) established from the apparatus scan's step0 capture.
- **Evidence**: `step-1-openTab.json`, `step-1-enumerateTabs.json`,
  `step0-enumerateTabs.json`

### Checkpoint 2 — PASS
- **Actions taken**: evaluate on wcId 3: `window.open('https://example.com/')`
  (no-await IIFE); settle; enumerateTabs.
- **Raw state**: `window.open` returned null to the opener; delta vs {2,3} is
  exactly one new tab — wcId 4, `jarId: work`.
- **Expected**: Exactly one NEW tab; `jarId` `work` (inherited, not default
  `personal`); popup is a tab, no native window.
- **Verdict**: pass — inheritance proven; the null return means the opener holds
  no WindowProxy handle to its popup (supporting isolation signal).
- **Evidence**: `step-2-evaluate.json`, `step-2-enumerateTabs.json`

### Checkpoint 3 — PASS
- **Actions taken**: chrome-target evaluate
  `window.createTab('https://example.com/', window.makeBurner())`; settle;
  enumerateTabs.
- **Raw state**: new tab wcId 5, `jarId: burner-788909891`.
- **Expected**: A tab with `jarId` matching `^burner-\d+$` exists.
- **Verdict**: pass — exact opener identity string recorded from the evidence
  file for step 4's difference check.
- **Evidence**: `step-3-evaluate.json`, `step-3-enumerateTabs.json`

### Checkpoint 4 — PASS
- **Actions taken**: evaluate on wcId 5 (burner opener): `window.open(...)`;
  settle; enumerateTabs.
- **Raw state**: exactly one new tab — wcId 6, `jarId: burner-681486315`.
- **Expected**: Exactly one new tab; `jarId` matches `^burner-\d+$` AND differs
  from the opener's — a FRESH burner (never-share-state).
- **Verdict**: pass — exact-string comparison: `burner-681486315` ≠
  `burner-788909891`; opener retained its jarId (no reassignment).
- **Evidence**: `step-4-evaluate.json`, `step-4-enumerateTabs.json`

### Checkpoint 5 — PASS
- **Actions taken**: final enumerateTabs.
- **Raw state**: {2: personal, 3: work, 4: work, 5: burner-788909891,
  6: burner-681486315} — 5 tabs.
- **Expected**: All steps-1-4 tabs retain their original `jarId`s; total = boot
  tab + 4 opened.
- **Verdict**: pass — field-by-field match against the expected set the
  Validator accumulated from prior-step evidence; both burner ids remain
  distinct in the final state.
- **Evidence**: `step-5-enumerateTabs.json`, `step0-enumerateTabs.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (SendMessage available); both
  agents persisted across all 5 checkpoints. Sonnet both roles per crew file.
- Cache mode: `cold` — fresh scratch profile + fresh launch, staged per the Leg 5
  per-run staging ruling (instance existed only for this run; torn down after —
  the run-1 instance had been fully torn down first, and this instance re-claimed
  the same fallback port 49709).
- The Validator's two [READY] concerns (boot baseline; before/after deltas) were
  addressed operationally: the Executor's apparatus scan captured a step0
  enumeration, and every popup step's report carried the full pre/post tab sets.
  Spec-revision items below codify both.
- No operator interventions; no failing checkpoints; no re-spawns. Preconditions
  Orchestrator-verified (autonomous execution, scratch profile).

## Closing Summaries

### Executor closing

Clean first-attempt run: one MCP session held throughout; enumerations
immediately consistent after each mutation; settle recaptures never needed.
Observations: (1) `window.open` returned null to the opener in BOTH popup steps —
the popup path grants no WindowProxy, so opener-side scripting of popups is
impossible (an observable fact the spec doesn't currently pin; reinforces
confinement); (2) chrome-target `window.createTab(url, window.makeBurner())`
works exactly as the spec's parenthetical predicted; (3) burner ids are large
random integers, not a small counter — `^burner-\d+$` handles it, but log
readers shouldn't expect burner-1/burner-2; (4) freshly opened tabs briefly
report title "New tab" before the page title lands (url correct immediately) — a
spec asserting on titles would need settle-until-title. No environment hiccups;
no key material in evidence.

### Validator closing

5/5 PASS, every verdict from independently read evidence files, zero fresh
observations needed. Spec-revision recommendations: (1) codify the step-0
boot-baseline row and phrase steps 2/4 as "enumerate before and after" — this
run satisfied the delta claims only through executor diligence; (2) the
no-native-window clause is an indirect observable ("popup is a tab in the
enumeration") — switch to a direct window-count observable if the apparatus ever
grows one; (3) the identity clause proves distinct partition NAMING; actual
storage isolation between the two burners is unasserted — a cookie-write/read
cross-check variant would close that gap cheaply (both burner tabs are open at
step 5) — candidate for Flight 4/5 planning; (4) incidental: openers hold no
scripting handle to popups (null return) — note in flight artifacts in case a
future feature expects a usable WindowProxy.

## Operator Notes

*(none — autonomous run; the flight debrief carries the spec-revision items)*
