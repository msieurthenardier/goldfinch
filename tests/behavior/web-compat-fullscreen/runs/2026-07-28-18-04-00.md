# Behavior Test Run: web-compat-fullscreen — 2026-07-28-18-04-00

**Spec**: [web-compat-fullscreen.md](../../web-compat-fullscreen.md)
**Status**: pass
**Started**: 2026-07-28T18:04:00Z
**Completed**: 2026-07-28T18:35:00Z (approx; incl. one inline apparatus fix + app relaunch)
**Executor**: live agent (admin-keyed SDK-client apparatus)
**Validator**: live agent (independent; rendered-state precedence)

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/web-compat-fullscreen/2026-07-28-18-04-00/` during the run.

## Summary

7 / 7 checkpoints passed at run level — with one mid-run apparatus fix, one operator override in each direction, and one **known issue recorded** (below). This was the HAT bundle's first item; a human operator witnessed the entire run live, which proved decisive twice.

## Checkpoint Results

### Checkpoint 1 — PASS
Fixture page baseline: chrome visible, `fullscreenElement` null. Rendered + DOM agree.

### Checkpoint 2 — FAIL → PASS (rerun after apparatus fix)
- Original: Validator FAIL — captures showed chrome visible during fullscreen. **Operator witness contradicted**: live window was correctly fullscreen (no tabs, no flicker; paint in a couple of discrete chunks — accepted; the discrete-`setBounds` invariant rendering as designed).
- **Root cause (apparatus, not app)**: `grabWindow`'s WSLg/Wayland composite pasted the guest at the renderer-reported slot rect — which structurally cannot reflect fullscreen (renderer bounds sends are deferred in that mode). Fixed inline (single-surface: guest layer positioned from main-side `view.getBounds()` ground truth); app relaunched; gates stayed green (3089).
- Rerun: PASS — capture shows the video edge to edge, zero chrome, inside the 2s bound. Both verdicts preserved.

### Checkpoint 3 — PASS
Esc exit: chrome restored, video in normal slot, inside the bound. (Environment notes: app relaunch restored a duplicate fixture tab; window geometry changed mid-run.)

### Checkpoint 4 — PASS
Tab-switch force-exit, re-entry proven non-vacuous (pre-capture). Eval provably targeted the background tab. Follow-up on the fixture's lagging `#fs-state` label resolved in step 5: background repaint throttling, `fullscreenchange` had fired — no app defect.

### Checkpoint 5 — PASS
Second exit route (native player control; the fixture's own button is enter-only/unreachable under the top-layer video — fixture authoring quirk). Convergence proven observable-by-observable against step 3's end state.

### Checkpoint 6 — PASS (with finding)
Real site (YouTube, "Me at the zoo"): player fullscreen entered twice via real clicks; no goldfinch chrome in any capture; 4:3 pillarboxing geometrically correct. Mid-step fullscreen drop attributed to YouTube's end-screen/miniplayer SPA flow (site-initiated). **Finding**: fullscreen logical viewport (1400x900 CSS) undershot the real window — first sighting of the geometry issue resolved at checkpoint 7.

### Checkpoint 7 — Validator PASS → operator FAIL → **operator-ruled PASS with known issue**
- Validator passed on capture evidence; the **operator's screenshot contradicted it**: chrome and page rendering oversized and cropped in the physical window — the capture channel is structurally blind to cropping (`capturePage` renders a view's full content regardless of window crop) — **second capture-veridicality gap**, recorded.
- Operator probe: **resizing the window immediately self-corrected the view** (renderer bounds pipeline reasserts), and manual YouTube fullscreen worked correctly end to end.
- **Operator ruling: PASS**, with the defect recorded as a mission Known Issue: *after some fullscreen-exit / window-geometry interleavings (observed under WSLg Wayland with display scaling), guest and chrome view bounds can be left stale-oversized until the next window resize; self-corrects on any manual resize; manual fullscreen unaffected.*

## Orchestrator Notes

- Live two-agent mode; cold cache; admin-keyed SDK-client apparatus (jar-tier gap from the 2026-07-27 aborted run closed by the dev-mint launch recipe).
- Inline fix mid-run (HAT protocol): `grabWindow` composite — see flight/02 branch working tree; committed with the HAT flight.
- Two capture-veridicality gaps now cataloged: (1) composite slot-rect staleness (fixed); (2) `capturePage` ≠ physical-window crop (open; capture evidence cannot detect stale-oversized-bounds states — human witness required for that class).
- Operator overrides exercised in both directions — the Witnessed pattern's independent-observer discipline plus a live human is what caught both apparatus lies.

## Closing Summaries

*(Executor and Validator closing summaries recorded in the flight log; delivered after run close.)*

## Operator Notes

Operator ruled the run a pass after the resize probe and a successful manual fullscreen cycle. Known issue accepted rather than blocking the HAT session; follow-up diagnosis queued (see flight log / mission Known Issues).
