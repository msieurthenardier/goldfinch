# Behavior Test Run: responsive-tab-strip — 2026-07-14-14-44-17

**Spec**: [responsive-tab-strip.md](../../responsive-tab-strip.md)
**Status**: fail
**Started**: 2026-07-14T14:44:17Z
**Completed**: 2026-07-14T16:55:00Z (approx)
**Duration**: ~02:10 (includes an apparatus reset before Step 1)
**Executor**: sub-agent (Sonnet), live SendMessage continuation
**Validator**: sub-agent (Sonnet), live SendMessage continuation

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/responsive-tab-strip/2026-07-14-14-44-17/`
during the run; re-derivable by re-running the spec.

## Summary

8 / 10 checkpoints passed. 1 failed (Step 5 — sliver-stage active-tab close
affordance clipped as rendered); 1 inconclusive (Step 9 — WSLg maximize
caveat, needs-human-recheck). Run verdict **fail**: the Step-5 defect is real
and must be fixed and the spec re-run in full before the verify-integration
leg can land.

First run of the M09-F1-evolved spec (numeric `evaluate` reads primary,
replaced Step 4→5 pathological-count assertion, middle-click step added).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Admin SDK MCP client connect (free-fallback port after
  apparatus reset — see Orchestrator Notes); `tools/list`; `getChromeTarget`;
  baseline captureWindow/readAxTree/evaluate.
- **Raw state**: 28 tools incl. `getChromeTarget` + `evaluate`; chrome wcId 1
  (numeric); baseline 1 tab, `#tabs` scrollWidth 240 = clientWidth 240.
- **Expected**: tools present; numeric chrome wcId returned.
- **Verdict**: pass — Validator independently read the saved baseline JSON.
- **Evidence**: `step-1-screenshot.png`, `step-1-baseline.json`,
  `step-1-eval-raw.txt`, `step-1-axtree-raw.txt`

### Checkpoint 2 — PASS
- **Actions taken**: Opened 4 fixture tabs (pages 1–4 on `127.0.0.1:8000`);
  distinctness probe via readAxTree + evaluate + enumerateTabs.
- **Raw state**: 5 tabs, titles/URLs pairwise distinct (set size = count);
  no port-collision collapse.
- **Expected**: pairwise-distinct titles/URLs; record count.
- **Verdict**: pass.
- **Evidence**: `step-2-distinctness.json`, `step-2-screenshot.png`,
  `step-2-axtree-raw.txt`
- **Validator notes**: fixture em dash renders as mojibake (server sends no
  charset; decoded as windows-1252) in DOM *and* pixels — consistent, not a
  strip defect; distinctness rode on the page numerals. Fix fixture charset.

### Checkpoint 3 — PASS
- **Actions taken**: evaluate rect widths + overflow at 5 tabs; screenshot.
- **Raw state**: uniform ~223.2px tabs, scrollWidth 1132 = clientWidth 1132,
  all titles + closes display:block; screenshot agrees.
- **Expected**: tabs expand to share; no overflow; visual corroboration.
- **Verdict**: pass.
- **Evidence**: `step-3-geometry.json`, `step-3-screenshot.png`

### Checkpoint 4 — PASS
- **Actions taken**: opened to 16 tabs; evaluate widths/overflow/close
  displays; screenshot.
- **Raw state**: uniform 67px tabs; scrollWidth 1132 = clientWidth (no
  overflow); title-hide stage engaged on all (title display:none), all closes
  still visible incl. active; screenshot agrees.
- **Expected**: numerically narrower; no overflow; close-hide permitted on
  inactive only; active close visible. [a11y]
- **Verdict**: pass.
- **Evidence**: `step-4-geometry.json`, `step-4-screenshot.png`

### Checkpoint 5 — FAIL
- **Actions taken**: opened to 62 tabs (pathological); evaluate overflow +
  all 62 rect widths + close displays; captureWindow + magnified crops;
  follow-up fine-grained read of the active tab's close-button rect/style.
- **Raw state**: scrollWidth 1132 = clientWidth at 62 tabs (no scroll
  affordance); every tab width ~14.33px > 0, count preserved (no clip);
  all 61 inactive closes display:none; active close reads
  display:block/visible/opacity:1 — **but its 16px layout box sits ~19px
  outside its own 14.33px tab and is not painted** (crops show favicon dot +
  gold arc only, no ✕; the pixels at the button's rect are the + pill).
- **Expected**: no scroll affordance; no clip; active `.tab-close` non-none
  display; screenshot corroborates; **DD1 premise check: STOP on
  DOM-vs-pixels visibility disagreement**.
- **Verdict**: **fail** — screenshot is authoritative per the spec's
  apparatus rule: the active tab's close affordance is NOT rendered at the
  sliver stage, contradicting the Intent's "active tab keeps its close
  affordance at every stage". The quantitative no-scroll/no-clip invariants
  themselves held.
- **Evidence**: `step-5-geometry.json`, `step-5-screenshot.png`,
  `step-5-strip-right-6x.png`, `step-5-closerect-8x.png`,
  `step-5-activeclose-raw.txt`
- **Validator notes**: mechanism is overflow-clipping (a 16px button in a
  14.33px `overflow:hidden` tab with no sliver-stage rule for the active
  close), not literally an `@container` misrender — but it is exactly the
  DOM-correct-≠-render-correct class the premise check exists to catch.
- **Operator decision**: continued (Steps 6–10 are independent, run at
  comfortable widths); fix + full re-run scheduled before the leg lands.

### Checkpoint 6 — PASS
- **Actions taken**: (setup: closed the 46 surplus tabs via closeTab, back to
  16) before-read; pointer left-click on a middle tab's close at (560,23);
  after-read at the unmoved coordinate.
- **Raw state**: count 16→15; all remaining widths byte-identical 67px (no
  resize); right neighbor slid left — its close center now exactly (560,23);
  scrollWidth −71 (≈ tab width + gap).
- **Expected**: count −1; widths kept; slide-under-cursor; trailing gap.
- **Verdict**: pass — deferred-reflow contract verified to the pixel. The
  literal "clientWidth is unchanged" clause did not hold because `#tabs`
  shrink-wraps (scrollWidth == clientWidth throughout; the gap opens outside
  `#tabs`) — judged a spec-wording box-model assumption, not a reflow
  failure; reword in the next spec touch.
- **Evidence**: `step-6-before.json`, `step-6-after.json`,
  `step-6-screenshot.png`
- **Validator notes**: because the pill hugs the strip, it slides left
  immediately on pointer-close (freeze protects tab close targets, not the
  pill) — potential UX follow-up, recorded.

### Checkpoint 7 — PASS
- **Actions taken**: second pointer-close at the same coordinate; mid-read;
  mouseleave induced via click at (700,500) in empty fixture content;
  final read + screenshot.
- **Raw state**: 15→14 with widths still frozen at 67 (second consecutive
  frozen close, next tab slid under the coordinate); after mouseleave all 14
  grew 67 → ~77.15 and scrollWidth returned to 1132 (full strip).
- **Expected**: second frozen close; re-expand on mouseleave; gap gone.
- **Verdict**: pass (same shrink-wrap wording caveat as Step 6).
- **Evidence**: `step-7-mid.json`, `step-7-final.json`,
  `step-7-screenshot.png`

### Checkpoint 8 — PASS
- **Actions taken**: trusted middle-click (`button:'middle'`) on a tab BODY
  at (514,23), 35px clear of its close button; before/after reads; no click
  outside the strip afterward.
- **Raw state**: 14→13; widths unchanged (77.14–77.16); right neighbor slid
  into the slot with byte-identical slot geometry; active tab unchanged.
- **Expected**: count −1 via the same deferred-reflow signature as Step 6.
- **Verdict**: pass — middle-click close (M09 F1 DD3) rides the identical
  freeze path as ✕-close.
- **Evidence**: `step-8-before.json`, `step-8-after.json`,
  `step-8-screenshot.png`

### Checkpoint 9 — INCONCLUSIVE (needs-human-recheck)
- **Actions taken**: located `#win-max` (aria-label "Maximize", data-state
  "normal"); clicked; read aria-label/title/data-state/AX name + screenshots;
  clicked again; re-read incl. settled +3s/+6s reads.
- **Raw state**: click 1: all four read channels flipped to
  Restore/maximized, but window geometry stayed 1400x900 at the read point.
  Click 2: channels flipped back to Maximize/normal, but the window then
  settled at 2560x1392 (screen-filling) while reporting "normal", stable
  through +6s.
- **Expected**: state flips tracking real window state; WSLg caveat — if the
  compositor is unreliable, needs-human-recheck with the IPC-seam assertion
  as the minimum bar.
- **Verdict**: inconclusive — judged under the caveat's degraded regime: the
  clicks demonstrably reached the IPC seam and the DD7 read path flipped
  correctly both directions, but geometry never corroborated state at any
  read point, so event-driven tracking vs blind toggle cannot be
  distinguished on this compositor. **Manual maximize/restore observation on
  a native compositor is required** (routed to the mission's HAT flight).
  If the geometry/state desync reproduces off-WSLg it is a real DD7 bug.
- **Evidence**: `step-9-state1.json`, `step-9-state2.json`,
  `step-9-screenshot-0..3.png`, `step-9-axtree-1/2-raw.txt`
- **Operator decision**: continued.

### Checkpoint 10 — PASS
- **Actions taken**: fresh rects at the 2560x1392 window; anchor click on the
  address bar (pointer off-strip); Shift+Tab walk backward into the tablist
  (forward Tab never reaches it — see notes); focus verified on the active
  tab; `Delete` pressed; before/after reads.
- **Raw state**: 13→12; widths grew immediately 172.6 → 187.3 in the
  ~0.5s after-read with no frozen phase — the opposite signature of
  Steps 6/8.
- **Expected**: keyboard close reflows immediately.
- **Verdict**: pass.
- **Evidence**: `step-10-before.json`, `step-10-after.json`,
  `step-10-screenshot.png`
- **Validator notes**: (1) forward Tab order from the address bar walks into
  media-panel controls (panel not visibly open) and never reaches the strip;
  the shortest keyboard route to the tablist is Shift+Tab through the window
  Close/Max/Min buttons — flag to `tab-keyboard-operability` owners
  (advisory). (2) Step ran in the desynced post-Step-9 geometry; the
  contract is width-relative, verdict unaffected.

## Orchestrator Notes

- **Execution mode**: live two-agent continuation (SendMessage available).
  Cache mode: cold (fresh app launch).
- **Apparatus reset before Step 1**: the launch-time `GOLDFINCH_MCP_PORT=49707`
  pin failed loudly ("port in use" — the port is held outside this WSL netns,
  not visible to `ss`; attributed to WSL mirrored networking / Windows-side
  listener), and a stale prior app instance briefly confused discovery. The
  Executor's initial `[BLOCKED:no-apparatus-admin-mcp-chrome]` was resolved
  by relaunching WITHOUT the env pin — free-fallback bound the next free
  port and fresh keys were minted. **Spec learning**: the preconditions'
  hard-coded `GOLDFINCH_MCP_PORT=49707` should be softened to "pin if free,
  else free-fallback and read the bound port" — fold into the next spec
  touch.
- Session-registered goldfinch MCP tools were explicitly disqualified for
  this run (their client config pointed at a dead endpoint with a stale key);
  the SDK admin client was the sole apparatus.
- Accessibility Validator not spawned (project crew has it `Enabled: false`);
  `[a11y]` rows judged by the base Validator observationally.
- Failed/inconclusive checkpoints: operator (Flight Director, pre-authorized
  autonomous) chose **continue** at Step 5 and Step 9 to capture the full
  picture; Step 5 triggers a fix + full re-run before the leg lands; Step 9
  routes to the mission HAT flight for native-compositor manual recheck.

## Closing Summaries

### Executor closing

Core apparatus performed flawlessly after the reset (60+ openTab, 47
closeTab, zero MCP errors). Key anomalies: (1) Step 5's clipped active close
— the button's layout box lands outside its own tab and is never painted; a
fix that merely keeps `display:block` won't re-run green against the
rendered-truth rule; the button must actually fit (or the active tab needs a
floor). (2) Step 9's state channels flip cleanly but geometry lags >1s and
ended desynced (screen-filling while "normal"). (3) `#tabs` shrink-wraps —
the spec's "clientWidth unchanged" framing doesn't map to this DOM. (4)
`#window-controls` is inside `#tabstrip`, so window-control clicks don't end
the width freeze. (5) Forward-Tab never reaches the tablist from the address
bar. (6) Fixture pages need `<meta charset>`.

### Validator closing

The core responsive contract held on strong paired numeric evidence
corroborated by screenshots. The one FAIL is exactly what the spec was built
to catch (DD1 premise check firing on a genuine DOM-vs-pixels divergence).
The INCONCLUSIVE is environmental and pre-declared (WSLg caveat).
Spec-quality items for the next touch: reword the clientWidth/gap clause
semantically; codify that window-control clicks can't end the freeze;
flag the forward-Tab order to `tab-keyboard-operability`; fix the fixture
charset. The numeric-primary + screenshot tie-break apparatus (DD4) worked
as designed — it confirmed agreement on nine checkpoints and caught the one
genuine divergence.

## Operator Notes

Disposition: Step-5 defect goes back to the flight as a fix (flight-log
Decision amends DD2 with an active-tab-only accommodation; global floor
remains barred); spec-wording items fold into the same fix leg; full spec
re-run required before verify-integration lands. Step 9 manual recheck is a
HAT-flight item. Advisory findings (focus order, pill slide, fixture
charset) recorded in the flight log for triage.
