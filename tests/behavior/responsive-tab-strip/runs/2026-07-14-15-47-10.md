# Behavior Test Run: responsive-tab-strip — 2026-07-14-15-47-10

**Spec**: [responsive-tab-strip.md](../../responsive-tab-strip.md)
**Status**: pass
**Started**: 2026-07-14T15:47:10Z
**Completed**: 2026-07-14T17:55:00Z (approx)
**Duration**: ~02:08
**Executor**: sub-agent (Sonnet), live SendMessage continuation (fresh agent — re-run policy)
**Validator**: sub-agent (Sonnet), live SendMessage continuation (fresh agent)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/responsive-tab-strip/2026-07-14-15-47-10/`
during the run; re-derivable by re-running the spec.

## Summary

**10 / 10 checkpoints passed.** Full re-run after the run-2026-07-14-14-44-17
Step-5 failure was fixed (M09 F1 DD2-amendment: 64px active-tab-only floor +
active-tab favicon-hide at the title threshold) and the spec was updated
(Step 5 rect-containment + rendered-✕ assertion; Steps 6/7 semantic gap
contract; softened port pin; apparatus facts). This run closes the fix loop:
the previously failed checkpoint passed with independently verified rect
arithmetic AND pixel inspection, and last run's Step-9 inconclusive resolved
to a primary-regime pass on stronger (outer-geometry) evidence.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: admin SDK client on the free-fallback port; tools/list;
  getChromeTarget; baseline captures.
- **Raw state**: 28 tools incl. `getChromeTarget`+`evaluate`; chrome wcId 1;
  baseline 1 tab, no overflow.
- **Verdict**: pass — Validator read the saved baseline JSON independently.
- **Evidence**: `step-1-baseline.json`, `step-1-screenshot.png`

### Checkpoint 2 — PASS
- **Actions taken**: 4 fixture tabs opened; distinctness probed via axtree +
  enumerateTabs + evaluate (three independent channels).
- **Raw state**: 5 tabs, titles/URLs pairwise distinct; no collision.
- **Verdict**: pass.
- **Evidence**: `step-2-distinctness.json`, `step-2-screenshot.png`
- **Notes**: fixture charset deviation (mojibake em dash) — fixture fixed on
  disk mid-run; already-open tabs kept cached titles (cosmetic; distinctness
  rides pairwise inequality). Future runs: bump fixture mtimes / no-cache.

### Checkpoint 3 — PASS
- **Raw state**: 5 tabs ~223.2px sharing the strip, no overflow, all titles +
  closes visible; screenshot agrees.
- **Verdict**: pass.
- **Evidence**: `step-3-geometry.json`, `step-3-screenshot.png`

### Checkpoint 4 — PASS
- **Raw state**: 16 tabs uniform 67px (vs 223.2 baseline), no overflow;
  title-hide engaged strip-wide, all closes still visible incl. active.
- **Verdict**: pass. [a11y]
- **Evidence**: `step-4-geometry.json`, `step-4-screenshot.png`
- **Notes**: disclosure order observed — title-hide engages strip-wide
  (including the active tab) before close-hide starts; spec permits, now
  documented here.

### Checkpoint 5 — PASS (the previously failed checkpoint)
- **Actions taken**: 62 tabs; overflow + all widths + close displays + active
  close-rect vs tab-rect containment; captureWindow + 8x active-tab crop.
- **Raw state**: scrollWidth 1132 == clientWidth at 62 tabs (no scroll);
  min width 13.5px > 0, count preserved (no clip); 61 inactive closes
  display:none, active close display:block; **containment: active close rect
  {1101..1117 × 15..31} fully inside active tab rect {1075..1139 × 7..39}**
  (all four edges); **8x crop shows a painted ✕ inside the gold-bordered
  active tab** — the DD2-amendment 64px active floor working as designed.
  DOM and pixels agree at every read.
- **Verdict**: pass — Validator re-derived the edge inequalities from raw
  numbers and inspected the magnified pixels itself.
- **Evidence**: `step-5-geometry.json`, `step-5-active-crop.png`,
  `step-5-strip-right-3x.png`, `step-5-screenshot.png`
- **Notes**: inactive slivers still paint favicon dots at 62 tabs
  (informational; spec is silent on favicon staging).

### Checkpoint 6 — PASS
- **Raw state**: pointer ✕-close at (560,23): 16→15, width set byte-identical
  {67}, right neighbor slid into the slot (elementFromPoint at the unmoved
  coordinate returns the next `.tab-close`), shrink-wrap semantics
  (scrollWidth == clientWidth, 1132→1061, gap outside `#tabs`).
- **Verdict**: pass — judged under the reworded semantic gap contract.
- **Evidence**: `step-6-before.json`, `step-6-after.json`,
  `step-6-screenshot.png`

### Checkpoint 7 — PASS
- **Raw state**: second frozen close (15→14, widths still 67); mouseleave via
  outside click → all tabs grew 67→~77.15, scrollWidth back to 1132.
- **Verdict**: pass.
- **Evidence**: `step-7-mid.json`, `step-7-final.json`,
  `step-7-screenshot.png`

### Checkpoint 8 — PASS
- **Raw state**: trusted middle-click on a tab BODY (35px clear of its ✕):
  14→13 with the identical deferred-reflow signature (byte-identical widths,
  slide-into-slot, shrink-wrap).
- **Verdict**: pass — M09 F1 DD3 verified.
- **Evidence**: `step-8-before.json`, `step-8-after.json`,
  `step-8-screenshot.png`

### Checkpoint 9 — PASS (primary regime; last run: inconclusive)
- **Raw state**: DD7 read path clean both directions (aria-label/title/
  data-state + axtree name + rendered glyph, all in lockstep on both clicks).
  **Outer geometry corroborated both real transitions**: 2560x1392 maximized
  (screen avail), 1432x942 restored — the evidence last run lacked. WSLg
  artifact: renderer inner viewport lagged the whole cycle and ended
  desynced (inner 2560x1392 vs outer 1432x942, stable ≥10s).
- **Verdict**: pass — the caveat's trigger ("maximize doesn't land") did not
  obtain; button state tracked real (outer) window state at both
  checkpoints. The inner-viewport lag is an environment artifact, recorded —
  a manual maximize/restore spot-check on native hardware remains
  recommended (HAT-flight item carried from run 1). [a11y]
- **Evidence**: `step-9-state1.json`, `step-9-state2.json`,
  `step-9-screenshot-1-maximized.png`, `step-9-screenshot-3-settled.png`,
  geometry/restore probe logs

### Checkpoint 10 — PASS
- **Raw state**: verified focus walk to the active `role=tab` (window
  controls traversed focus-only — win-max `data-state` unchanged pre-Delete);
  trusted Delete: 13→12, widths grew ~172.6→~187.3 within 0.5s, pointer
  off-strip — immediate reflow, clean contrast with Steps 6–8.
- **Verdict**: pass.
- **Evidence**: `step-10-before.json`, `step-10-after.json`,
  `step-10-focuswalk-raw.txt`, `step-10-screenshot.png`

## Orchestrator Notes

- Live two-agent mode; cache-cold; fresh agents per re-run policy.
- App launched WITHOUT the port pin per the updated precondition
  (free-fallback bound 49709; 49707 remains held outside the WSL netns).
- Accessibility Validator not spawned (project crew `Enabled: false`);
  `[a11y]` rows judged observationally by the base Validator.
- Apparatus learnings for future authors: `pressKey` accepts `ShiftTab`
  (not `Shift+Tab`); the backward focus walk to the tablist passes focus
  through win-close/win-max/win-min (focus-only, but a stray Enter during
  the walk would hit Close — keyboard specs beware); fixture HTTP caching
  can keep stale titles alive after an on-disk fixture fix.

## Closing Summaries

### Executor closing

All 10 steps executed; zero MCP errors; 46 opens + 46 closes at 60+ tabs
stayed responsive; re-expand settled <1s, keyboard reflow <0.5s. Substantive
anomaly: the Step-9 WSLg renderer-viewport lag/desync (outer corroborated
both transitions; inner lagged and stuck) — compositor artifact, worth a
human eyeball on real hardware. Fixture charset fixed mid-run with an HTTP
cache tail on already-fetched pages.

### Validator closing

The prior run's defect is affirmatively resolved (floor observable in the
numbers; containment re-derived from raw rects; painted ✕ verified in the
8x crop). The reworded Steps 6/7 semantic contract proved directly
falsifiable and matched reality precisely. Spec suggestions: name the
renderer-viewport-lag failure mode in Step 9's caveat; reconcile the
favicon-staging intent vs rendering at pathological counts; keep the
outer-geometry corroboration as a standing Step-9 requirement. Executor
evidence discipline was excellent (raw numbers, before/after pairs,
elementFromPoint identity checks, the focus-only probe).

## Operator Notes

Run closes the flight-1 fix loop: `responsive-tab-strip` is green end-to-end
on the evolved spec. Step-9 native-compositor manual recheck stays on the
mission HAT flight's list. Spec-polish suggestions (Step-9 caveat wording,
favicon staging note) are minor and folded forward as HAT/debrief material,
not blockers.
