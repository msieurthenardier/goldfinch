# Behavior Test Run: responsive-tab-strip — 2026-06-24-23-02-18

**Spec**: [tests/behavior/responsive-tab-strip.md](../../responsive-tab-strip.md)
**Status**: partial
**Started**: 2026-06-24T23:02:18Z
**Completed**: 2026-06-25T14:01Z (wall-clock spans the async multi-agent session; active execution was a fraction of this — long idle gaps between async Executor/Validator round-trips)
**Executor**: a4fd68fb8698022f8 (Sonnet)
**Validator**: af90fc3112546fd39 (Sonnet)
**Context**: Flight-2 Window Shell HAT (Leg 3, `verify-shell-hat`), branch `flight/02-window-shell` — verifying tab-strip parity on the migrated `BaseWindow` + chrome `WebContentsView` shell.
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/responsive-tab-strip/2026-06-24-23-02-18/` during the run; re-derivable by re-running the spec.

## Summary

3 / 3 executed checkpoints passed. Steps 4–8 were **not exercised** — the run was wrapped after Step 3 by operator decision once the migration-relevant assertions were confirmed (the remaining steps test renderer-internal tab-strip reflow timing that the window-host swap does not touch). Behavior (1) of the spec's Intent (responsive shrink/grow sizing) is fully verified; behaviors (2) deferred-pointer-close reflow and (3) the maximize-state read path via this spec's Step 7 are partially covered (DD7 read path confirmed out-of-band; see notes).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: Connected admin MCP SDK client (`127.0.0.1:49709/mcp`, admin Bearer); `tools/list` (27 tools); `getChromeTarget()`; baseline `captureWindow()`; tab count via `readAxTree`.
- **Raw state**: 27 tools incl. `getChromeTarget`; `getChromeTarget()` → `{wcId:1, kind:'chrome', url:file://…index.html}` (numeric wcId). 1 baseline tab (Google).
- **Expected**: `tools/list` includes `getChromeTarget`; `getChromeTarget()` returns `{wcId,kind:'chrome',url}` with numeric wcId.
- **Verdict**: pass — Validator independently confirmed via `step-1-screenshot.png`.
- **Evidence**: `step-1-screenshot.png`, `step-1-snapshot.txt`

### Checkpoint 2 — PASS
- **Actions taken**: Opened 4 tabs at distinct URLs (example.com/.org/.net, wikipedia.org); `captureWindow()`.
- **Raw state**: 5 tabs share the strip at ~226px each; favicon + full title + close visible on each; no horizontal scroll affordance.
- **Expected**: Tabs expand to share available width (comfortably wide, above floor); no horizontal scroll affordance.
- **Verdict**: pass — Validator read `step-2-screenshot.png` directly; 5 comfortably-wide tabs, no scrollbar.
- **Evidence**: `step-2-screenshot.png`

### Checkpoint 3 — PASS `[a11y]`
- **Actions taken**: Opened up to 15 tabs, then closed back to 12 and 9; `captureWindow()` at 1400px (and a clean maximized 2560px frame); `evaluate()` numeric cross-check of tab widths + `#tabs` overflow.
- **Raw state**: At 1400px, 9–15 tabs collapse to a uniform ~110px floor (favicon + 16px close kept, title ellipsized) with a horizontal scrollbar (overflow=TRUE). The SAME ~10 tabs at maximized 2560px render at ~226px comfortable with `#tabs` scrollWidth==clientWidth (overflow=FALSE, no scrollbar). Width tracks available strip width, not tab count.
- **Expected**: Tabs shrink (narrower than Step 2); favicon + close stay visible, title ellipsizes; NOT a fixed 120–220px width with an always-on scrollbar.
- **Verdict**: pass — Validator confirmed via two clean rendered frames (`step-3-screenshot.png` 1400px floored+scrolled; `step-3-screenshot-3.png` 2560px comfortable+no-scroll) + the `evaluate()` numeric read. `[a11y]` favicon/close visibility and title ellipsis at the floor confirmed.
- **Evidence**: `step-3-screenshot.png`, `step-3-screenshot-2.png`, `step-3-screenshot-3.png`, `step-3-snapshot.txt`

### Checkpoints 4–8 — SKIPPED (not exercised)
- Step 4 (scroll-onset as last resort): **effectively evidenced** by Step 3's data — overflow=TRUE only at the ~110px floor (1400px), overflow=FALSE when tabs fit (2560px) — but not run as its own checkpoint.
- Steps 5–6 (deferred pointer-close reflow): not run — renderer-internal reflow timing, untouched by the window-host migration.
- Step 7 (maximize/restore state, `[a11y]`): **DD7 read path confirmed out-of-band** during Step 3 recovery — clicking maximize flipped the control's `data-state` `normal→maximized` and its accessible label to `Restore`. The operator separately confirmed on-screen maximize/unmaximize work correctly (the spec's WSLg human-recheck fallback). Restore (un-maximize) via the MCP click is flaky on the WSLg compositor (matches the spec's Step 7 open question) — not a migration regression.
- Step 8 (keyboard-close reflow): not run.

## Orchestrator Notes
- **Mode**: live two-agent (SendMessage continuation available). Executor + Validator stayed alive across checkpoints; Validator took its own fresh screenshot reads rather than trusting Executor prose.
- **Models**: Executor + Validator both Sonnet (per crew file; Validator never Opus).
- **Apparatus**: the SDK-over-HTTP admin MCP client (port 49709, fallback from the spec's default 49707), NOT the disqualified chrome-devtools MCP. App already running via `npm run dev:automation` (the migrated shell); not relaunched by the crew.
- **Operator interventions**: (a) paused the loop after Step 3 to adjudicate the WSLg capture anomaly — operator confirmed the on-screen render is correct (maximize/unmaximize + page content all respond), establishing the distortion as a capture-path artifact, not a render bug; (b) elected to wrap after Step 3 once migration-relevant assertions were confirmed.
- **Spec-drift findings to fold back into the spec** (raised by both agents): (1) the spec's "no in-page numeric read" apparatus assumption is **outdated** — `evaluate()` executes JS in the chrome renderer and returns exact `getBoundingClientRect`/`scrollWidth`/overflow; it should be a first-class observable (and makes the Step 5/6 width-invariance checks far more robust than frame-diffing). (2) Formalize a fallback: when `captureWindow()` is distorted on WSLg, fall to `evaluate()` numeric reads rather than INCONCLUSIVE. (3) The fixture-distinctness precondition needs an active probe — three tabs shared the title "Example Domain", which would have made Step 5's "slid-left" identity judgment ambiguous.

## Closing Summaries

### Executor closing
Apparatus (SDK admin MCP client, port 49709) reachable + stable; 27 tools; chrome wcId=1. Confirmed Steps 1–3: shrink/grow-to-fit (width tracks available space — 110px floor+scroll at 1400px vs 226px+no-scroll at 2560px), not the old fixed-width+always-on-scrollbar. Anomalies: (1) WSLg `captureWindow()` distortion at un-maximized 1400×900 (whole chrome compressed top-left, stable; DOM provably intact at full size; maximizing cleared it) — same WSLg geometry instability the Step 7 caveat anticipates; (2) `evaluate()` numeric reads ARE available (spec's "no numeric read" premise outdated) and were decisive while the native capture was distorted; (3) restore-after-maximize did not un-maximize on this compositor (window left maximized). Deleted all 6 temp probe scripts from `scripts/`; tree restored.

### Validator closing
Steps 1–3 PASS establish the core migration claim (width tracks available strip width). Spec-quality: drop the "no in-page numeric read" assumption — `evaluate()` is available and cleaner than screenshot deltas; formalize a captureWindow→evaluate fallback for WSLg distortion; add an active fixture-distinctness probe (duplicate "Example Domain" titles would have made Step 5 ambiguous). Coverage gap from the early wrap: Intent behavior (1) confirmed; (2) deferred-reflow and (3) keyboard-close-reflow outstanding; Step 7 maximize-state `[a11y]` covered out-of-band via the DD7 `data-state` read + operator observation.

## Operator Notes
Run wrapped early by operator decision after the migration-relevant assertions (responsive sizing + DD7 maximize-state read) were confirmed and the on-screen render was operator-verified correct. The skipped steps test renderer-internal tab-strip behaviors that the Flight-2 window-host swap does not modify; they remain a valid future regression run. The WSLg `captureWindow` distortion is an apparatus/compositor caveat (on-screen render is correct), relevant to any future captureWindow-based behavior test — prefer maximized capture or the `evaluate()` numeric read.
