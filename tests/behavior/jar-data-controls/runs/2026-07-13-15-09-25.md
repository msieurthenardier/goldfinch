# Behavior Test Run: jar-data-controls — 2026-07-13-15-09-25

**Spec**: [jar-data-controls.md](../../jar-data-controls.md)
**Status**: pass
**Started**: 2026-07-13T15:09:25Z
**Completed**: 2026-07-13T15:26:54Z
**Duration**: ~00:17:29
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/jar-data-controls/2026-07-13-15-09-25/` (22 `step-N-*.json`
capture files + `args-*.json` helper files) during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. Second run of this spec — the first
run to exercise the **H6 close-not-reload** rewrite (M08 Flight 6 HAT). The prior run
(2026-07-10-22-10-41) observed Step 5 as a RELOAD-in-place sweep (`__bt_alive` UNDEFINED,
same wcId survived); this run confirms the rewritten behavior: the wiped jar's tabs are
CLOSED (pre-wipe wcId absent from enumeration + stale eval `no-such-contents`). This run
gates the M08 Flight 6 `hat-reverification` leg (the `jar-data-controls` re-run acceptance
item) and re-confirms the unchanged F4 clear/wipe/reject/isolation semantics did not regress.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: boot baseline enumerate (single personal boot tab, wcId 2); `openTab`
  (`jarId: work`, https://example.com/) → wcId 3; in-tab evaluate staged all three probes
  (cookie `bt_probe=work1`, localStorage `bt_probe=work1`, expando `__bt_alive=1`) and read
  them back.
- **Raw state**: stage evaluate on wcId 3 returned `{cookie:"bt_probe=work1", ls:"work1", alive:1}`.
- **Expected**: All three values read back — the work jar is staged.
- **Verdict**: pass — all three probes present; wcId 3 confirmed `jarId: work`.
- **Evidence**: `step-1-stage-work.json`, `step-1-openTab-work.json`, `step-1-enumerate-baseline.json`

### Checkpoint 2 — PASS
- **Actions taken**: `openTab` (`jarId: personal`) → wcId 4 (the load-bearing pre-wipe wcId);
  staged cookie `bt_probe=personal1` + expando; read back.
- **Raw state**: `{cookie:"bt_probe=personal1", alive:1}`. Personal jar now holds two tabs
  (boot wcId 2 + staged wcId 4).
- **Expected**: Both values read back — the personal jar is staged with its own cookie.
- **Verdict**: pass — both values read back; distinct cookie value (personal1 vs work1).
- **Evidence**: `step-2-stage-personal.json`, `step-2-openTab-personal.json`

### Checkpoint 3 — PASS
- **Actions taken**: chrome-eval `jarsClearData({id:'work', classes:['cookies']})`; probes re-read
  on both tabs.
- **Raw state**: resolved `{ok:true, cleared:['cookies']}`; work tab `{cookie:"", ls:"work1", alive:1}`;
  personal tab `{cookie:"bt_probe=personal1"}`.
- **Expected**: `{ok:true}`. Work: cookie gone, localStorage still `work1` (class independence),
  `__bt_alive` still `1` (no reload). Personal: cookie intact (containment).
- **Verdict**: pass — all four clauses on the first capture. No-reload probe confirmed on the
  work tab specifically.
- **Evidence**: `step-3-jarsClearData-work-cookies.json`, `step-3-work-tab-postclear.json`,
  `step-3-personal-tab-postclear.json`

### Checkpoint 4 — PASS
- **Actions taken**: `jarsClearData({id:'work', classes:['storage']})`; localStorage re-read.
- **Raw state**: `{ok:true, cleared:['storage']}`; work tab `{ls:null, alive:1}`.
- **Expected**: `{ok:true}` and localStorage now `null` — storage cleared independently of the
  earlier cookie clear.
- **Verdict**: pass — localStorage `null` (was `work1` in checkpoint 3); the before/after pair
  pins class independence; expando still alive (no reload).
- **Evidence**: `step-4-jarsClearData-work-storage.json`, `step-4-work-tab-storage-read.json`

### Checkpoint 5 — PASS  *(the H6 assertion under test)*
- **Actions taken**: `jarsWipe({id:'personal'})`; ~2s settle; full enumerate; stale evaluate
  against the personal tab's pre-wipe wcId 4.
- **Raw state**: `{ok:true}`. Post-wipe enumeration = ONLY `[{wcId:3, jarId:work, active:true}]`.
  BOTH personal tabs (staged wcId 4 AND boot wcId 2) are ABSENT. Stale eval against wcId 4
  errored `automation: no-such-contents — wcId 4 is not a live webContents` — the WebContents
  was DESTROYED (tab CLOSED), not reloaded. No last-tab-fallback tab appeared (the work tab
  kept the tab set non-empty).
- **Expected**: `{ok:true}`; the enumeration no longer contains the personal tab's pre-wipe
  wcId — the tab was CLOSED, not reloaded (H6 — supersedes the earlier reload sweep); the stale
  evaluate errors rather than reading a reloaded page.
- **Verdict**: pass — two independent close signals (wcId absent + stale-eval error) both
  positive. **Independently re-verified by the Validator** via a read-only `enumerateTabs`,
  which returned exactly `[{wcId:3, work, active:true}]` — corroborating the closure. This
  supersedes the 2026-07-10 run's observed reload-in-place sweep.
- **Evidence**: `step-5-jarsWipe-personal.json`, `step-5-enumerate-postwipe.json`,
  `step-5-stale-eval-wcId4.json`

### Checkpoint 6 — PASS
- **Actions taken**: all four rejection combinations (wipe/clear × burner/unknown), then `jarsList()`.
- **Raw state**: all four resolved `{ok:false, error:"jars: <op> — unknown-jar"}`; `jarsList()`
  returned exactly Personal + Work, unchanged.
- **Expected**: all four `{ok:false}` — Burner (no `partition`) + unknown ids rejected on BOTH
  channels; `jarsList()` unchanged.
- **Verdict**: pass — each rejection individually `{ok:false}` on both channels; list unchanged.
  (This run surfaces an `error` field — richer than the prior run's bare `{ok:false}`, partially
  addressing the prior Validator's reason-field carry-forward, though burner and unknown-id still
  share the `unknown-jar` code — see carry-forward 2.)
- **Evidence**: `step-6-wipe-burner.json`, `step-6-clear-burner.json`, `step-6-wipe-unknown.json`,
  `step-6-clear-unknown.json`, `step-6-jarsList.json`

### Checkpoint 7 — PASS
- **Actions taken**: two burner tabs via chrome-eval `createTab(url, makeBurner())` → wcIds 5/6,
  jarIds `burner-696650532` / `burner-248658354`; cookie `bt_burner=A` set + read back in A;
  cookie read in B.
- **Raw state**: both jarIds match `^burner-\d+$` and differ (distinct partitions
  `burner:696650532` vs `burner:248658354`); A reads back `bt_burner=A`; B's cookie is EMPTY.
- **Expected**: both jarIds match `^burner-\d+$` and differ; A reads back `bt_burner=A`; B has
  NO `bt_burner` — burner tabs do not share storage.
- **Verdict**: pass — distinct valid burner jarIds; asymmetric storage isolation confirmed on
  concrete cookie reads.
- **Evidence**: `step-7-createBurnerA.json`, `step-7-createBurnerB.json`, `step-7-enumerate.json`,
  `step-7-burnerA-set-cookie.json`, `step-7-burnerB-read-cookie.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (`SendMessage` available); both agents (Sonnet
  per crew) persisted across all 7 checkpoints. Judge-before-advance held throughout.
- Cache mode `cold` — fresh scratch profile (empty `XDG_CONFIG_HOME`) + fresh launch staged by
  the Orchestrator, then torn down (Electron pid killed directly, port 49731 verified dead,
  scratch dir removed).
- Port: fixed at 49731 this run (Orchestrator-assigned via `GOLDFINCH_MCP_PORT`, not the
  free-port fallback). Fresh-profile seed confirmed exactly Personal (id `personal`, default) +
  Work (id `work`) before spawning agents.
- Apparatus: the goldfinch automation server is an HTTP MCP (not a Claude-registered MCP); both
  agents drove it via a reusable SDK-client helper (`/tmp/gf-client.mjs`) over Bash with the
  admin key in the Authorization header. The Orchestrator smoke-tested the recipe (getChromeTarget,
  jarsList, openTab, in-tab evaluate) against the instance before spawning, then relaunched a
  pristine profile so the agents started clean.
- Admin key handled per protocol: used only in Authorization headers, never written to any
  evidence file or committed artifact.
- No operator interventions; no failing checkpoints; no re-spawns. One Validator independent
  re-observation (Step 5, read-only) corroborated the H6 closure.

## Closing Summaries

### Executor closing

7/7, every observable exactly as the spec predicted. No anomalies material to the assertions
(boot tab loaded google.com rather than about:blank — irrelevant). Step 5 detail worth carrying:
the wipe swept BOTH personal tabs (boot wcId 2 AND staged wcId 4), leaving a single work tab; no
last-tab-fallback appeared because the work tab kept the set non-empty — budget for the fallback
tab in a future run that wipes the only remaining jar. Main apparatus friction: shell
single-quote collision on quote-bearing JS expressions ("Unterminated string in JSON") — reliable
fix is to route the args JSON through a file rather than inlining a quote-bearing expression on
the Bash line; recommended as standard for future runs. The `/tmp/gf-client.mjs` helper was
rock-solid; the `x===undefined?'UNDEFINED':x` expando sentinel cleanly distinguished
"expando alive" from "tab gone." The stale-eval error surfaced on stderr with exit 0 — capture
stderr explicitly for expected-error steps. wcIds were allocated sequentially (2,3,4 then 5,6
after the personal pair was swept — closed ids not reused).

### Validator closing

7/7 PASS, every verdict on cited evidence. Spec is in good shape: falsifiable assertions with
concrete named signals, and the staged-probe design (distinct cookie/ls/expando values per jar)
made containment and class-independence provable. Strongest element: the Step 5 H6 assertion —
pre-recorded pre-wipe wcId + requiring BOTH its absence AND a stale-eval error gives two
independent "closed, not reloaded" signals, robust against the exact reload-in-place failure mode
the rewrite guards against; the `__bt_alive` expando as the inverse no-reload probe in steps 3/4
is the mirror-image idiom. Carry-forwards for future revisions:
1. **Step 5 tab-scope text is now stale.** The wipe closed BOTH personal tabs (staged wcId 4 AND
   boot wcId 2). The spec's parenthetical implies the boot tab survives ("the fresh-profile boot
   tab and/or the work tab ... keep other tabs open throughout") — but the boot tab is a
   personal-jar tab and WAS closed. Only the WORK tab is the true survivor. The verdict is
   unaffected (the core assertion targets wcId 4), but the text should be corrected — either
   assert the wipe closes ALL of the jar's tabs (stronger, matches observed behavior) or fix the
   parenthetical to name only the work tab. *(Applied this run — see spec update below.)*
2. **Step 6 error granularity.** Burner and unknown-id both reject via the identical `unknown-jar`
   error. Expected result only requires `{ok:false}` (PASS stands), but if the burner-vs-unknown
   distinction matters to the design, a future spec could assert distinct error codes so a
   regression that starts accepting burner wipes can't hide behind the unknown-jar path.
3. **Fixture URL drift.** Step 2 references "the fixture URL"; the run used https://example.com/.
   Harmless, but the spec should name the URL explicitly.

## Operator Notes

*(none — autonomous run under the M08 Flight 6 `hat-reverification` leg; carry-forward 1 applied
to the spec this run, carry-forwards 2–3 banked for a future revision.)*
