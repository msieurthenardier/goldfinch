# Behavior Test Run: jar-data-controls — 2026-07-10-22-10-41

**Spec**: [jar-data-controls.md](../../jar-data-controls.md)
**Status**: pass
**Started**: 2026-07-10T22:10:41Z
**Completed**: 2026-07-10T22:38:00Z (approx)
**Duration**: ~00:27
**Executor**: sub-agent (Sonnet)
**Validator**: sub-agent (Sonnet)

**Evidence (ephemeral, local-only, not committed)**: lived at
`/tmp/behavior-tests/goldfinch/jar-data-controls/2026-07-10-22-10-41/` (22 raw
JSON-RPC capture files) during the run; re-derivable by re-running the spec.

## Summary

7 / 7 checkpoints passed. 0 failed; 0 inconclusive. First run of this spec
(authored at M06 Flight 4 design); spec flips `draft` → `active`. Run gates M06 F4
Leg 4 (verify-integration) acceptance for DD2/DD3/DD4 (class-granular clears,
jar-scoped wipe, reload-after-wipe) and closes two declared carry-forwards: the
partition-less Burner rejection shape (`jar-delete-closes-tabs` run-1 Validator)
and direct burner storage isolation (`popup-jar-inheritance` Validator).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: baseline enumerate (boot tab wcId 2, personal); openTab
  (`jarId: work`, fixture URL) → wcId 3; in-tab evaluate staged all three probes
  (cookie `bt_probe=work1`, localStorage `bt_probe=work1`, expando
  `__bt_alive=1`) and read them back atomically.
- **Verdict**: pass — all three values read back; wcId 3 confirmed `jarId: work`
  by identity in the post-open enumeration.
- **Evidence**: `step-1-evaluate-stage-work.json`, `step-1-enumerateTabs-after.json`

### Checkpoint 2 — PASS
- **Actions taken**: openTab (`jarId: personal`) → wcId 4; staged cookie
  `bt_probe=personal1` + expando; read back.
- **Verdict**: pass — both values read back in a fresh personal-jar fixture tab.
  Incidental early isolation signal: the personal tab's same-origin cookie string
  showed only `personal1`, never the work jar's value.
- **Evidence**: `step-2-evaluate-stage-personal.json`, `step-2-enumerateTabs-after.json`

### Checkpoint 3 — PASS
- **Actions taken**: chrome-eval `jarsClearData({id:'work', classes:['cookies']})`;
  probes re-read on both tabs.
- **Raw state**: resolved `{ok:true, cleared:['cookies']}`; work tab cookie empty,
  localStorage STILL `work1` (class independence), `__bt_alive` still `1` (no
  reload); personal cookie intact (containment).
- **Verdict**: pass — all four clauses on the first capture; neither
  settle-recapture allowance needed.
- **Evidence**: `step-3-jarsClearData-work-cookies.json`,
  `step-3-work-tab-postclear.json`, `step-3-personal-tab-postclear.json`

### Checkpoint 4 — PASS
- **Actions taken**: `jarsClearData({id:'work', classes:['storage']})`; localStorage
  re-read.
- **Verdict**: pass — `{ok:true, cleared:['storage']}`; localStorage now `null`
  (was `work1` in the checkpoint-3 evidence — the before/after pair pins class
  independence); expando context read showed no reload from this clear either.
- **Evidence**: `step-4-jarsClearData-work-storage.json`, `step-4-work-tab-storage-read.json`

### Checkpoint 5 — PASS
- **Actions taken**: `jarsWipe({id:'personal'})`; ~2s settle; probes on wcId 4;
  enumerate.
- **Raw state**: `{ok:true}`; `__bt_alive` UNDEFINED (sentinel fired — the tab
  RELOADED via the `jar-wiped` broadcast sweep); cookie empty; wcId 4 unchanged in
  the enumeration with `jarId: personal` (reloaded in place); tab set exactly
  {2,3,4}.
- **Verdict**: pass — reload-in-place is the only interpretation consistent with
  all signals (same wcId + emptied cookie + stable tab count). The sibling
  personal tab (wcId 2) also survived with unchanged identity — consistent
  jar-wide in-place sweep (supplementary, not asserted).
- **Evidence**: `step-5-jarsWipe-personal.json`, `step-5-personal-tab-postwipe.json`,
  `step-5-enumerateTabs.json`

### Checkpoint 6 — PASS
- **Actions taken**: all four rejection combinations (wipe/clear × burner/unknown),
  then `jarsList()`.
- **Verdict**: pass — each resolved exactly `{ok:false}` (both channels reject the
  partition-less Burner identity and unknown ids); `jarsList()` still exactly
  Personal + Work by identity. Closes the Burner-identity-shape carry-forward.
- **Evidence**: `step-6-jarsWipe-burner.json`, `step-6-jarsClearData-burner.json`,
  `step-6-jarsWipe-unknown.json`, `step-6-jarsClearData-unknown.json`,
  `step-6-jarsList.json`

### Checkpoint 7 — PASS
- **Actions taken**: two burner tabs via chrome-eval
  `createTab(url, makeBurner())` → wcIds 5/6, jarIds `burner-329234146` /
  `burner-721722620`; cookie `bt_burner=A` set + read back in A; cookie read in B.
- **Verdict**: pass — both jarIds match `^burner-\d+$` and differ; A reads its
  cookie back; B's same-origin cookie string is empty. Direct storage-isolation
  pin, beyond partition naming — closes the popup-spec carry-forward.
- **Evidence**: `step-7-enumerateTabs.json`, `step-7-burnerA-set-cookie.json`,
  `step-7-burnerB-read-cookie.json`

## Orchestrator Notes

- Execution mode: **live two-agent continuation** (SendMessage available); both
  agents persisted across all 7 checkpoints. Sonnet both roles per crew file.
- Cache mode `cold` — fresh scratch profile + fresh launch staged by the
  Orchestrator (per-run staging ruling); torn down after the run (Electron main
  pid killed directly; port verified dead).
- Port: free-port fallback 49709 again (fifth consecutive observation on this
  rig). The launch log does not print the bound port — the `ss -tlnp` discovery
  step remains required.
- Admin key handled per protocol: read from the launch log by the agents, used
  only in Authorization headers, never written to any evidence file or artifact.
- No operator interventions; no failing checkpoints; no re-spawns; both
  settle-recapture allowances went unused.

## Closing Summaries

### Executor closing

Clean end-to-end: 21 apparatus calls, all first-attempt; one MCP session survived
the whole ~25-minute run; heredoc + dual-Accept protocol exactly as documented.
Operational notes: (1) step-6 rejections are bare `{ok:false}` — no reason field
to distinguish burner-rejection from unknown-id-rejection; (2) `createTab`'s
return value exposes the full container record (partition string included) —
richer than `enumerateTabs` for partition-level assertions; (3) the
`x === undefined ? 'UNDEFINED' : x` sentinel was load-bearing in steps 3-5 to
distinguish "expando gone" from falsy — future specs should prescribe it;
(4) the wipe sweep reloaded the whole jar in place (the unprobed sibling tab kept
its wcId too). Final tab census exactly baseline + the four spec-opened tabs.

### Validator closing

7/7 PASS, every verdict from directly read evidence. Strong spec instrumentation:
the in-memory expando converts the invisible broadcast sweep into a one-bit
in-page read, and steps 3/4 double-use it so the NEGATIVE case (granular clears
don't reload) is as well-pinned as the positive; identity-based assertions
throughout; same-origin staging makes isolation claims direct string comparisons;
`cleared:[...]` echo lets verdicts pin which class was acted on. Carry-forwards
for future revisions: (1) step 3's cross-jar containment is cookie-only — the
personal tab's localStorage/expando could be re-checked at the same cost;
(2) the two rejection causes are observationally identical (bare `{ok:false}`) —
a `reason` field would make regressions between the paths detectable; (3) burner
isolation is A→B one-directional; (4) no teardown/cleanup convention in the spec;
(5) step 5's `~1-2s` settle is prose — a poll-until condition would remove the
timing-assumption class. Both declared carry-forwards from prior runs are now
closed by checkpoints 6 and 7.

## Operator Notes

*(none — autonomous run under the flight's verify-integration leg; the flight
debrief carries the spec-revision items)*
