# Behavior Test Run: omnibox-suggestions — 2026-07-13-00-39-35

**Spec**: [omnibox-suggestions.md](../../omnibox-suggestions.md)
**Status**: pass
**Started**: 2026-07-13T00:39:35Z
**Completed**: 2026-07-13T01:35:00Z (approx)
**Duration**: ~00:55 (incl. two spec-premise fix cycles)
**Executor**: live Sonnet sub-agent (SendMessage continuation)
**Validator**: live Sonnet sub-agent (SendMessage continuation)
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/omnibox-suggestions/2026-07-13-00-39-35/` (54 files); re-derivable by re-running the spec.

## Summary

**7 / 7 checkpoints passed** (first run of this spec). Two checkpoints
passed on rerun after the run itself surfaced **spec-premise defects** —
both fixed in the spec mid-run and folded in permanently:
1. **Seed-vs-retention collision** (step 1, initial FAIL): the 120-day
   seed spread collided with the 30-day default retention — the app's
   startup prune (working as designed) deleted 75% of the fixture.
   Rerun with a ≤29-day spread survived intact (prune deleted 0/50,020).
2. **Unresolvable selection targets** (step 4, exception): fictional
   `.test` hosts proved navigation DISPATCH (the app log shows `loadURL`
   with exactly the selected URL) but could never prove arrival. Rerun
   staged resolvable local fixture pages through the REAL recording
   pipeline — which incidentally re-verified live recording end-to-end.

## Checkpoint Results

### Checkpoint 1 — PASS (rerun; initial FAIL was a spec-premise defect)
50,000 rows seeded into the resolved default jar (`rename-test`) + 20
`zebrafinch-marker.test` rows into `work`, all within 29 days; post-launch
counts verified intact by BOTH agents independently (prune deleted
nothing). Admin client connected; sheet wcId lazily deferred (the sheet is
a lazy singleton — probed successfully at step 2 as wcId 3).

### Checkpoint 2 — PASS
Typed `spar` → 6 suggestion rows rendered on the sheet flush under the
address bar, all from seeded default-jar hosts. **Latency: 114 ms** from
last keystroke to rendered rows — ~100 ms of which is the intentional
input debounce (render landed within ~14 ms of debounce expiry). Judged
felt-instant at 50k-row scale. (A first take incidentally proved the
"No matches" empty state: click-into-populated-bar appends rather than
selecting-all — see Observations.)

### Checkpoint 3 — PASS
Jar exclusivity: typed `zebraf` on a default-jar tab → actively rendered
"No matches" panel, zero rows — while the Validator independently proved
all 20 marker rows exist in `work`. Absence-by-jar-scoping, not
absence-of-data.

### Checkpoint 4 — PASS (rerun on resolvable fixtures)
Stage 1 (preserved): Enter on a selected `.test` row dispatched `loadURL`
with the exact selected URL (ERR_NAME_NOT_RESOLVED — dispatch proven).
Stage 2: three local fixture pages visited via the live recorder; typed
`localf` → stable rows; ArrowDown ×4 → "Localfinch Gamma" highlighted
(aria-selected + pixels); Enter → the tab ARRIVED at
`http://127.0.0.1:8000/gamma/` (enumerate + tab DOM + address bar);
dropdown gone in pixels.

### Checkpoint 5 — PASS
Pointer: clicked "Localfinch Beta" directly on the sheet at its own DOM
rect center → tab arrived at `/beta/` exactly; dropdown gone. (Click
geometry independently re-verified by the Validator.)

### Checkpoint 6 — PASS
Burner gate: on an active burner tab, typed the same `localf` prefix that
yields 6 rows on jar tabs → NO suggestion surface at all across 700 ms
(no panel, no empty state — the gate is UPSTREAM of the query), DOM and
pixels agreeing (hidden class set on this path).

### Checkpoint 7 — PASS
Escape closed the dropdown (pixels) with text `localf` and focus intact —
proven behaviorally: continued typing landed with NO re-click, producing
`localfinch`, which RE-OPENED the dropdown narrowed 6→3 (only the
Localfinch fixture rows; the google-search pollution rows dropped out —
narrowing by count AND content). The non-focusing regime held for the
entire run: the sheet never stole focus once.

## Orchestrator Notes

- Live two-agent mode (SendMessage), cache-cold, both crew on Sonnet.
- Operator decisions: step-1 FAIL → rerun with retention-safe seed +
  permanent spec amendment; step-4 exception → rerun on live-recorded
  resolvable fixtures + permanent spec amendment. No other interventions.
- Teardown confirmed: app quit (appQuit signature), fixture server killed
  (incl. a surviving python child a first kill missed), ports 8000/49707
  free, nothing left running.

## Closing Summaries

### Executor closing
Anomalies (all reported in-step, raw): jar-registry drift (defaultId
`rename-test`); the two spec-premise defects above; click-into-populated
address bar does NOT select-all (typed text appends — cost one take;
possible UX gap for the debrief); the sheet DOM retains stale rows after
Enter/click closes (view-layer detach, hidden class NOT set) — naive
rows>0 polling reads phantoms; guard with content-aware two-reads-200ms
stability polling; on the burner path the hidden class IS set. Enter with
no selection = search fallthrough navigation, which the live recorder
records (polluted `localf` history — then usefully sharpened step 7's
narrowing probe). Apparatus: 50k seeding via the store API is ~6–9 s (WAL,
no transaction needed); the sheet is lazily created; it accepts clicks at
sheet-local coordinates from its own DOM rects; latency polling overhead
~5–10 ms/read.

### Validator closing
7/7 with every PASS grounded in inspected pixels + DOM cross-checks +
independent sqlite reads; executor reports accurate at every independent
re-measurement. Spec amendments to keep permanently: retention-safe seed
windows (general authoring rule: seeded-precondition specs must check the
system's own data-lifecycle policies for launch-time mutations);
resolvable selection targets via the live recorder (strictly better — it
also verifies the recording pipeline). Patterns worth AUTHORING.md-level
guidance: the zero-row evidence bar (typed-prefix proof + actively
rendered state + independent proof the data exists in the excluded jar);
pixels-over-DOM (the sheet has TWO closed states — hidden-class when
never shown vs view-detached with stale rows after selection; dropdown
visibility is pixel-judged only). Debrief observations (not judged):
click-to-focus doesn't select-all (product ruling needed);
search-fallthrough navigations are recorded into history (intended?);
frecency re-scoring from live visits observed. The 1–2 char latency
variant was not exercised (store-level probe covered it: ~4 ms median).

## Operator Notes

*(autonomous run — none)*
