# Behavior Test Run: vault-filter — 2026-09-23-17-09-28

**Spec**: [vault-filter.md](../../vault-filter.md)
**Status**: pass
**Started**: 2026-09-23T17:09:28Z
**Completed**: 2026-09-23T20:40:52Z
**Duration**: 03:31:24 (includes operator pauses at checkpoints 7 and 11)
**Executor**: sub-agent a861915d1630da0a8
**Validator**: sub-agent a79ffc61a2373f238
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/vault-filter/2026-09-23-17-09-28/` during the run; re-derivable by re-running the spec.

## Summary

10 / 10 checkpoints passed (steps 2–11; step 1 is a setup row folded into checkpoint 2). 0 failed; 0 inconclusive after resolution. Checkpoint 7 was judged INCONCLUSIVE under the apparatus-focus escalation rule and resolved to PASS by operator by-eye confirmation.

## Checkpoint Results

### Checkpoint 2 — PASS
- **Actions taken**:
  - Setup step 1: opened a fresh `goldfinch://vault` tab (wcId 9) via `createTab("goldfinch://vault", null, { trusted: true })` on the chrome wcId, because `openVaultPage` isn't on the evaluate seam.
  - Inventoried the pre-existing items:
    - Personal: 5 logins, 3 cards, and identity "My details" (fullName "Candy Cane");
    - Global, Work, and test: empty;
    - Personal has the access key `nVVhaBSFAAE`.
    - No pre-existing non-secret field matched the step keywords.
  - Seeded through the page editor: Global login "Filter Alpha" and card "Filter Card"; Personal login "Beta Mail" and note "Filter Recipe". "Home" was deferred (see checkpoint 3).
  - Step 2: read the page.
- **Raw state**:
  - `#vault-filter` is `type=text` and empty, with AX name "Filter items" and role `textbox`.
  - `#vault-filter-clear` carries the `hidden` attribute.
  - The status is empty with role `status`.
  - All 13 rows are displayed.
  - No "search" appears anywhere.
- **Expected**: The "Vaults" section holds `#vault-filter`, which is empty. `#vault-filter-clear` is hidden (its `hidden` attribute) or absent. Every seeded item row is displayed. The status line is empty. [a11y] The field's accessible name contains "Filter", and neither the field nor the status uses a "search" name or role.
- **Verdict**: pass. All clauses are corroborated by the screenshot and the AX tree.
- **Evidence**: `step-1-screenshot.png`, `step-1-2-inventory-and-baseline.json`, `step-2-screenshot.png`, `step-2-axtree.json`, `step-2-dom-evals.json`

### Checkpoint 3 — PASS
- **Actions taken**:
  - Setup deviation: seeded identity "Home" (fullName Pat Example, email zq7marker@example.com) in the **Work** jar vault.
  - Real click on `#vault-filter`, then typed `filter`.
- **Raw state**:
  - Exactly Filter Alpha, Filter Card (Global), and Filter Recipe (Personal) are displayed.
  - Beta Mail, My details, and Home are `display:none` + `vault-filter-out`.
  - Personal's Logins, Cards, and Identity subsections are hidden; the Work and test sections are hidden.
  - The status reads "3 items match", the clear button is visible, `activeElement` is `vault-filter`, and `hasFocus()` is true.
  - A focus ring is visible.
- **Expected**: Displayed rows are exactly "Filter Alpha", "Filter Card", and "Filter Recipe". "Beta Mail" and "Home" are NOT displayed, and the jar vault's Logins and Identity subsections are NOT displayed. The status reads exactly "3 items match". `#vault-filter-clear` is displayed. Focus is still on `#vault-filter`.
- **Verdict**: pass.
- **Evidence**: `step-3-screenshot.png`, `step-3-axtree.json`, `step-3-dom-evals.json`

### Checkpoint 4 — PASS
- **Actions taken**: Ctrl+A, then typed `pat example`.
- **Raw state**: Only Filter Card (cardholder) and Home (fullName) are displayed. The status reads "2 items match". `hasFocus()` is false (not asserted by this row).
- **Expected**: Only "Filter Card" (cardholder) and "Home" (fullName) are displayed. The match is case-insensitive. The status reads exactly "2 items match".
- **Verdict**: pass. The lowercase query matched "Pat Example".
- **Evidence**: `step-4-screenshot.png`, `step-4-axtree.json`, `step-4-dom-evals.json`

### Checkpoint 5 — PASS
- **Actions taken**: Re-clicked the field, then Ctrl+A and typed `mail.example`.
- **Raw state**:
  - Only Beta Mail (origin) is displayed.
  - `section#vault-global` is `display:none` + `vault-filter-out`.
  - The Personal section is displayed, and its `.vault-accesskeys` is displayed with no `vault-filter-out` and the row `nVVhaBSFAAE` visible (scrolled screenshot).
  - The status reads "1 item matches".
- **Expected**: Only "Beta Mail" (origin) is displayed. The Global vault's section is NOT displayed. The jar vault section is displayed, and its Access-keys subsection is still displayed alongside it.
- **Verdict**: pass.
- **Evidence**: `step-5-screenshot.png`, `step-5-screenshot-accesskeys-scrolled.png`, `step-5-axtree.json`, `step-5-dom-evals.json`

### Checkpoint 6 — PASS
- **Actions taken**: Ctrl+A, then typed `zq7marker`.
- **Raw state**:
  - All 14 rows, all 20 type subsections, and all 4 vault sections are `display:none` + `vault-filter-out`.
  - Personal's `.vault-accesskeys` has its own `display:block` with no filter class, but isn't rendered because its ancestor section is hidden.
  - The status reads "No items match" in words (role `status`, live polite, atomic).
  - Every title, "Access keys", and `nVVhaBSFAAE` are absent from the AX tree.
  - `scrollHeight` equals `innerHeight`.
- **Expected**: Every item row and every vault section is NOT displayed. The jar vault's Access-keys subsection is also not displayed, because it collapses with its owning section (FD ruling). The status reads exactly "No items match", and the page says so in words. No marker-carrying row is displayed. [a11y] The status has role `status`, and every not-displayed element is absent from the AX tree.
- **Verdict**: pass. The secret-only marker produced zero matches.
- **Evidence**: `step-6-screenshot.png`, `step-6-axtree.json`, `step-6-dom-evals.json`

### Checkpoint 7 — PASS (operator-resolved; Validator verdict INCONCLUSIVE)
- **Actions taken**: A real click on `#vault-filter-clear`.
- **Raw state**:
  - The field is empty, `.vault-filter-out` count is 0, and every row, subsection, and section is displayed, including Personal's Access keys.
  - The status is empty and the clear button has `hidden` = true.
  - `activeElement` is `vault-filter` and the AX node has `focused:true`, but `document.hasFocus()` is false and the screenshot shows no focus ring.
  - This is `[BLOCKED:apparatus-focus]`.
- **Expected**: The field is empty, and everything is displayed again with nothing carrying `vault-filter-out`. The status is empty. The clear button is hidden. Focus is on `#vault-filter`.
- **Verdict**: inconclusive. Every non-focus clause was satisfied. The focus clause was escalated under the apparatus-focus rule ("verify by eye").
- **Operator decision**: after giving the Goldfinch window OS focus (clicking its title/tab bar, not the page), the operator confirmed **"yes, filter has focus ring"**. The checkpoint is recorded as PASS.
- **Evidence**: `step-7-screenshot.png`, `step-7-axtree.json`, `step-7-dom-evals.json`

### Checkpoint 8 — PASS
- **Actions taken**:
  - Clicked the field and found stray text "me" already in it (most likely from the operator's by-eye check). Ctrl+A, then typed `filter recipe`.
  - Captured the pre-click state.
  - Real click on `#vault-nav a[href="#vault-global"]`, then waited 1.2 s.
- **Raw state**:
  - Before the click: `section#vault-global` is `display:none` + `vault-filter-out`, and the status reads "1 item matches".
  - After the click: the field is empty, the `.vault-filter-out` count is 0, all rows and sections are displayed, and the status is empty.
  - `section#vault-global` top is 23.5 px, `location.hash` is `#vault-global`, and the Global nav link has `aria-current="true"`.
- **Expected**: Before the jump, the Global section is not displayed. After the click, the filter is cleared and the page has scrolled to the Global vault section.
- **Verdict**: pass.
- **Evidence**: `step-8-pre-nav-screenshot.png`, `step-8-post-nav-screenshot.png`, `step-8-axtree.json`, `step-8-dom-evals.json`

### Checkpoint 9 — PASS
- **Actions taken**:
  - Tagged the `#vault-filter` node with `data-executor-tag`, then typed `alpha` (status "1 item matches").
  - Opened Edit on Filter Alpha, changed nothing, and clicked Save. Waited 1.5 s.
- **Raw state**:
  - The tagged node is gone and a new `#vault-filter` exists, which proves the page re-rendered.
  - The field is empty, the status is empty, the clear button is hidden, and the `.vault-filter-out` count is 0.
  - All 14 rows are displayed. Filter Alpha's fields are unchanged.
- **Expected**: After the refresh, `#vault-filter` is empty, every item row is displayed, and the status is empty. The field and the page agree.
- **Verdict**: pass.
- **Evidence**: `step-9-pre-save-screenshot.png`, `step-9-screenshot.png`, `step-9-axtree.json`, `step-9-dom-evals.json`

### Checkpoint 10 — PASS
- **Actions taken**: Typed `alpha`, then a real click on "Lock now". Waited 1.5 s.
- **Raw state**:
  - `#vault-root` data-mode is `locked`.
  - The locked banner is shown, and each vault section shows its own unlock prompt.
  - `getElementById` returns null for `vault-filter`, `vault-filter-clear`, and `vault-filter-status`.
  - `[class*="vault-filter"]` matches 0 elements, and there is no "Filter items" in the AX tree.
- **Expected**: The page shows the locked view. `#vault-filter` is not present anywhere in the DOM.
- **Verdict**: pass.
- **Evidence**: `step-10-screenshot.png`, `step-10-axtree.json`, `step-10-dom-evals.json`

### Checkpoint 11 — PASS
- **Actions taken**: The operator unlocked through the vault-unlock sheet (attested). The Executor re-read the page without typing.
- **Raw state**:
  - data-mode is `unlocked`, and `#vault-filter` is present and empty (the pre-lock "alpha" did not survive).
  - The status is empty, the clear button is hidden, and the `.vault-filter-out` count is 0.
  - 14 of 14 rows are displayed.
- **Expected**: Unlocked view. `#vault-filter` is present and empty, every item row is displayed, and the status is empty. No query survived the lock.
- **Verdict**: pass.
- **Evidence**: `step-11-screenshot.png`, `step-11-axtree.json`, `step-11-dom-evals.json`

## Orchestrator Notes

- **Mode**: live two-agent (`SendMessage` continuation available). Cache mode was `cold`: a fresh vault tab was opened at step 1.
- **Apparatus**: an attach-only client (`scripts/lib/mcp-client.mjs` via a run-local `gf.mjs` wrapper) against a dev instance relaunched with `GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1`, using a freshly minted admin key.
  - The MCP bound **49708**, because a stale earlier dev instance held 49707. That instance was closed once found.
  - No session-registered `mcp__goldfinch*` tools were used (crew rule).
- **Spec deviation: identity placement.** Personal already held its single identity profile ("My details"), and the app enforces one identity per vault. Rather than modify operator data, "Home" was seeded in the empty **Work** jar vault. Every affected Expected Result names items individually, so no verdict depended on co-location. Personal remained "the jar vault" with the access key.
- **Checkpoint 7 escalation**: the Validator correctly rendered INCONCLUSIVE under the apparatus-focus rule. The Orchestrator paused, and the operator confirmed a visible focus ring after giving the window OS focus through its title/tab bar. Recorded as PASS on operator attestation.
- **Operator/automation interleaving**: stray "me" text appeared in the field before checkpoint 8, most likely from the by-eye check. The Executor verified and overwrote the field before acting, and it had no effect on any verdict.
- **Incidental finding (not in scope)**: the locked vault page still says "Unlock the manager to view and edit items." / "Unlock the manager to view this vault's items." / "Automatically lock the manager…". Squawk 0102's rename pass missed these. It is logged as a follow-up squawk.
- No Accessibility Validator was spawned. The `[a11y]` rows were judged by the base Validator from the AX tree; screen-reader speech is an alignment-leg spot check.

## Closing Summaries

### Executor closing
- `document.hasFocus()` false readings extend to internal guest pages (wcId 9 here), not just the chrome document. It read true at step 3 and false from step 4 on, even after a real click and after the operator focused the window. `activeElement` and the AX `focused` property stayed consistent, but the rendered ring was genuinely absent at step 7. This condition is page-scoped, not chrome-scoped only.
- The tag-the-node technique (set a throwaway data attribute, act, check whether it survives) is a cheap, decisive proof that a page re-rendered rather than mutated in place. Recommend naming it in the crew notes.
- Seeding through the page editor works well as a hybrid: open and Save via `.click()`, fill fields via `.value` plus input/change events. Real `click`/`typeText` stays reserved for the surface under test.
- Singleton-constrained item types (one identity per vault) can invalidate a spec's seed data. Inventory all pre-existing items before seeding.
- Operator and automation share one live window. Verify field state before trusting it.

### Validator closing
- 10/10 checkpoints pass. Checkpoint 7 was INCONCLUSIVE, escalated, then resolved to PASS by the operator. No system defects found: matching, case-insensitivity, subsection/section collapse (including the Access-keys FD-ruling nuance), status copy, the clear-button lifecycle, nav-clear with scroll, the refresh reset, and the lock reset all behaved per spec.
- Recommend the spec's Preconditions call out the one-identity-per-vault constraint and allow "Home" in any jar vault with free identity capacity.
- The step-7 focus clause is a recurring rig cost on WSLg, not a spec defect. Budget for the escalation.
- The mint-via-step-up-sheet contingency didn't trigger, because Personal already had a key. A fresh profile would exercise it.
- Promote the tagged-node refresh check into the crew notes.

## Operator Notes

- Checkpoint 7: operator confirmed "yes, filter has focus ring".
- Checkpoint 11: operator unlocked (attested "unlocked").
