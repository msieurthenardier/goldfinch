# Behavior Test Run: core-browsing-shields — 2026-07-08-21-57-55

**Spec**: [tests/behavior/core-browsing-shields.md](../../core-browsing-shields.md)
**Status**: pass
**Started**: 2026-07-09T02:57:55Z
**Completed**: 2026-07-09T03:02:00Z
**Executor**: F06 SC3 behavior-test Executor (Witnessed self-run — admin MCP client + evidence-judged)
**Validator**: (self, artifact-judged — every verdict cites a raw machine read: readDom `outerHTML`, guest `url`, enumerateTabs, screenshots)

## Summary
All 6 steps PASS on the landed F5 build (branch `flight/06-parity-land-and-release`). Core browsing survived: the
app launched cleanly on the new Electron, a tab navigates + renders a real page, the Shields `webRequest`
pipeline **blocks** a known tracker (`google-analytics.com`) and **strips** tracking params (`utm_source` gone,
`q=keep` kept), and multi-tab works. Apparatus: admin MCP client over `127.0.0.1:8899`, app via
`npm run dev:automation` (`GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1`).

**Apparatus notes (deviations from the spec letter, non-material):**
- **Fixture port** — port 8080 was occupied in this venue; served the fixture on **8081** instead
  (`python3 -m http.server 8081` from `tests/behavior/fixtures/core-browsing-shields/`). Port distinct from the
  MCP port (8899), as the spec requires.
- **devicePixelRatio = 1 here** (not the 1.25 the WSLg note anticipates): the app launched on the Wayland ozone
  backend and `captureWindow()` returns a 1400×900 frame == CSS pixels, so coordinate clicks used CSS coords 1:1.
- **Precondition fix (not a spec failure):** on first entry the dev profile's `shields.json` had `strip:false`
  (left over from a prior settings-controls run). The spec requires Shields at defaults (all-on), so `strip` was
  restored to `true` via the chrome `window.goldfinch.shieldsSet({strip:true})` bridge before Step 4; verified
  `shieldsGet()` == all-on and `shields.json` `strip:true`. Restored to the pre-run file at teardown.

## Step Results

### Step 1 — PASS (active precondition probe)
- `tools/list` includes `getChromeTarget` (present in the 27-tool set). `getChromeTarget()` →
  `{wcId:1, kind:"chrome", url:"file:///.../src/renderer/index.html"}` — numeric chrome wcId=1.
  Fixture `curl http://127.0.0.1:8081/` → HTTP 200.

### Step 2 — PASS (launch smoke)
- `getChromeTarget()` up (chrome wcId=1); `enumerateTabs()` listed ≥1 guest (`wcId:2 https://www.google.com/`).
  App started clean on the new Electron — no white screen / crash.

### Step 3 — PASS (navigation + render)
- `openTab('https://example.com/')` → numeric guest wcId=3; `readDom(3).html` contains "Example Domain";
  `readDom(3).url === https://example.com/`. Core nav + render intact.

### Step 4 — PASS (tracking-param strip)
- Opened `http://127.0.0.1:8081/?utm_source=test&q=keep` (fresh guest wcId=4). Guest **`url`** after load was
  `http://127.0.0.1:8081/?q=keep` — `utm_source` **stripped**, `q=keep` **preserved** (top-level nav strip
  redirect via `session.webRequest` → `redirectURL`). Fixture rendered (marker `core-shields-fixture-loaded`
  in html).

### Step 5 — PASS (tracker block — primary blocked-specific witness)
- Opened the chrome privacy panel by `click(1, 1249, 63)` on `#toggle-privacy` (aria-expanded flipped
  false→true). `readDom(1).html` contains the blocked row:
  `<div class="ps-item status"><span class="tag blk">blocked</span><span class="dom struck">google-analytics.com</span></div>`
  under `analytics (1)` / `1 blocked · 0 allowed`. The toggle's own `aria-label="Shields, 1 blocked"` corroborates.
  Secondary check: `#privacy-count` badge reads `1` (not hidden). The `onBeforeRequest` block path survived the
  Chromium bump.
- Evidence: `panel-open.png`, `step5c.json`.

### Step 6 — PASS (multi-tab)
- `openTab('https://example.org/')` → guest wcId=5; polled `enumerateTabs()` → 4 distinct guest tabs [2,3,4,5];
  `activateTab(2)` switched to the google tab without error (active tab confirmed wcId=2). No crash; session
  wiring intact.

## Evidence
`/tmp/behavior-tests/goldfinch/core-browsing-shields/2026-07-08-21-57-55/` — `step3-4.json`, `step4.json`,
`step5c.json`, `before-panel.png`, `panel-open.png`.

## Orchestrator Notes
**Witnessed-pattern deviation**: single-agent Executor+Validator self-run (not the two-live-agent crew).
Compensating control: every verdict cites a raw machine read (guest `url`, readDom `outerHTML`, enumerateTabs,
screenshots), not model judgment. No regressions. The `strip:false` starting state was an inherited fixture
condition, not a build defect — corrected to defaults per the spec's precondition and restored at teardown.
