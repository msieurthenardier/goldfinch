# Behavior Test Run: core-browsing-shields — 2026-06-05-17-43-36

**Spec**: [tests/behavior/core-browsing-shields.md](../../core-browsing-shields.md)
**Status**: pass
**Started**: 2026-06-05T17:43:36Z
**Completed**: 2026-06-05T17:48Z
**Executor**: abbbf1ce067cb0f7e
**Validator**: a725b2700f66830de

**Evidence (ephemeral, local-only, not committed)**: `/tmp/behavior-tests/goldfinch/core-browsing-shields/2026-06-05-17-43-36/` (A1–A5 target dumps, `A4-privacy-body-full.txt`, `renderer-screenshot.png`). Re-derivable by re-running.

## Summary

5 / 5 steps passed. Verifies core browsing + the Shields privacy pipeline still work on the upgraded **Electron 42.3.3**.

## Step Results

### Step A1 — PASS (launch smoke)
- Renderer `page` (Goldfinch) + a `webview` target exist; `createTab` is a live function. App launched cleanly on E42.
- Evidence: `A1-targets.json`

### Step A2 — PASS (navigation)
- `createTab('https://example.com/')` → webview url `https://example.com/`, title "Example Domain", body "Example Domain…". Navigation + render survived the Chromium bump.
- Evidence: `A2-targets.json`

### Step A3 — PASS (tracking-param strip)
- Loading `…:8090/?utm_source=test&q=keep` → resulting webview URL = `http://127.0.0.1:8090/?q=keep` (utm_source stripped, q=keep preserved), corroborated by the address bar. The top-level `session.webRequest` strip redirect works on E42.
- Evidence: `A3-targets.json`

### Step A4 — PASS (tracker block)
- The fixture's `google-analytics.com` request is cancelled by Shields: privacy panel shows `.tag.blk` = 1, "Shield (1)", and lists "ANALYTICS (1) / BLOCKED / google-analytics.com". The `onBeforeRequest` block pipeline survived the upgrade.
- Evidence: `A4-privacy-body-full.txt`

### Step A5 — PASS (multi-tab)
- Opening `https://example.org/` yields 4 distinct webview targets; no crash. Multi-tab + session wiring intact.
- Evidence: `A5-targets.json`, `renderer-screenshot.png`

## Orchestrator Notes
- Mode: consolidated single-pass Witnessed (independent Executor `abbb…` drove via raw CDP at `:9222`; independent Validator `a725…` judged). Act/judge separation preserved.
- Apparatus: raw CDP over Node WebSocket against the running E42 app (chrome-devtools MCP would launch its own browser). Param-strip observed on the webview URL (not the privacy aggregate `stripped` count, which is 0 for mainFrame by design); tracker-block read from the privacy panel DOM (opened via `#toggle-privacy`).
- This run accompanies a `tab-scheme-guard` re-run (same timestamp) confirming F1 still holds on E42.

## Closing Summaries
### Executor closing
Drove the live E42 app via raw CDP; all five steps produced clean observations. Shields block + strip both confirmed working post-upgrade.
### Validator closing
All five steps match expected. The Electron 33→42 upgrade did not regress core browsing or the Shields privacy enforcement — both the param-strip redirect and the tracker-block cancellation are live on the new Chromium.

## Operator Notes
First run of this spec; **promoted `draft → active`** on this all-pass result. It is now the runtime regression gate for the Shields pipeline.
