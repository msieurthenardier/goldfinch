# Behavior Test Run: farbling-correctness — 2026-06-17-17-23-30

**Spec**: [tests/behavior/farbling-correctness.md](../farbling-correctness.md)
**Status**: pass
**Started**: 2026-06-17T17:22:00
**Completed**: 2026-06-17T17:24:00
**Duration**: ~00:02:00
**Executor**: FD-driven (machine-read evidence; mission behavior-test standard — no Witnessed crew elected)
**Validator**: FD-driven

## Summary
All assertions pass on the migrated (Flight-9) eval-tool apparatus — `evaluate` over the loopback MCP surface in the **guest main world**, no `:9222`, no `chrome-devtools` MCP. Launched `dev:automation` on `:52351` (`:9222` absent — the F9 production posture), admin key from `AUTOMATION_DEV_MINT`. navigator spoof = 8, canvas noise applied + stable within session, and seed-dependence confirmed via the New-Identity reroll (the spec's documented Variant for Step 6, used because no second jar exists and there is no MCP createJar tool).

## Step Results

### Step 1 — PASS
- **Actions taken**: authenticated `initialize` + `tools/list` over `127.0.0.1:52351/mcp` (Bearer); `curl` fixture.
- **Raw state**: `tools/list` includes `evaluate` (21 tools); fixture `http://127.0.0.1:8055/` → HTTP 200.
- **Verdict**: pass — authenticated handshake + `evaluate` present + fixture reachable.

### Step 2 — PASS
- **Actions taken**: `openTab('http://127.0.0.1:8055/')` (default jar) → guest **G = wcId 3**; waited for load; `enumerateTabs` confirmed the guest.
- **Verdict**: pass — fixture rendered in the jar's guest tab; G acquired.

### Step 3 — PASS (navigator spoof)
- **Raw state**: `evaluate(3,'navigator.hardwareConcurrency')` → **8**; `evaluate(3,'navigator.deviceMemory')` → **8**.
- **Verdict**: pass — both spoofed to the constant 8 (`webview-preload.js:325,330`), masking the real machine.

### Step 4 — PASS (canvas noise stable within session)
- **Raw state**: a single self-contained draw expression (gradient rect + text) returned `toDataURL()` as **A** (len 17718); the identical expression again returned **A2**; `A === A2` → **true**.
- **Verdict**: pass — farbled output stable within the session (deterministic per-`(seed,index)` hash).

### Step 5 — PASS (noise applied, non-trivial)
- **Raw state**: `getImageData(0,0,4,1)` sample = `[254,0,0,255, 253,0,2,255, 252,0,3,255, 251,0,5,255]`. The clean gradient at x=0 is exactly `(255,0,0,255)`; the observed first pixel is `(254,0,0,255)` — perturbed by −1 (the `getImageData` hook noises ~1/8 of pixels by ±1, `webview-preload.js:247-257`).
- **Verdict**: pass — farbling is applied, not pass-through.

### Step 6 — PASS (seed-dependence; New-Identity reroll Variant)
- **Actions taken**: no second jar exists (only `default`; no MCP createJar), so used the spec's New-Identity Variant — `evaluate(chromeWc 1, 'typeof newIdentity')` → `function`; `evaluate(chromeWc, 'newIdentity()')` (reroll + guest reload, `renderer.js:1750/1756`); waited 3s; re-acquired the fixture guest (still wcId 3); re-ran the identical draw → **B**.
- **Raw state**: `B !== A` → **true**; `navigator.hardwareConcurrency` in the rerolled guest → **8** (unchanged).
- **Verdict**: pass — the identical draw produces different canvas noise after a seed reroll (noise is keyed to the per-jar/session seed), while the navigator spoof stays a constant. Seed-dependence proven.

### Step 7 — SKIPPED (optional control)
- farble-off-per-origin not driven (optional; the Step 3-6 differences already attribute the behavior to farbling).

## Orchestrator Notes
FD-driven via a throwaway `node` driver over `scripts/lib/mcp-client.mjs` (admin key; admin reaches the jar guest). Seed-dependence used the New-Identity reroll rather than two-container because Goldfinch ships only the `default` jar and exposes no MCP jar-creation tool — the spec lists New Identity as the documented Variant for exactly this. The eval tool (F9 legs 1, 4) is dogfooded here on a real privacy-correctness assertion: **PASS**. Recommend promoting the spec `draft → active` is an operator call (its note defers promotion to the next Electron upgrade); left `draft`.

## Closing Summaries
### Executor closing
The migrated apparatus works cleanly — `evaluate` in the guest main world reads the farbled `navigator`/canvas surfaces exactly as the old CDP `:9222` path did, with no port and no external MCP browser.
### Validator closing
Every load-bearing assertion (spoof constant, stability, applied noise, seed-dependence) holds. The migration off `:9222` did not weaken the test.

## Operator Notes
_(pending)_
