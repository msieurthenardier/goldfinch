# Behavior Test Run: farbling-correctness — 2026-07-08-18-27-53

**Spec**: [tests/behavior/farbling-correctness.md](../../farbling-correctness.md)
**Status**: pass
**Started**: 2026-07-08T18:27:00-05:00
**Completed**: 2026-07-08T18:29:00-05:00
**Duration**: ~00:02:00
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed)

## Summary
6 / 6 core steps passed (steps 1–6). Step 7 (control / farble-off) not driven — optional, and the real-machine control below already isolates farbling as the cause. 0 failed; 0 inconclusive.

Apparatus: Goldfinch MCP `evaluate` tool in the guest main world (zero CDP). Fixture served over HTTP at `http://127.0.0.1:8811/`. Two guests: **G = wcId 2** (default jar, read with the **jar key**), **G2 = wcId 5** (work jar, read with the **admin key** — the dev recipe mints only the one default jar key, so the second seed is reached via admin, exactly as the spec's precondition allows). Farble is ON (global default; confirmed in the Settings page: "Farble fingerprint" toggle on).

**Machine control:** real `nproc = 20`, but `navigator.hardwareConcurrency` reports `8` in both guests — the reported value is a spoofed constant, not the machine's real core count. This is the control the optional Step 7 would provide for navigator, obtained for free.

## Step Results

### Step 1 — PASS
- **Actions taken**: Connected the MCP client (authenticated); `initialize`; `tools/list`; `curl` fixture.
- **Raw state**: authenticated handshake ok; `tools/list` includes `evaluate` (present, among 27 tools). Fixture HTTP 200.
- **Verdict**: pass.

### Step 2 — PASS (setup)
- **Actions taken**: Navigated a default-jar tab (wcId 2) and a work-jar tab (wcId 5) to the fixture; `enumerateTabs` recorded guest wcIds; confirmed Farble active (Settings default ON).
- **Raw state**: both tabs on `http://127.0.0.1:8811/`; G=2 (default), G2=5 (work).
- **Verdict**: pass.

### Step 3 — PASS
- **Actions taken**: `evaluate(2, 'navigator.hardwareConcurrency')` and `evaluate(2, 'navigator.deviceMemory')` (jar key).
- **Raw state**: both returned exactly `8`. Real machine `nproc = 20`.
- **Expected**: both return exactly `8` (spoofed constants masking the real machine).
- **Verdict**: pass — 8 ≠ real 20, so the spoof is demonstrably masking the machine.

### Step 4 — PASS
- **Actions taken**: `evaluate(2, <draw expr>)` twice — a single self-contained expression that creates a canvas, draws a fixed image (fillRect + text + arc), and returns `length:hash` of `toDataURL()`. Captured A then A2.
- **Raw state**: A = `"12614:3773885476"`, A2 = `"12614:3773885476"`.
- **Expected**: `A === A2` — farbled output stable within the session.
- **Verdict**: pass — identical, deterministic per-(seed,index) noise.

### Step 5 — PASS (soft check)
- **Actions taken**: `evaluate` a `getImageData` sample (RGBA at [10,10],[50,20],[100,30],[150,40]) of the same fixed draw, on G (jar key) and G2 (admin). Flat `#f60` fill = (255,102,0,255).
- **Raw state**: G(2) = `[[148,103,64,255],[255,103,0,255],[255,103,0,255],[255,103,0,255]]`; G2(5) = `[[148,102,64,255],[255,102,0,255],[255,102,0,255],[255,102,0,255]]`.
- **Expected**: sampled pixels perturbed — farbling applied, not pass-through.
- **Verdict**: pass — G's green channel is perturbed **+1** off the flat (255,102,0) baseline (noise applied); the two seeds' samples differ (some pixels noised in one seed, not the other — the ~1/8 ±1 model). Cross-seed difference (Step 6) is the authoritative backstop.

### Step 6 — PASS (PRIMARY)
- **Actions taken**: Ran the identical draw expr in G2 (work jar, admin key) → B; re-read `navigator.hardwareConcurrency`/`deviceMemory` in G2.
- **Raw state**: B = `"12618:2753869836"` (vs A = `"12614:3773885476"`); navigator values both `8` in G2.
- **Expected**: `B !== A` (noise keyed to the per-jar seed); navigator stays 8 in both jars.
- **Verdict**: pass — different jars → different canvas noise (both length and hash differ), while the navigator spoof stays a constant 8. Distinct-jar guard satisfied (G default-session, G2 work-session).

### Step 7 — SKIPPED (optional)
- **Actions taken**: Not driven (Shields-off-per-origin toggle + reload not exercised).
- **Rationale**: Optional per the spec ("Skip if Shields-off-per-origin isn't readily drivable; note as untested"). The navigator control it targets is already covered by the real `nproc=20` vs reported `8` contrast, and the canvas causation by the cross-seed difference.
- **Verdict**: n/a (untested, by spec allowance).

## Orchestrator Notes
- Draws returned a compact `length:hash` of `toDataURL()` rather than the full data-URL, to keep the observable small; equality of the hash+length pair is the stability/difference test. Pixel samples were kept to four RGBA tuples per the spec's "small sampled subset" guidance.
- G2 (work jar) was read with the admin key because the dev auto-mint mints only the default jar key; the spec explicitly permits admin for the second-seed step.
- **Evidence**: [evidence.txt](../../../../tmp/behavior-tests/goldfinch/farbling-correctness/2026-07-08-18-27-53/evidence.txt)

## Closing Summaries

### Executor closing
Full pass on the eval-tool apparatus. Farbling fires on the live app: `navigator.hardwareConcurrency`/`deviceMemory` are spoofed to a constant 8 (real machine is 20 cores), canvas reads are noised deterministically (stable within a session: A===A2) and seed-dependently (B≠A across jars). The navigator spoof is a fixed constant (unchanged across seeds), the canvas noise is seed-keyed — exactly the two-mode behavior the spec asserts.

### Validator closing
Strong evidence with a built-in control (real 20-core machine reporting 8). The pixel-level +1 green perturbation directly witnesses the ±1 canvas noise. Spec draft status is fair to keep, but this run satisfies the F9 verify-integration intent; promotion `draft→active` remains the operator's call per the spec note.

## Operator Notes
Verdict PASS (6/6 core; step 7 optional/untested). Farbling intact on the native surface — no silent Chromium-bump breakage.
