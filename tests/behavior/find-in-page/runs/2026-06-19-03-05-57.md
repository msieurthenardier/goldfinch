# Behavior Test Run: find-in-page — 2026-06-19-03-05-57

**Spec**: [tests/behavior/find-in-page.md](../find-in-page.md)
**Status**: partial
**Started**: 2026-06-19T02:50:00Z
**Completed**: 2026-06-19T03:05:57Z
**Mode**: orchestrator-driven live verification (direct MCP/curl) — see Orchestrator Notes for why
this run did not re-use the two-agent Witnessed pattern.
**Evidence (ephemeral, local-only, not committed)**: live tool returns captured inline below;
re-derivable by re-running against `npm run dev:automation` on port 7799.

## Summary

Post-fix verification of the SC8 find automation tools after Deviation D1 (renderer-routed find).
The architectural bug from run `2026-06-19-01-04-20` (find returned `{0,0}` for present terms) is
**fixed and live-verified for warm/stepping finds**. One residual WSLg-only cold-start limitation
remains, accepted by the operator as a documented known issue.

- ✅ findInPage returns true match counts for warm/stepping finds (corroborated by `evaluate`).
- ✅ stepping (`findNext`/`forward`) moves the active match; no-match returns `{0,0}` cleanly;
  `stopFindInPage` → `{ok:true}`.
- ✅ tools discoverable on the surface (26 total; `findInPage`/`stopFindInPage` present).
- ⚠️ The FIRST find on a freshly-loaded page returns `{0,0}` in the WSLg dev environment
  (Chromium `<webview>` cold-start quirk); the next find is correct. WSLg-only; to confirm on macOS.
- ⏸️ Step 7 (cross-jar refusal) not executable through the apparatus (a single jar-scoped key
  cannot enumerate/obtain a foreign jar's wcId) — spec-executability gap, see Validator notes.

## Checkpoint Results (live, against example.com / example.net, term "example", evaluate count = 2)

### Checkpoint 2 (fresh search) — PARTIAL / WSLg-blocked
- **Observed**: cold first `findInPage(wcId,"example")` → `{activeMatchOrdinal:0, matches:0}` (WSLg
  cold-start); the immediately-following stepping call → `{activeMatchOrdinal:1, matches:2}` matching
  the `evaluate` count.
- **Expected**: `matches ≥ 2`, `activeMatchOrdinal = 1`.
- **Verdict**: fail on the cold-first-find in WSLg; **accepted as a documented known issue** (operator
  decision — ship with WSLg caveat; confirm on macOS). The find engine demonstrably returns the
  correct count once the subsystem is warm.

### Checkpoint 3 (step forward) — PASS
- `findInPage(wcId,"example",{findNext:true,forward:true})` → `{activeMatchOrdinal:1, matches:2}`
  (reliably across every run, cold or warm). matches corroborated by `evaluate` = 2.

### Checkpoint 4 (step backward) — PASS
- `findInPage(wcId,"example",{findNext:true,forward:false})` → `{activeMatchOrdinal:1, matches:2}`.

### Checkpoint 5 (no-match) — PASS
- `findInPage(wcId,"zzz-no-such-term-zzz")` → `{activeMatchOrdinal:0, matches:0}` (clean, no error).

### Checkpoint 6 (stopFindInPage) — PASS
- `stopFindInPage(wcId)` → `{ok:true}`.

### Checkpoint 7 (cross-jar refusal) — INCONCLUSIVE (apparatus/spec gap)
- A single Default-jar key cannot `enumerateTabs`/`openTab` in another jar, so the apparatus cannot
  obtain a foreign-jar `wcId` to attempt the cross-jar call. Jar-scoping IS enforced at
  `resolveContents` (unit-proven), but this step is not executable via the surface as written. Spec
  should be revised to feed a foreign wcId out-of-band or drop the live step (unit covers it).

## Orchestrator Notes

- **Why not the two-agent Witnessed re-run**: run `2026-06-19-01-04-20` already Witnessed the bug
  (independent Executor + Validator both observed `{0,0}` on a present term). The fix then required
  six edit→restart→verify iterations against the live main process (each restart mints fresh keys and
  drops the session's `goldfinch` MCP connection), so post-fix verification was driven directly by the
  Orchestrator over the live MCP surface (curl, jar + admin keys) — including a main-process
  `[FIND-DIAG]` instrumentation pass that pinpointed the root cause and admin-key chrome-renderer
  diagnostics that proved the renderer-routed recipe. The evidence (exact tool returns vs. `evaluate`
  corroboration) is recorded above. A future clean Witnessed re-run on macOS is the right way to close
  the cold-first-find caveat.
- Root cause + fix: Electron delivers `found-in-page` only to the renderer-side `<webview>` tag, never
  to a main-process `webContents`, for `<webview>` guests. The op now routes via
  `chromeContents.executeJavaScript` on the tag (Deviation D1). Unit suite 831 green with a
  `new Function(code)` parse-guard and a resolve-on-`matches>0` regression guard.

## Operator Notes
Operator decision: ship with the WSLg cold-first-find caveat; confirm the cold path on macOS per the
mission's platform plan. SC8 = live-verified-partial.
