# Behavior Test Run: find-in-page — 2026-07-08-19-32-27

**Spec**: [tests/behavior/find-in-page.md](../../find-in-page.md)
**Status**: partial
**Started**: 2026-07-08T19:32:27
**Completed**: 2026-07-08T19:36:00
**Duration**: 00:03:33
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run — no independent Validator spawned)

## Summary
7 steps. 4 PASS (1 setup, stepping fwd, stepping back, cross-jar refusal, stopFind) ; 1 FAIL (cold new-search, WSLg-known) ; 1 INCONCLUSIVE (no-match indistinguishable from quirk). Surface = native `WebContentsView`, admin + jar keys, port 8899.

## Cold-start question (FD-requested, IN SCOPE)
**Answer: the M04 `{0,0}` cold-start blank-match quirk STILL reproduces under `WebContentsView`.**
A truly cold first-find on a freshly-loaded `https://example.com` tab (wcId 3) returned
`{activeMatchOrdinal:0, matches:0}` and took **2510 ms** — the main-process `requestId`-correlated
retry ran to full exhaustion (~2.5 s budget) and still returned `{0,0}`. The quirk is broader than
"first find only": **every `findNext:false` new-search in the session** reproduced `{0,0}` after
~2.5 s, while **`findNext` stepping reported the correct `matches:2` instantly** and moved the active
ordinal correctly. So the engine *does* count matches — the failure is confined to the new-search
return path. Spec text updated to record this (authorized edit).

## Step Results

### Step 1 — PASS (setup)
- **Actions taken**: `openTab https://example.com` → wcId 3; `evaluate` `document.readyState`→`complete`, `document.title`→`Example Domain`; occurrence count `(body.innerText.match(/example/gi)||[]).length` → **2**.
- **Raw state**: bodyCount=2, matches≥2 premise satisfied.
- **Verdict**: pass — stable page with term occurring twice, corroborated by `evaluate`.
- **Evidence**: [coldfind.json](./2026-07-08-19-32-27/coldfind.json)

### Step 2 — FAIL (WSLg-known cold-start; dispositioned, not a Leg-5 regression)
- **Actions taken**: cold `findInPage(3,"example")` (new search).
- **Raw state**: `{activeMatchOrdinal:0, matches:0}`, 2510 ms (retry exhausted).
- **Expected**: `matches ≥ 2` == corroborated count (2); `activeMatchOrdinal == 1`.
- **Verdict**: fail — returned `{0,0}`. This is the cold-start quirk the spec's own warning anticipates ("Step 2 below may still fail on WSLg"). Prior run `2026-06-19-03-05-57` was likewise cold-first-find-blocked. Classification: **WSLg known-limitation**, not a new regression.
- **Evidence**: [coldfind.json](./2026-07-08-19-32-27/coldfind.json), [stepping.json](./2026-07-08-19-32-27/stepping.json)

### Step 3 — PASS (forward stepping)
- **Actions taken**: `findInPage(3,"example",{findNext:true,forward:true})` ×3 with 400 ms spacing.
- **Raw state**: fwd-a `{1,2}`, fwd-b `{2,2}`, fwd-c `{1,2}` (wrap). `matches` unchanged at 2.
- **Expected**: ordinal advances `1→2`; matches unchanged.
- **Verdict**: pass — forward stepping advances the active match `1→2` and wraps `2→1` cleanly; count stable at 2. (Ordinal 1 baseline established by the first `findNext`, since the new-search return path is quirk-blocked.)
- **Evidence**: [stepping.json](./2026-07-08-19-32-27/stepping.json)

### Step 4 — PASS (backward stepping)
- **Actions taken**: `findInPage(3,"example",{findNext:true,forward:false})` ×2.
- **Raw state**: back-a `{2,2}`, back-b `{1,2}`.
- **Expected**: ordinal moves back `2→1`.
- **Verdict**: pass — backward stepping retreats `2→1` and wraps `1→2` correctly; matches stable.
- **Evidence**: [stepping.json](./2026-07-08-19-32-27/stepping.json)

### Step 5 — INCONCLUSIVE
- **Actions taken**: `findInPage(3,"zzz-no-such-term-zzz")`.
- **Raw state**: `{activeMatchOrdinal:0, matches:0}`.
- **Expected**: `matches:0, activeMatchOrdinal:0` — no-match reports zero cleanly.
- **Verdict**: inconclusive — the payload matches Expected, BUT every `findNext:false` new-search returns `{0,0}` on this rig (cold-start quirk), so a genuine no-match and the quirk's false-zero are indistinguishable through the surface. Cannot cleanly attribute the zero to a correct no-match.
- **Evidence**: [stepping.json](./2026-07-08-19-32-27/stepping.json) (new-search#3 also `{0,0}`)

### Step 6 — PASS
- **Actions taken**: `stopFindInPage(3)`.
- **Raw state**: `{ok:true}`, no error.
- **Verdict**: pass — find session cleared cleanly.

### Step 7 — PASS (cross-jar refusal; apparatus gap resolved out-of-band)
- **Actions taken**: opened a **Work**-jar tab (`openTab example.org jarId:work` → wcId 4, admin); confirmed the **jar key** works in its own default jar (`findInPage(3,…)`→`{2,2}`); then `findInPage(4,"example")` with the **jar key**.
- **Raw state**: `automation: out-of-jar — wcId 4 does not belong to jar default`, `isError:true`.
- **Expected**: refused as `automation: out-of-jar` — jar-scoped, not a cross-jar reach.
- **Verdict**: pass — clean out-of-jar refusal. The spec's noted apparatus gap (a single jar key can't enumerate a foreign wcId) was resolved by feeding the admin-enumerated foreign wcId out-of-band, exactly as the spec permits.
- **Evidence**: [step7-crossjar.json](./2026-07-08-19-32-27/step7-crossjar.json)

## Orchestrator Notes
Single-agent convenience sweep (SC4), no independent Validator spawned. All observables are raw tool-result JSON, judged against Expected. Tool count observed = **27** (spec text says 26 — minor spec-drift, presence-checked, not a failure).

## Closing Summaries

### Executor closing
Native `WebContentsView` find engine counts matches correctly (stepping proves `matches:2`), and jar-scoping + `stopFindInPage` are solid. The single defect surface is the `findNext:false` new-search return path, which returns `{0,0}` after exhausting the ~2.5 s retry — the M04 cold-start quirk, still present on WSLg and now observed to affect every new-search, not just the first. Dispositioned WSLg-known per the spec's warning block; macOS is expected to pass step 2.

### Validator closing
The retry mitigation in the op does not recover the new-search zero on this rig. If macOS also shows the broadened "every new-search" behavior (not just first-find), the retry logic warrants revisiting; if macOS passes, this stays a WSLg-only environmental limitation. Recommend the FD note it for the flight debrief but NOT block on it (matches the prior run's disposition).

## Operator Notes
Cold-start answer recorded in the spec's warning block (authorized edit). Work-jar tab (wcId 4) left open; torn down at batch end.
