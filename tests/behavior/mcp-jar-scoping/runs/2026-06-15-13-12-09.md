# Behavior Test Run: mcp-jar-scoping — 2026-06-15-13-12-09

**Spec**: [tests/behavior/mcp-jar-scoping.md](../../mcp-jar-scoping.md)
**Status**: pass
**Started**: 2026-06-15T13:12:00
**Completed**: 2026-06-15T13:22:00
**Duration**: ~00:10:00
**Executor**: FD-driven (MCP SDK client over the loopback transport — `StreamableHTTPClientTransport` + `Authorization: Bearer`)
**Validator**: FD-driven (mission behavior-test mode: FD-driven with cited machine-read evidence)

## Summary
**PASS — flips the Flight-4 `partial` to a full live pass.** The new Flight-5 jars UI was the missing apparatus: the operator staged tabs across `default`/`personal`/`work`/burner + the internal `goldfinch://settings` tab, making the cross-jar / internal-session / burner refusals stageable live for the first time. Both tiers verified against a real multi-jar tab set:
- **Jar key (`default`)** is confined to its own jar — enumerate returns only the default-jar tab; out-of-jar, internal-session, and burner targets are refused.
- **Admin key** (env-gated, minted because `GOLDFINCH_AUTOMATION_ADMIN=1`) sees and drives all jars, including the internal session — its sole authorized relaxation.

**Launch**: `GOLDFINCH_MCP_PORT=49707 GOLDFINCH_AUTOMATION_DEV_MINT=1 [GOLDFINCH_AUTOMATION_ADMIN=1] electron . --no-sandbox --automation-dev` (two relaunches: jar-key run, then admin run; tabs re-staged each). Port pinned per DD7. Apparatus = MCP SDK client (NOT `chrome-devtools` MCP). Keys captured from the auto-mint-to-stdout seam (landed F4).

## Step Results

### Jar-key run (default-jar key; `GOLDFINCH_AUTOMATION_ADMIN` unset)

#### Step 1 — Active-precondition + setup — PASS
- Auto-mint printed `{ key, adminKey:null }` (jar key only; admin gate unset). MCP bound `127.0.0.1:49707` (ss: not `0.0.0.0`/`::`). `initialize` succeeded with the jar identity bound; `enumerateTabs` returned a JSON array. Operator staged a `default` (Google) tab + `personal`, `work`, burner, and internal `goldfinch://settings` tabs.

#### Step 2 — Jar confinement (enumerate) — PASS
- Default-jar key `enumerateTabs` returned **only** `wcId 2 / jarId default` — the staged `personal`/`work`/burner/internal tabs are **invisible** to the jar key. Evidence: `02-confinement-enumerate.json`.

#### Step 3 — Out-of-jar refusal (drive by wcId) — PASS
- Default-jar key `readDom`: in-jar `wcId 2` → **ok** (Google DOM). `wcId 3` (personal), `wcId 4` (work), `wcId 5` (burner) → `isError: automation: out-of-jar — wcId N does not belong to jar default`. Evidence: `03-refusal-matrix.json`.

#### Step 4 — Internal-session exclusion (absolute for jar keys) — PASS
- Default-jar key `readDom` on `wcId 6` (the internal `goldfinch://settings` guest) → `isError: automation: internal-session — wcId 6 belongs to the internal goldfinch://settings session and cannot be driven`. The privilege-escalation path into the internal bridge is refused.

#### Step 5 — Burner unautomatable — PASS
- The burner tab (`burner-190855950`) is not in the default jar, so the default-jar key cannot enumerate or drive it (out-of-jar refusal, step 3 `wcId 5`).

### Admin run (relaunch with `GOLDFINCH_AUTOMATION_DEV_MINT=1` AND `GOLDFINCH_AUTOMATION_ADMIN=1`; tabs re-staged)

#### Step 6 — Admin precondition — PASS
- Auto-mint printed a **non-null** `adminKey` (the admin tier exists because the env var was set at launch). Tabs re-staged. `initialize` succeeded with the admin identity.

#### Step 7 — Admin sees all jars — PASS
- Admin `enumerateTabs` returned **all 5 tabs**: `default`(2), `personal`(3), `work`(4), `burner-190855950`(5), `internal goldfinch://settings`(6) — jarIds `[default, personal, work, burner-190855950, internal]`. The SAME moment, the jar key saw only 1 (default). Evidence: `04-admin-enumerate.json`.

#### Step 8 — Admin drives cross-jar + internal — PASS
- Admin `readDom` on `wcId 3` (personal — refused for the jar key) → **ok** (Google DOM). Admin `readDom` on `wcId 6` (internal settings — absolutely refused for the jar key) → **ok** (`goldfinch://settings` DOM, title "Settings — Goldfinch"). The admin tier's sole authorized relaxation of the internal-session exclusion is exercised live. Evidence: `05-admin-drive.json`.

## Orchestrator Notes
- This run completes the disposition the Flight-4 `mcp-jar-scoping` partial carried: "the cross-jar/internal/burner refusals become stageable live once the jars Ui exists." It does. The Flight-4 run log remains the `partial` snapshot; this is the superseding `pass`.
- Keys minted via the auto-mint-to-stdout seam (kept per DD5); never written to the transcript or committed. SDK client used `terminateSession()` to clean up each probe session.
- Evidence dir (ephemeral, outside the tree per ARTIFACTS.md): `/tmp/behavior-tests/goldfinch/mcp-jar-scoping/2026-06-15-13-12-09/`.

## Closing Summaries

### Executor closing
The MCP SDK client authenticated cleanly with both the jar key and the admin key over the loopback transport. enumerate + readDom returned structured JSON; refusals came back as normal `isError` tool results with the `automation: <code> — <reason>` contract, never as transport errors. No environment hiccups.

### Validator closing
The jar-key-vs-admin contrast at the same staged tab set is the decisive evidence: identical apparatus, different identity, structurally different visibility (1 tab vs 5). The refusal-code taxonomy (`out-of-jar`, `internal-session`, `no-such-contents`) is precise and matches the engine's contract. Full matrix verified live.

## Operator Notes
_(Operator staged the multi-jar tab set via the new jars UI for both runs; this UI is exactly the apparatus Flight 4 lacked.)_
