# Behavior Test Run: mcp-jar-scoping — 2026-07-08-18-19-42

**Spec**: [tests/behavior/mcp-jar-scoping.md](../../mcp-jar-scoping.md)
**Status**: pass
**Started**: 2026-07-08T18:19:00-05:00
**Completed**: 2026-07-08T18:21:00-05:00
**Duration**: ~00:02:00
**Executor**: Flight-05 Leg-03 behavior-test executor (single-agent Witnessed run)
**Validator**: same agent (self-witnessed)

## Summary
10 / 10 steps passed. 0 failed; 0 inconclusive.

Native WebContentsView surface. App launched once admin-wired (`GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 GOLDFINCH_MCP_PORT=8899`), minting both a jar key and an admin key. SC8 confinement half verified end to end.

**Adaptations (SPEC-DRIFT, non-material to the property under test):**
1. **Jar mapping.** The dev auto-mint mints the jar key for the **`default`** jar (`main.js:2508` — `mintJarKey('default', …)`), not `personal` as the spec names. Mapping applied: spec `personal` → **`default`** (jar key's own jar); spec `work` → **`work`** (cross-jar target). The confinement property is identical.
2. **Single launch, not two relaunches.** The spec scripts a jar-key relaunch (admin unset) then an admin relaunch (admin set). Collapsed to one admin-wired launch: the **jar-key rows** used `GOLDFINCH_MCP_KEY` (jar identity), the **admin rows** used `GOLDFINCH_MCP_ADMIN_KEY`. Valid because a jar key's scoping is decided by session identity, independent of the `GOLDFINCH_AUTOMATION_ADMIN` env var (which only controls whether an admin key is *minted*). Tabs did not need re-staging since there was no relaunch.

**Staged tabs:** default → wcId 2 (`https://example.com/`); work → wcId 5 (`https://example.org/`, via admin `openTab(url,'work')`); burner → wcId 6 (`burner-160813809`, google, via chrome `evaluate` → `createTab(currentHomePage(), makeBurner())`); internal → wcId 4 (`goldfinch://settings/`).

## Step Results

### Step 1 — PASS (setup + probe)
- **Actions taken**: Launched admin-wired; captured minted `key`/`adminKey` from `AUTOMATION_DEV_MINT` stdout. Staged the four tabs. Connected the jar-key client; `initialize`; `enumerateTabs`.
- **Raw state**: Both keys printed (jar key + admin key non-null). Four tabs staged and visible in the chrome. Jar-key `initialize` succeeded; `enumerateTabs` returned a JSON array.
- **Expected**: Key printed; tabs staged; `initialize` succeeds; `enumerateTabs` returns an array.
- **Verdict**: pass.

### Step 2 — PASS
- **Actions taken**: Inspected the jar-key `enumerateTabs`.
- **Raw state**: `[{wcId:2, jarId:"default", url:"https://example.com/"}]` — only the default-session tab. Absent: work (5), burner (6), internal (4).
- **Expected**: Lists only the `default`-session tab(s); excludes work, burner, and internal.
- **Verdict**: pass — confinement of enumeration to the key's own jar.

### Step 3 — PASS [mixed-frame]
- **Actions taken**: With the jar key, `reload(5)` (the work tab). Confirmed via admin `enumerateTabs` the work tab unchanged (still `https://example.org/`, wcId 5).
- **Raw state**: `isError:true`, text `automation: out-of-jar — wcId 5 does not belong to jar default`. Work tab present and unchanged in the post-refusal admin enumerate.
- **Expected**: `isError` with `automation: out-of-jar`; work tab unchanged (no side effect).
- **Verdict**: pass — refusal fired ahead of the engine op; no side effect.

### Step 4 — PASS
- **Actions taken**: With the jar key, `reload(4)` (internal settings).
- **Raw state**: `isError:true`, text `automation: internal-session — wcId 4 belongs to the internal goldfinch://settings session and cannot be driven`.
- **Expected**: `isError` with `automation: internal-session`.
- **Verdict**: pass — internal session absolute-refused for the jar key (resolve-time, ahead of the membership check).

### Step 5 — PASS [mixed-frame]
- **Actions taken**: With the jar key, `reload(6)` (the burner). Confirmed via admin enumerate the burner unchanged.
- **Raw state**: `isError:true`, text `automation: out-of-jar — wcId 6 does not belong to jar default`. Burner still present (google) in the admin enumerate.
- **Expected**: `isError` with `automation: out-of-jar`; burner unchanged. Together with step 2's absence → full burner-unautomatable property for the jar key.
- **Verdict**: pass.

### Step 6 — PASS
- **Actions taken**: With the jar key, `captureWindow()` (no args).
- **Raw state**: `isError:true`, text `automation: admin-only — captureWindow (whole-window capture) is restricted to the admin identity`.
- **Expected**: `isError` with `automation: admin-only` (distinct from `out-of-jar`).
- **Verdict**: pass — distinct admin-only discriminator returned.

### Step 7 — PASS (admin identity bind)
- **Actions taken**: Connected the admin-key client; `initialize`. (No relaunch/re-stage — see adaptation 2; tabs remained staged.)
- **Raw state**: admin `initialize` succeeded; admin identity bound.
- **Expected**: non-null `adminKey`; admin identity bound.
- **Verdict**: pass.

### Step 8 — PASS
- **Actions taken**: With the admin key, `enumerateTabs`.
- **Raw state**: `[wcId2 default, wcId4 internal, wcId5 work, wcId6 burner-160813809]` — includes the `personal`(→default) tab, the `work` tab, AND the internal `goldfinch://settings` tab (plus a burner, permitted by the positive "at minimum" phrasing).
- **Expected**: At minimum every persistent jar's staged tab + the internal tab.
- **Verdict**: pass.

### Step 9 — PASS
- **Actions taken**: With the admin key, `reload(2)`, `reload(5)`, `reload(4)`.
- **Raw state**: each returned `{"ok":true}`, non-error — including `reload(4)` on the internal session.
- **Expected**: Each returns its non-`isError` success shape; admin drives across jars and into the internal session.
- **Verdict**: pass — no `internal-session`/jar-membership throw for admin.

### Step 10 — PASS
- **Actions taken**: With the admin key, `captureWindow()`.
- **Raw state**: image content block `{ type:'image', mimeType:'image/png', data:<base64> }` (120236 bytes), non-error.
- **Expected**: image content block, not `isError`, not the `admin-only` refusal.
- **Verdict**: pass — the same `captureWindow` that is `admin-only` for a jar key succeeds for admin.
- **Evidence**: [step10-captureWindow-admin.png](../../../../tmp/behavior-tests/goldfinch/mcp-jar-scoping/2026-07-08-18-19-42/step10-captureWindow-admin.png)

## Orchestrator Notes
- Burner staged via chrome-renderer `evaluate` (`createTab(currentHomePage(), makeBurner())`) rather than the `▾` container-menu UI click — the resulting `burner-160813809` renderer-only session is identical to the UI path (`renderer.js:361` dispatches `action:burner` to the same call). This was a staging convenience, not part of any asserted step.

## Closing Summaries

### Executor closing
Full pass. SC8 confinement holds on the live surface: the `default` jar key enumerates and drives only its own jar, and draws four distinct audit-legible refusal codes on everything else — `out-of-jar` (cross-jar work + burner), `internal-session` (settings), `admin-only` (captureWindow). Admin sees all persistent jars + internal, drives across all + internal, and `captureWindow` returns a PNG.

### Validator closing
Every discriminated code matched the spec verbatim. No BLOCKING concern. Only drift is cosmetic (jar named `default` not `personal`; single launch vs two). Recommend the spec note that the dev auto-mint targets `default`, so runners map `personal`→`default`.

## Operator Notes
BLOCKING jar-scoping spec — verdict PASS. Confinement + admin relaxation both intact on the native surface.
