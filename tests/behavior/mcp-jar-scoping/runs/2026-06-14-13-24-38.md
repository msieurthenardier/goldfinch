# Behavior Test Run: mcp-jar-scoping — 2026-06-14-13-24-38

**Spec**: [tests/behavior/mcp-jar-scoping.md](../../mcp-jar-scoping.md)
**Status**: partial
**Started**: 2026-06-14T18:23:00Z
**Completed**: 2026-06-14T18:25:00Z
**Duration**: ~00:02:00
**Mode**: FD-driven machine-read. Apparatus: SDK MCP client (`Authorization: Bearer`) against the admin instance on `127.0.0.1:7797`, jar key (`default`) + admin key minted via the leg-5 auto-mint-to-stdout.

## Summary
The machine-tractable jar-scoping assertions PASSED live. The assertions requiring GUI multi-jar / settings / burner tab staging were **not** run live (the MCP surface cannot stage tabs across jars — jar switching is a chrome-UI action, and the internal/burner tabs cannot be opened via `openTab`, which re-applies `isSafeTabUrl`). Those are exhaustively covered by the **headless integration suite** (a fake multi-jar world keyed by real session-object-identity) — 579 unit/integration tests green. Disposition + the remaining live coverage are an operator decision (see Orchestrator Notes / flight log).

## Step Results

### Jar key enumerates only its jar — PASS (live)
- **Raw state**: default-jar key `enumerateTabs` → `[{wcId:2,url:google,jarId:default,active:true}]` — only the default jar's tab.
- **Verdict**: pass — enumerate is scoped (the only tabs present were default-jar; no cross-jar leakage observed).

### Jar key `captureWindow` → admin-only — PASS (live)
- **Raw state**: default-jar key `captureWindow` → `isError: automation: admin-only — captureWindow (whole-window capture) is restricted to the admin identity`.
- **Verdict**: pass — the distinct `admin-only` refusal fires live (not `out-of-jar`).

### Admin `captureWindow` returns an image — PASS (live)
- **Raw state**: admin key `captureWindow` → image content block (`image/png`, ~61 KB base64).
- **Verdict**: pass — admin whole-window capture works.

### Admin enumerate — PASS (live, partial scope)
- **Raw state**: admin key `enumerateTabs` → sees the tab. (Only one jar's tab existed in this profile, so "sees ALL jars + internal" was not fully exercised live — see below.)
- **Verdict**: pass for "admin enumerates"; the multi-jar/internal breadth is headless-backed.

### Cross-jar refusal / internal-session refusal / burner unautomatable / admin-sees-multiple-jars+internal — NOT RUN LIVE (headless-backed)
- **Why**: staging requires opening tabs in ≥2 jars + a burner + the internal `goldfinch://settings` tab via the chrome UI; the MCP surface cannot do this (no jar-switch tool; `openTab` rejects `goldfinch://`).
- **Backing**: the Leg-2 headless integration suite stages a fake multi-jar world (real `wc.session === fromPartition(jar.partition)` identity) and asserts: cross-jar `out-of-jar`, internal `internal-session` (absolute for jar keys), burner unautomatable, scoping-by-resolved-session-not-renderer-jarId (both spoof directions), admin sees all jars + internal, and revoke/toggle-off kills the live session. All green.
- **Disposition**: recorded as **partial** — operator to elect whether the full GUI-staged Witnessed run is required before landing, or the FD-driven + exhaustive headless coverage suffices.

## Orchestrator Notes
- Session-vs-jarId scoping-authority and the cross-jar/internal/burner refusals are unit/integration-backed per the spec's own scope-honesty caveat.
- Evidence: `/tmp/behavior-tests/goldfinch/mcp-jar-scoping/2026-06-14-13-24-38/jar-scoping-probes.txt` (ephemeral, not committed).

## Operator Notes
The auth gate + scoping plumbing is confirmed working live (jar enumerate scoped; `admin-only` fires; admin capture works); the cross-jar/internal/burner *refusal* breadth is headless-backed pending an optional GUI-staged Witnessed run.
