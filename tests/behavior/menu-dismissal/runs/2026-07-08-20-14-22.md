# Behavior Test Run: menu-dismissal — 2026-07-08-20-14-22

**Spec**: [tests/behavior/menu-dismissal.md](../../menu-dismissal.md)
**Status**: pass
**Started**: 2026-07-08T20:11:00
**Completed**: 2026-07-08T20:15:00
**Duration**: 00:04:00
**Executor**: Leg-5 batch executor (self-witnessed; single-agent convenience sweep)
**Validator**: same agent (batch run)

## Summary
All 9 steps PASS on native `WebContentsView` (chrome `wcId` 1, sheet `wcId` 6). Outside-click dismissal + swallow, cross-trigger mutual exclusion, trigger re-click toggle-close, Escape + focus-restore, the container menu's full APG contract, container tab-open, and keyboard trigger-open all hold across the F8 sheet cutover.

## Step Results

### Step 1 — PASS (probe)
`tools/list` includes `getChromeTarget` + `enumerateTabs`; `getChromeTarget()` → numeric chrome `wcId 1`; `enumerateTabs()` lists ≥1 guest. Sheet `wcId 6` discovered by probe walk.

### Step 2 — PASS (kebab outside-click + swallow)
- Open kebab → `#kebab aria-expanded="true"`; `click(6, 700, 500)` (guest region outside menu) → `aria-expanded="false"`; active guest URL unchanged (`default:https://example.com/`). Menu dismissed, click swallowed (no navigation).

### Step 3 — PASS (container outside-click + swallow)
- Open container → `#new-tab-menu aria-expanded="true"`, sheet `data-menu-type="container"` (role=menu, 14 items, roving "Default"); `click(6, 700, 500)` outside → `aria-expanded="false"`; active URL unchanged. Dismissed + swallowed.

### Step 4 — PASS (cross-trigger mutual exclusion)
- Container open → open kebab → container `aria-expanded="false"`, kebab `true`, sheet `data-menu-type="kebab"` (model-replace). Never both open.

### Step 5 — PASS (re-click toggles closed, no blink)
- Open kebab (`aria="true"`) → re-click `#kebab` → `aria="false"` and stays closed. No close-then-reopen blink (F8 re-click race suppressed).

### Step 6 — PASS (Escape + focus-restore, both menus)
- Container: open → `pressKey(6,'Escape')` → `#new-tab-menu aria="false"`, `document.activeElement.id="new-tab-menu"`, chrome `hasFocus=true`.
- Kebab: validated in the `kebab-menu` run step 6 (Escape → focus restored to `#kebab`). Each menu restores focus to its own trigger; not stranded on `<body>`.

### Step 7 — PASS (container is full APG)
- Sheet `#sheet-menu role="menu"`, items `role="menuitem"` with exactly one `tabindex="0"`; ArrowDown Default→**Personal**, End→**"+ New container…"**, Home→**Default**. Roving stays within the menu.

### Step 8 — PASS (container behavior preserved)
- Open container, ArrowDown to **Personal**, `evaluate(6,'document.activeElement.click()')` → tab count **6 → 7**; new tab `wcId 10`, `jarId:"personal"`, active. The sheet migration did not break container selection.

### Step 9 — PASS (container opens by keyboard)
- After Escape, focus on `#new-tab-menu`. `pressKey(1,'Space')` → container opens (`aria="true"`) with roving on the **first** item ("Default"). `pressKey(1,'ArrowUp')` → opens with roving on the **last** item ("+ New container…"). Trigger-side keydown stays chrome-owned; the sheet runs the APG contract once open.

## Orchestrator Notes
Single-agent convenience sweep. **Coordinate-caching lesson (spec-confirmed):** the `▾` container trigger shifted right as tabs accumulated (from x≈1026 at 4 tabs to x≈1186 at 6 tabs); an early cached coord missed it and hit a tab. Re-located `#new-tab-menu` via `getBoundingClientRect` before every container click thereafter — matches the spec's "re-locate before each click, do NOT cache" rule. Injected-click hit-testing and OS-blur dismissal remain HAT-only per the spec's apparatus-limits block (not attempted).

## Closing Summaries

### Executor closing
The dismissal contract is fully intact on the sheet: outside-click dismiss-without-forwarding, mutual exclusion, re-click toggle, Escape focus-restore, container APG, container tab-open, and keyboard trigger-open all pass. No regressions in the menu machinery.

### Validator closing
Clean pass. The only operational hazard is the mobile container-trigger coordinate — the spec already documents it and the run honored the re-locate rule. HAT-only items (physical hit-test interception, OS blur) correctly not asserted.

## Operator Notes
Step 8 left a stray Personal-jar tab (wc10); torn down at batch end.
