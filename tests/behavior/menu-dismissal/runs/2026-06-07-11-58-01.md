# Behavior Test Run: menu-dismissal — 2026-06-07-11-58-01

**Spec**: [tests/behavior/menu-dismissal.md](../menu-dismissal.md)
**Status**: pass
**Started**: 2026-06-07T11:58:01Z
**Completed**: 2026-06-07T12:06:00Z
**Duration**: ~00:08:00
**Executor**: aa7a449af68fbf374
**Validator**: a3710fcb527f0adb2

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/menu-dismissal/2026-06-07-11-58-01/` during the run; re-derivable by re-running the spec.

## Summary

9 / 9 checkpoints passed. 0 failed; 0 inconclusive. Verifies Flight 3: robust outside-dismiss (page/webview click via `window` blur, in-chrome click, cross-trigger) for both menus + the container menu's APG uplift. The Validator independently re-drove checkpoints 2/3/4/5/6/7/9 with fresh trusted CDP and read the step-7 PNG.

## Checkpoint Results

### Checkpoint 1 — PASS
- Renderer (`index.html`, title "Goldfinch") + active webview guest (`webview-tab-1`) present on `:9222`; tab count 1.

### Checkpoint 2 — PASS — page/webview-click dismissal (kebab)
- Open kebab (aria-expanded=true) → focus the webview → kebab `aria-expanded=false`, menu hidden, `activeElement=webview-tab-1`. **Window-blur dismissal confirmed** (the bug's core case). Re-driven fresh by the Validator.

### Checkpoint 3 — PASS — page/webview-click dismissal (container)
- Same for the container menu: opens, then closes when the webview takes focus.

### Checkpoint 4 — PASS — cross-trigger
- container open → click kebab → kebab open + container closed → click ▾ → container open + kebab closed. Never both open. Re-driven fresh.

### Checkpoint 5 — PASS — in-chrome outside click
- Clicking the address bar closes each open menu.

### Checkpoint 6 — PASS [a11y] — Escape + focus-restore
- Escape closes each menu and restores focus to its own trigger (kebab → `#kebab`, container → `#new-tab-menu`).

### Checkpoint 7 — PASS [a11y] — container is full APG
- `#container-menu` `role="menu"`; 6 `role="menuitem"` items; roving tabindex (item0=0, rest=-1); `.cm-title` `role="presentation"`. ArrowDown/Up move + wrap, Home/End jump, focus stays within items. PNG confirms the rendered menu.
- **Evidence**: `step7-container-open.png`

### Checkpoint 8 — PASS — container selection preserved
- Activating the "Personal" container item opened a new tab with a `.tab-jar` dot (title "Personal"); tab count 1→2; menu auto-closed. The APG uplift did not break selection.

### Checkpoint 9 — PASS [a11y] — container trigger opens by keyboard
- `Space` on `#new-tab-menu` opens the menu **exactly once** (no toggle-closed) with the first item focused; `ArrowUp` opens with the last item focused. Confirms the `preventDefault`-suppresses-synthetic-click contract. Re-driven fresh.

## Orchestrator Notes

- **Execution mode**: single-pass Executor + independent Validator (Witnessed; the Validator re-drove the key checkpoints via the same apparatus). `SendMessage` absent.
- **Apparatus**: committed `scripts/cdp-driver.mjs` (trusted input, attach-don't-launch) on the live `:9222` renderer (reloaded to the flight-branch build); `chrome-devtools` MCP not used.
- **Cache**: cold (renderer hard-reloaded before Step 1).
- **Accessibility Validator**: `Enabled:false` per project config; base Validator judged the `[a11y]` rows.
- **Spec-quality finding (Validator)**: the `▾` container trigger MOVES as tabs are added (the pill hugs the tab strip), so cached click coordinates drift. A drifted click during validation hit the adjacent `+` button and created a stray Default tab (no jar dot) — a test-apparatus artifact, not a product defect. **Spec note added**: re-query each trigger's bounding rect immediately before clicking; never reuse cached coords across steps that change tab count.

## Closing Summaries

### Executor closing
All 9 steps driven via `cdp-driver.mjs`. Both menus dismiss on webview focus (window-blur), in-chrome outside click, and cross-trigger; Escape restores focus to the right trigger; the container is now a full APG menu (role=menu/menuitem, roving, Arrow/Home/End) and still opens container tabs with the correct jar dot; Space/ArrowUp open the container from the trigger exactly once.

### Validator closing
Re-drove 2/3/4/5/6/7/9 independently; all confirmed. The only wrinkle was apparatus coordinate-drift (the ▾ moves with tab count) which initially made a stale-coordinate mouse-open miss look like a focus failure — re-driving via keyboard confirmed the real behavior. Recommend the spec/apparatus re-query trigger rects per click. No product defects.

## Operator Notes

Run is the Flight-3 `verify-integration` leg's primary gate (SC8 container a11y + flight-local dismissal correctness). A stray Default tab created during validation is harmless; reload clears it.
