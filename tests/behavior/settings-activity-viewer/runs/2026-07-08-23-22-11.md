# Behavior Test Run: settings-activity-viewer — 2026-07-08-23-22-11

**Spec**: [tests/behavior/settings-activity-viewer.md](../../settings-activity-viewer.md)
**Status**: pass
**Started**: 2026-07-08T23:19:00
**Completed**: 2026-07-08T23:24:00
**Executor**: Leg-1c batch executor (SC3 parity sweep, self-witnessed single-agent)
**Surface**: native `WebContentsView` — internal settings guest `wcId=12` (`jarId:"internal"`),
chrome `wcId=1`. Admin MCP over `127.0.0.1:8899`.

## Summary
The `goldfinch://settings` **Automation → Activity** viewer renders the served audit log, paginates
with a conventional numbered control, and honors the F7 freshness contract. **All 8 steps PASS on
the native surface** — including the freeze-on-page-back and pager steps that were pre-classified as
apparatus-limited on WSLg; they ran cleanly here. The serve-seam guard (Step 1) is satisfied
(viewer renders with content, not blank/404). No `LIKELY-REGRESSION`.

**Apparatus note.** The harness's own admin session is reflexive — every MCP tool call appends an
audit entry — so entries were generated naturally (the ring grew from ~8 to 16 pages over the run).
Pager page buttons are coordinate-clicked in the guest viewport (verified: `click(12, 345, 381)` →
page 2; the guest screenshot space is 1:1 with click coords). Row content is read from `readDom`
(the `.activity-log-row` markup serializes).

Evidence: `/tmp/behavior-tests/goldfinch/settings-activity-viewer/2026-07-08-23-22-11/`
(`evidence.json`), plus `sav-automation.png` / `sav-pager.png`.

## Step Results

### Step 1 — PASS (serve-seam guard — load-bearing)
- **Raw:** `readDom(12)` → `<div id="automation-activity-log">` renders **with content** — a
  populated list of `.activity-log-row` entries (time / op / detail / identity / outcome), e.g.
  `11:19:35 PM · readDom · admin · ok`. Not blank, no 404 — `audit-paging.js` was **served** via
  `INTERNAL_PAGES`.
- **Verdict:** PASS — the exact F7-HAT serve-seam regression is absent.

### Step 2 — setup (entries exceed one page)
- **Raw:** the reflexive admin session had already produced many entries; the numbered pager
  renders. Read from the control: **`{pageSize}=20`** (page-1 row count) and **`{pageCount}=14`**
  (highest page number), growing to 16 as the run generated more entries.

### Step 3 — PASS (newest-first + conventional numbered pager)
- **Raw:** page 1 shows the **newest** entries most-recent-first, 20 rows (== `{pageSize}`). The
  pager is `<nav class="activity-pager">` with a **disabled** prev `‹`, contiguous numbers with an
  **ellipsis** (`‹ 1 2 … 14 ›`), a next `›`, and **current page 1** marked
  `class="pager-btn current" aria-current="page" aria-label="Page 1, current page"`.
- **Verdict:** PASS. `[a11y]`

### Step 4 — PASS (page-1 liveness)
- **Raw:** on page 1, top row before = `11:22:53 PM · readDom`; after one distinct benign op
  (`captureScreenshot` on chrome) the top row became `11:22:53 PM · captureScreenshot`
  (`liveUpdated=true`). The ring also grew live (14 → 16 pages) while paused on page 2 (Step 6).
- **Verdict:** PASS — page 1 tracks the live ring (re-renders on new entries).

### Step 5 — PASS (navigate to page 2)
- **Raw:** `click(12, 345, 381)` on the `2` button → pager marks **current page 2**
  (`aria-current="page"`); the viewer shows page 2's older window (first row
  `11:17:17 PM · captureScreenshot`), 20 rows.
- **Verdict:** PASS.

### Step 6 — PASS (freeze-on-page-back — F7 freshness contract)
- **Raw:** while page 2 was shown, issued **16** new benign ops
  (8× `getChromeTarget` + `enumerateTabs`). Re-reading page 2: the first row is **unchanged**
  (`11:17:17 PM · captureScreenshot`) and the **entire page-2 row set is byte-identical**
  (`page2RowsIdentical=true`), still on page 2. The new entries did **not** slide page 2's window.
- **Verdict:** PASS — page 2+ is **frozen** at open time (the F7 contract that prevents older
  entries sliding mid-read). *(Pre-classified as apparatus-limited on WSLg; ran cleanly here.)*

### Step 7 — PASS (back to live)
- **Raw:** `click(12, 313, 381)` on the `1` button → **current page 1**; page 1's top row is the
  **latest** (`11:22:21 PM · readDom`), including entries added during the Step-6 freeze; liveness
  resumes.
- **Verdict:** PASS.

### Step 8 — PASS (boundary / last page)
- **Raw:** by now `{pageCount}=16`; clicking the highest-page button lands on **page 16**
  (`current`), which renders a **partial** window of **12** rows (the oldest entries) without error;
  it is the last page (`next` at end).
- **Verdict:** PASS. `[a11y]`

## Closing Summary
The Activity viewer's served render, numbered pagination, page-1 liveness, page-2 freeze-on-page-
back, back-to-live resume, and last-page boundary all hold on the native `WebContentsView` internal
guest — the full SC10 audit-viewer + F7 freshness contract. Overall **PASS**, no
`LIKELY-REGRESSION`. The freeze/pager steps flagged as WSLg-apparatus-limited were observable and
passed this run (the reflexive audit stream generated entries fast enough to distinguish live from
frozen).
