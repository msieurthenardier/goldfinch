# Behavior Test Run: settings-activity-viewer — 2026-06-17-17-23-30

**Spec**: [tests/behavior/settings-activity-viewer.md](../settings-activity-viewer.md)
**Status**: partial
**Started**: 2026-06-17T17:22:00
**Completed**: 2026-06-17T17:24:00
**Duration**: ~00:02:00
**Executor**: FD-driven (machine-read evidence; mission standard — no Witnessed crew elected)
**Validator**: FD-driven

## Summary
The **load-bearing serve-seam guard (Step 1) PASSES**: the `#automation-activity-log` viewer **renders with real audit content** (not blank) inside the privileged `goldfinch://settings` guest — i.e. `audit-paging.js` is served via `INTERNAL_PAGES` (the exact F7-HAT regression this spec exists to catch). The numbered-pager render (Steps 2-3) and the freeze/liveness/pagination steps (4-8) are **carried** — the pager did not appear via MCP-driven entry generation (it tracks a live broadcast that a pure `readDom` poll doesn't pump into a re-render), and the pagination steps need coordinate-clicks on page-number buttons (geometry not FD-derivable under WSLg; eval-on-internal is correctly blocked). Apparatus: `dev:automation` on `:52351`, admin key; Settings opened via the chrome's `#kebab-settings`.

## Step Results

### Step 1 — PASS (serve-seam guard — the load-bearing assertion)
- **Raw state**: Settings guest = `goldfinch://settings` wcId 4. `readDom(4)` shows `#automation-activity-log` rendering **real entry rows**, e.g. `<div class="activity-log-row"><span class="activity-log-time muted">5:23:24 PM</span><span class="activity-log-op">readDom</span><span class="activity-log-identity">admin</span><span class="activity-log-outcome">ok</span></div>` (repeated). NOT blank.
- **Verdict**: pass — the served subresource composes in the real guest; no 404/blank. **This is the F7-HAT regression guard and it holds.**

### Step 2-3 — PARTIAL (numbered pager render)
- **Actions taken**: issued ~50 benign `readDom(4)` reads (each a reflexive audit entry), then `readDom(4)` to read the pager DOM.
- **Raw state**: the viewer showed **9 entry rows** and **no numbered pager** (`‹ 1 2 … ›`) in the captured DOM, even after the extra reads.
- **Verdict**: partial/inconclusive — page 1 renders the newest window, but a numbered pager did not appear in the MCP-read DOM. Likely the live re-render is broadcast-driven (the viewer re-renders on an automation-activity broadcast; a pure `readDom` poll reads a snapshot that hasn't re-rendered to surface the pager), or the rendered page size ≥ the entries the snapshot reflected. Not a confirmed failure — the page-1 render itself is correct; the pager surfacing needs the live broadcast to re-render between reads, which the FD poll didn't reliably pump.

### Step 4 — CARRIED (page-1 liveness)
- Requires distinguishing a live re-render between reads; same broadcast-timing limitation as Steps 2-3 over a pure MCP poll.

### Step 5-7 — CARRIED (navigate to page 2, freeze-on-page-back, back-to-live)
- Require coordinate-clicks on page-number buttons in the internal settings guest — geometry not FD-derivable under WSLg; `evaluate`-on-internal correctly blocked. Carried.

### Step 8 — CARRIED (last-page boundary)
- Depends on Steps 2-7; carried.

## Orchestrator Notes
FD-driven via `node` over `scripts/lib/mcp-client.mjs` (admin key); Settings opened via the chrome's own `#kebab-settings` click (chrome-renderer, not eval-on-internal). **The single hard-FAIL condition this spec defines — a blank Activity viewer (the F7-HAT serve-seam regression) — is NOT present; the viewer renders real audit rows.** The pagination/freeze state-machine steps need either (a) a coordinate-click-capable apparatus for the page buttons, or (b) the live-broadcast re-render to be observable between MCP reads — both beyond a pure FD `readDom` poll under WSLg. The pure paging math is already unit-tested (`test/unit/audit-paging.test.js`); this run confirms the **served + rendered** half. Carry the pager-navigation/freeze steps to a guided HAT or a coordinate-capable run.

## Closing Summaries
### Executor closing
The serve seam works — `audit-paging.js` is served and the viewer renders live audit rows from the reflexive admin session. Driving the numbered pager needs real clicks on page buttons in the internal guest.
### Validator closing
The load-bearing regression guard passes. The numbered-pager + freeze contract is unverified here (apparatus-limited), not failed — recommend a guided HAT for Steps 2-8, or accept the serve-seam + unit-tested paging math as coverage.

## Operator Notes
_(pending — decide whether the pager/freeze steps warrant a guided HAT)_
