# Behavior Test Run: bookmarks-omnibox — 2026-08-03-19-43-22

**Spec**: [bookmarks-omnibox.md](../bookmarks-omnibox.md)
**Status**: pass
**Started**: 2026-08-03T19:43:22Z
**Completed**: 2026-08-03
**Executor**: `a13c52df385fdca73`
**Validator**: `ada0ca1fa2a9b5309`

**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/bookmarks-omnibox/2026-08-03-19-43-22/` during the run; re-derivable by re-running the spec.

## Summary

6 / 6 checkpoints passed. 0 failed, 0 inconclusive.

Second run of this spec, and the first since **M15 Flight 2 inverted checkpoint 4**: bookmarks became jar-owned, so a bookmark created in one jar must no longer surface in another jar's suggestions. The prior run (2026-07-29) asserted the opposite, which was correct under the app-scoped ruling then in force.

Checkpoints 5 and 6 were **re-targeted during this leg** before the run. They had been written against the app-scoped behaviour — selecting the *default* jar's bookmark from inside the second jar — which jar scoping makes impossible by construction. Rather than retire them as redundant with the new `bookmarks-jar-scoping` spec, they were pointed at a bookmark created in the second jar, because what they actually test (activating a suggestion navigates without re-homing the tab; the indicator disappears when the bookmark does) is unaffected by the scoping change and is covered nowhere else.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Raw state**: one row, `Hacker News Personal Pick` / news.ycombinator.com, filled gold star, no duplicate history row for the same URL.
- **Verdict**: pass — the Validator confirmed from a JSON captured within 334ms of the screenshot that `bookmarksSuggest` and `historySuggest` **both** independently matched that URL, so the dedup collision was real at capture time.
- **Evidence**: `step-1-hacker-screenshot.png`, `step-1-star-crop-6x.png`, `step-1-suggest-sources-hacker-only.json`, `step-1-history-personal.json`
- **The Executor caught its own vacuity problem here, unprompted.** Its first probe typed `Hacker News P`; it then cross-checked and found `historySuggest` returns `[]` for that exact string (the trailing `P` prefix-matches no token in the history row's title). That probe would have made "appears once only, no duplicate history row" meaningless — there would have been nothing to dedupe. It discarded the capture, verified `Hacker` genuinely double-matches, and re-ran. This is the discipline the sibling spec's amendment codified, arrived at independently.

### Checkpoint 2 — PASS
- **Raw state**: one row, `Ferroquartz Docs` / rust-lang.org, filled star of the same shape and size as checkpoint 1's. `historySuggest` returns `[]` for the URL, so the row rests entirely on bookmark data.
- **Verdict**: pass — genuinely the never-visited, bookmark-only case.
- **Evidence**: `step-2-ferroquartz-screenshot.png`, `step-2-star-crop-6x.png`, `step-2-suggest-sources-ferroquartz.json`

### Checkpoint 3 — PASS *(the control that gives 1 and 2 meaning)*
- **Raw state**: one row for the history-only URL, rendering as the bare URL (its stored history title is null and the model falls back to url). No indicator: confirmed visually on a 4× crop **and** by a programmatic gold-pixel scan over the whole 275×45px row region returning **0** matching pixels.
- **Verdict**: pass, and explicitly non-vacuous — checkpoints 1–2 established the star renders reliably when a bookmark match exists, and here `bookmarksSuggest` genuinely returns `[]`, so the absence tracks a real data difference rather than an indicator that never renders.
- **Evidence**: `step-3-mozilla-screenshot.png`, `step-3-row-crop-4x.png`, `step-3-suggest-sources-mozilla.json`

### Checkpoint 4 — PASS *(the inverted assertion)*
- **Probe A** (`Hacker`, the `personal` bookmark's own prefix, typed in `work`): the sheet renders one row reading literally **"No matches"** — an explicit empty state, not an absent or collapsed sheet. Zero gold-toned pixels in the row region. `bookmarksSuggest` and `historySuggest`, both scoped to `work`, return `[]`.
- **Probe B, the liveness control**: `Second` — matching `work`'s *own* bookmark — typed moments later in the same field and jar returns a properly starred row.
- **Verdict**: pass. The control is what makes this checkpoint mean anything: a bare empty result is equally consistent with "jar scoping works", "suggestions are broken in this jar", and "the query matches nothing for unrelated reasons". Probe B rules out the second reading directly. **The Validator independently identified this vacuity risk at `[READY]`, before being asked**, and the control was added to the Executor's instructions on that basis.
- **Evidence**: `step-4-work-hacker-screenshot.png`, `step-4-work-hacker-row-crop-4x.png`, `step-4-suggest-sources-work-hacker.json`, `step-4-work-second-screenshot.png`, `step-4-work-second-star-crop-6x.png`, `step-4-suggest-sources-work-second.json`
- **Declared coverage gap**: `work` has no history hit for `Hacker` either, so the permissive sub-case — the jar's *own* unmarked history row surviving while the foreign bookmark is suppressed — is not exercised here. The clause makes that case optional ("if any"), so it does not weaken the verdict, but a future run should use a query where the second jar does hold unrelated history.

### Checkpoint 5 — PASS *(re-targeted)*
- **Setup**: a bookmark created in `work` (`Bluewren Docs` → python.org) on a URL absent from that jar's history, verified at the data layer before acting so the resulting row rests on bookmark data alone.
- **Raw state**: after ArrowDown the row gained a visible gold selection outline — captured deliberately **before** committing to Enter, as proof the keyboard genuinely reached the sheet's selection state rather than being silently ignored. After Enter, the pre-existing tab navigated in place: same wcId, URL changed to python.org, `jarId` still `work`, real page content rendered.
- **Verdict**: pass. The Validator re-derived tab identity with its own live `enumerateTabs` call and cross-read the screenshots to confirm the tab strip kept the same four tabs with only one title changed — ruling out a *new* tab that merely happened to land in the right jar, which a careless reading would not distinguish.
- **Evidence**: `step-5-before-selection-screenshot.png`, `step-5-after-arrowdown-screenshot.png`, `step-5-after-navigation-screenshot.png`
- **The keyboard activation path was not refused**, so no direct `navigate()` substitution was used. The Executor had standing instructions to escalate to the operator rather than substitute, because navigating directly would have bypassed the very mechanism under test and passed vacuously.
- **Validator-flagged gap**: the `enumerateTabs` output backing the central claim was never saved as a file — it survived only because the app was still reachable for live re-derivation. Future runs should save enumeration output rather than relying on it being re-derivable after the fact. This is the **second** occurrence of this pattern; the `bookmarks-jar-scoping` run hit it at its own checkpoint 14.

### Checkpoint 6 — PASS *(re-targeted, and richer than before)*
- **Removal**: operator-performed via the star popover's Remove control.
- **Storage**: `work` drops to 3 rows, all byte-identical to their pre-removal records; `Bluewren Docs` gone entirely. The address-bar star's class went from `star-btn starred` to plain `star-btn`.
- **Probe `Bluewren` — total disappearance**: "No matches". The Executor noted plainly that this proves less than it appears: `historySuggest` returned `[]` for that string both **before and after** removal, so the outcome was never about the bookmark's presence — the string simply never matched the page's real title.
- **Probe `Python` — survives unmarked, the informative result**: one row, `Welcome to Python.org` / www.python.org — **the same URL that carried a filled star minutes earlier** — now rendering with no star. Confirmed by 4× crop and by a gold-pixel scan returning exactly **0**. `historySuggest` for that query is unchanged before and after (same url, title, score, lastVisitedAt), so the history data was untouched; only the merge's kind-stamping changed, because the bookmark row that used to win the dedup and carry the star no longer exists.
- **Verdict**: pass.
- **Evidence**: `step-6-before-{state-screenshot,bookmarks-work,history-probes}`, `step-6-popover-open-screenshot.png`, `step-6-after-removal-star-screenshot.png`, `step-6-after-{bookmarks-work,history-probes}.json`, `step-6-probe-bluewren-screenshot.png`, `step-6-probe-python-screenshot.png`, `step-6-probe-python-row-crop-4x.png`
- **Why the re-target improved this checkpoint**: because checkpoint 5 navigated that tab to python.org *in the work jar*, the URL acquired a history entry there. So removing the bookmark leaves a same-URL, same-row before/after demonstration that the indicator tracks live **bookmark status**, not the URL — strictly stronger than the bare disappearance the step would otherwise have shown.

## Orchestrator Notes

**Mode**: live two-agent continuation. Checkpoints 1–3 were batched into a single Executor turn — all three are read-only query probes in the default jar that mutate nothing, so no state existed for a verdict to be taken against between them; each returned its own report and was judged separately.

**Spec amended before the run.** The known drift flagged during Flight 2 leg 3 was resolved rather than carried: checkpoints 5–6 had been written against the app-scoped suggestion and targeted a row the second jar no longer has. Ruling was to **re-target, not retire** — both test properties (activation navigating without re-homing; the indicator disappearing with the bookmark) that survive the scoping change and are covered nowhere else. The re-target is recorded inline in the spec so a later reader sees what changed and why.

**Evidence-hygiene finding.** The Validator flagged an apparent anomaly at checkpoint 1: a sibling JSON showing `historySuggest` returning `[]` for what looked like the same query, 26 seconds before the accepted capture, raising a possible timing-flakiness concern. The Orchestrator opened both files and resolved it — the earlier file is from the **discarded** `Hacker News P` probe, merely named as though it were the accepted one. Not a product concern. The lesson is naming: discarded captures must be self-identifying, or they read as contradictory evidence to anyone who did not watch them being taken.

**Two recurring Validator findings worth acting on**, both now seen across two consecutive runs: load-bearing claims backed only by inline prose rather than a saved artifact (checkpoint 5 here; checkpoint 14 in `bookmarks-jar-scoping`), and absence claims that need an explicit control to mean anything (checkpoint 4 here; checkpoints 8 and 13 there). Both are cheap to make standing requirements in the crew prompts rather than re-derived per run.

## Closing Summaries

*(the run closed at the final verdict; agents were released without a separate closing round)*

## Operator Notes

*(pending)*
