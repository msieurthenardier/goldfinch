# Behavior Test Run: chrome-guest-keyboard-nav — 2026-08-28-01-56-28

**Spec**: [chrome-guest-keyboard-nav.md](../chrome-guest-keyboard-nav.md)
**Status**: pass
**Started**: 2026-08-28T01:56:28Z
**Completed**: 2026-08-28T02:14:05Z
**Duration**: 00:17:37
**Executor**: sonnet sub-agent (live, SendMessage continuation)
**Validator**: sonnet sub-agent (live, SendMessage continuation)
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/chrome-guest-keyboard-nav/2026-08-28-01-56-28/` during the run; re-derivable by re-running the spec.

## Summary

**15 / 15 checkpoints passed.** 0 failed; 0 inconclusive. Rows 1–12 re-confirmed on the Leg 2 build; rows 13, 14 (DD6) and 15 (DD7) pass on first run — Leg 2 AC6 met.

Build under test: `flight/01-keyboard-reachability` with Leg 1 landed and Leg 2 (`chrome-to-guest-handoff`) implemented — DD6 (F6 / Shift+F6 from the guest via `crossViewNavAction` → `handleCrossView` forwarding the computed action), DD7 (Enter-on-commit one-shot in `navigation-controller.js`), DD9 (`tab-boundary.js` ESM; chrome backward path via the shared `tabSequence`). All 15 rows run — rows 1–12 as the regression net over the paths DD6/DD9 touched, rows 13–15 as Leg 2's acceptance (AC6).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: `getChromeTarget()`; `enumerateTabs()`.
- **Raw state**: C = wcId 1 (chrome, windowId 1); 12 restored tabs (wcIds 2–13, incl. stale `:8000`/`:8001` fixture tabs from runs 1–2), active wcId 13.
- **Expected**: numeric chrome wcId; this instance's tabs listed.
- **Verdict**: pass — litmus satisfied; leftover tabs noted, later rows judged by the wcId the Executor records per row.
- **Evidence**: `step-1-eval-chrome-target.json`, `step-1-eval-tabs.json`

### Checkpoint 2 — PASS
- **Actions taken**: `openTab` → `:8001/form.html` = **G = 14**; `activateTab(14)`; `readDom(14)` has `id="f3"`; collector armed → `armed`; blur → `BODY`.
- **Raw state**: fixture loaded (f1/f2/f3/b1 in order); collector `armed`; chrome `activeElement` `BODY`.
- **Expected**: fixture shown; `armed`; `BODY` baseline.
- **Verdict**: pass — all three confirmed from the cited evals.
- **Evidence**: `step-2-dom-G.txt`, `step-2-eval-arm.json`, `step-2-eval-baseline.json`

### Checkpoint 3 — PASS
- **Actions taken**: `pressKey(C,"F6")`; `readAxTree(G)`; `evaluate(G, tagName)`; `pressKey(G,"Tab")`; `evaluate(G, id)`; `typeText(G,"abc")`; `#f1.value`; screenshot.
- **Raw state**: after F6 `RootWebArea` focused, no control, `activeElement` `BODY`; after Tab `f1` (a11y: the "first field" textbox focused); `#f1.value === "abc"`.
- **Expected**: document-level landing (DD1); first Tab → `f1`; text lands.
- **Verdict**: pass — DOM and a11y agree at each sub-step; typed text proves entry.
- **Evidence**: `step-3-snapshot-G.txt`, `step-3-snapshot-G-2.txt`, `step-3-eval-*.json`, `step-3-screenshot-G.png`
- **Validator notes**: `RootWebArea` stays `focused` alongside the focused control on guests — the control node is *the* focused node; Executor reports name it explicitly from here on.

### Checkpoint 4 — PASS
- **Actions taken**: Tab, read id; Tab, read id; a11y after the second Tab.
- **Raw state**: `f2`, then `f3` (a11y: "third field" focused).
- **Expected**: `f2`, then `f3`.
- **Verdict**: pass.
- **Evidence**: `step-4-eval-id-1.json`, `step-4-eval-id-2.json`, `step-4-snapshot-G.txt`

### Checkpoint 5 — PASS
- **Actions taken**: Tab, read id; a11y.
- **Raw state**: `b1` (a11y: button "last" focused).
- **Expected**: `b1`.
- **Verdict**: pass.
- **Evidence**: `step-5-eval-id.json`, `step-5-snapshot-G.txt`

### Checkpoint 6 — PASS
- **Actions taken**: blur C → `BODY`; `pressKey(G,"Tab")` on `#b1`; read C active; `readAxTree(C)`; screenshot (before typing); collector; `typeText(C,"q")`; `#address.value`.
- **Raw state**: C `activeElement.id === "address"`; a11y focused node = textbox "Address and search bar" (sole); collector `["forward"]`; value `q`.
- **Expected**: `address` fresh gain; selected; `forward`; `q`.
- **Verdict**: pass — screenshot shows the URL selected (grey highlight) with **no** yellow ring: the boundary path's selection-only fingerprint.
- **Evidence**: `step-6-eval-active.json`, `step-6-snapshot-C.txt`, `step-6-screenshot-C.png`, `step-6-eval-tb.json`, `step-6-eval-address-value.json`

### Checkpoint 7 — PASS
- **Actions taken**: clear address + blur; `pressKey(C,"F6")`; `evaluate(G,id)`; `readAxTree(G)`; `readAxTree(C)`.
- **Raw state**: `b1`; G a11y: button "last" focused (RootWebArea also flagged); C a11y: **no** focused node.
- **Expected**: `b1` restored; C has no focused node.
- **Verdict**: pass — DD1 view-focus restore and F6 repeatability confirmed by the cross-view discriminator.
- **Evidence**: `step-7-eval-id.json`, `step-7-snapshot-G.txt`, `step-7-snapshot-C.txt`

### Checkpoint 8 — PASS
- **Actions taken**: Shift+Tab ×3 (`f3`, `f2`, `f1`); blur C → `BODY`; boundary Shift+Tab; read C active; `readAxTree(C)`; collector; screenshot.
- **Raw state**: C `activeElement.id === "bookmarks-overflow"`; a11y sole focused node button "More bookmarks"; collector `["forward","backward"]`.
- **Expected**: not `BODY`/`address`; chrome's last visible tabbable named; last entry `backward`.
- **Verdict**: pass — same landing as run 2, now through the shared `tabSequence(document)` (DD9); no ring visible on the chevron at capture size (known: squawk 0044), the a11y node is the rendered-state evidence.
- **Evidence**: `step-8-eval-ids.json`, `step-8-eval-active.json`, `step-8-snapshot-C.txt`, `step-8-eval-tb.json`, `step-8-screenshot-C.png`

### Checkpoint 9 — PASS
- **Actions taken**: `pressKey(C,"F6")`; blur → `BODY`; `pressKey(G,"l",["control"])`; read C active; `readAxTree(C)`; screenshot; `typeText(C,"z")`; value.
- **Raw state**: `address`; a11y: RootWebArea "Goldfinch" **and** the address textbox focused; value `z`.
- **Expected**: `address` fresh gain; `z`; yellow ring + selection fingerprint.
- **Verdict**: pass — screenshot compared against step 6: yellow ring around the address control plus blue selection (Ctrl+L path) vs selection-only (boundary path). The DD6 change to `crossViewNavAction` left Ctrl+L intact.
- **Evidence**: `step-9-eval-active.json`, `step-9-snapshot-C.txt`, `step-9-screenshot-C.png`, `step-9-eval-address-value.json`

### Checkpoint 10 — PASS
- **Actions taken**: `openTab` → `:8001/empty.html` = **E = 16**; activate; `readDom`; blur; `pressKey(C,"F6")`; `evaluate(E, tagName)`; `readAxTree(E)`; `pressKey(E,"Tab")`; C active; collector; `readAxTree(C)`.
- **Raw state**: E `BODY`, only `RootWebArea` focused; after Tab C `address` (sole a11y focused node, no RootWebArea flag); collector `["forward","backward","forward"]`.
- **Expected**: E document-focused; Tab → `address`; last entry `forward`.
- **Verdict**: pass — count-0 immediate handoff behaves as a boundary handoff.
- **Evidence**: `step-10-eval-tagname-E.json`, `step-10-snapshot-E.txt`, `step-10-eval-active-C.json`, `step-10-snapshot-C.txt`, `step-10-eval-tb.json`

### Checkpoint 11 — PASS
- **Actions taken**: `evaluate(C,"openJarsPage(); 'ok'")`; `enumerateTabs()` → **I = 17** (`goldfinch://jars/`, jarId `internal`); activate; blur; collector reset; `pressKey(C,"F6")`; `readAxTree(I)`; `pressKey(I,"Tab")`; `readAxTree(I)`; screenshot; `pressKey(I,"ShiftTab")`; C `{tb, active}`; `readAxTree(C)`.
- **Raw state**: after F6 only `RootWebArea` "Cookie Jars — Goldfinch" focused; after Tab the link "Personal DEFAULT" focused (ring visible in the screenshot); after Shift+Tab `{tb:["backward"], active:"kebab"}`; C a11y sole focused node button "More".
- **Expected**: fresh-document landing; a real control after Tab; `backward` + chrome's last visible tabbable.
- **Verdict**: pass — the internal bundle's listener is live (DD5), forwarded through `chromeForTab(I)`; `#kebab` is the last visible tabbable there because the bookmarks bar is suppressed on internal guests.
- **Evidence**: `step-11-snapshot-I.txt`, `step-11-snapshot-I-2.txt`, `step-11-screenshot-I.png`, `step-11-eval-tb-active.json`, `step-11-snapshot-C.txt`

### Checkpoint 12 — PASS (documentation row)
- **Actions taken**: `openTab` → `:8001/iframe.html` = **F = 18**; activate; `readDom`; blur; collector reset; `pressKey(C,"F6")`; tagName; Tab 1 id; Tab 2 top/inner probe; screenshot; Tab 3 id; C `{tb, active}`.
- **Raw state**: after F6 `BODY`; Tab 1 `top`; Tab 2 `{top:"fr", inner:"inner"}` (focus inside the same-origin frame, screenshot corroborates); **Tab 3: `top`** — wrapped inside the page; collector `[]`; C `activeElement` `BODY`.
- **Expected**: record the Tab-3 sequence verbatim; a `forward` entry would mean a subframe listener landed.
- **Verdict**: pass — the DD8 (C) residual reproduced exactly as on the Leg 1 build: no boundary event from the inner frame; no subframe preload on this build (Flight 2 / #147). Rows 13–14 supply the standard exit.
- **Evidence**: `step-12-eval-tab1.json`, `step-12-eval-tab2.json`, `step-12-screenshot-F.png`, `step-12-eval-tab3.json`, `step-12-eval-tb-active.json`

### Checkpoint 13 — PASS (Leg 2, DD6)
- **Actions taken**: `activateTab(14)`; blur → `BODY`; collector reset; `pressKey(C,"F6")` (re-entry landed on `f1`); `pressKey(G,"F6")`; C active; `readAxTree(C)`; screenshot; collector (Orchestrator addition); `typeText(C,"y")`; value.
- **Raw state**: C `activeElement.id === "address"`; a11y: RootWebArea "Goldfinch" and the address textbox focused; collector `[]`; value `y`.
- **Expected**: `address` fresh gain; a11y address textbox; yellow ring + selection; `y`; collector untouched.
- **Verdict**: pass — the screenshot shows the yellow ring plus selection (matches step 9, differs from step 6); the collector read-back closes the spec gap the Validator flagged at `[READY]`.
- **Evidence**: `step-13-eval-active.json`, `step-13-snapshot-C.txt`, `step-13-screenshot-C.png`, `step-13-eval-tb.json`, `step-13-eval-address-value.json`

### Checkpoint 14 — PASS (Leg 2, DD6)
- **Actions taken**: blur → `BODY`; `pressKey(C,"F6")`; `pressKey(G,"F6",["shift"])`; C active; `readAxTree(C)`; screenshot; collector.
- **Raw state**: C `activeElement === "bookmarks-overflow"`; a11y: RootWebArea "Goldfinch" **and** button "More bookmarks" focused; collector `[]`.
- **Expected**: neither `BODY` nor `address`; the last visible tabbable (row 8's element); collector unchanged.
- **Verdict**: pass — a11y names button "More bookmarks" (`#bookmarks-overflow`), the same element as row 8; the yellow ring on the chevron was visible in this capture; collector unchanged. Path note: RootWebArea is flagged alongside the button here (`handleCrossView` calls `chrome.focus()` first), unlike the boundary path — a possible DD6-vs-DD4 discriminator for a future revision.
- **Evidence**: `step-14-eval-active.json`, `step-14-snapshot-C.txt`, `step-14-screenshot-C.png`, `step-14-eval-tb.json`

### Checkpoint 15 — PASS (Leg 2, DD7)
- **Actions taken**: `activateTab(14)`; address focused and set to `http://127.0.0.1:8001/links.html` (`activeElement` `address`); collector reset; `pressKey(C,"Enter")`; `readDom(G)` (links.html on the first poll); `readAxTree(G)`; C tagName; `readAxTree(C)`; `pressKey(G,"Tab")`; id; `readAxTree(G)`; screenshot; collector.
- **Raw state**: post-commit G a11y: only `RootWebArea` "keyboard-nav: links" focused; C `activeElement.tagName === "BODY"` and C's a11y dump has zero focused nodes; after one Tab `l1` (a11y link "first"); collector `[]` throughout.
- **Expected**: document-level guest focus after commit; chrome `BODY`; one Tab → `l1`.
- **Verdict**: pass — guest document holds focus after commit (RootWebArea only), the chrome shows `BODY` **and** an a11y dump with zero focused nodes (the cross-view discriminator, resolving the Validator's `[READY]` concern about losing-view DOM state), one Tab reaches `l1` with a visible ring in the screenshot; `focusActiveGuest()` fired exactly once with no boundary event.
- **Evidence**: `step-15-snapshot-G.txt`, `step-15-eval-c-tagname.json`, `step-15-snapshot-C.txt`, `step-15-eval-tab-id.json`, `step-15-snapshot-G-2.txt`, `step-15-screenshot-G.png`, `step-15-eval-tb.json`

## Orchestrator Notes

- Mode: **live** two-agent (SendMessage continuation; both agents Sonnet); cache **cold** (fresh tabs G = 14, E = 16, I = 17, F = 18; the 12 restored tabs from runs 1–2 untouched). Build under test: `flight/01-keyboard-reachability` with Leg 1 landed and Leg 2 implemented (DD6, DD7, DD9; bundles rebuilt before launch; 3920 unit tests green); app launched with a fresh minted admin key; attach-only `gf.mjs`; **fixtures on :8001** (the foreign `:8000` squatter from run 2 persists — every `:8000` spec URL substituted; recorded deviation, not a fail).
- Full 15-row run rather than rows 13–15 alone: DD6 rewrote `crossViewNavAction`/`handleCrossView` (rows 9, 13, 14) and DD9 rerouted the chrome backward path through the shared `tabSequence` (rows 8, 11), so rows 1–12 are the regression net, not a formality.
- Orchestrator additions to the Actions during the run (folded into the spec after the run): row 13 reads `window.__tb` back after the F6/F6 sequence (the row asserted "collector untouched" without measuring it — Validator `[READY]` concern 1); row 15 reads `readAxTree(C)` after the commit (the losing view's DOM `BODY` alone is not evidence — concern 2). Both now in the spec's Actions column.
- No operator decisions were needed: 15/15.
- Apparatus facts this run: the chrome's RootWebArea is co-flagged `focused` with the landing control on the `handleCrossView` paths (Ctrl+L, F6, Shift+F6 — `chrome.focus()` first) and **not** on boundary handoffs (rows 6, 8, 10, 11 — control only): a repeatable DD6-vs-DD4 a11y discriminator alongside the ring-vs-no-ring screenshot fingerprint for `#address`; the chevron focus ring on `#bookmarks-overflow` *was* visible in row 14's capture though not row 8's — soften the crew note to "may not render visibly; a11y is primary"; wrapper quoting — build `evaluate` args containing single-quoted JS literals with `python3 -c 'json.dumps'` or double-quoted JSON; `readDom` output is JSON-escaped (`id=\"f3\"`) — grep for the bare id, not the quoted attribute. G's "last-held element" at F6 re-entry is whatever the page last focused (row 13 → `f1` because row 8 left G on `f1`), not the element at the previous handoff from that page.
- Teardown: `[CLOSING]` to the Executor immediately after its row-15 report, then the Validator after verdict 15; app and fixture server stopped; evidence dir (80 files) audited — no home path, no key; minted key file deleted.

## Closing Summaries

### Executor closing
All 15 rows executed via the attach-only caller; no apparatus failures; session-registered tools never invoked. Fresh tabs G=14, E=16, I=17 (via `openJarsPage()`), F=18; C=1. Every row matched its Expected Results including rows 13–15. Observations: row 11 lands on `#kebab` vs row 8's `#bookmarks-overflow` (bookmarks bar suppressed on internal guests — observed, cause not independently verified); row 13's re-entry landed on `f1` not `b1` (explained by the Orchestrator's row trace: row 8 left G on `f1`); Bash/JSON quoting of `evaluate` expressions with embedded single quotes needs `json.dumps` or double-quoted JSON; `readDom` output is JSON-escaped. No relaunches, no stale-tab reuse, no key printed; evidence scrubbed.

### Validator closing
15/15 with rendered-state evidence cited for every browser-frame claim; rows 1–12 clean re-confirmations; rows 13–15 held once two `[READY]` gaps were closed mid-run. Spec revisions: bake the `window.__tb` read into row 13's Actions; make `readAxTree(C)` a required Action in row 15; general authoring rule — a row asserting something did *not* happen needs its own instrumented read of that negative, not a prose claim (AUTHORING.md candidate). Apparatus: RootWebArea co-flagged with a control = standard gaining-view reporting; RootWebArea alone = fresh landing; no focused node on the losing side = OS focus genuinely left; the chevron ring is not reliably invisible at capture size — a11y stays primary; DD6-vs-DD4 chrome-side a11y fingerprint (RootWebArea co-flagged vs button-only) is a usable discriminator. No unresolved concerns.

## Operator Notes

AC6 met for Leg 2: DD6 (F6 / Shift+F6 from the guest, computed action forwarded), DD7 (Enter-on-commit one-shot → guest document focus), DD9 (shared `tabSequence` on the chrome backward path) all confirmed live; DD8 (C) residual reproduced verbatim on row 12 and stays the Mission 17 Known Issue for Flight 2 / #147. Carried forward: crew-note updates (ring visibility, quoting, JSON-escaped `readDom`, the co-flagged RootWebArea discriminator) for the flight-debrief crew-file squawk; the AUTHORING.md negative-assertion rule for the mission-control methodology list; squawk 0044 unchanged.
