# Behavior Test Run: chrome-guest-keyboard-nav — 2026-07-08-21-06-32

**Spec**: [tests/behavior/chrome-guest-keyboard-nav.md](../../chrome-guest-keyboard-nav.md)
**Status**: pass
**Started**: 2026-07-08T21:06:32
**Completed**: 2026-07-08T21:13:51
**Duration**: 00:07:19
**Executor**: behavior-test executor (single-agent, MCP loopback apparatus)
**Validator**: self-judged against observed state (no separate Validator agent spawned this run)

## Summary

6 / 6 judged steps passed (Step 2 is pure setup, no judgment). 0 failed; 0 inconclusive.
All three named cross-view gaps — (b) Ctrl+L from a focused guest, (a) Tab leaves the guest,
(c) chrome Tab-order wraps — plus the internal-tab Ctrl+L path (the route FD verification could
not reach) and address-bar **typeability** are confirmed on the running multi-`WebContentsView`
build. This is the first formal run of the flight's designated regression net for the cross-view
keyboard bridge (DD4/DD13); the fix is present and behaving.

Apparatus context: `npm run dev:automation` on `GOLDFINCH_MCP_PORT=8899`, admin identity.
Chrome renderer wcId **C = 1**; HTTP focus fixture (`http://127.0.0.1:8090/index.html`,
`#field` + `#btn`) opened as guest wcId **G = 3**; internal `goldfinch://settings` tab wcId
**I = 5**; menu-overlay sheet wcId **4** (probed). Keys redacted below as `<jar-key>` / `<admin-key>`.

## Step Results

### Step 1 — PASS
- **Actions taken**: `getChromeTarget()`; `enumerateTabs()`.
- **Raw state**: `getChromeTarget()` → `{"wcId":1,"kind":"chrome","url":"file://…/src/renderer/index.html"}` (numeric wcId). `enumerateTabs()` → single tab `{"wcId":2,"url":"https://www.google.com/","jarId":"default","active":true}` — this instance's default jar only, no foreign jar.
- **Expected**: numeric chrome wcId recorded as C; tabs are this instance's with no foreign jar.
- **Verdict**: pass — wiring litmus clean; C = 1. Preconditions met (DD2 halt not triggered).
- **Evidence**: [step1-getChromeTarget.json](./2026-07-08-21-06-32/step1-getChromeTarget.json), [step1-enumerateTabs.json](./2026-07-08-21-06-32/step1-enumerateTabs.json)

### Step 2 — SETUP (no judgment)
- **Actions taken**: `openTab("http://127.0.0.1:8090/index.html")` → G = 3; `activateTab(3)` → true; `readDom(3)` confirmed the fixture loaded (title "MCP Drive End-to-End Fixture", `#field`/`#btn` present); `getBoundingClientRect` for `#field` → center (553, 233); `click(3, 553, 233)` to give the guest OS keyboard focus.
- **Raw state**: fixture DOM served; field center resolved; clicks returned `{"ok":true}`.
- **Note (apparatus)**: the synthetic `click(3,…)` brought the guest view to OS focus but left DOM `activeElement` on `BODY` (a synthetic-input quirk in this multi-view apparatus — mouse click does not move DOM focus into the input). The spec explicitly permits `document.getElementById('field').focus()` as the equivalent focus-establishment (see Step 5 wording), used below as the baseline setter.

### Step 3 — PASS
- **Actions taken**: `evaluate(3, "…activeElement…")`; then `evaluate(3, "getElementById('field').focus(); …activeElement…")`.
- **Raw state**: post-click `activeElement` = `"BODY"`; after `.focus()` `activeElement` = `"field"` — the guest input holds keyboard focus (baseline for the cross-view checks).
- **Expected**: `evaluate(G,…)` returns the fixture's input element — guest view holds keyboard focus.
- **Verdict**: pass — guest baseline focus established on `#field` (via the spec-permitted `.focus()` after the click quirk).
- **Evidence**: [step3-guest-activeElement.json](./2026-07-08-21-06-32/step3-guest-activeElement.json)
- **Validator notes**: the click-vs-`.focus()` quirk is an apparatus property, not a product defect — it does not affect the bridge assertions, which turn on `pressKey(G,…)` delivering the synthetic accelerator to the guest `before-input-event` regardless.

### Step 4 — PASS (Gap (b): Ctrl+L from the guest)
- **Actions taken**: re-focused guest (`click(3,553,233)` + `.focus()` → `activeElement="field"`); `pressKey(3, "l", ["control"])`; read chrome via `evaluate(1,…)`, `readAxTree(1)`, `captureWindow()`; then proved typeability: `typeText(1, "example.com")` and re-read the `#address` value.
- **Raw state**: after Ctrl+L on the guest, chrome `evaluate(1,…)` `activeElement` = `"address"`. `readAxTree(1)` focused node = `{role:"textbox", name:"Address and search bar", editable:"plaintext", focused:true}`. `captureWindow` shows a gold focus ring around the whole address bar. `typeText(1,"example.com")` → `#address.value` = `"example.com"`, `activeElement` still `"address"`; a11y tree value updated to `"example.com"`.
- **Expected**: accelerator forwarded to chrome AND chrome view takes OS focus; address input is `activeElement` / AX focused node; focus ring visible; `typeText` lands (typeable, not merely DOM-focused). `[a11y]` focus on the address input.
- **Verdict**: pass — forwarded, focused, AND typeable. Ctrl+L is no longer dead when a guest holds focus. All three sub-observables (DOM focus, AX focused node, landed keystrokes) agree; pixels corroborate.
- **Evidence**: [step4-chrome-activeElement.json](./2026-07-08-21-06-32/step4-chrome-activeElement.json), [step4-address-value.json](./2026-07-08-21-06-32/step4-address-value.json), [step4-axtree.json](./2026-07-08-21-06-32/step4-axtree.json), [step4-captureWindow.png](./2026-07-08-21-06-32/step4-captureWindow.png)

### Step 5 — PASS (Gap (a): Tab leaves the guest)
- **Actions taken**: `pressKey(1,"Escape")` (clear address); re-focused guest (`click(3,…)` + `.focus()` → `activeElement="field"`); `pressKey(3, "Tab")`; read `evaluate(1,…)`, `evaluate(3,…)`, `captureWindow()`.
- **Raw state**: after Tab on the guest, chrome `activeElement` = `"address"`. Guest's own `activeElement` still reports `"field"` (documented cross-view semantics — Chromium dispatches no blur when OS focus leaves a sibling `WebContentsView`).
- **Expected**: keyboard focus lands on the chrome address bar (`id="address"`); load-bearing assertion is "chrome address focused", NOT "guest activeElement cleared".
- **Verdict**: pass — Tab traverses the guest→chrome boundary; chrome address focused. The inert guest `activeElement="field"` reading matches the spec's Note (F5); keystrokes go to chrome (proven by Step 4 typeability).
- **Evidence**: [step5-chrome-activeElement.json](./2026-07-08-21-06-32/step5-chrome-activeElement.json), [step5-guest-activeElement.json](./2026-07-08-21-06-32/step5-guest-activeElement.json), [step5-captureWindow.png](./2026-07-08-21-06-32/step5-captureWindow.png)

### Step 6 — PASS (Gap (c): chrome Tab-order wraps)
- **Actions taken**: from chrome focus (address), `pressKey(1,"Tab")` × 32, reading the focused control's id/aria-label via `evaluate(1,…)` after each press.
- **Raw state**: focus walked `address → zoom-out → zoom-in → zoom-reset → toggle-media → toggle-privacy → toggle-devtools → automation-indicator → kebab → media-rescan → media-close → (4× filter) → privacy-refresh → privacy-close → [tab title] → new-tab → new-tab-menu → win-min → win-max → win-close → reload → address-chip → **address (wrap, press 26)** → zoom-out → …` and continued cycling identically. Focus was **never** `null`/`BODY` at any of the 32 reads. (Media + privacy panels were open, contributing the extra ~10 buttons the spec anticipates.)
- **Expected**: Tab cycles chrome's focusable controls and wraps back to `address`; focus never stranded on `<body>`/`null`. `[a11y]` no focus trap, no lost focus.
- **Verdict**: pass — cycle wraps `address-chip → address` (matching the spec's observed F5 pattern) and repeats; no strand, no trap.
- **Evidence**: [step6-tab-walk.txt](./2026-07-08-21-06-32/step6-tab-walk.txt)

### Step 7 — PASS (Internal-tab Ctrl+L)
- **Actions taken**: opened the kebab (`evaluate(1,"getElementById('kebab').click()")` → `aria-expanded="true"`); probed the menu-overlay sheet wcId via `readDom` on candidate ids (4 → `menu-overlay.html`, "Menu overlay"; sheet = 4); confirmed the sheet menu (`data-menu-type="kebab"`, items Settings/Downloads/Print…/Exit, Settings focused on open); activated Settings via `evaluate(4, "…find(/settings/i).click()")`; `enumerateTabs()` recorded the internal tab **I = 5** (`goldfinch://settings/`, jarId `internal`); `activateTab(5)` + `click(5,500,400)` to hold focus in the internal view; read chrome baseline; `pressKey(5, "l", ["control"])`; observed **chrome only** via `evaluate(1,…)`, `readAxTree(1)`, `captureWindow()` (internal wcId NOT read — DD7).
- **Raw state**: chip flipped web → `internal`, `#address.value` = `"goldfinch://settings/"`, menu closed (Settings opened the internal tab). Chrome `activeElement` **before** Ctrl+L = `"kebab"` (not the address bar). After `pressKey(5, Ctrl+L)`: chrome `activeElement` = `"address"`; `readAxTree(1)` focused node = `{role:"textbox", name:"Address and search bar", value:"goldfinch://settings/", focused:true}`. No crash, no misroute.
- **Expected**: Ctrl+L focuses the address bar on the internal tab too (chrome-level accelerator via the minimal internal-guest `before-input-event`, Leg 2); no crash; trust boundary intact.
- **Verdict**: pass — internal-tab Ctrl+L focuses the chrome address bar. Baseline `activeElement="kebab"` before the press rules out a pre-focused address bar, so the transition is attributable to the accelerator. This is the path FD verification could not reach; it holds.
- **Evidence**: [step7-enumerateTabs.json](./2026-07-08-21-06-32/step7-enumerateTabs.json), [step7-chrome-before.json](./2026-07-08-21-06-32/step7-chrome-before.json), [step7-chrome-after.json](./2026-07-08-21-06-32/step7-chrome-after.json), [step7-axtree.json](./2026-07-08-21-06-32/step7-axtree.json), [step7-captureWindow.png](./2026-07-08-21-06-32/step7-captureWindow.png)

## Orchestrator Notes

- Single-agent run (Executor self-judging against observed state); no separate Validator or Accessibility Validator agent spawned. Judgments made on observed DOM `activeElement`, AX focused node, landed keystrokes, and window pixels — never on tool self-reports.
- Apparatus per the spec's Observables note: focus read via `evaluate(wc, "document.activeElement && (…id || …tagName)")` and `readAxTree`'s focused node — never `readDom` (which does not serialize `activeElement`). Typeability proven by `typeText` landing, not DOM focus alone.
- Sheet wcId (4) discovered by the documented probe walk (`readDom` on candidate ids until `menu-overlay.html` markup returned), consistent with the kebab-menu spec's non-enumerated-sheet note.
- Settings activation used `evaluate(sheetWcId, …click())` (DOM click) rather than raw coordinates, per the task's sheet-coordinate-offset gotcha (avoids the ~88px origin offset that can hit "Exit" and quit the app).

## Closing Summaries

### Executor closing
Clean run — all six judged steps passed on first attempt against the live merged build. One apparatus
quirk worth recording: synthetic `click` on a guest input does not move DOM `activeElement` into the
input (it lands on `BODY`); `.focus()` is the reliable focus-setter and is spec-sanctioned. This is
orthogonal to the bridge under test, which is driven by `pressKey` delivering the accelerator to the
target wcId's `before-input-event` regardless of DOM focus. The Step 4 `captureWindow` shows a clear
gold focus ring on the address bar (this build DOES paint the address-bar focus style under synthetic
focus, unlike the `:focus-visible` ring the kebab-menu run found unobservable on WSLg). No crashes, no
misroutes, no stranded focus across 32 chrome-Tab presses. The internal-tab Ctrl+L path (unreachable
by FD verification) passed with a `kebab`→`address` transition on a clean pre-press baseline.

### Validator closing
The spec's three-observable discipline for the address bar (DOM `activeElement` + AX focused node +
landed `typeText`) paid off — all three agreed at Step 4, making the "forwarded AND focused AND
typeable" claim airtight rather than resting on a single potentially-misleading signal. The spec's
explicit cross-view-semantics Note on Step 5 (guest `activeElement` may still read `field`) matched
observation exactly and prevented a false FAIL. Step 6's "never `null`/`BODY`" framing is the right
load-bearing assertion; the wrap point (`address-chip → address`) reproduced the F5 observation. No
spec drift observed; recommend promoting spec Status `draft → active`.

## Operator Notes
Environment: WSLg, admin identity, port 8899. Media + privacy toolbar panels were open during the run
(extra Tab stops in Step 6 — expected per spec). Incidental observation while probing for the internal
tab: `enumerateTabs` lists the internal `goldfinch://settings` tab (wcId 5, jarId `internal`) — this run used
the **admin** key, so that listing is admin's documented `allowInternal` relaxation (`resolve.js:104`), NOT a
regression. The jar-key exclusion was confirmed intact in Leg 3 + a dedicated security triage (the earlier
kebab-menu "regression" note was the same admin-vs-jar mis-labeling). Out of scope for this spec (which asserts
only the Ctrl+L bridge behavior); it did not affect any verdict here. App + fixture server torn down at run end.
