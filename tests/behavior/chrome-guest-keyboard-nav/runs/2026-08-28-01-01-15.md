# Behavior Test Run: chrome-guest-keyboard-nav — 2026-08-28-01-01-15

**Spec**: [chrome-guest-keyboard-nav.md](../chrome-guest-keyboard-nav.md)
**Status**: pass
**Started**: 2026-08-28T01:01:15Z
**Completed**: 2026-08-28T02:08:00Z
**Duration**: 01:07 (approx.)
**Executor**: Executor agent (general-purpose, live mode)
**Validator**: Validator agent (general-purpose, live mode)

## Summary

12 / 12 checkpoints passed; 0 failed; 0 inconclusive (row 12 as a documentation row). The re-authored DD1 contract (F6 → document focus; Tab enters; re-entry restores the last element) is the observed behaviour; forward and backward boundary handoffs are delivered and acted on (collector + focus + rendered state), the no-tabbables path hands off immediately, Ctrl+L is intact, and the internal jars page proves the bundled internal preload's listener is live. Documented for Leg 2: a trailing same-origin iframe still wraps forward Tab inside the page.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: `getChromeTarget()` → `{wcId:1, kind:'chrome', windowId:1}`; `enumerateTabs()` → nine restored dev-profile tabs (2–10; 8–10 are the prior run's :8000 fixture tabs titled "Error response", not reused).
- **Expected**: numeric chrome wcId; this instance's tabs.
- **Verdict**: pass — matches the Validator's own pre-`[READY]` litmus.
- **Evidence**: `step-1-eval-chrome-target.txt`, `step-1-eval-tabs.txt`

### Checkpoint 2 — PASS
- **Actions taken**: `openTab` form.html (:8001) → **G = 11**; `activateTab`; `readDom` shows `#f1 #f2 #f3 #b1`; collector armed on the chrome (`window.__tb` via `window.goldfinch.onTabBoundary`) → `armed`; `#address.blur()` → `BODY`.
- **Verdict**: pass — neutral chrome baseline; G's a11y tree has no focused node before F6.
- **Evidence**: `step-2-readdom-G.txt`, `step-2-eval-arm-C.txt`, `step-2-eval-blur-C.txt`, `step-2-axtree-G.txt`, `step-2-screenshot-C.png`

### Checkpoint 3 — PASS
- **Actions taken**: F6 on C; `readAxTree(G)`; `evaluate(G, activeElement.tagName)`; Tab on G; `evaluate(G, activeElement.id)`; `typeText(G,'abc')`; `#f1.value`; a11y + screenshot.
- **Raw state**: after F6 the only focused node is the `RootWebArea` and `activeElement` = `BODY`; after one Tab `f1`; `#f1.value` = `abc`; a11y 'first field' focused with value; screenshot: `abc` with caret and ring in the first input.
- **Expected**: DD1 Chrome-like landing (document focus), first Tab → `f1`, text lands.
- **Verdict**: pass — the re-authored contract is the observed behaviour; the `RootWebArea focused` flag is a clean discriminator (absent pre-F6, present post-F6).
- **Evidence**: `step-3-axtree-G-after-f6.txt`, `step-3-eval-G-after-f6.txt`, `step-3-eval-G-after-tab.txt`, `step-3-eval-G-f1-value.txt`, `step-3-axtree-G-after-type.txt`, `step-3-screenshot-G.png`

### Checkpoint 4 — PASS
- **Actions taken**: Tab → `f2`; Tab → `f3`; a11y 'third field' focused; ring on the third input.
- **Verdict**: pass — mid-page Tab is Chromium's; no ejection.
- **Evidence**: `step-4-eval-G-tab1.txt`, `step-4-eval-G-tab2.txt`, `step-4-axtree-G.txt`, `step-4-screenshot-G.png`

### Checkpoint 5 — PASS
- **Actions taken**: Tab → `b1`; a11y button 'last' focused; ring on the button. Validator's fresh read: collector still `[]` after four in-page Tabs.
- **Verdict**: pass.
- **Evidence**: `step-5-eval-G.txt`, `step-5-axtree-G.txt`, `step-5-screenshot-G.png`

### Checkpoint 6 — PASS
- **Actions taken**: baseline `#address.blur()` → `BODY`; Tab from `#b1`; C `activeElement` → `address`; `readAxTree(C)` only focused node = address textbox; collector `["forward"]`; screenshot: full-URL selection, no yellow ring; `typeText(C,'q')` → value `q` (replaced).
- **Verdict**: pass — forward boundary handoff delivered (collector) and acted on (focus + select + typed text).
- **Evidence**: `step-6-eval-C-baseline.txt`, `step-6-eval-C-active.txt`, `step-6-axtree-C.txt`, `step-6-eval-C-tb.txt`, `step-6-screenshot-C.png`, `step-6-eval-C-address-value.txt`, `step-6-screenshot-C-after-type.png`

### Checkpoint 7 — PASS
- **Actions taken**: setup clears + blurs `#address` (IIFE); F6; G `activeElement` → `b1`; a11y 'last' focused; screenshot byte-identical to checkpoint 5's.
- **Verdict**: pass — re-entry restores the last-held element (DD1). Validator's fresh cross-view read: G `RootWebArea` focused / `hasFocus:true`, C no focused node / `hasFocus:false`, collector unchanged — a real re-entry, not the stale pre-F6 state.
- **Evidence**: `step-7-eval-C-setup.txt`, `step-7-eval-G.txt`, `step-7-axtree-G.txt`, `step-7-screenshot-G.png`
- **Validator notes**: add `readAxTree(C)` (expect no focused node) to row 7 next revision — the row's own reads can't distinguish re-entry from "F6 did nothing".

### Checkpoint 8 — PASS
- **Actions taken**: Shift+Tab ×3 → `f3`, `f2`, `f1`; baseline → `BODY`; Shift+Tab from `#f1`; C `activeElement` → `bookmarks-overflow`; `readAxTree(C)` only focused node = button 'More bookmarks'; collector `["forward","backward"]`; detail: `{tag:BUTTON, id:bookmarks-overflow, label:'More bookmarks', visible:'visible'}`.
- **Verdict**: pass — the prior run's product defect is fixed: the backward event is delivered *and* acted on; the Validator independently computed the chrome's visible tabbable sequence (27 of 57) and confirmed `#bookmarks-overflow` is last; the chevron is rendered at the live rect.
- **Evidence**: `step-8-eval-G-shifttab{1,2,3}.txt`, `step-8-eval-C-baseline.txt`, `step-8-eval-C-active.txt`, `step-8-axtree-C.txt`, `step-8-eval-C-tb.txt`, `step-8-screenshot-C.png`, `step-8-eval-C-active-detail.txt`, `step-8-validator-crop-chevron.png`
- **Validator notes**: **the focused chevron draws no visible focus indicator** (computed `outline: none`; 8× crop shows a plain glyph) — WCAG 2.4.7 Focus Visible (AA), outside this flight's Level-A targets; recommend a squawk for `:focus-visible` on the toolbar buttons.

### Checkpoint 9 — PASS
- **Actions taken**: F6 (re-entry at `f1`); baseline `#address.blur()` → **`BUTTON`** (the chevron still held chrome DOM focus — F6 doesn't clear the chrome's `activeElement`); Ctrl+L on G; C `activeElement` → `address`; a11y: `RootWebArea` + address textbox focused; screenshot: yellow ring + selection; `typeText(C,'z')` → value `z`.
- **Verdict**: pass — `BUTTON → address` is a fresh gain; the pre-existing Ctrl+L bridge is intact; the ring-plus-selection fingerprint distinguishes this path from the boundary handoff.
- **Evidence**: `step-9-eval-G-after-f6.txt`, `step-9-eval-C-baseline.txt`, `step-9-eval-C-active.txt`, `step-9-axtree-C.txt`, `step-9-screenshot-C.png`, `step-9-eval-C-address-value.txt`, `step-9-screenshot-C-after-type.png`
- **Validator notes**: baselines should blur `document.activeElement`, not just `#address` (adopted from row 10 onward by Orchestrator instruction; to be folded into the spec). The chrome's `RootWebArea` is flagged focused on the Ctrl+L path (`chrome.focus()` at the webContents level) but not on the boundary handoff — a hint for Leg 2.

### Checkpoint 10 — PASS
- **Actions taken**: `openTab` empty.html → **E = 13** (wcId 12 skipped by a transient WebContents); amended baseline → `BODY`; F6 → E `activeElement` `BODY`, `RootWebArea` the only focusable/focused node; Tab on E → C `activeElement` `address`; collector `[…,"forward"]`; a11y address textbox focused; screenshot: selection-only.
- **Verdict**: pass — `count === 0` immediate handoff end to end.
- **Evidence**: `step-10-readdom-E.txt`, `step-10-eval-C-baseline.txt`, `step-10-eval-E-after-f6.txt`, `step-10-axtree-E.txt`, `step-10-eval-C-active.txt`, `step-10-eval-C-tb.txt`, `step-10-axtree-C.txt`, `step-10-screenshot-C.png`

### Checkpoint 11 — PASS
- **Actions taken**: `evaluate(C, "openJarsPage()")` → **I = 14** (`goldfinch://jars/`); baseline `BODY`; collector reset; F6 → `readAxTree(I)` `RootWebArea` only; Tab → link 'Personal DEFAULT' focused; Shift+Tab → collector `["backward"]`, C `activeElement` `kebab`; `readAxTree(C)` only focused node = button 'More'; no `evaluate` against I.
- **Verdict**: pass — **DD5 proven live**: a backward boundary event from the internal tab reached the chrome on a freshly reset collector, and the chrome placed focus on its last visible tabbable (the bookmarks bar is `display:none` on internal tabs, so the kebab is last of 12 — Validator's fresh computation).
- **Evidence**: `step-11-eval-tabs.txt`, `step-11-eval-C-baseline.txt`, `step-11-eval-C-reset.txt`, `step-11-axtree-I-after-f6.txt`, `step-11-axtree-I-after-tab.txt`, `step-11-eval-C-tb-active.txt`, `step-11-axtree-C.txt`, `step-11-screenshot-C.png`, `step-11-screenshot-I.png`
- **Validator notes**: same 2.4.7 concern on `#kebab` (computed `outline: none`); '+ New jar' appears before 'Personal DEFAULT' in the a11y dump yet is not first in the Tab sequence — worth a glance in a jars-page keyboard audit.

### Checkpoint 12 — PASS (documentation row)
- **Actions taken**: `openTab` iframe.html → **F = 15**; baseline `BODY`; collector reset; F6; three Tabs with top/inner reads (same-origin `contentDocument.activeElement`); collector; a11y; screenshot.
- **Raw state**: F6 → `BODY`; Tab 1 → `top`; Tab 2 → `{top:'fr', inner:'inner'}` (focus inside the frame); Tab 3 → `{top:'top', inner:'BODY'}`, collector `[]`, chrome `BODY`.
- **Verdict**: pass (documented) — the trailing same-origin iframe wraps forward Tab back to `#top` with no boundary event; identical to run 1. One-directional trap (Shift+Tab from `#top` does reach the chrome via row 8's path). Leg 2 Known Issue; a `forward` entry after Tab 3 is the pass signal for either candidate fix.
- **Evidence**: `step-12-readdom-F.txt`, `step-12-eval-F-after-f6.txt`, `step-12-eval-F-tab1.txt`, `step-12-eval-F-tab2.txt`, `step-12-eval-F-tab3.txt`, `step-12-eval-C-tb-active.txt`, `step-12-axtree-F.txt`, `step-12-screenshot-F.png`

## Orchestrator Notes

- Mode: **live** two-agent; cache **cold** (fresh tabs 11, 13, 14, 15; restored 2–10 untouched). Build under test: `flight/01-keyboard-reachability` Leg 1 + the two run-1 fixes (`shortcut-controller.js` and `tab-boundary.js` visibility via `getComputedStyle`), bundles rebuilt 20:01, app launched 20:01 with a fresh minted admin key; attach-only `gf.mjs`; **fixtures served on :8001** — a foreign `SimpleHTTP/0.6 Python/3.11.9` listener squats on :8000 (404 for every path), invisible to this environment's process table and unkillable from it; recorded deviation, every spec URL substituted.
- Spec re-authored before this run to the operator's DD1 ruling (Chrome-like landing), with the `onTabBoundary` collector armed in row 2 and row 11 moved to the jars page. Baselines were amended mid-run (from row 10) to blur `document.activeElement` rather than `#address` after row 9 showed the chrome's `activeElement` persists across F6 — folded into the spec after the run.
- No operator decisions were needed: 12/12. Both prior product/spec/apparatus findings are closed; the trailing-iframe trap is documented for Leg 2.
- Apparatus facts added this run (Executor + Validator closings): the spec's `#address.blur()` baseline is a no-op when another chrome control holds DOM focus — blur `document.activeElement`; activating another tab re-syncs the address bar and discards uncommitted typed text; `RootWebArea focused` is a clean view-focus discriminator on guests, but on the chrome it appears on the Ctrl+L path and not on boundary handoffs — key on the control node; the stale-`activeElement` fact applies to the chrome too; "last visible tabbable" is verifiable read-only by computing the filtered sequence via `evaluate(C)`; focus rings on small toolbar chevrons are not distinguishable at 1400×900 — the a11y focused node is the rendered-state evidence; `openTab` wcIds are not contiguous (12 was skipped).
- Teardown: `[CLOSING]` to the Executor right after its row-12 report, then the Validator; app and fixture server stopped; evidence dir (76 files) scrubbed; minted key file deleted.

## Closing Summaries

### Executor closing
Apparatus worked end to end; no session MCP tool; no `evaluate` against the internal guest; nothing written into the project tree. Hiccups: the :8000 squatter (fixtures re-served on :8001); `openTab` skipped wcId 12. Dispatch: every F6 on a fresh page landed with `activeElement` BODY and the RootWebArea alone focused (rows 3, 10, 11, 12); re-entry landed on the last-held element (row 7 → b1, row 9 → f1). Forward handoffs (rows 6, 10) reached `#address` with a `forward` entry, selection-only; Ctrl+L (row 9) yellow ring + selection. Backward handoffs reached `#bookmarks-overflow` (web) and `#kebab` (internal, bookmarks bar absent) with `backward` entries. Iframe row: Tab 3 wrapped to `#top`, collector empty. New apparatus facts: blur `document.activeElement` for baselines; tab activation re-syncs the address bar; don't key on the chrome's RootWebArea focused flag; chevron focus rings aren't visible at capture size; never predict wcIds. 13 tabs left open; `window.__tb` installed on C.

### Validator closing
12/12 (row 12 as documentation). All three run-1 causes closed: rows 3/7 by the DD1 re-authoring (observed exactly); row 8 by a real fix (backward handoff delivered and acted on); row 11 by the quiet jars page (DD5 proven). Row 12 reproduced the trailing-iframe wrap verbatim — Leg 2's fix not landed, as expected. Squawk candidate: WCAG 2.4.7 — `#bookmarks-overflow` and `#kebab` have `outline: none` when focused; an Accessibility Validator would have failed rows 8 and 11 on it. Spec revisions: baselines blur `document.activeElement`; row 7 adds `readAxTree(C)` (no focused node); row 12 screenshots after Tab 2; row 11 notes '+ New jar' is focusable but not first in the sequence. Observability: RootWebArea-focused is a guest-side discriminator only; `hasFocus()` split across views is informative; stale `activeElement` applies to the chrome; compute "last visible tabbable" read-only via `evaluate(C)`; keep the amended baseline and the per-row collector reset.

## Operator Notes

AC6 met. Leg 1 lands: DD1 (Chrome-like, operator-ruled), DD2, DD3, DD4 (with the `getComputedStyle` fix), DD5 all confirmed live. Carried forward: the trailing-iframe one-directional trap (Mission 17 Known Issue → Flight 1 Leg 2); WCAG 2.4.7 focus indicator on toolbar buttons (squawk 0044); the `tab-boundary.js`/chrome duplication (Leg 2 unifies via ESM per the DD4 amendment); the jars-page '+ New jar' order note; the chrome RootWebArea-focused asymmetry between the Ctrl+L and handoff paths (Leg 2 glance).
