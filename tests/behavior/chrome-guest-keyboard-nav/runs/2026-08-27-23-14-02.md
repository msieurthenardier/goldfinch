# Behavior Test Run: chrome-guest-keyboard-nav — 2026-08-27-23-14-02

**Spec**: [chrome-guest-keyboard-nav.md](../chrome-guest-keyboard-nav.md)
**Status**: fail
**Started**: 2026-08-27T23:14:02Z
**Completed**: 2026-08-28T00:22:00Z
**Duration**: 01:08 (approx.)
**Executor**: Executor agent (general-purpose, live mode)
**Validator**: Validator agent (general-purpose, live mode)

## Summary

8 / 12 checkpoints passed. 4 failed (3, 7, 8, 11); 0 inconclusive. Row 12 passed as a documentation row. Forward boundary handoff proven end to end (rows 6, 10, diag-7); F6 moves OS focus into the guest but lands on the document / last-focused element, not the first tabbable (rows 3, 7 — spec premise); backward handoff delivered to the chrome but not acted on (row 8 — product defect, localized by diag-8); internal-page row confounded by the Settings audit log (row 11 — open); trailing-iframe keyboard trap documented (row 12).

## Checkpoint Results

### Checkpoint 1 — PASS
- **Actions taken**: `getChromeTarget()` → `{wcId:1, kind:'chrome', windowId:1}`; `enumerateTabs()` → six restored dev-profile tabs (wcIds 2–7, window 1, wcId 7 active).
- **Raw state**: C = 1; no internal tabs open.
- **Expected**: numeric chrome wcId; this instance's tabs listed.
- **Verdict**: pass — matched the Validator's own pre-`[READY]` enumeration (same instance, same wcIds).
- **Evidence**: `step-1-eval-chrome-target.txt`, `step-1-eval-tabs.txt`

### Checkpoint 2 — PASS
- **Actions taken**: `openTab` form.html → **G = 8**; `activateTab(8)`; `readDom(8)` shows the fixture (`#f1 #f2 #f3 #b1`); `evaluate(C, focus #address; activeElement.id)`.
- **Raw state**: `address`; G's a11y tree: four focusable controls, only the RootWebArea focused.
- **Expected**: fixture loaded; chrome holds focus in the address bar.
- **Verdict**: pass — programmatic baseline (DOM focus, not OS focus — noted as such).
- **Evidence**: `step-2-readdom-G.txt`, `step-2-eval-C-activeElement.txt`, `step-2-screenshot.png`, `step-2-axtree-G.txt`

### Checkpoint 3 — FAIL
- **Actions taken**: `pressKey(C,'F6')` → ok; `evaluate(G, activeElement.id)`; `readAxTree(G)`; `typeText(G,'abc')`; `evaluate(G, #f1.value)`; screenshot.
- **Raw state**: G `activeElement` = `BODY` (id `""`); a11y: RootWebArea focused, no control focused; `#f1.value` = `""`; typed text landed nowhere (address bar unchanged); supplementary `document.hasFocus()` on G = true.
- **Expected**: `f1`, and `#f1.value` = `abc`.
- **Verdict**: fail — F6 was accepted and (per diag-3 below) moved OS focus to the guest, but the document's `activeElement` stayed `BODY`: the row's condition "Chromium places initial focus on the first tabbable" is false for `webContents.focus()`.
- **Evidence**: `step-3-eval-G-activeElement.txt`, `step-3-eval-G-f1-value.txt`, `step-3-axtree-G-after-f6.txt`, `step-3-screenshot-G.png`, `step-3-eval-G-supplementary.txt`
- **Operator decision**: continued — with the Validator's discriminator inserted as diag-3 (one Tab from `BODY`).

### Checkpoint 4 — PASS
- **Actions taken**: diag-3: `pressKey(G,'Tab')` from `BODY` → `activeElement` = `f1` (a11y: 'first field' focused) — the sequential-focus starting point was inside the guest, i.e. F6 did hand OS focus to the guest (case B). Row 4: Tab → `f2`; Tab → `f3`; a11y 'third field' focused; screenshot shows the orange ring on the third input.
- **Raw state**: `f2`, then `f3`; no ejection to the chrome.
- **Expected**: `f2`, then `f3`.
- **Verdict**: pass — mid-page Tab is Chromium's again; no `'tab-handoff'`.
- **Evidence**: `step-4-diag-axtree-G.txt`, `step-4-eval-G-activeElement-1.txt`, `step-4-eval-G-activeElement-2.txt`, `step-4-axtree-G.txt`, `step-4-screenshot-G.png`

### Checkpoint 5 — PASS
- **Actions taken**: Tab from `#f3`.
- **Raw state**: `b1`; a11y button 'last' focused; ring on the button.
- **Expected**: `b1`.
- **Verdict**: pass.
- **Evidence**: `step-5-eval-G-activeElement.txt`, `step-5-axtree-G.txt`, `step-5-screenshot-G.png`

### Checkpoint 6 — PASS
- **Actions taken**: setup-6: `blur()` `#address` so C's DOM focus was `BODY` (pre-state selection `[31,31]`). Row: Tab from `#b1`; `evaluate(C, activeElement.id)`; `readAxTree(C)`; screenshot; `typeText(C,'q')`; `#address.value`.
- **Raw state**: C `activeElement` = `address` (fresh gain from `BODY`); a11y: textbox 'Address and search bar' focused; screenshot: full-URL selection band; `typeText` replaced the whole value → `q`.
- **Expected**: `address`, value contains `q`.
- **Verdict**: pass — a key on the guest changed the chrome's focus: the forward boundary handoff end to end.
- **Evidence**: `step-6-setup-eval-C-blur.txt`, `step-6-eval-C-activeElement.txt`, `step-6-axtree-C.txt`, `step-6-screenshot-C.png`, `step-6-eval-C-address-value.txt`, `step-6-screenshot-C-after-type.png`
- **Validator notes**: chrome a11y dumps embed `file://` home paths — scrubbed to `<user>` in place from here on. A top-level `const` in an `evaluate` expression persists in the view's main world (later expressions wrapped in IIFEs).

### Checkpoint 7 — FAIL
- **Actions taken**: setup (IIFE): clear + blur `#address` → `BODY`; `pressKey(C,'F6')`; `evaluate(G, activeElement.id)`; a11y; screenshot. diag-7: one Tab from the landing element.
- **Raw state**: after F6, G `activeElement` = `b1` — the element the document last held; a11y button 'last' focused; ring on the button. diag-7: Tab from `#b1` → C `activeElement` = `address`, and the cleared address value repopulated with the tab's URL.
- **Expected**: `f1`.
- **Verdict**: fail — F6 is repeatable (it worked a second time) but re-entry restores the document's last-focused element, not the first tabbable. Standard Chromium view-focus behaviour; the spec's entry contract is what's wrong.
- **Evidence**: `step-7-eval-C-prestate.txt`, `step-7-eval-G-activeElement.txt`, `step-7-axtree-G.txt`, `step-7-screenshot-G.png`, `step-7-diag-eval-C-after-tab.txt`
- **Operator decision**: continued.

### Checkpoint 8 — FAIL
- **Actions taken**: setup-8: F6 (→ `b1`), blur C (→ `BODY`), three mid-page Shift+Tabs `b1 → f3 → f2 → f1`. Row: Shift+Tab from `#f1`; `evaluate(C, activeElement)`; `readAxTree(C)`; screenshot; guest reads.
- **Raw state**: C `activeElement` = `BODY`; C a11y: no focused node; no ring on any chrome control. G: focus stayed on `#f1` (a11y 'first field' focused, ring on the first input) — neither wrapped to `#b1` nor cleared.
- **Expected**: the chrome's last visible tabbable focused.
- **Verdict**: fail — the guest half fired (`Shift+Tab` at the boundary was `preventDefault`ed: focus stayed on `#f1`, which Chromium's default never produces) but the chrome gained nothing. A dead key for a keyboard user on this build.
- **Evidence**: `step-8-setup-eval-G-shifttab-3.txt`, `step-8-eval-C-activeElement.txt`, `step-8-axtree-C.txt`, `step-8-screenshot-C.png`, `step-8-axtree-G.txt`, `step-8-screenshot-G.png`
- **Operator decision**: continued — with diag-8 (a collector on the chrome's `onTabBoundary` bridge) inserted before row 9.

### Checkpoint 9 — PASS
- **Actions taken**: diag-8: `window.goldfinch` exposes `focusActiveGuest` and `onTabBoundary`; collector installed; repeat Shift+Tab from `#f1` → collector received exactly one `{"direction":"backward"}`; C `activeElement` still `BODY`. Row 9: F6 (re-entry at `#f1`, the last-held element); blur C; `pressKey(G,'l',['control'])`; reads; `typeText(C,'z')`.
- **Raw state**: C `activeElement` = `address`; a11y textbox focused; screenshot: bright yellow focus ring + full selection (`[0,31]`); value → `z`.
- **Expected**: `address`, value contains `z`.
- **Verdict**: pass — the pre-existing Ctrl+L bridge is intact.
- **Evidence**: `step-9-eval-C-prestate.txt`, `step-9-eval-C-activeElement.txt`, `step-9-axtree-C.txt`, `step-9-screenshot-C.png`, `step-9-eval-C-address-value.txt`, `step-9-screenshot-C-after-type.png`; diag-8: `step-9-diag-eval-C-after.txt`
- **Validator notes**: diag-8 localizes the row-8 defect to the chrome's backward handler — preload detection, IPC, main forwarding and chrome receipt all work; the handler focuses nothing. Ctrl+L focus draws a yellow `:focus-visible` ring; the boundary handoff draws selection only — two different focus mechanisms.

### Checkpoint 10 — PASS
- **Actions taken**: `openTab` empty.html → **E = 10**; `activateTab`; `readDom` contains 'No focusable elements'; blur C; F6; `evaluate(E, activeElement.tagName)`; `readAxTree(E)`; Tab on E; `evaluate(C, activeElement.id)`; a11y; screenshot; collector.
- **Raw state**: after F6 E `activeElement` = `BODY`, a11y RootWebArea focused, no focusable control; after Tab C `activeElement` = `address`, a11y textbox focused, selection band; collector gained one `{"direction":"forward"}`.
- **Expected**: `BODY`, then `address`.
- **Verdict**: pass — the cleanest end-to-end forward demonstration (`count === 0` → immediate handoff).
- **Evidence**: `step-10-readdom-E.txt`, `step-10-eval-E-activeElement.txt`, `step-10-axtree-E.txt`, `step-10-eval-C-activeElement.txt`, `step-10-axtree-C.txt`, `step-10-screenshot-C.png`, `step-10-eval-C-tb.txt`

### Checkpoint 11 — FAIL
- **Actions taken**: `evaluate(C, "kebabActionSettings()")` → **I = 11** (`goldfinch://settings/`, jar internal); `activateTab`; blur C; F6; `readAxTree(I)`; 40 × [Tab on I; `evaluate(C, activeElement.id)`], a11y sampled every 5; collector at the stop. No `evaluate` against I.
- **Raw state**: after F6 only the RootWebArea focused (39 focusable controls present). Tabs 1–25 walked real controls in order (Home page → Restore session → Shields → Spellcheck → Find free port); from Tab 30 no control focused at all; C `activeElement` `""` on all 40 reads; collector unchanged (no event). Settings' 'Recent actions' log re-rendered on every admin call (pagination 7 → 12 pages during the loop).
- **Expected**: a real control focused after F6; `address` within ~40 Tabs.
- **Verdict**: fail — (i) document-level landing again; (ii) no boundary event in 40 presses. Confounded: the walk never demonstrably reached the last tabbable — focus was dropped to the document once inside the Automation section, whose live audit log the run's own calls kept re-rendering.
- **Evidence**: `step-11-axtree-I-after-f6.txt`, `step-11-eval-tab-loop.txt`, `step-11-axtree-I-tab-25.txt`, `step-11-axtree-I-tab-30.txt`, `step-11-axtree-C.txt`, `step-11-screenshot-I.png`, `step-11-eval-C-tb.txt`
- **Operator decision**: continued — with diag-11 (one Tab, one Shift+Tab on I, collector reset) to test the internal listener independently of the log confound.

### Checkpoint 12 — PASS (documentation row)
- **Actions taken**: diag-11 (collector reset; F6 on I; one Tab → no control focused; one Shift+Tab → button 'Revoke' focused; collector empty — uninformative: a mid-page backward move inside the Automation section, whose audit log the run's own calls re-render). Row: `openTab` iframe.html → **F = 12**; blur C; F6; three Tabs with reads; collector; a11y; screenshots.
- **Raw state**: after F6 F `activeElement` = `BODY`; Tab 1 → `#top`; Tab 2 → the `<iframe id="fr">` element (focus inside the frame; the top document reports the iframe); Tab 3 → **`#top` again**, C `activeElement` = `BODY`, collector `[]` — no boundary event. The inner document (`#inner`) never appears in F's a11y tree; the Validator's fresh same-origin `contentDocument.activeElement` read = `BODY`.
- **Expected**: documents; does not gate.
- **Verdict**: pass (documented) — **forward Tab past a trailing iframe wraps to the top document's first tabbable with no exit**: the keydown fires in the inner frame where the top-frame preload's listener does not run; Blink exits the frame, finds nothing after `#fr`, offers focus to the embedder, is declined, and wraps. With the backward handoff also broken (row 8), the page is a keyboard trap (WCAG 2.1.2 shape) whose only exits are Ctrl+L / F6.
- **Evidence**: `step-12-readdom-F.txt`, `step-12-eval-F-after-f6.txt`, `step-12-eval-F-after-tab1.txt`, `step-12-eval-F-after-tab2.txt`, `step-12-eval-C-after-tab3.txt`, `step-12-eval-F-detail-tab3.txt`, `step-12-eval-C-tb.txt`, `step-12-axtree-F.txt`, `step-12-screenshot-F.png`, `step-12-screenshot-C.png`; diag-11: `step-12-diag-axtree-I-after-tab.txt`, `step-12-diag-eval-C-after-shifttab.txt`

## Orchestrator Notes

- Mode: **live** two-agent (SendMessage available); cache mode **cold** (every fixture row opened a fresh tab; restored dev-profile tabs 2–7 untouched). Apparatus: attach-only `gf.mjs` wrapper over `scripts/lib/mcp-client.mjs` with the run's minted admin key (`GOLDFINCH_AUTOMATION_ADMIN=1 GOLDFINCH_AUTOMATION_DEV_MINT=1 npm run dev:automation`, port 49707); fixtures served from `tests/behavior/fixtures/keyboard-nav/` on `:8000`. Session-registered `mcp__goldfinch*` tools were not used (stale keys; both failed to connect this session). Build under test: `flight/01-keyboard-reachability` Leg 1 (uncommitted), bundles built 18:13, app launched 18:14 — `internal-preload.bundle.js` confirmed present, containing the boundary listener, and referenced by `main.js:1548`.
- Five Orchestrator diagnostics were inserted, each recorded separately from its row: diag-3 (one Tab from `BODY` → `#f1`: F6 had moved OS focus into the guest — case B), setup-6 (blur C so the chrome read is discriminating), diag-7 (Tab from the re-entry element → forward handoff again), setup-8 (re-establish `#f1`), diag-8 (collector on `window.goldfinch.onTabBoundary` → the `backward` event IS delivered), diag-11 (uninformative — see checkpoint 12).
- Operator decisions: checkpoints 3, 7, 8, 11 failed → **continued** each time (the Flight Director, under the operator's standing "decide, tell me, keep moving"), because the remaining rows tested the other half of the spike's question and each failure's cause was already isolated.
- Failure taxonomy (Validator's, adopted): rows 3 and 7 — **spec premise** ("Chromium places initial focus on the first tabbable" is false; F6 → document focus on a fresh page, last-focused element on re-entry — standard Chromium); row 8 — **product defect** in the chrome's backward handler (delivery proven by diag-8); row 11 — **apparatus confound** (Settings' live 'Recent actions' log re-renders on every admin call; the internal listener's liveness remains OPEN) ; row 12 — **documented keyboard trap** (trailing iframe).
- Apparatus facts for the crew file (Executor + Validator closings): top-level `const` in `evaluate` persists per view — use IIFEs; chrome a11y dumps and `getChromeTarget`/`enumerateTabs` embed the `file://` home path — scrub; `readDom` returns a JSON envelope with escaped HTML; `document.activeElement.id` reads `""` for BODY — read `id || tagName`; `captureScreenshot(C)` never composites the guest; `readAxTree` omits iframe inner documents; the `onTabBoundary` collector (`evaluate(C)` installing `window.__tb`) separates "delivered" from "acted on"; boundary handoff renders selection-only while Ctrl+L renders a yellow ring + selection; CDP-dispatched keys/text land on the named wcId regardless of OS focus — the cross-view *sequence* plus rendered state is the evidence.
- Teardown: `[CLOSING]` sent to the Executor immediately after its row-12 report, then the Validator; app and fixture server stopped; evidence dir scrubbed of home paths (152 files); the minted key file deleted. The app was left with nine tabs and the `window.__tb` collector installed until shutdown.

## Closing Summaries

### Executor closing
Apparatus worked end to end through the attach-only wrapper; no session MCP tool used; no `evaluate` against the internal guest. Seven facts for the crew file (persistent `const` in `evaluate`; home path in chrome a11y dumps and tab enumeration; `readDom` JSON envelope; Settings 'Recent actions' re-render on every admin call — pagination 7 → 12 during row 11; `activeElement.id` empty for BODY; `captureScreenshot(C)` excludes the guest; `window.goldfinch.onTabBoundary` as a chrome-side probe). Dispatch observations: after every F6 the guest's `activeElement` was `BODY` on first entry (rows 3, 10, 12) and the last-held element on re-entry (rows 7, 8, 9); forward boundary handoffs from `#b1` and the empty page reached `#address` with a `forward` event and a full-value selection; the backward boundary delivered a `backward` event but the chrome stayed on `BODY`; Ctrl+L worked; 40 Tabs on Settings produced no event; the iframe's inner document never appears in `readAxTree(F)`. Housekeeping: nine tabs left open, `window.__tb` installed on C; nothing written into the project tree.

### Validator closing
8 pass / 4 fail (rows 3, 7, 8, 11); row 12 passed as documentation only. Three distinct causes: (1) spec premise — rows 3, 7 and half of 11: F6 works and is repeatable but lands on the document (fresh page) or the last-focused element (re-entry), never the first tabbable; the leg must pick a contract (guest focuses its first tabbable on `focus-content`, or the rows are re-authored) rather than straddle. (2) Product defect — row 8: delivery proven end to end by diag-8; the chrome's backward branch focuses nothing; Shift+Tab on the first control is a dead key. (3) Apparatus confound — row 11 / diag-11: the Settings audit log re-renders on every admin call and drops focus; the internal listener's liveness is open; re-author row 11 to walk backward from a control above the log to the first tabbable and assert a `backward` event on the collector, sampling a11y every press near the boundary. Documented: forward Tab past a trailing same-origin iframe wraps to `#top` with no exit — a keyboard-trap shape the leg's Edge Cases must decide. Observability limits: the `onTabBoundary` collector is the most useful instrument in this spec — install it in setup; IIFE-wrap `evaluate` setups; `readAxTree` omits iframe inner documents (`contentDocument.activeElement`, same-origin only); scrub chrome a11y dumps; selection-only vs yellow-ring is the handoff-vs-Ctrl+L fingerprint; CDP keys land on the named wcId regardless of OS focus.

## Operator Notes

Leg 1 (`focus-entry-spike`) is a spike: this run is its evidence table. Outcome for the flight: DD2/DD3 (boundary signal, mid-page Tab returned to Chromium) **confirmed** for the forward direction end to end; DD4's backward placement **defective** in the chrome handler; DD1's landing element is a **contract decision** now put to the operator (explicit first-tabbable focus on entry vs Chrome-like document focus + Tab); DD5's internal bundle is wired (present, referenced, contains the listener) but its live boundary signal is **unproven** on this run because of the Settings-page confound — the re-run uses a quiet internal page. New Edge Case for Leg 2: trailing-iframe keyboard trap. AC6 is **not met**; the leg does not land on this run — fix, re-run.
