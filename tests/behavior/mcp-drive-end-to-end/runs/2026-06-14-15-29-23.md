# Behavior Test Run: mcp-drive-end-to-end — 2026-06-14-15-29-23

**Spec**: [tests/behavior/mcp-drive-end-to-end.md](../../mcp-drive-end-to-end.md)
**Status**: pass
**Started**: 2026-06-14T15:29:23Z
**Completed**: 2026-06-14T15:40:00Z
**Duration**: ~00:10:00
**Executor**: a3fa8d62a2155acd6
**Validator**: ac7e50ef8dc788336
**Evidence (ephemeral, local-only, not committed)**: lived at `/tmp/behavior-tests/goldfinch/mcp-drive-end-to-end/2026-06-14-15-29-23/` during the run; re-derivable by re-running the spec.

## Summary

9 / 9 checkpoints passed. 0 failed; 0 inconclusive. SC6 is behavior-test-backed: a real MCP client over the loopback transport discovered all 16 tools and drove the running browser end to end — tab lifecycle, navigation, DOM/screenshot/a11y reads, and trusted synthetic input — with rendered-state (pixel + a11y) confirmation and an independent whole-window cross-check. Run performed against the multi-session transport fix (commit 69bae77); the Executor made several separate MCP connections against the same persistent tabs, which the pre-fix single-session server could not have served.

## Checkpoint Results

### Checkpoint 1 — PASS
- **Raw state**: `tools/list` = exactly the 16 named tools (12 drive + 4 observe); fixture HTTP 200.
- **Expected**: initialize ok; 16 named tools; fixture 200.
- **Verdict**: pass — Validator verified the 16 names one-by-one against the spec (exact set, no extras/omissions).
- **Evidence**: `steps-1-6.json`

### Checkpoint 2 — PASS
- **Raw state**: `openTab("https://example.com")` → `'3'` (numeric wcId A=3), isError false.
- **Expected**: numeric wcId, not null, not isError.
- **Verdict**: pass.
- **Evidence**: `steps-1-6.json`

### Checkpoint 3 — PASS
- **Raw state**: `enumerateTabs` entry `{wcId:3, url:'https://example.com/', title:'Example Domain', jarId:'default', active:true}`.
- **Expected**: array entry wcId===A with example.com url/title and the `{wcId,url,title,jarId,active}` shape.
- **Verdict**: pass — all five shape keys present.
- **Evidence**: `steps-1-6.json`

### Checkpoint 4 — PASS
- **Raw state**: `navigate` → `{"ok":true}`; `readDom` → url `https://example.com/`, title `Example Domain`, html contains `<title>Example Domain</title>`.
- **Expected**: navigate `{"ok":true}` no isError; readDom url/title/html match (cross-tool confirmation).
- **Verdict**: pass — independent readDom confirms the navigate landed.
- **Evidence**: `steps-1-6.json`

### Checkpoint 5 — PASS
- **Raw state**: image/png, 19171 bytes, 1398x810; pixels show example.com.
- **Expected**: image content (not JSON-wrapped, not isError) AND the page **visibly rendered** in the pixels (not blank).
- **Verdict**: pass — Validator READ `step-5-example-screenshot.png`: legible "Example Domain" heading + paragraph + "Learn more" link; not a blank frame.
- **Evidence**: `step-5-example-screenshot.png`

### Checkpoint 6 — PASS
- **Raw state**: `readAxTree` → JSON array length 14, not the refusal object, not isError.
- **Expected**: a JSON array (success shape), not the `debugger-unavailable` refusal, not isError.
- **Verdict**: pass — genuine AXNode array (the in-process debugger a11y read works over the transport on a real guest).
- **Evidence**: `steps-1-6.json`

### Checkpoint 7 — PASS
- **Actions taken**: openTab fixture (F=4); readDom confirms load; `typeText(F,"mcp-typed-hello")`; captureScreenshot (before); located CLICK ME button (~398,410) and `click(F,398,410)`; `pressKey(F, name:"Enter")`; captureScreenshot (after); readAxTree.
- **Raw state**: after-render shows all three echoes; a11y tree (48 nodes) contains all three strings.
- **Expected**: the AFTER render shows `ECHO: mcp-typed-hello`, `CLICK: button was clicked`, `KEY: Enter pressed` — none of which the initial render showed.
- **Verdict**: pass — Validator READ `step-7-after.png` (all three echoes visible in pixels: blue ECHO, orange CLICK, green KEY) and `step-7-axtree.json` (StaticText nodes contain all three strings; textbox value `mcp-typed-hello`); `step-7-before.png` shows `CLICK: not yet clicked` / `KEY: no key yet`, so the click + key echoes demonstrably flipped. Proves `click`/`typeText`/`pressKey` drove the real page.
- **Evidence**: `step-7-before.png`, `step-7-after.png`, `step-7-axtree.json`
- **Finding (ergonomics, not a defect)**: the Executor's first `pressKey` used arg `key:'Enter'` → error `automation: unknown key undefined`; the tool's input schema requires the key under `name` (`{name:'Enter'}`). Re-issuing with `name` succeeded. A consumer may intuitively reach for `key`; worth a HAT/Flight-9 note (clarify the schema/description or accept `key` as an alias).

### Checkpoint 8 — PASS
- **Raw state**: openTab example.org (B=5, active); `activateTab(3)` → `true`; enumerate shows 3 active, others not; `closeTab(5)` → `true`; re-enumerate wcIds `[2,3,4]` (5 gone).
- **Expected**: activateTab→boolean true; A active flag flips; closeTab→true; B no longer listed.
- **Verdict**: pass — all four sub-assertions satisfied (boolean signal preserved, not normalized to `{ok:true}`).
- **Evidence**: `steps-8-9.json`

### Checkpoint 9 — PASS (independent cross-check)
- **Raw state**: `captureWindow` → image/png, 32521 bytes; whole window shows 3 tabs (Google, active Example Domain @ `https://example.com/`, fixture tab); example.org absent.
- **Expected**: `[mixed-frame]` image content; whole-window pixels reflect the MCP-driven state (foregrounded tab + tab strip matching opens/closes).
- **Verdict**: pass — Validator READ `step-9-window.png`: tab strip count (3) and active tab (Example Domain) match the structured state; the closed example.org tab is absent. Independent (non-self-referential) confirmation the MCP-driven actions truly changed the browser. Completes all-16-tool coverage.
- **Evidence**: `step-9-window.png`

## Orchestrator Notes

- **Mode**: re-spawn (SendMessage unavailable). A SINGLE Executor drove all 9 steps (browser/tab state must persist across the sequence) + a SINGLE independent Validator judged all 9, reading the evidence PNGs itself for the rendered-state checkpoints (5, 7, 9). Witnessed separation preserved (actor ≠ judge, both independent of the orchestrator). Cache: cold (fresh app launch + fresh MCP connections).
- **Run venue**: performed AFTER the multi-session transport fix (69bae77). The Executor made multiple distinct MCP connections against the same persistent tabs — a workload the pre-fix single-session server could not serve; this run is itself secondary evidence the fix holds end to end.
- **Finding carried forward** (ergonomics, not a defect): `pressKey` arg name is `name` not `key` — surface in the HAT / Flight 9.

## Closing Summaries

### Executor closing
All 9 steps executed against the running app over MCP at 127.0.0.1:7777. wcIds persisted across connections (A=3, F=4, B=5). Every capability worked: 16 tools exact, open/enumerate/navigate/readDom/screenshot/readAxTree, trusted input (typeText+click+pressKey all visibly echoed in pixels and a11y), tab activate/close, captureWindow. One apparatus-side arg-name correction on pressKey (`key`→`name`). Fixture server killed; temp driver scripts removed; evidence retained.

### Validator closing
All nine steps pass with rendered-state evidence pixel-verified (steps 5, 7, 9) and corroborated by the a11y tree + structured tool results. The MCP tools genuinely drove tabs, navigation, input, and window state end to end — not self-report. pressKey arg-name correction did not affect the final state (the after-render confirms Enter registered).

## Operator Notes

SC6 acceptance met live. The run doubled as end-to-end confirmation of the multi-session transport fix.
