node_modules
dist/
out/
*.log
.DS_Store

# Playwright MCP test artifacts (screenshots, snapshots, console logs)
.playwright-mcp/
goldfinch-*.png

# Machine-local Claude Code settings (permissions/prefs)
.claude/settings.local.json

# Local env / secrets (e.g. dev automation Bearer key — never commit)
.env
.env.*
!.env.example

# Ephemeral test-run captures
test-out.txt
test-time.txt

# Generated preload bundles (flight/02 leg 1; internal bundle added M17 F1 L1,
# DD5) — regenerated at every launch/test/build entry, never committed
# (source↔bundle drift is structurally impossible when it's always freshly
# built).
src/preload/webview-preload.bundle.js
src/preload/internal-preload.bundle.js

# Throwaway TLS fixture certs/keys (web-compat client-cert leg) — generated
# locally by tests/behavior/fixtures/web-compat/gen-certs.mjs, never committed
# (no-committed-baselines rule; short-lived throwaway CA).
tests/behavior/fixtures/web-compat/certs/
